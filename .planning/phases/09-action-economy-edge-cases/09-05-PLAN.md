---
phase: 09-action-economy-edge-cases
plan: "05"
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - packages/g2-app/src/__tests__/09-integration-smoke.test.ts
  - packages/foundry-module/src/__tests__/09-integration-smoke.test.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md
autonomous: true
requirements:
  - COMB-02
must_haves:
  truths:
    - "ISM-W9-01..10 integration smoke covers the full Phase 9 flow end-to-end with REAL panels, dispatchers, and handlers (mock bridge + mock ws + mock socketlib only)."
    - "14-socketlib-handler invariant confirmed by grep gate inside ISM-W9-10."
    - "Phase 9 STATE.md is updated to PHASE_9_CLOSED with running hardware-pending SC total = 29."
    - "ROADMAP.md Phase 9 checkbox is flipped from [ ] to [x] with closure annotation."
    - "09-VERIFICATION.md is published documenting REQ COMB-02 software-closure + 3 hardware-pending SCs."
  artifacts:
    - path: "packages/g2-app/src/__tests__/09-integration-smoke.test.ts"
      provides: "ISM-W9-01..10 g2-app-side end-to-end smoke (real panels + mock ws)"
    - path: "packages/foundry-module/src/__tests__/09-integration-smoke.test.ts"
      provides: "Foundry-side end-to-end smoke (cast-spell concentration trigger + combat-action-tracker emission)"
    - path: ".planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md"
      provides: "Goal-backward closure audit per Phase 8 precedent"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "Atomic INV-3 commit updating both files (phase closure)"
      pattern: "PHASE_9_CLOSED"
---

<objective>
Close Phase 9 with an integration smoke harness covering the full action-economy + concentration + slot-picker flow end-to-end, then update STATE.md + ROADMAP.md + publish 09-VERIFICATION.md.

Purpose: COMB-02 success criteria #1..5 — verified holistically through integration tests using REAL implementation code (real dispatchers, real panels, real handlers); only the bridge + ws are mocked. This mirrors the Phase 8 ISM-W8-01..10 precedent.

Output: Two integration smoke files (g2-app side + foundry-module side), updated planning artifacts, and a single atomic INV-3 commit binding STATE + ROADMAP + SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-action-economy-edge-cases/09-CONTEXT.md
@.planning/phases/09-action-economy-edge-cases/09-01-PLAN.md
@.planning/phases/09-action-economy-edge-cases/09-02-PLAN.md
@.planning/phases/09-action-economy-edge-cases/09-03-PLAN.md
@.planning/phases/09-action-economy-edge-cases/09-04-PLAN.md
@.planning/phases/08-manual-action-ux/08-VERIFICATION.md
@.planning/phases/08-manual-action-ux/08-05-SUMMARY.md
@.planning/phases/07-foundry-module-write-path/07-06-SUMMARY.md
@packages/g2-app/src/__tests__/08-integration-smoke.test.ts
@packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts

<interfaces>
<!-- Integration smoke patterns to mirror -->

From packages/g2-app/src/__tests__/08-integration-smoke.test.ts (pattern reference):
```ts
// Real LayerManager + ToastQueueLayer + StatusHudLayer + ActionOptionsModal + dispatchers
// Mock EvenAppBridge (vi.fn spies)
// EventEmitter-backed MockSocket with `fireMessage` helper + send-spy
// Shared PanelGestureBus
// StubCaptureLayer at ZIndex.Z0_MAP (required by LayerManager capture invariant)
// 10 test IDs ISM-W8-01..10 covering: full layer set, modal mount/unmount, toast survival,
//   cross-player drop, EnvelopeSchema round-trip, T-08-02 silent drop, double-tap fire,
//   etc.
```

From packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts (pattern reference):
```ts
// Mock game.actors / game.users / ChatMessage.create
// Mock socketlib (vi.stubGlobal) + crypto.subtle via webcrypto
// Real dispatchTool + handlers + audit-log
// 10 test IDs ISM-W7-01..10 covering each handler round-trip plus IDEMPOTENCY-01..02
```

Phase 9 ISM-W9 scenarios to cover:
- ISM-W9-01: full Phase 9 layer set mounts cleanly (StubCaptureLayer + IdleInfill + StatusHud + ToastQueue + economy dispatcher attached + retry cache cleared).
- ISM-W9-02: synthetic `r1.action.economy` envelope → state cache updated AND widget row appears in next StatusHudRenderer output.
- ISM-W9-03: ActionOptionsModal tap when state shows `actionsUsed:1, multiAttackInProgress:false` → error toast enqueued; `ws.send` NOT called for tool.invoke.
- ISM-W9-04: ActionOptionsModal tap when state shows `multiAttackInProgress:true` → BYPASS preconditioner; tool.invoke envelope emitted.
- ISM-W9-05: cast-spell handler with concentration spell + active concentration ActiveEffect → returns `concentration-required` error AND emits `conc.conflict` envelope via mocked bridgeDeltaEmitter.
- ISM-W9-06: g2-app side end-to-end: synthetic conc.conflict → modal mounts → Y tap → drop-concentration tool.invoke + legacy conc.drop.confirmed + retry envelope (3 ws.send calls in order).
- ISM-W9-07: N cancel → 0 retry, error toast enqueued with `error.action.concentration-cancelled`.
- ISM-W9-08: SlotPickerPanel scroll cycles selection; tap emits cast-spell with `slot_level: <selected>`.
- ISM-W9-09: SlotPickerPanel skip path: when boot caller decides `availableSlots.length === 1`, ActionOptionsModal tap emits with `slot_level: <single available>` directly (no SlotPicker mount).
- ISM-W9-10: 14-socketlib-handler grep gate confirmed; full ws message round-trip preserves canonical EnvelopeSchema shape across all Phase 9 envelopes (action.economy, conc.conflict, conc.drop.confirmed, tool.invoke with slot_level).

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: g2-app 09-integration-smoke.test.ts (ISM-W9 g2-app side)</name>
  <files>packages/g2-app/src/__tests__/09-integration-smoke.test.ts</files>
  <behavior>
    - Test ISM-W9-01: makeSmokeSuite (StubCaptureLayer at z=0 + IdleInfill at z=0.5 + StatusHudLayer at z=1 + ToastQueueLayer at z=1.5) bundles cleanly; capture invariant intact; exactly one `bridge.rebuildPageContainer` call per bundle.
    - Test ISM-W9-02: attach `attachActionEconomyHandler(ws, 'user-test')`; fire envelope `{type:'r1.action.economy', payload:{actorId:'a1', actionsUsed:1, bonusActionsUsed:0, reactionsUsed:0, multiAttackInProgress:false, recipientUserId:'user-test'}}`. Assert `getActionEconomyState('a1')` returns the parsed payload AND StatusHudRenderer's `_getActionEconomyForTest()` is non-null after a subsequent character.delta fires (status-hud-layer re-renders).
    - Test ISM-W9-03: with state from ISM-W9-02 (actionsUsed:1), construct ActionOptionsModal({kind:'spell', actorId:'a1', requiresTarget:false, requiresSlotPicker:false, defaultSlotLevel:1, ...}) → fire tap → `ws.send` NOT called with `tool.invoke`; `toastQueue.enqueue` called once with message starting with `❌` and containing the IT `Azione già usata` text.
    - Test ISM-W9-04: state with `multiAttackInProgress:true, actionsUsed:1` → tap → `ws.send` called ONCE with `tool.invoke`; toast NOT enqueued (preconditioner bypassed).
    - Test ISM-W9-05: synthesise WS message `{type:'conc.conflict', session_id:'sess-1', payload:{effectId:'eff-1', currentConcentrationName:'Bless', newSpellName:'Hold Person', actorId:'a1'}}`. Conc-conflict-dispatcher mounts ConcentrationDropModalPanel at z=2. `layerManager.getLayer(Z2_OVERLAY).id === 'conc-drop-modal'`.
    - Test ISM-W9-06: PRE-cache an envelope via `cacheRetryEnvelope('idem-conf', <synthetic-cast-envelope>, 'unconfirmed')` THEN `markRetryConfirmed('idem-conf')`. Mount conc modal (as in ISM-W9-05). Fire `[Y]` tap. Assert:
      - `ws.send` called 3 times in order: (1) tool.invoke for drop-concentration, (2) conc.drop.confirmed legacy envelope, (3) the cached retry envelope.
      - Each envelope passes `EnvelopeSchema.safeParse` round-trip.
    - Test ISM-W9-07: Mount conc modal. Fire `[N]` double-tap. Assert: `ws.send` NOT called for retry; `toastQueue.enqueue` called once with message containing `concentration-cancelled` localisation; `consumeLatestConfirmed()` returns null afterwards.
    - Test ISM-W9-08: Construct SlotPickerPanel with availableSlots `[{level:3,value:2,max:4}, {level:4,value:3,max:3}]`, default selection 0. Fire scroll → selection 1. Fire tap → `ws.send` called once with envelope whose `payload.args.slot_level === 4`.
    - Test ISM-W9-09: Construct ActionOptionsModal with `requiresSlotPicker:false, defaultSlotLevel:3, kind:'spell'`. Fire tap → `ws.send` called once with `payload.args.slot_level === 3` (no SlotPicker mount path).
    - Test ISM-W9-10: 14-socketlib-handler grep gate — bash one-liner via `execSync` confirms `grep -c "registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` returns the expected number (use the same approach as ISM-W8-10 if it predates this; otherwise inline the assertion in the test description).
  </behavior>
  <action>
    Create `packages/g2-app/src/__tests__/09-integration-smoke.test.ts` based on the `08-integration-smoke.test.ts` harness:
    - Reuse `makeSmokeSuite` pattern (StubCaptureLayer + idleInfill + statusHud + toastQueue).
    - Reuse `MockSocket` with `fireMessage(envelope)` helper.
    - Reuse `crypto.randomUUID` stub + (for Phase 7-style assertions) webcrypto.subtle injection where needed.
    - Import REAL `attachActionEconomyHandler`, `cacheRetryEnvelope`, `markRetryConfirmed`, `consumeLatestConfirmed`, `clearRetryCache`, `clearActionEconomyState`.
    - `beforeEach`: clear both caches (action-economy-state + conc-retry-cache).
    - 10 tests as enumerated above.

    For ISM-W9-10: copy the grep-gate pattern from ISM-W8-10 (file-content read + line count via `String.split('\n').filter(...)`); avoid `execSync` to keep the test pure Node + happy-dom without shell access.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run 09-integration-smoke.test.ts</automated>
  </verify>
  <done>ISM-W9-01..10 green; harness reuses Phase 8 smoke pattern; no flakiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: foundry-module 09-integration-smoke.test.ts (concentration-detector + combat-action-tracker)</name>
  <files>packages/foundry-module/src/__tests__/09-integration-smoke.test.ts</files>
  <behavior>
    - Test FM-ISM-W9-01: cast-spell handler with `args.slot_level: 4` for a non-concentration spell → `activity.use` called with `{ configure: false, spell: { slot: 'spell4' } }`. No conc.conflict emitted.
    - Test FM-ISM-W9-02: cast-spell handler with `args.slot_level: 0` (cantrip) → `activity.use` called with `{ configure: false }` (no `spell.slot` key). No conc.conflict.
    - Test FM-ISM-W9-03: cast-spell handler with concentration spell + actor with active concentrating ActiveEffect → returns `{success:false, error:'concentration-required'}`; `bridgeDeltaEmitter` called with `(CONC_CONFLICT_TYPE, {...})`; `activity.use` NOT called.
    - Test FM-ISM-W9-04: registerCombatActionTracker — `createChatMessage` hook with audit `{toolId:'cast-spell', actorId:'a1'}` triggers emit with `actionsUsed:1`.
    - Test FM-ISM-W9-05: `createChatMessage` with audit `{toolId:'weapon-attack', actorId:'a1', attackId:'atk-1'}` increments actionsUsed once + sets multiAttackInProgress:true; second message with SAME attackId is a no-op for actionsUsed.
    - Test FM-ISM-W9-06: `updateCombat` with `change.turn:2` resets state — emit fires with `actionsUsed:0, multiAttackInProgress:false`.
    - Test FM-ISM-W9-07: `updateCombat` with neither `turn` nor `round` (e.g., a `combatant._id` update) → no emit.
    - Test FM-ISM-W9-08: action-result-watcher maps `result.error === 'concentration-required'` → `errorKind: 'concentration-required'` in emitted ActionResultPayload.
    - Test FM-ISM-W9-09: 14-socketlib-handler invariant — same grep-gate assertion as ISM-W9-10 (file-content read).
    - Test FM-ISM-W9-10: audit-log entry includes `attackId` when handler result.data carries one (regression guard for Plan 09-01 Task 1 extension).
  </behavior>
  <action>
    Create `packages/foundry-module/src/__tests__/09-integration-smoke.test.ts` based on `07-write-path-integration-smoke.test.ts` harness:
    - Mock `game.actors`, `game.users`, `ChatMessage.create`, `Hooks.on/off`, `socketlib.executeAsGM`.
    - Inject mock `bridgeDeltaEmitter` spy.
    - Use REAL `castSpellHandler`, `detectActiveConcentration`, `registerCombatActionTracker`, `registerActionResultWatcher`, `writeAuditLog`.
    - For ISM-W9-05 multi-attack: stage TWO synthetic chat messages with the same `attackId`; assert the tracker's emit count + state.
    - 10 tests per the behavior table.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run 09-integration-smoke.test.ts</automated>
  </verify>
  <done>FM-ISM-W9-01..10 green; full workspace `pnpm test` exit 0 with no regressions; tests pass on both g2-app and foundry-module integration smokes simultaneously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase 9 closure — STATE.md + ROADMAP.md + 09-VERIFICATION.md (atomic INV-3 commit)</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md</files>
  <behavior>
    - Test V-01: `.planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md` exists with frontmatter `phase: 9`, `slug: action-economy-edge-cases`, `status: human_needed`, `must_haves_total: 5`, `must_haves_passed: 5`, `must_haves_failed: 0`, `hardware_pending: 3`.
    - Test V-02: 09-VERIFICATION.md §"Success Criteria — Goal-Backward Check" lists all 5 ROADMAP §Phase 9 success criteria as ✅ PASS (software-side); SC #1 / #2 / #4 declare hardware-pending SC-09-01..03 in the Hardware column.
    - Test V-03: 09-VERIFICATION.md §"REQ-ID Coverage Matrix" lists COMB-02 as closed with the 5 Phase 9 plan IDs and the SC-09-01..03 carries.
    - Test V-04: STATE.md `status:` field updated to `PHASE_9_CLOSED — 5/5 plans committed; ActionEconomyPayloadSchema + combat-action-tracker + action-economy-dispatcher + Action Economy Widget + client preconditioner + concentration drop end-to-end + SlotPickerPanel + 09-ISM-W9-01..10 g2-app + FM-ISM-W9-01..10 foundry-module integration smoke. 14-socketlib-handler invariant upheld.`
    - Test V-05: STATE.md `completed_plans` and `percent` advanced (e.g., 50 → 55, 62 → 68).
    - Test V-06: STATE.md gains a `## /gsd-autonomous 2026-05-16 run — Phase 9 closure` section mirroring the Phase 7 closure block (commits per plan + test totals + invariants + REQ-ID coverage + hardware-pending SC-09-01..03 + running total 29).
    - Test V-07: ROADMAP.md Phase 9 line `- [ ] **Phase 9: Action Economy & Edge Cases** ...` flipped to `- [x]` with closure annotation `(5/5 plans complete 2026-05-16; SC-09-01..03 deferred to ADR-0005 Branch A human_needed; 14-socketlib-handler invariant upheld; running hardware-pending: 29)`.
    - Test V-08: ROADMAP.md §"Phases 9" detail block gains a Plans bullet list (5 entries with `[x]` boxes and short captions).
    - Test V-09: ROADMAP.md §"Progress" table row for Phase 9 changed from `0/TBD | Not started` to `5/5 | Complete | 2026-05-16`.
  </behavior>
  <action>
    Create `.planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md` modeled on `08-VERIFICATION.md`:
    - Frontmatter: phase 9, slug, status human_needed, must_haves totals, hardware_pending: 3, test_count (extracted from `pnpm test` output post-Plan 09-05), test_files count, test_delta from Phase 8 baseline (1858), commits count.
    - §Verdict block — SOFTWARE-SIDE: CLOSED; HARDWARE-SIDE: human_needed.
    - §Success Criteria — table with 5 ROADMAP success criteria, each ✅ PASS in Software column, SC-09-01..03 references in Hardware column, evidence cells citing each plan's tests (CAT-* / SHR-EW-* / AOM-PRE-* / CS-CONC-* / SPP-* / ISM-W9-*).
    - §REQ-ID Coverage Matrix — single row COMB-02 closed by Plans 09-01..05.
    - §Hardware-Deferred Stress Cases:
      - SC-09-01: Action Economy Widget renders character-perfect on real G2 (resolved via `pnpm --filter @evf/validation-harness validate:all`).
      - SC-09-02: Concentration drop end-to-end on real Foundry world + dnd5e 5.3.3.
      - SC-09-03: SlotPickerPanel scroll-cycle feels right on real R1.
    - Running total: 26 (Phase 8) + 3 (Phase 9) = 29.
    - §Quality Gates table: typecheck/lint/test exit 0; INV-1 fixtures (4 econ + 4 slot picker); 14-socketlib-handler invariant.
    - §Plan Closure Signal — 5 rows mirroring Phase 8 format.
    - §Invariant Confirmations — INV-1/INV-4/INV-5/14-socketlib-handler/T-09-01..05 mitigations.

    Update `.planning/STATE.md`:
    - `status:` field → PHASE_9_CLOSED text per V-04.
    - `stopped_at:` → "Completed 09-05-PLAN.md".
    - `last_updated:` → ISO timestamp.
    - `last_activity:` → Phase 9 closure narrative.
    - `progress.completed_plans` increment by 5; `progress.percent` recomputed.
    - Add `## /gsd-autonomous 2026-05-16 run — Phase 9 closure` section per V-06.
    - Append hardware-pending SC-09-01..03 to the running carry list (running total: 29).

    Update `.planning/ROADMAP.md`:
    - Flip Phase 9 checkbox `[ ]` → `[x]` per V-07.
    - Add the 5 plan entries with `[x]` boxes per V-08:
      ```
      Plans:
      - [x] 09-01-PLAN.md (wave 0) — ActionEconomyPayloadSchema + combat-action-tracker + action-economy-dispatcher + audit-log attackId
      - [x] 09-02-PLAN.md (wave 1) — Action Economy Widget + client preconditioner + i18n + 4 INV-1 fixtures
      - [x] 09-03-PLAN.md (wave 2) — concentration-detector + cast-spell extension + conc-retry-cache + single-attempt retry flow
      - [x] 09-04-PLAN.md (wave 3) — SlotPickerPanel + cast-spell slot_level forwarding + 4 INV-1 fixtures
      - [x] 09-05-PLAN.md (wave 4) — ISM-W9 g2-app + FM-ISM-W9 foundry-module integration smoke + Phase 9 closure
      ```
    - Update §Progress table row for Phase 9 per V-09.

    Commit the three changes as a SINGLE atomic INV-3 commit (`docs(09): PHASE_9_CLOSED — STATE + ROADMAP + VERIFICATION`).
  </action>
  <verify>
    <automated>test -f .planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md &amp;&amp; grep -q "PHASE_9_CLOSED" .planning/STATE.md &amp;&amp; grep -q "^- \[x\] \*\*Phase 9:" .planning/ROADMAP.md &amp;&amp; pnpm test</automated>
  </verify>
  <done>V-01..09 satisfied; single atomic commit lands STATE + ROADMAP + VERIFICATION; full workspace `pnpm test` exit 0; running hardware-pending SC total = 29.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Integration smoke harness → production code | Real implementations of dispatchers + panels + handlers; only bridge + ws + socketlib + game.actors are mocked. |
| Planning artifacts → git history | Atomic INV-3 commit binding STATE + ROADMAP + VERIFICATION in a single SHA (drift prevention). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-CLOSURE-01 | Tampering | ROADMAP/STATE drift across commits (INV-3 violation) | mitigate | Task 3 commits all three files in ONE git commit; `git log -1 --name-only HEAD` shows STATE.md + ROADMAP.md + 09-VERIFICATION.md atomically. Phase 7 / Phase 8 closure precedent. |
| T-09-CLOSURE-02 | Repudiation | Hardware-pending SCs forgotten | mitigate | 09-VERIFICATION.md explicitly carries SC-09-01..03 to ADR-0005 Branch A; STATE.md hardware-pending running total updated to 29. |
| T-09-CLOSURE-03 | Denial of service | Integration smoke flakiness | mitigate | Reuse Phase 8 ISM-W8 harness pattern (proven stable across 1858 tests). Avoid timer-dependent assertions where possible; use `vi.useFakeTimers()` + explicit advance when needed. |
</threat_model>

<verification>
- `pnpm test` workspace-wide exit 0; total tests ≈ 1858 + ~110 (plans 09-01..05 combined deltas) ≈ 1968.
- `grep -c registerComplexHandler packages/foundry-module/src/pair/socketlib-handlers.ts` returns 14 (final invariant gate).
- `git log -1 --name-only HEAD` shows STATE.md + ROADMAP.md + 09-VERIFICATION.md + 09-05-SUMMARY.md in a single commit (INV-3 atomic).
- Phase 9 ROADMAP §Progress table reads `5/5 | Complete | 2026-05-16`.
- `pnpm lint:ci` + `pnpm typecheck` exit 0.
</verification>

<success_criteria>
- ISM-W9-01..10 (g2-app) + FM-ISM-W9-01..10 (foundry-module) green.
- 09-VERIFICATION.md, STATE.md, ROADMAP.md updated atomically.
- COMB-02 marked Complete in REQUIREMENTS.md status column (carry-forward to Phase 10).
- Running hardware-pending total = 29 (26 prior + 3 Phase 9).
</success_criteria>

<output>
After completion, create `.planning/phases/09-action-economy-edge-cases/09-05-SUMMARY.md` declaring PHASE_9_CLOSED + commit SHAs + test count delta + the 3 hardware-pending SCs. Confirm INV-3 atomic commit signature in the §Self-Check block.
</output>
