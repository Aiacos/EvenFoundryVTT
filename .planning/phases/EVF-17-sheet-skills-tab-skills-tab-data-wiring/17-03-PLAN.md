---
phase: 17-sheet-skills-tab-skills-tab-data-wiring
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - packages/g2-app/src/panels/character-sheet-tab-renderers.ts
  - packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts
  - packages/shared-render/src/fixtures/sheet.main.2014.it.txt
  - packages/shared-render/src/fixtures/sheet.main.2024.it.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.en.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.de.txt
  - packages/shared-render/src/fixtures/sheet.skills.it.txt
  - packages/shared-render/src/fixtures/sheet.skills.en.txt
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-VERIFICATION.md
autonomous: true
requirements:
  - SHEET-10
tags:
  - renderer
  - g2-app
  - inv-1-fixture-update
  - inv-3-atomic-ratification

must_haves:
  truths:
    - "Player Sheet → Skills tab shows 18 real skill modifiers sourced from `snapshot.skills.<k>.total` formatted `+N`/`-N`; the hardcoded `DEFAULT_SKILLS` array is gone (replaced by dynamic snapshot lookup driven by SKILL_KEYS iteration + SKILL_NAMES static i18n map)"
    - "Proficiency glyph is data-driven from `snapshot.skills.<k>.proficient`: 0 → ○, 1 → ◉, 2 → ★; 0.5 → ◉ (half-prof rounds up per UI-SPEC §3)"
    - "Player Sheet → Main tab senses line surfaces passive Perception / Insight / Investigation from `snapshot.skills.{prc,ins,inv}.passive` — replaces the remaining `—` placeholder on row 16 (IT 'Sensi'); width budget preserved"
    - "INV-1 width invariant preserved: every row exactly 66 code-points after the hardcoded → dynamic swap"
    - "Existing `sheet.skills.it.txt` round-trips byte-identically post-swap (Thorin canonical values in snapshot match the DEFAULT_SKILLS spread the fixture was built from)"
    - "NEW `sheet.skills.en.txt` fixture added for EN locale parity with Main tab fixture set (CSTR-FIX-SKILLS-EN round-trip)"
    - "All 4 `sheet.main.*` fixtures row 16 (Sensi line) byte-updated with passive content per UI-SPEC §4"
    - "17+ downstream snapshot literals across g2-app + bridge + foundry-mcp test files extended with `skills` field (Phase 16 precedent: 11 files needed; Phase 17 expects a similar surface)"
    - "Phase 17 closes with a single INV-3 atomic ratification commit (STATE.md + ROADMAP.md + REQUIREMENTS.md + 17-VERIFICATION.md) per Phase 14 3a0c5cf + Phase 15 dc161d6 + Phase 16 d68d7f2 precedent"
    - "CI Gate 8 socketlib handler count = 17 preserved (grep-asserted)"
  artifacts:
    - path: "packages/g2-app/src/panels/character-sheet-tab-renderers.ts"
      provides: "SKILL_NAMES static i18n map + renderSkillsTab dynamic lookup + renderMainTab senses line data binding"
      contains: "const SKILL_NAMES"
      contains_2: "snapshot.skills"
    - path: "packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts"
      provides: "CSTR-SKILLS-DATA-1..5 + senses line tests + updated snapshot2014/snapshot2024 with Thorin skills"
      contains: "CSTR-SKILLS-DATA"
      contains_2: "skills:"
    - path: "packages/shared-render/src/fixtures/sheet.skills.it.txt"
      provides: "Existing fixture — byte-identical post-swap (validation only)"
      contains: "Atletica"
    - path: "packages/shared-render/src/fixtures/sheet.skills.en.txt"
      provides: "NEW INV-1 fixture for EN Skills tab"
      contains: "Athletics"
    - path: "packages/shared-render/src/fixtures/sheet.main.2014.it.txt"
      provides: "Row 16 Sensi line byte-updated with PP 11 · PI 11 · IND 14"
      contains: "PP 11"
    - path: ".planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-VERIFICATION.md"
      provides: "5/5 SC + 3/3 REQ verification record + INV-3 commit hash + ADR-0005 Branch A carry-forward note"
      contains: "SHEET-08 ✓"
  key_links:
    - from: "packages/g2-app/src/panels/character-sheet-tab-renderers.ts (renderSkillsTab)"
      to: "snapshot.skills.<k>.{total, ability, proficient, passive}"
      via: "SKILL_KEYS iteration + SKILL_NAMES static lookup"
      pattern: "snapshot\\.skills\\["
    - from: "packages/g2-app/src/panels/character-sheet-tab-renderers.ts (renderMainTab row 16)"
      to: "snapshot.skills.{prc,ins,inv}.passive"
      via: "direct property access for senses line"
      pattern: "skills\\.(prc|ins|inv)\\.passive"
    - from: "packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts"
      to: "Thorin canonical skills fixture"
      via: "loadFixture('sheet.skills.it.txt') + round-trip via renderSkillsTab"
      pattern: "loadFixture\\('sheet\\.skills"
---

<objective>
Wire the consumer half of SHEET-10: replace the 60-LOC hardcoded `DEFAULT_SKILLS` array in `renderSkillsTab` with a dynamic snapshot-driven lookup keyed by `SKILL_KEYS` and a static `SKILL_NAMES` i18n map (extracted from the DEFAULT_SKILLS strings); populate the Main tab senses line (row 16) with `snapshot.skills.{prc,ins,inv}.passive`; round-trip the existing `sheet.skills.it.txt` fixture byte-identically; add a new `sheet.skills.en.txt` fixture; byte-update row 16 of all 4 `sheet.main.*.txt` fixtures with senses content; extend the 17+ downstream snapshot literals atomically; close Phase 17 with a single INV-3 atomic ratification commit per Phase 14/15/16 precedent.

Purpose: This is the visible payoff of the entire phase. Plans 17-01 + 17-02 closed the wire contract and producer; this plan replaces the hardcoded skill array with live data AND closes the workspace-level test gate (the g2-app tests broken by Plan 17-01's REQUIRED schema field land green again because all snapshot literals are extended with real skills here). The Main tab senses line — `Sensi  —` (placeholder shipped since Phase 5) — finally renders real numbers.

Output:
- `SKILL_NAMES: Record<SkillKey, { it: string; en: string; de: string }>` static const map extracted from the existing `DEFAULT_SKILLS` strings (zero net translation work; just structural extraction)
- `renderSkillsTab` rewrites: `DEFAULT_SKILLS` const REMOVED; replaced with `SKILL_KEYS.map(k => buildSkillDef(snapshot.skills[k], k, locale))` pipeline producing the same SkillDef shape
- `renderMainTab` row 16 (senses line) data binding: `Sensi  PP {prc.passive} · PI {ins.passive} · IND {inv.passive}` (IT) / `Senses  PP {prc.passive} · INS {ins.passive} · INV {inv.passive}` (EN) / `Sinne  WN {prc.passive} · EIN {ins.passive} · UNT {inv.passive}` (DE — verify abbreviations via i18n-budgets.ts catalog at execution)
- `snapshot2014` + `snapshot2024` test fixtures extended with Thorin canonical skills spread (matching DEFAULT_SKILLS values for byte-identity round-trip)
- 17+ downstream snapshot literals across g2-app + bridge + foundry-mcp extended with zero-default or contextual `skills` field
- 4 INV-1 main fixtures byte-updated on row 16 (senses content; rows 0-15 + 17 byte-identical)
- NEW `sheet.skills.en.txt` fixture added (EN locale parity with IT)
- Existing `sheet.skills.it.txt` round-trips byte-identically (no edit; validation only — fail-fast if not byte-identical)
- New CSTR-SKILLS-DATA-1..5 tests (dynamic snapshot lookup + ★ glyph + 0.5 → ◉ round-up + ability grouping + senses-line passives)
- Single INV-3 atomic ratification commit (Task 4): STATE.md + ROADMAP.md + REQUIREMENTS.md + 17-VERIFICATION.md
- All 2648+ workspace tests pass (baseline + new CS-SK/CR-SK/CSTR-SKILLS-DATA tests + 17 literal-extension fixups); CI Gate 8 socketlib count = 17 preserved
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-CONTEXT.md
@.planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-UI-SPEC.md
@.planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-01-SUMMARY.md
@.planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-02-SUMMARY.md
@.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-03-PLAN.md
@.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-03-SUMMARY.md
@packages/g2-app/src/panels/character-sheet-tab-renderers.ts
@packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/shared-render/src/fixtures/sheet.skills.it.txt
@packages/shared-render/src/fixtures/sheet.main.2014.it.txt
@packages/shared-render/src/fixtures/sheet.main.2024.it.txt
@packages/shared-render/src/fixtures/sheet.main.2014.en.txt
@packages/shared-render/src/fixtures/sheet.main.2014.de.txt

<interfaces>
<!-- 1) Plan 17-01 wire contract (now landed) -->
```typescript
type Skill = {
  total: number;
  ability: AbilityKey;  // 'str'|'dex'|'con'|'int'|'wis'|'cha'
  proficient: 0 | 0.5 | 1 | 2;
  passive: number;
};
type Skills = Record<SkillKey, Skill>;

snapshot.skills.acr.total        // e.g. +2
snapshot.skills.ath.proficient   // 1 = proficient → ◉
snapshot.skills.prc.passive      // 11 (Thorin) — surfaces on Main tab senses
snapshot.skills.ins.passive      // 11
snapshot.skills.inv.passive      // 14
```

<!-- 2) Existing renderer landmarks to MODIFY: -->

`packages/g2-app/src/panels/character-sheet-tab-renderers.ts`:

- Line ~288 `const dash = '—';` in renderMainTab — used on row 16 senses line. Plan 17-03 REPLACES the senses-line emission with real passive data.

- Line ~383-384 `// Row 16: senses` + `rows.push(\`${sensesLabel}  ${dash}\`);` — this is the line to rewrite.

- Line ~411-416 `const PROF_GLYPHS: Record<ProfLevel, string> = { 0: '○', 1: '◉', 2: '★' }` — KEEP unchanged. The map is correct.

- Line ~427-577 `const DEFAULT_SKILLS: ReadonlyArray<SkillDef> = [ ... ]` — DELETE entirely; the 60-LOC hardcoded array is replaced by SKILL_NAMES + dynamic build.

- Line ~599-670 `function renderSkillsTab` — REWRITE the inner pipeline. The function signature `(snapshot, locale, scrollOffset)` and outer structure (legend row + blank + 14 visible rows + scroll hint + blank) stay; only the source of `skills` changes.

<!-- 3) NEW renderer-side artifacts: -->

`SKILL_NAMES: Record<SkillKey, { it: string; en: string; de: string }>` static const — extracted verbatim from the existing DEFAULT_SKILLS array (no translation work). Defined at module scope near `PROF_GLYPHS`. Schema:

```typescript
const SKILL_NAMES: Record<SkillKey, { it: string; en: string; de: string }> = {
  acr: { it: 'Acrobazia',           en: 'Acrobatics',      de: 'Akrobatik' },
  ani: { it: 'Addestrare animali',  en: 'Animal Handling', de: 'Tierführung' },
  arc: { it: 'Arcano',              en: 'Arcana',          de: 'Arkane Kunde' },
  ath: { it: 'Atletica',            en: 'Athletics',       de: 'Athletik' },
  dec: { it: 'Inganno',             en: 'Deception',       de: 'Täuschung' },
  his: { it: 'Storia',              en: 'History',         de: 'Geschichte' },
  ins: { it: 'Intuizione',          en: 'Insight',         de: 'Einblick' },
  itm: { it: 'Intimidazione',       en: 'Intimidation',    de: 'Einschüchterung' },
  inv: { it: 'Indagare',            en: 'Investigation',   de: 'Nachforschung' },
  med: { it: 'Medicina',            en: 'Medicine',        de: 'Heilkunde' },
  nat: { it: 'Natura',              en: 'Nature',          de: 'Naturkunde' },
  prc: { it: 'Percezione',          en: 'Perception',      de: 'Wahrnehmung' },
  prf: { it: 'Intrattenimento',     en: 'Performance',     de: 'Vorführung' },
  per: { it: 'Persuasione',         en: 'Persuasion',      de: 'Überzeugung' },
  rel: { it: 'Religione',           en: 'Religion',        de: 'Religion' },
  slt: { it: 'Rapidità di mano',    en: 'Sleight of Hand', de: 'Fingerfertigkeit' },
  ste: { it: 'Furtività',           en: 'Stealth',         de: 'Heimlichkeit' },
  sur: { it: 'Sopravvivenza',       en: 'Survival',        de: 'Naturkunde' },  // dnd5e Sopravvivenza/Survival → DE 'Naturkunde' shared with Nature per existing DEFAULT_SKILLS
};
```

NOTE: These strings ARE the existing DEFAULT_SKILLS values, mechanically extracted. No translation invention. The DE collision `nat/sur → 'Naturkunde'` already exists in the shipped fixture and is preserved verbatim (a known dnd5e-DE localization quirk; correcting it is out of scope this phase — DEFER if executor wants to flag).

<!-- 4) Skill row build pipeline replacing DEFAULT_SKILLS: -->

```typescript
// Inside renderSkillsTab, after the null guard and getLabel calls:
const skillEntries: ReadonlyArray<{
  abilityLabel: string;   // i18n key 'sheet.ability.dex' etc.
  name: string;           // localized SKILL_NAMES[k][locale-or-en-fallback]
  profLevel: ProfLevel;   // 0 = untrained, 1 = proficient (half-prof rounds up), 2 = expertise
  modifier: number;       // snapshot.skills[k].total
}> = SKILL_KEYS.map((k) => {
  const sk = snapshot.skills[k];
  const profLevel: ProfLevel =
    sk.proficient === 0 ? 0 :
    sk.proficient === 2 ? 2 :
    1;  // 0.5 (half-prof) AND 1 (full) both render as ◉
  const name =
    locale === 'it' ? SKILL_NAMES[k].it :
    locale === 'de' ? SKILL_NAMES[k].de :
    SKILL_NAMES[k].en;
  return {
    abilityLabel: `sheet.ability.${sk.ability}`,
    name,
    profLevel,
    modifier: sk.total,
  };
});
```

The downstream loop (line ~628-661) that emits each row stays the same — it consumes `SkillDef`-shaped entries. Inline the type alias if needed, or keep `SkillDef` and adapt.

<!-- 5) Main tab senses line content: -->

Current row 16 emission (line ~384):
```typescript
rows.push(`${sensesLabel}  ${dash}`);
```

NEW emission per UI-SPEC §4:
```typescript
// Locale-specific abbreviations:
//   IT: PP / PI / IND  (Percezione Passiva / Passiva Intuizione / INDagare)
//   EN: PP / INS / INV
//   DE: WN / EIN / UNT (executor verifies via i18n-budgets.ts at execution time)
const passivePrc = snapshot.skills.prc.passive;
const passiveIns = snapshot.skills.ins.passive;
const passiveInv = snapshot.skills.inv.passive;
const sensesContent =
  locale === 'it' ? `PP ${passivePrc} · PI ${passiveIns} · IND ${passiveInv}` :
  locale === 'de' ? `WN ${passivePrc} · EIN ${passiveIns} · UNT ${passiveInv}` :
  `PP ${passivePrc} · INS ${passiveIns} · INV ${passiveInv}`;
rows.push(`${sensesLabel}  ${sensesContent}`);
```

Width budget verification (IT, Thorin): `Sensi  PP 11 · PI 11 · IND 14` = 5+2+2+1+2+1+1+1+2+1+2+1+1+1+3+1+2 = let me recount character-by-character with codePointLen: `S`(1)`e`(2)`n`(3)`s`(4)`i`(5)` `(6)` `(7)`P`(8)`P`(9)` `(10)`1`(11)`1`(12)` `(13)`·`(14)` `(15)`P`(16)`I`(17)` `(18)`1`(19)`1`(20)` `(21)`·`(22)` `(23)`I`(24)`N`(25)`D`(26)` `(27)`1`(28)`4`(29) = 29 cells. The `padToRowCount` / row66 (whichever the file uses) handles trailing padding to 66.

**ALTERNATIVE for executor:** If `renderMainTab` does NOT use a `row66()` helper to pad row 16 (inspect the existing file), wrap the line with explicit `padRightUnicode(rowContent, 66)` to enforce the width invariant. The existing `padToRowCount` only pads the array length, NOT individual row widths — verify line 386-389.

<!-- 6) DE locale verification — i18n-budgets.ts catalog: -->

Currently `'sheet.vitals.senses'` is `{ it: 'Sensi', en: 'Senses', de: 'Sinne', max: 6 }`. The passive abbreviations PP/PI/IND/WN/EIN/UNT are NOT in the catalog. Two options:

(a) **Add 3 new i18n keys** (cleanest, recommended): `'sheet.vitals.senses.prc'` / `.ins` / `.inv` with per-locale abbreviations + `max` budget. Mirrors existing catalog discipline.

(b) **Inline as renderer-side constants** (simpler, lower-touch): a 3-key map `PASSIVE_ABBR: Record<HudLocale, { prc: string; ins: string; inv: string }>` at module scope alongside SKILL_NAMES.

Executor discretion. Recommend (b) for Phase 17 simplicity; the abbreviations are 2-3 chars and locale-bound to renderer (no other consumer needs them). Phase 18 milestone-close could promote them to catalog if surface demands it.

DE abbreviations rationale (executor verifies at execution time):
- `WN` = Wahrnehmung passiv (Perception) — common dnd5e-DE shorthand
- `EIN` = Einblick passiv (Insight)
- `UNT` = Untersuchung passiv (Investigation) — **NOTE:** dnd5e-DE may use "Nachforschung" not "Untersuchung". Verify against existing `SKILL_NAMES.inv.de` (which the present plan keeps as "Nachforschung" matching the existing DEFAULT_SKILLS line). If DE Investigation is "Nachforschung", the DE abbreviation should be `NCH` not `UNT`. **Executor: confirm at execution; pick the abbreviation matching the SKILL_NAMES.inv.de string.** UI-SPEC §4 listed `UNT 14` as illustrative — adjust as needed; the verification gate is "row 16 is 66 code-points and matches whatever renderer outputs".

<!-- 7) Existing sheet.skills.it.txt byte-identity check: -->

Current fixture (rows 0-17, IT locale) was generated from the renderer's pre-Phase-17 output driven by DEFAULT_SKILLS. Post-Plan-17-03, the same renderer produces the same output IF AND ONLY IF the snapshot's skills field matches the DEFAULT_SKILLS Thorin values exactly. CONTEXT §Decisions Area 4 confirms this. Verify via `git diff packages/shared-render/src/fixtures/sheet.skills.it.txt` → expected zero diff. If diff is non-zero, the snapshot2014 spread in the test file is wrong; fix the test fixture, NOT the on-disk fixture.

<!-- 8) Snapshot literal extension surface (Phase 16 precedent): -->

Plan 16-03's Deviation 1 found 11 distinct files + 17 literals needing extension after the schema REQUIRED a new field. Phase 17 will hit a similar surface (the same files use CharacterSnapshot literals). Expected files (anchor list — executor must run `pnpm typecheck` and follow TS2741 errors to discover the FULL list):

1. `packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts` — snapshot2014 (Thorin canonical with 18 skills) + snapshot2024 (already spreads from 2014, automatic)
2. `packages/g2-app/src/panels/__tests__/inventory-panel.test.ts`
3. `packages/g2-app/src/panels/__tests__/spellbook-panel.test.ts` — snapshotCaster + snapshotHalfCaster + snapshotWithSpell
4. `packages/g2-app/src/panels/__tests__/character-sheet-panel.test.ts` — mockSnapshot ×2 + bioSnapshot
5. `packages/g2-app/src/status-hud/__tests__/status-hud-layer.test.ts`
6. `packages/g2-app/src/status-hud/__tests__/snapshot.test.ts`
7. `packages/g2-app/src/status-hud/__tests__/status-hud-renderer.test.ts` — makeSnapshot + makeDeathSnapshot
8. `packages/g2-app/src/__tests__/04b-integration-smoke.test.ts`
9. `packages/g2-app/src/__tests__/scene-renderer-smoke.test.ts`
10. `packages/g2-app/src/__tests__/05-panel-integration-smoke.test.ts`
11. `packages/g2-app/src/__tests__/13-integration-smoke.test.ts` — 3 inline literals
12. `packages/g2-app/src/__tests__/example-status-hud.test.ts`
13. `packages/g2-app/src/__tests__/sync-lost-chip.test.ts`
14. `packages/bridge/src/server.test.ts`
15. `packages/foundry-mcp/src/resources/ws-subscription.test.ts`
16. `packages/foundry-mcp/src/resources/resource-cache.test.ts`
17. `packages/foundry-mcp/src/resources/register-resources.test.ts`

For each, inline a zero-default 18-skill literal (`{ acr: { total:0, ability:'dex', proficient:0, passive:10 }, ani: { total:0, ability:'wis', proficient:0, passive:10 }, ... × 18 keys }`). Phase 16 precedent: each extension is ~10 lines × 17 literals = ~170 lines of test fixture extension. Keep self-contained per Phase 16 deviation (no shared test helper or factory); each literal stays atomic.

OPTIONAL ergonomic shim: A helper `function ZERO_SKILLS_LITERAL()` could be defined in a shared test-utilities file. Executor discretion. Phase 16 chose NOT to do this (no shared helper introduced). Phase 17 may follow the same discipline or factor it; document choice in SUMMARY.md as a deviation if factored.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SKILL_NAMES const + PASSIVE_ABBR const + rewrite renderSkillsTab dynamic lookup + renderMainTab senses line data binding + extend snapshot2014/snapshot2024 + extend 17+ downstream snapshot literals + add CSTR-SKILLS-DATA-1..5 tests</name>
  <files>packages/g2-app/src/panels/character-sheet-tab-renderers.ts, packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts, + 17+ downstream test files (discovered via pnpm typecheck after schema landing)</files>
  <behavior>
    - SKILL_NAMES static const present at module scope with 18 entries × 3 locales, extracted verbatim from existing DEFAULT_SKILLS strings (zero translation invention)
    - DEFAULT_SKILLS hardcoded array REMOVED entirely (deleted; ~60 LOC reduction)
    - PASSIVE_ABBR const present (renderer-side abbreviations for passive PP/PI/IND etc.)
    - renderSkillsTab uses dynamic `SKILL_KEYS.map(k => ...)` pipeline driven by `snapshot.skills[k]`; downstream row-emission loop unchanged
    - 0.5 (half-prof) renders as ◉ (rounded up per UI-SPEC §3)
    - renderMainTab row 16 emits `${sensesLabel}  PP <prc.passive> · PI <ins.passive> · IND <inv.passive>` (IT) — replacing the `—` placeholder; row stays exactly 66 code-points
    - snapshot2014/snapshot2024 in test file carry the 18-skill Thorin canonical spread matching DEFAULT_SKILLS byte-for-byte
    - 17+ downstream snapshot literals extended with zero-default 18-skill `skills` field; workspace typecheck exits 0
    - CSTR-SKILLS-DATA-1: rendering snapshot2014 produces the same output as `loadFixture('sheet.skills.it.txt')` (proves the dynamic swap preserves byte-identity)
    - CSTR-SKILLS-DATA-2: a snapshot with `skills.ath.proficient = 2` renders `★ Atletica` (expert glyph)
    - CSTR-SKILLS-DATA-3: a snapshot with `skills.ath.proficient = 0.5` renders `◉ Atletica` (half-prof rounds up; NOT a separate glyph)
    - CSTR-SKILLS-DATA-4: ability grouping preserved — Acrobazia (DEX) shows `DES` label on first DEX row; subsequent DEX skills show blank label (lastAbility logic intact)
    - CSTR-SKILLS-DATA-5: renderMainTab row 16 contains `PP 11 · PI 11 · IND 14` (Thorin senses line passives)
    - CSTR-MAIN-WIDTH continues passing (all 18 rows × 66 code-points); CSTR-DISP-NULL continues passing
    - CSTR-FIX-SKILLS round-trip against `sheet.skills.it.txt` PASSES (byte-identical post-swap)
  </behavior>
  <action>
    **(a) Open `packages/g2-app/src/panels/character-sheet-tab-renderers.ts`.**

    **(a1) Update imports** (file head): add `SKILL_KEYS, type SkillKey` from `@evf/shared-protocol` alongside the existing `CharacterSnapshot` import. Keep `HudLocale` import.

    **(a2) Add SKILL_NAMES const** at module scope after PROF_GLYPHS (line ~416). Use the verbatim strings from this plan's `<interfaces>` block §3. Add JSDoc citing UI-SPEC §5 + CONTEXT §Area 3 (no translation invention — extracted from existing DEFAULT_SKILLS).

    **(a3) Add PASSIVE_ABBR const** at module scope alongside SKILL_NAMES:
    ```typescript
    /**
     * Per-locale abbreviations for the Main tab senses-line passives.
     *
     * Renderer-side static map — these tokens are not promoted to the
     * i18n-budgets catalog because no other consumer needs them. If Phase 18
     * milestone-close demands broader use, promote at that time.
     *
     * IT: PP (Percezione Passiva), PI (Passiva Intuizione), IND (INDagare)
     * EN: PP (Passive Perception), INS (Passive Insight), INV (Passive Investigation)
     * DE: <executor verifies abbreviations at execution time against the DE
     *      SKILL_NAMES strings — likely WN/EIN/NCH given 'Nachforschung'>
     */
    const PASSIVE_ABBR: Record<HudLocale, { prc: string; ins: string; inv: string }> = {
      it: { prc: 'PP', ins: 'PI', inv: 'IND' },
      en: { prc: 'PP', ins: 'INS', inv: 'INV' },
      de: { prc: 'WN', ins: 'EIN', inv: 'NCH' },  // ← executor verify; UI-SPEC §4 listed UNT but DE name is 'Nachforschung'
    };
    ```

    Executor: verify DE abbreviation matches `SKILL_NAMES.inv.de` ('Nachforschung' → 'NCH'). If UI-SPEC §4's `UNT` was a placeholder, replace. Document the chosen abbreviation in 17-03-SUMMARY.md.

    **(a4) DELETE DEFAULT_SKILLS** (the 60-LOC array, lines ~427-577). The const + comment block + final `];` all go.

    **(a5) Rewrite renderSkillsTab body** (lines ~599-670). After the null guard and getLabel calls, replace `const skills = DEFAULT_SKILLS;` with the SKILL_KEYS-driven dynamic pipeline from this plan's `<interfaces>` block §4. The downstream `clampedOffset` / `visibleSkills.slice` / row-emission loop with `lastAbility` tracking stays unchanged — it consumes the same `SkillDef`-shaped entries. Adapt `SkillDef` to use the new shape (the existing `interface SkillDef` lines ~402-409 with `nameIt/nameEn/nameDe` separate fields can simplify to `name: string` since localization happens in the build step — executor discretion to refactor SkillDef in-place).

    **(a6) Replace renderMainTab row 16 emission** (line ~384). Use the senses-line build from this plan's `<interfaces>` block §5. Wrap with `padRightUnicode(rowContent, 66)` if the existing row 16 wasn't already pad-wrapped (inspect line 386-389 first). The width-invariant gate is "all 18 rows × 66 code-points" — CSTR-MAIN-WIDTH already enforces this and will fail loudly if missed.

    **(a7) Update the file-header JSDoc block** (lines 1-25) to add a Phase 17 bullet describing the DEFAULT_SKILLS → SKILL_KEYS swap + SKILL_NAMES static map + senses-line passive data binding + 0.5-round-up rule.

    **(b) Open `packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts`.**

    **(b1) Extend snapshot2014** (lines ~90-112) with the 18-skill Thorin canonical spread. Use the values from Plan 17-01's `<interfaces>` block (the same VALID_SKILLS spread). Must match DEFAULT_SKILLS exactly for byte-identity (Acrobazia +2 prof 0, Atletica +6 prof 1, Animal Handling +4 prof 1, Medicina +4 prof 1, all others as listed). `passive` for prc=11, ins=11, inv=14; others computed as `10 + total` for simplicity (e.g. acr passive 12).

    `snapshot2024` is `{ ...snapshot2014, world: { modernRules: true } }` — automatic propagation; no skills work.

    **(b2) Add CSTR-SKILLS-DATA-1..5 tests** in a new `describe('renderSkillsTab — skills data binding (CSTR-SKILLS-DATA)', () => {...})` block after the existing renderSkillsTab tests (around line ~232 / wherever renderSkillsTab tests live):

    - CSTR-SKILLS-DATA-1: load `sheet.skills.it.txt` fixture → compare `renderSkillsTab(snapshot2014, 'it', 0).join('\n')` against fixture content via existing `normaliseRows` helper → expect byte-identity.
    - CSTR-SKILLS-DATA-2: build `snapshotExpert = { ...snapshot2014, skills: { ...snapshot2014.skills, ath: { ...snapshot2014.skills.ath, proficient: 2, total: 7 } } }`. Render → `expect(rows.join('\n')).toContain('★')` AND `expect(rows.join('\n')).toContain('Atletica')` (no longer ◉).
    - CSTR-SKILLS-DATA-3: build `snapshotHalfProf = { ...snapshot2014, skills: { ...snapshot2014.skills, acr: { ...snapshot2014.skills.acr, proficient: 0.5 } } }`. Render → `expect(rows.join('\n')).toContain('◉ Acrobazia')` (half-prof rounds UP to ◉; NOT a fourth glyph) AND `expect(rows.join('\n')).not.toContain('☉ Acrobazia')` (no half-glyph introduced).
    - CSTR-SKILLS-DATA-4: render snapshot2014 → assert the first DEX-grouped skill row (Acrobazia) shows `DES` label and subsequent DEX rows (Rapidità di mano, Furtività) show blank label (4-space pad). Use a regex match or substring.
    - CSTR-SKILLS-DATA-5: render snapshot2014 Main tab IT → `expect(rows[16]).toContain('PP 11')` AND `toContain('PI 11')` AND `toContain('IND 14')`; assert `codePointLen(rows[16]) === 66`. Repeat for EN (`PP 11 · INS 11 · INV 14`) and DE (`WN 11 · EIN 11 · NCH 14` — or whatever PASSIVE_ABBR.de resolves to).

    **(b3) Update the file-header JSDoc** (lines 1-52) to add CSTR-SKILLS-DATA-1..5 markers paralleling the CSTR-SKILLS-* baseline.

    **(c) Extend 17+ downstream snapshot literals.**

    Run `pnpm typecheck` at workspace root; gather the TS2741 "Property 'skills' is missing" error list. For each file in the anchor list (this plan's `<interfaces>` block §8) and any additional files surfaced by typecheck, inline a zero-default 18-skill literal. Recommended inline pattern (per Phase 16 precedent, no shared helper):

    ```typescript
    skills: {
      acr: { total: 0, ability: 'dex', proficient: 0, passive: 10 },
      ani: { total: 0, ability: 'wis', proficient: 0, passive: 10 },
      arc: { total: 0, ability: 'int', proficient: 0, passive: 10 },
      ath: { total: 0, ability: 'str', proficient: 0, passive: 10 },
      dec: { total: 0, ability: 'cha', proficient: 0, passive: 10 },
      his: { total: 0, ability: 'int', proficient: 0, passive: 10 },
      ins: { total: 0, ability: 'wis', proficient: 0, passive: 10 },
      itm: { total: 0, ability: 'cha', proficient: 0, passive: 10 },
      inv: { total: 0, ability: 'int', proficient: 0, passive: 10 },
      med: { total: 0, ability: 'wis', proficient: 0, passive: 10 },
      nat: { total: 0, ability: 'int', proficient: 0, passive: 10 },
      prc: { total: 0, ability: 'wis', proficient: 0, passive: 10 },
      prf: { total: 0, ability: 'cha', proficient: 0, passive: 10 },
      per: { total: 0, ability: 'cha', proficient: 0, passive: 10 },
      rel: { total: 0, ability: 'int', proficient: 0, passive: 10 },
      slt: { total: 0, ability: 'dex', proficient: 0, passive: 10 },
      ste: { total: 0, ability: 'dex', proficient: 0, passive: 10 },
      sur: { total: 0, ability: 'wis', proficient: 0, passive: 10 },
    },
    ```

    Add this block immediately after the existing `abilities: { ... }` field in each literal (preserving the existing ordering convention from Phase 16).

    ALTERNATIVE: if executor prefers, define a single `ZERO_SKILLS_LITERAL` helper in a shared test-utilities path (e.g. `packages/shared-protocol/src/test-fixtures.ts` exported alongside `BASE_CHARACTER_SNAPSHOT`). Phase 16 chose NOT to do this; Phase 17 may follow or factor. Document the choice in SUMMARY.md as a deviation if factored.

    Run `pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers` (after the rewrites): expect CSTR-SKILLS-DATA-* + CSTR-MAIN-AB-* (Phase 16) all pass; CSTR-FIX-SKILLS passes (byte-identical fixture round-trip).

    Pitfall avoidance:
    - Width-budget invariant 66 cells per row must be preserved on row 16 (senses line). If the rewritten row exceeds 66 chars (e.g. Thorin's `PP 11 · PI 11 · IND 14` = 29 chars + label `Sensi  ` = 36 chars; well under), the trailing `padRightUnicode(..., 66)` handles padding. If the existing renderMainTab uses `padToRowCount` only (not per-row padding), explicitly wrap the new line with `padRightUnicode(rowContent, 66)` mirroring how other lines in renderMainTab handle width.
    - DE abbreviation discipline: if DE Investigation in SKILL_NAMES is "Nachforschung", the abbreviation MUST be NCH (not UNT from UI-SPEC §4's draft). UI-SPEC §4 EXPLICITLY allows executor to verify and adjust.
    - 0.5 round-up: half-prof MUST render as ◉ (NOT a fourth glyph). UI-SPEC §3 contract.
    - Half-prof to-`total` consistency is NOT this plan's responsibility — the schema accepts any `total` regardless of `proficient`; the reader passes through; the renderer faithfully renders what the reader emits. If a Foundry world emits inconsistent total/proficient pairs, that's the dnd5e system's problem.
    - When extending downstream snapshot literals, keep INLINE per Phase 16 precedent — no shared helper unless executor explicitly justifies factoring.
    - Do NOT touch the `sheet.skills.it.txt` fixture on disk — it MUST round-trip byte-identically. If round-trip fails, the snapshot2014 spread is wrong; fix the test, NOT the fixture.
    - Do NOT introduce a fixture-generation script for sheet.skills.* — the existing IT fixture is already valid; the new EN fixture is generated in Task 2 (or inline as a tsx one-shot per Phase 16 Plan 16-03 precedent).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | tail -50</automated>
    Expected: CSTR-SKILLS-DATA-1..5 pass; CSTR-MAIN-AB-* still passes; CSTR-MAIN-WIDTH passes; CSTR-DISP-NULL passes; CSTR-FIX-SKILLS passes (byte-identical); CSTR-FIX-MAIN-2014/2024/EN/DE FAIL (row 16 senses-line mismatch — Task 2 closes them); CSTR-FIX-SKILLS-EN FAIL (new fixture not yet generated — Task 3 closes it).
  </verify>
  <done>
    - SKILL_NAMES + PASSIVE_ABBR consts present at module scope
    - DEFAULT_SKILLS hardcoded array DELETED (verified by `! grep -q "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts`)
    - renderSkillsTab uses SKILL_KEYS.map dynamic pipeline; ProfLevel mapping handles 0.5 → ◉ round-up
    - renderMainTab row 16 emits passive senses line per UI-SPEC §4; row stays 66 code-points
    - snapshot2014 + snapshot2024 carry 18-skill Thorin canonical spread
    - 17+ downstream snapshot literals extended with zero-default skills field (or shared helper if factored)
    - CSTR-SKILLS-DATA-1..5 + CSTR-MAIN-AB-* tests pass
    - CSTR-FIX-SKILLS passes (byte-identical round-trip — proves dynamic swap preserves existing fixture)
    - CSTR-FIX-MAIN-* + CSTR-FIX-SKILLS-EN FAIL (Tasks 2 + 3 close them — expected RED state)
    - `pnpm typecheck` at workspace root exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Byte-update 4 INV-1 fixtures row 16 (sheet.main.2014.it.txt + 2024.it + 2014.en + 2014.de) — senses-line passives</name>
  <files>packages/shared-render/src/fixtures/sheet.main.2014.it.txt, packages/shared-render/src/fixtures/sheet.main.2024.it.txt, packages/shared-render/src/fixtures/sheet.main.2014.en.txt, packages/shared-render/src/fixtures/sheet.main.2014.de.txt</files>
  <behavior>
    - sheet.main.2014.it.txt row 16 contains `Sensi  PP 11 · PI 11 · IND 14` (followed by trailing padding to 66 chars)
    - sheet.main.2024.it.txt row 16 IDENTICAL to 2014.it.txt row 16 (skill-data edition-agnostic — only row 2 [M] flag differs)
    - sheet.main.2014.en.txt row 16 contains `Senses  PP 11 · INS 11 · INV 14`
    - sheet.main.2014.de.txt row 16 contains `Sinne  WN 11 · EIN 11 · NCH 14` (or whatever PASSIVE_ABBR.de resolved to in Task 1)
    - All 4 fixtures retain their 18-row × 66-cell shape
    - Rows 0-15 + 17 byte-identical to pre-Plan-17-03 fixtures (Phase 16 baseline preserved)
    - CSTR-FIX-MAIN-2014 + CSTR-FIX-MAIN-2024 + PSM-FIX-EN-MAIN + PSM-FIX-DE-MAIN round-trip tests all pass
  </behavior>
  <action>
    DO NOT hand-author the fixture rows. The deterministic safe path mirrors Phase 16 Plan 16-03 Task 2 (precedent: `e8e7da0` commit; one-shot tsx script):

    1. Create a one-shot script under `packages/g2-app/scripts/generate-main-fixtures.ts` (or use `pnpm exec tsx -e`) that imports `renderMainTab` from `@evf/g2-app` and the relevant test snapshots (`snapshot2014`/`snapshot2024` for IT fixtures; `BASE_CHARACTER_SNAPSHOT` from `@evf/shared-protocol` test-fixtures path for EN/DE — Phase 16 Plan 16-03 Deviation 3 established that EN/DE fixtures use BASE_CHARACTER_SNAPSHOT to preserve byte-identity of row 6 HP bar).

    2. **CRITICAL — consumer-snapshot identity discipline (Phase 16 Deviation 3):**
       - IT fixtures (`sheet.main.2014.it.txt` + `sheet.main.2024.it.txt`): use `snapshot2014`/`snapshot2024` (Thorin canonical, tempHp:10, real skills) → row 16 emits `PP 11 · PI 11 · IND 14`
       - EN fixture (`sheet.main.2014.en.txt`): use `BASE_CHARACTER_SNAPSHOT` (zero-default abilities all 10/+0/+0 ○, tempHp:0, zero-default skills passive:10 each) → row 16 emits `PP 10 · INS 10 · INV 10`
       - DE fixture (`sheet.main.2014.de.txt`): same BASE_CHARACTER_SNAPSHOT → row 16 emits `WN 10 · EIN 10 · NCH 10` (with whatever DE abbreviations resolved)
       - WAIT — that means EN/DE fixtures show `10/10/10` not `11/11/14`. That's fine; the value preserves consumer-snapshot identity per Phase 16 precedent. UI-SPEC §4 mentioned `11/11/14` for ALL locales but that was illustrative; the verification gate is "row 16 matches renderer output for whichever snapshot identity each fixture consumes".

    3. Concretely produce 4 outputs:
       - `renderMainTab(snapshot2014, 'it')` → write to `sheet.main.2014.it.txt`
       - `renderMainTab(snapshot2024, 'it')` → write to `sheet.main.2024.it.txt`
       - `renderMainTab(BASE_CHARACTER_SNAPSHOT, 'en')` → write to `sheet.main.2014.en.txt`
       - `renderMainTab(BASE_CHARACTER_SNAPSHOT, 'de')` → write to `sheet.main.2014.de.txt`

       **BASE_CHARACTER_SNAPSHOT MUST have a `skills` field after Plan 17-01.** Verify it's in the literal-extension list from Task 1 step (c); if not, extend it now.

    4. **Byte-stability check before commit**: for each fixture, do a 3-way diff:
       - rows 0-15 + row 17 MUST be byte-identical to pre-Plan-17-03 fixture (Phase 16 baseline preserved — `git diff` shows ONLY row 16 changed)
       - row 16 (senses) is the delta
       - If any other row shifts, the renderer rewrite in Task 1 has a regression on a non-senses row — STOP and fix Task 1 before proceeding.

    5. Optional: delete the one-shot generator script post-generation (per Phase 16 Plan 16-03 precedent — keep diff clean, no developer-utility tooling shipped with the commit).

    6. Run `pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -E "(CSTR-FIX-MAIN|PSM-FIX-EN-MAIN|PSM-FIX-DE-MAIN|FAIL)"` and verify all 4 round-trips PASS.

    Pitfall avoidance:
    - Do NOT hand-author fixture rows — Phase 5 / Phase 16 precedent: fixtures generated by running real renderers.
    - Do NOT change row 6 HP bar — EN/DE consumer-snapshot identity MUST be BASE_CHARACTER_SNAPSHOT (tempHp:0) to preserve `git diff` row-6 byte-identity. This is the exact regression Phase 16 Plan 16-03 Deviation 3 fixed.
    - Do NOT widen the change beyond row 16 — `git diff HEAD~1 -- packages/shared-render/src/fixtures/sheet.main.*.txt` MUST show ONLY row 16 changed in each fixture. If it shows rows 9-15 too (abilities block changing), Task 1's renderMainTab edits broke Phase 16 — STOP.
    - Width invariant: each row exactly 66 code-points (CSTR-MAIN-WIDTH gate).
    - DE abbreviation: must match PASSIVE_ABBR.de constant from Task 1. If executor chose NCH for Investigation, the fixture says `NCH 10`.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -E "(CSTR-FIX-MAIN|PSM-FIX-EN-MAIN|PSM-FIX-DE-MAIN)" | head -10 && grep -c "PP 11" packages/shared-render/src/fixtures/sheet.main.2014.it.txt && grep -c "PP 10" packages/shared-render/src/fixtures/sheet.main.2014.en.txt</automated>
    Expected: All round-trip tests PASS. IT fixtures contain `PP 11`; EN fixture contains `PP 10` (BASE_CHARACTER_SNAPSHOT zero-default).
  </verify>
  <done>
    - All 4 fixtures row 16 byte-updated with locale-correct senses line
    - Rows 0-15 + 17 byte-identical to pre-Plan-17-03 fixtures (`git diff` shows ONLY row 16 changed)
    - Each fixture row exactly 66 code-points (CSTR-MAIN-WIDTH gate)
    - CSTR-FIX-MAIN-2014 + CSTR-FIX-MAIN-2024 + PSM-FIX-EN-MAIN + PSM-FIX-DE-MAIN round-trip tests pass
    - One-shot generator script created and deleted (or kept as developer utility; document in SUMMARY.md)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate NEW sheet.skills.en.txt fixture (EN locale parity) + add CSTR-FIX-SKILLS-EN round-trip test</name>
  <files>packages/shared-render/src/fixtures/sheet.skills.en.txt, packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts</files>
  <behavior>
    - New fixture `sheet.skills.en.txt` exists in `packages/shared-render/src/fixtures/`
    - Fixture content = `renderSkillsTab(snapshot2014, 'en', 0)` output verbatim
    - Mirrors the IT fixture structure: legend row + blank + 14 visible skill rows + scroll hint + blank
    - All 18 rows × 66 code-points (INV-1 width invariant)
    - EN locale labels: STR/DEX/INT/WIS/CHA for ability column; Athletics/Acrobatics/etc. for skill names; `◉ proficient · ★ expertise · ○ untrained` legend; `▼ scroll for more · R1 scroll-tap = roll skill` hint
    - New test CSTR-FIX-SKILLS-EN asserts byte-identical round-trip
  </behavior>
  <action>
    Mirror Task 2's fixture-generation discipline:

    1. Use a one-shot tsx invocation OR extend the Task 2 generator: `renderSkillsTab(snapshot2014, 'en', 0).join('\n')` → write to `packages/shared-render/src/fixtures/sheet.skills.en.txt`.

    2. Use `snapshot2014` (Thorin canonical) for consumer identity — matches the IT fixture's consumer choice. (Note: PSM-FIX-EN-SKILLS does NOT exist as a pre-Phase-17 test; this is a NEW fixture + NEW test. No backward consumer-snapshot identity constraint, so Thorin is the clean choice for INV-1 visual coverage parity with `sheet.skills.it.txt`.)

    3. Verify width: all 18 rows × 66 code-points via `[...row].length === 66` per row.

    4. Add CSTR-FIX-SKILLS-EN test in `character-sheet-tab-renderers.test.ts` mirroring the existing CSTR-FIX-SKILLS pattern (same `loadFixture` + `normaliseRows` + `expect(rendered).toBe(fixture)` shape). Place it adjacent to the existing CSTR-FIX-SKILLS test.

    5. Run `pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -E "(CSTR-FIX-SKILLS|FAIL)"` and verify both CSTR-FIX-SKILLS (IT, pre-existing) AND CSTR-FIX-SKILLS-EN (new) PASS.

    Pitfall avoidance:
    - Do NOT hand-author the fixture content. Run the renderer; write verbatim.
    - Do NOT introduce a third locale (DE) fixture — that's deferred (Phase 17 scope per UI-SPEC §7 covers only IT preservation + EN parity).
    - Width invariant: 66 code-points per row.
    - If the renderer crashes or produces unexpected output (e.g. <18 rows), Task 1 has a bug — STOP and fix Task 1.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -E "CSTR-FIX-SKILLS" | head -10 && wc -l packages/shared-render/src/fixtures/sheet.skills.en.txt && head -1 packages/shared-render/src/fixtures/sheet.skills.en.txt</automated>
    Expected: CSTR-FIX-SKILLS (IT) + CSTR-FIX-SKILLS-EN (EN) both PASS. Fixture has 18 lines + 1 trailing newline (or 18 lines depending on existing convention — match IT). Legend row starts with `◉ proficient` (EN).
  </verify>
  <done>
    - `sheet.skills.en.txt` exists with EN locale Thorin-canonical skills rendering
    - All 18 rows × 66 code-points
    - CSTR-FIX-SKILLS-EN test added and passes
    - Pre-existing CSTR-FIX-SKILLS (IT) still passes (byte-identical fixture preserved)
  </done>
</task>

<task type="auto">
  <name>Task 4: INV-3 atomic ratification commit (STATE.md + ROADMAP.md + REQUIREMENTS.md + 17-VERIFICATION.md) — single commit per Phase 14/15/16 precedent</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-VERIFICATION.md</files>
  <behavior>
    - Phase 17 closure recorded in STATE.md frontmatter + Current Position + Recent Trend + Accumulated Context (D-Area decisions from 17-CONTEXT)
    - ROADMAP.md Phase 17 entry flipped ✓ (3/3 plans complete; closed in milestone v0.9.13); v0.9.13 progress bumped from 1/3 → 2/3 phases (plans 3/~7 → 6/~7)
    - REQUIREMENTS.md SHEET-08 / SHEET-09 / SHEET-10 flipped to checked + status table updated to "Resolved"
    - 17-VERIFICATION.md created with 5/5 SC checklist + 3/3 REQ table + ADR-0005 Branch A carry-forward note (no new hardware-pending SCs from Phase 17) + commit hashes + INV-1 fixture delta description
    - All changes land in a SINGLE git commit per CLAUDE.md INV-3 (Phase 14 3a0c5cf + Phase 15 dc161d6 + Phase 16 d68d7f2 precedent)
    - Specs.md NOT bumped this phase — milestone-close bump conventionally in Phase 18
    - README.md + docs/showcase/index.html NOT touched — milestone-close artifacts
  </behavior>
  <action>
    Run pre-checks FIRST to ensure implementation gates are green BEFORE composing the ratification commit:

    ```bash
    pnpm test --run                                                                              # workspace-wide; expect ALL green
    pnpm typecheck                                                                               # exits 0
    pnpm lint:ci                                                                                 # exits 0
    grep -c 'socketlib.registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts  # MUST be 17 (CI Gate 8)
    ! grep -q "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts       # hardcoded array gone
    grep -c "SKILL_NAMES" packages/g2-app/src/panels/character-sheet-tab-renderers.ts             # SKILL_NAMES present
    grep -c "PASSIVE_ABBR" packages/g2-app/src/panels/character-sheet-tab-renderers.ts            # PASSIVE_ABBR present
    ```

    If any gate fails, ABORT and surface the failure — do NOT commit STATE/ROADMAP/REQ flips on a broken tree.

    Once gates are green, perform the 4 doc updates and stage them together for ONE atomic commit:

    **(a) `17-VERIFICATION.md`** (new file): Follow Phase 14/15/16 VERIFICATION.md structure. Include:
    - Frontmatter: `phase: 17-sheet-skills-tab-skills-tab-data-wiring`, `closed: 2026-05-18`, `commit: <fill in pre-commit>`
    - Success Criteria table: 5 rows, all `✓` with evidence column (commit hashes from 17-01 GREEN + 17-02 GREEN + 17-03 Task 1/2/3 commits + test names CSTR-SKILLS-DATA-1..5)
    - Requirements table: 3 rows (SHEET-08 ✓ schema; SHEET-09 ✓ reader; SHEET-10 ✓ renderer + fixtures + Main tab senses line)
    - Hardware-pending: NONE (Phase 17 is software-only, mirroring Phase 16). Existing 35 hardware-pending SCs from v0.9.11 carry under ADR-0005 Branch A unchanged.
    - CI Gate 8 evidence: `socketlib.registerComplexHandler` count = 17 preserved (grep output)
    - Test totals: pre-Plan-17 workspace count (2648) → post-Plan-17 count (expected delta: +17 to +25 tests across CS-SK-1..7 + CR-SK-1..5 + CSTR-SKILLS-DATA-1..5 + CSTR-FIX-SKILLS-EN). Document the actual delta from `pnpm test --run` output.
    - INV-1 fixture update summary:
      - `sheet.skills.it.txt` — byte-identical (no edit; validation only)
      - `sheet.skills.en.txt` — NEW (full 18 rows × 66 cells, EN Thorin)
      - `sheet.main.2014.it.txt` + `sheet.main.2024.it.txt` — row 16 updated to `Sensi  PP 11 · PI 11 · IND 14`
      - `sheet.main.2014.en.txt` — row 16 updated to `Senses  PP 10 · INS 10 · INV 10` (BASE consumer)
      - `sheet.main.2014.de.txt` — row 16 updated to `Sinne  WN 10 · EIN 10 · NCH 10` (or whatever DE resolved to)
      - Rows 0-15 + 17 byte-identical to Phase 16 baseline
    - DEFAULT_SKILLS removal evidence: `! grep -q "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts`
    - 17+ downstream snapshot literal extensions: enumerate the files touched (Phase 16 had 11; Phase 17 expected similar)
    - INV-3 atomic commit: this single commit, per Phase 14 3a0c5cf + Phase 15 dc161d6 + Phase 16 d68d7f2 precedent

    **(b) `STATE.md`** updates:
    - Frontmatter: `progress.completed_phases: 2`, `progress.completed_plans: 6`, `progress.percent: 67` (2 of 3 milestone phases done), `last_updated`, `last_activity: 2026-05-18`
    - Current Position: `Phase: Phase 17 ✓ closed | Plan: — | Status: Phase 18 (milestone close) next`
    - Recent Trend: prepend a top entry `2026-05-18 — Phase 17 (all 3 plans, INV-3 atomic close): CharacterSnapshotSchema.skills REQUIRED extension (18 keys × {total, ability, proficient, passive}) + extractSkills reader helper + SKILL_DEFAULT_ABILITY map + renderSkillsTab dynamic lookup replacing DEFAULT_SKILLS + Main tab senses line passives + 5 INV-1 fixtures (1 new + 4 row-16 updates) + new CSTR-SKILLS-DATA tests + 17+ downstream snapshot literal extensions. CI Gate 8 socketlib count = 17 preserved (read-path-only extension). Workspace +N tests. Single INV-3 atomic commit per Phase 14/15/16 precedent.`
    - Accumulated Context → Decisions: add bullets for the 4 grey areas from 17-CONTEXT (D-Area-1: 18-key strictObject + per-skill z.object forward-compat + 0|0.5|1|2 closed enum; D-Area-2: extractSkills defensive zero-defaults + SKILL_DEFAULT_ABILITY map + verbatim proficient pass-through; D-Area-3: renderer SKILL_NAMES static i18n + 0.5 rounds to ◉ + senses-line PP/PI/IND abbreviations; D-Area-4: sheet.skills.it.txt byte-identical + new sheet.skills.en.txt + 4 main-fixture row-16 byte-updates)

    **(c) `ROADMAP.md`** updates:
    - Phase 17 entry under v0.9.13: `- [x] **Phase 17: Sheet Skills Tab (Skills tab data wiring)** (3/3 plans) — ...` (was `- [ ] ... (~3 plans)`)
    - Progress table: v0.9.13 row: `Phases: 3 (16–18) | Plans: 6/~7 | Status: 🚧 In progress | Shipped: —` (was `3/~7`)
    - Footer "Last reorganized" line: bump date + add note "Phase 17 closed via INV-3 atomic single commit per Phase 14/15/16 precedent."

    **(d) `REQUIREMENTS.md`** updates:
    - Find `SHEET-08`, `SHEET-09`, `SHEET-10` bullets; flip `- [ ]` → `- [x]`
    - Status table: flip the 3 corresponding rows from "Defined" → "Resolved"

    **(e) Commit the 4 doc files + 17-VERIFICATION.md** in a single commit message per Phase 14/15/16 style:
    ```
    docs(phase-17): close Sheet Skills Tab phase (INV-3 atomic)

    Phase 17: Skills tab data wiring + Main tab senses passives ✓
    - SHEET-08: CharacterSnapshotSchema.skills REQUIRED (z.strictObject of 18 keys × {total, ability, proficient, passive})
    - SHEET-09: extractSkills reader helper + SKILL_DEFAULT_ABILITY map; proficient 0|0.5|1|2 preserved verbatim (Skills tab full spectrum, unlike Phase 16's boolean coercion for Main tab)
    - SHEET-10: renderSkillsTab dynamic lookup replacing DEFAULT_SKILLS + SKILL_NAMES static i18n map; Main tab senses line passives (PP/PI/IND); 5 INV-1 fixtures (1 new + 4 row-16 updates); CSTR-SKILLS-DATA-1..5 tests

    INV-3 atomic: STATE.md + ROADMAP.md + REQUIREMENTS.md + 17-VERIFICATION.md
    CI Gate 8 socketlib count = 17 preserved (read-path-only extension).
    Hardware-pending SCs: unchanged (35 from v0.9.11 carry under ADR-0005 Branch A).

    Refs: 17-CONTEXT D-Area-1..4 (smart-discuss autonomous on Phase 16 atomic-extension precedent)
    Precedent: Phase 14 3a0c5cf + Phase 15 dc161d6 + Phase 16 d68d7f2 (INV-3 single-commit pattern)
    ```

    Pitfall avoidance:
    - Do NOT split the doc updates across multiple commits — Phase 14/15/16 INV-3 precedent is a single atomic commit. Re-stage if any doc was committed early.
    - Do NOT bump Specs.md version this phase — milestone-close convention is Phase 18.
    - Do NOT touch README.md or docs/showcase/index.html — those are milestone-close artifacts; Phase 17 is intra-milestone.
    - Do NOT introduce new ADRs — no architectural deviation in Phase 17. If a deviation emerges during execution (e.g. DE abbreviation choice), surface it via DEVIATION block in 17-03-SUMMARY.md and propose ADR amendment as a follow-up quick task.
    - Do NOT push to remote — commit only; pushing is the user's call.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && git log -1 --name-only HEAD | grep -E "(STATE.md|ROADMAP.md|REQUIREMENTS.md|17-VERIFICATION.md)" | wc -l | grep -q '^4$' && pnpm test --run 2>&1 | tail -5 && grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts | grep -q '^17$' && ! grep -q "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts</automated>
    Expected: HEAD commit touches all 4 doc files (grep returns 4); workspace test suite exits 0; socketlib count = 17; DEFAULT_SKILLS absent from renderer.
  </verify>
  <done>
    - 17-VERIFICATION.md created with 5/5 SC ✓ + 3/3 REQ ✓ + ADR-0005 Branch A carry-forward note + CI Gate 8 evidence + INV-1 fixture delta description (5 fixtures total: 1 byte-identical + 1 new + 4 row-16 updates) + DEFAULT_SKILLS removal evidence
    - STATE.md frontmatter (2/3 phases, 6/~7 plans, percent: 67) + Current Position + Recent Trend updated; D-Area-1..4 decisions logged
    - ROADMAP.md Phase 17 entry flipped ✓; v0.9.13 progress row bumped to 6/~7 plans
    - REQUIREMENTS.md SHEET-08/09/10 flipped ✓; status table updated to Resolved
    - Single atomic git commit touches all 4 doc files (grep returns 4) per Phase 14/15/16 precedent
    - `pnpm test --run` exits 0 at HEAD (2648 + new tests passing)
    - CI Gate 8: socketlib count = 17 preserved
    - DEFAULT_SKILLS hardcoded array absent from renderer (verified via `! grep`)
    - `pnpm typecheck && pnpm lint:ci` both exit 0 at HEAD
    - Specs.md NOT bumped; README.md + showcase NOT touched (milestone-close artifacts)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CharacterSnapshot → renderSkillsTab → 18-row text payload | Input shape gated by CharacterSnapshotSchema (Plan 17-01); output is plain text to bridge.textContainerUpgrade — no DOM, no eval |
| CharacterSnapshot → renderMainTab row 16 → senses text | Same trust contract; reader's defensive `passive ≥ 0` clamp prevents negative input from reaching the renderer |
| INV-1 fixtures (disk) → round-trip test | Read-only files; tests assert byte equality; no taint flow into runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-S | Spoofing | renderSkillsTab + renderMainTab | accept | Snapshot is server-authenticated upstream (Phase 3); renderer is a pure function over validated input |
| T-17-03-T | Tampering | profLevel mapping + senses content | mitigate | `proficient` clamped to 0|0.5|1|2 by reader (Plan 17-02); renderer's profLevel mapping accepts all 4 values gracefully (0.5 round-up); `passive ≥ 0` schema-clamped; renderer never crashes on malformed data |
| T-17-03-R | Repudiation | n/a | accept | Read-only display surface; no audit-relevant action |
| T-17-03-I | Info Disclosure | n/a | accept | Same scope as Phase 5/16 Sheet display; player only sees their own actor's skills/passives |
| T-17-03-D | DoS | renderSkillsTab + renderMainTab | mitigate | Bounded 18-key loop over skills (SKILL_KEYS); senses-line is O(3) lookup; format helpers O(1); fixtures round-trip O(rows); no recursion or unbounded iteration |
| T-17-03-E | Elevation | n/a | accept | Read-path renderer; no write surface |
| T-17-03-SC | Tampering | npm/pip/cargo installs | accept | **NO new package installs in Plan 17-03.** Only renderer/test/fixture/doc edits. RESEARCH.md Package Legitimacy Audit not required. |

**Severity:** LOW — pure-function renderer extension; no new package installs; no network boundary; no write path; defensive enum clamping inherited from Plan 17-02 reader. Width-budget invariant gates render-output correctness; CSTR-MAIN-WIDTH test fails loudly on regression.
</threat_model>

<verification>
## Wave-17-03 Atomic Gates (all must be green at plan close)

- **WAVE-17-03-G1** Four-commit history for this plan (Task 1 + Task 2 + Task 3 are separate atomic units; Task 4 is the INV-3 ratification):
  - `git log --oneline -4 -- packages/g2-app/src/panels packages/shared-render/src/fixtures .planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-VERIFICATION.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` shows 4 commits
  - Subjects:
    - `test(17-03): RED — CSTR-SKILLS-DATA-1..5 + snapshot2014/2024 skills + 17 downstream literal extensions`
    - `feat(g2-app): GREEN — SKILL_NAMES + PASSIVE_ABBR + renderSkillsTab dynamic lookup + renderMainTab senses line + DEFAULT_SKILLS removed`
    - `fix(shared-render): byte-update 4 sheet.main fixtures row 16 + add sheet.skills.en.txt`
    - `docs(phase-17): close Sheet Skills Tab phase (INV-3 atomic)`

- **WAVE-17-03-G2** CSTR-SKILLS-DATA-* + CSTR-FIX-* all pass:
  - `pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -cE "✓"` ≥ baseline + 6 (CSTR-SKILLS-DATA-1..5 + CSTR-FIX-SKILLS-EN)
  - `pnpm --filter @evf/g2-app test --run character-sheet-tab-renderers 2>&1 | grep -cE "(FAIL|×)"` = 0

- **WAVE-17-03-G3** Workspace-wide test suite green at HEAD:
  - `pnpm test --run` exits 0; total count ≥ 2648 (Phase 16 close baseline) + at least 17 new tests (CS-SK-1..7 + CR-SK-1..5 + CSTR-SKILLS-DATA-1..5 + CSTR-FIX-SKILLS-EN, conservatively)

- **WAVE-17-03-G4** Width invariant preserved on all 5 fixtures (66 code-points per row):
  - For each fixture in `sheet.main.{2014.it,2024.it,2014.en,2014.de}.txt` + `sheet.skills.{it,en}.txt`:
    Every row exactly 66 visible code-points (verified via the existing CSTR-MAIN-WIDTH + CSTR-SKILLS-WIDTH tests that run on every render).

- **WAVE-17-03-G5** Rows 0-15 + 17 byte-identical to pre-Phase-17 main fixtures (only row 16 changed):
  - `git diff HEAD~2 -- packages/shared-render/src/fixtures/sheet.main.2014.it.txt` shows changes ONLY on row 16 (1-indexed) — Task 2 done criterion. Repeat for all 4 main fixtures.

- **WAVE-17-03-G6** `sheet.skills.it.txt` byte-identical (no edit):
  - `git diff HEAD~3 -- packages/shared-render/src/fixtures/sheet.skills.it.txt` shows ZERO lines changed.

- **WAVE-17-03-G7** `sheet.skills.en.txt` NEW file with 18 rows × 66 cells, EN Thorin canonical:
  - `wc -l packages/shared-render/src/fixtures/sheet.skills.en.txt` returns ≥ 18 (depending on trailing-newline convention)
  - `awk '{ if (length($0) > 0) printf "%d\n", length($0) }' packages/shared-render/src/fixtures/sheet.skills.en.txt` — verify all non-blank rows are 66 chars (byte length; for ASCII rows this equals codePointLen, but DEC encoded chars need `[...str].length` verification via Node)
  - `grep -c "Athletics" packages/shared-render/src/fixtures/sheet.skills.en.txt` returns 1

- **WAVE-17-03-G8** DEFAULT_SKILLS hardcoded array REMOVED:
  - `grep -c "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` returns `0`
  - `grep -c "SKILL_NAMES" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` ≥ 1
  - `grep -c "PASSIVE_ABBR" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` ≥ 1

- **WAVE-17-03-G9** CI Gate 8 socketlib count = 17 preserved:
  - `grep -c 'socketlib.registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts` returns `17`
  - `grep -v '^#' packages/foundry-module/src/pair/socketlib-handlers.ts | grep -c 'socketlib.registerComplexHandler'` returns `17` (comment-filtered, per planner_authority_limits "grep -c counts comments" hygiene)

- **WAVE-17-03-G10** INV-3 atomic single-commit ratification:
  - `git log -1 --name-only HEAD` lists at minimum: STATE.md, ROADMAP.md, REQUIREMENTS.md, 17-VERIFICATION.md (4 files MUST appear)
  - `git log -1 --pretty=%s HEAD | grep -E "phase-17|Phase 17"` returns 1 match

- **WAVE-17-03-G11** No package install / no socketlib handler / no write-path introduced:
  - `git diff HEAD~4..HEAD -- pnpm-lock.yaml package.json packages/*/package.json | wc -l | grep -q '^0$'` (no dep changes across the 3 plans + INV-3)
  - `git diff HEAD~4..HEAD -- packages/foundry-module/src/pair/socketlib-handlers.ts | wc -l | grep -q '^0$'` (no socketlib-handlers.ts changes)

- **WAVE-17-03-G12** Specs.md + README.md + docs/showcase NOT touched in Phase 17:
  - `git diff HEAD~4..HEAD -- Specs.md README.md docs/showcase/index.html | wc -l | grep -q '^0$'`
  - Rationale: milestone-close artifacts; Phase 18 handles those.

## Required test commands (run by executor in Task 4 pre-check)

```bash
pnpm test --run                                                                                # workspace-wide; ALL green
pnpm typecheck                                                                                 # exits 0
pnpm lint:ci                                                                                   # exits 0
grep -c 'socketlib.registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts  # = 17
grep -c "DEFAULT_SKILLS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts            # = 0 (hardcoded removed)
grep -c "SKILL_NAMES" packages/g2-app/src/panels/character-sheet-tab-renderers.ts                # ≥ 1
```
</verification>

<success_criteria>
- SKILL_NAMES + PASSIVE_ABBR static const maps present in `character-sheet-tab-renderers.ts` with JSDoc citing UI-SPEC §4-5
- DEFAULT_SKILLS hardcoded array REMOVED (grep-asserted absence)
- renderSkillsTab uses SKILL_KEYS-driven dynamic lookup; profLevel handles 0/0.5/1/2 with 0.5 → ◉ round-up per UI-SPEC §3
- renderMainTab row 16 emits senses passives `Sensi  PP <prc.passive> · PI <ins.passive> · IND <inv.passive>` (IT) / EN+DE equivalents; row stays 66 code-points
- snapshot2014 + snapshot2024 test fixtures carry 18-skill Thorin canonical spread matching DEFAULT_SKILLS values byte-for-byte
- 17+ downstream snapshot literals across g2-app + bridge + foundry-mcp extended with zero-default 18-skill `skills` field
- `sheet.skills.it.txt` byte-identical post-swap (validation: zero diff)
- `sheet.skills.en.txt` NEW fixture added (18 × 66 cells, EN Thorin)
- 4 `sheet.main.*` fixtures row 16 byte-updated with locale-correct senses content; rows 0-15 + 17 byte-identical to Phase 16 baseline
- 5 new CSTR-SKILLS-DATA-* tests pass + CSTR-FIX-SKILLS (IT) byte-identical pass + CSTR-FIX-SKILLS-EN (new) pass + 4 CSTR-FIX-MAIN-* pass
- INV-3 atomic single commit closes the phase: STATE.md + ROADMAP.md + REQUIREMENTS.md + 17-VERIFICATION.md all touched per Phase 14/15/16 precedent
- All 5 SC from ROADMAP.md Phase 17 Success Criteria flipped ✓ in 17-VERIFICATION.md
- SHEET-08 / SHEET-09 / SHEET-10 flipped Resolved in REQUIREMENTS.md
- Workspace `pnpm test` exits 0 with test count ≥ 2648 + 17 (Phase 16 baseline + new CS-SK / CR-SK / CSTR-SKILLS-DATA / CSTR-FIX-SKILLS-EN)
- CI Gate 8 socketlib count = 17 preserved (grep-asserted)
- Specs.md + README.md + docs/showcase NOT modified (milestone-close artifacts, Phase 18 scope)
</success_criteria>

<output>
Create `.planning/phases/EVF-17-sheet-skills-tab-skills-tab-data-wiring/17-03-SUMMARY.md` when done.

Include in SUMMARY:
- Four-commit log with hashes: Task 1 (renderer+tests+17 literal extensions), Task 2 (4 main fixtures row 16), Task 3 (new sheet.skills.en.txt + CSTR-FIX-SKILLS-EN), Task 4 (INV-3 atomic ratification)
- CSTR-SKILLS-DATA-1..5 + CSTR-FIX-SKILLS-EN test names + pass evidence
- Fixture byte-diff summary:
  - `sheet.skills.it.txt`: byte-identical (zero diff)
  - `sheet.skills.en.txt`: NEW (cite full 18 rows × 66 cells)
  - 4 `sheet.main.*`: row 16 changed; rows 0-15 + 17 byte-identical to Phase 16 (`git diff` evidence)
- DEFAULT_SKILLS removal evidence (grep)
- 17+ downstream snapshot literal extensions: full list of files touched (mirror Phase 16 16-03-SUMMARY §"11 downstream snapshot-literal fixups" table)
- DE abbreviation choice (NCH vs UNT for Investigation) — document rationale (matched SKILL_NAMES.inv.de 'Nachforschung')
- Workspace test count delta (pre 2648 → post X)
- CI Gate 8 grep evidence (= 17)
- 5/5 SC + 3/3 REQ check evidence
- INV-3 atomic commit hash (Task 4)
- Forward pointer to Phase 18 milestone close:
  - Specs.md v0.9.12 → v0.9.13 version bump + changelog entry + INV-3 pre-bump checklist
  - README.md badge + showcase v0.9.13 stat strip + hero stat
  - INV-3 milestone-close atomic commit (Specs.md + README + showcase + STATE + ROADMAP + REQUIREMENTS + 18-VERIFICATION + v0.9.13-MILESTONE-AUDIT)
  - Also: Phase-14.1 spec-drift polish work consolidated into Phase 18 (per ROADMAP v0.9.13 §Phase Details)
</output>
