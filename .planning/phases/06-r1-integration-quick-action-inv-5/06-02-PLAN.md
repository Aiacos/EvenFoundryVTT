---
phase: 06-r1-integration-quick-action-inv-5
plan: 02
type: execute
wave: 1
depends_on: ["06-01"]
files_modified:
  - packages/g2-app/src/engine/panel-router.ts
  - packages/g2-app/src/engine/__tests__/panel-router.test.ts
  - packages/g2-app/src/panels/quick-action-menu-panel.ts
  - packages/g2-app/src/panels/__tests__/quick-action-menu-panel.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - packages/shared-render/src/fixtures/quick-action.base.it.txt
  - packages/shared-render/src/fixtures/quick-action.combat-suspended.it.txt
  - packages/shared-render/src/fixtures/quick-action.language-submenu.it.txt
  - packages/shared-render/src/fixtures/quick-action.base.de.txt
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "Long-press from any mounted panel mounts the QuickActionMenuPanel at z=2 via `PanelRouter.pushOverlay()`."
    - "Quick Action menu renders 9 items (`[S][C][L][B][I][A][M][N][X]`) with active-row `▶ ` prefix and locale-aware labels."
    - "Tapping `[N] Language` opens the sub-menu rendering the 7 entries from `LOCALE_MENU`."
    - "Selecting a locale in the sub-menu calls `persistLocaleOverride` AND emits `LocaleEventEmitter.emit('changed', code)`."
    - "Long-press while the menu is mounted calls `popOverlay()` which destroys z=2 + restores the suspended panel (if any) atomically (single rebuildPageContainer call)."
    - "`panel-gesture-bus.size()` is exactly 1 at any steady state during the menu lifecycle (INV-5 architectural enforcement)."
    - "QuickActionMenuPanel filename matches `*-panel.ts` glob but is filtered out of `discoverPanels()` via empty `navKey` (relaxed schema to `z.string().max(1)`)."
  artifacts:
    - path: "packages/g2-app/src/panels/quick-action-menu-panel.ts"
      provides: "QuickActionMenuPanel class implementing OverlayPanel + static meta with navKey '' + 9-item main mode + language sub-menu"
      contains: "class QuickActionMenuPanel"
    - path: "packages/g2-app/src/engine/panel-router.ts"
      provides: "PanelRouter.pushOverlay(panel) + popOverlay() with overlayStack suspension semantics"
      contains: "pushOverlay"
    - path: "packages/shared-render/src/fixtures/quick-action.base.it.txt"
      provides: "INV-1 fixture: menu open, [S] active, IT locale"
    - path: "packages/shared-render/src/fixtures/quick-action.combat-suspended.it.txt"
      provides: "INV-1 fixture: menu open over CombatTrackerPanel (suspension state preserved)"
    - path: "packages/shared-render/src/fixtures/quick-action.language-submenu.it.txt"
      provides: "INV-1 fixture: [N] tapped, sub-menu showing 7 locales"
    - path: "packages/shared-render/src/fixtures/quick-action.base.de.txt"
      provides: "INV-1 fixture: DE locale stress (longest German labels)"
  key_links:
    - from: "packages/g2-app/src/panels/quick-action-menu-panel.ts"
      to: "packages/g2-app/src/locale/locale-override.ts"
      via: "persistLocaleOverride(bridge, code) in [N] Language tap handler"
      pattern: "persistLocaleOverride\\("
    - from: "packages/g2-app/src/panels/quick-action-menu-panel.ts"
      to: "packages/g2-app/src/locale/locale-events.ts"
      via: "localeEvents.emit('changed', code) AFTER persist (suspended panels re-render on restore)"
      pattern: "localeEvents\\.emit\\("
    - from: "packages/g2-app/src/engine/panel-router.ts"
      to: "packages/g2-app/src/engine/layer-manager.ts"
      via: "layerManager.bundle([destroy z=2, mount z=2 menu]) single atomic flush"
      pattern: "layerManager\\.bundle\\("
---

<objective>
Wave 1: ship the QuickActionMenuPanel (9-item main + 7-item Language sub-menu) and extend PanelRouter with an `overlayStack` for `pushOverlay/popOverlay` suspension semantics. Wires the `[N] Language` flow end-to-end through Plan 06-01's `LocaleEventEmitter` so suspended panels re-render with the new locale on restore.

Purpose: NAV-02 closure — Quick Action menu is reachable from every mounted state, and the differential demolish + container budget invariants from Phase 4b stay intact.
Output: 1 new panel class + 1 router extension + 4 INV-1 fixtures + ~10 new i18n keys + ~22 unit tests.

Open Question 2 resolution (planner-locked per objective directive): relax `PanelMetaSchema.navKey` to `z.string().max(1)` (allow empty) AND filter empty-navKey entries out of `discoverPanels()` result. Keeps the `quick-action-menu-panel.ts` filename consistent while preventing the menu from registering as a player-navigable panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-CONTEXT.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-RESEARCH.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-UI-SPEC.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-01-PLAN.md
@packages/g2-app/src/engine/panel-router.ts
@packages/g2-app/src/engine/layer-manager.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/g2-app/src/engine/panel-gesture-bus.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/panels/combat-tracker-panel.ts
@packages/g2-app/src/locale/locale-menu.ts
@packages/g2-app/src/locale/locale-override.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/shared-render/src/fixtures/conc-modal.open.it.txt

<interfaces>
<!-- Contracts the executor must implement against. -->

From Plan 06-01 outputs (must be on disk before this plan runs):
- `LocaleEventEmitter` class in `packages/g2-app/src/locale/locale-events.ts` with `on('changed', cb): () => void`, `emit('changed', code): void`, `size(): number`.
- `R1GesturePayloadSchema` exported from `@evf/shared-protocol`.
- `LayerManager.getTopLayer()` accessor.
- `Layer.getR1Hints?()` optional method.
- 6 new shared chip-vocabulary i18n keys (`hud_r1_default_tap/scroll/long`, `hud_r1_boot_label`, `hud_r1_boot_error_label`, `inv5_chip_tooltip`).

From `packages/g2-app/src/engine/panel-router.ts`:
- `PanelMetaSchema = z.object({ id: min(1), title: { it, en, de? }, navKey: z.string().length(1), requiredCaps: z.array(z.string()).optional(), defaultTab: z.string().optional() })`.
- `class PanelRouter` exposes `discoverPanels()`, `openPanel(id, deps)`, `closeActivePanel()`, `isPanelOpen(id)`, `getRegistrySize()`.
- Private fields: `registry: Map<string, RegistryEntry>`, `activePanel`, `activeId`, `_cachedLayerManager`.
- Panel construction in `openPanel`: `new entry.Cls(deps.bridge, deps.gestureBus, deps.locale)` — 3-arg signature.

From `packages/g2-app/src/panels/concentration-drop-modal.ts` (exemplar — read VERBATIM):
- Panel class implements `OverlayPanel` directly (no base class).
- `onMount()` body: `this.unsubscribe = this.gestureBus.subscribe((g) => this.onEvent(g));`.
- `onUnmount()` body: `if (this.unsubscribe !== null) { this.unsubscribe(); this.unsubscribe = null; }`.
- `onEvent(gesture)` switches on `gesture.kind`; ignores unknown.
- `draw()` issues SINGLE `bridge.textContainerUpgrade({ containerName: 'overlay-block', content: lines.join('\n') })`.
- `getContainerCount(): { image: 0; text: 1 }`.
- `_innerRow(text)` + `padRightUnicode(value, width)` helpers use code-point counting (`[...str].length`), NOT `.length`.

From `packages/g2-app/src/locale/locale-menu.ts`:
- `LOCALE_MENU` is a 7-entry readonly tuple — `{ code, nativeLabel, budget }[]` (auto, it, en, de, es, fr, pt-br).

From `packages/g2-app/src/locale/locale-override.ts`:
- `persistLocaleOverride(bridge, locale: LocaleOverride): Promise<void>` — never throws; logs and swallows.

From `packages/g2-app/src/panels/combat-tracker-panel.ts` (and other Phase 5 panels — read for `PanelMeta` shape):
- `static meta: PanelMeta = { id: 'combat-tracker', title: {...}, navKey: 'C', requiredCaps: [] };`.

From `packages/g2-app/src/engine/layer-manager.ts`:
- `bundle(ops)` enforces differential demolish + container budget + capture invariant + OverlayPanel lifecycle.
- Atomic multi-op: `bundle([{ type: 'destroy', z: ZIndex.Z2_OVERLAY }, { type: 'mount', z: ZIndex.Z2_OVERLAY, layer: nextPanel }])` is a SINGLE bridge flush (RESEARCH §Q3).

UI-SPEC reminders (canonical layout — see `06-UI-SPEC.md` §1 for full mockup):
- Menu width: 70 chars outer (`┌`...`┐`); inner content 66 chars.
- Item row format: `[2-space indent OR `▶ ` active marker][nav-key `[X] ` 4 chars][2-space gap][label padded to 22 chars][trailing space padding to 66]`.
- Longest German label `Schließen` (9) — fits inside the 22-char label budget with slack.
- Footer hint lines (3 lines, ≤66 chars each): `→ scroll = …`, `→ tap = …`, `→ long-press = annulla`.
- Sub-menu nav-keys (`A I E D S F P`) MUST be distinct from main-menu nav-keys (`S C L B I A M N X`) — verified in UI-SPEC §2.2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PanelMetaSchema relax + PanelRouter overlay stack (pushOverlay / popOverlay)</name>
  <files>
    packages/g2-app/src/engine/panel-router.ts,
    packages/g2-app/src/engine/__tests__/panel-router.test.ts
  </files>
  <behavior>
    - panel-router.test.ts (PRT-NK-01): existing Phase 5 panel discovery still passes — the 5 player-navigable panels (CharSheet/Combat/Log/Inv/Spell) all register; `getRegistrySize() === 5`.
    - panel-router.test.ts (PRT-NK-02): a stub panel with `navKey: ''` is silently filtered out of the registry (NOT counted in `getRegistrySize()`), even though `PanelMetaSchema.safeParse` accepts the meta. `console.warn` NOT called for navKey filtering (this is expected exclusion, not malformed input).
    - panel-router.test.ts (PRT-NK-03): a stub panel with `navKey: 'XX'` (2 chars) IS rejected by `PanelMetaSchema.safeParse` (`max(1)` constraint) → `console.warn('invalid meta')` + excluded.
    - panel-router.test.ts (PRT-PUSH-01): `pushOverlay(panel, lm)` with no current z=2 → single bundle call mounts panel at z=2; `overlayStack.length === 0` (nothing to suspend).
    - panel-router.test.ts (PRT-PUSH-02): `pushOverlay(menuPanel, lm)` while CharacterSheetPanel is mounted at z=2 → SINGLE atomic `bundle` call with `[{type:'destroy',z:2}, {type:'mount',z:2,layer:menuPanel}]` (RESEARCH Pitfall 3); `overlayStack` now contains `[charSheetPanel]`; `rebuildPageContainer` called exactly ONCE for the swap (verify via `mockBridge.rebuildPageContainer.mock.calls.length` delta).
    - panel-router.test.ts (PRT-POP-01): `popOverlay(lm)` with non-empty stack → atomic bundle destroys menu + remounts suspended panel; `overlayStack.length === 0` post-call.
    - panel-router.test.ts (PRT-POP-02): `popOverlay(lm)` with empty stack → bundle destroys z=2 only (no mount op); differential demolish auto-restores z=0.5 IdleInfillLayer via existing LayerManager logic.
    - panel-router.test.ts (PRT-POP-03): `popOverlay(lm)` when no z=2 is mounted at all → idempotent no-op + `console.warn('popOverlay: no z=2 mounted')` (defensive guard).
    - panel-router.test.ts (PRT-BUS-01): `panel-gesture-bus.size()` invariant — after `pushOverlay(menu)` over an active CharSheet: `bus.size() === 1` (CharSheet's subscription released via `onUnmount`, menu's subscription acquired via `onMount`); NEVER 0 (transient ok during the bundle await, but observable post-bundle is 1) and NEVER 2 (INV-5 enforcement).
    - panel-router.test.ts (PRT-BUS-02): after `popOverlay()` restoring CharSheet: `bus.size() === 1` (menu released, CharSheet re-acquired).
  </behavior>
  <action>
    Write the 9 new PRT-* tests FIRST in `panel-router.test.ts` (extend existing file with new `describe` blocks: `'PanelRouter.navKey filtering (PRT-NK-*)'` + `'PanelRouter.pushOverlay/popOverlay (PRT-PUSH-* / PRT-POP-* / PRT-BUS-*)'`). Use `TestablePanelRouter` pattern from Plan 05 (set mock modules via `setMockModules()`). Mock bridge from `05-panel-integration-smoke.test.ts` (`makeMockBridge()`).

    Run RED — confirm tests fail with `pushOverlay is not a function` / `overlayStack is undefined` / navKey-length errors.

    Modify `packages/g2-app/src/engine/panel-router.ts`:
      1. **Relax `PanelMetaSchema.navKey`**: change `z.string().length(1)` to `z.string().max(1)`. Keep all other fields identical. JSDoc update: "Single-character Quick Action menu key (e.g. `'S'` for Sheet). Empty string `''` is reserved for system overlays opened directly via `pushOverlay()` (e.g. QuickActionMenuPanel) — these are filtered out of `discoverPanels()` registry so they never appear in the user-navigable nav set."
      2. **Filter empty navKey in `discoverPanels`**: after `parseResult.success` branch, BEFORE `this.registry.set(meta.id, …)`, add `if (meta.navKey === '') { continue; }` — silent skip (no warn).
      3. **Add `private overlayStack: OverlayPanel[] = []` field**.
      4. **Add `pushOverlay(panel: OverlayPanel, layerManager: LayerManager): Promise<void>`** per RESEARCH §Q3 single-atomic-bundle pattern:
         - Cache `_cachedLayerManager = layerManager`.
         - `const current = layerManager.getLayer(ZIndex.Z2_OVERLAY);`
         - If `current !== undefined && isOverlayPanel(current)`: push `current as OverlayPanel` onto `overlayStack`; build ops = `[{type:'destroy', z:Z2_OVERLAY}, {type:'mount', z:Z2_OVERLAY, layer:panel}]`.
         - Else: build ops = `[{type:'mount', z:Z2_OVERLAY, layer:panel}]`.
         - `await layerManager.bundle(ops);` — SINGLE atomic flush (avoids the dual-bundle flicker per RESEARCH Pitfall 3).
         - Update `this.activePanel = panel; this.activeId = (panel as { id: string }).id;`.
      5. **Add `popOverlay(layerManager: LayerManager): Promise<void>`**:
         - If `layerManager.getLayer(ZIndex.Z2_OVERLAY) === undefined`: `console.warn('[PanelRouter] popOverlay: no z=2 mounted — idempotent no-op')` + return (RESEARCH Pitfall 4).
         - `const restored = this.overlayStack.pop();` (returns `undefined` on empty stack — JS Array semantics).
         - If `restored !== undefined`: `await layerManager.bundle([{type:'destroy', z:Z2_OVERLAY}, {type:'mount', z:Z2_OVERLAY, layer:restored}]);` AND set `this.activePanel = restored; this.activeId = restored.id;`.
         - Else: `await layerManager.bundle([{type:'destroy', z:Z2_OVERLAY}]);` (differential demolish restores z=0.5 automatically per ADR-0009 Amd 1) AND set `this.activePanel = null; this.activeId = null;`.
      6. Import `isOverlayPanel` from `./overlay-panel.js` and `OverlayPanel` from `./layer-types.js` (verify whether already imported; add if missing).
      7. JSDoc on `pushOverlay`: cite RESEARCH §Q3 (suspension via `onUnmount → onMount` round-trip preserves instance fields by reference; PanelRouter holds the panel reference in `overlayStack` so GC doesn't reclaim it) + Pitfall 3 (single atomic bundle).
      8. JSDoc on `popOverlay`: cite Pitfall 4 (empty-stack defensive guard) + ADR-0009 Amd 1 (differential demolish restores z=0.5 on empty-stack pop).

    Re-run tests — verify GREEN.

    Negative grep gate: `! grep -E "navKey: z.string\\(\\)\\.length\\(1\\)" packages/g2-app/src/engine/panel-router.ts` (proves the relax landed).

    Commit:
      `git add packages/g2-app/src/engine/panel-router.ts packages/g2-app/src/engine/__tests__/panel-router.test.ts`
      `git commit -m "feat(06-02): PanelMetaSchema navKey relax + PanelRouter.pushOverlay/popOverlay (NAV-02 overlay stack)"`
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run panel-router && ! grep -E "navKey: z.string\(\)\.length\(1\)" packages/g2-app/src/engine/panel-router.ts</automated>
  </verify>
  <done>
    - 9 new PRT-* tests green + all pre-existing PRT-* tests still green.
    - `pnpm typecheck` exits 0.
    - `grep -c "pushOverlay" packages/g2-app/src/engine/panel-router.ts` ≥ 2; `grep -c "popOverlay" ≥ 2`; `grep -c "overlayStack" ≥ 2`.
    - Negative grep gate above passes (schema relaxed to `max(1)`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuickActionMenuPanel — 9-item main mode + `[N] Language` sub-menu + chip hints + 10 i18n keys</name>
  <files>
    packages/g2-app/src/panels/quick-action-menu-panel.ts,
    packages/g2-app/src/panels/__tests__/quick-action-menu-panel.test.ts,
    packages/g2-app/src/status-hud/i18n-budgets.ts
  </files>
  <behavior>
    - quick-action-menu-panel.test.ts (QAM-01): `QuickActionMenuPanel.meta` parses via `PanelMetaSchema.safeParse` successfully with `navKey: ''`.
    - QAM-02: instance conforms to `OverlayPanel` — `isOverlayPanel(instance) === true`; `getContainerCount() === { image: 0, text: 1 }`.
    - QAM-03 (main mode draw): default state renders 9 rows (`[S][C][L][B][I][A][M][N][X]`) with `▶ ` on row 0 (`[S]` active by default); every row exactly 70 visible chars (border-to-border) per UI-SPEC §1.
    - QAM-04 (scroll cycle): `onEvent({kind:'scroll',direction:'down'})` → `activeIndex` increments → next draw moves `▶ ` to row 1 (`[C]`).
    - QAM-05 (scroll wrap-around): from `activeIndex === 8` (`[X]`), scroll down → `activeIndex === 0` (`[S]`) (modulo wrap).
    - QAM-06 (tap on `[N]`): scroll to `[N]` (index 7) → `onEvent({kind:'tap'})` → `mode === 'language'`; next draw renders the 7 LOCALE_MENU entries (UI-SPEC §2.1 nav-keys `A I E D S F P`).
    - QAM-07 (sub-menu nav-keys distinct): the 7 sub-menu nav-keys MUST be `A I E D S F P` (`Auto Italiano English Deutsch Español Français Português`) — verify via parsed draw output; AND assert `setMain.intersection(setSub).size === 4` (or whatever the actual overlap is — note: `A`, `I`, `S` collide with main-menu keys, BUT this is acceptable because the modes are mutually exclusive — comment per UI-SPEC §2.2).
    - QAM-08 (sub-menu locale select): in language mode, scroll to `[I]` (Italiano) → `onEvent({kind:'tap'})` → calls `persistLocaleOverride(bridge, 'it')` AND `localeEvents.emit('changed', 'it')` AND `mode === 'main'` (returns to main menu after selection) AND `bridge.setLocalStorage` called with `('view.locale.override', 'it')`.
    - QAM-09 (sub-menu Auto): tap `[A]` in language mode → `persistLocaleOverride(bridge, 'auto')` + `localeEvents.emit('changed', 'auto')`.
    - QAM-10 (long-press cancel from main): in main mode, `onEvent({kind:'long-press'})` → calls injected `onClose()` (which the dispatcher binds to `panelRouter.popOverlay`).
    - QAM-11 (long-press cancel from sub-menu): in language mode, `onEvent({kind:'long-press'})` → `mode === 'main'` (returns to main, does NOT close menu) — per UI-SPEC §1 footer hint "long-press = annulla" interpreted as "back one level" when in sub-menu.
    - QAM-12 (active-item tap actions): tap on `[S]` from main mode → calls `onClose()` AND invokes injected `onNavigate('character-sheet')` callback. Similar for `[C] → 'combat-tracker'`, `[L] → 'log'`, `[B] → 'spellbook'`, `[I] → 'inventory'`, `[M]` → `onMapModeToggle()` callback, `[A]` → `onAction()` callback (Phase 7 stub), `[X]` → `onClose()` only.
    - QAM-13 (getR1Hints — main mode): `panel.getR1Hints()` returns `{ tap: <local 'apri'>, scroll: <local 'voce'>, longPressLabel: <local 'annulla'> }` using `getLabel` for each (locale-aware).
    - QAM-14 (getR1Hints — language sub-menu): in language mode, `getR1Hints()` returns hint values for the sub-menu (`tap: 'applica'/'apply'`, `scroll: 'lingua'/'language'`, `longPressLabel: 'indietro'/'back'`).
    - QAM-FIX-01 — INV-1 fixture `quick-action.base.it.txt`: `matchAsciiFixture` round-trip on the rendered draw output (IT locale, `[S]` active, no suspension).
    - QAM-FIX-02 — INV-1 fixture `quick-action.combat-suspended.it.txt`: full-page 96×24 composition of menu over a mock CombatTrackerPanel context (the suspended-panel state is preserved underneath — the panel itself is NOT rendered here because it's unmounted; the fixture documents the menu in its full 70-char width centered on the 96-char page).
    - QAM-FIX-03 — INV-1 fixture `quick-action.language-submenu.it.txt`: 7 LOCALE_MENU entries; `[A] Auto` row marked `▶ ` (current saved override = `'auto'`).
    - QAM-FIX-04 — INV-1 fixture `quick-action.base.de.txt`: DE locale stress (`Schließen` longest label fits within 22-char budget per UI-SPEC §1.3 verification).
  </behavior>
  <action>
    Append the 10 new i18n keys to `packages/g2-app/src/status-hud/i18n-budgets.ts` HUD_WIDTH_BUDGETS (max budgets per UI-SPEC §6 table):
      - `quick_menu_title: { it: 'AZIONE RAPIDA', en: 'QUICK ACTION', de: 'SCHNELLAKTION', max: 22 }`
      - `quick_lang_submenu_title: { it: 'LINGUA', en: 'LANGUAGE', de: 'SPRACHE', max: 22 }`
      - `quick_item_sheet: { it: 'Scheda', en: 'Sheet', de: 'Blatt', max: 22 }`
      - `quick_item_combat: { it: 'Combatt', en: 'Combat', de: 'Kampf', max: 22 }`
      - `quick_item_log: { it: 'Log', en: 'Log', de: 'Log', max: 22 }`
      - `quick_item_book: { it: 'Libro', en: 'Book', de: 'Buch', max: 22 }`
      - `quick_item_inventory: { it: 'Inventario', en: 'Inventory', de: 'Inventar', max: 22 }`
      - `quick_item_action: { it: 'Azione', en: 'Action', de: 'Aktion', max: 22 }`
      - `quick_item_map: { it: 'Mappa', en: 'Map mode', de: 'Karte', max: 22 }`
      - `quick_item_language: { it: 'Lingua', en: 'Language', de: 'Sprache', max: 22 }`
      - `quick_item_close: { it: 'Chiudi', en: 'Close', de: 'Schließen', max: 22 }`
      - `quick_hint_scroll: { it: 'scroll = cambia voce attiva', en: 'scroll = change selected item', de: 'scroll = Eintrag wählen', max: 66 }`
      - `quick_hint_tap: { it: 'tap = apri voce', en: 'tap = open item', de: 'tap = öffnen', max: 66 }`
      - `quick_hint_long: { it: 'long-press = annulla', en: 'long-press = cancel', de: 'long-press = abbrechen', max: 66 }`
      - `quick_r1_main_tap: { it: 'apri', en: 'open', de: 'öffnen', max: 22 }`
      - `quick_r1_main_scroll: { it: 'voce', en: 'item', de: 'Eintrag', max: 22 }`
      - `quick_r1_main_long: { it: 'annulla', en: 'cancel', de: 'Abbruch', max: 22 }`
      - `quick_r1_lang_tap: { it: 'applica', en: 'apply', de: 'anwenden', max: 22 }`
      - `quick_r1_lang_scroll: { it: 'lingua', en: 'language', de: 'Sprache', max: 22 }`
      - `quick_r1_lang_long: { it: 'indietro', en: 'back', de: 'zurück', max: 22 }`
    (Adjust upward to 20 keys total; the audit table in must_haves above said "~10" — the actual count is 20 for full coverage. `pnpm typecheck` will fail if any string exceeds its `max`.)

    Run typecheck — verify GREEN on i18n.

    Write tests FIRST in `quick-action-menu-panel.test.ts`. Harness mirrors `concentration-drop-modal.test.ts`:
      - Mock bridge via `vi.fn()` for `textContainerUpgrade` + `setLocalStorage`.
      - Real `PanelGestureBus`.
      - Real `LocaleEventEmitter`.
      - Injected callbacks: `onClose: vi.fn()`, `onNavigate: vi.fn()`, `onMapModeToggle: vi.fn()`, `onAction: vi.fn()`.
    Use `matchAsciiFixture(AsciiGrid.fromString(payload.content), resolve(fixtureDir(), 'quick-action.base.it.txt'))` pattern from `05-panel-integration-smoke.test.ts` for QAM-FIX-01..04.

    Run RED.

    Implement `packages/g2-app/src/panels/quick-action-menu-panel.ts` mirroring `concentration-drop-modal.ts` structure:
      - Imports: `EvenAppBridge`, `TextContainerUpgrade` from `@evenrealities/even_hub_sdk`; `OverlayPanel`, `R1Gesture` from `../engine/layer-types.js`; `PanelGestureBus` from `../engine/panel-gesture-bus.js`; `PanelMeta` from `../engine/panel-router.js`; `getLabel`, `HudLocale` from `../status-hud/i18n-budgets.js`; `LOCALE_MENU` from `../locale/locale-menu.js`; `persistLocaleOverride`, `LocaleOverride` from `../locale/locale-override.js`; `LocaleEventEmitter` from `../locale/locale-events.js`.
      - Module-level constants:
        - `QUICK_MENU_CONTAINER_NAME = 'overlay-block' as const` — same Strategy A container as conc-modal.
        - `MENU_WIDTH = 70` (outer); `INNER_WIDTH = 66`; `LABEL_BUDGET = 22`.
        - `MAIN_ITEMS = [{ key: 'S', i18n: 'quick_item_sheet', action: 'navigate', target: 'character-sheet' }, { key: 'C', i18n: 'quick_item_combat', action: 'navigate', target: 'combat-tracker' }, { key: 'L', i18n: 'quick_item_log', action: 'navigate', target: 'log' }, { key: 'B', i18n: 'quick_item_book', action: 'navigate', target: 'spellbook' }, { key: 'I', i18n: 'quick_item_inventory', action: 'navigate', target: 'inventory' }, { key: 'A', i18n: 'quick_item_action', action: 'action-stub' }, { key: 'M', i18n: 'quick_item_map', action: 'map-mode-toggle' }, { key: 'N', i18n: 'quick_item_language', action: 'open-sub-menu' }, { key: 'X', i18n: 'quick_item_close', action: 'close' }] as const;` — note 9 entries.
        - `SUB_MENU_KEYS = ['A', 'I', 'E', 'D', 'S', 'F', 'P'] as const;` — per UI-SPEC §2.1; positionally maps to `LOCALE_MENU` entries.
      - Class signature: `class QuickActionMenuPanel implements OverlayPanel`. Constructor takes `(bridge, gestureBus, locale, currentLocaleOverride: LocaleOverride, localeEvents: LocaleEventEmitter, callbacks: { onClose, onNavigate, onMapModeToggle, onAction })`.
      - `static meta: PanelMeta = { id: 'quick-action-menu', title: { it: 'Azione Rapida', en: 'Quick Action', de: 'Schnellaktion' }, navKey: '', requiredCaps: [] };`.
      - `readonly id = 'quick-action-menu';`.
      - Private state: `mode: 'main' | 'language' = 'main'`; `activeIndex = 0`; `unsubscribe: (() => void) | null = null`.
      - `onMount()` / `onUnmount()` — verbatim from conc-modal pattern (subscribe / unsubscribe with null-guard idempotency).
      - `onEvent(gesture)` dispatch table:
        - `kind === 'scroll'`: `_cycleIndex(gesture.direction === 'up' ? -1 : 1)` + `void this.draw()`.
        - `kind === 'tap'`: `_activateCurrentItem()` (branches on mode).
        - `kind === 'long-press'`: if mode === 'language': `mode = 'main'; activeIndex = 7; void this.draw();` (return to main, focus `[N]`). Else: `callbacks.onClose();` (close menu — outer dispatcher pops the overlay).
        - other kinds (`double-tap`): no-op.
      - `_cycleIndex(delta)`: `const len = mode === 'main' ? MAIN_ITEMS.length : LOCALE_MENU.length; this.activeIndex = ((this.activeIndex + delta) % len + len) % len;` (modulo wrap, handles negative delta).
      - `_activateCurrentItem()`:
        - If mode === 'main': switch on `MAIN_ITEMS[activeIndex].action`:
          - `'navigate'`: call `callbacks.onNavigate(MAIN_ITEMS[activeIndex].target)` + `callbacks.onClose()` (dispatcher closes menu then opens the target panel via `PanelRouter.openPanel`).
          - `'open-sub-menu'`: `this.mode = 'language';` `this.activeIndex = LOCALE_MENU.findIndex(e => e.code === this.currentLocaleOverride);` (focus saved override). If not found, default to 0.
          - `'map-mode-toggle'`: `callbacks.onMapModeToggle()` + `callbacks.onClose()`.
          - `'action-stub'`: `callbacks.onAction()` + `callbacks.onClose()` (Phase 7 will wire real `[A]` panel).
          - `'close'`: `callbacks.onClose()`.
        - If mode === 'language': `const code = LOCALE_MENU[activeIndex].code; await persistLocaleOverride(this.bridge, code); this.localeEvents.emit('changed', code); this.mode = 'main'; this.activeIndex = 7;` (return to main, keep `[N]` focused).
      - `getR1Hints()` — locale-aware via `getLabel`:
        - main: `{ tap: getLabel('quick_r1_main_tap', locale), scroll: getLabel('quick_r1_main_scroll', locale), longPressLabel: getLabel('quick_r1_main_long', locale) }`.
        - language: `{ tap: getLabel('quick_r1_lang_tap', locale), scroll: getLabel('quick_r1_lang_scroll', locale), longPressLabel: getLabel('quick_r1_lang_long', locale) }`.
      - `getContainerCount(): { image: 0; text: 1 }`.
      - `draw()`: build `lines: string[]` (rows top border `┌…┐` + title + spacer + 9 (or 7) item rows + bottom border `└…┘` + 3 footer hint lines). Each row exactly 70 visible chars; use `_innerRow(text)` + `padRightUnicode` helpers (code-point counting, NOT `.length`).
      - `destroy(): void` — no-op (subscription released in `onUnmount`).
      - Helper `_buildLines()`: pure function over `this.mode` + `this.activeIndex` + `this.locale` + `this.currentLocaleOverride`. Returns the row array.
      - Helper `_buildItemRow(prefix, navKey, label)`: `prefix` is `'▶ '` (active) or `'  '` (inactive); `navKey` is `[X]` 4-char cell; `label` is the localised string truncated/padded to `LABEL_BUDGET` (22) via the existing `truncateField`/`padRightUnicode` pattern. Return exactly 70-char row.
      - JSDoc cites: CONTEXT.md §Area 2, UI-SPEC §1+§2, RESEARCH §Q3 (suspension via instance-field preservation), RESEARCH §Pitfall 7 resolution (separate `LocaleEventEmitter` per planner-locked Q1).

    Run tests + INV-1 fixtures (use `vitest --update` ONLY on first run to seed; commit the seeded fixtures verbatim). Verify GREEN.

    Commit:
      `git add packages/g2-app/src/panels/quick-action-menu-panel.ts packages/g2-app/src/panels/__tests__/quick-action-menu-panel.test.ts packages/g2-app/src/status-hud/i18n-budgets.ts packages/shared-render/src/fixtures/quick-action.*.txt`
      `git commit -m "feat(06-02): QuickActionMenuPanel + 4 INV-1 fixtures + 20 i18n keys (NAV-02)"`
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run quick-action-menu-panel && pnpm typecheck</automated>
  </verify>
  <done>
    - QAM-01..14 + QAM-FIX-01..04 green (18 tests total).
    - 4 INV-1 fixtures in `packages/shared-render/src/fixtures/` committed (NOT in test/__tests__ dir).
    - `grep -c "quick_item_close" packages/g2-app/src/status-hud/i18n-budgets.ts` ≥ 1; `pnpm typecheck` exits 0 (the `satisfies` width-budget gate confirms all 20 new entries within budget).
    - `grep -c "class QuickActionMenuPanel" packages/g2-app/src/panels/quick-action-menu-panel.ts` ≥ 1.
    - QAM-08 verifies the `persistLocaleOverride` + `localeEvents.emit('changed', ...)` wiring per planner-locked Q1 resolution (separate emitter, NOT R1Gesture extension).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QuickActionMenuPanel → Even Hub kv | `persistLocaleOverride` writes user-selected locale; defensive validation in `loadLocaleOverride` at next boot. |
| `LocaleEventEmitter` fan-out → suspended panel | Trusted in-process channel; panels self-validate locale on `'changed'` emission before re-render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | User selects an unrecognised locale code via menu | mitigate | `LOCALE_MENU` is a typed tuple; QuickActionMenuPanel can only emit the 7 hard-coded codes. `loadLocaleOverride` whitelist normalises anything outside the set to `'auto'` (Phase 5 mitigation, carry-forward). |
| T-06-02-02 | Tampering | `setLocalStorage` write fails silently | accept | `persistLocaleOverride` swallows rejection per Phase 5 contract (cosmetic persistence — in-memory locale unaffected). |
| T-06-02-03 | DoS | Bus subscription leak across menu push/pop cycles | mitigate | Menu's `onMount` subscribes, `onUnmount` unsubscribes (null-guard idempotency); PRT-BUS-01/02 + QAM tests assert `bus.size() === 1` invariant. T-4b-01-03 carry. |
| T-06-02-04 | Repudiation | User claims they didn't change locale | accept | No audit trail required — single-tenant homelab, device-local override. |
| T-06-02-05 | Elevation of Privilege | QuickActionMenuPanel bypasses PanelRouter capability gate | mitigate | `meta.requiredCaps: []` declared (no caps needed); even if extended, `pushOverlay` does NOT enforce caps (it's a system overlay, not user-navigable). Risk accepted via architectural constraint: only the long-press dispatcher in boot-engine-core can construct this panel (Plan 06-04 wires it). |
</threat_model>

<verification>
- `pnpm test --filter @evf/g2-app` workspace-side green (no regressions in 1149+ tests).
- `pnpm typecheck && pnpm lint:ci` exit 0.
- Both task commits land sequential with conventional-commits subjects.
- Negative grep gate: `! grep -E "z.string\(\)\.length\(1\)" packages/g2-app/src/engine/panel-router.ts` — schema relaxed.
- Negative grep gate: `! grep -E "extend\s*R1Gesture|kind: 'locale.changed'" packages/g2-app/src/engine/layer-types.ts` — R1Gesture union NOT extended (planner-locked Q1 resolution preserved via separate emitter).
- INV-1 fixtures present in `packages/shared-render/src/fixtures/quick-action.{base,combat-suspended,language-submenu}.it.txt` + `quick-action.base.de.txt`.
- `bus.size() === 1` invariant verified via PRT-BUS-01/02 + QAM tests across menu push/pop lifecycle (INV-5 architectural enforcement).
</verification>

<success_criteria>
- ✅ NAV-02 closure: 9-item Quick Action menu + 7-item Language sub-menu reachable via `PanelRouter.pushOverlay(menu)` from any active panel; long-press cancels (back-one-level in sub-menu, close in main mode).
- ✅ INV-5 architectural enforcement: `panel-gesture-bus.size() === 1` at every steady state during menu lifecycle.
- ✅ I18N-02 wiring: `[N] Language` sub-menu calls `persistLocaleOverride` + `LocaleEventEmitter.emit('changed', code)` — suspended panels re-render with the new locale on restore (Plan 06-04 verifies cross-overlay reachability with locale change).
- ✅ INV-1 layout integrity: 4 INV-1 fixtures (IT main, IT combat-suspended, IT language-submenu, DE stress) verified via `matchAsciiFixture` round-trip.
- ✅ Hardware-pending: SC-06-03 — menu-open latency p50 ≤ 200 ms on real G2 + R1 (carry-forward; closes via `pnpm --filter @evf/validation-harness validate:all` when Even Hub access granted).
- ✅ No new npm dependencies; only existing libs (CONTEXT.md §Area 6).
</success_criteria>

<output>
After completion, create `.planning/phases/06-r1-integration-quick-action-inv-5/06-02-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`. Self-check MUST list:
- packages/g2-app/src/panels/quick-action-menu-panel.ts FOUND
- packages/g2-app/src/engine/panel-router.ts contains `pushOverlay` + `popOverlay`
- 4 INV-1 fixtures in shared-render/src/fixtures/ FOUND
- 20 new i18n keys added to i18n-budgets.ts (typecheck exits 0)
- 2 task commits in git log
</output>
