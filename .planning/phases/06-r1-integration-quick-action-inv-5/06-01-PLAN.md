---
phase: 06-r1-integration-quick-action-inv-5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared-protocol/src/payloads/r1.ts
  - packages/shared-protocol/src/index.ts
  - packages/g2-app/src/engine/r1-timings.ts
  - packages/g2-app/src/engine/r1-event-source.ts
  - packages/g2-app/src/engine/layer-manager.ts
  - packages/g2-app/src/engine/layer-types.ts
  - packages/g2-app/src/locale/locale-events.ts
  - packages/g2-app/src/engine/__tests__/r1-timings.test.ts
  - packages/g2-app/src/engine/__tests__/r1-event-source.test.ts
  - packages/g2-app/src/engine/__tests__/layer-manager.test.ts
  - packages/g2-app/src/locale/__tests__/locale-events.test.ts
  - packages/shared-protocol/src/payloads/r1.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - docs/architecture/INVARIANTS.md
  - docs/architecture/README.md
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "Bridge `r1.gesture` envelopes are schema-validated at the WS receive boundary (double trust)."
    - "R1 gestures are routed to exactly one panel handler — the layer returned by `LayerManager.getTopLayer()`."
    - "Empty stack / boot-error active = explicit no-op + `console.warn` (INV-5 zero-handler case)."
    - "`DEFAULT_R1_TIMINGS = { tapMs: 250, doubleTapWindowMs: 350, longPressMs: 600, scrollDebounceMs: 50 }` is exported and importable from `@evf/g2-app/engine/r1-timings`."
    - "`LocaleEventEmitter` is a typed pub/sub bus distinct from `PanelGestureBus` — `R1Gesture` union stays clean."
    - "INV-5 Gesture Determinism is ratified in `docs/architecture/INVARIANTS.md` and indexed from `docs/architecture/README.md`."
  artifacts:
    - path: "packages/shared-protocol/src/payloads/r1.ts"
      provides: "R1GesturePayloadSchema + R1_GESTURE_TYPE constant"
      contains: "R1GesturePayloadSchema"
    - path: "packages/g2-app/src/engine/r1-timings.ts"
      provides: "DEFAULT_R1_TIMINGS frozen const + R1Timings type"
      contains: "DEFAULT_R1_TIMINGS"
    - path: "packages/g2-app/src/engine/r1-event-source.ts"
      provides: "attachR1EventSource(ws, gestureBus, layerManager, timings) → unsubscribe"
      contains: "attachR1EventSource"
    - path: "packages/g2-app/src/engine/layer-manager.ts"
      provides: "LayerManager.getTopLayer(): Layer | null"
      contains: "getTopLayer"
    - path: "packages/g2-app/src/engine/layer-types.ts"
      provides: "Layer.getR1Hints?() optional method on Layer interface"
      contains: "getR1Hints"
    - path: "packages/g2-app/src/locale/locale-events.ts"
      provides: "LocaleEventEmitter class + LocaleChangedListener type"
      contains: "LocaleEventEmitter"
    - path: "docs/architecture/INVARIANTS.md"
      provides: "INV-1..5 consolidated; INV-5 Gesture Determinism ratified"
      contains: "INV-5"
  key_links:
    - from: "packages/g2-app/src/engine/r1-event-source.ts"
      to: "packages/shared-protocol/src/payloads/r1.ts"
      via: "import { R1GesturePayloadSchema, R1_GESTURE_TYPE } from '@evf/shared-protocol'"
      pattern: "R1GesturePayloadSchema\\.safeParse"
    - from: "packages/g2-app/src/engine/r1-event-source.ts"
      to: "packages/g2-app/src/engine/layer-manager.ts"
      via: "layerManager.getTopLayer()"
      pattern: "layerManager\\.getTopLayer\\("
    - from: "docs/architecture/README.md"
      to: "docs/architecture/INVARIANTS.md"
      via: "Index row link"
      pattern: "INVARIANTS\\.md"
---

<objective>
Wave 0 atomic foundation for Phase 6: ship the R1 event source provider, top-of-stack routing accessor, locale event emitter, R1 wire schema, and INV-5 ratification — all the shared contracts that Plans 06-02 / 06-03 / 06-04 consume.

Purpose: keep every downstream plan (menu panel, status chip, reachability harness) free of forward-declaration scavenger hunts. Plans 02–04 import from this plan's outputs verbatim.
Output: 7 source modules + 5 test files + INVARIANTS.md + ADR index update + ~6 new i18n-budgets keys for the boot-error chip variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-CONTEXT.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-RESEARCH.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-UI-SPEC.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-VALIDATION.md
@.planning/REQUIREMENTS.md
@packages/g2-app/src/engine/panel-gesture-bus.ts
@packages/g2-app/src/engine/layer-manager.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/shared-protocol/src/envelope.ts
@packages/shared-protocol/src/payloads/concentration.ts
@packages/g2-app/src/panels/conc-conflict-dispatcher.ts
@packages/g2-app/src/locale/locale-override.ts
@docs/architecture/README.md
@Specs.md

<interfaces>
<!-- Contracts the executor must implement against — extracted from the codebase verbatim. No exploration needed. -->

From `packages/shared-protocol/src/envelope.ts`:
- `EnvelopeSchema = z.object({ proto: z.literal('evf-v1'), seq, ts, type: z.string(), session_id: uuid, payload: z.unknown() })`.
- `type Envelope = z.infer<typeof EnvelopeSchema>`.

From `packages/shared-protocol/src/payloads/concentration.ts` (the exemplar payload pattern this plan mirrors):
- Exports `CONC_CONFLICT_TYPE = 'conc.conflict' as const`, `ConcConflictPayloadSchema = z.object({ … }).strict()`, `type ConcConflictPayload = z.infer<…>`.
- Test file exists at `packages/shared-protocol/src/payloads/concentration.test.ts` covering happy + negative parse cases.

From `packages/g2-app/src/engine/layer-types.ts` (Phase 6 extends — DO NOT remove existing fields):
- `interface Layer { readonly id: string; draw(): Promise<void>; destroy(): void; getCaptureContainer?(): string; getContainerCount?(): { image: number; text: number } }`.
- `enum ZIndex { Z0_MAP = 0, Z0_5_IDLE_INFILL = 0.5, Z1_STATUS_HUD = 1, Z1_5_TOAST = 1.5, Z2_OVERLAY = 2 }`.
- `R1Gesture` discriminated union with `kind: 'tap' | 'scroll' (direction up/down) | 'long-press' | 'double-tap'`.

From `packages/g2-app/src/engine/layer-manager.ts`:
- `class LayerManager` exposes `private readonly layers = new Map<ZIndex, Layer>()`, `bundle(ops)`, `mount(z, layer, requiredCaps)`, `destroy(z)`, `getLayer(z)`, `getCaptureContainerCount()`. Phase 6 adds `getTopLayer()` as a public method — no other touch.

From `packages/g2-app/src/engine/overlay-panel.ts`:
- `isOverlayPanel(layer): layer is OverlayPanel` duck-types on `onMount + onUnmount + onEvent`.

From `packages/g2-app/src/engine/panel-gesture-bus.ts`:
- `class PanelGestureBus { subscribe(fn): () => void; publish(g: R1Gesture): void; size(): number }`.
- Publish snapshots subscribers; per-subscriber try/catch; INV-5 fan-out determinism rests on "exactly one subscriber at a time" architectural contract (NOT bus single-dispatch).

From `packages/g2-app/src/panels/conc-conflict-dispatcher.ts` (the exemplar WS double-trust-boundary pattern this plan mirrors for `r1-event-source.ts`):
- `ws.addEventListener('message', handler)` + return `() => ws.removeEventListener('message', handler)`.
- Inside handler: `try { JSON.parse } → EnvelopeSchema.safeParse → narrow on type === EXPECTED_TYPE → inner schema safeParse → console.warn + return on failure; on success build and act`. Outer `try/catch` belt-and-suspenders.

From `packages/g2-app/src/locale/locale-override.ts`:
- `LOCALE_OVERRIDE_KEY = 'view.locale.override' as const`.
- `type LocaleOverride = 'auto' | HudLocale`.
- `persistLocaleOverride(bridge, locale): Promise<void>` and `loadLocaleOverride(bridge): Promise<LocaleOverride>`.

From `packages/g2-app/src/status-hud/i18n-budgets.ts`:
- `HUD_WIDTH_BUDGETS` is a `const` table keyed by field name; each entry has shape `{ it, en, de, max }` (+ optional es/fr/pt-br for best-effort entries).
- `type HudLocale = 'it' | 'en' | 'de' | 'es' | 'fr' | 'pt-br'`.
- `getLabel(field, locale): string` and `assertWithinBudget(value, field): void` are the only public helpers Phase 6 needs.

Phase 6 conventions inherited from Phase 4b/5 (verified in source):
- Tests colocated under `__tests__/` directory next to the module (e.g. `packages/g2-app/src/engine/__tests__/foo.test.ts`).
- Bus subscribers ALWAYS unsubscribe in `onUnmount` (T-4b-01-03).
- WS handlers ALWAYS validate via `EnvelopeSchema.safeParse` first, then inner payload schema (double trust boundary — Phase 4b pattern).
- No virtual DOM — render via `bridge.textContainerUpgrade` / `bridge.rebuildPageContainer`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R1 wire schema + timing constants + locale event emitter (atomic shared-contract foundation)</name>
  <files>
    packages/shared-protocol/src/payloads/r1.ts,
    packages/shared-protocol/src/payloads/r1.test.ts,
    packages/shared-protocol/src/index.ts,
    packages/g2-app/src/engine/r1-timings.ts,
    packages/g2-app/src/engine/__tests__/r1-timings.test.ts,
    packages/g2-app/src/locale/locale-events.ts,
    packages/g2-app/src/locale/__tests__/locale-events.test.ts
  </files>
  <behavior>
    - r1.test.ts: `R1GesturePayloadSchema.safeParse({ kind: 'tap', timestamp: 1700000000000 }).success === true`.
    - r1.test.ts: `R1GesturePayloadSchema.safeParse({ kind: 'scroll-up', timestamp: 0 }).success === true` for each of `'tap' | 'scroll-up' | 'scroll-down' | 'long-press' | 'double-tap'`.
    - r1.test.ts: `safeParse({ kind: 'unknown', timestamp: 0 }).success === false` (rejected — enum guard).
    - r1.test.ts: `safeParse({ kind: 'tap' }).success === false` (timestamp required).
    - r1.test.ts: `safeParse({ kind: 'tap', timestamp: '0' }).success === false` (timestamp must be integer).
    - r1.test.ts: `R1_GESTURE_TYPE === 'r1.gesture'` exact-literal assertion.
    - r1-timings.test.ts: `DEFAULT_R1_TIMINGS.tapMs === 250 && doubleTapWindowMs === 350 && longPressMs === 600 && scrollDebounceMs === 50` (locked decision from CONTEXT.md per D-Area-1).
    - r1-timings.test.ts: `Object.isFrozen(DEFAULT_R1_TIMINGS) === true` (immutability guard — prevents accidental mutation by hardware-tuning code in SC-06-01 follow-up).
    - r1-timings.test.ts: `DEFAULT_R1_TIMINGS.longPressMs >= 500` regression guard against Specs §3.2 / §10.0.1 GO criterion (`≥500 ms`).
    - locale-events.test.ts (LEM-01): a registered listener receives the emitted code: `emitter.on('changed', cb); emitter.emit('changed', 'it'); expect(cb).toHaveBeenCalledWith('it')`.
    - locale-events.test.ts (LEM-02): unsubscribe returned by `on()` removes the listener: `const off = emitter.on(...); off(); emitter.emit(...); expect(cb).not.toHaveBeenCalled()`.
    - locale-events.test.ts (LEM-03): double-call of unsubscribe is idempotent — no throw.
    - locale-events.test.ts (LEM-04): per-listener `try/catch` isolation — a throwing listener does NOT prevent subsequent listeners from running; `console.warn` called once.
    - locale-events.test.ts (LEM-05): `emitter.size() === 0` after `off()` (parity with PanelGestureBus.size()).
  </behavior>
  <action>
    Write tests FIRST in `r1.test.ts` / `r1-timings.test.ts` / `locale-events.test.ts` (vitest happy-dom env, follow existing test patterns from `packages/shared-protocol/src/payloads/concentration.test.ts` and `packages/g2-app/src/engine/__tests__/panel-gesture-bus.test.ts`). Run `pnpm test --filter @evf/shared-protocol --filter @evf/g2-app` — verify RED (tests fail with module-not-found / undefined-export).

    Implement `packages/shared-protocol/src/payloads/r1.ts` mirroring `concentration.ts`:
      - `export const R1_GESTURE_TYPE = 'r1.gesture' as const;`
      - `export const R1GesturePayloadSchema = z.object({ kind: z.enum(['tap', 'scroll-up', 'scroll-down', 'long-press', 'double-tap']), timestamp: z.number().int() }).strict();`
      - `export type R1GesturePayload = z.infer<typeof R1GesturePayloadSchema>;`
      - JSDoc cites Specs §3.2 + §4.4 + §10.0.1 (R1 hardware model, event SDK surface, GO criteria) and notes the wire format is split from the internal `R1Gesture` union (which uses `kind: 'scroll' + direction` — translation lives in `r1-event-source.ts` per D-Area-1 + R-Q7).

    Extend `packages/shared-protocol/src/index.ts` Phase 6 block (append after the existing concentration `export { CONC_CONFLICT_TYPE, … }` group):
      - `export { R1_GESTURE_TYPE, R1GesturePayloadSchema, type R1GesturePayload } from './payloads/r1.js';`

    Implement `packages/g2-app/src/engine/r1-timings.ts`:
      - `export type R1Timings = { readonly tapMs: number; readonly doubleTapWindowMs: number; readonly longPressMs: number; readonly scrollDebounceMs: number };`
      - `export const DEFAULT_R1_TIMINGS: R1Timings = Object.freeze({ tapMs: 250, doubleTapWindowMs: 350, longPressMs: 600, scrollDebounceMs: 50 });`
      - JSDoc cites CONTEXT.md §Area 1 + RESEARCH.md §Q1 marking values `[ASSUMED]` pending SC-06-01 hardware closure; documents that the bridge currently classifies `'long-press'` directly, making `longPressMs` a client-side guard rather than primary detection.

    Implement `packages/g2-app/src/locale/locale-events.ts` (resolves RESEARCH Pitfall 7 per the planner-locked decision: a typed emitter SEPARATE from R1Gesture / PanelGestureBus):
      - Class `LocaleEventEmitter` with: `on(event: 'changed', fn: LocaleChangedListener): () => void`, `emit(event: 'changed', code: HudLocale | 'auto'): void`, `size(): number`.
      - Per-listener try/catch + `console.warn('[locale-events] listener threw; continuing fan-out', err)` (mirror PanelGestureBus error isolation, T-4b-01-03 carry).
      - Idempotent unsubscribe closure.
      - JSDoc explicitly explains "this is SEPARATE from PanelGestureBus on purpose — `R1Gesture` union is reserved for actual R1 hardware gestures; locale.changed is an in-process synthetic signal with a distinct type surface" (resolves RESEARCH Pitfall 7 in favour of separation, NOT R1Gesture union extension).
      - Export `type LocaleChangedListener = (code: HudLocale | 'auto') => void` from `../status-hud/i18n-budgets.js` HudLocale import.

    Re-run `pnpm test --filter @evf/shared-protocol --filter @evf/g2-app -- --run` — verify GREEN. Then commit:
      `git add packages/shared-protocol/src/payloads/r1.ts packages/shared-protocol/src/payloads/r1.test.ts packages/shared-protocol/src/index.ts packages/g2-app/src/engine/r1-timings.ts packages/g2-app/src/engine/__tests__/r1-timings.test.ts packages/g2-app/src/locale/locale-events.ts packages/g2-app/src/locale/__tests__/locale-events.test.ts`
      `git commit -m "feat(06-01): R1GesturePayloadSchema + DEFAULT_R1_TIMINGS + LocaleEventEmitter (NAV-01 shared contracts)"`
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol --filter @evf/g2-app test -- --run r1 r1-timings locale-events</automated>
  </verify>
  <done>
    - All ~15 unit tests green; `pnpm typecheck` exits 0.
    - `grep -c "R1GesturePayloadSchema" packages/shared-protocol/src/index.ts` ≥ 1.
    - `grep -c "DEFAULT_R1_TIMINGS" packages/g2-app/src/engine/r1-timings.ts` ≥ 1.
    - `grep -c "class LocaleEventEmitter" packages/g2-app/src/locale/locale-events.ts` ≥ 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LayerManager.getTopLayer() + Layer.getR1Hints?() + R1 event source provider (top-of-stack routing infrastructure)</name>
  <files>
    packages/g2-app/src/engine/layer-types.ts,
    packages/g2-app/src/engine/layer-manager.ts,
    packages/g2-app/src/engine/__tests__/layer-manager.test.ts,
    packages/g2-app/src/engine/r1-event-source.ts,
    packages/g2-app/src/engine/__tests__/r1-event-source.test.ts
  </files>
  <behavior>
    - layer-manager.test.ts (LMT-TOP-01): `lm.getTopLayer()` returns `null` when no OverlayPanel mounted (empty stack, or only z=0/0.5/1/1.5 layers).
    - layer-manager.test.ts (LMT-TOP-02): with a single OverlayPanel mounted at z=2, `getTopLayer()` returns that panel.
    - layer-manager.test.ts (LMT-TOP-03): map insertion order regression guard — mount z=2 OverlayPanel FIRST, then z=1 StatusHudLayer (a non-OverlayPanel); `getTopLayer()` still returns the z=2 panel (proves sort-by-z-descending, NOT insertion order — RESEARCH Pitfall 2).
    - layer-manager.test.ts (LMT-TOP-04): non-OverlayPanel layers (no `onMount/onUnmount/onEvent`) are skipped even at higher z — `isOverlayPanel` guard filters z=1 StatusHudLayer / z=0.5 IdleInfillLayer / z=1.5 ToastQueueLayer.
    - r1-event-source.test.ts (R1E-01): a malformed JSON message (`'not-json'`) → handler swallows + `console.warn` called once + `gestureBus.size()` zero impact.
    - r1-event-source.test.ts (R1E-02): an envelope with `type: 'character.delta'` (not `'r1.gesture'`) → silent skip (NOT a warn — wrong-type is normal traffic).
    - r1-event-source.test.ts (R1E-03): a valid `r1.gesture` envelope with malformed inner payload (`{ kind: 'invalid' }`) → `console.warn('invalid payload')` + no publish.
    - r1-event-source.test.ts (R1E-04): a valid `r1.gesture` envelope with `kind: 'tap'` + `getTopLayer()` returns a panel → `gestureBus.publish` called with `{ kind: 'tap' }`.
    - r1-event-source.test.ts (R1E-05): wire `kind: 'scroll-up'` translates to internal `{ kind: 'scroll', direction: 'up' }` published on bus (RESEARCH §Q7 translation contract).
    - r1-event-source.test.ts (R1E-06): wire `kind: 'scroll-down'` translates to internal `{ kind: 'scroll', direction: 'down' }`.
    - r1-event-source.test.ts (R1E-07): `kind: 'long-press'` passes through verbatim (bridge classifies — `longPressMs` client guard not applied here per D-Area-1).
    - r1-event-source.test.ts (R1E-08): when `getTopLayer()` returns null (no panel mounted) → `console.warn('no top layer — gesture dropped (INV-5 no-op)')` + `gestureBus.publish` NOT called (INV-5 zero-handler explicit no-op).
    - r1-event-source.test.ts (R1E-09): unsubscribe closure idempotency — `const off = attachR1EventSource(…); off(); off();` does NOT throw; `ws.removeEventListener` called at most once.
    - r1-event-source.test.ts (R1E-10): after `off()`, subsequent `ws.fireMessage(validEnvelope)` does NOT publish (subscriber removed).
  </behavior>
  <action>
    Write tests FIRST in `layer-manager.test.ts` (extend existing file — add a new `describe('LayerManager.getTopLayer (LMT-TOP-*)', () => { … })` block) and a new `r1-event-source.test.ts` (mirror `conc-conflict-dispatcher.test.ts` harness: EventEmitter-backed mock `WebSocket` with `fireMessage` + `addEventListener('message', handler)` + `removeEventListener`). Run RED.

    Extend `packages/g2-app/src/engine/layer-types.ts` — add optional method to `Layer` interface (resolves planner-locked decision on RESEARCH Pitfall 5):
      ```
      /** Optional R1 hints exposed by overlay-aware layers for the status-hud context chip.
       *  Layers that don't implement this (z=0 map, z=1 status HUD, z=1.5 toast, z=0.5 idle infill)
       *  inherit the StatusHudRenderer default `{ tap: 'cycle', scroll: 'nav', longPressLabel: 'quick' }`. */
      getR1Hints?(): { readonly tap: string; readonly scroll: string; readonly longPressLabel: string };
      ```
      Place the field directly after `getContainerCount?()`; do NOT mutate any other field.

    Extend `packages/g2-app/src/engine/layer-manager.ts`:
      - Add public method `getTopLayer(): Layer | null` per CONTEXT.md §Area 1 + RESEARCH §Q2 implementation pattern:
        ```
        getTopLayer(): Layer | null {
          // Sort by z DESCENDING — Map iteration is insertion order, NOT numeric order (RESEARCH Pitfall 2).
          const sorted = [...this.layers.entries()].sort(([a], [b]) => b - a);
          for (const [, layer] of sorted) {
            if (isOverlayPanel(layer)) return layer;
          }
          return null;
        }
        ```
      - Import `isOverlayPanel` from `./overlay-panel.js` (already present per the existing `bundle()` impl).
      - JSDoc references INV-5 (this is the routing authority) + RESEARCH Pitfall 2.

    Implement `packages/g2-app/src/engine/r1-event-source.ts` per RESEARCH §Code Example + Open Question 3 narrow-interface pattern:
      - Local interface `R1EventSourceWebSocket { addEventListener(type: 'message', handler: (event: MessageEvent) => void): void; removeEventListener(type: 'message', handler: (event: MessageEvent) => void): void }` (narrower than full `WebSocket` — mirrors `ConcModalWebSocket` pattern; resolves RESEARCH Q-OQ3).
      - `export function attachR1EventSource(ws, gestureBus, layerManager, timings = DEFAULT_R1_TIMINGS): () => void` body:
        1. Define `handler(event: MessageEvent)` — outer `try/catch` belt-and-suspenders (`console.warn('[r1-event-source] handler threw')` on any non-Zod throw — JSON.parse, etc.).
        2. Inside `try`: `JSON.parse` → `EnvelopeSchema.safeParse` (silent skip on failure — bridge sometimes sends non-r1 envelopes through the same socket, this is normal traffic).
        3. Narrow `envelope.type === R1_GESTURE_TYPE` — silent skip if not (wrong-type is normal traffic).
        4. `R1GesturePayloadSchema.safeParse(envelope.payload)` — on failure: `console.warn('[r1-event-source] invalid payload', innerResult.error.issues)` + return.
        5. `const top = layerManager.getTopLayer();` — if null: `console.warn('[r1-event-source] no top layer — gesture dropped (INV-5 no-op)')` + return. INV-5 zero-handler explicit no-op (NOT a silent drop).
        6. Translate wire kind → internal R1Gesture via the explicit branch table in RESEARCH §Code Example (`scroll-up` → `{ kind: 'scroll', direction: 'up' }`; `scroll-down` → down; `tap | long-press | double-tap` → `{ kind }`).
        7. `gestureBus.publish(gesture);` — bus's per-subscriber try/catch isolates faulty panels (T-4b-01-03 carry).
      - Register handler via `ws.addEventListener('message', handler)`.
      - Return idempotent unsubscribe: `let removed = false; return () => { if (!removed) { removed = true; ws.removeEventListener('message', handler); } };`.
      - JSDoc references CONTEXT.md §Area 1, RESEARCH §Q1+§Q7+Pitfall 2, INV-5 zero-handler rule. Cite the bridge classifies long-press directly per Specs §4.4 + CONTEXT.md D-Area-1 (the `timings.longPressMs` is currently a client guard reserved for SC-06-01 hardware-tuning closure — not active in Phase 6 software).

    Re-run tests — verify GREEN. Then commit:
      `git add packages/g2-app/src/engine/layer-types.ts packages/g2-app/src/engine/layer-manager.ts packages/g2-app/src/engine/__tests__/layer-manager.test.ts packages/g2-app/src/engine/r1-event-source.ts packages/g2-app/src/engine/__tests__/r1-event-source.test.ts`
      `git commit -m "feat(06-01): LayerManager.getTopLayer + Layer.getR1Hints? + attachR1EventSource (NAV-01 routing core)"`
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run layer-manager r1-event-source</automated>
  </verify>
  <done>
    - All ~14 unit tests green (LMT-TOP-01..04 + R1E-01..10).
    - `pnpm typecheck` exits 0.
    - `grep -c "getTopLayer" packages/g2-app/src/engine/layer-manager.ts` ≥ 2 (method declaration + JSDoc).
    - `grep -c "getR1Hints" packages/g2-app/src/engine/layer-types.ts` ≥ 1.
    - `grep -c "no top layer — gesture dropped" packages/g2-app/src/engine/r1-event-source.ts` ≥ 1 (INV-5 zero-handler warn message present for tests to assert).
  </done>
</task>

<task type="auto">
  <name>Task 3: INVARIANTS.md ratification + ADR index update + 6 i18n-budgets keys (status-hud chip vocabulary)</name>
  <files>
    docs/architecture/INVARIANTS.md,
    docs/architecture/README.md,
    packages/g2-app/src/status-hud/i18n-budgets.ts
  </files>
  <action>
    Create `docs/architecture/INVARIANTS.md` consolidating INV-1..5 per CONTEXT.md §Area 3 + RESEARCH §Specifics §"INV-5 invariant statement (verbatim for INVARIANTS.md)". Required structure:
      - Frontmatter: `--- title: "EVF Project Invariants (INV-1..5)" status: ratified date: 2026-05-16 binds: "Phase 6+ — applies to every commit" ---`.
      - § 1. INV-1 Layout integrity — copy the canonical text from `CLAUDE.md` § "Project Invariants" verbatim, plus pointer to Specs.md §7.1a + §7.14.4 ck 11–15.
      - § 2. INV-2 Online cross-validation — copy from CLAUDE.md verbatim + canonical-source allow-list.
      - § 3. INV-3 Documentation coherence — copy from CLAUDE.md verbatim + cross-file atomicity rule.
      - § 4. INV-4 Code quality — copy from CLAUDE.md verbatim + Biome/TS/Vitest gate citations.
      - § 5. INV-5 Gesture Determinism (Phase 6 ratification — NEW):
        Use the verbatim statement from CONTEXT.md §Specifics (RESEARCH copies it). Add:
          - Architectural enforcement reference: `LayerManager.getTopLayer()` is the runtime authority (single line: highest-z mounted layer where `isOverlayPanel(layer) === true`).
          - Zero-handler edge case: empty stack / boot-error active = explicit no-op + `console.warn('[r1-event-source] no top layer — gesture dropped (INV-5 no-op)')`. Never a silent drop.
          - Multi-handler ban: bus fan-out remains snapshot-iteration, but architectural constraint says exactly one panel subscribed at any moment. Phase 5 + Phase 6 panel lifecycles enforce this by subscribing in `onMount` + unsubscribing in `onUnmount`.
          - Verification: `packages/g2-app/src/__tests__/06-cross-overlay-reachability.test.ts` (15 cases, Plan 06-04) + `panel-gesture-bus.test.ts` single-receiver invariant tests.
          - Visible enforcement (SC-4 per ROADMAP Phase 6 success criteria): status-hud footer chip names the live long-press target via `getTopLayer()?.getR1Hints?.()` (Plan 06-03).
      - § 6. Cross-cutting note: any new invariant in future phases MUST be added here and indexed from `docs/architecture/README.md`.

    Update `docs/architecture/README.md`:
      - In the See-also section (bottom of file), add a bullet immediately AFTER the `[Specs.md §0.1]` line: `- [INVARIANTS.md](./INVARIANTS.md) — consolidated INV-1..5 ratification (INV-5 Gesture Determinism added Phase 6)`.
      - No other edits.

    Extend `packages/g2-app/src/status-hud/i18n-budgets.ts` HUD_WIDTH_BUDGETS table — append ONLY the 6 chip-vocabulary keys needed by `getR1Hints()` consumers in Plan 06-02 + 06-03. Plan 06-02 + 06-03 will add the per-panel chip variant keys; this task only ships the shared vocabulary needed by the QuickActionMenuPanel + the default-hints fallback.

    Required new entries (append BEFORE the closing `} as const;` of `HUD_WIDTH_BUDGETS`; alphabetical placement within the existing groups is fine; max budget = 38 for chip-string keys per UI-SPEC §3.2; max = 22 for short labels):
      - `hud_r1_default_tap: { it: 'cicla', en: 'cycle', de: 'Wechsel', max: 22 }` — default `tap=` label when no overlay top layer exists.
      - `hud_r1_default_scroll: { it: 'nav', en: 'nav', de: 'Nav', max: 22 }` — default `scroll=` label.
      - `hud_r1_default_long: { it: 'quick', en: 'quick', de: 'Schnell', max: 22 }` — default long-press label.
      - `hud_r1_boot_label: { it: '—', en: '—', de: '—', max: 4 }` — boot-splash chip placeholder (single em-dash; UI-SPEC §3.2 row `Boot`).
      - `hud_r1_boot_error_label: { it: 'riprova', en: 'retry', de: 'erneut', max: 22 }` — boot-error long-press label per UI-SPEC §3.1 row "Boot error active".
      - `inv5_chip_tooltip: { it: 'INV-5: long-press apre il menu del livello attivo', en: 'INV-5: long-press opens active layer menu', de: 'INV-5: Halten öffnet Menü der aktiven Ebene', max: 60 }` — telemetry/help string (NOT rendered in Phase 6 chip; reserved for future tooltip overlay — keeps the INV-5 visible-enforcement vocabulary centralized).

    Run `pnpm typecheck` (the `satisfies` constraint on HUD_WIDTH_BUDGETS will fail-stop if any entry overflows its `max` per Phase 4a build-time gate).

    Commit:
      `git add docs/architecture/INVARIANTS.md docs/architecture/README.md packages/g2-app/src/status-hud/i18n-budgets.ts`
      `git commit -m "docs(06-01): INV-5 Gesture Determinism ratified — INVARIANTS.md + ADR index + 6 chip i18n keys"`
  </action>
  <verify>
    <automated>pnpm typecheck && grep -c "INV-5 — Gesture Determinism" docs/architecture/INVARIANTS.md && grep -c "INVARIANTS.md" docs/architecture/README.md && grep -c "hud_r1_default_tap" packages/g2-app/src/status-hud/i18n-budgets.ts</automated>
  </verify>
  <done>
    - `docs/architecture/INVARIANTS.md` exists with all 5 INV sections; INV-5 includes the verbatim statement from CONTEXT.md §Specifics.
    - `docs/architecture/README.md` See-also section links to INVARIANTS.md.
    - 6 new i18n keys added with locale-canonical strings within budget; `pnpm typecheck` exits 0 (the `satisfies` build-time width-budget gate passes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bridge WS → g2-app `r1-event-source` | Untrusted JSON messages cross here; outer + inner Zod safeParse double trust boundary. |
| `r1-event-source` → `panel-gesture-bus` → top panel | Trusted in-process channel; INV-5 enforces single-receiver by architectural constraint (not by bus single-dispatch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Malformed `r1.gesture` JSON over WS | mitigate | Outer `try/catch` around `JSON.parse`; inside, `EnvelopeSchema.safeParse` + narrow on `type === R1_GESTURE_TYPE` + `R1GesturePayloadSchema.safeParse`. Drop invalid silently for envelope rejects; `console.warn` for inner payload rejects (RESEARCH Q-Pattern + concentration-dispatcher exemplar). |
| T-06-01-02 | Spoofing | Replayed / cross-session `r1.gesture` envelopes | accept | `session_id` validation belongs to the bridge handshake layer (Phase 2/3 boundary); the r1-event-source consumes envelopes already on a session-authenticated socket. Accepting per Phase 6 security domain analysis (RESEARCH §Security Domain). |
| T-06-01-03 | Denial of Service | Gesture-flood loop (hardware stuck) | accept | `scrollDebounceMs: 50` is documented but not actively applied in Phase 6 (bridge classifies). Hardware-tuning closes via SC-06-01. No actionable mitigation beyond debounce; accept per RESEARCH §Security Domain row 3. |
| T-06-01-04 | Tampering | Inner payload with wrong `kind` literal | mitigate | `z.enum(['tap','scroll-up','scroll-down','long-press','double-tap'])` rejects unknown variants; explicit translation branch with no default fall-through (R1E-03 + R1E-07 verify). |
| T-06-01-05 | Information Disclosure | Locale change event leaks user PII | accept | `LocaleEventEmitter` carries only a locale code (`'it'|'en'|…|'auto'`); no identifying data. Accept per low-value-target analysis. |
</threat_model>

<verification>
- All 3 task commits land in sequential order with conventional-commits subjects.
- `pnpm test --filter @evf/g2-app --filter @evf/shared-protocol` workspace-side suite green (no regressions in the existing 1149+ tests).
- `pnpm typecheck` exits 0 (especially the `i18n-budgets.ts satisfies` build-time width-budget gate).
- `pnpm lint:ci` exits 0 (Biome 2.4.15 + zero warnings).
- `grep -nE "^# *INV-5" docs/architecture/INVARIANTS.md` returns one match (or `## 5\. INV-5` heading equivalent).
- Negative grep gate: `! grep -E "kind: 'locale.changed'" packages/g2-app/src/engine/layer-types.ts` (LocaleEventEmitter is separate — R1Gesture union NOT extended with synthetic kinds per planner-locked Q1 resolution).
- Negative grep gate: `! grep -E "z.string\(\).length\(1\)" packages/g2-app/src/engine/panel-router.ts` is INTENTIONALLY skipped here — Plan 06-02 relaxes navKey to `max(1)`; this plan only adds the optional `getR1Hints` field.
</verification>

<success_criteria>
- ✅ NAV-01 partial: R1 wire schema + timing constants + top-of-stack accessor + R1 event source provider land in Wave 0; Plan 06-03 closes NAV-01 by wiring the visible context chip.
- ✅ INV-5 ratified in `docs/architecture/INVARIANTS.md` consolidating INV-1..5; ADR index links the new file.
- ✅ Wave 0 atomic foundation: Plan 06-02 (Wave 1) imports `attachR1EventSource`, `LayerManager.getTopLayer`, `LocaleEventEmitter`, `R1GesturePayloadSchema` verbatim without further forward declarations.
- ✅ Hardware-pending carry-forward declared:
  - SC-06-01 — `DEFAULT_R1_TIMINGS` validated against real R1 ring per Phase 0 §10.0.1 (close via `pnpm --filter @evf/validation-harness validate:all` when Even Hub access granted).
  - (Wave 0 ships software-side only; SC-06-02 + SC-06-03 land via Plan 06-02 + 06-04 hardware-pending sections.)
- ✅ No new npm dependencies (RESEARCH §Standard Stack — Phase 6 uses only existing libs).
</success_criteria>

<output>
After completion, create `.planning/phases/06-r1-integration-quick-action-inv-5/06-01-SUMMARY.md` following `@$HOME/.claude/get-shit-done/templates/summary.md`. Self-check MUST list:
- packages/shared-protocol/src/payloads/r1.ts FOUND
- packages/g2-app/src/engine/r1-timings.ts FOUND
- packages/g2-app/src/engine/r1-event-source.ts FOUND
- packages/g2-app/src/locale/locale-events.ts FOUND
- packages/g2-app/src/engine/layer-manager.ts contains `getTopLayer`
- packages/g2-app/src/engine/layer-types.ts contains `getR1Hints`
- docs/architecture/INVARIANTS.md FOUND
- docs/architecture/README.md links INVARIANTS.md
- 3 task commits in git log (in order)
</output>
