---
phase: 14
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - docs/architecture/0001-layered-ui-model.md
  - Specs.md
  - README.md
  - docs/showcase/index.html
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md
autonomous: true
requirements:
  - INFILL-01
  - INFILL-04

must_haves:
  truths:
    - "Developer/auditor opening ADR-0001 sees its Amendment 1 status row marked as ratified by Phase 14 (date + commit ref + reference to UI-SPEC §13 deliverable 5) — INFILL-04 verified"
    - "Specs.md §7.4c is formally enumerated in the layered-model layer list (§7.2) and the changelog has a Phase 14 ratification entry citing INFILL-01..05 (INFILL-01 verified — layered model formalized end-to-end)"
    - "All four projection artifacts (Specs.md + README.md + showcase + ADR-0001) remain coherent post-commit (INV-3 atomic doc coherence gate green)"
    - "STATE.md current_position advances to Phase 14 complete; ROADMAP.md Phase 14 box [x] checked with plan list final (INFILL-01 + INFILL-04 phase-close verification)"
    - "Phase 14 UI-SPEC §12 Checker Sign-Off Dimension 2 (Visuals) flips from `pending fixture commit` to PASS with date + commit ref"
  artifacts:
    - path: "docs/architecture/0001-layered-ui-model.md"
      provides: "Updated Amendment 1 status note: ratified by Phase 14 + cross-ref to UI-SPEC + commit hash placeholder"
      contains: "Phase 14"
    - path: "Specs.md"
      provides: "Changelog entry under v0.9.12 ratification with INFILL-01..05 traceability"
      contains: "Phase 14"
    - path: ".planning/STATE.md"
      provides: "Updated current_position, progress counts, performance metrics row for Phase 14 plans 01-03"
      contains: "Phase 14"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14 status flipped to [x]; Plans list updated to 3 plans complete; v0.9.12 progress table updated"
      contains: "Phase 14"
  key_links:
    - from: "docs/architecture/0001-layered-ui-model.md"
      to: "Specs.md §7.4c"
      via: "cross-reference"
      pattern: "§7\\.4c"
    - from: "README.md"
      to: "Specs.md"
      via: "version badge alignment"
      pattern: "v0\\.9\\.12"
    - from: "docs/showcase/index.html"
      to: "Specs.md"
      via: "version stat alignment"
      pattern: "v0\\.9\\.12"
---

<objective>
Close Phase 14 with a single INV-3 atomic commit that ratifies the z=0.5 Idle Content Infill layer end-to-end across all four projection artifacts (ADR-0001 + Specs.md + README.md + showcase) AND updates the planning state (STATE.md + ROADMAP.md + UI-SPEC sign-off).

Phase 14 is a **ratification phase**, not a new-build phase. The z=0.5 layer code (`idle-infill-layer.ts`), the differential-demolish behavior (`LayerManager.bundle`), and Specs.md/README/showcase pre-bumps to v0.9.12 were all shipped during the 2026-05-14 quick task `20260514-raster-dynamic-infill` (commit `ee82b83`). Phase 14's job is to:

1. **INFILL-01**: Confirm the layered model's z=0.5 enumeration in Specs.md §7.2 + state machine in §7.4c is final + add a Phase 14 ratification entry to the Specs.md changelog (under the existing v0.9.12 entry) that cites INFILL-01..05 by ID and references the new fixtures + LMT-DD-07 test landing in plans 01-02.
2. **INFILL-04**: Reconcile ADR-0001's Amendment 1 section — currently it says "z=0.5 layer extension" with date 2026-05-14 but does not record that Phase 14 ratified the contract end-to-end (fixtures + cross-state invariants + LMT-DD-07 race coverage). Add a paragraph to the §Amendments / Amendment 1 block stating: "Phase 14 ratification (date + commit ref TBD): INV-1 fixtures locked (3 added in Plan 01); cross-state column-equality invariants verified (Z05-INV-01..04 in z05-state-machine-fixtures.test.ts); race coverage locked (LMT-DD-07 in layer-manager.test.ts)."

The README badge + showcase hero version + Specs header all ALREADY read `v0.9.12` from the 2026-05-14 quick task. This plan does NOT bump them again — it adds a single-sentence Phase 14 ratification note in each per UI-SPEC §0 scope ("Phase 14 reconciles ADR-0001 status + STATE.md + ROADMAP.md + any drift in one final atomic commit").

Output: 7 file modifications in a single atomic commit per CLAUDE.md INV-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-CONTEXT.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md
@CLAUDE.md
@docs/architecture/0001-layered-ui-model.md
@Specs.md
@README.md

<interfaces>
<!-- Key load-bearing strings and locations the executor needs. -->

ADR-0001 (`docs/architecture/0001-layered-ui-model.md`):
- Line 13: `**ACCEPTED** — 2026-05-11. **AMENDED** — 2026-05-14 (z=0.5 Idle Content Infill layer extension; see §Amendments).`
- Lines 83-108: `## Amendments` → `### Amendment 1 — z=0.5 Idle Content Infill layer (2026-05-14, Specs v0.9.12)` block.
- Line 109 (last paragraph end of Amendment 1) is the insertion point for the Phase 14 ratification note.
- Line 78 in `## More Information` already cites `Specs.md §7.4c (Idle Content Infill z=0.5 — added v0.9.12)` — no change needed.

Specs.md changelog (`Specs.md`):
- Line 4046: `## Changelog`
- Line 4048: `- **2026-05-14 (v0.9.12)** — Idle Content Infill layer (z=0.5) + INV-2 spot-check re-verification of image-API constraint.` — this is the existing entry; Phase 14 adds a NEW dated bullet ABOVE it referencing Phase 14 ratification.
- Specs.md header line 9: `# EvenFoundryVTT — Project Specification (v0.9.12)` — DO NOT bump.

README.md:
- Line 6: spec badge `v0.9.12` — DO NOT bump.
- Line 56: NEW v0.9.12 callout for Idle Content Infill (z=0.5) — already present, do NOT duplicate. May add a one-line "ratified Phase 14 [commit]" suffix.
- Line 98: spec-bump paragraph — already cites z=0.5; may add Phase 14 reference.
- Line 138: docs paragraph cites Specs.md — no change required.

docs/showcase/index.html:
- Line 463: `<div class="stat"><span class="num">v0.9.12</span><span class="lab">4 invariants · ×5 cross-check + spot-check</span></div>` — DO NOT bump.
- Line 1036: footer `EvenFoundryVTT — design specification v0.9.12 (2026-05-14)` — may update date to Phase 14 ratification date.
- Line 1040: paragraph already cites z=0.5; one-line append acceptable.

.planning/STATE.md frontmatter (lines 1-13):
- `milestone: v0.9.12`
- `status: planning` → update to `executing` while Phase 14 is in flight, then `complete` for Phase 14 portion (Phase 15 still pending — leave milestone status as `executing` until Phase 15 closes).
- `progress.completed_phases: 0` → `1` after Phase 14 close.
- `progress.completed_plans: 0` → `3` after Phase 14 close.
- `progress.percent: 0` → ~43 (3/7 = 43%).

.planning/STATE.md `## Current Position` (around line 23):
- `Phase: 14 — Raster z=0.5 Idle Content Infill (not started)` → `(complete — Phase 15 next)`.
- `Status: ROADMAP defined; awaiting plan-phase 14 invocation` → `Phase 14 closed — 3 plans shipped (01 fixtures + cross-state invariants, 02 LMT-DD-07 race coverage, 03 INV-3 atomic ratification)`.

.planning/ROADMAP.md:
- Line 37: `- [ ] **Phase 14: Raster z=0.5 Idle Content Infill** —` → flip `[ ]` → `[x]`.
- Line 52: `**Plans**: TBD` → `**Plans**: 3 plans (14-01-PLAN.md fixtures + cross-state invariants · 14-02-PLAN.md LMT-DD-07 race coverage · 14-03-PLAN.md INV-3 atomic ratification)`.
- Line 71: `v0.9.12 Quick Wins | 2 (14–15) | 0/~7 | 🟢 Planning |` → `| 0/~7 |` becomes `| 3/~7 |`.
- Line 78: `| 14. Raster z=0.5 Idle Content Infill | 0/~4 | Not started | — |` → `| 14. ... | 3/3 | ✅ Complete | <date>|`.

.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md:
- Line 357 (Dimension 2 Visuals): `→ **pending fixture commit**` → `→ **PASS** — 3 fixtures added in Plan 01 (raster-overlay-open.it.txt + raster-overlay-open.en.txt + glyph-scene.glyph-idle-z05.it.txt); cross-state invariants Z05-INV-01..04 green; LMT-DD-07 race coverage green; ratified Phase 14 <commit>`.
- Line 363 (Approval): `**Approval:** pending (Phase 14 ...)` → `**Approval:** APPROVED — Phase 14 <commit> <date>`.

INV-3 atomic commit body template (per CLAUDE.md §Pre-bump checklist):
```
docs(phase-14): ratify z=0.5 Idle Content Infill layer (INFILL-01..05)

INV-3 atomic commit covering ADR-0001 Amendment 1 ratification, Specs.md
changelog ratification entry, README + showcase Phase 14 references,
and full planning state advance (STATE.md + ROADMAP.md + UI-SPEC §12
sign-off flip).

Closes: INFILL-01, INFILL-02, INFILL-03, INFILL-04, INFILL-05
Plans: 14-01 (fixtures + cross-state invariants), 14-02 (LMT-DD-07),
       14-03 (this atomic closure)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit ADR-0001 Amendment 1 + Specs.md changelog + README + showcase (4-artifact projection sync)</name>
<files>docs/architecture/0001-layered-ui-model.md, Specs.md, README.md, docs/showcase/index.html</files>
  <read_first>
    - docs/architecture/0001-layered-ui-model.md (full file, 110 lines)
    - Specs.md lines 4046-4060 (Changelog header + v0.9.12 entry)
    - README.md lines 1-60, 95-100, 135-140
    - docs/showcase/index.html lines 460-470, 1030-1045
    - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md §13 deliverables
  </read_first>
  <action>
    Apply four coordinated edits — DO NOT bump v0.9.12 anywhere; only ADD Phase 14 ratification references:

    (a) `docs/architecture/0001-layered-ui-model.md`:
        - Append a final paragraph at the END of the `### Amendment 1` block (after the current line 109 "INV-2 status" paragraph) reading:
          `**Phase 14 ratification (2026-05-17):** Phase 14 (commit hash TBD — set by the commit step) ratifies the z=0.5 contract end-to-end: INV-1 fixtures locked (3 added — \`raster-overlay-open.it.txt\`, \`raster-overlay-open.en.txt\`, \`glyph-scene.glyph-idle-z05.it.txt\`); cross-state column-equality invariants verified (Z05-INV-01..04 in \`packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts\`); race coverage locked (LMT-DD-07 in \`packages/g2-app/src/engine/__tests__/layer-manager.test.ts\`). Full UI contract: \`.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md\` (Approval flipped to APPROVED Phase 14).`
        - Update line 13 (Status row): replace `**AMENDED** — 2026-05-14` with `**AMENDED** — 2026-05-14 (added z=0.5 extension) + **RATIFIED** — 2026-05-17 (Phase 14 — INFILL-01..05 closed).`
        - Do NOT change the original Amendment 1 body content (lines 85-108) — preserve the 2026-05-14 historical record verbatim.

    (b) `Specs.md` Changelog (insertion BEFORE the existing v0.9.12 entry at line 4048):
        - Insert a new bullet AT THE TOP of the changelog (immediately under `## Changelog`):
          `- **2026-05-17 (v0.9.12 Phase 14 ratification)** — z=0.5 Idle Content Infill layer ratified end-to-end per requirements INFILL-01..05. **INFILL-01** layer formalized in §7.2 layered model + state machine (unchanged from 2026-05-14 entry; Phase 14 confirms). **INFILL-02** 3 dynamic text containers (combat-log · z=0.5 label · stats strip) verified via existing \`packages/g2-app/src/status-hud/idle-infill-layer.ts\` (Phase 4a) + 3 new INV-1 fixtures (\`raster-overlay-open.it.txt\` · \`raster-overlay-open.en.txt\` · \`glyph-scene.glyph-idle-z05.it.txt\`) locked in \`packages/shared-render/src/fixtures/\`. **INFILL-03** auto-demolish atomicity verified via differential-demolish (Phase 4b \`LayerManager.bundle()\`) + new LMT-DD-07 race-coverage test in \`packages/g2-app/src/engine/__tests__/layer-manager.test.ts\`. **INFILL-04** ADR-0001 Amendment 1 ratification stanza added (status row: ACCEPTED + AMENDED + RATIFIED Phase 14). **INFILL-05** INV-1 fixtures + cross-state column-equality invariants (Z05-INV-01..04) verified — frame chars at cols {0, 71, 95} and rows {0, 2, 21, 23} byte-identical across State A (raster idle) ↔ State B (overlay-open) ↔ State C (glyph-idle-z05). No spec version bump; v0.9.12 stays at 2026-05-14 baseline. INV-3 atomic commit covers ADR + Specs + README + showcase + planning state.`
        - DO NOT modify the 2026-05-14 entry body or any older entries.

    (c) `README.md`:
        - At line 56 (the existing `**Idle Content Infill (z=0.5)**` table row), append after the `[§7.4c](Specs.md)` link: ` Ratified Phase 14 (2026-05-17).`
        - At line 98 (the spec-bump paragraph), append after `z=0.5 Idle Content Infill layer (§7.4c) that fills previously-empty raster-mode rows without violating the 4-image hardware budget.`: ` (Phase 14 ratified end-to-end 2026-05-17 — INV-1 fixtures + cross-state column equality + LMT-DD-07 race coverage.)`
        - DO NOT bump the badge or any version number.

    (d) `docs/showcase/index.html`:
        - At line 1036, update the footer date: `EvenFoundryVTT — design specification v0.9.12 (2026-05-14, Phase 14 ratification 2026-05-17).`
        - At line 1040, append after `4-image hardware budget.`: ` Phase 14 ratified the z=0.5 contract end-to-end on 2026-05-17 (INV-1 fixtures · cross-state column equality · LMT-DD-07 race coverage).`
        - DO NOT update the version stat (line 463) or hero block.

    Do all four edits in this single task — they form the projection-sync core of the INV-3 commit. Task 2 handles planning-state files (STATE/ROADMAP/UI-SPEC); Task 3 commits everything atomically.
  </action>
  <verify>
    <automated>grep -q "Phase 14 ratification (2026-05-17)" docs/architecture/0001-layered-ui-model.md && grep -q "RATIFIED.*Phase 14" docs/architecture/0001-layered-ui-model.md && grep -q "v0.9.12 Phase 14 ratification" Specs.md && grep -q "INFILL-01\|INFILL-05" Specs.md && grep -q "Ratified Phase 14" README.md && grep -q "Phase 14 ratification 2026-05-17" docs/showcase/index.html && echo "PROJECTION SYNC OK"</automated>
  </verify>
  <done>4-artifact projection-sync edits applied; grep validator above exits 0 with `PROJECTION SYNC OK`; no version bump; Specs.md changelog has a new entry above the 2026-05-14 v0.9.12 entry; ADR-0001 Amendment 1 has a final paragraph citing the 3 fixtures + LMT-DD-07.</done>
</task>

<task type="auto">
  <name>Task 2: Update planning state — STATE.md + ROADMAP.md + UI-SPEC sign-off</name>
<files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md</files>
  <read_first>
    - .planning/STATE.md (full file)
    - .planning/ROADMAP.md (full file)
    - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md §12 Checker Sign-Off + §13 Deliverables
  </read_first>
  <action>
    Apply three coordinated planning-state edits:

    (a) `.planning/STATE.md` frontmatter (lines 1-13):
        - `last_updated: "2026-05-17T<HH:MM>:00.000Z"` (use the current date — `2026-05-17` per memory.currentDate).
        - `progress.completed_phases: 0` → `1`.
        - `progress.completed_plans: 0` → `3`.
        - `progress.percent: 0` → `43` (3/7 = ~42.86 round up to 43).
        - Leave `status: planning` value as-is; it gets flipped to `complete` only after Phase 15 also closes.
        - Update `current_focus` paragraph to: `Phase 14 closed; awaiting first \`/gsd-plan-phase 15\`.`

    (b) `.planning/STATE.md` Current Position (around lines 17-22):
        - `Phase: 14 — Raster z=0.5 Idle Content Infill (not started)` → `Phase: 14 — Raster z=0.5 Idle Content Infill (✅ closed)`.
        - `Plan: —` → `Plan: 14-01..03 (all complete)`.
        - `Status: ROADMAP defined; awaiting plan-phase 14 invocation` → `Status: Phase 14 closed 2026-05-17 — 3 plans shipped (01 fixtures + cross-state invariants, 02 LMT-DD-07 race coverage, 03 INV-3 atomic ratification). Phase 15 (Deepgram Keyterm) next.`
        - `Last activity: 2026-05-17 — v0.9.12 ROADMAP written (Phases 14–15, 9 v1 REQ-IDs mapped)` → `Last activity: 2026-05-17 — Phase 14 INV-3 atomic ratification commit (INFILL-01..05 closed)`.

        Also append a row in the `## Quick Tasks Completed` table at the bottom (use the same column structure as the existing rows): one row with date `2026-05-17`, slug `phase-14-z0.5-ratification`, commit `_pending_` (the execute step will replace with hash), and a notes column matching the changelog entry summary (1-2 lines describing INFILL-01..05 closure + the 4-artifact INV-3 sync).

    (b) `.planning/ROADMAP.md`:
        - Line 37: `- [ ] **Phase 14: Raster z=0.5 Idle Content Infill**` → `- [x] **Phase 14: Raster z=0.5 Idle Content Infill** (✅ closed 2026-05-17)`.
        - Line 52: `**Plans**: TBD` → `**Plans**: 3 plans complete · \`14-01-PLAN.md\` (3 INV-1 fixtures + Z05-INV-01..04 cross-state invariants) · \`14-02-PLAN.md\` (LMT-DD-07 race coverage) · \`14-03-PLAN.md\` (INV-3 atomic ratification — this commit)`.
        - Line 71 progress table row: `v0.9.12 Quick Wins | 2 (14–15) | 0/~7 | 🟢 Planning | — ` → `v0.9.12 Quick Wins | 2 (14–15) | 3/~7 | 🟢 Executing | — `.
        - Line 78 phase progress row: `| 14. Raster z=0.5 Idle Content Infill | 0/~4 | Not started | — |` → `| 14. Raster z=0.5 Idle Content Infill | 3/3 | ✅ Complete | 2026-05-17 |`.

    (c) `.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md`:
        - §12 line 357 (Dimension 2 Visuals): replace `→ **pending fixture commit**` with `→ **PASS** — 3 fixtures added in Plan 01; cross-state invariants Z05-INV-01..04 green; LMT-DD-07 race coverage green; ratified Phase 14 (commit hash TBD).`
        - §12 line 363 (Approval): replace the `**Approval:** pending (Phase 14 ...)` line with `**Approval:** APPROVED — Phase 14 (2026-05-17, commit hash TBD).`

    Use the literal placeholder `commit hash TBD` — the commit-creation step (Task 3) will replace it via post-commit `sed` or leave as-is since the commit IS the artifact (resolvable via `git log --grep "phase-14"` later).
  </action>
  <verify>
    <automated>grep -q "completed_phases: 1" .planning/STATE.md && grep -q "completed_plans: 3" .planning/STATE.md && grep -q "Phase 14.*closed" .planning/STATE.md && grep -q "\[x\] \*\*Phase 14" .planning/ROADMAP.md && grep -q "3/3 | ✅ Complete" .planning/ROADMAP.md && grep -q "APPROVED — Phase 14" .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md && echo "PLANNING STATE OK"</automated>
  </verify>
  <done>3 planning-state files updated; grep validator above exits 0 with `PLANNING STATE OK`; STATE.md progress counts updated to 1/2 phases and 3/7 plans; ROADMAP shows Phase 14 as complete; UI-SPEC §12 Dimension 2 + Approval flipped.</done>
</task>

<task type="auto">
  <name>Task 3: INV-3 atomic commit + workspace CI gates final pass</name>
<files>(no edits — git commit aggregating Tasks 1+2)</files>
  <read_first>
    - CLAUDE.md §INV-3 (atomic commit discipline)
    - CLAUDE.md §Pre-bump checklist (6-step manual verification)
    - All 7 files modified across Tasks 1-2
  </read_first>
  <action>
    Run the final INV-3 atomic-commit gate sequence:

    (1) Pre-commit CI sweep: `pnpm lint:ci && pnpm typecheck && pnpm test:coverage`. All three must exit 0. The test:coverage run must succeed with NEW fixtures from Plan 01 + LMT-DD-07 from Plan 02 already in place — both Wave 1 plans are already committed via their own atomic commits per Plan 01 Task 3 / Plan 02 Task 2.

    (2) INV-2 cross-check sanity (per CLAUDE.md pre-bump checklist step 6): NOT required for this commit because Phase 14 does NOT bump Specs.md version (still v0.9.12). The INV-2 spot-check was already performed and logged in EVIDENCE.md (2026-05-14). Phase 14 adds zero new external-source claims; the projection-sync is purely internal.

    (3) Cross-ref integrity (per CLAUDE.md pre-bump checklist step 5): `grep -nE '§[0-9]+\.[0-9]+' Specs.md | grep -v ':' | head` should still return 0 broken refs. New §7.4c references added in step 1 already exist in the file (verified via `grep -n "^### 7.4c" Specs.md` returning line 1962+).

    (4) Stage and commit using the message template from `<interfaces>`. Use `git add` with EXPLICIT file paths (do NOT use `-A` or `.`): the 7 files modified in Tasks 1-2. The commit message uses a HEREDOC per project commit-discipline pattern.

    (5) Post-commit verification: `git show --stat HEAD` should list exactly 7 files. `git log -1 --format=%s` should match the commit subject. The commit hash retroactively resolves the `commit hash TBD` placeholders left in ADR-0001 / UI-SPEC §12 / STATE.md Quick Tasks row — these can be patched in a follow-up cosmetic commit IF the user explicitly requests it (default: leave as `commit hash TBD` — the git log itself is the authoritative reference per `git log --grep "phase-14"`).
  </action>
  <verify>
    <automated>pnpm lint:ci && pnpm typecheck && pnpm test:coverage && git status --porcelain | wc -l | grep -q "^0$" && git log -1 --format=%s | grep -q "docs(phase-14): ratify z=0.5" && [ "$(git show --stat HEAD --name-only --format= | grep -v '^$' | wc -l)" = "7" ] && echo "INV-3 ATOMIC COMMIT OK"</automated>
  </verify>
  <done>All four CI gates green; INV-3 atomic commit on HEAD with exactly 7 files; commit subject begins with `docs(phase-14): ratify z=0.5`; `git status` clean; final stdout line `INV-3 ATOMIC COMMIT OK`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 4-artifact projection sync | INV-3 invariant — Specs.md / README.md / showcase / ADR must stay coherent. A split commit erodes the cross-reference integrity that downstream auditors rely on. |
| Planning state ↔ artifact state | STATE.md + ROADMAP.md are the source of truth for "where are we?" — a stale STATE.md after Phase 14 close lets the next `/gsd-autonomous` invocation re-attempt Phase 14. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | INV-3 atomic commit | mitigate | `git add` uses explicit file paths (not `-A`); post-commit `git show --stat` asserts exactly 7 files; CI Gate INV-3 (existing v0.9.11 invariant — see CLAUDE.md "Pre-bump checklist") asserts version + claim coherence across Specs/README/showcase |
| T-14-03-02 | Repudiation | commit message claims unverified | mitigate | Commit body cites INFILL-01..05 with the test files / fixture files actually present in HEAD — verifiable via `git log -p HEAD -- packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts` and `git log -p HEAD~2..HEAD -- packages/g2-app/src/engine/__tests__/layer-manager.test.ts` |
| T-14-03-03 | Information Disclosure | none new | accept | No secrets, no PII, no API keys introduced — purely doc + state files |
| T-14-03-SC | Tampering | npm/pip/cargo installs | mitigate | NO dependency installs in this plan — purely documentation + planning state. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- Phase 14 Plan 03 success = (Task 1 4-artifact projection-sync grep green) + (Task 2 planning-state grep green) + (Task 3 INV-3 atomic commit + 4 CI gates green).
- ZERO version bump — Specs.md / README / showcase stay at v0.9.12 per UI-SPEC §0 scope (the 2026-05-14 quick task already bumped them; Phase 14 ratifies, does not re-bump).
- All 5 requirement IDs (INFILL-01..05) explicitly cited in the commit message and in the new Specs.md changelog entry.
- Single INV-3 atomic commit — exactly 7 files in the commit per `git show --stat HEAD`.
</verification>

<success_criteria>
- INFILL-01 verified: layered model formally enumerates z=0.5 in Specs §7.2 (pre-existing) + Phase 14 ratification entry in Specs Changelog
- INFILL-04 verified: ADR-0001 Amendment 1 has a Phase 14 ratification paragraph + Status row updated to ACCEPTED + AMENDED + RATIFIED
- 4-artifact projection coherence: Specs.md + README.md + showcase + ADR-0001 all reference Phase 14 ratification 2026-05-17 without contradicting v0.9.12 baseline
- Planning state: STATE.md `completed_phases: 1, completed_plans: 3, percent: 43`; ROADMAP Phase 14 [x] complete; UI-SPEC Dimension 2 + Approval flipped
- Single INV-3 atomic commit on HEAD with subject `docs(phase-14): ratify z=0.5 Idle Content Infill layer (INFILL-01..05)` and exactly 7 files
</success_criteria>

<output>
Create `.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-03-SUMMARY.md` when done.
</output>
