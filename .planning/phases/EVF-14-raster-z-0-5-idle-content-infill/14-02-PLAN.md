---
phase: 14
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/g2-app/src/engine/__tests__/layer-manager.test.ts
autonomous: true
requirements:
  - INFILL-03

must_haves:
  truths:
    - "Player can verify, via repeatable unit test, that calling `lm.bundle([mount(z=2)])` while z=0.5 is mounted produces exactly ONE `bridge.rebuildPageContainer` call (INFILL-03 — atomicity guarantee per UI-SPEC §6.4 / §8.1)"
    - "Player can verify, via repeatable unit test, that the effective op list synthesized inside `LayerManager.bundle()` rewrites `[mount(z=2)]` to `[destroy(z=0.5), mount(z=2)]` BEFORE the bridge flush (INFILL-03 — differential demolish race coverage)"
    - "Player can verify, via repeatable unit test, that a mid-flush `bundle()` invocation cannot leave the manager in a state with both z=0.5 AND z=2 mounted (INFILL-03 — no transient frame)"
  artifacts:
    - path: "packages/g2-app/src/engine/__tests__/layer-manager.test.ts"
      provides: "LMT-DD-07 race-coverage test extending the existing LMT-DD-01..06 differential-demolish suite"
      contains: "LMT-DD-07"
  key_links:
    - from: "packages/g2-app/src/engine/__tests__/layer-manager.test.ts"
      to: "packages/g2-app/src/engine/layer-manager.ts"
      via: "lm.bundle()` invocation + `bridge.rebuildPageContainer` spy"
      pattern: "bridge\\.rebuildPageContainer"
---

<objective>
Add the LMT-DD-07 race-coverage unit test to `layer-manager.test.ts`, extending the existing LMT-DD-01..06 differential-demolish suite. The test asserts UI-SPEC §6.4 State D (mid-mount transition / race coverage) behavioral guarantees that today are only implied by the existing tests:

1. The "effective op list" rewrite inside `LayerManager.bundle()` synthesizes a `destroy(z=0.5)` op BEFORE the explicit `mount(z=2)` op when z=0.5 is currently mounted (verified by inspecting layer state at the boundary between `effective` rewrite STEP 1 and the bridge flush STEP 6 in layer-manager.ts:191-271).
2. EXACTLY ONE `bridge.rebuildPageContainer` call is issued per bundle even when both ops happen "simultaneously" in the input list (i.e., a hypothetical reentrant or rapid-fire double-bundle cannot produce 2 flushes for a single state transition).
3. The manager state at the moment of bridge flush has z=2 PRESENT and z=0.5 ABSENT — no `[...this.layers.values()]` snapshot ever contains both keys.

Purpose: lock the INFILL-03 atomicity contract per UI-SPEC §6.4 (State D) + §8.1 (Atomicity guarantee). The existing LMT-DD-01..06 tests cover individual differential-demolish steps; LMT-DD-07 explicitly covers the race-window invariant that a mid-mount overlay-mount can NEVER produce a transient frame with both z=0.5 and z=2 visible (Specs.md §11.5.8.6 failure-mode mitigation).

Output: 1 new `it(...)` block named `LMT-DD-07: ...` added inside the existing `describe('Phase 4b differential demolish + container budget + OverlayPanel lifecycle', ...)` block in `layer-manager.test.ts` at line 341+. No production code change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-CONTEXT.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key types/APIs the executor needs. Extracted from layer-manager.ts + layer-types.ts. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From packages/g2-app/src/engine/layer-types.ts:
- `enum ZIndex { Z0_MAP = 0, Z0_5_IDLE_INFILL = 0.5, Z1_STATUS_HUD = 1, Z1_5_TOAST = 1.5, Z2_OVERLAY = 2 }`
- `interface Layer { id: string; draw(): Promise<void>; destroy(): void; getCaptureContainer?(): string; getContainerCount?(): { image: number; text: number } }`
- `interface OverlayPanel extends Layer { onMount(): Promise<void>; onUnmount(): Promise<void>; onEvent(g: R1Gesture): void }`
- `type LayerOp = { type: 'mount', z, layer, requiredCaps? } | { type: 'destroy', z }`
- `class LayerManagerError extends Error { code: LayerManagerErrorCode }`

From packages/g2-app/src/engine/layer-manager.ts:
- `class LayerManager` constructor takes `EvenAppBridge`.
- `mount(z, layer, requiredCaps?)`, `destroy(z)`, `async bundle(ops)`, `setNegotiatedCaps(caps)`, `getLayer(z)`, `getCaptureContainerCount()`, `setMapMode/getMapMode`.
- `bundle()` STEP 1 (lines 191-224) rewrites ops to include implicit `destroy(z=0.5)` before any `mount(z=2)` when z=0.5 is mounted; STEP 6 (line 271) issues a single `await this.bridge.rebuildPageContainer(...)` call.
- `_suspendedZ05` (line 80) stashes the demolished z=0.5 instance for round-trip restoration.

From existing layer-manager.test.ts (already imported in file):
- `makeMockBridge()` — returns object with `rebuildPageContainer: vi.fn().mockResolvedValue(true)` etc.
- `makeMockLayer(id, captureContainer?)` — Layer without getContainerCount (defaults to `{image:0, text:1}`).
- `makeCountedLayer(id, count, captureContainer?)` — Layer with `getContainerCount: () => count`.
- `makeOverlayPanelStub(id, count, captureContainer)` — OverlayPanel with lifecycle hooks.
- Existing LMT-DD-01..06 sit inside `describe('Phase 4b differential demolish + container budget + OverlayPanel lifecycle', ...)` starting at line 341.

Test budget context:
- Existing test count for this file: 50+ tests across capture-invariant, capability-gate, bundle, setMapMode, LT-1..5, LMT-DD-01..06.
- Phase 14 adds exactly ONE new `it(...)` block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LMT-DD-07 race-coverage unit test</name>
  <files>packages/g2-app/src/engine/__tests__/layer-manager.test.ts</files>
  <read_first>
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts (existing LMT-DD-01..06 block at lines 341-460)
    - packages/g2-app/src/engine/layer-manager.ts lines 190-272 (bundle() STEP 1 + STEP 6)
    - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md §6.4 (State D Mid-mount transition assertion table)
  </read_first>
  <behavior>
    LMT-DD-07 inside `describe('Phase 4b differential demolish + container budget + OverlayPanel lifecycle', ...)`:

    - **Pre-arrange**: Mount `mapLayer` at Z0_MAP (capture provider, container count `{image:4, text:1}`); mount `idle` (idle-infill stand-in, count `{image:0, text:3}`) at Z0_5_IDLE_INFILL. Assert `lm.getCaptureContainerCount() === 1` and `lm.getLayer(Z0_5_IDLE_INFILL) === idle`. Reset `bridge.rebuildPageContainer` mock so it has zero calls.
    - **Action**: Build `panel = makeOverlayPanelStub('overlay', {image:0, text:3}, 'overlay-capture')`. Call `await lm.bundle([{ type: 'destroy', z: Z0_MAP }, { type: 'mount', z: Z2_OVERLAY, layer: panel }])` in ONE awaited expression (single bundle).
    - **Assertion 1 (race coverage — atomicity)**: `bridge.rebuildPageContainer.mock.calls.length === 1`. The bundle MUST issue exactly one flush even though three op-effects fire (implicit destroy z=0.5 + explicit destroy z=0 + explicit mount z=2).
    - **Assertion 2 (no transient state)**: After the bundle resolves, `lm.getLayer(Z0_5_IDLE_INFILL)` is `undefined` AND `lm.getLayer(Z2_OVERLAY) === panel`. There is no intermediate awaitable state in which both keys are populated — the test asserts the post-condition because the bundle is atomic from caller perspective.
    - **Assertion 3 (`_suspendedZ05` round-trip)**: Issue a follow-up bundle `await lm.bundle([{ type: 'mount', z: Z0_MAP, layer: makeCountedLayer('map2', {image:4, text:1}, 'map-capture') }, { type: 'destroy', z: Z2_OVERLAY }])`. After it resolves: `lm.getLayer(Z0_5_IDLE_INFILL) === idle` (reference equality with the originally-mounted instance — the suspend stash worked) AND `bridge.rebuildPageContainer.mock.calls.length === 2` (the previous flush + this flush = exactly 2, no extras).
    - **Assertion 4 (no race with toast)**: Pre-arrange a NEW LayerManager + bridge inside the same `it`. Mount map (capture), idle infill, and a toast layer at Z1_5_TOAST (count `{image:0, text:1}`). Bundle the same overlay mount; assert toast still mounted (`getLayer(Z1_5_TOAST)` defined) AND bridge.rebuildPageContainer called exactly once. (Re-affirms UI-SPEC §6.4 row 1 + LMT-DD-04 carve-out under race conditions.)
  </behavior>
  <action>
    Insert ONE new `it('LMT-DD-07: ...')` block at the end of the `describe('Phase 4b differential demolish + container budget + OverlayPanel lifecycle', ...)` block immediately after the existing LMT-DD-06 test (around line 460). Reuse the existing `makeMockBridge`, `makeMockLayer`, `makeCountedLayer`, `makeOverlayPanelStub` helpers from the same file (DO NOT redefine). Reuse the existing `let bridge: ...; let lm: LayerManager;` + `beforeEach` initialization at the top of the describe — for Assertion 4 create a separate `bridge2`/`lm2` pair scoped to that assertion block within the same `it`.

    The new test name is the exact string `'LMT-DD-07: race coverage — bundle with z=0.5 mounted issues exactly one bridge flush; no transient frame with both visible'`. JSDoc above the test cites `UI-SPEC §6.4 State D` and `Specs.md §11.5.8.6`. The body uses `await lm.bundle(...)` for both bundles; do NOT use `Promise.all` (the bundle API is sequential by contract). Use `expect(bridge.rebuildPageContainer.mock.calls.length).toBe(1)` style assertions (NOT `.toHaveBeenCalledTimes(1)` after the second bundle — the count is cumulative across the `bridge` instance lifetime). Reset / clear the mock with `bridge.rebuildPageContainer.mockClear()` between assertions ONLY when explicitly testing per-bundle counts (Assertions 1 + 3).
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app exec vitest run src/engine/__tests__/layer-manager.test.ts -t "LMT-DD-07" --reporter=verbose</automated>
  </verify>
  <done>LMT-DD-07 test passes; total LMT-DD count in this file is exactly 7 (verified by `grep -c "LMT-DD-0[1-9]: " packages/g2-app/src/engine/__tests__/layer-manager.test.ts` returning 7); no other test regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Quality gates — workspace lint / typecheck / full test suite</name>
<files>packages/g2-app/src/engine/__tests__/layer-manager.test.ts (verification only — no edits)</files>
  <read_first>
    - package.json (root scripts)
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts (after Task 1 edit)
  </read_first>
  <action>
    Run the four CI quality gates that Phase 14 Plan 02 must keep green:
    (a) `pnpm lint:ci` — Biome 2.4.15 read-only mode.
    (b) `pnpm typecheck` — root tsc + per-package tsc.
    (c) `pnpm --filter @evf/g2-app test` — full g2-app test suite (verifies LMT-DD-01..06 still pass; LMT-DD-07 also passes; no other test regresses).
    (d) `grep -c "LMT-DD-0[1-9]: " packages/g2-app/src/engine/__tests__/layer-manager.test.ts` returns exactly `7` (six existing + one new).

    Do NOT modify production code (layer-manager.ts, layer-types.ts) — the test must pass against the EXISTING implementation; if it doesn't, that indicates a contract mismatch between UI-SPEC §6.4 expectations and the current bundle() rewrite logic, which would be a finding to surface, NOT a code patch.
  </action>
  <verify>
    <automated>pnpm lint:ci && pnpm typecheck && pnpm --filter @evf/g2-app test && [ "$(grep -c "LMT-DD-0[1-9]: " packages/g2-app/src/engine/__tests__/layer-manager.test.ts)" = "7" ] && echo "GATES GREEN"</automated>
  </verify>
  <done>All four gates green; final stdout line is `GATES GREEN`; LMT-DD count is exactly 7; no production code touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test ↔ LayerManager runtime | The race-coverage test is a STATEMENT about the bundle() atomicity contract; any future refactor that breaks the single-flush guarantee must be caught HERE before it reaches a real flush race in hardware UAT (where it would manifest as visible flicker per Specs.md §11.5.8.6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | LayerManager.bundle() effective op rewrite | mitigate | LMT-DD-07 asserts the post-condition that `_suspendedZ05` round-trip restores the SAME instance — silent reference-swap (instance-replacement) would fail Assertion 3 |
| T-14-02-02 | Denial of Service | bundle() called rapidly mid-flush | accept | The bundle API is async-sequential by `Layer` contract (Phase 4b precedent); a reentrant call awaits the previous flush — LMT-DD-07 does not exercise reentrancy directly because the API does not support it (acceptable scope for this plan) |
| T-14-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced — Vitest 4 already present |
</threat_model>

<verification>
- Phase 14 Plan 02 success = (Task 1 LMT-DD-07 green) + (Task 2 four CI gates green).
- Test count delta = +1 across the workspace.
- INFILL-03 demonstrably linked to test name in commit message.
- No production code modification — runs entirely against existing LayerManager.bundle() implementation from Phase 4b.
</verification>

<success_criteria>
- LMT-DD-07 test green, covering UI-SPEC §6.4 State D + §8.1 Atomicity guarantee + Specs.md §11.5.8.6 failure-mode mitigation
- 4 explicit assertions inside the test (atomicity, no-transient-state, suspend round-trip, toast carve-out under race)
- `pnpm lint:ci && pnpm typecheck && pnpm --filter @evf/g2-app test` exit 0
- INFILL-03 explicitly cited in the test JSDoc and commit message
</success_criteria>

<output>
Create `.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-02-SUMMARY.md` when done.
</output>
