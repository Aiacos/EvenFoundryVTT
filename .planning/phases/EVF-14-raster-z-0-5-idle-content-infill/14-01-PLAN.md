---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-render/src/fixtures/raster-overlay-open.it.txt
  - packages/shared-render/src/fixtures/raster-overlay-open.en.txt
  - packages/shared-render/src/fixtures/glyph-scene.glyph-idle-z05.it.txt
  - packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts
autonomous: true
requirements:
  - INFILL-02
  - INFILL-05

must_haves:
  truths:
    - "Player can verify, via repeatable snapshot, that the raster idle state mounts the 3 z=0.5 strips (combat-log · label · stats) in the previously-empty map-area rows (INFILL-02)"
    - "Player can verify, via repeatable snapshot, that opening an overlay swaps the 3 z=0.5 rows for z=2 panel content without shifting frame chars or the z=1 Status HUD column (INFILL-05)"
    - "Player can verify, via repeatable snapshot, that glyph-mode idle infill renders ONLY 2 strips (label + stats; combat-log omitted) per UI-SPEC §6.3 locked degradation (INFILL-02 + INFILL-05)"
  artifacts:
    - path: "packages/shared-render/src/fixtures/raster-overlay-open.it.txt"
      provides: "State B IT-locale canonical fixture (z=0+z=1+z=2; z=0.5 demolished) — 96×24 char-precision"
      contains: "SHEET · BIO"
    - path: "packages/shared-render/src/fixtures/raster-overlay-open.en.txt"
      provides: "State B EN-locale variant fixture — 96×24 char-precision"
      contains: "SHEET · BIO"
    - path: "packages/shared-render/src/fixtures/glyph-scene.glyph-idle-z05.it.txt"
      provides: "State C glyph-mode idle infill (2-strip degraded) — label + stats only, no combat-log"
      contains: "z=0.5 idle infill"
    - path: "packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts"
      provides: "INV-1 snapshot tests + cross-state column-position equality assertions for State A/B/C"
      min_lines: 90
  key_links:
    - from: "packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts"
      to: "packages/shared-render/src/fixtures/raster-overlay-open.it.txt"
      via: "matchAsciiFixture round-trip"
      pattern: "matchAsciiFixture"
    - from: "packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts"
      to: "packages/shared-render/src/fixtures/glyph-scene.raster-idle.txt"
      via: "AsciiGrid.at(col, row) cross-state equality"
      pattern: "AsciiGrid\\.at"
---

<objective>
Add the three Phase 14 INV-1 snapshot fixtures (State B IT + EN, State C glyph-mode-with-z=0.5) and a dedicated test file that (a) round-trips each new fixture through `matchAsciiFixture`, and (b) asserts that frame-character column positions (cols 0, 71, 95) and corner rows (0, 2, 21, 23) are byte-identical across State A (raster idle) ↔ State B (overlay-open) ↔ State C (glyph idle) per UI-SPEC §8.2 INV-1 invariants.

Purpose: lock the z=0.5 state machine visual contract per INFILL-02 (3 dynamic text containers in raster idle) and INFILL-05 (INV-1 fixtures for idle-fill + overlay-mount transitions). The existing 5 `glyph-scene.*` fixtures are frozen as-is (no edits) per UI-SPEC §13 deliverables checklist item 1; this plan adds the 3 NEW fixtures and the cross-state assertion test that proves frame integrity holds across the entire state machine.

Output: 3 new fixture files (96 cols × 24 rows each) + 1 new test file (in `packages/g2-app/src/status-hud/__tests__/` to align with the existing `snapshot.test.ts` per-ck pattern that already loads `glyph-scene.raster-idle*.txt` via `loadSceneFixture`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-CONTEXT.md
@.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key types/APIs the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From packages/shared-render/src/ascii-grid.ts:
- `class AsciiGrid` — immutable rectangular char grid.
- `AsciiGrid.fromString(text: string): AsciiGrid` — trims trailing blank line if file ends with `\n`.
- `grid.toString(): string` — LF-joined, NO trailing newline.
- `grid.at(col: number, row: number): string | undefined` — bounds-safe per `noUncheckedIndexedAccess`.
- `grid.width: number` / `grid.height: number`.

From packages/shared-render/src/snapshot.ts:
- `async matchAsciiFixture(grid: AsciiGrid, fixturePath: string): Promise<void>` — wraps Vitest 4 `expect.toMatchFileSnapshot`. `fixturePath` is relative to the calling test file (Vitest 4 convention). Serialized form is `grid.toString() + '\n'`.

From packages/shared-render/src/index.ts:
- Re-exports `AsciiGrid` and `matchAsciiFixture` from `@evf/shared-render`.

Existing pattern in packages/g2-app/src/status-hud/__tests__/snapshot.test.ts:
- `loadSceneFixture(filename: string): AsciiGrid` — reads `resolve(__dirname, '../../../../shared-render/src/fixtures', filename)`, calls `AsciiGrid.fromString(readFileSync(path, 'utf-8'))`.
- Per-ck `describe`/`it` blocks call `matchAsciiFixture(grid, '../../../../shared-render/src/fixtures/<file>.txt')`.
- INV-1 ck 11 = HUD overflow, ck 12 = raster-idle, ck 13 = glyph-idle + [GLY], ck 14 = locale variants, ck 15 = HUD loading.

Frame-char columns and rows (UI-SPEC §8.2 INV-1 invariants):
- Width = 96 cells; rows = 24 (height inclusive of frame).
- Frame `║` columns: 0, 71, 95. Frame `═` / corner-char rows: 0, 2, 21, 23.
- Right Status HUD column = cols 71..95 (byte-identical between State A and State B per §8.2 invariant 2).
- z=0.5 rows in State A: 18 (label), 19 (combat-log), 20 (stats) per UI-SPEC §6.1 row inventory.
- In State B those 3 rows are owned by z=2 panel content (UI-SPEC §6.2 mockup shows `┌─[ SHEET · BIO ]─...` at row 18, content rows 19-20).
- In State C (glyph) z=0.5 content occupies the rows immediately above the footer with `─── z=0.5 idle infill ───` separator on the first occupied row and stats strip on the next — no combat-log.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3 new INV-1 fixtures (State B IT + EN; State C glyph + z=0.5)</name>
<files>packages/shared-render/src/fixtures/raster-overlay-open.it.txt, packages/shared-render/src/fixtures/raster-overlay-open.en.txt, packages/shared-render/src/fixtures/glyph-scene.glyph-idle-z05.it.txt</files>
  <read_first>
    - packages/shared-render/src/fixtures/glyph-scene.raster-idle.txt (canonical State A EN)
    - packages/shared-render/src/fixtures/glyph-scene.raster-idle-it.txt (State A IT)
    - packages/shared-render/src/fixtures/glyph-scene.raster-idle-en.txt (State A EN variant)
    - packages/shared-render/src/fixtures/glyph-scene.glyph-idle.txt (existing glyph baseline — do NOT edit)
    - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md §6.2 (State B mockup) + §6.3 (State C mockup)
  </read_first>
  <action>
    Create three NEW fixture files under `packages/shared-render/src/fixtures/`:

    (1) `raster-overlay-open.it.txt` — IT-locale State B canonical (per UI-SPEC §6.2 mockup, translated to IT). Width 96 cells, height 24 rows. Frame chars at cols 0, 71, 95; corner rows 0, 2, 21, 23. Header row 1 IT content = `MAP · Sala Banchetti · raster        TURNO 2/5                  ⌁ R1 92%` (matches existing `glyph-scene.raster-idle-it.txt` row 1 byte-for-byte). Rows 3-17 = byte-identical copy of `glyph-scene.raster-idle-it.txt` rows 3-17 (left map area with 2×2 raster tiles + right Status HUD column). Rows 18-20 SWAP from z=0.5 strips to z=2 SHEET panel: row 18 left half = `║   ┌─[ SCHEDA · BIO ]────────────────────────────────────┐          ` (left-margin 3 chars at col 4, content within cols 4..63, closing `┐` then padding to col 70, then `║` at col 71); row 19 left half = `║   │ Thorin Stonebeard · F3/W5 · LG nano eroe popolare    │          `; row 20 left half = `║   │ Background: Eroe del popolo · Allineamento: LB        │          `. The right Status HUD column (cols 71..95) for rows 18, 19, 20 MUST be byte-identical to `glyph-scene.raster-idle-it.txt` rows 18, 19, 20 (UI-SPEC §8.2 invariant 2 — `║  ▶ Bless (7r)            ║`, `║    Concentr.             ║`, `║                          ║`). Footer rows 21-23 follow UI-SPEC §6.2 mockup but localized: row 22 = `║ R1: scroll=naviga  tap=action  long=close   ▶SCHEDA active                                   ║`. Reuse the literal frame chars (`╔`, `╗`, `╚`, `╝`, `║`, `═`, `╠`, `╣`, `╦`, `╩`, `┌`, `┐`, `└`, `┘`, `│`, `─`) without substitution.

    (2) `raster-overlay-open.en.txt` — EN-locale State B variant. Same structure as (1) but EN strings: row 1 = `MAP · Sala Banchetti · raster        ROUND 3 · TURN 2/5         ⌁ R1 92%` (from `glyph-scene.raster-idle-en.txt`). Rows 3-17 = byte-identical copy of `glyph-scene.raster-idle-en.txt` rows 3-17. Rows 18-20 panel content per UI-SPEC §6.2 EN mockup: row 18 = `║   ┌─[ SHEET · BIO ]────────────────────────────────────┐          `, row 19 = `║   │ Thorin Stonebeard · F3/W5 · LG dwarf folk hero     │          `, row 20 = `║   │ Background: Folk Hero · Alignment: Lawful Good     │          `. Status HUD column (71..95) byte-identical to `glyph-scene.raster-idle-en.txt` rows 18-20. Footer row 22 = `║ R1: scroll=navigate  tap=action  long=close   ▶SHEET active                                  ║`.

    (3) `glyph-scene.glyph-idle-z05.it.txt` — IT-locale State C (glyph + z=0.5 2-strip degradation per UI-SPEC §6.3 + §10 width-budget lock). Start from `glyph-scene.glyph-idle.txt` byte-for-byte for header (row 1), frame rows, and the glyph grid block (rows 3..14). Then for rows 18-20 (where the original glyph fixture has empty padding `║                                                                   ║   …   ║`) inject the 2 z=0.5 strips per UI-SPEC §6.3: row 18 left half = `║   ─── z=0.5 idle infill ──────────────────────────────            ` then byte-identical Status HUD column (cols 71..95) from `glyph-scene.glyph-idle.txt` row 18; row 19 left half = `║   glyph 66×21 · token-delta · BLE 64k · 0.3 fps · [Q]             ` then identical right column from row 19. Row 20 unchanged from `glyph-scene.glyph-idle.txt` row 20. Footer row 22 localized IT = `║ R1: scroll=pan  tap=ping  long=quick   modo: ▶GLYPH (toggle RASTER)   [scheda] [combat]      ║`. Width = 96 chars per row; verify each row has exactly 96 cells before saving (test in Task 2 will fail loudly on mismatch via `AsciiGrid` constructor).

    Each file MUST end with a trailing newline (matches `AsciiGrid.fromString` round-trip behavior used by `matchAsciiFixture` per snapshot.ts line 21).
  </action>
  <verify>
    <automated>cd packages/shared-render && node -e "const fs=require('node:fs');const files=['raster-overlay-open.it.txt','raster-overlay-open.en.txt','glyph-scene.glyph-idle-z05.it.txt'];for(const f of files){const t=fs.readFileSync('src/fixtures/'+f,'utf-8');const lines=t.replace(/\r\n/g,'\n').split('\n');if(lines[lines.length-1]==='')lines.pop();if(lines.length!==24)throw new Error(f+': expected 24 rows, got '+lines.length);for(let i=0;i&lt;lines.length;i++){if([...lines[i]].length!==96)throw new Error(f+' row '+i+': expected 96 cells, got '+[...lines[i]].length)}console.log(f+' OK')}"</automated>
  </verify>
  <done>3 fixture files created under `packages/shared-render/src/fixtures/`; each is 96 cols × 24 rows; each ends with `\n`; node grid-validator script above exits 0 with three `OK` lines printed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test — INV-1 cross-state column equality + new-fixture snapshot match</name>
  <files>packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts</files>
  <read_first>
    - packages/g2-app/src/status-hud/__tests__/snapshot.test.ts (existing loadSceneFixture pattern + matchAsciiFixture relative-path convention)
    - packages/shared-render/src/ascii-grid.ts (AsciiGrid API)
    - packages/shared-render/src/snapshot.ts (matchAsciiFixture API)
    - .planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-UI-SPEC.md §8.2 (cross-state INV-1 invariants 1..5)
  </read_first>
  <behavior>
    - Test Z05-FX-01: `raster-overlay-open.it.txt` round-trips through `matchAsciiFixture` (covers INFILL-05 State B IT).
    - Test Z05-FX-02: `raster-overlay-open.en.txt` round-trips through `matchAsciiFixture` (covers INFILL-05 State B EN).
    - Test Z05-FX-03: `glyph-scene.glyph-idle-z05.it.txt` round-trips through `matchAsciiFixture` (covers INFILL-05 State C glyph + z=0.5).
    - Test Z05-INV-01 (UI-SPEC §8.2 invariant 1 — frame columns): for every (col, row) ∈ { (0, 0), (71, 0), (95, 0), (0, 2), (71, 2), (95, 2), (0, 21), (71, 21), (95, 21), (0, 23), (71, 23), (95, 23) }, `gridA.at(col, row) === gridB.at(col, row) === gridC.at(col, row)` where A = `glyph-scene.raster-idle.txt`, B = `raster-overlay-open.en.txt`, C = `glyph-scene.glyph-idle-z05.it.txt`.
    - Test Z05-INV-02 (UI-SPEC §8.2 invariant 2 — right Status HUD column byte-identical A↔B): for every (col, row) with col ∈ [71..95] and row ∈ [3..20], `gridA.at(col, row) === gridB.at(col, row)` where A = `glyph-scene.raster-idle.txt` (EN) and B = `raster-overlay-open.en.txt`. The EN-EN pairing is intentional — IT-vs-EN row 1 differs by locale label content, but the Status HUD column inside one locale must be invariant across A↔B.
    - Test Z05-INV-03 (UI-SPEC §8.2 invariant 4 — frame columns identical across the IT pair): for State A IT (`glyph-scene.raster-idle-it.txt`) and State B IT (`raster-overlay-open.it.txt`), assert frame chars at cols { 0, 71, 95 } for rows { 0, 2, 21, 23 } match between the two grids.
    - Test Z05-INV-04 (UI-SPEC §8.2 invariant 5 — content swap rows 18/19/20 are owned by z=2 in State B): in `raster-overlay-open.en.txt` row 18, the substring at col 4..63 (left content within left half) must START with `┌─[ SHEET ·` (proving z=2 panel content replaced the z=0.5 strips). The right column cols 71..95 must still contain the Status HUD content (assert that col 73..86 substring contains `▶ Bless (7r)`).

    All test names use the prefix `Z05-FX-` for fixture-match tests and `Z05-INV-` for cross-state invariant tests. Failures must surface UI-SPEC §8.2 invariant number in the assertion message.
  </behavior>
  <action>
    Create the new test file `packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts`. Replicate the `loadSceneFixture` helper pattern from `snapshot.test.ts` (resolve via `__dirname` + `'../../../../shared-render/src/fixtures'`). Import `AsciiGrid` and `matchAsciiFixture` from `@evf/shared-render`. Use Vitest 4 `describe` + `it` + `expect` from `vitest`. Wire each Z05-FX-* test by loading the fixture, then calling `await matchAsciiFixture(grid, '../../../../shared-render/src/fixtures/<filename>.txt')` (same relative-path convention as existing snapshot.test.ts). For Z05-INV-* tests, load both grids and iterate the (col, row) tuples above with `expect(gridX.at(col, row)).toBe(gridY.at(col, row))` and include the col/row in the assertion message via the `.toBe` second-arg-less form (use `expect.soft` if helpful to collect all mismatches). For Z05-INV-04, extract the substring with `[...lines[18]].slice(4, 14).join('')` and `[...lines[18]].slice(73, 87).join('')` — be precise about codepoint slicing (the existing AsciiGrid implementation uses `[...string]` per ascii-grid.ts line 39 to handle multi-byte glyphs). Do NOT use `String.prototype.slice` directly — use `.cells[row]?.slice(col, col+n).join('')`.

    Reference UI-SPEC §8.2 invariants 1..5 in JSDoc at the top of the test file. Cite INFILL-02 + INFILL-05 in the file-header comment.

    Run the test BEFORE Task 1 fixtures land to verify it fails (RED — Z05-FX-* must fail because fixture files don't yet exist). Run AFTER Task 1 to verify GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app exec vitest run src/status-hud/__tests__/z05-state-machine-fixtures.test.ts --reporter=verbose</automated>
  </verify>
  <done>All 7 tests pass (Z05-FX-01, -02, -03 + Z05-INV-01, -02, -03, -04); zero `console.warn` from snapshot mismatch; test file uses the same `loadSceneFixture` pattern as existing `snapshot.test.ts`.</done>
</task>

<task type="auto">
  <name>Task 3: Quality gates — workspace lint / typecheck / full test suite</name>
<files>packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts (verification only — no edits)</files>
  <read_first>
    - package.json (root scripts: lint:ci, typecheck, test:coverage)
    - packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts (new)
  </read_first>
  <action>
    Run the four CI quality gates that Phase 14 Plan 01 must keep green:
    (a) `pnpm lint:ci` — Biome 2.4.15 in CI mode (read-only, fails on any warning).
    (b) `pnpm typecheck` — root `tsc --noEmit` on tsconfig.base.json + `pnpm -r exec tsc --noEmit` per-package.
    (c) `pnpm test:coverage` — Vitest 4 with v8 coverage; assert coverage stays ≥80% (current baseline 2546 tests post-archive per MEMORY.md project_milestone_v091).
    (d) Workspace test count assertion: count of `it(` blocks in the new file === 7 (3 FX + 4 INV).

    Do NOT modify any production code in this task. If lint or typecheck fails, the fix belongs in Task 2 (add missing imports, format compliance) — do not introduce new logic here. Fix any Biome auto-fixable formatting via `pnpm format` ONLY if the failure is purely cosmetic (whitespace / trailing newline).
  </action>
  <verify>
    <automated>pnpm lint:ci && pnpm typecheck && pnpm --filter @evf/g2-app exec vitest run src/status-hud/__tests__/z05-state-machine-fixtures.test.ts && grep -c "^  it(" packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts | grep -q "^7$" && echo "GATES GREEN"</automated>
  </verify>
  <done>All four gates green; final stdout line is `GATES GREEN`; no Biome auto-fix needed beyond cosmetic formatting; new file has exactly 7 `it(` blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture ↔ runtime | INV-1 fixtures are LOAD-BEARING contracts — a malicious or buggy edit could erode layout integrity invisibly until field test catches it. Mitigation: cross-state column-equality assertions (Z05-INV-01..04) bind ANY frame-char drift to a CI failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | new fixture files | mitigate | `AsciiGrid` constructor (ascii-grid.ts:28) throws on row-width mismatch; node grid-validator script in Task 1 catches before commit; Task 2 INV tests enforce cross-state column equality so a single-cell edit anywhere on cols 0/71/95 fails CI |
| T-14-01-02 | Information Disclosure | locale fixtures | accept | Fixtures contain no PII; canonical character "Thorin Stonebeard" is public fixture data |
| T-14-01-03 | Denial of Service | snapshot test execution time | accept | 7 tests × ~10 ms each = negligible vs 2546-test baseline; no new I/O patterns beyond existing fixture-load pattern |
| T-14-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced — Vitest 4, AsciiGrid, matchAsciiFixture all pre-existing |
</threat_model>

<verification>
- Phase 14 Plan 01 success = (Task 1 fixture validator green) + (Task 2 vitest 7/7 green) + (Task 3 four CI gates green).
- Cross-state INV-1 invariants (UI-SPEC §8.2 invariants 1, 2, 4, 5) explicitly tested.
- No production code change — `idle-infill-layer.ts`, `layer-manager.ts`, `status-hud-renderer.ts` untouched.
- Existing 5 fixtures (`glyph-scene.raster-idle.txt` + 3 locale variants + `glyph-scene.glyph-idle.txt`) NOT edited — UI-SPEC §13 deliverable 1 freeze contract preserved.
</verification>

<success_criteria>
- 3 new fixture files exist at the locked paths and shapes (96×24, trailing `\n`)
- 7 new tests pass (Z05-FX-01..03, Z05-INV-01..04)
- `pnpm lint:ci && pnpm typecheck && pnpm test:coverage` exit 0
- Workspace test count increases by exactly 7 (no inadvertent test additions)
- INFILL-02 + INFILL-05 demonstrably linked to test names in commit message
</success_criteria>

<output>
Create `.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/14-01-SUMMARY.md` when done.
</output>
