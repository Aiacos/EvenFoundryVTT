---
phase: 19-adr-0013-amendment-1-canvas-compositor-core
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - packages/g2-app/src/engine/layer-manager.ts
  - packages/g2-app/src/engine/__tests__/layer-manager.test.ts
  - packages/g2-app/src/hud/hud-poc-page.ts
autonomous: true
requirements: [RAST-03, RAST-04, RAST-01]
must_haves:
  truths:
    - "LayerManager has renderMode: 'canvas' | 'glyph' (default 'glyph'); setRenderMode/getRenderMode work"
    - "_flushPage() in glyph mode is byte-identical to today (containerTotalNum:3, 3 text containers, no _compositeAndPush) — existing Test 8b still passes unchanged"
    - "_flushPage() in canvas mode calls rebuildPageContainer with containerTotalNum:5 then _compositeAndPush()"
    - "_compositeAndPush() serializes the 4 tile pushes (await each — buildHudTiles then pushHudTiles), guarded against a null compositor"
    - "_assertContainerBudget() canvas branch uses fixed 5-container budget; a CanvasLayer returning {image:0,text:0} does not throw; a layer declaring non-zero image/text throws; glyph branch unchanged"
  artifacts:
    - path: "packages/g2-app/src/engine/layer-manager.ts"
      provides: "renderMode + _flushPage selector + _compositeAndPush + canvas budget branch"
      contains: "renderMode"
  key_links:
    - from: "packages/g2-app/src/engine/layer-manager.ts"
      to: "buildHudRasterPageSchema"
      via: "_flushPage canvas-mode schema selector"
      pattern: "buildHudRasterPageSchema"
    - from: "packages/g2-app/src/engine/layer-manager.ts"
      to: "pushHudTiles"
      via: "_compositeAndPush serialized push"
      pattern: "pushHudTiles"
---

<objective>
Wire the compositor substrate into `LayerManager` (RAST-04, RAST-03, RAST-01): add `renderMode: 'canvas' | 'glyph'` (default `'glyph'`), make `_flushPage()` a mode-aware schema selector (implementing the existing `TODO(j0t-05)`), add `_compositeAndPush()` (serialized 4-tile push), and add the canvas-mode fixed-budget branch to `_assertContainerBudget()`. The glyph path stays **byte-identical** to today — all 2668+ existing tests pass unchanged. In Phase 19 the default mode is glyph and no `CanvasLayer` is registered, so canvas mode is exercised only by tests (the compositor returns a blank buffer).

Purpose: Complete the substrate so Phase 20 can flip `renderMode='canvas'` and register real `CanvasLayer`s. This is the integration point named in CONTEXT.md ("`_flushPage()` is the innesto").

Output: Modified `layer-manager.ts` (renderMode field + optional compositor constructor param + setRenderMode/getRenderMode + mode-aware `_flushPage` + `_compositeAndPush` + canvas budget branch); new canvas-mode tests in `layer-manager.test.ts`; JSDoc de-"PoC" of `pushHudTiles` in `hud-poc-page.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-19-adr-0013-amendment-1-canvas-compositor-core/19-RESEARCH.md
@docs/architecture/0013-hud-raster-rendering.md
@packages/g2-app/src/engine/layer-manager.ts
@packages/g2-app/src/engine/container-registry.ts
@packages/g2-app/src/engine/canvas-compositor.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LayerManager renderMode + mode-aware _flushPage + _compositeAndPush</name>
  <read_first>
    - packages/g2-app/src/engine/layer-manager.ts: imports (lines 37-48), constructor (lines 99-102: `constructor(private readonly bridge, private readonly debugMirror?)`), private mapMode field (line 73), _flushPage (lines 466-477) + its TODO(j0t-05) (lines 456-461)
    - packages/g2-app/src/engine/container-registry.ts: buildHudRasterPageSchema + HUD_RASTER_CONTAINER_TOTAL (added in plan 19-03), buildStatusViewTextContainers, BOOT_CONTAINER_TOTAL
    - packages/g2-app/src/engine/canvas-compositor.ts: CanvasCompositorLike (added in plan 19-03)
    - packages/g2-app/src/hud/hud-raster-frame.ts: buildHudTiles (400×200 after plan 19-02)
    - packages/g2-app/src/hud/hud-poc-page.ts: pushHudTiles (already serialized via for...of + await — lines ~197-215)
    - .planning/phases/EVF-19-adr-0013-amendment-1-canvas-compositor-core/19-RESEARCH.md "Pattern 5: LayerManager Modifications", "Pattern 7", Pitfall 2 (null-compositor guard), and the Open Question #3 (optional 3rd constructor param — backward compat)
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts Test 8b (line 188: pins glyph _flushPage = containerTotalNum:3 — must stay passing unchanged)
  </read_first>
  <behavior>
    - getRenderMode() defaults to 'glyph'; setRenderMode('canvas') then getRenderMode()==='canvas'
    - glyph mode _flushPage: rebuildPageContainer called once with containerTotalNum:3 (buildStatusViewTextContainers), imageObject:[], NO _compositeAndPush (existing Test 8b byte-identical)
    - canvas mode _flushPage: rebuildPageContainer called once with containerTotalNum:5 (buildHudRasterPageSchema), THEN _compositeAndPush invoked
    - _compositeAndPush with a registered compositor: calls compositor.composite() → buildHudTiles → pushHudTiles, with updateImageRawData called 4 times sequentially (CM-01)
    - _compositeAndPush with null compositor: returns without throwing (Pitfall 2)
    - existing LayerManager construction `new LayerManager(bridge, debugMirror)` still compiles (compositor 3rd param optional)
  </behavior>
  <action>
    Add imports to layer-manager.ts: `buildHudRasterPageSchema`, `HUD_RASTER_CONTAINER_TOTAL` from './container-registry.js'; `type CanvasCompositorLike` from './canvas-compositor.js'; `buildHudTiles` from '../hud/hud-raster-frame.js'; `pushHudTiles` from '../hud/hud-poc-page.js'.

    Add private field `renderMode: 'canvas' | 'glyph' = 'glyph'` (after mapMode, line ~73) with JSDoc citing ADR-0013 Amendment 1 and noting default glyph until canvas infra lands. Add `private readonly compositor: CanvasCompositorLike | null;`. Change the constructor to accept an OPTIONAL 3rd param `compositor?: CanvasCompositorLike` and assign `this.compositor = compositor ?? null;` (keep bridge/debugMirror as parameter properties; the existing two-arg call sites in boot-engine-core.ts and tests must still compile — do NOT make compositor required). Add `setRenderMode(mode)` and `getRenderMode()` public methods.

    Replace `_flushPage()` with the mode selector (19-RESEARCH Pattern 5 / Code Examples): build `schema` = `renderMode==='canvas' ? buildHudRasterPageSchema() : { containerTotalNum: BOOT_CONTAINER_TOTAL, textObject: buildStatusViewTextContainers(), imageObject: [] }`; `await this.bridge.rebuildPageContainer(new RebuildPageContainer(schema))`; then `if (renderMode==='canvas') await this._compositeAndPush()`. Preserve the existing single-call contract (exactly one rebuildPageContainer per bundle). REMOVE the `TODO(j0t-05)` comment and replace it with a `@see docs/architecture/0013-hud-raster-rendering.md Amendment 1` reference (INV-4: the TODO is now resolved — leaving it would be a dead TODO violation). Keep the j0t-05 render-blank-fix JSDoc context but update it to describe the two-mode selector.

    Add `private async _compositeAndPush(): Promise<void>`: `if (this.compositor === null) return;` (Pitfall 2 null guard); `const rgba = this.compositor.composite();` `const tiles = buildHudTiles(rgba);` `await pushHudTiles(this.bridge, tiles);` — pushHudTiles is already serialized (for...of + await per tile), do NOT use Promise.all (anti-pattern). JSDoc cites the SDK no-concurrent-updateImageRawData constraint + ADR-0013 Amendment 1.

    In hud-poc-page.ts, update the `pushHudTiles` JSDoc to drop the "PoC-only" qualifier and note it is now the production serialized-push path called from LayerManager._compositeAndPush() — NO functional change to the function body (the for...of + await loop is already correct).

    Add tests to layer-manager.test.ts: (a) getRenderMode defaults 'glyph'; setRenderMode flips it. (b) canvas-mode _flushPage: construct `new LayerManager(bridge, debugMirror, fakeCompositor)`, setRenderMode('canvas'), trigger a bundle/flush, assert rebuildPageContainer called once with containerTotalNum===5 AND compositor.composite() was called AND pushHudTiles path ran. (c) CM-01: spy on bridge.updateImageRawData, assert it is called 4 times sequentially (mock buildHudTiles to return 4 tiles, or use the real 320000 blank buffer → 4 tiles). (d) null-compositor canvas mode: _compositeAndPush returns without throwing. Use the existing bridge mock pattern (rebuildPageContainer: vi.fn().mockResolvedValue(true), add updateImageRawData: vi.fn().mockResolvedValue(true)). Do NOT modify the existing Test 8b — it must still assert glyph containerTotalNum===3 unchanged.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app exec vitest run src/engine/__tests__/layer-manager.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderMode" packages/g2-app/src/engine/layer-manager.ts` ≥ 1; setRenderMode/getRenderMode present
    - `grep -c "TODO(j0t-05)" packages/g2-app/src/engine/layer-manager.ts` returns 0 (TODO removed, INV-4)
    - layer-manager.test.ts: existing Test 8b (glyph containerTotalNum:3) passes UNCHANGED; new canvas-mode test asserts containerTotalNum:5 + _compositeAndPush invoked; CM-01 asserts updateImageRawData called 4× sequentially; null-compositor test does not throw
    - `new LayerManager(bridge, debugMirror)` (2-arg) still compiles (boot-engine-core.ts untouched, glyph default)
    - `corepack pnpm --filter @evf/g2-app exec tsc --noEmit` clean
  </acceptance_criteria>
  <done>LayerManager has renderMode + a mode-aware _flushPage selector + serialized _compositeAndPush; glyph path byte-identical (Test 8b unchanged); canvas mode pushes 4 tiles serially via the compositor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _assertContainerBudget() canvas-mode fixed-budget branch</name>
  <read_first>
    - packages/g2-app/src/engine/layer-manager.ts _assertContainerBudget (lines 363-377: current glyph per-layer sum, throws panel_mount_budget_exceeded when img>4 || txt>8) + the ordering note (runs AFTER _assertCaptureInvariant, LMT-CB-03 pins this)
    - .planning/phases/EVF-19-adr-0013-amendment-1-canvas-compositor-core/19-RESEARCH.md "Pattern 5: _assertContainerBudget canvas branch" + Pitfall 1 (false-fire) + the anti-pattern "do not reorder capture vs budget assertion"
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts LMT-CB-* tests (existing glyph budget + capture-ordering tests — must stay passing)
  </read_first>
  <behavior>
    - canvas mode: a layer whose getContainerCount() returns {image:0,text:0} does NOT throw
    - canvas mode: a layer declaring {image:1,text:0} or {image:0,text:1} throws panel_mount_budget_exceeded (canvas layers must declare zero containers)
    - glyph mode: existing per-layer sum behavior byte-identical (img>4 || txt>8 throws; otherwise passes) — existing LMT-CB tests unchanged
    - capture-invariant still asserted before budget (LMT-CB-03 ordering preserved)
  </behavior>
  <action>
    Add an early `if (this.renderMode === 'canvas') { ... return; }` branch at the top of `_assertContainerBudget()` per 19-RESEARCH Pattern 5: in canvas mode the budget is FIXED at page creation (5 containers); iterate `this.layers.values()`, read `getContainerCount?.() ?? { image: 0, text: 1 }`, and throw `LayerManagerError('panel_mount_budget_exceeded', ...)` ONLY if any layer declares `image > 0 || text > 0` (canvas layers must return {image:0,text:0} per ADR-0013 Amendment 1 decision #3; a non-zero count signals a mis-classified glyph layer in canvas mode). Then `return` — do NOT fall through to the per-layer sum. Keep the existing glyph-mode per-layer sum (img>4 || txt>8 → throw) UNCHANGED below the branch. Add a JSDoc note explaining the fixed-budget rationale + Pitfall 1 (per-layer sum is meaningless in canvas mode; MapBaseLayer's {image:4,text:1} would false-fire). Do NOT move this method relative to `_assertCaptureInvariant` (capture assertion must still run first — LMT-CB-03).

    Add tests to layer-manager.test.ts: canvas-mode budget — (a) mount a stub layer with getContainerCount()→{image:0,text:0} in canvas mode → no throw; (b) mount a stub layer with getContainerCount()→{image:1,text:0} in canvas mode → throws panel_mount_budget_exceeded; (c) confirm glyph-mode budget behavior is unchanged (an existing LMT-CB test or a new explicit glyph-mode assertion). Use setRenderMode('canvas') to enter canvas mode.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app exec vitest run src/engine/__tests__/layer-manager.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - canvas-mode budget tests pass: {image:0,text:0} layer → no throw; {image:1,text:0} layer → throws panel_mount_budget_exceeded
    - all existing LMT-CB-* glyph budget tests + LMT-CB-03 capture-ordering test pass UNCHANGED
    - no false `capture_invariant_violated` / `panel_mount_budget_exceeded` when mounting a canvas-mode layer with {image:0,text:0}
    - `tsc --noEmit` clean
  </acceptance_criteria>
  <done>_assertContainerBudget() has a canvas-mode fixed-budget branch that validates canvas layers declare {image:0,text:0} without false-firing; glyph budget + capture-ordering behavior byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate — 2668+ tests byte-identical</name>
  <read_first>
    - .planning/phases/EVF-19-adr-0013-amendment-1-canvas-compositor-core/19-VALIDATION.md (19-REGRESSION row: full suite green, zero regressions)
    - CLAUDE.md §Build/Test/Lint Commands + CI Gate 8 (socketlib handler count = 17)
  </read_first>
  <action>
    Run the full workspace test suite, typecheck, and lint to confirm zero regressions across the 2668+ existing tests after all Phase 19 changes. This is render-only infrastructure: the glyph path must be byte-identical, INV-1 fixtures must not break, and CI Gate 8 socketlib handler count must remain 17 (no socketlib changes this phase). If any pre-existing test fails, the change is a regression and must be fixed before this task is done (the geometry/test updates in plan 19-02 are the only intentional fixture changes; everything else must be untouched). Do NOT add/modify socketlib handlers. Confirm `corepack pnpm test`, `corepack pnpm typecheck`, and `corepack pnpm lint:ci` all exit 0.
  </action>
  <verify>
    <automated>corepack pnpm test 2>&1 | tail -15 && corepack pnpm typecheck 2>&1 | tail -5 && corepack pnpm lint:ci 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `corepack pnpm test` exits 0 with ≥2668 tests passing (new Phase 19 tests are additive on top)
    - `corepack pnpm typecheck` exits 0
    - `corepack pnpm lint:ci` exits 0
    - CI Gate 8: socketlib handler count still 17 (no socketlib files changed in Phase 19 — confirm `git diff --stat` shows no socketlib handler changes)
    - INV-1 glyph fixtures unchanged (no glyph-mode snapshot diffs)
  </acceptance_criteria>
  <done>Full workspace suite + typecheck + lint green; zero regressions; glyph path byte-identical; socketlib count preserved at 17. Phase 19 substrate is complete and safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| g2-app → G2 SDK | `_flushPage()` → `bridge.rebuildPageContainer`; `_compositeAndPush()` → `bridge.updateImageRawData`. Both are existing SDK calls already inside the trust boundary; no new input parsing, network, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Tampering | _compositeAndPush concurrent send | mitigate | pushHudTiles uses serialized for...of + await (no Promise.all); SDK no-concurrent-updateImageRawData constraint honored; CM-01 test asserts 4 sequential calls. |
| T-19-05 | Denial of Service | _compositeAndPush null compositor | mitigate | null-guard returns early (Pitfall 2); default renderMode='glyph' ensures canvas path never triggers accidentally in production. |
| T-19-06 | Elevation of Privilege | capture-invariant ordering | accept | _assertCaptureInvariant runs before _assertContainerBudget (LMT-CB-03 preserved); canvas branch does not reorder; INV-5 unchanged. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in Phase 19. Gate not triggered. |
</threat_model>

<verification>
- `corepack pnpm --filter @evf/g2-app exec vitest run src/engine/__tests__/layer-manager.test.ts` → glyph Test 8b unchanged + new canvas tests green
- `corepack pnpm test` + `corepack pnpm typecheck` + `corepack pnpm lint:ci` → all exit 0 (≥2668 tests)
- `grep -c "TODO(j0t-05)" packages/g2-app/src/engine/layer-manager.ts` → 0
- CI Gate 8 socketlib handler count = 17 (no socketlib diff)
</verification>

<success_criteria>
LayerManager carries renderMode with a byte-identical glyph path and a working canvas path (5-container schema + serialized _compositeAndPush + fixed-budget assertion). RAST-03 (canvas budget audit), RAST-04 (renderMode selector + glyph coexistence), and RAST-01 (serialized push wired) satisfied. Full suite green with zero regressions — Phase 19 substrate complete; Phase 20 can flip renderMode='canvas' and register real CanvasLayers.
</success_criteria>

<output>
Create `.planning/phases/EVF-19-adr-0013-amendment-1-canvas-compositor-core/19-04-SUMMARY.md` when done
</output>
