---
phase: 25-promozione-raster-a-default-boot-fallback-glyph
plan: 03
type: execute
wave: 3
depends_on: ["25-01"]
files_modified:
  - packages/g2-app/src/hud/boot-hud-raster-poc.ts
  - packages/g2-app/src/hud/hud-poc-page.ts
  - packages/g2-app/src/hud/hud-live-render.ts
  - packages/g2-app/src/hud/hud-poc-page.test.ts
  - packages/g2-app/src/hud/hud-live-render.test.ts
  - packages/g2-app/src/internal/launch.ts
  - packages/g2-app/src/__tests__/launch.test.ts
autonomous: true
requirements: [RPROMO-02]

must_haves:
  truths:
    - "The ?hud=raster PoC trigger is removed from launch.ts; the no-auth dev branch always calls bootEngine (canvas default) — D-25.2, success criterion #2"
    - "All PoC files are deleted (boot-hud-raster-poc.ts, hud-poc-page.ts, hud-live-render.ts + their PoC-only test files); zero dead/unreachable code remains — INV-4, D-25.1"
    - "Canvas default boot at boot-engine-core line 644 is intact (no ?hud=raster gate) — D-25.2"
    - "All gates green: pnpm test + typecheck + lint:ci (changed-file scoped re pre-existing deploy lint); socketlib count == 17; ~60 INV-1 glyph fixtures byte-identical — D-25.4, D-25.5"
  artifacts:
    - path: "packages/g2-app/src/internal/launch.ts"
      provides: "Launch with the ?hud=raster branch + bootHudRasterPoc dep removed; bootEngine-only no-auth path"
      contains: "bootEngine"
  key_links:
    - from: "packages/g2-app/src/internal/launch.ts"
      to: "bootEngine"
      via: "no-auth dev branch (no hudMode if/else)"
      pattern: "deps.bootEngine\\("
---

<objective>
Remove the entire isolated `?hud=raster` PoC scaffolding now that Plan 01 has extracted the only reused helper (`pushHudTiles`). Delete the three PoC files (`boot-hud-raster-poc.ts`, `hud-poc-page.ts`, `hud-live-render.ts`) and their PoC-only test files; strip the `?hud=raster` branch + `bootHudRasterPoc` dep from `launch.ts`. This is the INV-4 zero-dead-code closure of Phase 25 (D-25.1). The canvas path is already the production default (boot-engine-core line 644 since Phase 20), so removing the PoC leaves the real boot path untouched (D-25.2).

Purpose: The PoC was a parallel dev path that predated the real canvas engine. RESEARCH symbol-classification confirmed (exhaustive grep) that every remaining symbol in these three files is PoC-only after Plan 01 — safe to delete with grep-zero guards.

Output: Three PoC files + two PoC test files deleted; launch.ts simplified to a single bootEngine call in the no-auth branch; launch.test.ts adapted; all grep guards return 0 repo-wide (non-doc); full regression suite + typecheck + lint:ci green; socketlib==17; INV-1 fixtures unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-CONTEXT.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-RESEARCH.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the ?hud=raster branch + bootHudRasterPoc dep from launch.ts and adapt launch.test.ts</name>
  <files>packages/g2-app/src/internal/launch.ts, packages/g2-app/src/__tests__/launch.test.ts</files>
  <read_first>
    - packages/g2-app/src/internal/launch.ts (import line 47 `{ type BootHudRasterPocOpts, bootHudRasterPoc } from '../hud/boot-hud-raster-poc.js'`; LaunchDeps.bootHudRasterPoc field line 79; deps default line 117; the hudMode read line 149 + the if(hudMode==='raster') branch lines 152-163; the header JSDoc lines 9-10 + 35 referencing Branch A-raster)
    - packages/g2-app/src/__tests__/launch.test.ts (makeDeps line 29 — does NOT set bootHudRasterPoc; LAUNCH-A/B/C tests + LAUNCH-W4 grep gate line 137 — no existing bootHudRasterPoc/hudMode test to delete, only confirm none re-introduced)
  </read_first>
  <action>In launch.ts: remove the import on line 47 (BootHudRasterPocOpts + bootHudRasterPoc); remove the bootHudRasterPoc field from the LaunchDeps interface (line 79) including its JSDoc; remove bootHudRasterPoc from the deps default object (line 117); remove the const hudMode = params.get('hud') read (line 149) and collapse the if (hudMode === 'raster') {...} else { await deps.bootEngine({...}) } block (lines 152-176) down to just the bootEngine call body that was in the else branch — the no-auth dev branch now unconditionally calls deps.bootEngine. Keep the surrounding try/catch fail-soft block and its error logging intact (existing tests assert on the launch bootEngine-failed log line). Update the module header JSDoc (lines 9-10, 35) to drop the Branch A-raster / bootHudRasterPoc description (no stale doc — INV-4). Keep the ?actor= param read untouched (RESEARCH §LaunchDeps). In launch.test.ts: confirm makeDeps has no bootHudRasterPoc reference (it doesn't); if any test sets a hud URL param or stubs bootHudRasterPoc, remove it. Rely on LAUNCH-A (or extend it) to assert the no-auth path calls bootEngine. Run typecheck.</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec tsc --noEmit &amp;&amp; corepack pnpm exec vitest run src/__tests__/launch.test.ts src/__tests__/launch-actor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'bootHudRasterPoc' packages/g2-app/src/internal/launch.ts` returns 0
    - `grep -c "params.get('hud')" packages/g2-app/src/internal/launch.ts` returns 0
    - `grep -Ec "hud=raster|Branch A-raster" packages/g2-app/src/internal/launch.ts` returns 0
    - `grep -c 'deps.bootEngine(' packages/g2-app/src/internal/launch.ts` returns at least 1 (bootEngine still the boot path)
    - `corepack pnpm exec vitest run src/__tests__/launch.test.ts src/__tests__/launch-actor.test.ts` exits 0
  </acceptance_criteria>
  <done>launch.ts no-auth branch calls bootEngine unconditionally; bootHudRasterPoc import/field/default and the hudMode read/branch are gone; header JSDoc updated; launch tests green; ?actor= preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the three PoC files + their PoC-only test files (INV-4)</name>
  <files>packages/g2-app/src/hud/boot-hud-raster-poc.ts, packages/g2-app/src/hud/hud-poc-page.ts, packages/g2-app/src/hud/hud-live-render.ts, packages/g2-app/src/hud/hud-poc-page.test.ts, packages/g2-app/src/hud/hud-live-render.test.ts</files>
  <read_first>
    - packages/g2-app/src/hud/boot-hud-raster-poc.ts (PoC entry — entire file PoC-only per RESEARCH symbol table; imports createHudPocPage from hud-poc-page.js + makeSnapshotRenderHandler from hud-live-render.js + pushHudTiles from push-hud-tiles.js after the Plan 01 re-point)
    - packages/g2-app/src/hud/hud-poc-page.ts (after Plan 01: pushHudTiles removed; remaining exports createHudPocPage, buildHudPocPageSchema, HUD_POC_CONTAINERS, HudPocContainerDef are all PoC-only)
    - packages/g2-app/src/hud/hud-live-render.ts (renderRasterHudFrame, makeSnapshotRenderHandler, RasterHudRenderDeps — all PoC-only, superseded by HudDeltaDriver in Phase 24)
    - packages/g2-app/src/hud/hud-poc-page.test.ts (13 tests, PoC-local symbols — pushHudTiles coverage migrated to push-hud-tiles.test.ts in Plan 01)
    - packages/g2-app/src/hud/hud-live-render.test.ts (5 tests, PoC-local symbols)
  </read_first>
  <action>Delete the five files using git rm so deletion is staged: packages/g2-app/src/hud/boot-hud-raster-poc.ts, hud/hud-poc-page.ts, hud/hud-live-render.ts, hud/hud-poc-page.test.ts, hud/hud-live-render.test.ts. Order: delete boot-hud-raster-poc.ts FIRST (it is the consumer of the other two), then hud-poc-page.ts + hud-live-render.ts, then the two test files. After deletion, grep repo-wide for every PoC symbol to prove no dangling reference remains in source — exclude .planning/ docs and the deleted files themselves: bootHudRasterPoc, BootHudRasterPocOpts, renderRasterHudFrame, makeSnapshotRenderHandler, RasterHudRenderDeps, createHudPocPage, buildHudPocPageSchema, HUD_POC_CONTAINERS, HudPocContainerDef, and the string 'hud=raster'. Each must return 0 matches in packages/. Then run typecheck to confirm no broken imports. Note the expected test-count delta in the SUMMARY (-18: 13 from hud-poc-page.test.ts + 5 from hud-live-render.test.ts, offset by +3 from push-hud-tiles.test.ts in Plan 01 and +1 from the Plan 02 atomic test) — RESEARCH Pitfall 4.</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec tsc --noEmit &amp;&amp; corepack pnpm exec vitest run src</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f packages/g2-app/src/hud/boot-hud-raster-poc.ts` (deleted)
    - `test ! -f packages/g2-app/src/hud/hud-poc-page.ts` (deleted)
    - `test ! -f packages/g2-app/src/hud/hud-live-render.ts` (deleted)
    - `test ! -f packages/g2-app/src/hud/hud-poc-page.test.ts &amp;&amp; test ! -f packages/g2-app/src/hud/hud-live-render.test.ts` (test files deleted)
    - `grep -rEl 'bootHudRasterPoc|BootHudRasterPocOpts|renderRasterHudFrame|makeSnapshotRenderHandler|RasterHudRenderDeps|createHudPocPage|buildHudPocPageSchema|HUD_POC_CONTAINERS|HudPocContainerDef|hud=raster' packages/ | wc -l` returns 0
    - `corepack pnpm exec tsc --noEmit` exits 0
    - `corepack pnpm exec vitest run src` exits 0 (all remaining g2-app tests pass)
  </acceptance_criteria>
  <done>All five PoC files deleted; every PoC symbol returns grep-zero across packages/; typecheck + full g2-app suite green; canvas default boot (boot-engine-core:644) untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate — tests + typecheck + lint (scoped) + socketlib==17 + INV-1 fixtures byte-identical</name>
  <files>packages/g2-app/src/internal/boot-engine-core.ts</files>
  <read_first>
    - CLAUDE.md (INV-1 ~60 glyph ASCII fixtures byte-identical; INV-4 zero dead code; socketlib count == 17 / CI Gate 8; pre-existing deploy/sync-app-whitelist.mjs:62 useTemplate lint error + foundry-mcp mcp-inspector-smoke.test.ts:44 — OUT OF SCOPE, do NOT fix)
    - packages/g2-app/src/internal/boot-engine-core.ts (line 644 setRenderMode('canvas') — confirm intact, the canvas default boot)
    - packages/shared-render/src/fixtures/ (~96 .txt INV-1 fixtures; ~60 glyph subset — must be byte-identical, never modified by this phase)
  </read_first>
  <action>Run the full phase regression gate (this task does NOT edit boot-engine-core.ts — it is listed only as the grep-confirm target; the Plan 02 wire is the sole edit to that file). (1) corepack pnpm test (workspace-wide) — must be green; the only expected delta is the PoC test-count drop (-18) offset by push-hud-tiles.test.ts (+3) and the Plan 02 atomic-switch test (+1). (2) corepack pnpm typecheck — exit 0. (3) Lint: full corepack pnpm lint:ci WILL surface the two pre-existing useTemplate errors (deploy/sync-app-whitelist.mjs:62 + foundry-mcp mcp-inspector-smoke.test.ts:44). Per D-25.5 these are pre-existing and OUT OF SCOPE — do NOT fix them. Instead run lint scoped to the files this phase changed and confirm zero errors on them: biome check on push-hud-tiles.ts, launch.ts, boot-engine-core.ts, layer-manager.ts, hud-delta-driver.ts. Document in the SUMMARY that the two deploy/foundry-mcp errors pre-exist on main and are NOT introduced by Phase 25. (4) socketlib count == 17: corepack pnpm --filter @evf/foundry-module test (MOD-CAT-01 gate) — defense check (no foundry-module file touched this phase). (5) INV-1 fixtures byte-identical: git status --porcelain packages/shared-render/src/fixtures/ returns EMPTY (zero modified fixtures) — if any fixture shows modified, STOP and flag as a regression bug (D-25.4). (6) Canvas default boot intact: grep for layerManager.setRenderMode('canvas') in boot-engine-core.ts returns at least 1.</action>
  <verify>
    <automated>corepack pnpm typecheck &amp;&amp; corepack pnpm test &amp;&amp; corepack pnpm exec biome check packages/g2-app/src/hud/push-hud-tiles.ts packages/g2-app/src/internal/launch.ts packages/g2-app/src/internal/boot-engine-core.ts packages/g2-app/src/engine/layer-manager.ts packages/g2-app/src/engine/hud-delta-driver.ts</automated>
  </verify>
  <acceptance_criteria>
    - `corepack pnpm typecheck` exits 0
    - `corepack pnpm test` exits 0 (full workspace suite green)
    - `corepack pnpm exec biome check` on the 5 changed files exits 0 (no NEW lint errors introduced — D-25.5)
    - `git status --porcelain packages/shared-render/src/fixtures/ | wc -l` returns 0 (INV-1 fixtures byte-identical — D-25.4)
    - `corepack pnpm --filter @evf/foundry-module test` exits 0 with MOD-CAT-01 socketlib count == 17 (D-25.5)
    - `grep -c "layerManager.setRenderMode('canvas')" packages/g2-app/src/internal/boot-engine-core.ts` returns at least 1 (canvas default boot intact — D-25.2)
  </acceptance_criteria>
  <done>Full workspace test + typecheck green; changed-file lint clean (two pre-existing deploy/foundry-mcp errors documented as out-of-scope); socketlib==17; INV-1 glyph fixtures byte-identical; canvas default boot confirmed.</done>
</task>

</tasks>

<verification>
- corepack pnpm test (workspace) green — PoC removal leaves no broken imports
- corepack pnpm typecheck exit 0
- Changed-file biome check clean; pre-existing deploy + foundry-mcp lint errors documented as out-of-scope (D-25.5)
- All PoC symbols grep-zero across packages/ (INV-4)
- INV-1 ~60 glyph fixtures byte-identical (git status clean on fixtures dir) — D-25.4
- socketlib count == 17 (D-25.5)
</verification>

<success_criteria>
- ?hud=raster trigger + bootHudRasterPoc dep removed from launch.ts; no-auth path is bootEngine-only (D-25.2)
- All five PoC files deleted; zero dead/unreachable code (INV-4, D-25.1)
- Canvas default boot at boot-engine-core:644 intact (D-25.2)
- Full gates green; socketlib==17; INV-1 glyph fixtures unchanged (D-25.4, D-25.5)
</success_criteria>

<artifacts_removed>
Files deleted (INV-4 zero dead code — D-25.1):
- `packages/g2-app/src/hud/boot-hud-raster-poc.ts` (PoC entry)
- `packages/g2-app/src/hud/hud-poc-page.ts` (PoC page; pushHudTiles already extracted in Plan 01)
- `packages/g2-app/src/hud/hud-live-render.ts` (PoC live-render; superseded by HudDeltaDriver)
- `packages/g2-app/src/hud/hud-poc-page.test.ts` (PoC-only tests)
- `packages/g2-app/src/hud/hud-live-render.test.ts` (PoC-only tests)

Symbols removed (grep-zero guards):
- bootHudRasterPoc, BootHudRasterPocOpts (boot-hud-raster-poc.ts)
- createHudPocPage, buildHudPocPageSchema, HUD_POC_CONTAINERS, HudPocContainerDef (hud-poc-page.ts)
- renderRasterHudFrame, makeSnapshotRenderHandler, RasterHudRenderDeps (hud-live-render.ts)
- LaunchDeps.bootHudRasterPoc field + the 'hud=raster' URL trigger (launch.ts)
</artifacts_removed>

<output>
Create `.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-03-SUMMARY.md` when done
</output>
