---
phase: 22-features-biography-schema-extension
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - packages/g2-app/src/panels/character-sheet-tab-renderers.ts
  - packages/g2-app/src/panels/canvas-character-sheet-panel.ts
  - packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts
  - packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts
autonomous: true
requirements: [RDATA-03, RDATA-04]

must_haves:
  truths:
    - "Features tab renders real snapshot.feats data, not the DEFAULT_FEATS fixture"
    - "Biography tab renders real snapshot.biography fields, not hardcoded text"
    - "Bio tab is scrollable: scroll-down/up adjusts _scrollOffset (within-tab) instead of cycling tabs; Feats tab too"
    - "DEFAULT_FEATS fixture and the hardcoded bio text are removed (INV-4 zero dead code)"
    - "Empty feats/biography render a graceful empty state (no crash)"
  artifacts:
    - path: "packages/g2-app/src/panels/character-sheet-tab-renderers.ts"
      provides: "renderFeatsTab/renderBioTab consuming snapshot data; paintFeatsTab/paintBioTab accepting scrollOffset; FeatDef.category widened to string"
      contains: "snapshot.feats"
    - path: "packages/g2-app/src/panels/canvas-character-sheet-panel.ts"
      provides: "tab-aware onEvent: Bio/Feats scroll content via _scrollOffset; _scrollOffset passed into paintFeatsTab/paintBioTab"
      contains: "_scrollOffset"
  key_links:
    - from: "renderFeatsTab"
      to: "snapshot.feats"
      via: "snapshot.feats ?? [] mapping"
      pattern: "snapshot\\.feats"
    - from: "renderBioTab"
      to: "snapshot.biography"
      via: "snapshot.biography?.personality etc"
      pattern: "snapshot\\.biography"
    - from: "CanvasCharacterSheetPanel.onEvent"
      to: "_scrollOffset"
      via: "bio/feats tab-aware scroll increment/decrement"
      pattern: "_scrollOffset(\\+\\+|--)"
---

<objective>
Wire real data into the canvas Features and Biography tab renderers and make the Bio (and Feats) tabs scrollable via the existing `_scrollOffset`. This is the atomic completion of Phase 22 (success criterion #3): with this plan, schema + reader + both renderers ship together — no intermediate state where the snapshot carries data but the tabs still show fixtures.

Purpose: RDATA-03 (Features tab → real `snapshot.feats`) and RDATA-04 (Biography tab → real `snapshot.biography` + gesture-driven scroll per D-22.5). Removes the `DEFAULT_FEATS` array and hardcoded bio text (INV-4 dead-code rule).
Output: updated `renderFeatsTab`/`renderBioTab`/`paintFeatsTab`/`paintBioTab`, tab-aware `onEvent` scroll in the panel, widened `FeatDef.category`, and CSTR-FEAT-* / CSTR-BIO-* / RCSP-BIO-* tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-VALIDATION.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-01-SUMMARY.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-02-SUMMARY.md
@packages/g2-app/src/panels/character-sheet-tab-renderers.ts
@packages/g2-app/src/panels/canvas-character-sheet-panel.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderFeatsTab/renderBioTab consume real snapshot data; widen FeatDef.category; remove dead fixtures (RED→GREEN)</name>
  <files>packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts, packages/g2-app/src/panels/character-sheet-tab-renderers.ts</files>
  <read_first>
    - packages/g2-app/src/panels/character-sheet-tab-renderers.ts (FeatDef interface lines 683-688; DEFAULT_FEATS lines 690-713; renderFeatsTab lines 727+ esp. the DEFAULT_FEATS.filter at line 762; renderBioTab lines 877-916 with hardcoded personalityText/idealText/bondText/flawText/backstoryText; stripHtml line 804; wordWrap line 814; truncateUnicode line 135; ROW_COUNT/INNER_WIDTH constants)
    - packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts (existing CSTR-* test conventions; the RCSP-PAINT-ADDITIVE contract that render*Tab returns 18 rows × 66 code-points — PRESERVE this width invariant)
    - .planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md (Code Example 4 + 5; Open Question 1 widening FeatDef.category to string; §State of the Art — DEFAULT_FEATS becomes dead code)
    - .planning/phases/EVF-22-features-biography-schema-extension/22-01-SUMMARY.md (exact FeatEntry/BiographySnapshot field names)
  </read_first>
  <behavior>
    - CSTR-FEAT-1: renderFeatsTab(snapshotWithFeats, 'en', 0) includes a real feat name from snapshot.feats and does NOT include any DEFAULT_FEATS-only name (e.g. 'Second Wind' must be absent unless present in the snapshot)
    - CSTR-FEAT-2: renderFeatsTab(snapshotWith feats:[], 'en', 0) returns 18 rows (graceful empty state, no crash)
    - CSTR-FEAT-3: renderFeatsTab still returns exactly ROW_COUNT rows each INNER_WIDTH code-points wide (width invariant preserved)
    - CSTR-BIO-1: renderBioTab(snapshotWithBio, 'en', 0) includes snapshot.biography.personality text and does NOT include the old hardcoded 'guerriero onesto' string
    - CSTR-BIO-2: renderBioTab skips a section whose field is empty-string (no header line emitted for empty personality)
    - CSTR-BIO-3: renderBioTab(snapshot with biography undefined, 'en', 0) → 18 blank-ish rows, no crash (graceful fallback per D-22.1)
  </behavior>
  <action>
    Per RDATA-03: widen `FeatDef.category` from the `'class'|'race'|'background'|'feat'` union to `string` (Open Question 1 resolution — consistent with `FeatEntrySchema.category: z.string()`). In `renderFeatsTab`, replace the `DEFAULT_FEATS.filter(...)` data source with `snapshot.feats ?? []` mapped to FeatDef (`{category: f.category, name: f.name, isOrigin: f.isOrigin, desc: truncateUnicode(f.description, 40)}`); when the array is empty, render the graceful empty state (keep returning ROW_COUNT rows). Preserve the section-grouping/`[Origine]`-annotation logic but drive it off the real categories. DELETE the `DEFAULT_FEATS` constant (INV-4 dead code). Per RDATA-04: in `renderBioTab`, replace the five hardcoded `personalityText/idealText/bondText/flawText/backstoryText` literals with `snapshot.biography?.personality ?? ''` etc.; keep the existing `addSection`/`wordWrap` machinery but make `addSection` skip empty text (no header for empty fields — D-22.4). Backstory is already HTML-stripped reader-side, but keep the existing wordWrap. Preserve the 18-rows × 66-code-points width invariant the RCSP-PAINT-ADDITIVE test asserts. Write CSTR-FEAT-1..3 + CSTR-BIO-1..3 first (RED), then implement to green. Do NOT re-introduce DEFAULT_FEATS as a fallback (research anti-pattern).
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run -t "CSTR-FEAT"</automated>
    <automated>pnpm --filter @evf/g2-app test -- --run -t "CSTR-BIO"</automated>
    <automated>pnpm --filter @evf/g2-app test -- --run -t "RCSP-PAINT-ADDITIVE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DEFAULT_FEATS" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` returns 0 (dead fixture removed)
    - `grep -n "snapshot.feats" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` returns a match
    - `grep -n "snapshot.biography" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` returns a match
    - `grep -n "category: string" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` shows FeatDef widened (or equivalent `readonly category: string`)
    - CSTR-FEAT-* + CSTR-BIO-* + RCSP-PAINT-ADDITIVE tests all pass (width invariant preserved)
  </acceptance_criteria>
  <done>Both renderers consume real snapshot data; DEFAULT_FEATS + hardcoded bio text removed; empty states graceful; 18×66 width invariant intact; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: paintFeatsTab/paintBioTab accept scrollOffset; panel onEvent becomes tab-aware (Bio/Feats scroll content) (RED→GREEN)</name>
  <files>packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts, packages/g2-app/src/panels/character-sheet-tab-renderers.ts, packages/g2-app/src/panels/canvas-character-sheet-panel.ts</files>
  <read_first>
    - packages/g2-app/src/panels/character-sheet-tab-renderers.ts (paintFeatsTab lines 1136-1157, paintBioTab lines 1168-1189 — both hardcode `0` when calling render*Tab; add an optional trailing scrollOffset param)
    - packages/g2-app/src/panels/canvas-character-sheet-panel.ts (onEvent lines 417-442; _scrollOffset line 182; isAtTopBoundary lines 449-451; _paintActiveTab lines 534-557 — where paintFeatsTab/paintBioTab are invoked; TABS import line 68)
    - .planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md (Pattern 4 onEvent patch + Pattern 5 signature extension; Pitfall 5 — do NOT change isAtTopBoundary; §Suggested Test IDs RCSP-BIO-N; Open Question 2 — tap stays tab-cycle)
  </read_first>
  <behavior>
    - RCSP-BIO-1: with activeTab==='bio', a scroll-down gesture increments _scrollOffset (does NOT change _activeTabIndex)
    - RCSP-BIO-2: with activeTab==='bio' and _scrollOffset>0, scroll-up decrements _scrollOffset (does NOT cycle tab)
    - RCSP-BIO-3: with activeTab==='bio' and _scrollOffset===0, scroll-up cycles tab backward AND isAtTopBoundary() returns true (over-scroll gate unchanged per Pitfall 5)
    - RCSP-BIO-4: with activeTab a NON-scrollable tab (e.g. 'main'), scroll-down cycles tab forward (existing behaviour preserved)
    - RCSP-BIO-5 (feats parity): with activeTab==='feats', scroll-down increments _scrollOffset (Feats also scrollable, research Pattern 4 note)
    - RCSP-PAINT-SCROLL: paintBioTab(ctx, snapshot, bounds, font, locale, 3) forwards scrollOffset 3 to renderBioTab (spy/observe windowed output differs from offset 0)
  </behavior>
  <action>
    Per Pattern 5 (RDATA-04): extend `paintBioTab` and `paintFeatsTab` with a trailing optional param `scrollOffset = 0` and pass it into `renderBioTab`/`renderFeatsTab` instead of the hardcoded `0` (backward compatible — existing callers unaffected). Per Pattern 4 (D-22.5): rewrite the `case 'scroll':` block in `CanvasCharacterSheetPanel.onEvent` to be tab-aware. Compute `const tab = TABS[this._activeTabIndex] ?? 'main'`. For scroll-up: if `tab` is scrollable ('bio'|'feats') AND `_scrollOffset > 0` → `_scrollOffset--`; else cycle tab backward and reset `_scrollOffset = 0`. For scroll-down: if `tab` is scrollable → `_scrollOffset++` (the renderer clamps over-scroll); else cycle tab forward and reset `_scrollOffset = 0`. Keep `tap` as tab-cycle (Open Question 2). Do NOT modify `isAtTopBoundary()` — it stays `return this._scrollOffset === 0` (Pitfall 5; the over-scroll Quick-Action router contract relies on this unchanged). In `_paintActiveTab`, pass `this._scrollOffset` as the new arg to `paintFeatsTab` and `paintBioTab` (lines 553/556). Keep `_persistLastTab()` + `_dirty = true` behaviour. Note: tab-change still resets `_scrollOffset` (existing semantics, single scroll cursor — research anti-pattern says no per-tab independent scroll). Write RCSP-BIO-1..5 + RCSP-PAINT-SCROLL first (RED), then implement. Update any existing RCSP-GEST/SC4 test if the broadened scroll semantics legitimately change its expectation (the old behaviour was "scroll-down always advances tab"; document the change in the test).
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run -t "RCSP-BIO"</automated>
    <automated>pnpm --filter @evf/g2-app test -- --run -t "RCSP-PAINT"</automated>
    <automated>pnpm --filter @evf/g2-app test -- --run</automated>
    <automated>pnpm --filter @evf/g2-app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scrollOffset = 0" packages/g2-app/src/panels/character-sheet-tab-renderers.ts` shows both paint*Tab signatures gained the param (2 matches in the paint functions)
    - `grep -En "_scrollOffset(\+\+|--)" packages/g2-app/src/panels/canvas-character-sheet-panel.ts` shows both increment and decrement in onEvent
    - `grep -n "return this._scrollOffset === 0" packages/g2-app/src/panels/canvas-character-sheet-panel.ts` STILL returns a match (isAtTopBoundary unchanged — Pitfall 5)
    - `grep -n "paintBioTab(ctx, this._snapshot, bounds, font, locale, this._scrollOffset)" packages/g2-app/src/panels/canvas-character-sheet-panel.ts` (or equivalent passing _scrollOffset) returns a match
    - RCSP-BIO-* + RCSP-PAINT-* pass; full g2-app suite green; package typecheck exits 0
  </acceptance_criteria>
  <done>Bio + Feats tabs scroll content via _scrollOffset; paint*Tab forward scrollOffset; isAtTopBoundary unchanged; non-scrollable tabs keep tab-cycle on scroll; full g2-app suite + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS `character.delta` → CanvasCharacterSheetPanel.onSnapshot → renderers | snapshot is validated by CharacterSnapshotSchema.safeParse (T-21-01) before reaching the renderers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | malformed snapshot.feats/biography reaching renderer | mitigate | existing `safeParse` gate in onSnapshot (T-21-01) covers the new optional fields; renderers additionally use `?? []` / `?? ''` defensive fallbacks |
| T-22-06 | DoS | extremely long backstory | mitigate | renderBioTab windows output to ROW_COUNT rows × INNER_WIDTH chars (existing T-05-03-03 bound); scroll offset clamped by renderer |
| T-22-SC | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
- `pnpm --filter @evf/g2-app test -- --run` full suite green (no regressions; CSTR-FEAT/BIO + RCSP-BIO added)
- `pnpm --filter @evf/g2-app exec tsc --noEmit` exits 0
- INV-1: no `.txt` glyph fixture references the removed DEFAULT_FEATS/bio strings (verified during planning) → no byte-aligned fixture update required; the raster INV-1 baseline (`20-raster-inv1.test.ts`) is status-HUD synthetic-RGBA only, unaffected by character-sheet tab text. If a renderer change unexpectedly trips an INV fixture, update it byte-aligned per 21-05 precedent and note in SUMMARY.
- `pnpm lint:ci` clean on touched files
</verification>

<success_criteria>
- Features tab renders real feats; Biography tab renders real bio; both fixtures/hardcoded text removed (INV-4)
- Bio and Feats tabs scroll within-tab via _scrollOffset; tap still cycles tabs; isAtTopBoundary unchanged
- Empty/absent feats/biography render graceful empty states
- Full g2-app suite + typecheck + lint green; no INV-1 fixture drift (or byte-aligned update documented)
- Atomic phase complete: schema (22-01) + reader (22-02) + both renderers (22-03) all shipped together
</success_criteria>

<output>
Create `.planning/phases/EVF-22-features-biography-schema-extension/22-03-SUMMARY.md` when done. Record: DEFAULT_FEATS removal, the new paint*Tab signatures, the tab-aware scroll behaviour, and confirm zero INV-1 fixture drift (or list any byte-aligned fixture updates).
</output>
