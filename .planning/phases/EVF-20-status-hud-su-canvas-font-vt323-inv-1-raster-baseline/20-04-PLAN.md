---
phase: 20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline
plan: 04
type: execute
wave: 2
depends_on: [20-02]
files_modified:
  - packages/validation-harness/src/inv-suite.ts
  - packages/validation-harness/src/__tests__/inv-suite.test.ts
autonomous: true
requirements: [RINV-01]
must_haves:
  truths:
    - "inv:all exposes two labelled INV-1 sub-results: a glyph suite (existing ASCII fixtures) and a raster suite (PNG-tile SHA-256 hashes)"
    - "Both suites must be green for the INV-1 gate to pass; either red fails the gate"
    - "A --testNamePattern that matches zero raster tests fails loudly (FALSE-PASS guard), not silently green"
  artifacts:
    - path: "packages/validation-harness/src/inv-suite.ts"
      provides: "checkInv1Raster() + checkInv1Glyph() merged into the INV-1 result (RINV-01)"
      contains: "checkInv1Raster"
  key_links:
    - from: "packages/validation-harness/src/inv-suite.ts"
      to: "packages/g2-app/src/__tests__/20-raster-inv1.test.ts"
      via: "runSpawn pnpm --filter @evf/g2-app test --testNamePattern RINV-01"
      pattern: "RINV-01"
---

<objective>
Extend the `inv:all` orchestrator so the INV-1 gate reports TWO clearly-labelled
suites: the existing "glyph suite" (ASCII `.txt` fixtures via `@evf/shared-render`)
and the new "raster suite" (the RINV-01 SHA-256 PNG-tile hashes from plan 20-02).
Both green = INV-1 passes; either red = INV-1 fails. The raster check uses the
FALSE-PASS guard pattern from `checkInv5` so a zero-match `--testNamePattern`
fails loudly instead of passing silently.

Purpose: The locked decision requires `inv:all` to validate both the BLE-degraded
glyph fallback AND the new canvas raster output. Without the raster suite wired
into the gate, raster drift would go undetected.
Output: `checkInv1Raster` + glyph/raster result merge in `inv-suite.ts`, with a
guard against silent zero-test passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-CONTEXT.md
@.planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-RESEARCH.md
@.planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-PATTERNS.md
@packages/validation-harness/src/inv-suite.ts
</context>

<artifacts_this_phase_produces>
This plan contributes:
- `checkInv1Raster(repoRoot)` — runs the RINV-01 raster test, with FALSE-PASS guard
- `checkInv1Glyph(repoRoot)` — the renamed existing `checkInv1` (glyph ASCII suite)
- `mergeInv1Results(glyph, raster)` — merges into a single `INV-1` result (red if either red)
- Updated `runInvSuite` wiring + an inv-suite test asserting both labels appear

Consumes from 20-02: the `RINV-01`-named test + committed hash fixture.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add checkInv1Raster + merge glyph/raster into a single labelled INV-1 gate</name>
  <files>
    packages/validation-harness/src/inv-suite.ts,
    packages/validation-harness/src/__tests__/inv-suite.test.ts
  </files>
  <read_first>
    - packages/validation-harness/src/inv-suite.ts (`checkInv1` ~lines 104-117 to mirror/rename; `checkInv5` ~lines 318-378 for the FALSE-PASS guard; `InvId` type ~line 38; `runInvSuite` Promise.all ~lines 438-454; `runSpawn` + `extractFirstError` helpers)
    - .planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-PATTERNS.md (inv-suite.ts MODIFY section — checkInv1Raster body, FALSE-PASS guard lines, mergeInv1Results, runInvSuite modification)
    - .planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-RESEARCH.md (§Pattern 5 Approach A — recommended; §Open Question 3 — keep InvId = 'INV-1' with compound detail)
  </read_first>
  <action>
    Rename the existing `checkInv1` to `checkInv1Glyph` (glyph ASCII suite, runs
    `pnpm --filter @evf/shared-render test -- --run`); keep its detail string
    prefixed `glyph suite:`. Add `checkInv1Raster(repoRoot)` mirroring its shape but
    running `pnpm --filter @evf/g2-app test -- --run --testNamePattern RINV-01`.
    Before reporting green, apply the FALSE-PASS guard from `checkInv5`: combine
    stdout+stderr and if `exitCode === 0` but the output matches
    `/no test files found|no tests found|\b0 tests\b/i`, return status `skipped`
    (or `red` — choose `skipped` per PATTERNS.md) with a `raster suite: no RINV-01
    tests found` detail so a zero-match filter never silently passes. Keep the
    `InvId` union unchanged at `'INV-1'` (Open Question 3 resolution): add
    `mergeInv1Results(glyph, raster)` that returns a single `{ id: 'INV-1' }` result
    — red if either is red (or skipped), green with a compound detail
    `glyph suite: pass; raster suite: pass` only when both are green. In
    `runInvSuite`, replace the single `checkInv1(repoRoot)` entry in the `Promise.all`
    with `mergeInv1Results(await checkInv1Glyph(repoRoot), await checkInv1Raster(repoRoot))`
    (or await both then merge — preserve overall result ordering). Ensure
    `inv:all:skip-inv2` still works (the raster check is independent of INV-2). Add
    a unit test in `inv-suite.test.ts` that stubs `runSpawn` to return: (a) both
    green → INV-1 green with compound detail mentioning both suites; (b) raster red
    → INV-1 red; (c) raster exit-0-with-zero-tests → INV-1 not green (guard fires).
    All new functions get TSDoc (INV-4); use `[EVF]` prefix on any logs.
  </action>
  <verify>
    <automated>pnpm --filter @evf/validation-harness exec tsc --noEmit && pnpm --filter @evf/validation-harness test -- --run inv-suite</automated>
  </verify>
  <acceptance_criteria>
    - `checkInv1Raster` and `checkInv1Glyph` both exist in `inv-suite.ts`
      (`grep -c 'checkInv1Raster\|checkInv1Glyph' packages/validation-harness/src/inv-suite.ts` returns ≥2).
    - The merged INV-1 result is red if either sub-suite is red (unit-tested with stubbed `runSpawn`).
    - The FALSE-PASS guard is present: a zero-test exit-0 from the raster check does
      NOT yield INV-1 green (unit-tested).
    - The INV-1 green detail string references both "glyph suite" and "raster suite".
    - `pnpm --filter @evf/validation-harness exec tsc --noEmit` exits 0;
      `pnpm --filter @evf/validation-harness test -- --run inv-suite` green.
  </acceptance_criteria>
  <done>inv:all INV-1 gate validates glyph + raster suites with a FALSE-PASS guard; both-green required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-runner exit codes → gate verdict | A zero-test pass must not be mistaken for a real pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-GATE | Spoofing | `checkInv1Raster` exit-0 interpretation | mitigate | FALSE-PASS guard (checkInv5 pattern) treats zero-match `--testNamePattern` as not-green |
</threat_model>

<verification>
- `pnpm --filter @evf/validation-harness test -- --run inv-suite` green (merge + guard behaviors)
- `pnpm --filter @evf/validation-harness exec tsc --noEmit` exits 0
- After 20-02 + 20-03 merge: `pnpm --filter @evf/validation-harness inv:all:skip-inv2` reports INV-1 green with both suites
</verification>

<success_criteria>
- inv:all exposes labelled glyph + raster suites; both green is the gate (phase SC4 second half / RINV-01).
- Zero-test filter fails loudly (FALSE-PASS guard).
</success_criteria>

<output>
Create `.planning/phases/EVF-20-status-hud-su-canvas-font-vt323-inv-1-raster-baseline/20-04-SUMMARY.md` when done.
</output>
