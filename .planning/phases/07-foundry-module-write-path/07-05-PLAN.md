---
phase: 07-foundry-module-write-path
plan: "05"
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - packages/foundry-module/src/write-path/reaction-watcher.ts
  - packages/foundry-module/src/write-path/reaction-watcher.test.ts
  - packages/foundry-module/src/write-path/handlers/drop-concentration.ts
  - packages/foundry-module/src/write-path/handlers/drop-concentration.test.ts
  - packages/foundry-module/src/write-path/handlers/index.ts
  - packages/foundry-module/src/pair/socketlib-handlers.ts
  - packages/foundry-module/src/pair/socketlib-handlers.test.ts
  - packages/foundry-module/src/module.ts
  - packages/foundry-module/src/module.test.ts
  - packages/foundry-module/src/types/foundry-globals.d.ts
  - packages/shared-protocol/src/payloads/reaction.ts
  - packages/shared-protocol/src/payloads/reaction.test.ts
  - packages/shared-protocol/src/tools/drop-concentration.ts
  - packages/shared-protocol/src/tools/drop-concentration.test.ts
  - packages/shared-protocol/src/index.ts
  - packages/g2-app/src/panels/reaction-toast-dispatcher.ts
  - packages/g2-app/src/panels/reaction-toast-dispatcher.test.ts
  - packages/g2-app/src/panels/concentration-drop-modal.ts
  - packages/g2-app/src/panels/concentration-drop-modal.test.ts
autonomous: true
requirements: [REACT-01, CONC-01]
must_haves:
  truths:
    - "`reaction-watcher.ts` registers a single `Hooks.on('dnd5e.preUseActivity', ...)` subscription — CORRECT hook name per RESEARCH §Q3 (CONTEXT.md §Area 3 had wrong name `preActivityUse` which does NOT exist in dnd5e 5.3.3)."
    - "Reaction-watcher emits `r1.reaction.available` envelope on NPC activities targeting the player (Phase 7 minimal heuristic: any non-own-actor activity → emit; precise per-trigger matching deferred to Phase 9 COMB-02). NEVER returns `false` from the hook (display-only — V2 ACT-04 will block; RESEARCH §Pitfall 1)."
    - "`ReactionAvailablePayloadSchema` validates `{ kind: 'shield'|'counterspell'|'opportunity-attack', sourceName: string, expiresAt: number }`. `reaction-toast-dispatcher.ts` validates double trust boundary then calls Phase 4b `toastQueue.enqueueToast({ kind: 'reaction', text, dwellMs: 3000 })`. NOT clickable (REACT-01 explicit scope; ACT-04 V2)."
    - "`dropConcentrationHandler` (new ToolHandler<DropConcentrationInput>) resolves `actor.effects.contents.find(e => e.id === args.effect_id)` then calls `await effect.delete()`. Returns `{ success: true, data: { effectId } }` or `{ success: false, error: code }` on actor/effect missing or delete throw."
    - "`DropConcentrationInputSchema` exported from `@evf/shared-protocol/tools/drop-concentration.ts` as `{ actor_id: string.min(1), effect_id: string.min(1) }` strict object."
    - "Phase 4b ConcentrationDropModalPanel's tap path is EXTENDED: now emits BOTH the legacy `conc.drop.confirmed` envelope (preserving 04b-05 ISM-10 + downstream observability) AND a new `tool.invoke` envelope `{ toolId: 'drop-concentration', idempotencyKey: crypto.randomUUID(), args: { actor_id, effect_id } }` to actually drive the write. Phase 4b tests stay green (the legacy envelope still fires); new tests assert the new tool.invoke envelope shape."
    - "6th of 7 Phase 2/3 stubs replaced: socketlib handlerId `evf.setTargets` body and registration renamed in-place to `evf.dropConcentration` (matching Plan 07-01 TOOL_HANDLER_IDS). Total `registerComplexHandler` calls stay 14. module.test.ts assertion `expect(handlers?.has('evf.setTargets')).toBe(true)` updated to `expect(handlers?.has('evf.dropConcentration')).toBe(true)`."
    - "module.ts wires `registerReactionWatcher((payload) => bridgeDeltaEmitter('r1.reaction.available', payload))` inside `Hooks.once('ready')` after `registerHookSubscribers(...)`. NO new socketlib handler registered (count stays 14)."
  artifacts:
    - path: "packages/foundry-module/src/write-path/reaction-watcher.ts"
      provides: "registerReactionWatcher(emit) — Hooks.on('dnd5e.preUseActivity', ...) emitter; NEVER returns false"
      contains: "dnd5e.preUseActivity"
    - path: "packages/foundry-module/src/write-path/handlers/drop-concentration.ts"
      provides: "dropConcentrationHandler — resolves effectId on actor.effects + calls effect.delete()"
      contains: "effect.delete"
    - path: "packages/shared-protocol/src/payloads/reaction.ts"
      provides: "ReactionAvailablePayloadSchema + R1_REACTION_AVAILABLE_TYPE constant"
      contains: "ReactionAvailablePayloadSchema"
    - path: "packages/shared-protocol/src/tools/drop-concentration.ts"
      provides: "DropConcentrationInputSchema (new 8th tool — Phase 3 only shipped 7)"
      contains: "DropConcentrationInputSchema"
    - path: "packages/g2-app/src/panels/reaction-toast-dispatcher.ts"
      provides: "Double trust boundary dispatcher pushing reaction toasts to Phase 4b ToastQueueLayer"
      contains: "ReactionAvailablePayloadSchema"
    - path: "packages/g2-app/src/panels/concentration-drop-modal.ts"
      provides: "EXTENDED tap path: emits tool.invoke envelope for drop-concentration BEFORE existing conc.drop.confirmed (dual-emit, Phase 4b legacy preserved)"
      contains: "drop-concentration"
  key_links:
    - from: "packages/foundry-module/src/write-path/reaction-watcher.ts"
      to: "dnd5e.preUseActivity"
      via: "Hooks.on('dnd5e.preUseActivity', handler) — CORRECT hook name per RESEARCH §Q3"
      pattern: "dnd5e\\.preUseActivity"
    - from: "packages/g2-app/src/panels/concentration-drop-modal.ts"
      to: "packages/foundry-module/src/write-path/handlers/drop-concentration.ts"
      via: "Tap emits tool.invoke envelope → bridge → socketlib evf.dropConcentration → dispatchTool('drop-concentration')"
      pattern: "drop-concentration"
key_anti_patterns:
  - "DO NOT use `dnd5e.preActivityUse` or `dnd5e.preItemUsage` — these hooks DO NOT EXIST in dnd5e 5.3.3 (RESEARCH §Q3 + Pitfall 1)"
  - "DO NOT `return false` from the reaction-watcher hook handler — that CANCELS the NPC action (Phase 7 is display-only; V2 ACT-04 owns blocking)"
---

<objective>
Wave 3: Reaction passive-notification (REACT-01) + concentration-drop write closure (CONC-01). Subscribe to `dnd5e.preUseActivity` (CORRECT hook name — CONTEXT.md had `preActivityUse` which doesn't exist; RESEARCH §Q3 resolved this) and emit `r1.reaction.available` envelopes for NPC activities targeting the player. g2-app's reaction-toast-dispatcher enqueues a Phase 4b ToastQueueLayer entry (3s dwell, NOT clickable). Plan 07-05 also closes CONC-01 by adding a `dropConcentrationHandler` that calls `effect.delete()` and extending the Phase 4b ConcentrationDropModalPanel's tap path to emit the `tool.invoke` envelope (dual-emit alongside the legacy `conc.drop.confirmed` for backward-compat).

Purpose: REACT-01 closure (display-only — ACT-04 stays V2); CONC-01 write closure (Phase 4b modal now wires `effect.delete()` end-to-end). 6-of-7 stubs replaced (skill-check → confirmTemplatePlacement in 07-03; setTargets → dropConcentration here). Plan 07-06 ships bearer rotation + integration smoke + Phase 7 closure.
Output: 5 new module files (reaction-watcher + drop-concentration handler + schemas) + 2 new g2-app files (reaction-toast-dispatcher + concentration-drop-modal extension). socketlib count stays 14. ~45-55 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/07-foundry-module-write-path/07-CONTEXT.md
@.planning/phases/07-foundry-module-write-path/07-RESEARCH.md
@.planning/phases/07-foundry-module-write-path/07-01-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-02-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-03-PLAN.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-05-SUMMARY.md
@packages/foundry-module/src/module.ts
@packages/foundry-module/src/pair/socketlib-handlers.ts
@packages/foundry-module/src/write-path/tool-registry.ts
@packages/foundry-module/src/types/foundry-globals.d.ts
@packages/g2-app/src/panels/conc-conflict-dispatcher.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/status-hud/toast-queue-layer.ts
@packages/shared-protocol/src/envelope.ts
@packages/shared-protocol/src/payloads/concentration.ts

<interfaces>
<!-- Reused from prior phases. -->

dnd5e 5.3.3 hook (RESEARCH §Q3 — VERIFIED source `mixin.mjs` line ~348):
```js
if (Hooks.call("dnd5e.preUseActivity", activity, usageConfig, dialogConfig, messageConfig) === false) return;
```
Hook signature: `(activity, usageConfig, dialogConfig, messageConfig)`. Returning false cancels — DO NOT do that in Phase 7.

Phase 4b `concentration-drop-modal.ts` tap path (existing):
- On tap, emits `{ proto: 'evf-v1', seq, ts, type: 'conc.drop.confirmed', session_id, payload: { effectId } }` via ws.send.
- Phase 7 adds a SECOND envelope emission BEFORE the existing one: `{ proto: 'evf-v1', seq, ts, type: 'tool.invoke', session_id, payload: { toolId: 'drop-concentration', idempotencyKey: crypto.randomUUID(), args: { actor_id, effect_id: effectId } } }`. The actor_id is threaded into the panel constructor (NEW required arg — pulled from the inbound conc-conflict-dispatcher; conc-conflict payload schema needs widening OR actor_id is extracted from `game.user.character?.id` at modal construction time. Plan 07-05 picks the latter — simpler, no schema migration; the player's character is the one dropping concentration, so `actor_id = game.user.character.id`. Document this assumption — V2 multi-character may revisit).

Plan 07-01 `tool-registry.ts`:
- `ToolId` includes `'drop-concentration'`.
- `TOOL_HANDLER_IDS['drop-concentration']` = `'evf.dropConcentration'`.

Phase 4b `toast-queue-layer.ts`:
- `enqueueToast({ kind, text, dwellMs })` — Phase 7 reuses; `kind: 'reaction'` is a new kind.

Plan 07-03 rename precedent (skill-check → confirmTemplatePlacement). Plan 07-05 does the same for setTargets → dropConcentration. socketlib registration in `socketlib-handlers.ts`:
```ts
// BEFORE (Plan 07-03):
socketlib.registerComplexHandler(MODULE_ID, 'evf.setTargets', handleSetTargetsStub);

// AFTER (Plan 07-05):
socketlib.registerComplexHandler(MODULE_ID, 'evf.dropConcentration', handleDropConcentration);
```
Count stays 14.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reaction-watcher + reaction payload + reaction-toast-dispatcher (REACT-01 closure)</name>
  <files>packages/foundry-module/src/write-path/reaction-watcher.ts, packages/foundry-module/src/write-path/reaction-watcher.test.ts, packages/shared-protocol/src/payloads/reaction.ts, packages/shared-protocol/src/payloads/reaction.test.ts, packages/shared-protocol/src/index.ts, packages/g2-app/src/panels/reaction-toast-dispatcher.ts, packages/g2-app/src/panels/reaction-toast-dispatcher.test.ts, packages/foundry-module/src/types/foundry-globals.d.ts</files>
  <behavior>
    - `reaction.ts`: `export const R1_REACTION_AVAILABLE_TYPE = 'r1.reaction.available' as const;` `export const ReactionAvailablePayloadSchema = z.object({ kind: z.enum(['shield','counterspell','opportunity-attack']), sourceName: z.string().min(1), expiresAt: z.number().int() }).strict();` `export type ReactionAvailablePayload = z.infer<typeof ReactionAvailablePayloadSchema>`.
    - `reaction.test.ts`: round-trip valid + invalid (missing kind, unknown kind enum, missing sourceName, extra fields rejected, negative expiresAt allowed since int(); wrong type rejected).
    - `index.ts` (shared-protocol): additive append `export * from './payloads/reaction.js';` at end. (Plan 07-03 appended template; Plan 07-04 appended multi-attack; Plan 07-05 appends reaction. Sequential plans = append-only = no merge conflicts.)
    - `foundry-globals.d.ts`: widen `FoundryActiveEffect` adding `id: string`. Add `game.user.character?: { id: string } | null` field (player's selected character — Phase 7's reaction-watcher uses this; Phase 5 readers didn't need it). Add `Hooks.off(hookId: number): void` if not already declared.
    - `reaction-watcher.ts`: `export function registerReactionWatcher(emit: (payload: ReactionAvailablePayload) => void): () => void`. Body sketch: `const hookId = Hooks.on('dnd5e.preUseActivity', (rawActivity, _u, _d, _m) => { try { const activity = rawActivity as FoundryActivity; const actor = activity?.actor; if (!actor) return; const playerActorId = game.user?.character?.id ?? null; if (!playerActorId) return; if (actor.id === playerActorId) return; if (!inspectTargetIncludesActor(activity, playerActorId)) return; const reactionKind = inferReactionKind(activity); if (!reactionKind) return; emit({ kind: reactionKind, sourceName: actor.name, expiresAt: Date.now() + 6000 }); } catch (err) { console.warn('[reaction-watcher] hook handler threw', err); } /* NEVER return false — display-only */ }); return () => { Hooks.off(hookId); };`. Module-local helpers: `inspectTargetIncludesActor(activity, playerActorId): boolean` → Phase 7 broad heuristic returns `true` for any NPC activity (precise per-shape matching is Phase 9 COMB-02 widget territory; document in JSDoc); `inferReactionKind(activity): 'shield'|'counterspell'|'opportunity-attack'|null` → `'shield'` when `activity.type === 'attack'`, `'counterspell'` when `activity.item?.type === 'spell'` AND `activity.activation?.type !== 'reaction'`, `null` otherwise. JSDoc cites RESEARCH §Q3 (correct hook name) + §Pitfall 1 (never return false).
    - `reaction-watcher.test.ts`: 8+ tests — Hooks.on called with literal string `'dnd5e.preUseActivity'`; player's own action does NOT emit; NPC attack on player emits with `kind: 'shield'` + sourceName = NPC name; NPC spell emits with `kind: 'counterspell'`; null game.user.character → no emit; handler throws → swallowed + console.warn; hook handler return value is NEVER `false` (verify return is `undefined`); unsubscribe via Hooks.off (capture hookId then verify Hooks.off called with same id).
    - `reaction-toast-dispatcher.ts`: `export function attachReactionToastHandler(ws, toastQueue, locale): () => void`. Mirrors conc-conflict-dispatcher EXACTLY: outer EnvelopeSchema.safeParse → narrow on `R1_REACTION_AVAILABLE_TYPE` → inner ReactionAvailablePayloadSchema.safeParse → success enqueues `toastQueue.enqueueToast({ kind: 'reaction', text: formatReactionText(payload, locale), dwellMs: 3000 })`. `formatReactionText(payload, locale)` returns localized string like `'REAZIONE: ${kind} disponibile (${sourceName})'` for IT; English equivalent for EN. Returns unsubscribe.
    - `reaction-toast-dispatcher.test.ts`: 6+ tests — valid envelope enqueues toast with kind='reaction', text contains kind + sourceName, dwellMs=3000; malformed envelope shape rejected + console.warn; wrong type silent-skipped; invalid payload rejected; unsubscribe idempotent.
  </behavior>
  <action>Create `reaction.ts` payload schema + constant + tests. Additive-append re-export in shared-protocol `index.ts`. Extend `foundry-globals.d.ts` with `FoundryActiveEffect.id`, `game.user.character` field, `Hooks.off` (verify only). Create `reaction-watcher.ts` registering Hooks.on('dnd5e.preUseActivity', ...) — CORRECT hook name. Handler skips own actions, infers reactionKind heuristically, emits via injected emit, NEVER returns false. Test with mocked Hooks.on capturing the callback + verifying return value undefined. Create `reaction-toast-dispatcher.ts` in g2-app mirroring conc-conflict-dispatcher exactly. Test with synthetic ws.fireMessage.</action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test -- --run payloads/reaction.test.ts &amp;&amp; pnpm --filter @evf/foundry-module test -- --run write-path/reaction-watcher.test.ts &amp;&amp; pnpm --filter @evf/g2-app test -- --run panels/reaction-toast-dispatcher.test.ts &amp;&amp; pnpm typecheck &amp;&amp; grep -q "dnd5e.preUseActivity" packages/foundry-module/src/write-path/reaction-watcher.ts &amp;&amp; ! grep -q "dnd5e.preActivityUse" packages/foundry-module/src/write-path/reaction-watcher.ts &amp;&amp; ! grep -q "dnd5e.preItemUsage" packages/foundry-module/src/write-path/reaction-watcher.ts</automated>
  </verify>
  <done>reaction-watcher uses CORRECT hook name; never returns false; reaction toast dispatcher mirrors conc-conflict pattern; schemas exported; foundry-globals declares game.user.character + FoundryActiveEffect.id; all tests green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drop-concentration handler + concentration-drop-modal extension + setTargets→dropConcentration rename + module.ts wiring</name>
  <files>packages/shared-protocol/src/tools/drop-concentration.ts, packages/shared-protocol/src/tools/drop-concentration.test.ts, packages/shared-protocol/src/index.ts, packages/foundry-module/src/write-path/handlers/drop-concentration.ts, packages/foundry-module/src/write-path/handlers/drop-concentration.test.ts, packages/foundry-module/src/write-path/handlers/index.ts, packages/foundry-module/src/pair/socketlib-handlers.ts, packages/foundry-module/src/pair/socketlib-handlers.test.ts, packages/foundry-module/src/module.ts, packages/foundry-module/src/module.test.ts, packages/g2-app/src/panels/concentration-drop-modal.ts, packages/g2-app/src/panels/concentration-drop-modal.test.ts</files>
  <behavior>
    - `drop-concentration.ts` (shared-protocol/tools): `export const DropConcentrationInputSchema = z.object({ actor_id: z.string().min(1), effect_id: z.string().min(1) }).strict()`. `export type DropConcentrationInput = z.infer<typeof DropConcentrationInputSchema>`. (NOT added to TOOL_REGISTRY array exported from `tools/index.ts` to avoid changing the Phase 3 7-tool surface served at `GET /v1/tools` — the new tool is module-internal at Phase 7 + Phase 8/9 may expose later. Re-export from main shared-protocol `index.ts` only.)
    - `drop-concentration.test.ts` (shared-protocol): round-trip valid + invalid (missing actor_id, empty actor_id, missing effect_id, extra fields rejected).
    - shared-protocol `index.ts`: additive append `export { DropConcentrationInputSchema, type DropConcentrationInput } from './tools/drop-concentration.js';`.
    - `drop-concentration.ts` (foundry-module/write-path/handlers): `export const dropConcentrationHandler: ToolHandler<DropConcentrationInput> = { argsSchema: DropConcentrationInputSchema, async handle(args) { const actor = game.actors.get(args.actor_id); if (!actor) return { success: false, error: 'actor_not_found' }; const effect = actor.effects?.contents.find(e =&gt; e.id === args.effect_id); if (!effect) return { success: false, error: 'effect_not_found' }; try { await effect.delete(); return { success: true, data: { effectId: args.effect_id } }; } catch (err) { if (String(err).includes('no_gm_connected') || String(err).includes('No connected GM')) return { success: false, error: 'no_gm_connected' }; return { success: false, error: String(err) }; } } }`.
    - `drop-concentration.test.ts` (foundry-module): 8+ tests — happy path resolves actor + effect + calls effect.delete() → success; missing actor → `actor_not_found`; missing effect → `effect_not_found`; delete throws → error result; GM-offline error normalised to `no_gm_connected`; argsSchema rejects empty strings.
    - `handlers/index.ts`: add `registerToolHandler('drop-concentration', dropConcentrationHandler)`.
    - `socketlib-handlers.ts`: REPLACE `handleSetTargetsStub` body in-place with the dispatchTool('drop-concentration', ...) adapter (same pattern as Plans 07-02/03/04 — extract { args, idempotencyKey, bearer }, validate input shape, call dispatchTool, return ToolResult). RENAME the registration line `socketlib.registerComplexHandler(MODULE_ID, 'evf.setTargets', handleSetTargetsStub)` → `socketlib.registerComplexHandler(MODULE_ID, 'evf.dropConcentration', handleDropConcentration)`. JSDoc cites Plan 07-05 + Plan 04b-05 CONC-01 closure. NO new registerComplexHandler call — count stays 14.
    - `socketlib-handlers.test.ts`: regression — count stays 14; new assertion `'evf.dropConcentration'` registered AND `'evf.setTargets'` NOT registered.
    - `module.ts`: ADD `import { registerReactionWatcher } from './write-path/reaction-watcher.js'` near top. In `Hooks.once('ready')` callback, AFTER `registerHookSubscribers(...)` and AFTER `setMultiAttackProgressEmitter(...)` (from Plan 07-04), add `registerReactionWatcher((payload) =&gt; bridgeDeltaEmitter('r1.reaction.available', payload))`. Optional: return value (unsubscribe) discarded for MVP — module lifecycle is for-the-session.
    - `module.test.ts`: update `expect(handlers?.has('evf.setTargets'))` → `expect(handlers?.has('evf.dropConcentration'))`. Add regression test asserting `Hooks.on` was called with `'dnd5e.preUseActivity'` exactly once after `ready` fires (verify reaction-watcher subscription).
    - `concentration-drop-modal.ts` (g2-app): EXTEND constructor with no new args (actor_id derived from `game.user.character?.id` at modal construction time? — actually no, the modal runs in g2-app context which does NOT have `game.user` directly. Pull from the Phase 4b ConcConflictPayload shape: add `actorId: string` to `ConcConflictPayloadSchema` in shared-protocol's concentration.ts — BUT that breaks Phase 4b ISM-10 which asserts the existing schema shape. SAFER PATH: thread actorId via the conc-conflict-dispatcher — module side enriches the payload with `actorId: game.user.character?.id` before emitting. Concretely: `concentration.ts` ConcConflictPayloadSchema is widened ADDITIVELY with `actorId: z.string().min(1).optional()` (optional preserves Phase 4b backward-compat); module-side conc-conflict emitter sets it; modal reads `payload.actorId ?? ''` (empty string suppresses the tool.invoke emission if unknown — fallback to legacy-only behavior). Extend the tap path in the modal: build BOTH envelopes (tool.invoke first, then legacy conc.drop.confirmed) and send via ws.send sequentially. Use `crypto.randomUUID()` for idempotencyKey.
    - `concentration-drop-modal.test.ts`: regression — Phase 4b legacy conc.drop.confirmed envelope still emitted; NEW assertion — tool.invoke envelope ALSO emitted with toolId='drop-concentration' + args.effect_id matches payload.effectId + args.actor_id matches payload.actorId; when payload.actorId is undefined/empty, only the legacy envelope is emitted (graceful fallback — test asserts ws.send called exactly once); idempotencyKey is a fresh uuid per tap; both envelopes share the same sessionId.
  </behavior>
  <action>Create `packages/shared-protocol/src/tools/drop-concentration.ts` exporting DropConcentrationInputSchema + type. Round-trip tests. Re-export from main `index.ts` (additive append). Widen `packages/shared-protocol/src/payloads/concentration.ts` `ConcConflictPayloadSchema` ADDITIVELY with `actorId: z.string().min(1).optional()` — Phase 4b tests stay green because optional. Create `packages/foundry-module/src/write-path/handlers/drop-concentration.ts` implementing the ToolHandler<DropConcentrationInput>. Wire it into `handlers/index.ts` via `registerToolHandler`. In `socketlib-handlers.ts`: replace `handleSetTargetsStub` body with the dispatchTool adapter; rename the registration handlerId from `'evf.setTargets'` to `'evf.dropConcentration'`. Update `module.test.ts` assertion accordingly. Verify total registerComplexHandler count stays 14. Wire `registerReactionWatcher(emit)` in `module.ts` ready hook. Extend `packages/g2-app/src/panels/concentration-drop-modal.ts` tap path to emit BOTH envelopes (tool.invoke first with actor_id from payload + fresh idempotencyKey UUID; legacy conc.drop.confirmed second). Update modal tests asserting dual-emit + graceful single-emit when actorId missing.</action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test -- --run tools/drop-concentration.test.ts &amp;&amp; pnpm --filter @evf/foundry-module test -- --run write-path/handlers/drop-concentration.test.ts pair/socketlib-handlers.test.ts module.test.ts &amp;&amp; pnpm --filter @evf/g2-app test -- --run panels/concentration-drop-modal.test.ts &amp;&amp; pnpm typecheck &amp;&amp; grep -c 'registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts | awk '{ if ($1 != 14) exit 1 }' &amp;&amp; ! grep -q "'evf.setTargets'" packages/foundry-module/src/pair/socketlib-handlers.ts &amp;&amp; grep -q "'evf.dropConcentration'" packages/foundry-module/src/pair/socketlib-handlers.ts</automated>
  </verify>
  <done>dropConcentrationHandler implements ToolHandler; setTargets stub replaced in-place + renamed to dropConcentration; count stays 14; module.ts wires registerReactionWatcher; ConcentrationDropModalPanel emits tool.invoke + legacy envelope (Phase 4b tests stay green); typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dnd5e hook → reaction-watcher | Trusted source (Foundry-internal); handler defensively skips on missing fields |
| module → g2-app (r1.reaction.available) | Untrusted to client; g2-app validates double trust boundary before enqueueToast |
| g2-app → module (tool.invoke for drop-concentration) | Untrusted; dispatchTool argsSchema + idempotency + audit cover threats |
| effect.delete() | Runs as GM via socketlib; standard Foundry permission semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | reaction-watcher emits false-positive toasts | accept | Phase 7 broad heuristic is intentional (display-only); Phase 9 COMB-02 owns precise match |
| T-07-05-02 | DoS | reaction-watcher hook fires on every activity (1000s/session) | mitigate | inferReactionKind returns null for non-attack non-spell — short-circuit; emit only when reactionKind defined |
| T-07-05-03 | Tampering | Wrong hook name (preActivityUse) silently never fires | mitigate | TEST asserts Hooks.on called with literal `'dnd5e.preUseActivity'`; grep gate `! grep -q dnd5e.preActivityUse` |
| T-07-05-04 | Privilege Escalation | Hook returns false → cancels NPC action | mitigate | TEST asserts hook handler return value is `undefined` never `false` |
| T-07-05-05 | Tampering | Player drops someone ELSE's concentration | mitigate | dropConcentrationHandler resolves actor via `game.actors.get(args.actor_id)` — Foundry actor permissions apply on `effect.delete()` (GM ownership semantics); plus actor_id is derived from `game.user.character.id` at modal-side (player can only drop own concentration in MVP) |
| T-07-05-06 | DoS | Toast queue saturation from reaction flood | mitigate | Phase 4b TOAST-01 squash-to-+N already handles overflow gracefully (TOAST-01 test coverage; no new mitigation needed) |
</threat_model>

<verification>
- `pnpm --filter @evf/shared-protocol test` green.
- `pnpm --filter @evf/foundry-module test` green.
- `pnpm --filter @evf/g2-app test` green.
- `pnpm typecheck` green.
- `grep -c "registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` exactly 14.
- `grep -q "'evf.dropConcentration'" packages/foundry-module/src/pair/socketlib-handlers.ts` returns 0.
- `! grep -q "'evf.setTargets'" packages/foundry-module/src/pair/socketlib-handlers.ts` (setTargets fully removed).
- `! grep -q "'evf.skillCheck'" packages/foundry-module/src/pair/socketlib-handlers.ts` (skill-check still removed by Plan 07-03).
- `grep -q "dnd5e.preUseActivity" packages/foundry-module/src/write-path/reaction-watcher.ts` returns 0.
- `! grep -q "dnd5e.preActivityUse" packages/foundry-module/src/write-path/reaction-watcher.ts` (wrong hook name not present).
- `! grep -q "dnd5e.preItemUsage" packages/foundry-module/src/write-path/reaction-watcher.ts` (other wrong hook name not present).
- `! grep -rE 'activity\.use\(' packages/g2-app packages/bridge --include='*.ts'` (Gate 8 still empty).
- Phase 4b ConcentrationDropModalPanel tests (Plans 04b-05 ISM-01..10) stay green — legacy envelope still emitted.
- SC-07-04 declared hardware-pending (Phase 4b modal → dropConcentration → effect.delete() removes concentration effect in real Foundry world).
- SC-07-05 declared hardware-pending (real NPC attack fires dnd5e.preUseActivity → reaction toast visible on G2).
- Phase 7 test count grows by ~45-55 tests.
</verification>

<success_criteria>
1. reaction-watcher uses CORRECT hook name `'dnd5e.preUseActivity'`; tests prevent regression to wrong names.
2. reaction-watcher NEVER returns `false` from the hook handler (display-only invariant).
3. ReactionAvailablePayloadSchema + R1_REACTION_AVAILABLE_TYPE exported.
4. reaction-toast-dispatcher pushes Phase 4b toast queue entries with kind='reaction', 3s dwell.
5. dropConcentrationHandler resolves effect + calls effect.delete; GM-offline path returns no_gm_connected.
6. DropConcentrationInputSchema exported (NOT in TOOL_REGISTRY served by GET /v1/tools — module-internal).
7. ConcentrationDropModalPanel dual-emits (tool.invoke + legacy conc.drop.confirmed); Phase 4b ISM tests stay green.
8. socketlib registration renamed setTargets → dropConcentration; count stays 14; module.test.ts updated.
9. module.ts wires registerReactionWatcher in ready hook.
10. SC-07-04 + SC-07-05 hardware-pending declared.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundry-module-write-path/07-05-SUMMARY.md` documenting: CORRECT hook name codified + tested + grep-gated; reaction-toast end-to-end software path; CONC-01 write closure (Phase 4b modal → effect.delete via dispatchTool); setTargets stub renamed → dropConcentration in-place (count stays 14); SC-07-04 + SC-07-05 hardware-pending. Plan 07-06 ships bearer rotation + Phase 7 closure.
</output>
