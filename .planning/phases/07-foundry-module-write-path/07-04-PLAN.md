---
phase: 07-foundry-module-write-path
plan: "04"
type: execute
wave: 2
depends_on: ["07-02", "07-03"]
files_modified:
  - packages/shared-protocol/src/tools/weapon-attack.ts
  - packages/shared-protocol/src/tools/tools.test.ts
  - packages/shared-protocol/src/payloads/multi-attack.ts
  - packages/shared-protocol/src/payloads/multi-attack.test.ts
  - packages/shared-protocol/src/index.ts
  - packages/foundry-module/src/write-path/handlers/weapon-attack.ts
  - packages/foundry-module/src/write-path/handlers/weapon-attack.test.ts
  - packages/g2-app/src/panels/combat-tracker-panel.ts
  - packages/g2-app/src/panels/combat-tracker-panel.test.ts
  - packages/g2-app/src/panels/multi-attack-progress-dispatcher.ts
  - packages/g2-app/src/panels/multi-attack-progress-dispatcher.test.ts
  - packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt
autonomous: true
requirements: [MULTI-01]
must_haves:
  truths:
    - "`WeaponAttackInputSchema` in `@evf/shared-protocol` is EXTENDED with `count: z.number().int().min(1).max(10).default(1)` field. Existing 5/5 Phase 3 tools tests stay green because default is 1 (backward-compatible)."
    - "`weaponAttackHandler.handle()` (Plan 07-02) is EXTENDED with a client-side loop: `for (let i = 0; i &lt; args.count; i++) { const result = await activity.use({ configure: false, consume: { action: i === 0 } }); emitProgress({ current: i+1, total: args.count, attackId, chatCardId: result?.id }); }`. Per RESEARCH §Q1: `count` field DOES NOT EXIST in dnd5e 5.3.3 ActivityUseConfiguration — Path B (client-side loop) is the ONLY valid implementation."
    - "Module emits `r1.multiattack.progress` envelope after each iteration via `socketlib.executeForUser(playerId, ...)`; bridge propagates to g2-app via WS."
    - "`MultiAttackProgressPayloadSchema` exported from `@evf/shared-protocol/payloads/multi-attack.ts` (separate file to avoid `index.ts` merge conflict with Plan 07-03)."
    - "`CombatTrackerPanel` acquires transient `multiAttackState: { current: number; total: number; attackId: string } | null` field; when defined, the footer chip renders `[Atk current/total]` in the dist+dir column (cols 53-58, slight overflow ok during active multi-attack per CONTEXT.md §Area 2)."
    - "`multi-attack-progress-dispatcher.ts` subscribes to ws, validates incoming `r1.multiattack.progress` envelopes via double trust boundary (EnvelopeSchema + MultiAttackProgressPayloadSchema), updates the active CombatTrackerPanel's `multiAttackState` and triggers a re-render. State auto-clears when `current === total` OR on combat-turn-advance."
    - "Existing INV-1 fixtures from Phase 5 Plan 05-05 (`combat-tracker-panel.test.ts`) stay green — multiAttackState defaults to null and the renderer omits the chip. A NEW INV-1 fixture `combat-tracker-multi-attack.txt` shows the panel with `[Atk 1/2]` chip rendered."
  artifacts:
    - path: "packages/shared-protocol/src/tools/weapon-attack.ts"
      provides: "WeaponAttackInputSchema with count field (default 1, max 10)"
      contains: "count"
    - path: "packages/shared-protocol/src/payloads/multi-attack.ts"
      provides: "MultiAttackProgressPayloadSchema + R1_MULTIATTACK_PROGRESS_TYPE constant"
      contains: "MultiAttackProgressPayloadSchema"
    - path: "packages/foundry-module/src/write-path/handlers/weapon-attack.ts"
      provides: "Path B client-side loop (for i in count: activity.use with consume.action only on i=0)"
      contains: "for (let i"
    - path: "packages/g2-app/src/panels/combat-tracker-panel.ts"
      provides: "multiAttackState field + [Atk N/M] chip rendering in footer"
      contains: "multiAttackState"
    - path: "packages/g2-app/src/panels/multi-attack-progress-dispatcher.ts"
      provides: "WS dispatcher updating CombatTrackerPanel.multiAttackState on each iteration"
      contains: "MultiAttackProgressPayloadSchema"
    - path: "packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt"
      provides: "INV-1 fixture showing [Atk 1/2] chip in combat tracker footer"
      contains: "[Atk 1/2]"
  key_links:
    - from: "packages/foundry-module/src/write-path/handlers/weapon-attack.ts"
      to: "activity.use({ configure: false, consume: { action: i === 0 } })"
      via: "Path B client-side loop iterating args.count times"
      pattern: "for \\(let i"
    - from: "packages/g2-app/src/panels/multi-attack-progress-dispatcher.ts"
      to: "packages/g2-app/src/panels/combat-tracker-panel.ts"
      via: "Calls panel.setMultiAttackState({ current, total, attackId }) on each progress envelope"
      pattern: "setMultiAttackState"
    - from: "packages/shared-protocol/src/payloads/multi-attack.ts"
      to: "packages/shared-protocol/src/index.ts"
      via: "Re-export added — separate file from template.ts (Plan 07-03) to avoid merge conflict"
      pattern: "multi-attack"
---

<objective>
Wave 2 (sequential after 07-03 — see note below): Multi-attack tracker (MULTI-01) via Path B (client-side loop). RESEARCH §Q1 closed Phase 0 §10.0.10 P2 open question: `count` field does NOT exist in dnd5e 5.3.3 `ActivityUseConfiguration`. Path B is the only valid path: weapon-attack handler iterates `args.count` times, calling `activity.use({ configure: false })` per iteration with `consume.action: true` only on i=0 (action economy: Extra Attack feature does not double-spend the Action). Each iteration emits an `r1.multiattack.progress` envelope; g2-app's CombatTrackerPanel chip ticks `[Atk 1/2]` → `[Atk 2/2]`.

**Wave 2 sequential discipline:** Plan 07-03 and 07-04 are nominally parallel by Wave assignment but share `packages/shared-protocol/src/index.ts` as a merge-conflict surface. RESEARCH §Wave 2 parallelism note recommends sequential foreground execution to avoid merge pain. 07-04 lands AFTER 07-03 commits, NOT in parallel. files_modified frontmatter excludes any `package/shared-protocol/src/index.ts` line — instead, ship `multi-attack.ts` as a SEPARATE payload file (added to index.ts via additive edit at end of file; conflict surface minimised).

Purpose: MULTI-01 closure. Single Phase 0 P2 open question resolved (Path B confirmed). No new socketlib handlers added — extends existing `evf.weaponAttack` (Plan 07-02). Total registerComplexHandler count stays 14.
Output: 3 new source files (multi-attack.ts payload + dispatcher + dispatcher test) + 1 new INV-1 fixture + 4 edited files (weapon-attack schema + handler + combat-tracker-panel + index.ts append). ~25-35 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/07-foundry-module-write-path/07-CONTEXT.md
@.planning/phases/07-foundry-module-write-path/07-RESEARCH.md
@.planning/phases/07-foundry-module-write-path/07-01-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-02-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-03-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-05-SUMMARY.md
@packages/foundry-module/src/write-path/handlers/weapon-attack.ts
@packages/g2-app/src/panels/combat-tracker-panel.ts
@packages/g2-app/src/panels/conc-conflict-dispatcher.ts
@packages/shared-protocol/src/tools/weapon-attack.ts
@packages/shared-protocol/src/index.ts
@packages/shared-protocol/src/envelope.ts

<interfaces>
<!-- Contracts extended in this plan + reused from prior plans. -->

From Plan 07-02 `packages/foundry-module/src/write-path/handlers/weapon-attack.ts`:
```ts
export const weaponAttackHandler: ToolHandler<WeaponAttackInput> = {
  argsSchema: WeaponAttackInputSchema,
  async handle(args) {
    // Phase 07-02 ships single-attack version. Plan 07-04 extends with `count` loop.
    const actor = game.actors.get(args.actor_id); ...
    const activity = item.system.activities?.contents.find(a => a.type === 'attack');
    await activity.use({ configure: false });
    return { success: true, data: { chatCardId } };
  },
};
```
Plan 07-04 wraps the `activity.use` call in a `for` loop iterating `args.count` (default 1, so existing tests pass unchanged).

From Phase 3 `packages/shared-protocol/src/tools/weapon-attack.ts`:
```ts
export const WeaponAttackInputSchema = z.object({ actor_id: z.string().min(1), item_id: z.string().min(1), targets: z.array(z.string()), advantage: z.enum(['normal','advantage','disadvantage']).default('normal') });
```
Plan 07-04 ADDS `count: z.number().int().min(1).max(10).default(1)` (RESEARCH Open Question 3 — default 1 keeps backward compatibility).

From `packages/g2-app/src/panels/combat-tracker-panel.ts` (Phase 5 Plan 05-05):
- Single text container `'overlay-block'` (ADR-0009 Amd 1).
- Layout: 18 content rows × 66 cp per UI-SPEC §5.8.
- Footer row renders `Rapida: [A] [S] [I] [M]` quick-action bar.
- Plan 07-04 ADDS: instance field `multiAttackState: { current: number; total: number; attackId: string } | null = null`; setter `setMultiAttackState(state: typeof this.multiAttackState): void` which assigns + triggers re-render; renderer's combatant-row generation reads `multiAttackState` and overlays `[Atk N/M]` chip in cols 53-58 when `attackId === currentCombatantId`.

From `packages/g2-app/src/panels/conc-conflict-dispatcher.ts` (PATTERN to mirror EXACTLY):
- Double trust boundary: outer EnvelopeSchema + inner payload schema.
- Subscribes to ws.message; on validated envelope, performs side-effect.

Wire format additions:
```ts
// outbound: module → g2-app (one per iteration)
type: 'r1.multiattack.progress'
payload: { attackId: string; current: number; total: number; chatCardId: string | null; actorId: string }
```
On `current === total`, dispatcher clears the panel's multiAttackState (chip disappears).

Module pattern Path B (RESEARCH §Pattern 2):
```ts
const count = args.count ?? 1;
const attackId = crypto.randomUUID();
const results: Array<{ attackIndex: number; chatCardId: string | null }> = [];
for (let i = 0; i < count; i++) {
  const result = await activity.use({ configure: false, consume: { action: i === 0 } });
  const chatCardId = (result as { id?: string } | undefined)?.id ?? null;
  results.push({ attackIndex: i + 1, chatCardId });
  // Emit progress envelope (the function emitProgressForUser is provided in module-level)
  emitMultiAttackProgress({ attackId, current: i + 1, total: count, chatCardId, actorId: args.actor_id });
}
return { success: true, data: { attackId, attacks: results } };
```
The `emitMultiAttackProgress` calls `socketlib.executeForUser(playerUserId, 'evf.multiAttackProgress', payload)` (a NEW socketlib handlerId would push count > 14 — instead emit via the existing bridge delta channel: import `bridgeDeltaEmitter` from `module.ts` and call `bridgeDeltaEmitter('r1.multiattack.progress', payload)` analogous to Phase 5 hook subscribers. The bridge wraps in EnvelopeSchema server-side. NO new socketlib handlers needed.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend WeaponAttackInputSchema with count + extend handler with Path B loop + emit progress via bridgeDeltaEmitter</name>
  <files>packages/shared-protocol/src/tools/weapon-attack.ts, packages/shared-protocol/src/tools/tools.test.ts, packages/shared-protocol/src/payloads/multi-attack.ts, packages/shared-protocol/src/payloads/multi-attack.test.ts, packages/shared-protocol/src/index.ts, packages/foundry-module/src/write-path/handlers/weapon-attack.ts, packages/foundry-module/src/write-path/handlers/weapon-attack.test.ts</files>
  <behavior>
    - `weapon-attack.ts` (shared-protocol): add `count: z.number().int().min(1).max(10).default(1)` to the existing object schema. The `WeaponAttackInput` type now includes `count: number` (post-parse, always defined due to default).
    - `tools.test.ts` Phase 3 drift test: the toJSONSchema output includes `count` in the properties. Existing Phase 3 tests pass with count omitted (default 1). Add 2 tests: `count: 2` valid; `count: 11` rejected; `count: 0` rejected; `count: -1` rejected.
    - `multi-attack.ts`: `export const R1_MULTIATTACK_PROGRESS_TYPE = 'r1.multiattack.progress' as const;` `export const MultiAttackProgressPayloadSchema = z.object({ attackId: z.string().uuid(), current: z.number().int().min(1), total: z.number().int().min(1).max(10), chatCardId: z.string().nullable(), actorId: z.string().min(1) }).strict()`. `export type MultiAttackProgressPayload = z.infer<typeof MultiAttackProgressPayloadSchema>;`.
    - `multi-attack.test.ts`: round-trip valid + invalid (missing fields, current > total, total > 10 rejected, extra fields rejected, missing attackId, non-uuid attackId).
    - `index.ts` (shared-protocol): ADDITIVE append at end of file — `export * from './payloads/multi-attack.js';`. No insertions in the middle; if Plan 07-03 already appended `export * from './payloads/template.js';` we add after it. (07-04 runs sequentially AFTER 07-03 so the file already includes the template export — additive append is conflict-free.)
    - `weapon-attack.ts` (foundry-module handler): wrap existing single `activity.use` call in `for (let i = 0; i < args.count; i++) { ... }`. Each iteration: `await activity.use({ configure: false, consume: { action: i === 0 } })`; collect result; call `emitMultiAttackProgress(payload)` after each iteration. Module-local `emitMultiAttackProgress` function: if `bridgeDeltaEmitter` is wired (verify by checking for a module-level `setMultiAttackProgressEmitter(fn)` injection point at file top), call it; else no-op. Export `setMultiAttackProgressEmitter(fn: (payload: MultiAttackProgressPayload) =&gt; void): void` from `weapon-attack.ts` so `module.ts` boot wires the actual emitter (similar to the registerHookSubscribers pattern from Phase 5). Default emitter is a no-op so unit tests don't need to inject.
    - Update `module.ts`: after `import './write-path/handlers/index.js'`, add `import { setMultiAttackProgressEmitter } from './write-path/handlers/weapon-attack.js'`. In `Hooks.once('ready')` callback, AFTER `registerHookSubscribers(...)`, add `setMultiAttackProgressEmitter((payload) =&gt; bridgeDeltaEmitter('r1.multiattack.progress', payload))`. NO new socketlib handler registered — count stays 14.
    - `weapon-attack.test.ts`: ADD 6+ tests for Path B loop — `count: 1` calls activity.use once (existing Phase 07-02 tests pass unchanged); `count: 2` calls activity.use twice with `consume.action: true` on first call and `false` on second (Pattern 2 RESEARCH); `count: 3` calls 3 times; emit progress envelope called once per iteration with correct current/total/attackId fields (attackId stable across iterations); attackId is a fresh UUID per handler invocation (no reuse across calls); when activity.use throws on iteration 2, returns `{ success: false, error }` and progress emitter is NOT called for iteration 2 onwards (early break); count: 0 → schema rejects at argsSchema validation in dispatchTool (we don't even reach handle()); count: 11 → schema rejects.
  </behavior>
  <action>Edit `WeaponAttackInputSchema` in `packages/shared-protocol/src/tools/weapon-attack.ts` adding `count: z.number().int().min(1).max(10).default(1)` field. Verify Phase 3 `tools.test.ts` still passes (toJSONSchema drift test catches any unintended changes). Add `count` validation tests. Create `packages/shared-protocol/src/payloads/multi-attack.ts` with the progress payload schema + constant. Add `multi-attack.test.ts` round-trip tests. Append `export * from './payloads/multi-attack.js'` to `packages/shared-protocol/src/index.ts`. Edit `packages/foundry-module/src/write-path/handlers/weapon-attack.ts`: wrap activity.use call in Path B loop; add `emitMultiAttackProgress` module-local + `setMultiAttackProgressEmitter(fn)` injection point; collect results; return `{ success: true, data: { attackId, attacks } }`. Edit `packages/foundry-module/src/module.ts` to wire `setMultiAttackProgressEmitter((p) =&gt; bridgeDeltaEmitter('r1.multiattack.progress', p))` inside the `ready` hook AFTER `registerHookSubscribers`. Extend `weapon-attack.test.ts` with 6+ Path B loop tests. NO new socketlib registration — count stays 14.</action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test &amp;&amp; pnpm --filter @evf/foundry-module test -- --run write-path/handlers/weapon-attack.test.ts module.test.ts &amp;&amp; pnpm typecheck &amp;&amp; ! grep -rE 'activity\.use\(' packages/g2-app packages/bridge --include='*.ts' &amp;&amp; grep -c 'registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts | awk '{ if ($1 != 14) exit 1 }'</automated>
  </verify>
  <done>count field on WeaponAttackInputSchema; Phase 3 tests still pass; multi-attack payload schema exported; weapon-attack handler does Path B loop; module.ts wires the progress emitter; count stays 14; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CombatTrackerPanel multiAttackState chip + multi-attack-progress-dispatcher + INV-1 fixture</name>
  <files>packages/g2-app/src/panels/combat-tracker-panel.ts, packages/g2-app/src/panels/combat-tracker-panel.test.ts, packages/g2-app/src/panels/multi-attack-progress-dispatcher.ts, packages/g2-app/src/panels/multi-attack-progress-dispatcher.test.ts, packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt</files>
  <behavior>
    - `combat-tracker-panel.ts`: ADD instance field `private multiAttackState: { current: number; total: number; attackId: string; actorId: string } | null = null;`. ADD public method `setMultiAttackState(state: MultiAttackState | null): void { this.multiAttackState = state; this.requestRender(); }` (where `requestRender` is the existing re-render entry — verify the panel has it; if not, expose `render()` method). Extend the combatant-row renderer: when `multiAttackState !== null && combatant.actorId === multiAttackState.actorId`, the dist+dir field (cols 53-58 per CONTEXT.md §Area 2) is REPLACED with `[Atk ${current}/${total}]` (9 chars). The replacement is overlay-only on the active combatant row; other rows render normally. When `current === total`, the dispatcher (Task 2 below) is responsible for clearing — the panel's renderer does NOT auto-clear.
    - `combat-tracker-panel.test.ts`: ADD tests verifying — setMultiAttackState(null) doesn't change existing output (regression — Phase 5 fixtures stay green); setMultiAttackState({ current: 1, total: 2, attackId: 'a-1', actorId: 'actor-aragorn' }) when aragorn is the current combatant overlays `[Atk 1/2]` in cols 53-58 of his row; non-aragorn combatants render normally; setMultiAttackState() applied when target actor is NOT in the visible 5-row window → no chip rendered (graceful).
    - `multi-attack-progress-dispatcher.ts`: `export function attachMultiAttackProgressHandler(ws, combatTrackerPanelRef): () => void`. Subscribes to ws.message; on each: JSON.parse → EnvelopeSchema.safeParse → narrow on `envelope.type === R1_MULTIATTACK_PROGRESS_TYPE` → MultiAttackProgressPayloadSchema.safeParse → if `payload.current === payload.total` call `panel.setMultiAttackState(null)` (clear); else call `panel.setMultiAttackState({ current: payload.current, total: payload.total, attackId: payload.attackId, actorId: payload.actorId })`. Returns unsubscribe closure. `combatTrackerPanelRef` is a `{ current: CombatTrackerPanel | null }` mutable ref so the dispatcher can pick up panel mounts/unmounts.
    - `multi-attack-progress-dispatcher.test.ts`: 6+ tests — valid envelope updates panel state; final iteration (current === total) clears state; malformed envelope rejected + warned; wrong type silent-skipped; invalid payload rejected; unsubscribe is idempotent; missing panelRef.current is a silent no-op (panel not mounted).
    - `__fixtures__/combat-tracker-multi-attack.txt`: New 24-row × 96-cp INV-1 fixture showing the combat tracker with `[Atk 1/2]` chip overlaying Aragorn's row dist+dir column. Generate by running the renderer with the Phase 5 fixture's CombatSnapshot + `setMultiAttackState({ current: 1, total: 2, attackId: 'fixture-a-1', actorId: 'aragorn' })` and `expect.toMatchFileSnapshot` via shared-render matcher.
    - Existing Phase 5 INV-1 fixtures stay green (multiAttackState defaults null = no chip = identical output).
  </behavior>
  <action>Edit `packages/g2-app/src/panels/combat-tracker-panel.ts` adding `multiAttackState` field + `setMultiAttackState()` setter + chip rendering logic in the combatant-row renderer (overlay cols 53-58 when actorId matches active multi-attack target). Extend `combat-tracker-panel.test.ts` with multiAttackState tests + verify Phase 5 fixtures stay green. Create `multi-attack-progress-dispatcher.ts` mirroring conc-conflict-dispatcher exactly — outer Envelope parse, narrow on type, inner payload parse, side-effect into panel ref. Test the dispatcher with synthetic ws.fireMessage. Generate new INV-1 fixture `combat-tracker-multi-attack.txt` via the renderer (use the Phase 5 fixture infrastructure — matchAsciiFixture from shared-render).</action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run panels/combat-tracker-panel.test.ts panels/multi-attack-progress-dispatcher.test.ts &amp;&amp; pnpm typecheck &amp;&amp; test -f packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt &amp;&amp; grep -q "Atk 1/2" packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt</automated>
  </verify>
  <done>CombatTrackerPanel renders [Atk N/M] chip when multiAttackState set; Phase 5 fixtures untouched; multi-attack-progress-dispatcher updates panel via double trust boundary; new INV-1 fixture committed; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module → g2-app (r1.multiattack.progress) | Untrusted progress signal — g2-app validates double trust boundary before mutating panel state |
| Path B loop iteration | Each `activity.use` runs as GM via existing socketlib boundary; loop runs in module context, not on player |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | DoS | count: 1000 burns 1000 chat cards | mitigate | argsSchema enforces `count: max(10)` — server-side rejection before dispatch |
| T-07-04-02 | Tampering | Player spoofs r1.multiattack.progress with current > total | mitigate | MultiAttackProgressPayloadSchema validates current ≤ total via z.number().int().min(1); panel ignores out-of-range silently |
| T-07-04-03 | DoS | Iteration 2 throws — caller doesn't know how many succeeded | mitigate | Handler returns `attacks: [...partial results, error]`; idempotency cache stores partial result; player can re-issue ONLY remaining iterations (future Phase 8/9 enhancement) |
| T-07-04-04 | Information Disclosure | attackId UUID leakage | accept | UUID is non-secret; same risk profile as idempotencyKey |
| T-07-04-05 | Repudiation | "I only attacked once, not twice" | mitigate | Audit log records `count: N` arg + each iteration's chatCardId; full reconstruction via flags.evf.audit chat search |
</threat_model>

<verification>
- `pnpm --filter @evf/shared-protocol test` green.
- `pnpm --filter @evf/foundry-module test` green.
- `pnpm --filter @evf/g2-app test` green.
- `pnpm typecheck` green.
- `grep -c "registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` exactly 14 (no new registration).
- `grep -c 'count' packages/shared-protocol/src/tools/weapon-attack.ts` ≥ 1.
- `grep -c 'for (let i' packages/foundry-module/src/write-path/handlers/weapon-attack.ts` ≥ 1.
- `grep -q 'Atk 1/2' packages/g2-app/src/panels/__fixtures__/combat-tracker-multi-attack.txt`.
- `! grep -rE 'activity\.use\(' packages/g2-app packages/bridge --include='*.ts'` (Gate 8 still empty).
- Phase 5 INV-1 fixtures for CombatTracker (Plan 05-05) stay byte-identical (regression).
- Phase 7 test count grows by ~25-35 tests.
- SC-07-03 declared hardware-pending (Fighter L5+ Extra Attack with MidiQOL autoFastForward produces 2 chat cards + chip ticks `[Atk 1/2]` → `[Atk 2/2]`).
</verification>

<success_criteria>
1. WeaponAttackInputSchema extended with `count: z.number().int().min(1).max(10).default(1)`; Phase 3 tests stay green (default = backward-compat).
2. weaponAttackHandler implements Path B loop (RESEARCH §Q1 verdict): `for i in count: activity.use({ configure: false, consume: { action: i === 0 } })`.
3. emitMultiAttackProgress wired in module.ts via setMultiAttackProgressEmitter + bridgeDeltaEmitter — no new socketlib handler (count stays 14).
4. MultiAttackProgressPayloadSchema + R1_MULTIATTACK_PROGRESS_TYPE exported from @evf/shared-protocol; separate file from template.ts to minimise merge conflicts.
5. CombatTrackerPanel.setMultiAttackState renders `[Atk N/M]` chip on the active combatant row; defaults null = no behavioural change.
6. multi-attack-progress-dispatcher.ts mirrors conc-conflict-dispatcher (double trust boundary); updates panel ref on validated envelopes; auto-clears on final iteration.
7. New INV-1 fixture `combat-tracker-multi-attack.txt` proves character-perfect chip rendering.
8. Phase 5 CombatTracker INV-1 fixtures stay byte-identical (regression test).
9. SC-07-03 hardware-pending declared.
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundry-module-write-path/07-04-SUMMARY.md` documenting: Path B confirmed + implemented (count field on schema; client-side loop in handler); progress envelope wired via bridgeDeltaEmitter (no new socketlib handler); CombatTrackerPanel chip extension; INV-1 fixture for multi-attack state; SC-07-03 hardware-pending; Phase 5 fixtures untouched.
</output>
