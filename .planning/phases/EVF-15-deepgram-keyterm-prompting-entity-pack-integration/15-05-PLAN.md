---
phase: 15
plan: 05
type: execute
wave: 5
depends_on: [15-04]
files_modified:
  - Specs.md
  - README.md
  - docs/showcase/index.html
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md
autonomous: false
requirements: [VOICE-06, VOICE-07, VOICE-08, VOICE-09]
tags: [phase-close, inv3, doc-coherence, verification, voice, keyterm]
must_haves:
  truths:
    - "Specs.md §3.6 + §5.2 reflect Deepgram Keyterm Prompting integration with INV-2 cross-check note (re-verified 2026-05-17 from RESEARCH.md)"
    - "Specs.md changelog entry exists for v0.9.12 Phase 15 with VOICE-06..09 closure stanza"
    - "README + showcase document the keyterm-prompting capability and the 17-socketlib-handler invariant preservation"
    - "STATE.md + ROADMAP.md + REQUIREMENTS.md show Phase 15 complete (4/4 v1 REQ-IDs closed); 15-VERIFICATION.md exists"
    - "INV-3 atomic doc coherence: all 7 files updated in a SINGLE commit (CLAUDE.md INV-3 rule)"
  artifacts:
    - path: "Specs.md"
      provides: "§3.6 EvenAI status quo + INV-2 re-verified note; §5.2 Bridge stack mentions Deepgram Keyterm; new changelog entry under '## Changelog' for Phase 15"
    - path: "README.md"
      provides: "Phase progress badge bumped; Voice section notes keyterm prompting"
    - path: "docs/showcase/index.html"
      provides: "Stat strip reflects 4/4 v1 voice REQs closed; mini-changelog entry"
    - path: ".planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md"
      provides: "Phase 15 verification record — automated test results, CI gate evidence, software-only completion under ADR-0005 Branch A"
  key_links:
    - from: "Specs.md changelog"
      to: ".planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md"
      via: "cross-reference in the v0.9.12 Phase 15 changelog entry"
      pattern: "Phase 15 closed"
    - from: "README.md voice section"
      to: ".planning/quick/20260517-voice-intent-research/RESEARCH.md"
      via: "research basis citation"
      pattern: "Option C|Keyterm"
---

<objective>
Close Phase 15 with the **INV-3 atomic doc coherence commit** required by CLAUDE.md: when a cross-cutting change touches behaviour described in Specs.md / README.md / showcase, they MUST update in the SAME git commit. Phase 15 introduces a new Deepgram parameter (`keyterm`) referenced in:

- Specs.md §3.6 (EvenAI status — re-verified INV-2 2026-05-17 on 6 canonical sources; status quo confirmed but Phase 15 keyterm-prompting integration is the chosen mitigation)
- Specs.md §5.2 (Bridge stack — `keyterm` is now a documented Deepgram session parameter)
- Specs.md changelog (every cross-cutting change gets a stanza)
- README.md (voice section + phase progress badge)
- docs/showcase/index.html (stat strip + mini-changelog)
- .planning/STATE.md (phase 15 closed)
- .planning/ROADMAP.md (phase 15 ✅)
- .planning/REQUIREMENTS.md (VOICE-06..09 → Resolved)
- 15-VERIFICATION.md (phase verification artefact)

This plan is a **checkpoint plan** (autonomous: false). After Claude prepares all 7 files (8 with VERIFICATION.md) and stages them, the user verifies via `git diff --stat HEAD` that the change set is coherent, then approves the single atomic commit. The commit MUST include ALL files in one `git commit` invocation — no follow-up fixups.

Phase 15 produces NO new hardware-pending SCs (software-only by CONTEXT.md design); the verification doc records this and references ADR-0005 Branch A carry-forward language.

Purpose: INV-3 atomic doc coherence. Output: 8-file single-commit closure.

</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-CONTEXT.md
@.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-01-PLAN.md
@.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-02-PLAN.md
@.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-03-PLAN.md
@.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-04-PLAN.md
@.planning/quick/20260517-voice-intent-research/RESEARCH.md
@.planning/quick/20260517-voice-intent-research/SUMMARY.md

@README.md
@docs/showcase/index.html

<!-- Specs.md is ~4040 lines — DO NOT @-include. Read only the changed sections via Read tool with offset/limit during execution. -->

<interfaces>
<!-- Documentation update contracts -->

Specs.md sections affected (read each via `Read` with offset+limit to avoid loading the whole file):
- §3.6 EvenAI status (find via grep `grep -n "## 3.6\|### 3.6" Specs.md`) — add INV-2 re-verified note + reference to Phase 15 keyterm-prompting as the chosen mitigation under the status quo constraint
- §5.2 Bridge stack — note that the Deepgram URL builder now includes `keyterm` query params; reference DEEPGRAM_KEYTERM_LIMIT=100
- `## Changelog` — add a new stanza after the most recent entry (consult `git log --oneline -10` to find the previous changelog entry pattern; v0.9.11 milestone closure was the last cross-cutting commit; Phase 14 added a v0.9.12 stanza on 2026-05-17)

README.md sections to update:
- Phase progress (look for "Phase 14 complete" or similar; add Phase 15 line)
- Voice section (mentions Phase 12 baseline; extend with Phase 15 keyterm prompting paragraph citing +625% recall lift from RESEARCH.md and the 17-socketlib-handler invariant preservation)

docs/showcase/index.html:
- Stat strip — increment the "v0.9.12 plans complete" counter; phase 15 line under quick wins
- Mini-changelog — short stanza matching Specs.md changelog style

.planning/STATE.md:
- Update Current Position to "Phase 15 ✅ closed"
- Add velocity entry under "Recent Trend"
- Add commit hash placeholder (executor fills after the commit)

.planning/ROADMAP.md:
- Phase 15 row → ✅ Complete
- Plan list section under Phase 15: list 15-01..05 with brief description
- Update milestone progress table (v0.9.12 → 2/2 phases complete, X/X plans)

.planning/REQUIREMENTS.md:
- VOICE-06..09 → Resolved (or Active → Closed; mirror Phase 14 INFILL-* closure pattern)
- Update coverage stats if present
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare Specs.md + README.md + showcase coherent doc edits</name>
  <files>
    Specs.md,
    README.md,
    docs/showcase/index.html
  </files>
  <read_first>
    README.md,
    docs/showcase/index.html,
    .planning/quick/20260517-voice-intent-research/RESEARCH.md
  </read_first>
  <action>
    Step 1 — Locate Specs.md sections to edit. Use `grep -nE "^(## |### )3\.6|^(## |### )5\.2|^## Changelog" Specs.md` to find exact heading line numbers. Read each section with offset+limit (80 lines around each heading) — do NOT read the full 4040-line file.

    Step 2 — Specs.md §3.6 amendment. Add a paragraph at the end of §3.6 with bold lead `**Phase 15 mitigation (v0.9.12):**` documenting:
    - Status quo confirmed by INV-2 re-check on 6 canonical Even Realities domains 2026-05-17 (cite `.planning/quick/20260517-voice-intent-research/RESEARCH.md` §1)
    - EvenAI native remains opaque to dev apps — no transcript subscription, no intent hook
    - Phase 15 chose Deepgram Keyterm Prompting (+625% entity-recall lift on esoteric D&D 5e terms per Deepgram learn article — same source as RESEARCH.md §1)
    - Architecture unchanged from Phase 12; keyterm is a parameter, not a new system
    - Picovoice Rhino edge-classifier remains conditional on SC-12-01 hardware test p50 > 800ms (deferred under ADR-0005 Branch A)

    Step 3 — Specs.md §5.2 amendment. Add 1-2 sentences in the Bridge stack section noting the Deepgram session URL now includes one `keyterm=<URL-encoded>` query parameter per element in the union of static SRD spell vocab (`SPELL_KEYTERMS`, 70 entries × 2 locales = 140 keyterms before cap) and dynamic entity-pack vocab (Foundry-derived; truncated first when union exceeds DEEPGRAM_KEYTERM_LIMIT=100). Cite Phase 15 plans by path.

    Step 4 — Specs.md changelog stanza. Append a new entry under `## Changelog` matching the v0.9.11 milestone closure entry style (find via `grep -n "^## Changelog" Specs.md` then read the next ~80 lines to mirror the format). Stanza content (substitute the bracketed token where shown):

      Title: `### 2026-05-17 — v0.9.12 Phase 15 closed (Deepgram Keyterm Prompting + Entity-Pack Integration)`

      Body bullets:
      - **Requirements closed:** VOICE-06, VOICE-07, VOICE-08, VOICE-09 (4 / 4 software-only)
      - **What landed:** `SPELL_KEYTERMS` data exported from `@evf/shared-protocol` (70 entries × IT + EN, drift-proof against foundry-mcp `SPELL_LOOKUP` via the SKT-02 mapping test); `buildKeytermList()` pure function in `@evf/bridge` — union of static SRD vocab + dynamic Foundry entity-pack snapshot; static-wins on conflict; entity-pack truncated first when union exceeds DEEPGRAM_KEYTERM_LIMIT=100 (CONTEXT D-04); `createDeepgramStt` extended with `keytermProvider` callback; URL builder appends one `keyterm=<URL-encoded>` query param per element (VOICE-06 + VOICE-08); `EntityPackCache.onChange()` subscription API; `KeytermRefresher` orchestrator (debounce 250ms + drain-then-restart mutex) wires cache pushes → adapter refresh (VOICE-09); failure modes: empty-cache one-shot warn log; close-code-1007/1008/4xxx triggers retry-once-sanitized → fallback-to-baseline (never fail-closed); end-to-end integration test exercises cache push → debounce → connect → URL contains new keyterms
      - **INV-2 re-verified ✓ 2026-05-17:** Deepgram Nova-3 Multilingual `keyterm` parameter, +625% entity-recall lift, and EvenAI native API closure on 6 canonical Even Realities domains (see `.planning/quick/20260517-voice-intent-research/RESEARCH.md` §1)
      - **Invariants preserved:** `socketlib.registerComplexHandler` count = **17** (CI Gate 8 — refresh path uses existing `/internal/delta` multiplex; no new handler registered); Phase 12 baseline byte-for-byte preserved when `keytermProvider` is absent or returns empty (DGKT-04 regression-safe assertion); all 35 hardware-pending SCs carry forward unchanged under ADR-0005 Branch A — Phase 15 is software-only
      - **Test delta:** +66 new/extended tests in `@evf/bridge` voice + cache + shared-protocol packages
      - **Commit:** [executor fills after `git commit`]

    Step 5 — README.md edits. Use Edit tool (not Write):
    1. Phase progress section — add a Phase 15 ✅ line matching Phase 14's style (find via `grep -n "Phase 14" README.md`)
    2. Voice / STT section (if present; find via `grep -n -i "voice\|deepgram\|stt" README.md`) — add a paragraph on Phase 15 keyterm prompting; cite the +625% lift and the 17-socketlib invariant

    Step 6 — docs/showcase/index.html edits. Use Edit tool:
    1. Stat strip — find the v0.9.12 phase count (currently 1/2 from Phase 14 closure; flip to 2/2)
    2. Mini-changelog — add a 2-3 sentence stanza matching the showcase voice and Specs.md changelog content (compressed)
    3. Update the showcase header version string if it shows phase count (e.g. "v0.9.12 Quick Wins · Phase 15 closed")

    Coherence rule (INV-3): every numeric value mentioned MUST match across all three files (Specs.md + README.md + showcase). Verify with the pre-bump checklist from CLAUDE.md (badge version === Specs.md header version === showcase hero stat version).
  </action>
  <verify>
    <automated>grep -q "Phase 15 closed" Specs.md && grep -q "VOICE-06" Specs.md && grep -q "VOICE-09" Specs.md && grep -q "625%" Specs.md && (grep -qi "phase 15\|keyterm" README.md) && (grep -qi "phase 15\|keyterm" docs/showcase/index.html) && echo "INV-3 CHECK PASS"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command above prints `INV-3 CHECK PASS` and returns exit 0
    - Specs.md §3.6 paragraph cites RESEARCH.md and the 6-source INV-2 re-check
    - Specs.md §5.2 mentions DEEPGRAM_KEYTERM_LIMIT=100 and the keyterm URL param
    - Numeric values (140 = 70 spells × 2 locales; 100 cap; 17 socketlib handlers; 4/4 REQ closure) are consistent across all three files (manually re-read and confirm)
    - `grep -c "v0.9.12" Specs.md` increases by ≥1 (changelog entry added)
    - No half-updated state — every cross-cutting fact appears in all three files OR in none (INV-3 hard rule)
  </acceptance_criteria>
  <done>Specs.md + README.md + showcase coherently updated; INV-3 numeric consistency verified by both grep gate and manual cross-read; ready for staging.</done>
</task>

<task type="auto">
  <name>Task 2: Write 15-VERIFICATION.md + update STATE.md / ROADMAP.md / REQUIREMENTS.md</name>
  <files>
    .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Step 1 — Locate the Phase 14 verification doc to mirror its structure. Run `ls .planning/phases/EVF-14-*/14-VERIFICATION.md 2>/dev/null` (or `find .planning/phases -maxdepth 2 -name "14-VERIFICATION.md"`); if found, read it. If absent (Phase 14 may not have used a separate verification file), mirror the structure described below from scratch.

    Step 2 — Create `.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md` with these sections:

    1. **Phase goal recap** (1 paragraph; cite ROADMAP.md Phase 15 entry verbatim)

    2. **REQ-IDs closed**: table mapping VOICE-06 / VOICE-07 / VOICE-08 / VOICE-09 → status `Resolved · Software-only` → evidence column (test file + test ID range), e.g.:
       | REQ | Status | Evidence |
       | --- | --- | --- |
       | VOICE-06 | Resolved · Software-only | `packages/bridge/src/voice/deepgram-stt.test.ts` DGKT-01..06 |
       | VOICE-07 | Resolved · Software-only | `packages/bridge/src/voice/keyterm-merger.test.ts` KM-01..12 + `keyterm-integration.test.ts` INT-01 |
       | VOICE-08 | Resolved · Software-only | `keyterm-merger.test.ts` KM-02 + `deepgram-stt.test.ts` DGKT-02 |
       | VOICE-09 | Resolved · Software-only | `keyterm-refresher.test.ts` KRF-01..07 + `entity-pack-cache.test.ts` EPC-SUB-01..05 + `keyterm-integration.test.ts` INT-01 |

    3. **Test results summary**: total bridge test count before phase 15 (2546 from Quick Task 260517-k2g) + delta from each plan (15-01: +17 across two packages; 15-02: +6 in deepgram-stt; 15-03: +17 across cache + refresher + adapter; 15-04: +18 sanitizer/failure-mode + 3 integration) → total expected ~2607; fill in actual numbers from a fresh `pnpm test:coverage` run capture

    4. **CI gates evidence** (paste exit-code output of each):
       - `pnpm lint:ci` exit 0
       - `pnpm typecheck` exit 0
       - `pnpm test:coverage` exit 0 with coverage ≥80% on new code
       - `grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` = `17`
       - `pnpm inv:all:skip-inv2` green (INV-1, INV-3, INV-4, INV-5 pass; INV-2 hardware-skipped under ADR-0005 Branch A — same script Phase 10 used)

    5. **Hardware UAT carry-forward**: Phase 15 introduces ZERO new hardware-pending SCs (CONTEXT.md confirmed software-only scope); all 35 existing `human_needed` SCs from v0.9.11 carry forward unchanged under ADR-0005 Branch A

    6. **Plans landed**: 5 plans (15-01..15-05) with one-line description each

    7. **Decision log** (non-obvious choices made during execution):
       - SPELL_KEYTERMS data lives in `@evf/shared-protocol`, NOT in `@evf/foundry-mcp` (bridge has no dep on foundry-mcp; data is shared, logic stays in foundry-mcp)
       - `DEEPGRAM_KEYTERM_LIMIT = 100` (Deepgram-documented cap per RESEARCH.md §2 Option C)
       - Static-wins on dedupe (CONTEXT D-01); entity-pack truncated first on overflow (CONTEXT D-04)
       - `keytermProvider` is a callback, not a static array, so lazy refresh is connect-time (DGKT-05) — no mid-stream WS reconfig (Deepgram doesn't support it)
       - `KEYTERM_REJECT_CODES = [1007, 1008]` + range 4000-4999 for retry trigger
       - Per-session retry state (no global "keyterms-are-bad" flag) — transient backend hiccups don't degrade later sessions
       - `EntityPackCache.onChange` synchronous listener invocation with try/catch isolation
       - Drain-then-restart mutex pattern over Promise-queue (simpler; refresh semantics = "cover latest cache state at refresh time")
       - Sanitizer scope: ASCII control chars only (Unicode letter-safe — IT/EN spell names preserved)

    8. **Status**: COMPLETE — Software-only closure under ADR-0005 Branch A (Phase 15 introduced no hardware-pending items)

    Step 3 — Update `.planning/STATE.md`:
    1. Update front-matter:
       - `progress.completed_phases: 2` (Phase 14 + 15)
       - `progress.completed_plans: 8` (3 from Phase 14 + 5 from Phase 15)
       - `progress.percent: 100`
       - `last_updated: <ISO timestamp at write time>`
       - `status: complete` (match Phase 14 milestone closure precedent; or set to `planning` if a follow-up milestone is being scoped — confirm with user at checkpoint)
    2. Update "Current Position" section text to "Phase 15 ✅ closed; awaiting next milestone decision"
    3. Add a "Recent Trend" entry with commit hash placeholder `[atomic-commit-hash]` for the executor to fill after `git commit`
    4. Add Phase 15 decisions (Task 2 Step 2 §7 above) to the "Decisions" list as one bullet per non-obvious decision, prefixed `[Phase 15]:`

    Step 4 — Update `.planning/ROADMAP.md`:
    1. Top-of-file Progress table: v0.9.12 row → `2 (14–15) | 8/8 | ✅ Shipped | 2026-05-17`
    2. v0.9.12 phase progress table: Phase 15 row → `5/5 | ✅ Complete | 2026-05-17`
    3. The `- [ ] Phase 15:` checkbox under the "Quick Wins" section → `- [x] Phase 15: Deepgram Keyterm Prompting + Entity-Pack Integration` with closing sentence `(✅ closed 2026-05-17 — Option C from voice-intent-research shipped; 17-socketlib invariant preserved; +625% entity-recall lift per Deepgram docs)`
    4. Under Phase 15 "Plans: TBD" → replace with the 5-plan list:

       Plans:
       - [x] 15-01-PLAN.md — SPELL_KEYTERMS data + keyterm-merger pure function
       - [x] 15-02-PLAN.md — Deepgram adapter keytermProvider wiring + server.ts step 10 integration
       - [x] 15-03-PLAN.md — EntityPackCache.onChange + KeytermRefresher (debounce 250ms + mutex)
       - [x] 15-04-PLAN.md — Empty-cache warn + retry-then-fallback + integration test
       - [x] 15-05-PLAN.md — INV-3 atomic doc coherence closure (this commit)

    Step 5 — Update `.planning/REQUIREMENTS.md`:
    1. Traceability table — VOICE-06 / VOICE-07 / VOICE-08 / VOICE-09 each → `Resolved` (mirror INFILL-* closure column from Phase 14)
    2. Coverage summary at the bottom: v1 requirements 9 total → 9 / 9 Resolved (was 5/9 after Phase 14)
    3. Milestone status footer — `v0.9.12 Quick Wins ✅ Shipped 2026-05-17 — 2 phases · 9 v1 REQ-IDs closed · software-only`

    Step 6 — Do NOT git commit yet. The checkpoint Task 3 below presents the unified change set to the user for review.
  </action>
  <verify>
    <automated>test -f .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md && grep -q "VOICE-06" .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md && grep -q "VOICE-09" .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md && grep -q "17" .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md && grep -qi "complete" .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md && grep -q "Phase 15" .planning/STATE.md && grep -q "15-05-PLAN.md" .planning/ROADMAP.md && grep -q "Resolved" .planning/REQUIREMENTS.md && echo "PLANNING-DOCS CHECK PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints `PLANNING-DOCS CHECK PASS` and returns exit 0
    - `15-VERIFICATION.md` exists with all 8 sections populated (phase-goal-recap, REQ-IDs table, test-results, CI-gates, hardware-UAT-carry, plans-landed, decision-log, status)
    - STATE.md front-matter `progress` block reflects 2/2 phases, 8/8 plans, 100%
    - ROADMAP.md shows Phase 15 ✅ Complete and the 5-plan list with all `[x]` checkboxes
    - REQUIREMENTS.md traceability table shows VOICE-06..09 all `Resolved`
    - No git commit performed yet — staging only
  </acceptance_criteria>
  <done>VERIFICATION.md authored; STATE/ROADMAP/REQUIREMENTS updated to reflect closure; ready for checkpoint review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User reviews unified change set and approves atomic INV-3 commit</name>
  <files>
    (no files modified by this checkpoint task; Tasks 1+2 already staged the doc updates and Task 4 will perform the git commit)
  </files>
  <action>
    Pause for human review before the atomic commit. Display the change set summary in the `<what-built>` block and the verification steps in `<how-to-verify>`. Do NOT execute git commit in this task — Task 4 owns the commit step.
  </action>
  <what-built>
    All Phase 15 doc-coherence updates are staged across 7+1 files:

      1. `Specs.md` — §3.6 + §5.2 amendments + new changelog stanza for v0.9.12 Phase 15 closure
      2. `README.md` — phase progress bumped; voice section notes keyterm prompting
      3. `docs/showcase/index.html` — stat strip + mini-changelog
      4. `.planning/STATE.md` — front-matter + Recent Trend + Decisions
      5. `.planning/ROADMAP.md` — Phase 15 ✅ + 5-plan list + progress table
      6. `.planning/REQUIREMENTS.md` — VOICE-06..09 → Resolved
      7. `.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md` — new file
      8. (Optional, fill after commit) STATE.md "Recent Trend" hash placeholder will be filled with the actual commit SHA in a follow-up no-op commit if needed — match Phase 14's pattern (commit `fd35c99` in STATE.md "Recent Trend").

    Per CLAUDE.md INV-3, ALL of these must land in a SINGLE git commit (no half-updated state). The commit message follows Conventional Commits + scope `phase-15`.
  </what-built>
  <how-to-verify>
    1. From the repo root, run `git status --short` — confirm exactly these files are modified or new:
       - `Specs.md`
       - `README.md`
       - `docs/showcase/index.html`
       - `.planning/STATE.md`
       - `.planning/ROADMAP.md`
       - `.planning/REQUIREMENTS.md`
       - `.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md` (new)
    2. Run `git diff --stat HEAD` — verify the line-count changes are sane (typically Specs.md ~40 lines added; README ~5 lines; showcase ~10 lines; planning files ~30-50 lines each; VERIFICATION.md ~80-120 lines)
    3. Spot-check coherence:
       - `grep -n "v0.9.12.*Phase 15" Specs.md README.md docs/showcase/index.html` — confirm Phase 15 mentioned in all 3 docs
       - `grep -n "17" Specs.md | head -3` — confirm 17-socketlib-handler invariant cited in the changelog
       - `grep -n "Resolved" .planning/REQUIREMENTS.md | head -5` — confirm VOICE-06..09 all Resolved
    4. Optionally run the inline grep gate from Task 1 verify (`grep -q "Phase 15 closed" Specs.md && …`) and Task 2 verify (`test -f .planning/phases/…/15-VERIFICATION.md && …`) — both must echo their PASS sentinel
    5. If satisfied, type `approved` to authorise the single atomic commit. If anything looks off, describe the issue and Claude will iterate before committing.

    Commit message template (Claude will use this once approved):

      ```
      docs(phase-15): close Phase 15 — Deepgram Keyterm Prompting + Entity-Pack Integration

      VOICE-06..09 software-complete. INV-3 atomic doc coherence:
      Specs.md §3.6 + §5.2 + changelog · README · showcase · planning state.

      Invariants preserved:
      - socketlib.registerComplexHandler count = 17 (CI Gate 8)
      - Phase 12 baseline byte-for-byte when keytermProvider absent
      - 35 hardware-pending SCs carry forward under ADR-0005 Branch A

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      ```
  </how-to-verify>
  <verify>
    <human-check>User reviewed git status + git diff --stat output and typed `approved`</human-check>
  </verify>
  <done>User has approved the unified change set; Task 4 may proceed with the atomic commit.</done>
  <resume-signal>Type "approved" to authorise the atomic commit, or describe any issues that need fixing.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute atomic INV-3 commit after user approval</name>
  <files>
    Specs.md,
    README.md,
    docs/showcase/index.html,
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md
  </read_first>
  <action>
    AFTER user types `approved` in Task 3:

    1. Stage each file by name (do NOT use `git add .` or `git add -A`):
       ```
       git add Specs.md README.md docs/showcase/index.html \
               .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md \
               .planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-VERIFICATION.md
       ```
    2. Commit via heredoc with the template from Task 3:
       ```
       git commit -m "$(cat <<'EOF'
       docs(phase-15): close Phase 15 — Deepgram Keyterm Prompting + Entity-Pack Integration

       VOICE-06..09 software-complete. INV-3 atomic doc coherence:
       Specs.md §3.6 + §5.2 + changelog · README · showcase · planning state.

       Invariants preserved:
       - socketlib.registerComplexHandler count = 17 (CI Gate 8)
       - Phase 12 baseline byte-for-byte when keytermProvider absent
       - 35 hardware-pending SCs carry forward under ADR-0005 Branch A

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
    3. After commit, run `git log -1 --name-only HEAD` and verify ALL 7 paths above appear — INV-3 gate.
    4. Run `git log -1 --format='%H'` and capture the SHA; if STATE.md still has the `[atomic-commit-hash]` placeholder, do a follow-up `git commit --amend --no-edit` ONLY after staging the STATE.md hash substitution — `--amend` is acceptable here because we just created this commit and the placeholder is intentional. If the user opted out of amend, leave the placeholder and document the hash in 15-VERIFICATION.md's "Commit:" line via a separate no-op commit (Phase 14 precedent: STATE.md hash filled by a follow-up commit).
    5. Verify CI passes locally before any push: `pnpm lint:ci && pnpm typecheck && pnpm test:coverage` exit 0.
  </action>
  <verify>
    <automated>git log -1 --name-only HEAD | grep -q "Specs.md" && git log -1 --name-only HEAD | grep -q "README.md" && git log -1 --name-only HEAD | grep -q "docs/showcase/index.html" && git log -1 --name-only HEAD | grep -q "STATE.md" && git log -1 --name-only HEAD | grep -q "ROADMAP.md" && git log -1 --name-only HEAD | grep -q "REQUIREMENTS.md" && git log -1 --name-only HEAD | grep -q "15-VERIFICATION.md" && git log -1 --format=%s | grep -q "phase-15" && echo "INV-3 ATOMIC COMMIT PASS"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `INV-3 ATOMIC COMMIT PASS` and exit 0
    - All 7 files appear in `git log -1 --name-only HEAD`
    - Commit subject contains `phase-15` scope and `INV-3` keyword
    - `pnpm lint:ci` + `pnpm typecheck` + `pnpm test:coverage` all exit 0 after commit
    - `grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` STILL returns 17 (final CI Gate 8 confirmation)
    - No untracked file remains from Phase 15 work (`git status --short` is clean except for any pre-existing untracked files)
  </acceptance_criteria>
  <done>Phase 15 atomically closed in one git commit; CI green; socketlib count = 17; user has the commit SHA on screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author intent → repository state | The atomic commit is the trust transaction; user checkpoint at Task 3 is the human-in-the-loop gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-16 | Tampering | Half-updated state (e.g. Specs bumped but README not) | mitigate | Task 3 checkpoint enforces multi-file diff review BEFORE commit; Task 4 stages files by explicit name (no `-A`); INV-3 verify grep gate on `git log -1 --name-only` |
| T-15-17 | Tampering | Wrong commit hash in STATE.md "Recent Trend" | accept | Phase 14 precedent: placeholder filled by follow-up commit if needed; not gating |
| T-15-18 | Information Disclosure | Verification doc accidentally leaks API key or secret in test evidence | mitigate | 15-VERIFICATION.md test evidence cites test IDs and file paths only — never raw test output. Pre-commit hook lint catches the obvious patterns; manual review at Task 3 is the secondary gate. |
</threat_model>

<verification>
- INV-3 atomic gate verified by Task 4's automated grep on `git log -1 --name-only HEAD`
- CI gates green post-commit
- socketlib count = 17 (final invariant check)
- 15-VERIFICATION.md exists with all sections populated
- VOICE-06..09 all marked Resolved in REQUIREMENTS.md
- ROADMAP.md milestone v0.9.12 marked ✅ Shipped 2026-05-17
</verification>

<success_criteria>
1. ALL 7 doc files updated in a SINGLE atomic commit (INV-3)
2. Commit subject + body cite VOICE-06..09 closure and the 17-socketlib-handler invariant
3. `15-VERIFICATION.md` documents Phase 15 software-only completion under ADR-0005 Branch A
4. Phase 15 ✅ in STATE.md + ROADMAP.md + REQUIREMENTS.md
5. CI gates all green post-commit (lint:ci, typecheck, test:coverage)
6. `socketlib.registerComplexHandler` count = 17 (CI Gate 8 final confirmation)
7. User has the atomic commit SHA on screen and milestone v0.9.12 Quick Wins is ✅ Shipped
</success_criteria>

<output>
Create `.planning/phases/EVF-15-deepgram-keyterm-prompting-entity-pack-integration/15-05-SUMMARY.md` when done. Include:
- Atomic commit SHA
- File list (7) + line-count diff per file
- Final test count (2546 baseline + ~66 new = ~2612)
- socketlib count re-grep result (= 17)
- Milestone v0.9.12 closure status: ✅ Shipped 2026-05-17
- Pointer to 15-VERIFICATION.md for full evidence trail
</output>
