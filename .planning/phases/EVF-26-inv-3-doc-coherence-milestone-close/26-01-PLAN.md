---
plan: 26-01
phase: 26
wave: 1
depends_on: []
autonomous: true
requirements: [RINV-03]
files_modified:
  - Specs.md
  - README.md
  - docs/showcase/index.html
---

# Plan 26-01 — INV-3 Doc Coherence Milestone Close (v0.10.0)

<objective>
Atomically update Specs.md §7 + README.md + docs/showcase/index.html (single INV-3 commit, NO application code) so the docs reflect the raster canvas substrate as the default boot, demote the ASCII mockups to a "Glyph Fallback Mode — BLE-degraded path" subsection (preserved, not deleted), and bump Specs v0.9.15 → v0.10.0 with a milestone changelog stanza. This is the closing act of milestone v0.10.0.
</objective>

<artifacts_produced>
- Specs.md §7.2 raster-substrate + CanvasCompositor paragraph
- Specs.md §7.4 "Glyph Fallback Mode — BLE-degraded path" subsection wrapping the existing ASCII mockup
- Specs.md changelog stanza v0.10.0 (incl. INV-2 Re-verified ✓ 2026-06-08, no drift)
- README.md Rendering section updated to canvas compositor + version badge v0.10.0
- docs/showcase/index.html stats + version → v0.10.0
</artifacts_produced>

<tasks>

<task id="1" type="execute">
<title>Specs.md: §7.2 raster substrate + §7.4 glyph-fallback subsection + v0.10.0 changelog + header version</title>
<read_first>
- Specs.md lines 1–60 (header version + INV-2 section), 1295–1370 (§7.2 Layered Rendering, §7.4 Default View 27px grid), and the changelog section (study v0.9.6–v0.9.15 stanza format)
- .planning/phases/EVF-19*/*-SUMMARY.md … EVF-25*/*-SUMMARY.md (milestone source of truth for the changelog summary)
- .planning/phases/EVF-26-inv-3-doc-coherence-milestone-close/26-CONTEXT.md (D-26.1 INV-2 result, D-26.2 mockup-preserve, D-26.5 changelog content)
</read_first>
<action>
In Specs.md: (a) header version v0.9.15 → v0.10.0 (line 9). (b) §7.2 "Layered Rendering Model": add a paragraph stating the raster CanvasCompositor substrate (z=0 map raster + z=1 status HUD + z=2 overlay panels composited on canvas, 4 sub-tiles 200×100 of a 400×200 region, ~5fps xxhash delta loop) is the DEFAULT render path; the glyph/text path is the BLE-degraded fallback. (c) §7.4: wrap the EXISTING ASCII 27px mockup in a clearly-labeled subsection "Glyph Fallback Mode — BLE-degraded path" — do NOT delete the mockup (D-26.2, INV-1 contract); add one sentence that this is the fallback rendering, with raster as default. (d) Add a changelog stanza for v0.10.0 (above v0.9.15) summarizing the milestone per D-26.5 (CanvasCompositor P19, status HUD canvas+VT323 P20, 6-tab raster sheet+main-tab P21, features+biography schema P22, combat tracker raster+AC P23, delta loop ~5fps xxhash P24, raster default boot + glyph fallback P25, INV-1 raster contract) and a line "INV-2 Re-verified ✓ 2026-06-08 — no drift (internal rendering milestone, no new upstream claims; G2 576×288 4-bit, gestures, dnd5e 5.3.3 all confirmed)".
</action>
<acceptance_criteria>
- `grep -n "v0.10.0" Specs.md` shows the header version + a changelog stanza
- `grep -n "Glyph Fallback Mode" Specs.md` returns ≥1 (the §7.4 subsection)
- The original ASCII mockup lines in §7.4 still exist (not deleted) — verify the frame/box-drawing characters remain
- `grep -n "Re-verified ✓ 2026-06-08" Specs.md` returns 1
- `grep -nE '§[0-9]+\.[0-9]+' Specs.md` references still resolve to existing headings (no dangling §)
</acceptance_criteria>
<done>Specs.md reflects raster-default + preserved glyph fallback subsection + v0.10.0 header + changelog stanza.</done>
</task>

<task id="2" type="execute">
<title>README.md + docs/showcase/index.html: rendering description + version v0.10.0 (coherence grep)</title>
<read_first>
- README.md (version badge + Rendering/HUD section)
- docs/showcase/index.html (line ~473 stat span "v0.9.15 ... 27px HUD 8-row"; hero/version stats)
- .planning/phases/EVF-26-inv-3-doc-coherence-milestone-close/26-CONTEXT.md (D-26.3/D-26.4/D-26.7)
</read_first>
<action>
README.md: bump version badge to v0.10.0; in the Rendering/HUD section, replace any glyph-grid-centric phrasing (e.g. "10 rows × 50 chars", "27px ... text-container status" as the DEFAULT) with a description of the canvas CanvasCompositor raster substrate as default + glyph as BLE-degraded fallback. docs/showcase/index.html: bump the version stat (line ~473 and any hero/version occurrence) v0.9.15 → v0.10.0 and update the "27px HUD 8-row" stat to reflect the raster substrate (e.g. canvas compositor / raster default / current test count). Ensure README badge version == Specs header version == showcase version == v0.10.0 (pre-bump checklist). Any remaining "27px"/glyph-grid mention must be explicitly in a "glyph fallback" context.
</action>
<acceptance_criteria>
- `grep -c "v0.10.0" README.md` ≥1 and `grep -c "v0.10.0" docs/showcase/index.html` ≥1
- `grep -nE "10 rows × 50 chars|27px SDK|text-container.*status" README.md docs/showcase/index.html` returns 0 OR every remaining match is on a line containing "glyph"/"fallback"
- `grep -c "v0.9.15" docs/showcase/index.html` returns 0 (old version fully bumped)
- README version badge, Specs header (Task 1), and showcase version all read v0.10.0
</acceptance_criteria>
<done>README + showcase describe raster-default rendering and read v0.10.0, coherent with Specs.</done>
</task>

<task id="3" type="execute">
<title>Atomic INV-3 commit + inv:all gate</title>
<read_first>
- packages/validation-harness/package.json (inv:all / inv:all:skip-inv2 scripts)
</read_first>
<action>
Run `corepack pnpm --filter @evf/validation-harness inv:all` (fall back to `inv:all:skip-inv2` only if the INV-2 network step is unavailable in-suite, noting it in SUMMARY) — must be green on both glyph + raster suites (D-26.6). Verify NO application code file is staged (only Specs.md, README.md, docs/showcase/index.html). Commit ALL THREE doc files in a SINGLE atomic commit (INV-3): `docs(26): INV-3 milestone close v0.10.0 — raster default substrate, glyph fallback subsection`. Do NOT split across commits.
</action>
<acceptance_criteria>
- `git show --stat HEAD` for the INV-3 commit lists exactly Specs.md, README.md, docs/showcase/index.html (no code files)
- inv:all (or inv:all:skip-inv2 with note) exits 0
- `git status --short` shows no remaining uncommitted doc changes (pre-existing unrelated EVF-19/release-artifacts changes excluded)
</acceptance_criteria>
<done>Single INV-3 commit lands the three doc projections coherently; inv:all green.</done>
</task>

</tasks>

<must_haves>
truths:
  - "Specs.md header + a changelog stanza read v0.10.0; the v0.10.0 stanza summarizes the milestone and records INV-2 Re-verified ✓ 2026-06-08 no-drift"
  - "Specs §7 ASCII mockups are PRESERVED in a labeled 'Glyph Fallback Mode' subsection (not deleted); §7.2 states raster CanvasCompositor is the default substrate"
  - "README.md + docs/showcase/index.html read v0.10.0 and describe raster-default rendering; grep for glyph-grid-default phrasing returns 0 (or only glyph-fallback-context matches)"
  - "A SINGLE atomic commit updates exactly the 3 doc files (Specs.md, README.md, showcase) — zero application code (INV-3)"
  - "pnpm inv:all is green on both glyph + raster suites"
</must_haves>

<verification>
- corepack pnpm --filter @evf/validation-harness inv:all (or :skip-inv2 with note) → exit 0
- git show --stat <INV-3 commit> → only 3 doc files
- grep coherence guards (Task 1 + Task 2 acceptance criteria) all pass
</verification>
