---
phase: 05
plan: 04
type: execute
wave: 3
subsystem: g2-app
tags: [g2-app, panels, inventory, spellbook, sheet-tab-inv, sheet-tab-spells, dual-edition, modern-rules, weapon-mastery, spell-slots, inv-1, fixtures]
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - packages/g2-app/src/panels/inventory-panel.ts
  - packages/g2-app/src/panels/spellbook-panel.ts
  - packages/g2-app/src/panels/__tests__/inventory-panel.test.ts
  - packages/g2-app/src/panels/__tests__/spellbook-panel.test.ts
  - packages/g2-app/src/panels/character-sheet-tab-renderers.ts
  - packages/shared-render/src/fixtures/sheet.inventory.2014.it.txt
  - packages/shared-render/src/fixtures/sheet.inventory.2024.it.txt
  - packages/shared-render/src/fixtures/sheet.spells.it.txt
  - packages/shared-render/src/fixtures/inventory.2014.it.txt
  - packages/shared-render/src/fixtures/inventory.2024.it.txt
  - packages/shared-render/src/fixtures/spellbook.caster.it.txt
  - packages/shared-render/src/fixtures/spellbook.half-caster.it.txt
autonomous: true
requirements: [SHEET-01, SHEET-02, SHEET-03]
gap_closure: false

requires:
  - phase: 05-01 (HUD_WIDTH_BUDGETS Phase 5 inventory + spellbook keys + modernRules)
  - phase: 05-02 (CharacterSheetPanel + buildTabStrip + TABS)
  - phase: 05-03 (character-sheet-tab-renderers dispatcher with stub branches for inventory + spells)
provides:
  - InventoryPanel class — standalone OverlayPanel rendering UI-SPEC §5.10 (condensed equipped + consumables + carried summary)
  - SpellbookPanel class — standalone OverlayPanel rendering UI-SPEC §5.11 (cantrips + level sections + slot bars + concentration flag)
  - renderInventoryTabContent / renderSpellsTabContent — sheet-tab variants shared with the standalone panels (both panels reuse the same row-render helpers; UI-SPEC §5.4 sheet-tab Inventory shares column layout with §5.10 standalone Inventory — single helper, both call sites)
  - Updated character-sheet-tab-renderers.ts dispatcher — inventory + spells branches now call the real renderers (stubs from 05-03 removed)
  - 7 INV-1 fixtures — 2 Inv sheet-tab (2014/2024) + 1 Spells sheet-tab + 2 standalone Inv (2014/2024) + 2 standalone Spellbook (full caster + half caster)
affects:
  - 05-06 integration smoke — composes inventory + spellbook panels via PanelRouter

tech-stack:
  added: []
  patterns:
    - "Per-tab + standalone panel renderer sharing — InventoryPanel and renderInventoryTabContent both call the same `renderInventoryRow(item, locale, modernRules)` helper; column layout from UI-SPEC §5.4/§5.10"
    - "modernRules `[M]` weapon-mastery flag insertion — render-side branch in `renderInventoryRow` (NOT a schema field on each item — RESEARCH §Pattern 3 places the flag at render time)"
    - "Spell-slot bar rendering — `▓▓░░ 2/4` style per UI-SPEC §5.11; 2-char filled-spent + N-char empty + N/M counter; locale-aware label via `getLabel('spell.level_section', locale)` template"
    - "Concentration flag on spells — `≡` (always-prepared, 2024) and `≀` (concentration, all editions) at col 3 (cursor position) per UI-SPEC §6.2 glyph dictionary"

must_haves:
  truths:
    - "InventoryPanel.meta passes PanelMetaSchema (id 'inventory', navKey 'I', requiredCaps [])"
    - "SpellbookPanel.meta passes PanelMetaSchema (id 'spellbook', navKey 'B' [book], requiredCaps [])"
    - "Both panels implement OverlayPanel (`isOverlayPanel(p) === true`)"
    - "Both panels return getContainerCount image=0, text=1 (Strategy A — exemplar pattern)"
    - "Standalone Inventory differs from Sheet Inventory tab ONLY in condensed CARRIED line (UI-SPEC §5.10 vs §5.4)"
    - "2024 weapon rows contain `[M]` flag after weapon name (modernRules=true); 2014 rows omit `[M]`"
    - "Spellbook level sections show `▓` (spent) + `░` (empty) bars correctly sized to N/M slot counter"
    - "Updated dispatcher: `renderTabContent('inventory', snapshot, locale, scrollOffset)` no longer returns the placeholder stub — returns real Inv tab rows"
  artifacts:
    - path: packages/g2-app/src/panels/inventory-panel.ts
      provides: InventoryPanel class + renderInventoryRow helper + renderInventoryTabContent (shared with sheet)
      exports: ["InventoryPanel (default)", "renderInventoryTabContent", "renderInventoryRow"]
      min_lines: 200
    - path: packages/g2-app/src/panels/spellbook-panel.ts
      provides: SpellbookPanel class + renderSpellRow + renderSpellsTabContent (shared with sheet)
      exports: ["SpellbookPanel (default)", "renderSpellsTabContent", "renderSpellRow"]
      min_lines: 200
    - path: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      provides: dispatcher inventory/spells branches now call real renderers (replaces 05-03 stubs)
      contains: "renderInventoryTabContent|renderSpellsTabContent"
    - path: packages/shared-render/src/fixtures/sheet.inventory.2014.it.txt
      provides: Sheet Inventory tab body 2014 (no [M] flag)
      contains: "EQUIPAGGIAMENTO"
    - path: packages/shared-render/src/fixtures/sheet.inventory.2024.it.txt
      provides: Sheet Inventory tab body 2024 (with [M] flag)
      contains: "[M]"
    - path: packages/shared-render/src/fixtures/sheet.spells.it.txt
      provides: Sheet Spells tab body
      contains: "CANTRIP"
    - path: packages/shared-render/src/fixtures/inventory.2014.it.txt
      provides: Standalone InventoryPanel body 2014
      contains: "INVENTARIO"
    - path: packages/shared-render/src/fixtures/inventory.2024.it.txt
      provides: Standalone InventoryPanel body 2024 (with [M])
      contains: "[M]"
    - path: packages/shared-render/src/fixtures/spellbook.caster.it.txt
      provides: Standalone SpellbookPanel full caster
      contains: "LIBRO INCANTESIMI"
    - path: packages/shared-render/src/fixtures/spellbook.half-caster.it.txt
      provides: Standalone SpellbookPanel half caster
      contains: "LIBRO INCANTESIMI"
  key_links:
    - from: packages/g2-app/src/panels/inventory-panel.ts
      to: snapshot.world.modernRules
      via: renderInventoryRow conditional [M] insertion
      pattern: "modernRules"
    - from: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      to: renderInventoryTabContent + renderSpellsTabContent
      via: dispatcher branch wiring
      pattern: "renderInventoryTabContent|renderSpellsTabContent"
    - from: packages/g2-app/src/panels/spellbook-panel.ts
      to: getLabel HUD i18n keys
      via: spell.* + sheet.spell.* i18n look-up
      pattern: "getLabel.*spell\\."

metrics:
  duration: TBD
  completed: TBD
  tasks: 3
  task_commits: TBD
  files_created: 9
  files_modified: 1
  tests_added: TBD
---

<objective>
Ship the standalone InventoryPanel + SpellbookPanel + their shared sheet-tab renderers + 7 INV-1 fixtures. Both panels reuse the same row-render helpers (UI-SPEC §5.4 sheet-tab Inventory shares column layout with §5.10 standalone Inventory; §5.5 Spells tab shares with §5.11 Spellbook). The character-sheet-tab-renderers dispatcher's inventory + spells branches are replaced with the real renderers (the stubs from 05-03 are removed).

Purpose: complete SHEET-01 (all 6 sheet tabs) + close SHEET-03 dual-edition on the remaining 2 tab surfaces (Inv + Spells) — 2024 `[M]` mastery flag + 2024 always-prepared `≡` spell flag. Standalone panels are auto-discovered by PanelRouter and consumable independently of the sheet (Phase 6 wires the Quick Action `[I]` Inventory + `[B]` Spellbook entries).

Output:
- inventory-panel.ts (standalone panel + tab renderer + row helper)
- spellbook-panel.ts (standalone panel + tab renderer + row helper)
- 7 INV-1 fixtures
- character-sheet-tab-renderers.ts updated (stubs removed, real branches wired)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-01-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-02-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-03-PLAN.md
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/panels/character-sheet-panel.ts
@packages/g2-app/src/panels/character-sheet-tab-renderers.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/shared-protocol/src/payloads/character.ts

<interfaces>
From 05-01 i18n-budgets extension:
- `sheet.inv.{currency,carried,equipped,consumables,equipment,container,mastery_flag}` (sheet-tab Inventory keys)
- `inv.{panel_title,section.equipped,section.consumables,section.carried,scroll_hint,empty}` (standalone Inventory keys)
- `sheet.spell.{header_title,prepared_label,filter_bar,cantrips_section,level_section,scroll_hint}` (sheet-tab Spells keys)
- `spell.{panel_title,prepared_count,cantrips_section,level_section,available_marker,activation.action,activation.reaction,activation.bonus,activation.ritual,cursor_marker,scroll_hint,empty}` (standalone Spellbook keys)

From 05-03 character-sheet-tab-renderers.ts:
- Dispatcher `renderTabContent` with inventory + spells branches currently returning a stub placeholder string
- Pattern: this plan REPLACES those stub branches with `return renderInventoryTabContent(snapshot, locale, scrollOffset)` etc.

From CharacterSnapshot (05-01 extended):
- `world.modernRules: boolean` drives `[M]` insertion on weapons and `≡` insertion on always-prepared spells.
- For SHEET-02 live-binding: `actor.system.items` is the source array (this plan reads it via the snapshot field — current schema may need extension; if so, document and call out as an assumption).
- Spell slots: `actor.system.spells.spell{N}.value / max` for N=1..9 + `pact.value/max` for pact slots; cantrips are at level 0.

Note: the existing CharacterSnapshotSchema in 05-01 does NOT yet enumerate items or spells — it carries actor identity, HP/AC/level/conditions/exhaustion/death/world. Either: (a) extend the schema in this plan with `inventory: z.array(InventoryItemSchema)` + `spells: SpellbookSchema` (atomic with reader); OR (b) accept the panels rendering placeholder rows when those fields are absent and document the schema extension as a Phase 7 prerequisite. The CONTEXT.md §Area 6 implies the panels render REAL data; therefore option (a) is correct — schema extension in this plan is in-scope.
</interfaces>

<ui_fixtures>
Each fixture is the 19-row × 70-code-point body for the panel (rows 3-21 per UI-SPEC layout grid).

Per UI-SPEC.md verbatim:
- §5.4 Inventory sheet-tab (2014 + 2024 delta — `[M]` flag on weapons)
- §5.5 Spells sheet-tab (cantrips + level sections + slot bars + 2024 always-prepared `≡`)
- §5.10 Standalone InventoryPanel (condensed CARRIED line; otherwise shares §5.4 column layout)
- §5.11 Standalone SpellbookPanel (full caster — 9 levels; half caster — 5 levels)

Every row exactly 70 code-points. Fixtures use the same THORIN character as 05-03 to keep cross-fixture comparison sane.
</ui_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InventoryPanel + renderInventoryTabContent + 4 fixtures (sheet-tab 2014/2024 + standalone 2014/2024)</name>
  <files>packages/g2-app/src/panels/inventory-panel.ts, packages/g2-app/src/panels/__tests__/inventory-panel.test.ts, packages/shared-render/src/fixtures/sheet.inventory.2014.it.txt, packages/shared-render/src/fixtures/sheet.inventory.2024.it.txt, packages/shared-render/src/fixtures/inventory.2014.it.txt, packages/shared-render/src/fixtures/inventory.2024.it.txt</files>
  <action>
Create `inventory-panel.ts` exporting:
- Default `class InventoryPanel implements OverlayPanel` with `static meta: PanelMeta = { id: 'inventory', title: { it: 'Inventario', en: 'Inventory', de: 'Inventar' }, navKey: 'I', requiredCaps: [] }`. Constructor + lifecycle follow the ConcentrationDropModalPanel + CharacterSheetPanel exemplars.
- `renderInventoryTabContent(snapshot, locale, scrollOffset): string[]` — 18 rows of 66 code-points per UI-SPEC §5.4 (sheet-tab variant; includes currency strip + encumbrance bar + EQUIPPED/CONSUMABLES/EQUIPMENT/CONTAINER sections + scroll hint footer).
- `renderInventoryStandaloneContent(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.10 (condensed: EQUIPPED + CONSUMABLES + condensed CARRIED summary line; no currency strip, no encumbrance bar).
- `renderInventoryRow(item: InventoryItem, locale, modernRules): string` — shared helper rendering a single 66-char inventory row: glyph + name + damage/effect + tags. Inserts `[M]` after weapon name when `modernRules === true` AND item type is weapon (RESEARCH §Pattern 3 + UI-SPEC §5.4 delta). Name column 18 chars, damage column 22 chars (UI-SPEC §5.4 column budget).
- `renderEquippedSection`, `renderConsumablesSection`, `renderCarriedSection` — helpers for the section blocks.

The schema needs extension. Two sub-options:
(a) Atomic schema extension in THIS plan: extend `CharacterSnapshotSchema` with `inventory: z.array(z.object({ id: z.string(), name: z.string(), type: z.enum(['weapon', 'armor', 'consumable', 'equipment', 'container', 'currency']), damage: z.string().optional(), tags: z.array(z.string()).optional(), weight: z.number().optional() }))` plus character-reader.ts extension to map `actor.items`. This is the canonical SHEET-02 live-binding source.
(b) Stub with placeholder rows: render "(inventory data pending schema extension)" until Phase 7 lands the writes.

Choose (a) — atomic schema extension (same playbook as Phase 4b Plan 06 atomic). The test file mocks the actor.items source per the existing character-reader.test.ts pattern. Schema extension lands in `packages/shared-protocol/src/payloads/character.ts` + reader at `packages/foundry-module/src/readers/character-reader.ts` in the SAME commit as this task.

IMPORTANT: this places `character.ts` and `character-reader.ts` into 05-04's files_modified — they were ALSO modified by 05-01 (Wave 0). Since 05-01 is wave 0 (completed before this plan starts), there is no parallel conflict; 05-04 is a sequential consumer + extender. Update the plan frontmatter `files_modified` to include both. The schema extension chain at this point: 05-01 added `world.modernRules`; 05-04 adds `inventory[]`. Combined, the snapshot supports both fields.

Lifecycle methods follow the ConcentrationDropModalPanel exemplar: onMount subscribes to gestureBus, onUnmount unsubscribes, onEvent handles tap (toggle row detail expansion — Phase 5 boundary returns immediately per UI-SPEC §7.1) / scroll (cycle item highlight per scroll direction) / double-tap (close → MAIN_MAP, Phase 6 wires) / long-press (Phase 6 stub).

Tests (IP-* discriminator markers — 20+ tests):
- IP-META-1/2: meta passes schema; navKey is 'I'.
- IP-CTOR-1/2: implements OverlayPanel; getContainerCount image=0 text=1.
- IP-MOUNT-UNMOUNT-1: bus subscribe/unsubscribe symmetry.
- IP-DRAW-1: single textContainerUpgrade call per draw.
- IP-ROW-2014: renderInventoryRow with `modernRules=false, item.type='weapon'` does NOT include `[M]`.
- IP-ROW-2024: renderInventoryRow with `modernRules=true, item.type='weapon'` includes `[M]` after the weapon name (column-aligned).
- IP-ROW-2024-NON-WEAPON: `modernRules=true, item.type='consumable'` does NOT include `[M]`.
- IP-ROW-TRUNC: weapon name 30 chars long is truncated with `…` to fit 18-char name column.
- IP-ROW-WIDTH: every row exactly 66 code-points.
- IP-SECTION-EQUIPPED-1: renderEquippedSection produces the section header `getLabel('sheet.inv.equipped', 'it')` + N item rows.
- IP-CONTENT-SHEET: renderInventoryTabContent returns 18 rows.
- IP-CONTENT-STANDALONE: renderInventoryStandaloneContent returns 18 rows, includes the condensed CARRIED line, NO currency strip.
- IP-FIX-SHEET-2014: matchAsciiFixture against sheet.inventory.2014.it.txt.
- IP-FIX-SHEET-2024: matchAsciiFixture against sheet.inventory.2024.it.txt (only differs by `[M]` insertions).
- IP-FIX-STANDALONE-2014: matchAsciiFixture against inventory.2014.it.txt.
- IP-FIX-STANDALONE-2024: matchAsciiFixture against inventory.2024.it.txt.
- IP-SCHEMA-1: CharacterSnapshotSchema.parse on a payload with empty `inventory: []` succeeds.
- IP-SCHEMA-2: CharacterSnapshotSchema.parse on a payload with `inventory: [{ id, name, type:'weapon', damage:'1d8 sl' }]` succeeds.
- IP-SCHEMA-3: missing `inventory` field FAILS (strict atomic — same as 05-01 pattern).

Fixtures: 4 INV-1 files per UI-SPEC §5.4 + §5.10 (2 each for 2014 and 2024). Use the THORIN character with 4 weapons + 3 consumables + carried summary. Every row exactly 70 code-points wide.

Schema extension consequence: this plan ALSO modifies `packages/shared-protocol/src/payloads/character.ts` + `packages/foundry-module/src/readers/character-reader.ts` + their test files. Update files_modified accordingly. Atomic — schema + reader + render in a single commit (Phase 4b Plan 06 pattern).

Commit message: feat(05-04): InventoryPanel + standalone Inventory + sheet-tab renderer + schema extension + 4 fixtures (SHEET-01/02/03)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test inventory-panel</automated>
  </verify>
  <done>
InventoryPanel ships standalone + sheet-tab variants; `[M]` flag toggles correctly on 2024; 4 INV-1 fixtures pass matchAsciiFixture; CharacterSnapshotSchema extended with inventory array; reader maps actor.items; all 20+ IP-* tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: SpellbookPanel + renderSpellsTabContent + 3 fixtures (sheet-tab + standalone caster + standalone half-caster)</name>
  <files>packages/g2-app/src/panels/spellbook-panel.ts, packages/g2-app/src/panels/__tests__/spellbook-panel.test.ts, packages/shared-render/src/fixtures/sheet.spells.it.txt, packages/shared-render/src/fixtures/spellbook.caster.it.txt, packages/shared-render/src/fixtures/spellbook.half-caster.it.txt</files>
  <action>
Create `spellbook-panel.ts` exporting:
- Default `class SpellbookPanel implements OverlayPanel` with `static meta: PanelMeta = { id: 'spellbook', title: { it: 'Libro Incantesimi', en: 'Spellbook', de: 'Zauberbuch' }, navKey: 'B', requiredCaps: [] }`. Constructor + lifecycle per exemplar.
- `renderSpellsTabContent(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.5 (sheet-tab; filter bar + cantrips + level sections).
- `renderSpellbookStandaloneContent(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.11 (panel title + prepared counter + cantrips + level sections + slot bars).
- `renderSpellRow(spell, locale, modernRules): string` — 66-char row: indent + prepared marker (◉ / ▶ cursor / space) + space + name (20 chars truncated) + activation 6 chars + range 7 chars + effect 26 chars (UI-SPEC §5.11 column layout verbatim).
- `renderSlotBar(spent: number, max: number): string` — 2-char filled (▓▓) + N-char empty (░) + " " + counter (e.g., `▓▓░░ 2/4` for 2/4 spent).
- `renderLevelSection(level: number, spells: SpellEntry[], spellSlots, locale): string[]` — section header `L<N>   slot ▓▓░░ N/M` + spell rows.

The schema needs a new `spells` field. Extend `CharacterSnapshotSchema` (already touched by 05-01 + 05-04 Task 1) with `spells: z.object({ slots: z.array(z.object({ level: z.number().int().min(0).max(9), value: z.number().int().nonnegative(), max: z.number().int().nonnegative() })), spells: z.array(z.object({ id, name, level: z.number().int(), school: z.string(), activation: z.enum(['action','reaction','bonus','ritual']), range: z.string(), effect: z.string(), prepared: z.boolean(), alwaysPrepared: z.boolean(), concentration: z.boolean() })) })`. Same atomic commit pattern as Task 1. Reader extension at `character-reader.ts` maps `actor.system.spells.*`.

Modify `character-sheet-tab-renderers.ts` dispatcher: replace the `'inventory'` stub with `return renderInventoryTabContent(snapshot, locale, scrollOffset)` (already done in Task 1); replace the `'spells'` stub with `return renderSpellsTabContent(snapshot, locale, scrollOffset)`. Add the corresponding imports at the top of the file. Re-run the test suite for `character-sheet-tab-renderers.test.ts` — the existing CSTR-DISP-INV-STUB and CSTR-DISP-SPL-STUB tests will now fail (they were checking for stub placeholder text). Update those tests to check for sentinel strings from the real renderers (e.g., the IT string for "EQUIPAGGIAMENTO" on the inventory branch, "CANTRIP" for the spells branch). Mark them CSTR-DISP-INV-REAL / CSTR-DISP-SPL-REAL.

Lifecycle methods per exemplar (onMount/onUnmount/onEvent — scroll cycles spell highlight + tap toggles spell detail; double-tap close, Phase 6 wires).

Tests (SP-* discriminator markers — 20+ tests):
- SP-META-1/2: meta passes schema; navKey is 'B'.
- SP-CTOR-1/2: OverlayPanel + getContainerCount.
- SP-MOUNT-UNMOUNT-1: subscribe/unsubscribe symmetry.
- SP-ROW-WIDTH: every spell row exactly 66 code-points.
- SP-ROW-ACTIVATION-IT: 'action' renders as `azione` (6 chars).
- SP-ROW-ACTIVATION-EN: same renders as `action`.
- SP-ROW-CONCENTRATION: spell with `concentration: true` shows `≀` marker.
- SP-ROW-2024-ALWAYS: `modernRules=true` + `alwaysPrepared: true` → `≡` glyph prefix; 2014 omits the marker (uses `◉`).
- SP-SLOT-BAR-FULL: renderSlotBar(0, 4) → `░░░░ 0/4` (or similar per UI-SPEC §5.11).
- SP-SLOT-BAR-PARTIAL: renderSlotBar(2, 4) → `▓▓░░ 2/4`.
- SP-SLOT-BAR-FULL-SPENT: renderSlotBar(4, 4) → `▓▓▓▓ 4/4`.
- SP-LEVEL-1: renderLevelSection(1, spellsForLevel1, slots, 'it') returns header + N rows.
- SP-CONTENT-SHEET: renderSpellsTabContent returns 18 rows.
- SP-CONTENT-STANDALONE: renderSpellbookStandaloneContent returns 18 rows; contains panel title from `getLabel('spell.panel_title','it')`.
- SP-FIX-SHEET: matchAsciiFixture against sheet.spells.it.txt.
- SP-FIX-CASTER: matchAsciiFixture against spellbook.caster.it.txt (full caster; 9 levels).
- SP-FIX-HALF-CASTER: matchAsciiFixture against spellbook.half-caster.it.txt (half caster; 5 levels max).
- SP-SCHEMA-1: CharacterSnapshotSchema.parse with `spells.slots: []` + empty `spells.spells: []` succeeds.
- SP-SCHEMA-2: parse with realistic spell data succeeds.
- SP-DISP-INV-REAL (replaces 05-03 stub test): `renderTabContent('inventory', snapshot, 'it', 0)` returns rows containing `EQUIPAGGIAMENTO`.
- SP-DISP-SPL-REAL: `renderTabContent('spells', snapshot, 'it', 0)` returns rows containing `CANTRIP`.

Fixtures: 3 INV-1 files per UI-SPEC §5.5 + §5.11 (sheet-tab + standalone caster + standalone half-caster). Use THORIN F3/W5 as the caster (multi-class so the full-caster fixture has spell slots 1°-5°; the half-caster fixture uses a pure Paladin 5 with slots 1°-2° per the half-caster spell progression).

Commit message: feat(05-04): SpellbookPanel + standalone Spellbook + sheet-tab renderer + schema spells field + 3 fixtures (SHEET-01/02/03)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test spellbook-panel character-sheet-tab-renderers</automated>
  </verify>
  <done>
SpellbookPanel ships; sheet-tab variant integrated via dispatcher; slot bars render correctly; concentration + 2024 always-prepared glyphs verified; 3 INV-1 fixtures pass; CharacterSnapshotSchema.spells extension lands atomically with reader; all 20+ SP-* tests green; CSTR-DISP-INV-REAL + CSTR-DISP-SPL-REAL pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Schema + reader extensions for inventory + spells (atomic; folded into Tasks 1+2 or split per executor preference)</name>
  <files>packages/shared-protocol/src/payloads/character.ts, packages/shared-protocol/src/__tests__/payloads/character.test.ts, packages/foundry-module/src/readers/character-reader.ts, packages/foundry-module/src/readers/__tests__/character-reader.test.ts</files>
  <action>
This task ratifies the schema + reader extensions made in Tasks 1+2 (the schema rows + reader mappings can land in those commits, or be split here for a separate audit gate). The atomic pattern is: schema + reader land in the SAME commit as the renderer that consumes the new fields — Phase 4b Plan 06 precedent.

Schema extensions to CharacterSnapshotSchema (atomic with reader):
- `inventory: z.array(InventoryItemSchema)` where `InventoryItemSchema = z.object({ id: z.string(), name: z.string(), type: z.enum(['weapon','armor','consumable','equipment','container','currency']), damage: z.string().optional(), tags: z.array(z.string()).optional(), weight: z.number().optional() })`. Field is REQUIRED (atomic — same gate as world.modernRules).
- `spells: SpellbookSchema` where `SpellbookSchema = z.object({ slots: z.array(SpellSlotSchema), spells: z.array(SpellEntrySchema) })` with sub-schemas per Task 2.
- Both fields REQUIRED — atomic strict gate.

Reader extension at `getCharacterSnapshot`:
- Read `actor.items.contents` → map to inventory array (filter for player-visible types).
- Read `actor.system.spells` → map to slots array (levels 0-9 + pact) + spell list.
- Defensive nullish-coalesce defaults (empty arrays when actor has no items/spells).
- Document RESEARCH assumptions A2 + A3 (concentration flag path + duration label) and any further dnd5e API shapes encountered during execution.

Test the schemas + readers with sentinel data:
- Snapshot with empty inventory + empty spells parses successfully.
- Snapshot with 3 weapons + 4 consumables + 2 cantrips + 5 spells parses successfully.
- Snapshot missing `inventory` OR `spells` FAILS to parse.
- Reader test: mock actor with realistic items + spells produces a snapshot whose schema parse succeeds.
- Reader test: actor with no items → empty inventory array.

If Task 1 or Task 2 already landed these changes atomically inside their own commits, this task is a no-op audit — verify the schema has the new fields + verify the reader is wired + verify the tests exist. Document in the SUMMARY which task's commit landed which subset.

NO change to LayerManager / PanelRouter / PanelMetaSchema / i18n-budgets — those are owned by other plans.

Commit message (only if not folded into Tasks 1+2): chore(05-04): audit + ratify CharacterSnapshot inventory + spells extensions
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test character && pnpm --filter @evf/foundry-module test character-reader</automated>
  </verify>
  <done>
CharacterSnapshotSchema.parse with new fields succeeds end-to-end; reader produces wire-compliant payloads; atomic-commit discipline upheld (schema + reader + renderer in the same commit per task); all tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor.items → snapshot.inventory | Foundry-side data crosses to wire; reader filters + Zod gates the wire payload |
| actor.system.spells → snapshot.spells | Same |
| modernRules → render branch | Boolean control flow; renderer treats any non-true value as 2014 (defensive default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Malformed inventory item shape | mitigate | InventoryItemSchema z.object strict; reader produces only valid items |
| T-05-04-02 | Tampering | Spell entry with invalid level (e.g., 10) | mitigate | SpellEntrySchema z.number().int().min(0).max(9); reader clamps |
| T-05-04-03 | DoS | Pathological inventory length (1000 items) | accept | scrollOffset windowing only renders 18 rows; SHEET-02 live-binding may stutter under extreme cases — accepted, MVP single-tenant |
</threat_model>

<verification>
- pnpm typecheck exits 0
- pnpm lint:ci exits 0
- pnpm test workspace-wide passes; ≥40 new tests (IP + SP families) added; 2 existing CSTR-DISP-*-STUB tests updated to CSTR-DISP-*-REAL
- 7 fixture files exist; all matchAsciiFixture round-trip tests green
- CharacterSnapshotSchema now requires `world.modernRules` (from 05-01) + `inventory` + `spells` (this plan)
- Wave-3 dependency: 05-05 (Combat + Log) and this plan have ZERO file overlap — both run parallel
</verification>

<success_criteria>
- 4 panels now exist for Phase 5: ConcentrationDropModalPanel (Phase 4b) + CharacterSheetPanel (05-02 + 05-03) + InventoryPanel + SpellbookPanel
- PanelRouter discovers 4 panels at boot (verified in 05-06 integration smoke)
- 2024 `[M]` mastery flag verified on Inventory + Main attack list across 4 fixtures
- 2024 always-prepared `≡` glyph verified on Spellbook + Spells tab
- Slot bars render correctly across full caster + half caster fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-04-SUMMARY.md` documenting: task commits, schema fan-out details, dispatcher rewire, deviations (esp. dnd5e item shape + spell shape discoveries), Wave-3 parallelism signal (05-05 runs alongside this), Self-Check table.
</output>
