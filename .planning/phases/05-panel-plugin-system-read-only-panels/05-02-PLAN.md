---
phase: 05
plan: 02
type: execute
wave: 1
subsystem: g2-app
tags: [g2-app, panels, character-sheet, tab-strip, sheet-01, sheet-04, inv-1, fixtures, persistence, panel-skeleton]
depends_on: [05-01]
files_modified:
  - packages/g2-app/src/panels/character-sheet-panel.ts
  - packages/g2-app/src/panels/__tests__/character-sheet-panel.test.ts
  - packages/shared-render/src/fixtures/sheet.tab-strip.main-active.it.txt
  - packages/shared-render/src/fixtures/sheet.tab-strip.skills-active.it.txt
  - packages/shared-render/src/fixtures/sheet.tab-strip.inventory-active.it.txt
  - packages/shared-render/src/fixtures/sheet.tab-strip.spells-active.it.txt
  - packages/shared-render/src/fixtures/sheet.tab-strip.feats-active.it.txt
  - packages/shared-render/src/fixtures/sheet.tab-strip.bio-active.it.txt
autonomous: true
requirements: [SHEET-01, SHEET-04]
gap_closure: false

requires:
  - phase: 05-01 (PanelRouter + PanelMeta + HUD i18n keys + CharacterSnapshotSchema.world.modernRules + Strategy A pattern locked)
  - phase: 04b (OverlayPanel + PanelGestureBus + LayerManager.bundle differential demolish + hub-polyfill setLocalStorage/getLocalStorage)
provides:
  - CharacterSheetPanel class — OverlayPanel skeleton with 6-tab state machine + last-viewed persistence
  - TABS constant — `['main','skills','inventory','spells','feats','bio'] as const` shared by 05-03
  - TAB_LABELS constant — `['MAI','SKI','INV','SPL','FEA','BIO']` (3-char ASCII, locale-fixed per UI-SPEC §4.2)
  - buildTabStrip(activeIdx) — pure helper producing the 70-char tab strip row (SHEET-04 contract)
  - PERSIST_KEY = 'view.sheet.lastTab' — Even Hub kv key
  - Private `_renderTabContentStub` injection point — 05-03 replaces its body with the real per-tab renderer
  - 6 INV-1 fixtures — `sheet.tab-strip.<tab>-active.it.txt` (tab strip row only, 70-char width, one per active state — SHEET-04 ck 13)
affects:
  - 05-03 (per-tab content renderer) — replaces `_renderTabContentStub` body / adds import of new tab-renderers module
  - 05-04 / 05-05 / 05-06 — consume CharacterSheetPanel as a registry entry via auto-discovery

tech-stack:
  added: []
  patterns:
    - "Single-class 6-tab state machine — `private activeTabIndex: number` + `private scrollOffset: number` instance fields, no per-tab class split (anti-pattern from RESEARCH §Pattern 2)"
    - "Tab strip pure helper — `buildTabStrip(activeIdx)` is a pure function returning the exact 70-char row; the panel calls it; tests assert via INV-1 fixture round-trip"
    - "Last-viewed persistence via Even Hub kv — `setLocalStorage('view.sheet.lastTab', tabId)` on tab change + `getLocalStorage` on onMount (same pattern as `view.map.mode` in Phase 4b Plan 02)"
    - "Private renderer injection point — `_renderTabContentStub(tab, snapshot, locale, scrollOffset)` placeholder method; 05-03 replaces body or pivots to an import"

must_haves:
  truths:
    - "CharacterSheetPanel is auto-discovered by PanelRouter.discoverPanels() (filename matches *-panel.ts; static meta passes PanelMetaSchema)"
    - "tap gesture cycles activeTabIndex forward with wrap (Main → Skills → Inventory → Spells → Feats → Bio → Main)"
    - "scroll-up gesture cycles activeTabIndex backward with wrap"
    - "Each of 6 active-tab states renders the tab strip with one cell of form `[▶XXX ]` and 5 cells `[ XXX ]`; total row width is exactly 70 code-points"
    - "On first-ever mount, activeTabIndex = 0 (Main) — view.sheet.lastTab read returns empty/absent → defensive default"
    - "On subsequent mount, activeTabIndex matches the persisted value from `view.sheet.lastTab`"
    - "draw() issues exactly one `bridge.textContainerUpgrade` call per gesture with content embedding the tab strip row"
  artifacts:
    - path: packages/g2-app/src/panels/character-sheet-panel.ts
      provides: CharacterSheetPanel class implementing OverlayPanel; exports TABS, TAB_LABELS, buildTabStrip, PERSIST_KEY
      exports: ["CharacterSheetPanel (default)", "TABS", "TAB_LABELS", "buildTabStrip", "PERSIST_KEY"]
      min_lines: 150
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.main-active.it.txt
      provides: tab strip row with active Main cell (70 chars wide)
      contains: "MAI"
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.skills-active.it.txt
      provides: tab strip row with active Skills cell (70 chars wide)
      contains: "SKI"
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.inventory-active.it.txt
      provides: tab strip row with active Inventory cell (70 chars wide)
      contains: "INV"
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.spells-active.it.txt
      provides: tab strip row with active Spells cell (70 chars wide)
      contains: "SPL"
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.feats-active.it.txt
      provides: tab strip row with active Feats cell (70 chars wide)
      contains: "FEA"
    - path: packages/shared-render/src/fixtures/sheet.tab-strip.bio-active.it.txt
      provides: tab strip row with active Bio cell (70 chars wide)
      contains: "BIO"
  key_links:
    - from: packages/g2-app/src/panels/character-sheet-panel.ts
      to: PanelGestureBus.subscribe (from 04b-01)
      via: onMount subscribes; onUnmount unsubscribes
      pattern: "gestureBus\\.subscribe"
    - from: packages/g2-app/src/panels/character-sheet-panel.ts
      to: bridge.setLocalStorage('view.sheet.lastTab')
      via: hub-polyfill envelope dispatch
      pattern: "view\\.sheet\\.lastTab"
    - from: packages/g2-app/src/panels/character-sheet-panel.ts
      to: PanelMetaSchema validation at discovery
      via: static meta exported per PanelMeta shape
      pattern: "static meta"

metrics:
  duration: TBD
  completed: TBD
  tasks: 2
  task_commits: TBD
  files_created: 8
  files_modified: 0
  tests_added: TBD
---

<objective>
Ship the CharacterSheetPanel **skeleton** (SHEET-01 navigation core + SHEET-04 tab strip + last-viewed persistence + 6 tab-strip INV-1 fixtures). Per-tab content rendering is delivered by Plan 05-03 as a sequential follow-up in the same wave — 05-03 depends on 05-02 (the two cannot run in parallel because both touch `character-sheet-panel.ts`).

Purpose: establish the panel that consumes the OverlayPanel contract end-to-end + prove SHEET-04 tab strip width-perfect across all 6 active states.

Output:
- CharacterSheetPanel.ts skeleton (state machine + lifecycle + tab strip helper + persistence)
- 6 INV-1 fixtures for the tab strip row (one per active state)
- Tests asserting tap-cycle round-trip + scroll-back + persistence + 6 fixture matches
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-VALIDATION.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-01-PLAN.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-01-SUMMARY.md
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/engine/panel-gesture-bus.ts
@packages/g2-app/src/hub-polyfill.ts
@packages/g2-app/src/engine/map-mode-toggle.ts

<interfaces>
Key contracts from 05-01 and prior phases.

From `packages/g2-app/src/engine/panel-router.ts` (delivered by 05-01):
- `interface PanelMeta { readonly id: string; readonly title: { it: string; en: string; de?: string }; readonly navKey: string; readonly requiredCaps?: ReadonlyArray<string>; readonly defaultTab?: string }`
- Constructor shape: `new entry.Cls(bridge: EvenAppBridge, gestureBus: PanelGestureBus, locale: HudLocale)`

From `packages/g2-app/src/panels/concentration-drop-modal.ts` (exemplar):
- `class implements OverlayPanel { static meta: PanelMeta; readonly id; readonly z; constructor(bridge, gestureBus, locale, ...); async onMount(): Promise<void>; async onUnmount(): Promise<void>; onEvent(g: R1Gesture): void; async draw(): Promise<void>; destroy(): void; getContainerCount(): { image: 0; text: 1 } }`
- Strategy A: `new TextContainerUpgrade({ containerName: 'overlay-block', content: rows.join('\\n') })`

From `packages/g2-app/src/hub-polyfill.ts`:
- `bridge.setLocalStorage(key: string, value: string): Promise<boolean>` and `bridge.getLocalStorage(key: string): Promise<string>` (proven by map-mode-toggle.ts in Phase 4b Plan 02)
- Namespace: `view.<feature>.<setting>` — this plan uses `view.sheet.lastTab`

From `packages/g2-app/src/status-hud/i18n-budgets.ts` (extended by 05-01):
- `getLabel('panel.title.sheet', locale)` returns the localised sheet title
- `getLabel('footer.hint.sheet', locale)` returns the IT/EN/DE sheet hint string

From `packages/shared-protocol/src/payloads/character.ts` (extended by 05-01):
- `CharacterSnapshotSchema` now requires `world: { modernRules: boolean }`
- `type CharacterSnapshot = z.infer<typeof CharacterSnapshotSchema>`
</interfaces>

<ui_fixtures>
Tab strip row contract (UI-SPEC §4.2 verbatim — character-perfect):

Active Skills, others inactive (illustrative; replicate verbatim from UI-SPEC.md §4.2 / §5.3):
"┌─[ MAI ]─[▶SKI ]─[ INV ]─[ SPL ]─[ FEA ]─[ BIO ]──────────────────┐"

Each cell is exactly 7 code-points (`[` + space-or-▶ + 3-char-label + space + `]`); 6 cells × 7 = 42 code-points; row begins with `┌─` (2 code-points) and ends with `─...─┐` padding to col 70. Total row: exactly 70 code-points.

The 6 fixtures together prove SHEET-04 ck 13 — only column shift between any two adjacent fixtures is the `▶`/space swap at the active cell position.

Each fixture in this plan contains ONLY the tab strip row (single line + terminal newline matching Phase 4b fixture convention from `status-hud.death-saves-initial.it.txt`). The full sheet body fixtures are owned by 05-03.
</ui_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CharacterSheetPanel skeleton — state machine + tab strip + persistence + meta</name>
  <files>packages/g2-app/src/panels/character-sheet-panel.ts, packages/g2-app/src/panels/__tests__/character-sheet-panel.test.ts</files>
  <action>
Create `character-sheet-panel.ts` implementing `OverlayPanel` per RESEARCH §Pattern 2 + the ConcentrationDropModalPanel exemplar verbatim (constructor shape, lifecycle hooks, `overlay-block` container name, getContainerCount returns image=0 text=1).

Module exports:
- `const TABS = ['main', 'skills', 'inventory', 'spells', 'feats', 'bio'] as const`; `type TabId = (typeof TABS)[number]`.
- `const TAB_LABELS = ['MAI', 'SKI', 'INV', 'SPL', 'FEA', 'BIO'] as const` (locale-fixed per UI-SPEC §4.2).
- `buildTabStrip(activeIdx: number): string` — pure helper returning the 70-char tab strip row. Implementation: for each tab cell, render `[▶XXX ]` when its index equals `activeIdx`, else `[ XXX ]`; concatenate the 6 cells (no separator between cells per UI-SPEC §4.2 mockup); prefix with `┌─` (2 code-points) and pad with `─` to col 70, ending with `┐`. Width assertion uses `[...result].length === 70` (NEVER `result.length` — RESEARCH Pitfall 5 Unicode code-point counting).
- `PERSIST_KEY = 'view.sheet.lastTab' as const`.
- Default export `class CharacterSheetPanel implements OverlayPanel`.

Class shape (per exemplar):
- `static meta: PanelMeta = { id: 'character-sheet', title: { it: 'Scheda', en: 'Sheet', de: 'Blatt' }, navKey: 'S', requiredCaps: [], defaultTab: 'main' }`
- `readonly id = 'character-sheet'`
- `readonly z = ZIndex.Z2_OVERLAY` (numeric 2)
- Private fields: `activeTabIndex: number = 0`, `scrollOffset: number = 0`, `snapshot: CharacterSnapshot | null = null` (set by `onSnapshot` from the boot orchestrator WS handler in 05-06; render defensively when null — show a "loading" stub or empty content area until snapshot arrives), `unsubscribe: (() => void) | null = null`.
- Constructor: `(private readonly bridge: EvenAppBridge, private readonly gestureBus: PanelGestureBus, private readonly locale: HudLocale)`.

Lifecycle methods:
- `async onMount(): Promise<void>` — subscribe to gestureBus, save unsubscribe closure; await `this._restoreLastTab()`; call `await this.draw()`.
- `async onUnmount(): Promise<void>` — invoke `this.unsubscribe()` if non-null, set to null (idempotent — T-4b-01-03 mitigation from Phase 4b).
- `onEvent(gesture: R1Gesture): void` — dispatch per RESEARCH §Pattern 2:
  - `tap` → `activeTabIndex = (activeTabIndex + 1) % TABS.length`; reset `scrollOffset = 0`; `void this._persistLastTab()`; `void this.draw()`.
  - `scroll` with `direction === 'up'` → `activeTabIndex = (activeTabIndex - 1 + TABS.length) % TABS.length`; reset scroll; persist; draw.
  - `scroll` with `direction === 'down'` → forward cycle (same as tap).
  - `double-tap` → no-op stub (Phase 6 NAV-01 wires close).
  - `long-press` → no-op stub (Phase 6 Quick Action wires this).
- `onSnapshot(newSnapshot: CharacterSnapshot): void` — public method consumed by 05-06 boot orchestrator wiring: set `this.snapshot = newSnapshot`; `void this.draw()`. NO remount — re-render in place per RESEARCH §Pattern 2 anti-pattern guard.

Render method:
- `async draw(): Promise<void>` — build content as newline-joined rows: row 3 is `buildTabStrip(this.activeTabIndex)`; rows 4-21 are `this._renderTabContentStub(TABS[this.activeTabIndex], this.snapshot, this.locale, this.scrollOffset)` (placeholder owned by this plan; 05-03 replaces this method body or pivots to an external module import). Wrap in `new TextContainerUpgrade({ containerName: 'overlay-block', content: rows.join('\\n') })` and `await this.bridge.textContainerUpgrade(payload)` — exemplar verbatim.

Stub method (placeholder for 05-03):
- `private _renderTabContentStub(tab: TabId, snapshot: CharacterSnapshot | null, locale: HudLocale, scrollOffset: number): string[]` — returns an array of 18 rows, each 66 code-points wide, with text content "(content rendered by 05-03)" centered horizontally on a single row and remaining rows space-padded. Include the inline comment `// STUB: 05-03 replaces this with real per-tab content from character-sheet-tab-renderers.ts`.

Required helpers:
- `destroy(): void` — intentionally empty (Strategy A — exemplar).
- `getContainerCount(): { image: 0; text: 1 }` — verbatim from exemplar.
- Private `async _persistLastTab(): Promise<void>` — wrap `this.bridge.setLocalStorage(PERSIST_KEY, TABS[this.activeTabIndex] ?? 'main')` in try/catch (cosmetic — non-fatal per RESEARCH).
- Private `async _restoreLastTab(): Promise<void>` — wrap `this.bridge.getLocalStorage(PERSIST_KEY)` in try/catch; on success, set `activeTabIndex` to `Math.max(0, TABS.indexOf(stored as TabId))` (defensive index lookup); on fail/empty, leave at 0.

Tests (CHSP-* discriminator markers, 18 tests):
- CHSP-META-1: `CharacterSheetPanel.meta` passes `PanelMetaSchema.safeParse`.
- CHSP-META-2: meta.id is `character-sheet`; navKey is `S`; defaultTab is `main`; requiredCaps is empty array.
- CHSP-CTOR-1: instance passes `isOverlayPanel` predicate.
- CHSP-CTOR-2: getContainerCount returns image=0, text=1.
- CHSP-MOUNT-1: onMount subscribes to gestureBus — `bus.size() === 1` after mount.
- CHSP-UNMOUNT-1: onUnmount unsubscribes — `bus.size() === 0` after unmount.
- CHSP-TAP-1: 6 sequential tap gestures cycle through all 6 tabs and return to Main.
- CHSP-TAP-2: tap from Main → Skills (forward direction confirmed).
- CHSP-SCROLL-UP-1: scroll-up from Main → Bio (wrap backward).
- CHSP-SCROLL-DOWN-1: scroll-down behaves identically to tap (forward).
- CHSP-PERSIST-1: tap triggers `bridge.setLocalStorage` with key `view.sheet.lastTab` and value `skills` (after first tap from Main).
- CHSP-RESTORE-1: stored value `spells` → onMount sets activeTabIndex to 3.
- CHSP-RESTORE-2: stored value `invalid` → onMount defaults to 0.
- CHSP-RESTORE-3: getLocalStorage throws → onMount defaults to 0 (defensive).
- CHSP-TABSTRIP-1: `buildTabStrip(0)` is exactly 70 code-points and includes `▶MAI`.
- CHSP-TABSTRIP-2: `buildTabStrip(3)` is exactly 70 code-points and includes `▶SPL`.
- CHSP-DRAW-1: draw issues exactly one `bridge.textContainerUpgrade` call with containerName `overlay-block`.
- CHSP-DBL-TAP-1: double-tap is a no-op (no state change, no draw call).
- CHSP-LONG-PRESS-1: long-press is a no-op stub (no state change, no draw call).

Reference Phase 4b `panels/__tests__/concentration-drop-modal.test.ts` for the canonical mock-bridge + mock-gestureBus + spy pattern. Constraints: NO React/Vue/Svelte (D-2.04); NO browser localStorage (CLAUDE.md); use code-point-counting widths (RESEARCH Pitfall 5); NO LayerManager.bundle from inside the panel (RESEARCH anti-pattern). 05-03 will delete or replace the `_renderTabContentStub` method body — this plan must leave the method clearly marked.

Commit message: feat(05-02): CharacterSheetPanel skeleton + tab strip + view.sheet.lastTab persistence (SHEET-01/04)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test character-sheet-panel</automated>
  </verify>
  <done>
CharacterSheetPanel implements OverlayPanel; tap cycles forward through 6 tabs with wrap; scroll-up cycles backward; persist + restore via Even Hub view.sheet.lastTab; buildTabStrip is pure + 70-char wide for all 6 active indices; static meta passes PanelMetaSchema; all 18 CHSP-* tests green; typecheck and lint:ci exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 6 tab-strip INV-1 fixtures (SHEET-04 ck 13)</name>
  <files>packages/shared-render/src/fixtures/sheet.tab-strip.main-active.it.txt, packages/shared-render/src/fixtures/sheet.tab-strip.skills-active.it.txt, packages/shared-render/src/fixtures/sheet.tab-strip.inventory-active.it.txt, packages/shared-render/src/fixtures/sheet.tab-strip.spells-active.it.txt, packages/shared-render/src/fixtures/sheet.tab-strip.feats-active.it.txt, packages/shared-render/src/fixtures/sheet.tab-strip.bio-active.it.txt</files>
  <action>
Author 6 INV-1 fixtures — one per active-tab state. Each fixture contains the EXACT 70-code-point tab strip row that `buildTabStrip(activeIdx)` produces, plus a single terminal newline (matching Phase 4b fixture convention from `status-hud.death-saves-initial.it.txt`).

Per UI-SPEC.md §4.2 verbatim — replicate character-perfect:
- Active Main (index 0): the row with the `▶` glyph inside the MAI cell, all other cells inactive.
- Active Skills (index 1): `▶` inside the SKI cell.
- Active Inventory (index 2): `▶` inside the INV cell.
- Active Spells (index 3): `▶` inside the SPL cell.
- Active Feats (index 4): `▶` inside the FEA cell.
- Active Bio (index 5): `▶` inside the BIO cell.

Each line MUST be exactly 70 visible code-points (verifiable via `[...line].length === 70` in the test). The separator dash between cells (`─`) appears 5 times between the 6 cells per UI-SPEC mockup; the trailing fill uses `─` to col 70 followed by `┐`. Count carefully — character-perfect per INV-1 ck 13.

Wire 6 INV-1 round-trip tests in `character-sheet-panel.test.ts` using `matchAsciiFixture` from `@evf/shared-render` (reference Phase 4a exemplar from `concentration-drop-modal.test.ts` CDM-13 for the call pattern):
- CHSP-FIX-MAIN: `buildTabStrip(0)` round-trips against `sheet.tab-strip.main-active.it.txt`.
- CHSP-FIX-SKILLS: `buildTabStrip(1)` matches `sheet.tab-strip.skills-active.it.txt`.
- CHSP-FIX-INVENTORY: `buildTabStrip(2)` matches `sheet.tab-strip.inventory-active.it.txt`.
- CHSP-FIX-SPELLS: `buildTabStrip(3)` matches `sheet.tab-strip.spells-active.it.txt`.
- CHSP-FIX-FEATS: `buildTabStrip(4)` matches `sheet.tab-strip.feats-active.it.txt`.
- CHSP-FIX-BIO: `buildTabStrip(5)` matches `sheet.tab-strip.bio-active.it.txt`.

The fixtures together prove SHEET-04 ck 13 — the only column shift between any two adjacent fixtures is the `▶`/space swap at the active cell position. If during execution the planner-cited UI-SPEC line drifts by even 1 char (count error in the mockup), the executor MUST follow UI-SPEC as the design contract (Phase 4b Plan 01 Deviation #1 pattern — UI-SPEC is source of truth; the plan summary text may have typos). Document inline in the SUMMARY.

NO width-budget tests in this task (those live in 05-01 i18n-budgets work). NO content rendering — only the tab strip row.

Commit message: feat(05-02): 6 INV-1 tab-strip fixtures (SHEET-04 ck 13)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test character-sheet-panel</automated>
  </verify>
  <done>
6 fixtures committed; each is exactly 70 code-points wide (verified via the CHSP-FIX-* matchAsciiFixture assertions); the 6 adjacent fixtures differ only by the `▶`/space swap at the active cell position; SHEET-04 ck 13 contract holds in software.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R1 gesture → CharacterSheetPanel.onEvent | Untrusted input enters the panel via PanelGestureBus; gesture discriminator union is exhaustive |
| EvenAppBridge.getLocalStorage → CharacterSheetPanel state | External storage value may be tampered with; must be validated against `TABS` array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | `view.sheet.lastTab` value outside the locked tab set | mitigate | `TABS.indexOf(stored as TabId)` returns -1 for invalid values; `Math.max(0, ...)` defaults to Main; never throws |
| T-05-02-02 | Tampering | `scrollOffset` overflow on pathological scroll | mitigate | Reset to 0 on every tab change; 05-03's renderer clamps within content bounds |
| T-05-02-03 | DoS | gesture bus subscriber leak across mount/unmount cycles | mitigate | onMount stores unsubscribe; onUnmount invokes it + nulls (T-4b-01-03 idempotency) |
</threat_model>

<verification>
- pnpm typecheck exits 0
- pnpm lint:ci exits 0
- pnpm test --filter @evf/g2-app passes; ≥18 CHSP-* tests + 6 CHSP-FIX-* tests added
- 6 fixture files exist under packages/shared-render/src/fixtures/sheet.tab-strip.*-active.it.txt
- Each fixture is exactly 70 code-points wide (matchAsciiFixture round-trip is the gate)
- CharacterSheetPanel is auto-discoverable when PanelRouter.discoverPanels() runs in an integration test (verified in 05-06 integration smoke)
</verification>

<success_criteria>
- 6-tab cycle round-trips through tap (forward) and scroll-up (backward) with wrap behavior at both ends
- Last-viewed tab persists via Even Hub setLocalStorage and is restored on subsequent mount
- 6 INV-1 fixtures prove SHEET-04 ck 13 character-perfect contract
- Wave-1 readiness: 05-03 receives a clean handoff — the panel skeleton compiles + tab strip is the source of truth + `_renderTabContentStub` is the single injection point
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-02-SUMMARY.md` documenting: 2 task commits (one per task), files created, test counts before+after, deviations (esp. any UI-SPEC drift on tab strip width), Wave-1 sequential signal (05-03 unblocked), Self-Check table.
</output>
