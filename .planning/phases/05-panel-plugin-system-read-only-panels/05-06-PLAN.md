---
phase: 05
plan: 06
type: execute
wave: 4
subsystem: g2-app
tags: [g2-app, locale-override, boot-engine, integration-smoke, i18n-02, i18n-05, inv-1, stress-fixtures, panel-router-wire, phase-5-closure]
depends_on: [05-01, 05-02, 05-03, 05-04, 05-05]
files_modified:
  - packages/g2-app/src/locale/locale-override.ts
  - packages/g2-app/src/locale/__tests__/locale-override.test.ts
  - packages/g2-app/src/internal/boot-engine-core.ts
  - packages/g2-app/src/__tests__/boot-engine-locale-override.test.ts
  - packages/g2-app/src/__tests__/05-panel-integration-smoke.test.ts
  - packages/shared-render/src/fixtures/locale-override.stress-es.it.txt
  - packages/shared-render/src/fixtures/locale-override.stress-fr.it.txt
  - packages/shared-render/src/fixtures/locale-override.stress-pt-br.it.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.de.txt
  - packages/shared-render/src/fixtures/combat-tracker.full-window.de.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.en.txt
  - packages/shared-render/src/fixtures/sheet.skills.en.txt
  - packages/shared-render/src/fixtures/inventory.2014.es.txt
autonomous: true
requirements: [I18N-02, I18N-05]
gap_closure: false

requires:
  - phase: 05-01 (LOCALE_MENU constant + widened HudLocale + per-key EN fallback in getLabel)
  - phase: 05-02 (CharacterSheetPanel auto-discoverable)
  - phase: 05-03 (character-sheet-tab-renderers dispatching all 6 tabs)
  - phase: 05-04 (Inventory + Spellbook panels + spell + inv renderers wired)
  - phase: 05-05 (Combat tracker + Log panels + log schema)
  - phase: 04b (boot-engine-core.ts existing step ordering)
provides:
  - locale-override.ts module — `loadLocaleOverride(bridge): Promise<HudLocale>` + `persistLocaleOverride(bridge, locale)` (Even Hub kv round-trip)
  - boot-engine step 9-area extension — after BLE probe + map mode override + capability handshake, read `view.locale.override` from Even Hub kv; if not 'auto', override the runtime locale (I18N-02)
  - 05-panel-integration-smoke.test.ts — end-to-end harness composing all 5 Phase 5 panels via real PanelRouter + LayerManager + PanelGestureBus (no mocking of layer internals); proves PanelRouter discovers all 5 panels at boot
  - 8 INV-1 fixtures — 3 stress fixtures (ES/FR/PT-BR per-key EN fallback) + 2 DE fixtures + 2 EN fixtures + 1 ES fixture (auxiliary I18N-04/05 coverage)
affects:
  - none — this is the Phase 5 closure plan

tech-stack:
  added: []
  patterns:
    - "boot-engine step extension via in-place modification — boot-engine-core.ts step 9-area adds locale override read-back analogous to Phase 4b Plan 02's map mode override at the same step (CONTEXT.md §Area 4)"
    - "Stress-fixture per-key EN fallback verification — author 3 fixtures rendering a panel in ES/FR/PT-BR locale; expected output is the EN string for each i18n key (proves the 05-01 getLabel best-effort branch works end-to-end on a real panel render)"
    - "Integration smoke harness — real LayerManager + real PanelRouter + real PanelGestureBus + mock EvenAppBridge (vi.fn spies); composes all 5 panels in a single test file to assert end-to-end PanelRouter contract (analogous to Phase 4b 04b-integration-smoke.test.ts ISM-01..10)"

must_haves:
  truths:
    - "loadLocaleOverride returns 'auto' when key absent; returns the stored value when present + valid; returns 'auto' on parse failure"
    - "persistLocaleOverride calls bridge.setLocalStorage with key 'view.locale.override' and the locale code as value"
    - "bootEngine reads view.locale.override AFTER game.i18n.lang auto-detect; if value !== 'auto', overrides the runtime locale"
    - "Boot rejects invalid stored values (e.g. 'xx') by falling through to 'auto' — never throws"
    - "Integration smoke: PanelRouter.discoverPanels() returns a Map of size 5 (Char-sheet + Inventory + Spellbook + Combat-tracker + Log — plus the Phase 4b ConcDropModal that is mounted by dispatcher, NOT discovered as a player-navigable panel; verify the count matches the player-navigable surface)"
    - "Integration smoke: PanelRouter.openPanel('character-sheet') → panel mounts at z=2 via LayerManager.bundle; close → bundle destroy with differential demolish restoring z=0.5"
    - "Stress fixture (ES): rendering a panel with locale='es' produces output where every i18n key returns the EN string (per-key fallback proven on real panel output)"
  artifacts:
    - path: packages/g2-app/src/locale/locale-override.ts
      provides: loadLocaleOverride + persistLocaleOverride + LOCALE_OVERRIDE_KEY constant
      exports: ["loadLocaleOverride", "persistLocaleOverride", "LOCALE_OVERRIDE_KEY"]
      min_lines: 60
    - path: packages/g2-app/src/internal/boot-engine-core.ts
      provides: step 9-area locale override read-back integrated into bootEngine
      contains: "loadLocaleOverride|view.locale.override"
    - path: packages/g2-app/src/__tests__/05-panel-integration-smoke.test.ts
      provides: 10+ PSM-* integration tests (panel discovery, mount/unmount round-trip, persistence, locale override end-to-end)
      contains: "PanelRouter|discoverPanels"
      min_lines: 200
    - path: packages/shared-render/src/fixtures/locale-override.stress-es.it.txt
      provides: ES stress — panel output where every key returns EN fallback
      contains: ""
    - path: packages/shared-render/src/fixtures/locale-override.stress-fr.it.txt
      provides: FR stress
      contains: ""
    - path: packages/shared-render/src/fixtures/locale-override.stress-pt-br.it.txt
      provides: PT-BR stress
      contains: ""
    - path: packages/shared-render/src/fixtures/sheet.main.2014.de.txt
      provides: DE locale canonical render — exercises DE width-budget per UI-SPEC §10
      contains: "ATTRIBUTE"
    - path: packages/shared-render/src/fixtures/combat-tracker.full-window.de.txt
      provides: DE combat tracker — exercises DE quick-action labels
      contains: "KAMPF"
    - path: packages/shared-render/src/fixtures/sheet.main.2014.en.txt
      provides: EN canonical render
      contains: "ABILITIES"
    - path: packages/shared-render/src/fixtures/sheet.skills.en.txt
      provides: EN skills render
      contains: "proficient"
    - path: packages/shared-render/src/fixtures/inventory.2014.es.txt
      provides: ES best-effort render (EN fallback per key)
      contains: ""
  key_links:
    - from: packages/g2-app/src/internal/boot-engine-core.ts
      to: loadLocaleOverride from locale-override.ts
      via: step 9-area extension call
      pattern: "loadLocaleOverride"
    - from: packages/g2-app/src/locale/locale-override.ts
      to: bridge.setLocalStorage / bridge.getLocalStorage
      via: Even Hub envelope
      pattern: "view\\.locale\\.override"
    - from: packages/g2-app/src/__tests__/05-panel-integration-smoke.test.ts
      to: PanelRouter from 05-01
      via: real router + real layer manager composition
      pattern: "new PanelRouter"

metrics:
  duration: TBD
  completed: TBD
  tasks: 3
  task_commits: TBD
  files_created: 11
  files_modified: 1
  tests_added: TBD
---

<objective>
Close Phase 5: ship locale override persistence (I18N-02) + boot read-back integration into bootEngine step 9-area + 8 INV-1 stress + canonical fixtures (I18N-05 best-effort per-key EN fallback proven end-to-end) + the integration smoke that composes all Phase 5 panels via the real PanelRouter + LayerManager.

Purpose: prove I18N-02 + I18N-05 end-to-end at the boot level + verify Phase 5 panels compose under real layer-manager semantics (analogous to Phase 4b's ISM-01..10 integration smoke).

Output:
- locale-override.ts (Even Hub kv round-trip + boot read-back hook)
- boot-engine-core.ts step 9-area extension
- 05-panel-integration-smoke.test.ts (PSM-01..PSM-10+)
- 8 INV-1 fixtures: 3 stress + 2 DE + 2 EN + 1 ES auxiliary
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-VALIDATION.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-01-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-02-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-03-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-04-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-05-PLAN.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-05-SUMMARY.md
@packages/g2-app/src/locale/locale-menu.ts
@packages/g2-app/src/engine/panel-router.ts
@packages/g2-app/src/engine/map-mode-toggle.ts
@packages/g2-app/src/internal/boot-engine-core.ts
@packages/g2-app/src/hub-polyfill.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/g2-app/src/__tests__/04b-integration-smoke.test.ts

<interfaces>
From 05-01:
- `LOCALE_MENU` 7-entry constant with code + nativeLabel + budget tier
- Widened HudLocale: 'it'|'en'|'de'|'es'|'fr'|'pt-br'
- `getLabel(field, locale)` with per-key EN fallback for es/fr/pt-br

From `packages/g2-app/src/engine/map-mode-toggle.ts` (Phase 4b Plan 02 — exemplar for this plan's locale-override module):
- `STORAGE_KEY = 'view.map.mode'` pattern
- `loadPersistedMapMode(bridge): Promise<MapMode>` defensive read pattern (try/catch + normalize unknown to 'auto')
- Integration point in boot-engine-core.ts step 9b: after BLE probe, read persistence key, override if non-'auto'

From `packages/g2-app/src/internal/boot-engine-core.ts`:
- Boot step ordering is already documented in the JSDoc header (1-14). Step 9 is BLE probe + map mode override. This plan adds step 9c: locale override read-back (between map mode override at step 9b and layer construction at step 10).
- The boot engine receives `opts.locale: BootEngineLocale` (currently 'it'|'en'|'de'). This needs widening or a passthrough — the locale override may produce 'es'/'fr'/'pt-br' which were not previously valid. Decision: widen `BootEngineLocale` to match the new `HudLocale` (CONTEXT.md §Area 4 + I18N-05 best-effort policy). Boot signatures + tests update accordingly.

From `packages/g2-app/src/__tests__/04b-integration-smoke.test.ts` (Phase 4b exemplar for the new integration smoke):
- Harness pattern: real LayerManager + StatusHudLayer + ToastQueueLayer + mock EvenAppBridge (vi.fn spies) + EventEmitter-backed mock WebSocket + shared PanelGestureBus.
- This plan's harness adds: real PanelRouter + the 5 Phase 5 panels.
</interfaces>

<ui_fixtures>
INV-1 fixture set for this plan (8 files):

- **3 stress fixtures** (locale-override.stress-{es,fr,pt-br}.it.txt — naming convention preserved despite .it.txt: the file *contains* IT/EN text but tests the ES/FR/PT-BR fallback rendering). Each shows a panel rendered with the respective best-effort locale: every i18n key returns the EN string per the per-key fallback rule. Use InventoryPanel as the canonical stress panel (rich key surface). Compare against EN baseline to prove identity.
- **2 DE fixtures**: sheet.main.2014.de.txt + combat-tracker.full-window.de.txt — exercise DE canonical strings (ATTRIBUTE / KAMPF-TRACKER / Erfolge / etc.). UI-SPEC §10 INV-1 Fixture Inventory rows.
- **2 EN fixtures**: sheet.main.2014.en.txt + sheet.skills.en.txt — EN canonical baseline. UI-SPEC §10.
- **1 ES fixture**: inventory.2014.es.txt — sentinel best-effort fixture proving per-key fallback at the panel level.

All fixtures 19 rows × 70 code-points wide.
</ui_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: locale-override.ts + boot-engine step 9c extension</name>
  <files>packages/g2-app/src/locale/locale-override.ts, packages/g2-app/src/locale/__tests__/locale-override.test.ts, packages/g2-app/src/internal/boot-engine-core.ts, packages/g2-app/src/__tests__/boot-engine-locale-override.test.ts</files>
  <action>
Create `packages/g2-app/src/locale/locale-override.ts` analogous to `map-mode-toggle.ts` from Phase 4b Plan 02 (exemplar):
- Export `LOCALE_OVERRIDE_KEY = 'view.locale.override' as const`.
- Export `type LocaleOverride = 'auto' | HudLocale` (where HudLocale now includes 'it'|'en'|'de'|'es'|'fr'|'pt-br' from 05-01).
- Export `async function loadLocaleOverride(bridge: EvenAppBridge): Promise<LocaleOverride>`:
  - Try `await bridge.getLocalStorage(LOCALE_OVERRIDE_KEY)`.
  - Validate against LOCALE_MENU codes (`['auto','it','en','de','es','fr','pt-br']`). Unknown values → return 'auto' (defensive — never throw).
  - On exception (e.g. bridge not initialised), return 'auto'.
- Export `async function persistLocaleOverride(bridge: EvenAppBridge, locale: LocaleOverride): Promise<void>`:
  - Try `await bridge.setLocalStorage(LOCALE_OVERRIDE_KEY, locale)`. Swallow exceptions (cosmetic).

Tests in `locale/__tests__/locale-override.test.ts` (LO-* markers, 10+ tests):
- LO-LOAD-AUTO: getLocalStorage returns '' → result is 'auto'.
- LO-LOAD-IT: returns 'it' → result is 'it'.
- LO-LOAD-EN / DE / ES / FR / PT-BR: same for each canonical + best-effort code.
- LO-LOAD-INVALID: returns 'xx' → result is 'auto' (defensive normalise).
- LO-LOAD-THROW: getLocalStorage throws → result is 'auto'.
- LO-PERSIST: setLocalStorage called with the right key + value.
- LO-PERSIST-THROW: setLocalStorage throws → resolves without exception.

**Modify `packages/g2-app/src/internal/boot-engine-core.ts` step 9c extension:**

Widen `BootEngineLocale` from `'it' | 'en' | 'de'` to the full HudLocale union — `'it' | 'en' | 'de' | 'es' | 'fr' | 'pt-br'`. The JSDoc + BootEngineOpts interface update. Add a TODO comment noting that ES/FR/PT-BR are best-effort per I18N-05.

Insert step 9c between the existing step 9b (map mode override) and step 10 (Construct 3 layers). The step 9c body:

```
// 9c. (Phase 5 / I18N-02) Locale override read-back — device-local override from
//     Even Hub `view.locale.override`. 'auto' (or missing/invalid) lets the boot-
//     time auto-detected locale win; otherwise the stored value overrides.
const localeOverride = await loadLocaleOverride(bridge);
const effectiveLocale: BootEngineLocale = localeOverride === 'auto' ? opts.locale : localeOverride;
```

Pass `effectiveLocale` to downstream constructions: `StatusHudRenderer({ locale: effectiveLocale })`, etc. (Identify the existing pass-through points — likely `new StatusHudRenderer` constructor and `new StatusHudLayer`.) Reference Phase 4b Plan 02 SUMMARY for the analogous map-mode override insertion pattern.

Update the boot-engine JSDoc header to add step 9c to the numbered sequence.

Tests in `packages/g2-app/src/__tests__/boot-engine-locale-override.test.ts` (BELO-* markers, 8 tests):
- BELO-1: opts.locale='it' + override='auto' → effectiveLocale 'it'.
- BELO-2: opts.locale='it' + override='en' → effectiveLocale 'en'.
- BELO-3: opts.locale='it' + override='de' → effectiveLocale 'de'.
- BELO-4: opts.locale='it' + override='es' → effectiveLocale 'es' (best-effort widened type).
- BELO-5: opts.locale='it' + override unset (returns 'auto') → effectiveLocale 'it'.
- BELO-6: opts.locale='it' + override invalid 'xx' → effectiveLocale 'it' (load returns 'auto').
- BELO-7: override='fr' → downstream StatusHudRenderer constructed with locale 'fr' (spy on construction).
- BELO-8: override='pt-br' → effective locale 'pt-br' + per-key fallback path verified via getLabel call.

NO change to LOCALE_MENU (owned by 05-01). NO change to map-mode-toggle.ts (owned by 04b-02).

Commit message: feat(05-06): locale-override.ts + boot step 9c read-back + BootEngineLocale widen (I18N-02)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test locale-override boot-engine-locale-override</automated>
  </verify>
  <done>
loadLocaleOverride round-trips through Even Hub kv; persistLocaleOverride writes the right key+value; bootEngine step 9c overrides `opts.locale` when stored value !== 'auto'; BootEngineLocale widened; all 10+ LO-* + 8 BELO-* tests green; typecheck + lint:ci exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 05-panel-integration-smoke.test.ts (PSM-01..PSM-12)</name>
  <files>packages/g2-app/src/__tests__/05-panel-integration-smoke.test.ts</files>
  <action>
Create the integration smoke modeled on `04b-integration-smoke.test.ts` (Phase 4b ISM-01..10) per CONTEXT.md §canonical_refs.

Harness:
- Real `LayerManager` (constructed with a mock EvenAppBridge — vi.fn spies for textContainerUpgrade / rebuildPageContainer / updateImageRawData / setLocalStorage / getLocalStorage).
- Real `PanelRouter` instance — call `await router.discoverPanels()` and assert the registry contains the 5 Phase 5 panels (character-sheet, inventory, spellbook, combat-tracker, log).
- Real `StatusHudLayer` + `ToastQueueLayer` mounted at z=1 / z=1.5 (consume Phase 4a/4b existing layers).
- Real `PanelGestureBus` shared across all panels.
- Synthetic snapshots — define `mkCharacterSnapshot()` + `mkCombatSnapshot()` + `mkLogSnapshot()` helpers for each panel's data shape.

Tests (PSM-* discriminator markers, 12 tests):
- PSM-01: PanelRouter.discoverPanels() registers 5 panels — character-sheet, inventory, spellbook, combat-tracker, log. ConcentrationDropModalPanel is mounted by the dispatcher (Phase 4b), NOT discoverable via the same glob — verify ConcDropModal is NOT in the registry (or IS — depending on whether its filename matches *-panel.ts; verify and document. Note: it is `concentration-drop-modal.ts` — does NOT match `*-panel.ts` pattern. So it is correctly excluded from auto-discovery. Document this in the test).
- PSM-02: PanelRouter.openPanel('character-sheet', deps) mounts the panel at z=2; LayerManager.getLayer(Z2_OVERLAY) returns the panel instance; differential demolish removes z=0.5 from the page.
- PSM-03: openPanel('combat-tracker') after a CharacterSheet was open → closeActivePanel(CharSheet) happens first, then mount(CombatTracker); single z=2 occupant.
- PSM-04: closeActivePanel destroys z=2, restores z=0.5 (idle infill from Phase 4b Plan 01 differential demolish Rule 1).
- PSM-05: panel-gesture-bus cleanup — after closeActivePanel, `bus.size() === 0` (no leaked subscribers from the closed panel — T-4b-01-03 mitigation).
- PSM-06: PanelRouter.openPanel with missing capability → toast emitted via ToastQueueLayer (CONTEXT.md §Area 7 cap gate); panel NOT mounted.
- PSM-07: full-route auto-discovery integration — all 5 panel metas pass PanelMetaSchema.safeParse; navKeys are distinct (S/I/B/C/L).
- PSM-08: boot-engine integration — call bootEngine with deps DI; assert step 9c runs and effectiveLocale matches expected; assert the StatusHudRenderer is constructed with the locale.
- PSM-09: locale override persistence round-trip — `persistLocaleOverride(bridge, 'en')` followed by `loadLocaleOverride(bridge)` returns 'en'.
- PSM-10: per-key EN fallback end-to-end — render CharacterSheetPanel with locale 'es'; the panel's draw output contains EN strings (e.g., `getLabel('sheet.section.abilities', 'es')` returns `'ABILITIES'` — the EN string from 05-01).
- PSM-11: cross-panel snapshot delta — onSnapshot(newCharacterSnapshot) on a mounted CharacterSheetPanel re-renders without a LayerManager.bundle call (hot-swap pattern — RESEARCH §Pattern 3); spies on bridge.textContainerUpgrade vs bundle confirm the count.
- PSM-12: Phase 5 panel discovery boot-splash integration — boot-splash JSDoc says `panelsAvailable: 5` (currently a hardcoded constant in boot-engine-core.ts step 4); assert that bootEngine reports `panelsAvailable` matching the actual registry size. If currently hardcoded, update the boot-engine to pass the live size from PanelRouter.

Reference Phase 4b `04b-integration-smoke.test.ts` for the harness setup pattern; copy + adapt. Use the `EventEmitter`-backed mock WebSocket from Phase 4b for any WS subscription assertions (PSM-11 in particular).

If PanelRouter does not currently expose a `discoverPanels()` API hook for unit tests (e.g., the glob is hard-coded to `../panels/**/*-panel.ts` and cannot be re-mapped in tests), document the gap in the SUMMARY + provide a test-only `vi.doMock` glob mock so the registry populates from the test environment. RESEARCH §Pattern 1 anticipated this — the `vi.doMock` approach is the canonical solution.

NO change to existing 04b-integration-smoke.test.ts (Phase 4b owns it). NO modification to any panel file — this task is test-only.

Commit message: test(05-06): 05-panel-integration-smoke PSM-01..12 (Phase 5 layer composition + locale override end-to-end)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test 05-panel-integration-smoke</automated>
  </verify>
  <done>
05-panel-integration-smoke.test.ts ships with 12 PSM-* tests; PanelRouter discovers all 5 panels; mount/unmount round-trip with differential demolish + bus cleanup verified end-to-end; boot-engine locale override verified through the real boot path; per-key EN fallback proven on a real panel render; all tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: 8 INV-1 fixtures — 3 stress (ES/FR/PT-BR) + 2 DE + 2 EN + 1 ES auxiliary</name>
  <files>packages/shared-render/src/fixtures/locale-override.stress-es.it.txt, packages/shared-render/src/fixtures/locale-override.stress-fr.it.txt, packages/shared-render/src/fixtures/locale-override.stress-pt-br.it.txt, packages/shared-render/src/fixtures/sheet.main.2014.de.txt, packages/shared-render/src/fixtures/combat-tracker.full-window.de.txt, packages/shared-render/src/fixtures/sheet.main.2014.en.txt, packages/shared-render/src/fixtures/sheet.skills.en.txt, packages/shared-render/src/fixtures/inventory.2014.es.txt</files>
  <action>
Author 8 INV-1 fixtures per UI-SPEC §10 INV-1 Fixture Inventory final rows:

1. `locale-override.stress-es.it.txt` — InventoryPanel rendered with locale='es'. Since 05-01 widened HudLocale to include 'es' and getLabel returns the EN string for best-effort locales, this fixture SHOULD be byte-identical to the InventoryPanel rendered with locale='en' (proves per-key EN fallback works end-to-end). Author by hand: copy `inventory.2014.it.txt` from 05-04 and replace every IT string with its EN equivalent per the i18n-budgets.ts table.

2. `locale-override.stress-fr.it.txt` — same content as #1 (FR also falls back to EN per-key — identical to EN baseline).

3. `locale-override.stress-pt-br.it.txt` — same content as #1 (PT-BR same fallback).

4. `sheet.main.2014.de.txt` — CharacterSheetPanel Main tab rendered with locale='de'. Use the DE strings from i18n-budgets.ts: `ATTRIBUTE` (sheet.section.abilities), `RETTUNGSWÜRFE` (sheet.section.saves), `STR/GES/KON/INT/WEI/CHA` (sheet.ability.*), `Trefferwürfel` (sheet.vitals.hit_dice), etc.

5. `combat-tracker.full-window.de.txt` — CombatTrackerPanel full-window with DE strings: `KAMPF-TRACKER` (combat.tracker.panel_title), `Schnell:` (combat.tracker.quick_label), `Aktive Effekte:` (combat.tracker.effects_section), `TP` (HP label), `RK` (AC label).

6. `sheet.main.2014.en.txt` — CharacterSheetPanel Main with EN: `ABILITIES`, `SAVING THROWS`, `STR/DEX/CON/INT/WIS/CHA`, `Hit Dice`, etc.

7. `sheet.skills.en.txt` — CharacterSheetPanel Skills with EN strings: `◉ proficient · ★ expertise · ○ untrained`, skill names from dnd5e EN catalog.

8. `inventory.2014.es.txt` — Standalone InventoryPanel with locale='es' (per-key fallback to EN — same as stress-es but as the standalone panel render, NOT the sheet-tab variant).

Each fixture: 19 rows × exactly 70 code-points. Use the THORIN character (same as prior plans).

Wire 8 INV-1 round-trip tests in the existing `05-panel-integration-smoke.test.ts` Task 2 file using `matchAsciiFixture` (extend the existing test suite with PSM-FIX-* markers):
- PSM-FIX-STRESS-ES: InventoryPanel rendered with locale 'es' matches `locale-override.stress-es.it.txt`.
- PSM-FIX-STRESS-FR / PT-BR: same for fr / pt-br.
- PSM-FIX-DE-MAIN: CharacterSheetPanel Main with locale 'de' matches `sheet.main.2014.de.txt`.
- PSM-FIX-DE-COMBAT: CombatTrackerPanel with locale 'de' matches `combat-tracker.full-window.de.txt`.
- PSM-FIX-EN-MAIN: CharacterSheetPanel Main with locale 'en' matches `sheet.main.2014.en.txt`.
- PSM-FIX-EN-SKILLS: CharacterSheetPanel Skills with locale 'en' matches `sheet.skills.en.txt`.
- PSM-FIX-ES-INV: standalone InventoryPanel with locale 'es' matches `inventory.2014.es.txt`.

If during execution any DE string exceeds its `max` budget at runtime, the renderer truncates with `…` (assertWithinBudget warns in dev). The fixture should reflect the TRUNCATED output (not the un-truncated DE string).

NO new i18n keys (owned by 05-01). NO new panel rendering — fixtures consume the existing 05-02..05-05 renderers.

Commit message: feat(05-06): 8 INV-1 stress + canonical fixtures (I18N-05 best-effort + DE/EN coverage)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test 05-panel-integration-smoke</automated>
  </verify>
  <done>
8 fixtures committed; matchAsciiFixture round-trip green; ES/FR/PT-BR stress fixtures prove per-key EN fallback end-to-end; DE canonical fixtures prove DE width budget enforcement on real panel render; EN canonical fixtures provide baseline; INV-1 ck 14 (i18n stress) verified across all canonical + best-effort locales in software.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| view.locale.override stored value → bootEngine | Untrusted external storage; defensive normalise via LOCALE_MENU code-set validation |
| game.i18n.lang → effectiveLocale | Trusted (Foundry-internal); auto-detected before override read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | view.locale.override value outside LOCALE_MENU codes (e.g., 'xx', 'jp') | mitigate | loadLocaleOverride normalises unknown values to 'auto' (defensive) — never throws (RESEARCH §Security Domain Pattern 2 verbatim) |
| T-05-06-02 | Tampering | Bridge polyfill regression: getLocalStorage returns malformed result | mitigate | try/catch wrap; falls through to 'auto' default |
| T-05-06-03 | DoS | Boot step 9c blocks if Even Hub kv is unresponsive | accept | Even Hub kv calls are awaited; the existing Phase 4b map-mode override has the same property; accept (single-tenant, local kv) |
</threat_model>

<verification>
- pnpm typecheck exits 0
- pnpm lint:ci exits 0
- pnpm test workspace-wide passes; ≥30 new tests (LO + BELO + PSM + PSM-FIX families)
- All 8 fixture files exist; all matchAsciiFixture round-trip green
- 5-panel integration smoke composes all Phase 5 panels via real PanelRouter + LayerManager
- Locale override end-to-end: bridge.setLocalStorage + bootEngine read-back + downstream StatusHudRenderer locale propagation verified
- BootEngineLocale widened to match HudLocale (typecheck-enforced)
</verification>

<success_criteria>
- Phase 5 closure: all 8 phase_req_ids closed (SHEET-01/02/03/04 + COMB-01/03 + I18N-02/05) — software-side
- Hardware-pending SC carry-forward (SC-05-01..SC-05-05) declared in 05-VALIDATION.md acknowledged in this plan's SUMMARY for ADR-0005 Branch A `human_needed` gate per Phase 4a/4b precedent
- 5 panels auto-discovered + composable via PanelRouter under real layer-manager semantics
- I18N-02 device-local override never writes Foundry world settings (verified by audit grep — no `game.settings.set` calls outside the foundry-module package)
- I18N-05 per-key EN fallback proven end-to-end on real panel renders via 3 stress fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-06-SUMMARY.md` documenting: 3 task commits, locale-override module + boot step 9c integration, 05-panel-integration-smoke harness composition, 8 INV-1 fixture additions, deviations (esp. PanelRouter glob mock pattern at test boundary), hardware-pending SC carry-forward (SC-05-01..05) for ADR-0005 Branch A, Phase 5 global closure signal (all 8 REQ-IDs software-side complete), Self-Check table.
</output>
