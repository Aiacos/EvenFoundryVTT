---
phase: 05
plan: 01
type: execute
wave: 0
subsystem: g2-app
tags: [g2-app, engine, panel-router, panel-meta, adr, wave-0, i18n-budgets, atomic-fan-out, foundation, schema-extension, dual-edition, concentration]
depends_on: []
files_modified:
  - packages/g2-app/src/engine/panel-router.ts
  - packages/g2-app/src/engine/__tests__/panel-router.test.ts
  - packages/g2-app/src/locale/locale-menu.ts
  - packages/g2-app/src/locale/__tests__/locale-menu.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - packages/g2-app/src/status-hud/__tests__/i18n-budgets.test.ts
  - packages/shared-protocol/src/payloads/character.ts
  - packages/shared-protocol/src/payloads/combat.ts
  - packages/shared-protocol/src/__tests__/payloads/character.test.ts
  - packages/shared-protocol/src/__tests__/payloads/combat.test.ts
  - packages/foundry-module/src/readers/character-reader.ts
  - packages/foundry-module/src/readers/combat-reader.ts
  - packages/foundry-module/src/readers/__tests__/character-reader.test.ts
  - packages/foundry-module/src/readers/__tests__/combat-reader.test.ts
  - docs/architecture/0010-panel-plugin-registry.md
  - docs/architecture/README.md
autonomous: true
requirements: [SHEET-02, SHEET-03, COMB-01]
gap_closure: false

requires:
  - phase: 04b-overlay-slot-map-mode-toggle-adversarial-ui
    provides: OverlayPanel interface + R1Gesture union + PanelGestureBus + LayerManager.bundle differential demolish + isOverlayPanel guard + HUD_WIDTH_BUDGETS (36 keys) + Strategy A container pattern + ConcentrationDropModalPanel exemplar
provides:
  - PanelRouter class — discoverPanels via import.meta.glob + openPanel(id, deps) + closeActivePanel() + isPanelOpen(id) + registry size accessor
  - PanelMeta interface + PanelMetaSchema (Zod) + PanelConstructor type + PanelDeps interface
  - LOCALE_MENU constant — 7-entry locale list (auto + 3 canonical + 3 best-effort) with budget tier annotation
  - LocaleMenuEntry type — `as const satisfies` brand
  - HudLocale widened to 'it'|'en'|'de'|'es'|'fr'|'pt-br' + getLabel best-effort EN per-key fallback
  - HUD_WIDTH_BUDGETS extended with ~82 Phase 5 keys (Sheet Main+Skills+Inv+Spells+Feats+Bio + Combat tracker + Log + Inventory + Spellbook + empty states + panel titles + footer hints + router boot-error)
  - CharacterSnapshotSchema.world.modernRules boolean — atomic with character-reader extension
  - CombatantSchema.concentration optional { spellName; duration } — atomic with combat-reader extension
  - ADR-0010 "Panel Plugin Registry" — ACCEPTED — discovery, metadata contract, capability gating
affects:
  - 05-02-PLAN (Char-sheet skeleton)
  - 05-03-PLAN (Char-sheet tab renderers)
  - 05-04-PLAN (Inventory + Spellbook panels)
  - 05-05-PLAN (Combat tracker + Log panels)
  - 05-06-PLAN (Locale override + integration smoke)

tech-stack:
  added: []
  patterns:
    - "Wave-0 centralisation of fan-out — single atomic plan ships PanelRouter scaffolding + ~82 i18n keys + dual schema extensions + dual reader extensions + ADR-0010 so downstream plans are READ-ONLY consumers (no file-overlap conflicts across waves 1-3). Same playbook as Phase 4b Plan 01."
    - "Build-time schema gate via `as const satisfies Record<string, WidthBudgetRow>` — extending HUD_WIDTH_BUDGETS passes `pnpm typecheck` only if every IT/EN/DE row satisfies the WidthBudgetRow shape."
    - "Locale-set widening with per-key EN fallback — HudLocale extended to 6+1 codes; `getLabel(field, locale)` returns row.it/en/de for canonical and row.en for best-effort (es/fr/pt-br) — I18N-05 fallback rule."
    - "Atomic schema+reader+test fan-out — CharacterSnapshotSchema.world + CombatantSchema.concentration extensions land in same plan as character-reader + combat-reader Foundry-side mappings (Phase 4b Plan 06 atomic pattern)."
    - "import.meta.glob lazy-import discovery — Vite 8 build-time static module map + PanelMetaSchema.safeParse gate at discovery time (silent exclusion + console.warn)."
    - "ADR-0010 dedicated (not amendment) — panel plugin registry warrants its own ADR distinct from ADR-0009 per CONTEXT.md §Deferred research disposition."

must_haves:
  truths:
    - "PanelRouter.discoverPanels() returns a non-empty Map when at least 1 *-panel.ts exists in packages/g2-app/src/panels/"
    - "Malformed panel meta is silently excluded with a console.warn — boot proceeds with surviving panels"
    - "CharacterSnapshotSchema.parse rejects payloads without world.modernRules (atomic with reader extension)"
    - "CombatantSchema accepts payloads with optional concentration:{spellName,duration} and rejects malformed concentration objects"
    - "getLabel(field, 'es'|'fr'|'pt-br') returns the EN string for that key (per-key fallback) without throwing"
    - "HUD_WIDTH_BUDGETS extension passes `pnpm typecheck` — every new key satisfies WidthBudgetRow shape"
    - "ADR-0010 ACCEPTED with status header + Context/Decision/Consequences sections + INV-2 evidence citations"
  artifacts:
    - path: packages/g2-app/src/engine/panel-router.ts
      provides: PanelRouter class + PanelMeta + PanelMetaSchema + PanelDeps types
      exports: ["PanelRouter", "PanelMeta", "PanelMetaSchema", "PanelConstructor", "PanelDeps"]
      min_lines: 100
    - path: packages/g2-app/src/locale/locale-menu.ts
      provides: LOCALE_MENU constant + LocaleMenuEntry type
      exports: ["LOCALE_MENU", "LocaleMenuEntry"]
      min_lines: 20
    - path: packages/g2-app/src/status-hud/i18n-budgets.ts
      provides: HUD_WIDTH_BUDGETS Phase 5 extension + widened HudLocale + per-key EN fallback in getLabel
      contains: "sheet.ability.str|combat.tracker.panel_title|log.panel_title|inv.panel_title|spell.panel_title|pt-br"
    - path: packages/shared-protocol/src/payloads/character.ts
      provides: CharacterSnapshotSchema.world.modernRules
      contains: "modernRules"
    - path: packages/shared-protocol/src/payloads/combat.ts
      provides: CombatantSchema.concentration optional sub-schema
      contains: "concentration"
    - path: packages/foundry-module/src/readers/character-reader.ts
      provides: world.modernRules mapping from `game.settings.get('dnd5e','rulesVersion')`
      contains: "rulesVersion"
    - path: packages/foundry-module/src/readers/combat-reader.ts
      provides: concentration effect sourcing from actor.effects with flags.dnd5e.concentrating
      contains: "concentrating"
    - path: docs/architecture/0010-panel-plugin-registry.md
      provides: ADR-0010 ACCEPTED
      contains: "status: accepted"
      min_lines: 80
    - path: docs/architecture/README.md
      provides: ADR-0010 row in index
      contains: "ADR-0010"
  key_links:
    - from: packages/g2-app/src/engine/panel-router.ts
      to: import.meta.glob('../panels/**/*-panel.ts', { eager: false })
      via: Vite build-time static analysis
      pattern: "import\\.meta\\.glob"
    - from: packages/g2-app/src/engine/panel-router.ts
      to: PanelMetaSchema.safeParse(Cls.meta)
      via: Zod runtime gate at discoverPanels
      pattern: "PanelMetaSchema\\.safeParse"
    - from: packages/foundry-module/src/readers/character-reader.ts
      to: CharacterSnapshotSchema.world.modernRules
      via: game.settings.get('dnd5e','rulesVersion') === 'modern'
      pattern: "rulesVersion.*modern"
    - from: packages/g2-app/src/status-hud/i18n-budgets.ts
      to: getLabel best-effort branch
      via: HudLocale widened + EN fallback for es/fr/pt-br
      pattern: "pt-br"

metrics:
  duration: TBD
  completed: TBD
  tasks: 3
  task_commits: TBD
  files_created: 7
  files_modified: 9
  tests_added: TBD
---

<objective>
**Wave 0 atomic foundation** for Phase 5 Panel Plugin System. Land — across **3 atomic commits inside one plan** — the PanelRouter + PanelMeta scaffolding, the ~82-key HUD_WIDTH_BUDGETS extension, the CharacterSnapshotSchema.world.modernRules + CombatantSchema.concentration extensions with their atomic foundry-module reader counterparts, the widened HudLocale with per-key EN fallback, the LOCALE_MENU constant, and ADR-0010 ACCEPTED. After this plan lands, plans 05-02..05-06 become **READ-ONLY consumers** of these contracts — no file-overlap conflicts across waves 1-3 (Phase 4b Plan 01 playbook).

Purpose: same architectural rationale as Phase 4b Plan 01 — centralise the fan-out so downstream waves can run in parallel without touching shared scaffolding. This is the "blueprints first" task for the next 5 plans.

Output:
- Engine module: `panel-router.ts` (class + Zod schema + types)
- Locale module: `locale-menu.ts` (constant + type)
- i18n extension: ~82 new HUD_WIDTH_BUDGETS keys + widened HudLocale + per-key fallback in getLabel
- Schema extensions: world.modernRules + concentration sub-fields (Zod strictObject — Phase 4b atomic pattern)
- Reader extensions: dnd5e modernRules mapping + concentration effect sourcing
- ADR-0010 ACCEPTED + README index updated
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-VALIDATION.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-01-SUMMARY.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-05-SUMMARY.md
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/g2-app/src/engine/panel-gesture-bus.ts
@packages/g2-app/src/engine/layer-manager.ts
@packages/g2-app/src/engine/capability-handshake.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/hub-polyfill.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/shared-protocol/src/payloads/character.ts
@packages/shared-protocol/src/payloads/combat.ts
@packages/foundry-module/src/readers/character-reader.ts
@packages/foundry-module/src/readers/combat-reader.ts
@docs/architecture/0009-layer-manager-contract.md
@docs/architecture/README.md

<interfaces>
Key contracts the executor consumes verbatim — extracted from codebase. Do NOT explore.

From `packages/g2-app/src/engine/layer-types.ts`:
- `ZIndex.Z2_OVERLAY = 2` (numeric enum value)
- `interface Layer { readonly id: string; draw(): Promise<void>; destroy(): void; getCaptureContainer?(): string; getContainerCount?(): { image: number; text: number }; }`
- `interface OverlayPanel extends Layer { onMount(): Promise<void>; onUnmount(): Promise<void>; onEvent(g: R1Gesture): void; }`
- `type R1Gesture = { kind:'tap' } | { kind:'scroll'; direction:'up'|'down' } | { kind:'long-press' } | { kind:'double-tap' }`
- `type LayerOp = { type:'mount'; z:ZIndex; layer:Layer; requiredCaps?:ReadonlyArray<ServerCap> } | { type:'destroy'; z:ZIndex }`

From `packages/g2-app/src/engine/layer-manager.ts`:
- `class LayerManager { constructor(bridge); bundle(ops: LayerOp[]): Promise<void>; getLayer(z: ZIndex): Layer | null; setNegotiatedCaps(caps: ReadonlySet<ServerCap>); }`

From `packages/g2-app/src/engine/panel-gesture-bus.ts`:
- `class PanelGestureBus { publish(g: R1Gesture): void; subscribe(fn: (g: R1Gesture) => void): () => void; size(): number; }`

From `packages/g2-app/src/status-hud/i18n-budgets.ts`:
- `interface WidthBudgetRow { readonly it: string; readonly en: string; readonly de: string; readonly max: number; }`
- `const HUD_WIDTH_BUDGETS = { ... } as const satisfies Record<string, WidthBudgetRow>;` (currently 36 keys — Phase 4a 9 + Phase 4b 27)
- `type HudLocale = 'it' | 'en' | 'de';` (this plan widens it)
- `function getLabel(field: HudBudgetField, locale: HudLocale): string;` (this plan extends with per-key EN fallback)

From `packages/shared-protocol/src/payloads/character.ts`:
- `CharacterSnapshotSchema = z.strictObject({ actorId, name, hp, maxHp, tempHp, ac, level, conditions, exhaustion, death })` — this plan extends with `world: z.object({ modernRules: z.boolean() })`

From `packages/shared-protocol/src/payloads/combat.ts`:
- `CombatantSchema = z.strictObject({ id, name, actorId, initiative, hp, maxHp, isCurrentTurn })` — this plan extends with optional `concentration: z.object({ spellName, duration }).optional()`

From `packages/g2-app/src/panels/concentration-drop-modal.ts`:
- Exemplar pattern for OverlayPanel implementation (constructor signature, onMount/onUnmount/onEvent lifecycle, single 'overlay-block' container, getContainerCount returns `{ image: 0, text: 1 }`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PanelRouter + PanelMeta schema + LOCALE_MENU + ADR-0010 (atomic engine + locale + ADR commit)</name>
  <files>packages/g2-app/src/engine/panel-router.ts, packages/g2-app/src/engine/__tests__/panel-router.test.ts, packages/g2-app/src/locale/locale-menu.ts, packages/g2-app/src/locale/__tests__/locale-menu.test.ts, docs/architecture/0010-panel-plugin-registry.md, docs/architecture/README.md</files>
  <action>
Create `packages/g2-app/src/engine/panel-router.ts` exporting (per RESEARCH §Pattern 1 verbatim):

- `PanelMetaSchema` — Zod schema with fields `id` (`z.string().min(1)`), `title` (`z.object({ it: z.string(), en: z.string(), de: z.string().optional() })`), `navKey` (`z.string().length(1)`), `requiredCaps` (`z.array(z.string()).optional()`), `defaultTab` (`z.string().optional()`).
- `type PanelMeta = z.infer<typeof PanelMetaSchema>`.
- `type PanelConstructor = { new(...args: unknown[]): OverlayPanel; meta?: unknown }` — type-cast at the glob boundary (RESEARCH Pitfall 1 mitigation — `static meta` is on constructor, not instance).
- `interface PanelDeps { bridge: EvenAppBridge; gestureBus: PanelGestureBus; locale: HudLocale; layerManager: LayerManager; negotiatedCaps: ReadonlySet<string>; toastQueue?: ToastQueueLayer }`.
- `class PanelRouter` with private fields `registry = new Map<string, { meta: PanelMeta; Cls: PanelConstructor }>()`, `activePanel: OverlayPanel | null = null`, `activeId: string | null = null`.
- Method `async discoverPanels(): Promise<void>` — iterate `Object.entries(import.meta.glob<{ default: PanelConstructor }>('../panels/**/*-panel.ts', { eager: false }))`. For each entry: try { await loader(); `PanelMetaSchema.safeParse((Cls as { meta?: unknown }).meta)`; on success register, on failure `console.warn` + continue } catch (err) { `console.warn` + continue } — never propagate.
- Method `async openPanel(id: string, deps: PanelDeps): Promise<void>` — registry lookup; capability gate against `deps.negotiatedCaps` (missing cap → toast via `panel_cap_denied_template` key from Task 2 + return); single-active invariant (close active first); construct via `new entry.Cls(deps.bridge, deps.gestureBus, deps.locale)` — same shape as ConcentrationDropModalPanel exemplar; `await deps.layerManager.bundle([{ type:'mount', z: ZIndex.Z2_OVERLAY, layer: panel }])`; store activePanel + activeId. Per CONTEXT.md §Area 1: panels never touch LayerManager directly — router owns ALL z=2 bundle calls.
- Method `async closeActivePanel(): Promise<void>` — if `activePanel === null` no-op; else `await this.layerManager.bundle([{ type:'destroy', z: ZIndex.Z2_OVERLAY }])` (LayerManager itself invokes panel.onUnmount per ADR-0009 Amd 1 Rule 1); null out activePanel + activeId. Note: this method needs access to a LayerManager — pass it through PanelDeps cached on construction, or accept it as an argument; choose the construction-cached pattern for consistency with the exemplar router design. Document the choice inline.
- Methods `isPanelOpen(id: string): boolean` and `getRegistrySize(): number` (test-only diagnostics).
- TODO comment at top citing ADR-0010 as the design rationale.

Create `packages/g2-app/src/locale/locale-menu.ts` exporting `LOCALE_MENU` per RESEARCH §Pattern 5 + UI-SPEC §8.5 verbatim — 7 entries (`auto`, `it`, `en`, `de` canonical + `es`, `fr`, `pt-br` best-effort) as `const satisfies ReadonlyArray<LocaleMenuEntry>` where `LocaleMenuEntry = { readonly code: 'auto'|'it'|'en'|'de'|'es'|'fr'|'pt-br'; readonly nativeLabel: string; readonly budget: 'canonical'|'best-effort' }`. Export both `LOCALE_MENU` and `LocaleMenuEntry`.

Author `docs/architecture/0010-panel-plugin-registry.md` as MADR 4.0 (same shape as ADR-0009 — read it for the section list): frontmatter `status: accepted`, `date: 2026-05-15`, `deciders: planner`, `consulted: planner-checker`, `informed: executor`; sections — Context (why a registry distinct from LayerManager: auto-discovery, capability gating at router level, LOCALE_MENU tier annotation), Decision Drivers (zero-boilerplate registration; type-safe meta; capability gate at mount entry per CONTEXT.md §Area 1+7), Considered Options (Option A: runtime filesystem scan REJECTED — no Node.js runtime in WebView; Option B: parallel-file registry REJECTED — drift risk; Option C: `import.meta.glob` CHOSEN — Vite build-time + tree-shake), Decision Outcome (PanelRouter class + PanelMeta + PanelMetaSchema + LOCALE_MENU + per-key EN fallback for best-effort locales; differential demolish unchanged from ADR-0009 Amd 1 Rule 1; z=1.5 toast carve-out Rule 2 + in-process bus Rule 3 also unchanged), Pros (panels are pluggable; INV-1 width-budget remains sacred via per-key fallback; LayerManager surface stays minimal), Cons (panels must compile at build time — no runtime hot-load, accepted because MVP is single-binary), INV-2 evidence (Vite 8 docs URL `vite.dev/guide/features#glob-import`; `@evenrealities/even_hub_sdk@0.0.10` cited for the envelope API consumed by panels; CONTEXT.md §Area 1+4+7 references), cross-refs (`@see ADR-0001 Amendment 1`, `@see ADR-0009 Amendment 1`). End with `## Confirmation` listing consumer plans 05-02..05-06. Reserve `## Amendment 1` as placeholder (mirror ADR-0009 §Amendment 1 style).

Update `docs/architecture/README.md` ADR index table: insert one new row at the natural numerical position — `[ADR-0010](./0010-panel-plugin-registry.md) | Panel Plugin Registry — discovery, metadata contract, capability gating | accepted | Phase 5 entry`. Also update the "Numbering" note text mentioning ADR-0010 lands Phase 5 entry; ADR-0007 still reserved for RTL.

Author 8-12 unit tests in `panel-router.test.ts` (PRT-* discriminator markers): PRT-DISC-01 (discoverPanels populates registry from one valid mock — use `vi.doMock` on the glob path; reference Phase 4b panel-gesture-bus.test.ts for mock-doMock idiom); PRT-DISC-02 (malformed meta → console.warn + skip + survivors registered); PRT-DISC-03 (loader throw → console.warn + skip); PRT-DISC-04 (registry size accessor); PRT-OPEN-01 (happy path: constructs panel + bundle mount op + activePanel set); PRT-OPEN-02 (missing cap → toastQueue.push + no mount); PRT-OPEN-03 (activePanel !== null → closes first then mounts new); PRT-OPEN-04 (unknown id → console.warn + no bundle); PRT-CLOSE-01 (closeActivePanel destroys z=2 + nulls activePanel); PRT-CLOSE-02 (no active → no-op); PRT-IS-OPEN (isPanelOpen matches id).

Author 5 unit tests in `locale-menu.test.ts` (LM-* markers): LM-1 (exactly 7 entries); LM-2 (codes unique + match locked set); LM-3 (budget tier matches CONTEXT.md §Area 4 — auto+it+en+de canonical; es+fr+pt-br best-effort); LM-4 (`as const satisfies` compile-time brand holds — assert `typeof LOCALE_MENU[0].code` narrows to the literal); LM-5 (all nativeLabels non-empty strings).

Constraints: NO direct LayerManager mutation from panels (anti-pattern). NO React/Vue/Svelte (D-2.04). NO browser localStorage (CLAUDE.md §What NOT to Use). NO inline implementation code beyond the structures declared above — the executor writes the bodies.

Commit message (single atomic commit ending the task): `feat(05-01): PanelRouter + LOCALE_MENU + ADR-0010 (Wave 0 panel registry foundation)`.

References: CLAUDE.md INV-2 + INV-4; CONTEXT.md §Area 1 + 4 + 7; RESEARCH.md §Pattern 1 + §Pattern 5; UI-SPEC.md §8.5.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test panel-router locale-menu && pnpm typecheck && pnpm lint:ci && test -f docs/architecture/0010-panel-plugin-registry.md && grep -q "status: accepted" docs/architecture/0010-panel-plugin-registry.md && grep -q "ADR-0010" docs/architecture/README.md && grep -c "import.meta.glob" packages/g2-app/src/engine/panel-router.ts</automated>
  </verify>
  <done>
PanelRouter discovers panels via import.meta.glob, malformed meta silently excluded with console.warn, openPanel honors capability gate + single-active invariant, LOCALE_MENU exports 7 entries with budget tier, ADR-0010 ACCEPTED with status header + Context/Decision/Consequences/Confirmation sections + INV-2 citations, README index includes ADR-0010 row. All 12+ PRT tests + 5 LM tests green. `pnpm typecheck` and `pnpm lint:ci` exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: HUD_WIDTH_BUDGETS extension ~82 keys + widened HudLocale + per-key EN fallback (atomic i18n commit)</name>
  <files>packages/g2-app/src/status-hud/i18n-budgets.ts, packages/g2-app/src/status-hud/__tests__/i18n-budgets.test.ts</files>
  <action>
Extend `HUD_WIDTH_BUDGETS` in `i18n-budgets.ts` with the Phase 5 key set verbatim from UI-SPEC.md tables. Add a fenced comment block `// ─── Phase 5 — Panel keys (Plan 05-01 Wave-0 centralisation) ─────────` at the insertion point (after the existing Phase 4b 27-key region per Phase 4b Plan 01 pattern).

Key groups (IT/EN/DE + `max` values copied verbatim from UI-SPEC tables — do NOT invent translations):

1. Sheet Main tab (UI-SPEC §5.2 — 15 keys): `sheet.ability.{str,dex,con,int,wis,cha}`, `sheet.section.{abilities,saves}`, `sheet.vitals.{hp,ac,init,speed,prof,hit_dice,senses}`.
2. Sheet Skills tab (UI-SPEC §5.3 — 2 keys): `sheet.skill.{prof_legend,scroll_hint}`.
3. Sheet Inventory tab (UI-SPEC §5.4 — 7 keys): `sheet.inv.{currency,carried,equipped,consumables,equipment,container,mastery_flag}`.
4. Sheet Spells tab (UI-SPEC §5.5 — 6 keys): `sheet.spell.{header_title,prepared_label,filter_bar,cantrips_section,level_section,scroll_hint}`.
5. Sheet Feats tab (UI-SPEC §5.6 — 6 keys): `sheet.feat.{class_section,race_section,background_section,feats_section,origin_flag,scroll_hint}`.
6. Sheet Bio tab (UI-SPEC §5.7 — 6 keys): `sheet.bio.{personality,ideal,bond,flaw,backstory,scroll_hint}`.
7. Combat tracker (UI-SPEC §5.8 — 11 keys): `combat.tracker.{panel_title,effects_section,you_marker,party_label,quick_label,quick_attack,quick_spell,quick_item,quick_move}`, `combat.{hp_label,ac_label}`.
8. Log panel (UI-SPEC §5.9 — 13 keys): `log.{panel_title,filter.all,filter.rolls,filter.damage,filter.status,filter.chat,result.hit,result.miss,result.pass,result.fail,concentrating,round_marker,scroll_hint}`.
9. Inventory panel standalone (UI-SPEC §5.10 — 5 keys): `inv.{panel_title,section.equipped,section.consumables,section.carried,scroll_hint}`.
10. Spellbook panel standalone (UI-SPEC §5.11 — 11 keys): `spell.{panel_title,prepared_count,cantrips_section,level_section,available_marker,activation.action,activation.reaction,activation.bonus,activation.ritual,cursor_marker,scroll_hint}`.
11. Empty states (UI-SPEC §8.1 — 4 keys): `combat.empty`, `log.empty`, `inv.empty`, `spell.empty`.
12. Panel titles for header breadcrumb (UI-SPEC §8.2 — 5 keys): `panel.title.{sheet,combat,log,inventory,spellbook}`.
13. Footer hints (UI-SPEC §8.3 — 5 keys): `footer.hint.{sheet,combat,log,inventory,spellbook}`.
14. PanelRouter boot-error states (UI-SPEC §8.4 — 2 keys): `panel_router_zero_panels`, `panel_cap_denied_template` (the latter has `{panel}` + `{cap}` placeholders; `_template` suffix exempts it from IB-3 literal-length check per Phase 4b Plan 01 Deviation #2).

Target final count: ~98 (Phase 4a 9 + Phase 4b 27 + Phase 5 ~82 — exact count verified at commit when the UI-SPEC tables are enumerated exhaustively). Where a DE string exceeds its `max` budget, add inline JSDoc `// DE overflows budget — assertWithinBudget warns at runtime` and accept (UI-SPEC §4.4 per-key fallback rule). Otherwise prefer abbreviation only if the DE string is canonically established (e.g., `DE TP` for HP — already locked).

Widen HudLocale: change `export type HudLocale = 'it' | 'en' | 'de';` to `export type HudLocale = 'it' | 'en' | 'de' | 'es' | 'fr' | 'pt-br';`. Update the JSDoc to reference CONTEXT.md §Area 4 + I18N-05.

Extend getLabel with per-key EN fallback: replace the current `return HUD_WIDTH_BUDGETS[field][locale];` with explicit branches — `if (locale==='it') return row.it; if (locale==='en') return row.en; if (locale==='de') return row.de; return row.en;` (best-effort locales fall back to EN per key, not full-locale fallback). Update the JSDoc with reference to I18N-05 + UI-SPEC §4.4.

Test extensions (add to `i18n-budgets.test.ts` — discriminator markers IB-P5-*): IB-P5-COUNT (total Phase 5 keys added matches the exhaustive UI-SPEC enumeration); IB-P5-SHEET-MAIN (`getLabel('sheet.ability.str','it')` → `'FOR'`); IB-P5-SHEET-SKILLS (`getLabel('sheet.skill.prof_legend','it')` → the IT legend string from §5.3); IB-P5-INVENTORY (`getLabel('sheet.inv.mastery_flag','it')` → `'[M]'`); IB-P5-SPELL (`getLabel('spell.activation.action','it')` → `'azione'`); IB-P5-COMBAT (`getLabel('combat.tracker.panel_title','it')` → `'COMBAT TRACKER'`); IB-P5-LOG (`getLabel('log.result.hit','it')` → `'COLPITO'`); IB-P5-FOOTER (`getLabel('footer.hint.combat','it')` → IT combat hint from §8.3); IB-P5-EMPTY (`getLabel('combat.empty','it')` → `'Nessun combattimento attivo'`); IB-P5-FALLBACK-ES, IB-P5-FALLBACK-FR, IB-P5-FALLBACK-PT-BR (each asserts the EN string for a sentinel key); IB-P5-CANONICAL-DE (asserts DE returns the DE string, NOT fallback — proves the canonical/best-effort dispatch is correct); IB-P5-FALLBACK-DOES-NOT-AFFECT-IT-EN-DE (regression guard — existing Phase 4a/4b key returns unchanged).

The `_template` suffix carve-out from Phase 4b Plan 01 Deviation #2 applies to `panel_cap_denied_template` — IB-3 literal-length check skips it.

NEVER use browser `localStorage` for `view.locale.override` — that key lives in Even Hub kv via the polyfill (consumed by 05-06 boot read-back). NO inline implementation code beyond the structures + key list above.

Commit message: `feat(05-01): HUD_WIDTH_BUDGETS Phase 5 extension + widened HudLocale + per-key EN fallback (Wave 0)`.

References: I18N-02, I18N-05, UI-SPEC.md §5.2-§5.11 + §8.1-§8.4 + §9, RESEARCH.md §Pattern 5, CLAUDE.md INV-1 + INV-4.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test i18n-budgets && pnpm typecheck && grep -c "panel\.title\." packages/g2-app/src/status-hud/i18n-budgets.ts && grep -c "combat\.tracker\." packages/g2-app/src/status-hud/i18n-budgets.ts && grep -E "'pt-br'" packages/g2-app/src/status-hud/i18n-budgets.ts</automated>
  </verify>
  <done>
HUD_WIDTH_BUDGETS contains ~82 new Phase 5 keys (exact count verified at commit from UI-SPEC tables); `as const satisfies` typecheck passes; HudLocale widened to 6+1 codes; `getLabel(field,'es'|'fr'|'pt-br')` returns the EN string for each key; all IB-P5-* tests green plus the IB-P5-CANONICAL-DE regression guard.
  </done>
</task>

<task type="auto">
  <name>Task 3: CharacterSnapshotSchema.world.modernRules + CombatantSchema.concentration + reader extensions (atomic schema + reader commit)</name>
  <files>packages/shared-protocol/src/payloads/character.ts, packages/shared-protocol/src/payloads/combat.ts, packages/shared-protocol/src/__tests__/payloads/character.test.ts, packages/shared-protocol/src/__tests__/payloads/combat.test.ts, packages/foundry-module/src/readers/character-reader.ts, packages/foundry-module/src/readers/combat-reader.ts, packages/foundry-module/src/readers/__tests__/character-reader.test.ts, packages/foundry-module/src/readers/__tests__/combat-reader.test.ts</files>
  <action>
Atomic schema + reader fan-out (Phase 4b Plan 06 atomic pattern — schema extension and reader extension land in the same commit so `z.strictObject` does not reject mid-flight payloads).

**Schema work (`packages/shared-protocol/src/payloads/character.ts`):**
Extend `CharacterSnapshotSchema` with a new required field `world: z.object({ modernRules: z.boolean() })`. Per CONTEXT.md §Area 3, the field is REQUIRED (not `.optional()`) so the atomic commit closes the drift window (Phase 4b Plan 06 pitfall — `.optional()` allowed silent producer drift). Add an exported `WorldStateSchema = z.object({ modernRules: z.boolean() })` for forward-compat (Phase 7+ may add more world fields like `currentEdition: z.enum(['2014','2024'])` — keep this in `world` namespace).

**Schema work (`packages/shared-protocol/src/payloads/combat.ts`):**
Extend `CombatantSchema` with optional `concentration: z.object({ spellName: z.string(), duration: z.string() }).optional()` (UI-SPEC §5.8 sub-line shape — name truncated to 12 chars / duration to 6 chars at render time, NOT at schema). Concentration is optional because most combatants are not concentrating.

**Reader work (`packages/foundry-module/src/readers/character-reader.ts`):**
Extend `getCharacterSnapshot` to populate the new `world.modernRules` field. Per RESEARCH §Pattern 3 + §Open Question 1: read `game.settings.get('dnd5e', 'rulesVersion')` (cast to `string`); map `=== 'modern'` → `true`, anything else → `false`. Defensive nullish-coalesce default to `false` (PHB 2014 — same defaulting strategy as Phase 4b death-saves defensive default per character-reader.ts current pattern). Update the JSDoc to document the new field + cite the RESEARCH assumption A1 (`'modern'`/`'legacy'` values to be verified during execution; safe-cast lands here).

**Reader work (`packages/foundry-module/src/readers/combat-reader.ts`):**
Extend `getCombatSnapshot` to populate optional `concentration` for each combatant. Per RESEARCH §Pattern 4 + §Open Question 2: iterate `c.actor?.effects` collection; find effect with `flags?.dnd5e?.concentrating === true` (RESEARCH assumption A2 — verify exact flag path during execution); on match, populate `{ spellName: effect.name ?? '', duration: effect.duration?.label ?? '' }`; on miss, omit the field. Update JSDoc to document assumption + cite RESEARCH A2 + A3. Pattern of access: defensive optional-chain everywhere (`noUncheckedIndexedAccess` compliance). DO NOT cast `effect.flags` as `any` — narrow with a typed predicate or use existing `foundry-globals.d.ts` types and extend them if needed.

If the foundry-module test mocks need the `effects` collection shape, look at the existing `__tests__/character-reader.test.ts` death-saves mock pattern for the canonical extension shape (Phase 4b Plan 06 added `attributes.death` to the actor mock — same approach here for `effects` + `flags.dnd5e.concentrating`).

**Schema test work (`packages/shared-protocol/src/__tests__/payloads/character.test.ts`):**
Add tests CHAR-MR-1..5:
- CHAR-MR-1: payload with `world.modernRules: true` parses successfully.
- CHAR-MR-2: payload with `world.modernRules: false` parses successfully.
- CHAR-MR-3: payload WITHOUT `world` field FAILS to parse (proves field is required — atomic commit guard).
- CHAR-MR-4: payload with extra fields in `world` (e.g., `{ modernRules: true, foo: 'bar' }`) FAILS if `world` uses strictObject (verify whether the spec calls for strict or open shape — RESEARCH §Pattern 3 implies open `z.object`, so this case PASSES; document accordingly).
- CHAR-MR-5: payload with `world.modernRules: 'true'` (string) FAILS to parse (boolean type guard).

**Schema test work (`packages/shared-protocol/src/__tests__/payloads/combat.test.ts`):**
Add tests COMB-CONC-1..5:
- COMB-CONC-1: combatant WITHOUT `concentration` parses successfully (optional field).
- COMB-CONC-2: combatant WITH `concentration: { spellName: 'Bless', duration: '1m' }` parses successfully.
- COMB-CONC-3: combatant with `concentration: {}` (missing required sub-fields) FAILS.
- COMB-CONC-4: combatant with `concentration: { spellName: 'Bless' }` (missing duration) FAILS.
- COMB-CONC-5: combatant with extra fields in `concentration` (depends on whether the sub-object is strict) — match the spec choice and document.

**Reader test work (`packages/foundry-module/src/readers/__tests__/character-reader.test.ts`):**
Add tests CHRD-MR-1..3:
- CHRD-MR-1: `game.settings.get('dnd5e','rulesVersion')` returns `'modern'` → snapshot has `world.modernRules === true`.
- CHRD-MR-2: returns `'legacy'` → `world.modernRules === false`.
- CHRD-MR-3: returns `undefined` (fresh world without the setting) → defensive default `world.modernRules === false`.

**Reader test work (`packages/foundry-module/src/readers/__tests__/combat-reader.test.ts`):**
Add tests CMRD-CONC-1..4:
- CMRD-CONC-1: combatant with NO concentrating effect → snapshot combatant has no `concentration` field.
- CMRD-CONC-2: combatant with ONE concentrating effect → snapshot has `concentration: { spellName: '<effect.name>', duration: '<effect.duration.label>' }`.
- CMRD-CONC-3: combatant with multiple effects but only one has `flags.dnd5e.concentrating: true` → that effect is picked.
- CMRD-CONC-4: combatant.actor === null → no `concentration` field.

NO downstream files (panel-router.ts, panels, fixtures) consume these new fields in this plan — Wave 1+ plans do. The atomic commit only ships schema + reader + tests.

If RESEARCH assumption A1 (`'modern'` literal) or A2 (`flags.dnd5e.concentrating`) is discovered to be wrong at execution time, document via the Deviations playbook in the SUMMARY — the field path is the only thing that changes; the schema shape stays put.

Commit message: `feat(05-01): CharacterSnapshot.world.modernRules + Combatant.concentration + reader extensions (atomic Wave 0)`.

References: SHEET-02, SHEET-03, COMB-01, CONTEXT.md §Area 3 + 4, RESEARCH.md §Pattern 3 + §Pattern 4 + §Assumptions A1/A2/A3, Phase 4b Plan 06 SUMMARY atomic-schema pattern.
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test character combat && pnpm --filter @evf/foundry-module test character-reader combat-reader && pnpm typecheck && grep -c "modernRules" packages/shared-protocol/src/payloads/character.ts && grep -c "concentration" packages/shared-protocol/src/payloads/combat.ts && grep -c "rulesVersion" packages/foundry-module/src/readers/character-reader.ts && grep -c "concentrating" packages/foundry-module/src/readers/combat-reader.ts</automated>
  </verify>
  <done>
`CharacterSnapshotSchema.parse({...without world})` fails; `getCharacterSnapshot()` populates `world.modernRules` from the dnd5e setting; `CombatantSchema` accepts optional concentration; `getCombatSnapshot()` populates concentration sub-fields when the effect flag matches. All CHAR-MR-*, COMB-CONC-*, CHRD-MR-*, CMRD-CONC-* tests green. `pnpm typecheck` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel module → PanelRouter | Static-meta data crosses from author-controlled `*-panel.ts` into the runtime registry; must be validated by PanelMetaSchema |
| WS payload → CharacterSnapshotSchema | Extended `world.modernRules` arrives over the wire from foundry-module; Zod strictObject is the boundary |
| WS payload → CombatantSchema | Optional `concentration` sub-object arrives over the wire; Zod is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | Malformed `static meta` on a `*-panel.ts` module | mitigate | `PanelMetaSchema.safeParse` at `discoverPanels`; silent exclusion + `console.warn`; boot proceeds with surviving panels (CONTEXT.md §Area 1 silent-exclude policy). Internal contract — no third-party panel authors in MVP. |
| T-05-01-02 | Tampering | Wire-shape drift on `world.modernRules` (string vs boolean) | mitigate | `CharacterSnapshotSchema` requires `world.modernRules: z.boolean()` strict — non-boolean payloads rejected; reader-side `=== 'modern'` mapping produces the boolean. |
| T-05-01-03 | Tampering | Malformed `concentration` sub-object on Combatant | mitigate | `z.object({ spellName: z.string(), duration: z.string() }).optional()` — missing sub-fields rejected; downstream renderers always treat the field as optional. |
| T-05-01-04 | Information disclosure | LOCALE_MENU constant exposed to debug consumers | accept | Static data, no PII, no auth tokens — accept. |
</threat_model>

<verification>
**Wave 0 overall gate (all 3 tasks complete + atomic):**
- `pnpm typecheck` exits 0
- `pnpm lint:ci` exits 0
- `pnpm test` workspace-wide passes; test counts increase by ≥30 (3 task families)
- `grep -c "modernRules" packages/shared-protocol/src/payloads/character.ts` ≥ 1
- `grep -c "concentration" packages/shared-protocol/src/payloads/combat.ts` ≥ 1
- `grep -c "import.meta.glob" packages/g2-app/src/engine/panel-router.ts` == 1
- `grep -c "pt-br" packages/g2-app/src/status-hud/i18n-budgets.ts` ≥ 1
- `test -f docs/architecture/0010-panel-plugin-registry.md` exits 0
- `grep -q "ADR-0010" docs/architecture/README.md` exits 0
- `grep -q "status: accepted" docs/architecture/0010-panel-plugin-registry.md` exits 0

**Atomic-commit discipline:** each task commit MUST include its own test additions in the same commit. No task may leave the workspace in a state where `pnpm test` fails between task commits (Phase 4b Plan 01 + Plan 06 precedent).

**INV-1 / INV-2 / INV-3 / INV-4 verification:**
- INV-1: No ASCII fixtures land in this plan — fixtures arrive in plans 05-02..05-06. The width-budget contract (`as const satisfies` clause) gates downstream INV-1 fidelity.
- INV-2: ADR-0010 cites canonical sources (Vite docs + Even Hub SDK lines + Phase 4b ADRs).
- INV-3: Phase 5 does NOT modify Specs.md / README.md / showcase index.html in this plan (Wave 0 is engine-only). INV-3 cross-cutting commits happen on phase closure if needed.
- INV-4: Biome + TypeScript strict + Vitest coverage all gated by CI; no `// TODO` without issue ref allowed.
</verification>

<success_criteria>
- PanelRouter discovers, registers, and exposes via `discoverPanels()`/`openPanel()`/`closeActivePanel()` API the panel surface for downstream consumers (Plans 05-02..05-06).
- HUD_WIDTH_BUDGETS contains the exhaustive Phase 5 key set per UI-SPEC tables; `getLabel` per-key fallback works for `es`/`fr`/`pt-br`.
- Schema and reader extensions land atomically — `CharacterSnapshotSchema.parse` and `CombatantSchema.parse` both succeed end-to-end on real reader output.
- ADR-0010 ACCEPTED + README index updated.
- All gates green at end of plan: `pnpm typecheck` + `pnpm lint:ci` + `pnpm test` + INV-2 spot-check passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-01-SUMMARY.md` documenting: 3 task commits (one per task), files created/modified, test counts before+after per file, deviations (esp. RESEARCH assumptions A1/A2 verification outcome), Wave-1 readiness signal (Plans 05-02 / 05-03 / 05-04 / 05-05 are now READ-ONLY consumers of i18n-budgets.ts and schema files; ADR-0010 is ACCEPTED), Self-Check table proving each must-haves artifact exists.
</output>
