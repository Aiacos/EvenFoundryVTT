---
phase: 19-release-distribution
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - README.md
  - Specs.md
  - docs/showcase/index.html
  - docs/release/bridge.md
autonomous: true
requirements: [REL-05]

must_haves:
  truths:
    - "README has an ## Installation section documenting all 3 components: foundry-module manifest URL, bridge via docker pull/compose from GHCR, g2-app static host of g2-app-dist.zip"
    - "README, Specs.md changelog, and docs/showcase/index.html are updated coherently in a single INV-3 atomic commit"
    - "The GHCR first-push private→public visibility one-time step is documented in a release runbook note"
  artifacts:
    - path: "README.md"
      provides: "## Installation section (3 components)"
      contains: "## Installation"
    - path: "Specs.md"
      provides: "Phase 19 changelog stanza"
      contains: "Release & Distribution"
    - path: "docs/release/bridge.md"
      provides: "bridge GHCR pull/compose runbook + first-push visibility step"
      contains: "ghcr.io"
  key_links:
    - from: "README.md :: ## Installation"
      to: "ghcr.io/aiacos/evf-bridge"
      via: "docker pull instruction (REL-02 artifact)"
      pattern: "ghcr.io/aiacos/evf-bridge"
    - from: "README.md :: ## Installation"
      to: "g2-app-dist.zip GitHub Release asset"
      via: "static-host instruction (REL-03 artifact)"
      pattern: "g2-app-dist.zip"
    - from: "README.md :: ## Installation"
      to: "releases/latest/download/module.json"
      via: "manifest-URL instruction (REL-01 artifact)"
      pattern: "releases/latest/download/module.json"
---

<objective>
Add a `## Installation` section to README.md documenting how an end user installs each of the 3 CI-built components — foundry-module (manifest URL), bridge (`docker pull` / compose from `ghcr.io/aiacos/evf-bridge`), g2-app (download `g2-app-dist.zip` + serve `dist/` from an HTTPS static host) — and close the loop per INV-3 by updating Specs.md (changelog stanza) and docs/showcase/index.html (coherence) in the SAME commit. Also document the GHCR first-push private→public visibility one-time step in a `docs/release/bridge.md` runbook (REL-02 operational note surfaced by Plan 01's user_setup).

Purpose: close REL-05 so the published artifacts from Plan 01 have authoritative, INV-3-coherent install docs.
Output: README ## Installation + Specs changelog stanza + showcase coherence update + bridge release runbook — one atomic commit.

This plan references the artifacts Plan 01's workflow produces (GHCR image name, g2-app-dist.zip asset name) — hence wave 2 / depends_on 19-01. It introduces NO code and fires NO release.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-19-release-distribution/19-RESEARCH.md

<read_first>
@README.md
@deploy/README.md
@deploy/.env.example
@docs/release/foundry-module.md
</read_first>

<interfaces>
<!-- Artifact names the install docs must reference verbatim (produced by Plan 01). -->
- foundry-module manifest URL: https://github.com/Aiacos/EvenFoundryVTT/releases/latest/download/module.json (already in README "Quick install")
- bridge GHCR image: ghcr.io/aiacos/evf-bridge:<version> + :latest (lowercase owner)
- g2-app release asset: g2-app-dist.zip — archive root holds dist/ (zip layout from Plan 01: `zip -r ../../g2-app-dist.zip dist/`)
- bridge env contract: deploy/.env.example → EVF_INTERNAL_SECRET (openssl rand -base64 32) + EVF_PLUGIN_HOST_URL (origin-complete, no wildcards)
- compose: deploy/docker-compose.yml uses `build: context: ..` (local build) — do NOT break it; document GHCR `image:` as an OPTIONAL alternative (RESEARCH Pitfall 6 / Open Question 2)

INV-3 atomic precedent (Phases 14–18, e.g. commit 3a0c5cf / d68d7f2 / c208d24): README + Specs.md changelog + docs/showcase/index.html move together in ONE commit. Specs changelog anchor is the `## Changelog` heading (Specs.md ~line 4050); newest entry goes at the top of the list, matching the existing "tooling — GitFlow" entry format. This is internal tooling/distribution — NO spec version bump required (mirror the 2026-05-25 GitFlow entry which states "No spec version bump").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REL-02 operational runbook — bridge GHCR pull/compose + first-push visibility note</name>
  <files>docs/release/bridge.md</files>
  <read_first>
@docs/release/foundry-module.md
@deploy/README.md
@deploy/.env.example
@.planning/phases/EVF-19-release-distribution/19-RESEARCH.md
  </read_first>
  <action>
Create `docs/release/bridge.md` mirroring the tone/structure of `docs/release/foundry-module.md`. Document: (1) the bridge image is published to `ghcr.io/aiacos/evf-bridge:<version>` + `:latest` by the `build-bridge-ghcr` job in `foundry-module-release.yml` on a release dispatch; (2) the one-time post-first-release manual step — new GHCR packages default to PRIVATE, so after the first push the owner must set visibility to Public at GitHub → Profile → Packages → evf-bridge → Package settings → Change visibility → Public (RESEARCH Pitfall 1; not automatable via GITHUB_TOKEN); (3) how an operator consumes the image — `docker pull ghcr.io/aiacos/evf-bridge:latest`, and the OPTIONAL compose path: substitute `image: ghcr.io/aiacos/evf-bridge:<version>` for the `build:` block in `deploy/docker-compose.yml` (note that the default compose keeps `build:` for local dev — do NOT instruct removing it); (4) required env vars by reference to `deploy/.env.example` (EVF_INTERNAL_SECRET via `openssl rand -base64 32`, EVF_PLUGIN_HOST_URL origin-complete no-wildcards). Keep claims grounded in existing files; do not invent flags.
  </action>
  <verify>
    <automated>test -f docs/release/bridge.md && grep -qi 'visibility' docs/release/bridge.md && grep -q 'ghcr.io/aiacos/evf-bridge' docs/release/bridge.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/release/bridge.md` succeeds
    - `grep -c 'ghcr.io/aiacos/evf-bridge' docs/release/bridge.md` >= 1
    - `grep -ci 'visibility' docs/release/bridge.md` >= 1 (first-push private→public step documented)
    - `grep -c 'EVF_INTERNAL_SECRET\|deploy/.env' docs/release/bridge.md` >= 1
    - References the OPTIONAL `image:` compose substitution without instructing removal of the default `build:` path
  </acceptance_criteria>
  <done>Bridge release runbook documents GHCR consumption + the one-time visibility flip + env contract, grounded in existing deploy files.</done>
</task>

<task type="auto">
  <name>Task 2: REL-05 — README ## Installation (3 components) + INV-3 atomic close (Specs changelog + showcase coherence)</name>
  <files>README.md, Specs.md, docs/showcase/index.html</files>
  <read_first>
@README.md
@Specs.md
@docs/showcase/index.html
@.planning/phases/EVF-19-release-distribution/19-RESEARCH.md
  </read_first>
  <action>
Add a `## Installation` section to README.md covering all three components, sequenced 1) Foundry Module 2) Bridge 3) G2 App. (1) Foundry Module: reuse the existing manifest URL `https://github.com/Aiacos/EvenFoundryVTT/releases/latest/download/module.json` (the existing "Quick install" block may be folded into / cross-linked from the new section — keep a single source of truth, no contradictory duplicate). (2) Bridge: `docker pull ghcr.io/aiacos/evf-bridge:latest` (note version-pinned tags also exist on the releases page — RESEARCH Open Question 3), plus the compose path via `deploy/docker-compose.yml` referencing `deploy/.env.example` for EVF_INTERNAL_SECRET + EVF_PLUGIN_HOST_URL, and a pointer to `docs/release/bridge.md` for the first-push visibility note. (3) G2 App: download `g2-app-dist.zip` from the GitHub Release, extract, serve the `dist/` directory from any HTTPS static host (nginx/Caddy/Pages), and set the bridge `EVF_PLUGIN_HOST_URL` to that origin.

Then close INV-3 ATOMICALLY in the SAME commit: (a) add a Specs.md `## Changelog` stanza at the top of the list (newest-first) describing Phase 19 Release & Distribution — REL-01..05, the two new workflow jobs (build-bridge-ghcr GHCR push + build-g2app-zip), the CHANGELOG.md release-notes sourcing, the README Installation section, and an explicit "No spec version bump (tooling/distribution — no change to hardware/library/fps/phase/locale claims)" line mirroring the 2026-05-25 GitFlow entry; include an `INV-3 coerenza:` line listing README.md + Specs.md + docs/showcase/index.html updated in the same commit, and a `Re-verified ✓ (n/a — internal tooling/distribution change, no upstream claim)` line. (b) Update docs/showcase/index.html for coherence — add a brief distribution/install note in the closing paragraph (the existing ~line 1051 paragraph that enumerates phases) consistent with the README Installation section; do NOT change version stats unless a real bump occurs (none here). Keep README badges + showcase version stat untouched (no version bump). Run the INV-3 suite + tests at the end.
  </action>
  <verify>
    <automated>grep -q '^## Installation' README.md && grep -q 'ghcr.io/aiacos/evf-bridge' README.md && grep -q 'g2-app-dist.zip' README.md && grep -q 'releases/latest/download/module.json' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Installation' README.md` == 1
    - README documents all 3 artifacts: `grep -c 'releases/latest/download/module.json' README.md` >= 1 (foundry-module), `grep -c 'ghcr.io/aiacos/evf-bridge' README.md` >= 1 (bridge), `grep -c 'g2-app-dist.zip' README.md` >= 1 (g2-app)
    - README points at env contract + runbook: `grep -c 'deploy/.env\|docs/release/bridge.md' README.md` >= 1
    - Specs.md changelog has the Phase 19 stanza: `grep -ci 'Release & Distribution' Specs.md` >= 1 AND a "no spec version bump" line present (`grep -ci 'no spec version bump\|nessun.*version bump\|No spec version bump' Specs.md` >= 1 for the new stanza)
    - INV-3 same-commit coherence: after the final commit, `git show --name-only HEAD` lists README.md AND Specs.md AND docs/showcase/index.html together
    - No version bump introduced: README status/spec badges unchanged (`grep -c 'v0.9.13' README.md` unchanged vs pre-edit) — distribution change only
    - INV-4 + CI Gate 8 preserved: `pnpm test` + `pnpm lint:ci` + `pnpm typecheck` exit 0; socketlib handler count still 17 (no socketlib files touched)
    - INV suite green: `pnpm run inv:all` (or the project's INV-3 coherence check) exits 0
  </acceptance_criteria>
  <done>README has a 3-component ## Installation section coherent with Specs.md changelog + showcase, committed atomically per INV-3; no version bump; tests/lint/typecheck/INV suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs → end user | install instructions an operator will follow with real credentials/hosts |
| README/Specs/showcase coherence (INV-3) | drift between projections is a documentation-integrity hazard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Information Disclosure | install docs env guidance | mitigate | Reference `deploy/.env.example` for EVF_INTERNAL_SECRET (generated via openssl, never committed); reiterate origin-complete no-wildcard EVF_PLUGIN_HOST_URL — no literal secrets in docs |
| T-19-07 | Tampering | INV-3 doc drift | mitigate | Atomic single-commit update of README + Specs.md + showcase; INV suite gate enforces cross-file coherence at commit time |
| T-19-08 | Spoofing | install-source authenticity | accept-with-control | Docs point only at canonical first-party URLs (github.com/Aiacos/EvenFoundryVTT releases + ghcr.io/aiacos/evf-bridge); no third-party mirror instructions |
| T-19-SC | Tampering | supply-chain | mitigate | Docs-only change; no npm/pip/cargo installs; no new deps; CI Gate 8 socketlib=17 untouched |
</threat_model>

<verification>
- README `## Installation` documents all 3 components (manifest URL + GHCR image + g2-app-dist.zip static host) with env-contract + runbook pointers.
- Specs.md changelog stanza for Phase 19 present with explicit "no spec version bump" + INV-3 coherence line; showcase coherence note added.
- INV-3 atomic: `git show --name-only HEAD` lists README.md + Specs.md + docs/showcase/index.html in one commit.
- docs/release/bridge.md documents the one-time GHCR private→public visibility step.
- `pnpm test` + `pnpm lint:ci` + `pnpm typecheck` + INV suite green; socketlib handler count 17.
- NO live release fired; NO version bump.

**human_needed (ADR-0005):** End-to-end install validation (Foundry manifest install, `docker pull` of the real image, serving extracted g2-app dist) requires a published release + Even Hub/Foundry runtime and is user-gated post-release.
</verification>

<success_criteria>
- REL-05 closed: 3-component ## Installation section coherent across README + Specs + showcase (INV-3 atomic commit).
- GHCR first-push visibility step documented (REL-02 operational completeness).
- No code, no deps, no version bump, no live release; INV-1..5 + INV-4 gates green; socketlib=17.
</success_criteria>

<output>
Create `.planning/phases/EVF-19-release-distribution/19-02-SUMMARY.md` when done.
</output>
