---
phase: 21-character-sheet-su-canvas-dati-main-tab
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - packages/g2-app/src/panels/canvas-character-sheet-panel.ts
  - packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts
autonomous: true
requirements: [RSHEET-03]
must_haves:
  truths:
    - "Portrait is fetched async ONCE from snapshot.portrait.url, decoded to RGBA, greyscale-dithered via dither-utils ditherTile + buildGreyscalePalette, encoded as a 4-bit PNG, and pushed to MapBaseLayer slot 3 via setPortraitOverride"
    - "Portrait fetch is fire-and-forget from onMount (NOT awaited inside attachCanvas/onMount) so the panel appears immediately; on fetch/decode failure the portrait is omitted silently with no crash"
    - "Portrait PNG is encoded at 100×60 (Phase 13 portrait-state precedent, fits slot 3's 200×100 frame) reusing UPNG.encode([buf], 100, 60, 16)"
  artifacts:
    - path: "packages/g2-app/src/panels/canvas-character-sheet-panel.ts"
      provides: "_fetchPortraitAsync() async-once portrait pipeline → setPortraitOverride(3, bytes)"
      contains: "_fetchPortraitAsync"
  key_links:
    - from: "_fetchPortraitAsync"
      to: "dither-utils ditherTile + buildGreyscalePalette"
      via: "import + call on decoded RGBA"
      pattern: "ditherTile\\("
    - from: "_fetchPortraitAsync"
      to: "MapBaseLayer.setPortraitOverride(3, bytes)"
      via: "slot-3 portrait override infra (reuse)"
      pattern: "setPortraitOverride\\(\\s*3"
---

<objective>
Add the portrait pipeline to `CanvasCharacterSheetPanel`: fetch the portrait URL async once,
decode it to RGBA in the WebView, greyscale-dither it reusing the extracted `dither-utils`
Floyd-Steinberg pipeline (Plan 21-02), encode a 100×60 4-bit PNG, and push it to the existing
`MapBaseLayer` slot-3 portrait-override infra. On any failure the portrait is silently omitted.

This plan touches the same panel file as Plan 21-03, so it runs in a later wave (sequential by
file ownership). It is the final piece of the panel: paint + tabs + gesture (21-03) + portrait (this).

Purpose: RSHEET-03 (portrait greyscale-dithered, async-once, reuse MapBaseLayer portrait-override
slot infra, silent on fetch-fail).
Output: `_fetchPortraitAsync` + onMount fire-and-forget wiring + RCSP-PORTRAIT tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-RESEARCH.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-PATTERNS.md
@packages/g2-app/src/panels/canvas-character-sheet-panel.ts
@packages/g2-app/src/raster/map-base-layer.ts
</context>

<artifacts_this_phase_produces>
This plan introduces these new symbols:
- `CanvasCharacterSheetPanel._fetchPortraitAsync(): Promise<void>` (private async-once portrait pipeline)
- portrait slot constant (slot 3) consumption via `setPortraitOverride(3, bytes | null)`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _fetchPortraitAsync (fetch → dither → encode → slot 3) + fire-and-forget wiring</name>
  <files>packages/g2-app/src/panels/canvas-character-sheet-panel.ts, packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts</files>
  <read_first>
    - packages/g2-app/src/panels/canvas-character-sheet-panel.ts (the Plan 21-03 class — add _fetchPortraitAsync; call it fire-and-forget `void this._fetchPortraitAsync()` from onMount)
    - packages/g2-app/src/raster/map-base-layer.ts (`setPortraitOverride(slot, bytes | null)` ~line 170, slot 3 convention = map-tile-3 bottom-right; the override re-issues updateImageRawData — never call updateImageRawData directly)
    - packages/g2-app/src/panels/character-sheet-panel.ts (`_applyPortraitOverride` ~line 502 — the glyph path's portrait slot usage + `portraitSlot` field)
    - packages/g2-app/src/raster/dither-utils.ts (from Plan 21-02 — `ditherTile(rgba,w,h,pal)` + `buildGreyscalePalette()`)
    - 21-PATTERNS.md §`Portrait fetch / dither / slot pattern` (full pipeline excerpt; W=100 H=60) + §Pitfall 3 (must NOT block onMount) + §Pitfall 5 (size within 200×100 container)
    - 21-RESEARCH.md §Pattern 4 + §Open Q 3 (100×60 consistent with Phase 13 precedent)
  </read_first>
  <behavior>
    - Test (RCSP-PORTRAIT-OK): with a mocked fetch returning a decodable blob + stubbed createImageBitmap/OffscreenCanvas, _fetchPortraitAsync calls setPortraitOverride(3, <Uint8Array>) exactly once
    - Test (RCSP-PORTRAIT-MISSING-URL): snapshot.portrait undefined → _fetchPortraitAsync returns without calling setPortraitOverride
    - Test (RCSP-PORTRAIT-FETCH-FAIL): fetch rejects OR response.ok false → no throw, setPortraitOverride NOT called (silent omit)
    - Test (RCSP-PORTRAIT-NONBLOCK): onMount resolves without awaiting the portrait fetch (assert onMount promise resolves even when the fetch promise is still pending)
    - Test (RCSP-PORTRAIT-ONCE): two onMount/render cycles fetch the portrait at most once per mounted snapshot (async-once guard)
  </behavior>
  <action>
    Add a private `async _fetchPortraitAsync(): Promise<void>` to CanvasCharacterSheetPanel. Read `this._snapshot?.portrait?.url`; return early if undefined. Wrap the whole body in try/catch (silent on any error). Steps: `fetch(url)` → bail if `!response.ok`; `response.blob()`; `createImageBitmap(blob, { resizeWidth: 100, resizeHeight: 60 })`; draw to a 100×60 `OffscreenCanvas` scratch ctx (null-ctx guard → return); `getImageData(0,0,100,60)`; `buildGreyscalePalette()` + `ditherTile(imageData.data, 100, 60, pal)` from dither-utils; `UPNG.encode([dithered.buffer as ArrayBuffer], 100, 60, 16)` → `new Uint8Array(...)`; `this._mapBaseLayer?.setPortraitOverride(3, pngBytes)`. Add an async-once guard (e.g. a `_portraitFetched` boolean or per-url memo) so repeated mounts/renders do not re-fetch the same portrait. Wire `void this._fetchPortraitAsync()` (fire-and-forget — NOT awaited) into `onMount` AFTER the gesture subscribe, so onMount never blocks on the network (Pitfall 3). Ensure `onUnmount` already clears slot 3 (`setPortraitOverride(3, null)` — added in Plan 21-03) and resets the async-once guard so re-open re-fetches. Full TSDoc per INV-4. Add the RCSP-PORTRAIT-* tests with mocked fetch/createImageBitmap/OffscreenCanvas (happy-dom-safe; degrade gracefully when those globals are absent).
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run canvas-character-sheet-panel</automated>
  </verify>
  <acceptance_criteria>
    - RCSP-PORTRAIT-OK/MISSING-URL/FETCH-FAIL/NONBLOCK/ONCE all green
    - setPortraitOverride called with (3, Uint8Array) on success; never called on fetch-fail/missing-url
    - onMount does not await the fetch (NONBLOCK test green)
    - ditherTile + buildGreyscalePalette imported from dither-utils (not duplicated)
  </acceptance_criteria>
  <done>Portrait async-once pipeline reuses dither-utils + slot-3 override; silent on failure; non-blocking; RCSP-PORTRAIT tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portrait URL fetch → WebView | external image bytes enter the WebView |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Information Disclosure | portrait URL to non-whitelisted origin | mitigate | Even Hub blocks non-whitelisted fetches; portrait served by the already-whitelisted bridge endpoint; no user-controlled URL injection path |
| T-21-03b | Tampering | malicious PNG payload | mitigate | createImageBitmap decodes to pixels (consumed as pixel data, not executed) |
| T-21-03c | Denial of Service | slow/failing portrait URL stalling mount | mitigate | fetch is fire-and-forget from onMount (Pitfall 3); try/catch silent on failure |
| T-21-SC | Tampering | npm installs | accept | no new packages (upng-js + image-q already in g2-app) |
</threat_model>

<verification>
- `pnpm --filter @evf/g2-app test -- --run` green (RCSP-PORTRAIT-*)
- `pnpm typecheck` exits 0
- `pnpm lint:ci` exits 0
- `git diff --quiet packages/g2-app/src/engine/panel-gesture-bus.ts` (unchanged)
- CI Gate 8 socketlib handler count = 17 unchanged
</verification>

<success_criteria>
Portrait fetched async-once, greyscale-dithered via the reused dither-utils pipeline, encoded
100×60 4-bit PNG, pushed to MapBaseLayer slot 3; fire-and-forget non-blocking; silent on
fetch-fail; RCSP-PORTRAIT tests green.
</success_criteria>

<output>
Create `.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-04-SUMMARY.md` when done
</output>
