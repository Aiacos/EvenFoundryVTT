---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - packages/g2-app/package.json
  - packages/g2-app/tsconfig.json
  - packages/g2-app/vite.config.ts
  - packages/g2-app/src/index.ts
  - packages/g2-app/README.md
  - packages/bridge/package.json
  - packages/bridge/tsconfig.json
  - packages/bridge/src/index.ts
  - packages/bridge/README.md
  - packages/foundry-module/package.json
  - packages/foundry-module/tsconfig.json
  - packages/foundry-module/src/index.ts
  - packages/foundry-module/README.md
  - packages/shared-protocol/package.json
  - packages/shared-protocol/tsconfig.json
  - packages/shared-protocol/src/index.ts
  - packages/shared-protocol/README.md
  - packages/shared-render/package.json
  - packages/shared-render/tsconfig.json
  - packages/shared-render/src/index.ts
  - packages/shared-render/README.md
  - packages/validation-harness/package.json
  - packages/validation-harness/tsconfig.json
  - packages/validation-harness/upng-js.d.ts
  - packages/validation-harness/README.md
  - packages/validation-harness/src/lib/schemas.ts
  - packages/validation-harness/src/lib/output.ts
  - packages/validation-harness/src/lib/stats.ts
  - packages/validation-harness/src/lib/branch-decision.ts
  - packages/validation-harness/src/lib/hub.ts
  - packages/validation-harness/scripts/10-0-1-r1-timing.ts
  - packages/validation-harness/scripts/10-0-2-image-format.ts
  - packages/validation-harness/scripts/10-0-3-ble-multi-env.ts
  - packages/validation-harness/scripts/10-0-7-dle-sustained.ts
  - packages/validation-harness/scripts/10-0-8-queue-depth.ts
  - packages/validation-harness/scripts/10-0-9-palette-calibration.ts
  - packages/validation-harness/scripts/midiqol-config-probe.ts
  - packages/validation-harness/scripts/run-all.ts
  - packages/validation-harness/foundry-modules/midiqol-probe-module/
  - packages/validation-harness/tests/path-resolution.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-1.01 honored: 5 new packages exist (g2-app, bridge, foundry-module, shared-protocol, shared-render) — all with @evf/{name}@0.1.0-alpha.0 private, workspace:* protocol enabled"
    - "D-1.02 honored: tests/phase-0/ folded into packages/validation-harness/; the directory tests/phase-0/ NO LONGER EXISTS at end of Wave 1"
    - "Phase 0 hardware test scripts preserved: 6 hardware scripts + midiqol probe + run-all kept executable via tsx (in scripts/, not tests/, per RESEARCH Open Question 1 — hardware-bound stays operational)"
    - "Phase 0 _shared/ utilities promoted to validation-harness/src/lib/ — schemas.ts, output.ts, stats.ts, branch-decision.ts, hub.ts re-exported via index"
    - "Path resolution preserved: validation-harness writer still targets repo-root docs/perf/phase-0/ (Pitfall 8 mitigation via path.resolve from import.meta.dirname OR EVF_REPO_ROOT env override)"
    - "upng-js.d.ts ambient module declaration preserved (Phase 0 Plan 03 deviation #1) at packages/validation-harness/upng-js.d.ts"
    - "WAVE-1-G1..G5 green: pnpm ls shows 6 workspace pkgs · pnpm -r exec tsc green · validation-harness Vitest discoverable · tests/phase-0/ removed · path-resolution.test passes"
  artifacts:
    - path: "packages/g2-app/package.json"
      provides: "@evf/g2-app private package, Vite 8 devDep, workspace:* on shared-protocol+shared-render"
      contains: "@evf/g2-app"
    - path: "packages/bridge/package.json"
      provides: "@evf/bridge private package, runtime deps deferred Phase 3"
      contains: "@evf/bridge"
    - path: "packages/foundry-module/package.json"
      provides: "@evf/foundry-module private package + module.json scaffold (compatibility deferred Phase 2)"
      contains: "@evf/foundry-module"
    - path: "packages/shared-protocol/package.json"
      provides: "@evf/shared-protocol — types + Zod schemas single source of truth"
      contains: "@evf/shared-protocol"
    - path: "packages/shared-render/package.json"
      provides: "@evf/shared-render — ASCII snapshot infrastructure (impl Wave 2)"
      contains: "@evf/shared-render"
    - path: "packages/validation-harness/package.json"
      provides: "@evf/validation-harness — folded from tests/phase-0/"
      contains: "@evf/validation-harness"
    - path: "packages/validation-harness/src/lib/output.ts"
      provides: "Evidence writer with repo-root-aware path resolution (Pitfall 8 mitigation)"
      contains: "docs/perf/phase-0"
    - path: "packages/validation-harness/tests/path-resolution.test.ts"
      provides: "Smoke test asserting writer still targets repo-root after fold-in"
      contains: "docs/perf/phase-0"
  key_links:
    - from: "all packages/*/tsconfig.json"
      to: "tsconfig.base.json"
      via: "extends ../../tsconfig.base.json"
      pattern: "extends.*tsconfig\\.base\\.json"
    - from: "packages/g2-app/package.json#dependencies"
      to: "packages/shared-protocol + packages/shared-render"
      via: "workspace:* protocol"
      pattern: "workspace:\\*"
    - from: "packages/validation-harness/src/lib/output.ts"
      to: "docs/perf/phase-0/"
      via: "path.resolve(import.meta.dirname, '../../../../docs/perf/phase-0/') OR process.env.EVF_REPO_ROOT"
      pattern: "docs/perf/phase-0"
---

<objective>
Wave 1 — scaffold dei **6 packages** del monorepo: 5 packages nuovi + 1 fold-in promotion. Ogni package è minimale (placeholder `src/index.ts` + tsconfig extends base + package.json `@evf/{name}@0.1.0-alpha.0` private). Nessuna logica applicativa — solo struttura, contratti, workspace linkage.

**Purpose:** Plan 03 (Wave 2 — ADRs + snapshot framework + CI) richiede che i 6 packages siano già linkati via `workspace:*` per scrivere il test esempio in `packages/g2-app/src/__tests__/example-status-hud.test.ts` che importa da `@evf/shared-render`. Le fasi downstream (Phase 2-13) consumeranno questi packages senza modificarne il contratto base.

**Critical fold-in (D-1.02 / Phase 0 D-15):** `tests/phase-0/` viene **promosso** a `packages/validation-harness/` preservando:
1. Tutti i 6 hardware test scripts (Phase 0 hardware-bound, esecuzione gated su Even Hub grant)
2. Il MidiQOL probe + run-all orchestrator
3. Le 5 utility `_shared/` (diventano `src/lib/`)
4. Il Foundry mini-module `midiqol-probe-module/`
5. La `upng-js.d.ts` ambient declaration
6. Il contratto path: writer DEVE continuare a scrivere in **repo-root** `docs/perf/phase-0/`, NON in `packages/validation-harness/docs/perf/phase-0/`

A fine Wave 1: `tests/phase-0/` directory **rimossa interamente** (verifica con `[ ! -d tests/phase-0 ]`).

**Output:** 6 packages green, workspace `pnpm ls --depth=0` mostra `@evf/g2-app`, `@evf/bridge`, `@evf/foundry-module`, `@evf/shared-protocol`, `@evf/shared-render`, `@evf/validation-harness`. `pnpm -r exec tsc --noEmit` exit 0. `pnpm test --filter @evf/validation-harness --run` exit 0 OR 2 (Pattern 3 skip uniform da Phase 0). Phase 0 Plan 04 hardware operational step può ancora eseguire i 6 hardware scripts via `pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts --skip-hardware` (with hardware: drop the flag).
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/00-validation-gates/00-01-SUMMARY.md
@.planning/phases/00-validation-gates/00-02-SUMMARY.md
@.planning/phases/00-validation-gates/00-03-SUMMARY.md

<!-- Phase 0 fold-in source — every file in here moves or is removed -->
@tests/phase-0/package.json
@tests/phase-0/tsconfig.json
@tests/phase-0/README.md
@tests/phase-0/upng-js.d.ts
@tests/phase-0/_shared/schemas.ts
@tests/phase-0/_shared/output.ts
@tests/phase-0/_shared/stats.ts
@tests/phase-0/_shared/branch-decision.ts
@tests/phase-0/_shared/hub.ts
@tests/phase-0/midiqol-config-probe.ts
@tests/phase-0/run-all.ts
@tests/phase-0/10-0-1-r1-timing.ts

<!-- Foundations from Wave 0 (Plan 01) -->
@tsconfig.base.json
@pnpm-workspace.yaml
@package.json

<interfaces>
<!-- Per-package tsconfig.json shape — RESEARCH.md §Code Examples lines 570-583 -->
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],  // varies per package
    "types": ["happy-dom", "@evf/shared-protocol"],  // varies per package
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}

<!-- Phase 0 _shared/output.ts current path logic (to be modified for Pitfall 8) -->
<!-- Currently writes to: docs/perf/phase-0/{file} relative to process.cwd() -->
<!-- After fold-in MUST resolve to repo-root docs/perf/phase-0/ via: -->
<!--   const REPO_ROOT = process.env.EVF_REPO_ROOT ?? path.resolve(import.meta.dirname, '../../../..'); -->
<!--   const OUTPUT_DIR = path.join(REPO_ROOT, 'docs/perf/phase-0'); -->

<!-- Phase 0 Plan 03 SUMMARY confirms upng-js.d.ts MUST stay as standalone .d.ts file -->
<!-- (inline `declare module` was rejected with TS2665) -->

<!-- Phase 0 README warns: "real evidence under repo-root docs/perf/phase-0/ when scripts run from repo root" -->
<!-- That contract MUST survive fold-in. -->

</interfaces>
</context>

<threat_model>
<!-- ASVS Level 1, block on high severity -->

| ID | Severity | Threat | Mitigation in this Plan |
|----|----------|--------|------------------------|
| T-01-01 | Medium | Dependency confusion via per-package devDeps | All per-package `devDependencies` minimal (Wave 1 only `tsx` per-package as runner — workspace inherits everything else); `workspace:*` protocol for internal deps prevents external impersonation; `pnpm-lock.yaml` from Plan 01 already pins transitive |
| T-01-05 | Low | Phase 0 evidence writer path drift after fold-in (Pitfall 8) — could silently write to packages/validation-harness/docs/perf/phase-0/ instead of repo-root | Task 3 explicit path-resolution rewrite + dedicated smoke test `tests/path-resolution.test.ts` that asserts target dir is repo-root, NOT package-local |
| — | Info | Phase 0 hardware scripts unable to execute after fold-in (lose tsx executability) | Scripts moved to `scripts/` not `tests/` per RESEARCH Open Question 1 — `pnpm --filter @evf/validation-harness exec tsx scripts/{script}.ts` still works just like Phase 0 `pnpm exec tsx {script}.ts` |

Block on: high severity (none — proceed).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 5 new packages — g2-app, bridge, foundry-module, shared-protocol, shared-render — package.json + tsconfig + src/index.ts + README</name>
  <files>
    packages/g2-app/package.json
    packages/g2-app/tsconfig.json
    packages/g2-app/vite.config.ts
    packages/g2-app/src/index.ts
    packages/g2-app/README.md
    packages/bridge/package.json
    packages/bridge/tsconfig.json
    packages/bridge/src/index.ts
    packages/bridge/README.md
    packages/foundry-module/package.json
    packages/foundry-module/tsconfig.json
    packages/foundry-module/src/index.ts
    packages/foundry-module/README.md
    packages/shared-protocol/package.json
    packages/shared-protocol/tsconfig.json
    packages/shared-protocol/src/index.ts
    packages/shared-protocol/README.md
    packages/shared-render/package.json
    packages/shared-render/tsconfig.json
    packages/shared-render/src/index.ts
    packages/shared-render/README.md
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 196-300 — Recommended Project Structure tree; lines 570-583 — per-package tsconfig example)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-1.01 5 packages + 1 fold-in; D-1.04 strict TS extends; D-1.12 Changesets pre-1.0)
    - tsconfig.base.json (from Plan 01 — must `extends` this)
    - pnpm-workspace.yaml (verifies `packages/*` glob will pick up new dirs)
  </read_first>
  <action>
For each of the 5 NEW packages (g2-app, bridge, foundry-module, shared-protocol, shared-render), create the canonical structure. Variations per-package documented inline. Use **Write tool** for every file (never echo/heredoc).

---

### A. `packages/g2-app/` — Browser bundle for Even Realities App WebView (Phase 4a real consumer)

**`packages/g2-app/package.json`:**

```json
{
  "name": "@evf/g2-app",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF G2 plugin host bundle — served via static HTTP to Even Realities App WebView (Phase 4a fills real implementation).",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@evf/shared-protocol": "workspace:*",
    "@evf/shared-render": "workspace:*"
  },
  "devDependencies": {
    "vite": "8.0.11"
  }
}
```

**`packages/g2-app/tsconfig.json`** (per RESEARCH.md lines 570-583, WebView target ES2023):

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable", "WebWorker"],
    "types": ["happy-dom"],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**`packages/g2-app/vite.config.ts`** (minimal scaffold — Phase 4a fills real):

```typescript
// Phase 4a placeholder — minimal Vite 8 config so `pnpm --filter @evf/g2-app build` works.
// Real config (workers, OffscreenCanvas, raster pipeline bundling) lands Phase 4a.
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    target: 'es2023',
    outDir: 'dist',
    emptyOutDir: true,
  },
});
```

**`packages/g2-app/src/index.ts`** (placeholder export):

```typescript
/**
 * @evf/g2-app — placeholder for Phase 4a G2 plugin host implementation.
 * @see Specs.md §3.7 (plugin host architecture) + §11.5.7 (raster pipeline)
 * @see .planning/phases/01-foundation/01-CONTEXT.md D-1.01
 */
export const PACKAGE_NAME = '@evf/g2-app';
```

**`packages/g2-app/README.md`:**

```markdown
# @evf/g2-app

Browser bundle for the EVF G2 plugin host (Even Realities App WebView).

**Status:** Phase 4a placeholder. Real implementation lands in Phase 4a (G2 Engine + Raster + Status HUD).

## Stack

- Vite 8 (build + dev server)
- Web Worker + OffscreenCanvas (Phase 4a — raster pipeline)
- `image-q@4.0.0` + `upng-js@2.1.0` + `xxhash-wasm@1.1.0` (Phase 4a — dither + 4-bit PNG + delta hash)

## Consumers / Downstream

Phase 4a: render Foundry scene as 4-bit dithered raster to G2 glasses.
Phase 4b: overlay slot + map mode toggle + adversarial UI.
Phase 5: 6-tab character sheet panel consumes via shared-render.

## See also

- `Specs.md` §3.7 (plugin host architecture)
- `docs/architecture/0001-layered-ui-model.md` (when Wave 2 lands)
- `.planning/phases/01-foundation/01-CONTEXT.md`
```

---

### B. `packages/bridge/` — Node 24 Fastify+ws service (Phase 3 real consumer)

**`packages/bridge/package.json`:**

```json
{
  "name": "@evf/bridge",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF Bridge service — Fastify + ws CORS proxy with bearer auth + Tool Registry (Phase 3 fills real implementation).",
  "scripts": {
    "build": "tsup src/index.ts --format esm --target node24",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@evf/shared-protocol": "workspace:*"
  },
  "devDependencies": {
    "tsup": "8.5.1"
  }
}
```

**`packages/bridge/tsconfig.json`** (Node 24 target, no DOM):

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**`packages/bridge/src/index.ts`:**

```typescript
/**
 * @evf/bridge — placeholder for Phase 3 Fastify + ws bridge service.
 * @see Specs.md §5.2 (Bridge stack) + §5.3 (Tool Registry)
 * @see docs/architecture/0002-protocol-versioning.md (when Wave 2 lands)
 * @see docs/architecture/0003-tool-registry-pattern.md (when Wave 2 lands)
 */
export const PACKAGE_NAME = '@evf/bridge';
```

**`packages/bridge/README.md`:**

```markdown
# @evf/bridge

EVF Bridge service — Node 24 Fastify + ws CORS-friendly reverse-proxy with bearer auth + Tool Registry.

**Status:** Phase 3 placeholder. Real implementation lands in Phase 3 (Bridge Service Skeleton).

## Stack (Phase 3 install)

- Fastify 5.8.5 + @fastify/websocket 11.2.0 + @fastify/cors 11.2.0 + @fastify/rate-limit 10.3.0
- `ws@8.20.0` for outbound Foundry socket
- `zod@4.4.3` (via @evf/shared-protocol workspace:*)
- `pino@10.3.1` structured logging
- `prom-client@15.1.3` Prometheus metrics
- `qrcode@1.5.4` pairing QR generation

## Endpoints (Phase 3)

- `POST /v1/actor/*` — Tool Registry dispatch
- `GET /v1/scene`, `GET /v1/combat` — read API
- `GET /v1/tools` — Tool Registry discovery
- `/healthz`, `/readyz`, `/metrics` — ops

## Deployment

Docker Compose homelab single-tenant — `Dockerfile` under `deploy/` (Phase 3).
```

---

### C. `packages/foundry-module/` — Foundry module `evenfoundryvtt` (Phase 2 real consumer)

**`packages/foundry-module/package.json`:**

```json
{
  "name": "@evf/foundry-module",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF Foundry VTT module (evenfoundryvtt) — readers, writers, pairing UI (Phase 2 fills real implementation).",
  "scripts": {
    "build": "tsup src/index.ts --format esm --target es2022",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@evf/shared-protocol": "workspace:*"
  },
  "devDependencies": {
    "tsup": "8.5.1"
  }
}
```

**`packages/foundry-module/tsconfig.json`** (Foundry ESM runtime, happy-dom for unit tests):

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM"],
    "types": ["happy-dom"],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**`packages/foundry-module/src/index.ts`:**

```typescript
/**
 * @evf/foundry-module — placeholder for Phase 2 Foundry module entry point.
 *
 * NOTE: Foundry-side `module.json` manifest (compatibility/relationships/scripts) lands in
 * Phase 2 — Phase 1 does NOT generate it yet (would mislead Foundry into trying to load
 * an empty module). See REQUIREMENTS.md FOUN-01..04, CONN-01..05.
 *
 * @see Specs.md §3.4 (Foundry compatibility) + §4.4 (dnd5e 5.x adapter)
 * @see Phase 0 Plan 02 MIDIQ-01 — `relationships.requires.midi-qol` will be declared here
 */
export const PACKAGE_NAME = '@evf/foundry-module';
```

**`packages/foundry-module/README.md`:**

```markdown
# @evf/foundry-module (`evenfoundryvtt`)

EVF Foundry VTT module — runs in Foundry game world, exposes read API + write path via socketlib.

**Status:** Phase 2 placeholder. Real implementation lands in Phase 2 (Foundry Module Core + Pairing UI).

## Compatibility (Phase 2 module.json)

- Foundry VTT: minimum 13.347, verified 14
- dnd5e system: ≥ 5.3.3
- socketlib: required (Foundry sibling module, NOT npm — `relationships.requires`)
- MidiQOL: required for MVP write path (Phase 0 MIDIQ-01 evidence) — `relationships.requires.midi-qol`

## Read API (Phase 2)

`getCharacterState`, `getCombatState`, `getSceneViewport`, `getEventLog`, `subscribeUpdates`.

## Write Path (Phase 7)

`socketlib.executeAsGM` → `activity.use({configure: false, event: null})` → MidiQOL workflow.
Single-workflow-origin discipline option A (player client NEVER directly invokes activity).

## See also

- `Specs.md` §3.4, §4.4, §5.6
- `docs/architecture/0003-tool-registry-pattern.md`
```

---

### D. `packages/shared-protocol/` — Types + Zod schemas (Phase 2+ all consumers)

**`packages/shared-protocol/package.json`:**

```json
{
  "name": "@evf/shared-protocol",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF shared protocol — TypeScript types + Zod schemas. Single source of truth per ADR-0002.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  }
}
```

**Note:** `main`/`types`/`exports` point to `./src/index.ts` (NOT `dist/`) — consumer packages transpile during their own build (Vite for g2-app, tsup for bridge). This is the standard "source-package" pattern for internal monorepos (avoids double-build).

**`packages/shared-protocol/tsconfig.json`:**

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": [],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**`packages/shared-protocol/src/index.ts`:**

```typescript
/**
 * @evf/shared-protocol — TypeScript types + Zod schemas shared across all EVF packages.
 *
 * Single source of truth for protocol contracts per ADR-0002 (WS envelope + idempotency)
 * and ADR-0003 (Tool Registry tool input shapes).
 *
 * Phase 2 fills first real schemas (WS envelope + actor read API).
 *
 * @see Specs.md §4 (architecture) + §5.3 (Tool Registry)
 */
export const PACKAGE_NAME = '@evf/shared-protocol';
```

**`packages/shared-protocol/README.md`:**

```markdown
# @evf/shared-protocol

Single source of truth for protocol contracts across all EVF packages.

**Status:** Phase 2 placeholder for real schemas. Wave 2 ADR-0002 will document the WS envelope shape that lands here in Phase 2.

## Contents (Phase 2+)

- WS envelope: `{ proto, seq, ts, type, path?, value?, prev_seq? }` (ADR-0002)
- Idempotency key shape (UUID-per-action)
- Tool Registry tool input schemas: `cast_spell`, `weapon_attack`, `use_item`, `skill_check`, `move_token`, `place_template`, `set_targets` (ADR-0003)
- Actor read API result shapes (`getCharacterState`, `getCombatState`, etc.)
- Capability negotiation handshake schema

## Pattern

All schemas defined as Zod. Static types derived via `z.infer<typeof Schema>`. Imported by `@evf/bridge`, `@evf/foundry-module`, `@evf/g2-app`, `@evf/foundry-mcp` (V2).

## See also

- `docs/architecture/0002-protocol-versioning.md` (Wave 2)
- `docs/architecture/0003-tool-registry-pattern.md` (Wave 2)
```

---

### E. `packages/shared-render/` — ASCII snapshot framework (impl in Plan 03)

**`packages/shared-render/package.json`:**

```json
{
  "name": "@evf/shared-render",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF shared render — ASCII grid model + snapshot matcher for INV-1 layout integrity testing.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "vitest": "4.1.5"
  }
}
```

**Note:** `vitest` is a `peerDependencies` not a `dependencies` because `expect` is consumed via the test harness in each consumer package; shared-render just imports types from `vitest`.

**`packages/shared-render/tsconfig.json`:**

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": [],
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

**`packages/shared-render/src/index.ts`:**

```typescript
/**
 * @evf/shared-render — placeholder.
 *
 * Wave 2 (Plan 03) implements:
 * - `ascii-grid.ts` — character-precision grid model per INV-1 §7.14.4 ck 11-15
 * - `snapshot.ts` — `matchAsciiFixture(grid, path)` Vitest 4 matcher
 * - `fixtures/status-hud-baseline.txt` — example INV-1 fixture (D-1.16)
 *
 * @see Specs.md §7.1a (Layout Integrity Invariants) + §7.14.4 (verification checklist)
 * @see docs/architecture/0001-layered-ui-model.md (Wave 2)
 */
export const PACKAGE_NAME = '@evf/shared-render';
```

**`packages/shared-render/README.md`:**

```markdown
# @evf/shared-render

ASCII grid model + INV-1 layout integrity snapshot matcher for Vitest 4.

**Status:** Phase 1 scaffold. Real implementation (`ascii-grid.ts` + `snapshot.ts` + `fixtures/`) lands in Plan 03 (Wave 2).

## Pattern (D-1.11 + INV-1)

- Character-precision rectangular grid (`AsciiGrid` class — every row exactly `width` cells)
- LF line endings, no BOM, no trailing whitespace (enforced by `.gitattributes` + `.editorconfig` from Plan 01)
- Wraps Vitest 4's built-in `expect.toMatchFileSnapshot()` via custom matcher `matchAsciiFixture(grid, fixturePath)`
- Phase 4a expands to full column-misalignment reporting per INV-1 ck 11

## Consumers

- `@evf/g2-app` Phase 4a (real Status HUD + raster panel snapshots)
- `@evf/foundry-module` Phase 2 (less critical — module UI is Foundry-shaped, not G2 ASCII)
- `packages/g2-app/src/__tests__/example-status-hud.test.ts` (Plan 03 D-1.16 wire-up demo)

## See also

- `Specs.md` §7.1a, §7.14.4 ck 11-15
- `docs/architecture/0001-layered-ui-model.md` (Wave 2)
```

---

**Step 9: Reinstall workspace to link new packages:**

```bash
pnpm install
pnpm ls --depth=0  # MUST show 5 new @evf/* packages
pnpm -r exec tsc --noEmit  # all 5 packages green (placeholder src/index.ts compiles)
```

**What NOT to do:**
- Do NOT put runtime deps in any package yet (Phase 2/3/4a add them when needed)
- Do NOT add `"composite": true` (RESEARCH.md anti-pattern line 376)
- Do NOT generate `dist/` (placeholder packages don't build; `pnpm build` works because tsup/vite scripts exist but `dist/` is gitignored)
- Do NOT write the `module.json` Foundry manifest for `foundry-module` (Phase 2 task — would crash Foundry on load)
- Do NOT create per-package `vitest.config.ts` (Plan 03 only adds one for g2-app when example test needs happy-dom env)
  </action>
  <verify>
<automated>
ls packages/g2-app/{package.json,tsconfig.json,vite.config.ts,README.md} packages/g2-app/src/index.ts
ls packages/bridge/{package.json,tsconfig.json,README.md} packages/bridge/src/index.ts
ls packages/foundry-module/{package.json,tsconfig.json,README.md} packages/foundry-module/src/index.ts
ls packages/shared-protocol/{package.json,tsconfig.json,README.md} packages/shared-protocol/src/index.ts
ls packages/shared-render/{package.json,tsconfig.json,README.md} packages/shared-render/src/index.ts
grep -q '"@evf/g2-app"' packages/g2-app/package.json
grep -q '"@evf/bridge"' packages/bridge/package.json
grep -q '"@evf/foundry-module"' packages/foundry-module/package.json
grep -q '"@evf/shared-protocol"' packages/shared-protocol/package.json
grep -q '"@evf/shared-render"' packages/shared-render/package.json
grep -q '"private": true' packages/g2-app/package.json
grep -q 'workspace:\*' packages/g2-app/package.json
grep -q 'extends.*tsconfig\.base\.json' packages/g2-app/tsconfig.json
grep -q 'extends.*tsconfig\.base\.json' packages/bridge/tsconfig.json
grep -q 'extends.*tsconfig\.base\.json' packages/foundry-module/tsconfig.json
grep -q 'extends.*tsconfig\.base\.json' packages/shared-protocol/tsconfig.json
grep -q 'extends.*tsconfig\.base\.json' packages/shared-render/tsconfig.json
pnpm install --frozen-lockfile || pnpm install
pnpm ls --depth=0 | grep -E '@evf/(g2-app|bridge|foundry-module|shared-protocol|shared-render)' | wc -l | grep -q '^5$'
pnpm -r exec tsc --noEmit
echo "OK"
</automated>
  </verify>
  <done>
- All 5 new packages scaffolded with @evf/{name}@0.1.0-alpha.0 private
- Every per-package tsconfig.json extends ../../tsconfig.base.json
- g2-app + shared-render have correct DOM lib variants; bridge/shared-protocol have ES2023-only
- workspace:* protocol used for internal deps (g2-app → shared-protocol+shared-render)
- `pnpm ls --depth=0` shows 5 new packages
- `pnpm -r exec tsc --noEmit` exit 0 across all 5 packages
- No runtime dependencies installed yet (Phase 2+ adds them)
- pnpm-lock.yaml updated to reflect new workspace pkgs (commit it)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fold-in `tests/phase-0/` → `packages/validation-harness/` — move all files, preserve hardware-script executability, update path resolution (Pitfall 8)</name>
  <files>
    packages/validation-harness/package.json
    packages/validation-harness/tsconfig.json
    packages/validation-harness/upng-js.d.ts
    packages/validation-harness/README.md
    packages/validation-harness/src/lib/schemas.ts
    packages/validation-harness/src/lib/output.ts
    packages/validation-harness/src/lib/stats.ts
    packages/validation-harness/src/lib/branch-decision.ts
    packages/validation-harness/src/lib/hub.ts
    packages/validation-harness/scripts/10-0-1-r1-timing.ts
    packages/validation-harness/scripts/10-0-2-image-format.ts
    packages/validation-harness/scripts/10-0-3-ble-multi-env.ts
    packages/validation-harness/scripts/10-0-7-dle-sustained.ts
    packages/validation-harness/scripts/10-0-8-queue-depth.ts
    packages/validation-harness/scripts/10-0-9-palette-calibration.ts
    packages/validation-harness/scripts/midiqol-config-probe.ts
    packages/validation-harness/scripts/run-all.ts
    packages/validation-harness/foundry-modules/midiqol-probe-module/
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 274-300 — validation-harness target tree; lines 463-470 — Pitfall 8 path drift; Open Question 1)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-1.02 fold-in; specifics §validation-harness path contract)
    - .planning/phases/00-validation-gates/00-CONTEXT.md (D-15 promotion policy)
    - .planning/phases/00-validation-gates/00-01-SUMMARY.md (Phase 0 Plan 01 — _shared utilities + ADR stubs)
    - .planning/phases/00-validation-gates/00-02-SUMMARY.md (Phase 0 Plan 02 — MidiQOL probe + run-all)
    - .planning/phases/00-validation-gates/00-03-SUMMARY.md (Phase 0 Plan 03 — 6 hardware scripts + upng-js.d.ts ambient)
    - tests/phase-0/_shared/output.ts (CRITICAL — current path resolution that must be preserved)
    - tests/phase-0/upng-js.d.ts (ambient module declaration — must stay standalone .d.ts)
  </read_first>
  <action>
**CRITICAL POLICY (RESEARCH Open Question 1):** Hardware-bound scripts stay in `scripts/` (tsx-executable), NOT `tests/` (Vitest-runnable). Reason: Phase 0 Plan 04 (still pending) needs to execute these scripts against real hardware via `pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts` after Even Hub access grant. Vitest's discovery + reporter machinery would complicate the operational workflow. **Only software-only smoke tests** (path-resolution.test.ts, future MidiQOL probe wrappers if any) go in `tests/`.

---

**Step 1: Create `packages/validation-harness/package.json`:**

```json
{
  "name": "@evf/validation-harness",
  "version": "0.1.0-alpha.0",
  "private": true,
  "type": "module",
  "description": "EVF validation harness — Phase 0 hardware/SDK validation tests + future re-validation entry point. Folded from tests/phase-0/ during Phase 1 per D-15.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "validate:all": "tsx scripts/run-all.ts",
    "validate:r1-timing": "tsx scripts/10-0-1-r1-timing.ts",
    "validate:image-format": "tsx scripts/10-0-2-image-format.ts",
    "validate:ble-multi-env": "tsx scripts/10-0-3-ble-multi-env.ts",
    "validate:dle-sustained": "tsx scripts/10-0-7-dle-sustained.ts",
    "validate:queue-depth": "tsx scripts/10-0-8-queue-depth.ts",
    "validate:palette-calibration": "tsx scripts/10-0-9-palette-calibration.ts",
    "validate:midiqol-probe": "tsx scripts/midiqol-config-probe.ts"
  },
  "dependencies": {
    "csv-stringify": "6.5.2",
    "upng-js": "2.1.0",
    "zod": "4.4.3"
  }
}
```

**Step 2: Create `packages/validation-harness/tsconfig.json` — includes `*.d.ts` glob (Open Question 1):**

```jsonc
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2023"],
    "types": ["node"],
    "outDir": "dist",
    "rootDir": "."
  },
  "include": ["src/**/*", "scripts/**/*", "tests/**/*", "*.d.ts"],
  "exclude": ["dist", "node_modules", "foundry-modules"]
}
```

**WHY `rootDir: "."`:** scripts are at `scripts/` not `src/scripts/` per Open Question 1 recommendation — keep operational entry-points clearly separated from library code. `include` covers all three trees + the upng-js.d.ts.
**WHY `exclude: foundry-modules`:** that subdirectory contains Foundry-side artifact (not Node TS); shouldn't be type-checked.

**Step 3: Move files from `tests/phase-0/` → `packages/validation-harness/`:**

```bash
mkdir -p packages/validation-harness/{src/lib,scripts,tests,foundry-modules}

# Shared utilities → src/lib/
mv tests/phase-0/_shared/schemas.ts        packages/validation-harness/src/lib/schemas.ts
mv tests/phase-0/_shared/output.ts         packages/validation-harness/src/lib/output.ts
mv tests/phase-0/_shared/stats.ts          packages/validation-harness/src/lib/stats.ts
mv tests/phase-0/_shared/branch-decision.ts packages/validation-harness/src/lib/branch-decision.ts
mv tests/phase-0/_shared/hub.ts            packages/validation-harness/src/lib/hub.ts

# Hardware test scripts → scripts/
mv tests/phase-0/10-0-1-r1-timing.ts            packages/validation-harness/scripts/10-0-1-r1-timing.ts
mv tests/phase-0/10-0-2-image-format.ts         packages/validation-harness/scripts/10-0-2-image-format.ts
mv tests/phase-0/10-0-3-ble-multi-env.ts        packages/validation-harness/scripts/10-0-3-ble-multi-env.ts
mv tests/phase-0/10-0-7-dle-sustained.ts        packages/validation-harness/scripts/10-0-7-dle-sustained.ts
mv tests/phase-0/10-0-8-queue-depth.ts          packages/validation-harness/scripts/10-0-8-queue-depth.ts
mv tests/phase-0/10-0-9-palette-calibration.ts  packages/validation-harness/scripts/10-0-9-palette-calibration.ts
mv tests/phase-0/midiqol-config-probe.ts         packages/validation-harness/scripts/midiqol-config-probe.ts
mv tests/phase-0/run-all.ts                      packages/validation-harness/scripts/run-all.ts

# Ambient module declaration → top level (Open Question 1 recommendation)
mv tests/phase-0/upng-js.d.ts packages/validation-harness/upng-js.d.ts

# Foundry mini-module → foundry-modules/
mv tests/phase-0/midiqol-probe-module packages/validation-harness/foundry-modules/midiqol-probe-module

# Remove obsolete Phase 0 files (workspace inherits all these now)
rm tests/phase-0/package.json
rm tests/phase-0/tsconfig.json
rm tests/phase-0/pnpm-lock.yaml
rm -rf tests/phase-0/node_modules
rm tests/phase-0/.gitignore
rm tests/phase-0/README.md

# Verify directory is empty, then remove
ls tests/phase-0/  # should print nothing
rmdir tests/phase-0
rmdir tests  # if no other tests/* dirs (verify first)
```

**Verify after move:**
```bash
test ! -d tests/phase-0
test -f packages/validation-harness/src/lib/schemas.ts
test -f packages/validation-harness/scripts/run-all.ts
test -f packages/validation-harness/upng-js.d.ts
test -d packages/validation-harness/foundry-modules/midiqol-probe-module
```

**Step 4: Update all import paths in moved files.** Every script under `scripts/` previously imported from `./_shared/*.js` (Phase 0 convention). Update to `../src/lib/*.js`:

```bash
# In every scripts/*.ts file:
#   Before: import { ... } from './_shared/schemas.js';
#   After:  import { ... } from '../src/lib/schemas.js';
```

For each file in `packages/validation-harness/scripts/*.ts`, use the Read tool first then the Edit tool to update the import path. Files affected (all 8 scripts):
- `10-0-1-r1-timing.ts`, `10-0-2-image-format.ts`, `10-0-3-ble-multi-env.ts`, `10-0-7-dle-sustained.ts`, `10-0-8-queue-depth.ts`, `10-0-9-palette-calibration.ts`, `midiqol-config-probe.ts`, `run-all.ts`

Pattern replacement (per file):
- `from './_shared/schemas.js'` → `from '../src/lib/schemas.js'`
- `from './_shared/output.js'` → `from '../src/lib/output.js'`
- `from './_shared/stats.js'` → `from '../src/lib/stats.js'`
- `from './_shared/branch-decision.js'` → `from '../src/lib/branch-decision.js'`
- `from './_shared/hub.js'` → `from '../src/lib/hub.js'`

**Step 5: Update `packages/validation-harness/src/lib/output.ts` for repo-root path resolution (Pitfall 8 + RESEARCH Open Question 8).** Read the current implementation first; the file currently uses something like `path.join('docs/perf/phase-0/', filename)` resolved against `process.cwd()`. Replace with explicit repo-root resolution:

```typescript
// At top of file, add:
import path from 'node:path';
import { fileURLToPath } from 'node:url';

// Resolve repo-root reliably regardless of cwd. After fold-in, the file lives at
// packages/validation-harness/src/lib/output.ts — 4 levels up = repo-root.
// EVF_REPO_ROOT override allows CI / custom test envs to redirect (Pitfall 8 + Open Question 8).
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const DEFAULT_REPO_ROOT = path.resolve(__dirname, '../../../..');
const REPO_ROOT = process.env.EVF_REPO_ROOT ?? DEFAULT_REPO_ROOT;
const OUTPUT_DIR = path.join(REPO_ROOT, 'docs', 'perf', 'phase-0');

// Use OUTPUT_DIR everywhere writeJsonEvidence/writeCsvEvidence builds the target file path.
// (Replace the existing 'docs/perf/phase-0/' string concatenation with `path.join(OUTPUT_DIR, filename)`.)
```

**Concrete edits required (Read file first, then Edit):** find every occurrence of the literal `'docs/perf/phase-0'` or `"docs/perf/phase-0"` in `output.ts` and replace with the `OUTPUT_DIR` constant. Adapt to the precise current shape (Phase 0 writer style).

**Step 6: Create `packages/validation-harness/README.md`:**

```markdown
# @evf/validation-harness

EVF validation harness — Phase 0 hardware/SDK validation tests + future re-validation entry point.

**Status:** Folded from `tests/phase-0/` during Phase 1 (D-1.02 / Phase 0 D-15). Phase 0 hardware-bound tests are still operational; the closure step (Phase 0 Plan 04) executes them against real hardware once Even Hub developer access is granted.

## Layout

```
packages/validation-harness/
├── package.json                  # @evf/validation-harness@0.1.0-alpha.0 private
├── tsconfig.json                 # extends ../../tsconfig.base.json
├── upng-js.d.ts                  # ambient module declaration (Phase 0 Plan 03 deviation #1)
├── src/lib/                      # shared utilities (was tests/phase-0/_shared/)
│   ├── schemas.ts                # Zod EvidenceMeta + discriminated unions
│   ├── output.ts                 # writeJsonEvidence + writeCsvEvidence → repo-root docs/perf/phase-0/
│   ├── stats.ts                  # percentile R-7, hartiganDipTest, ci95
│   ├── branch-decision.ts        # deriveBranch + DEFAULT_THRESHOLDS (D-09/D-10/D-12)
│   └── hub.ts                    # loadHub + isHubAvailable (env-only credential)
├── scripts/                      # hardware test scripts — tsx-executable
│   ├── 10-0-1-r1-timing.ts
│   ├── 10-0-2-image-format.ts
│   ├── 10-0-3-ble-multi-env.ts
│   ├── 10-0-7-dle-sustained.ts
│   ├── 10-0-8-queue-depth.ts
│   ├── 10-0-9-palette-calibration.ts
│   ├── midiqol-config-probe.ts
│   └── run-all.ts                # orchestrator with --skip-hardware flag
├── tests/
│   └── path-resolution.test.ts   # smoke test for Pitfall 8 (writer → repo-root)
└── foundry-modules/
    └── midiqol-probe-module/     # Foundry-side artifact for MIDIQ-01 probe
```

## How to run

### Software-only smoke (Vitest, no hardware)
```bash
pnpm test --filter @evf/validation-harness --run
```

### Hardware-bound (requires Even Hub access + real G2/R1/phone)
```bash
# Skip hardware (Pattern 3 capability-negotiation skip, exit 2)
pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts --skip-hardware

# Full run (with hardware grant)
EVEN_HUB_TOKEN=... pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts
```

Evidence is always written to **repo-root** `docs/perf/phase-0/` regardless of cwd, via `EVF_REPO_ROOT` env override or `path.resolve(import.meta.dirname, '../../../..')` default.

### Override evidence root (CI / sandbox)
```bash
EVF_REPO_ROOT=/tmp/evf-test pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts --skip-hardware
```

## Migration from `tests/phase-0/`

| Was | Is now |
|-----|--------|
| `tests/phase-0/package.json` | DELETED (inherits workspace) |
| `tests/phase-0/tsconfig.json` | DELETED (extends `../../tsconfig.base.json`) |
| `tests/phase-0/_shared/{schemas,output,stats,branch-decision,hub}.ts` | `packages/validation-harness/src/lib/{schemas,output,stats,branch-decision,hub}.ts` |
| `tests/phase-0/10-0-*.ts` + `midiqol-config-probe.ts` + `run-all.ts` | `packages/validation-harness/scripts/*.ts` |
| `tests/phase-0/upng-js.d.ts` | `packages/validation-harness/upng-js.d.ts` |
| `tests/phase-0/midiqol-probe-module/` | `packages/validation-harness/foundry-modules/midiqol-probe-module/` |

## See also

- `.planning/phases/00-validation-gates/00-{01,02,03}-SUMMARY.md` — Phase 0 plans context
- `.planning/phases/01-foundation/01-CONTEXT.md` D-1.02 — fold-in decision
- `docs/architecture/0005-phase0-go-no-go.md` — ADR consuming validation evidence
```

**Step 7: Verify type-check green:**

```bash
pnpm install
pnpm --filter @evf/validation-harness exec tsc --noEmit
```

Must exit 0. Phase 0 already proved this config + content green — only new variable is import path rewrites + output.ts path resolution.

**What NOT to do:**
- Do NOT convert scripts to Vitest test files (Open Question 1 — keep operational tsx executability for Phase 0 closure)
- Do NOT inline `declare module 'upng-js'` into scripts (Phase 0 Plan 03 TS2665 — must stay standalone .d.ts)
- Do NOT leave `tests/phase-0/` partial (orchestrator config gate WAVE-1-G4 requires complete removal)
- Do NOT hardcode `OUTPUT_DIR = 'docs/perf/phase-0'` relative — Pitfall 8 demands explicit repo-root resolution
- Do NOT change DEFAULT_THRESHOLDS values in `branch-decision.ts` (D-12 strict-numeric immutability — Phase 0 closure depends on these constants)
  </action>
  <verify>
<automated>
test ! -d tests/phase-0 || (echo "FAIL: tests/phase-0/ still exists"; exit 1)
test -d packages/validation-harness/src/lib
test -d packages/validation-harness/scripts
test -d packages/validation-harness/foundry-modules/midiqol-probe-module
test -f packages/validation-harness/upng-js.d.ts
test -f packages/validation-harness/src/lib/schemas.ts
test -f packages/validation-harness/src/lib/output.ts
test -f packages/validation-harness/src/lib/stats.ts
test -f packages/validation-harness/src/lib/branch-decision.ts
test -f packages/validation-harness/src/lib/hub.ts
test -f packages/validation-harness/scripts/run-all.ts
test -f packages/validation-harness/scripts/10-0-1-r1-timing.ts
test -f packages/validation-harness/scripts/midiqol-config-probe.ts
grep -q 'EVF_REPO_ROOT' packages/validation-harness/src/lib/output.ts
grep -q 'import.meta.url' packages/validation-harness/src/lib/output.ts || grep -q 'import.meta.dirname' packages/validation-harness/src/lib/output.ts
grep -q 'p50_min_kbps: 200' packages/validation-harness/src/lib/branch-decision.ts
grep -q 'p99_min_kbps: 100' packages/validation-harness/src/lib/branch-decision.ts
! grep -r "from './_shared/" packages/validation-harness/scripts/ 2>/dev/null || (echo "FAIL: stale _shared/ imports"; exit 1)
grep -r "from '../src/lib/" packages/validation-harness/scripts/ | head -5
pnpm install
pnpm --filter @evf/validation-harness exec tsc --noEmit
echo "OK"
</automated>
  </verify>
  <done>
- WAVE-1-G4 green: `[ ! -d tests/phase-0 ]` true
- All Phase 0 _shared/*.ts content preserved (no deletions, only moves + import path updates)
- All 8 scripts moved to `scripts/` (per Open Question 1, NOT `tests/`)
- `upng-js.d.ts` at package top-level (per Open Question 1, NOT moved into `src/`)
- `foundry-modules/midiqol-probe-module/` preserved as Foundry-side artifact (excluded from tsc per `exclude` config)
- `output.ts` path resolution rewritten to repo-root-aware (Pitfall 8 mitigation)
- `pnpm --filter @evf/validation-harness exec tsc --noEmit` exit 0
- All import paths updated from `./_shared/` to `../src/lib/`
- Per-package scripts (`validate:r1-timing`, etc.) exposed in `package.json` for easy hardware execution
- DEFAULT_THRESHOLDS constants preserved unchanged (D-12 immutability)
  </done>
</task>

<task type="auto">
  <name>Task 3: Path resolution smoke test + workspace verification — WAVE-1-G1..G5 final acceptance</name>
  <files>
    packages/validation-harness/tests/path-resolution.test.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 463-470 — Pitfall 8 fix recommendation; Open Question 8)
    - .planning/phases/01-foundation/01-VALIDATION.md (WAVE-1-G5 gate definition)
    - packages/validation-harness/src/lib/output.ts (the file under test — Task 2 just modified it)
  </read_first>
  <action>
**Step 1: Create `packages/validation-harness/tests/path-resolution.test.ts` — smoke test that the writer targets repo-root, NOT package-local:**

```typescript
/**
 * Smoke test for Pitfall 8 (RESEARCH.md lines 463-470): validation-harness writer MUST
 * resolve `docs/perf/phase-0/` to the repo root regardless of cwd.
 *
 * Without this, post-fold-in evidence could silently land in
 * `packages/validation-harness/docs/perf/phase-0/` instead of the canonical repo-root
 * path that Phase 0 ADR-0005 / ADR-0006 cite as evidence locations.
 *
 * @see .planning/phases/01-foundation/01-RESEARCH.md Pitfall 8 + Open Question 8
 */
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

describe('output.ts path resolution (Pitfall 8 mitigation)', () => {
  const ORIGINAL_ENV = process.env.EVF_REPO_ROOT;

  beforeEach(() => {
    delete process.env.EVF_REPO_ROOT;
  });

  afterEach(() => {
    if (ORIGINAL_ENV === undefined) {
      delete process.env.EVF_REPO_ROOT;
    } else {
      process.env.EVF_REPO_ROOT = ORIGINAL_ENV;
    }
  });

  it('resolves repo-root via 4-level up traversal from src/lib/output.ts by default', async () => {
    // Compute expected: this test file lives at packages/validation-harness/tests/
    // output.ts lives at packages/validation-harness/src/lib/
    // From output.ts, ../../../.. = repo root
    const testFileDir = path.dirname(fileURLToPath(import.meta.url));
    // tests/ -> validation-harness/ -> packages/ -> repo-root
    const expectedRepoRoot = path.resolve(testFileDir, '../../..');
    const expectedOutputDir = path.join(expectedRepoRoot, 'docs', 'perf', 'phase-0');

    // Import the writer module — it computes OUTPUT_DIR at module load.
    // We re-import to get a fresh module evaluation with EVF_REPO_ROOT unset.
    const { OUTPUT_DIR } = await import('../src/lib/output.js');

    expect(OUTPUT_DIR).toBe(expectedOutputDir);
    expect(OUTPUT_DIR.endsWith(path.join('docs', 'perf', 'phase-0'))).toBe(true);
    // Critically: must NOT point inside the package
    expect(OUTPUT_DIR).not.toContain(path.join('packages', 'validation-harness', 'docs'));
  });

  it('honors EVF_REPO_ROOT env override (CI / sandbox path)', async () => {
    const customRoot = '/tmp/evf-test-root';
    process.env.EVF_REPO_ROOT = customRoot;

    // Bust module cache to re-evaluate OUTPUT_DIR with new env
    // (Vitest hoists imports — use dynamic import + vi.resetModules for clean test)
    const { resetModules } = await import('vitest').then((m) => ({
      // @ts-expect-error vi global may not be typed for ESM import — Vitest doc pattern
      resetModules: (m.vi ?? (globalThis as { vi?: { resetModules?: () => void } }).vi)?.resetModules,
    }));
    if (typeof resetModules === 'function') resetModules();

    const { OUTPUT_DIR } = await import('../src/lib/output.js?env=' + Date.now());

    expect(OUTPUT_DIR).toBe(path.join(customRoot, 'docs', 'perf', 'phase-0'));
  });
});
```

**Note on the second test:** If Vitest module caching makes the env-override re-import tricky, simplify by exporting a `computeOutputDir(env: NodeJS.ProcessEnv, currentDir: string): string` helper from `output.ts` and unit-test that pure function directly (no module re-evaluation needed). The executor MAY refactor the second test to use that helper-based approach if cleaner; the critical guarantee is that BOTH paths are exercised (default + env override).

**Step 2: Create per-package vitest config for validation-harness if needed.** Most likely not needed — root `vitest.config.ts` already has `projects: ['packages/*']` and the harness inherits Node env (default). Confirm by running:

```bash
pnpm test --filter @evf/validation-harness --run
```

If discovery fails or environment is wrong (uses happy-dom instead of node), THEN create `packages/validation-harness/vitest.config.ts`:

```typescript
import { defineProject } from 'vitest/config';

export default defineProject({
  extends: true,  // CRITICAL: inherit root config (Pitfall 3)
  test: {
    name: 'validation-harness',
    environment: 'node',
    include: ['tests/**/*.test.ts'],
  },
});
```

But default workspace inheritance should be fine — try root config first.

**Step 3: Final Wave 1 verification suite:**

```bash
# WAVE-1-G1: 6 packages linked
pnpm install --frozen-lockfile
pnpm ls --depth=0 | grep -E '@evf/' | wc -l  # MUST be 6

# WAVE-1-G2: TS green across all packages
pnpm -r exec tsc --noEmit

# WAVE-1-G3: validation-harness Vitest test discoverable
pnpm test --filter @evf/validation-harness --run

# WAVE-1-G4: tests/phase-0/ removed (verified in Task 2)
test ! -d tests/phase-0

# WAVE-1-G5: writer path resolves to repo-root
pnpm test --filter @evf/validation-harness tests/path-resolution.test.ts --run
```

All exits 0 = Wave 1 complete; Plan 03 (Wave 2 — ADRs + snapshot + CI + INV-3 closure) can proceed.

**What NOT to do:**
- Do NOT skip the env-override test — Pitfall 8 calls for BOTH paths exercised
- Do NOT hardcode the expected repo-root absolute path — use `import.meta.url` relative resolution so the test works in any clone location
- Do NOT add Phase-0-hardware tests to `tests/` — they stay in `scripts/` per Open Question 1
- Do NOT enable per-package `vitest.config.ts` unless root config genuinely fails (Pitfall 3 — extends:true required if you do)
  </action>
  <verify>
<automated>
test -f packages/validation-harness/tests/path-resolution.test.ts
grep -q 'EVF_REPO_ROOT' packages/validation-harness/tests/path-resolution.test.ts
grep -q 'OUTPUT_DIR' packages/validation-harness/tests/path-resolution.test.ts
pnpm install --frozen-lockfile
pnpm ls --depth=0 2>/dev/null | grep -cE '@evf/(g2-app|bridge|foundry-module|shared-protocol|shared-render|validation-harness)' | grep -q '^6$'
pnpm -r exec tsc --noEmit
pnpm test --filter @evf/validation-harness --run
echo "OK"
</automated>
  </verify>
  <done>
- WAVE-1-G1 green: `pnpm ls --depth=0` shows all 6 @evf/* packages
- WAVE-1-G2 green: `pnpm -r exec tsc --noEmit` exit 0
- WAVE-1-G3 green: `pnpm test --filter @evf/validation-harness --run` exit 0 (path-resolution.test.ts passes; if any Phase 0 script wrappers exist they pass or skip cleanly per Pattern 3)
- WAVE-1-G4 green: tests/phase-0/ directory removed (verified Task 2)
- WAVE-1-G5 green: path-resolution.test.ts confirms repo-root targeting + env override
- Phase 0 Plan 04 unblocked: `pnpm --filter @evf/validation-harness validate:all` works just like the old `pnpm exec tsx run-all.ts`
- Workspace fully populated: ready for Plan 03 to add ADRs + snapshot framework consuming `@evf/shared-render` + INV-3 atomic closure
  </done>
</task>

</tasks>

<verification>
**Phase 1 Wave 1 acceptance — all 5 gates GREEN:**

```bash
# WAVE-1-G1: 6 packages
pnpm install --frozen-lockfile
pnpm ls --depth=0 | grep -cE '@evf/' | grep -q '^6$'

# WAVE-1-G2: TS green
pnpm -r exec tsc --noEmit

# WAVE-1-G3: validation-harness test runs
pnpm test --filter @evf/validation-harness --run

# WAVE-1-G4: Phase 0 fold-in complete
test ! -d tests/phase-0

# WAVE-1-G5: path resolution preserved
grep -q 'EVF_REPO_ROOT' packages/validation-harness/src/lib/output.ts
```

**Sanity-check the workspace shape:**

```bash
find packages -maxdepth 2 -name 'package.json' | sort
# Expected output:
# packages/bridge/package.json
# packages/foundry-module/package.json
# packages/g2-app/package.json
# packages/shared-protocol/package.json
# packages/shared-render/package.json
# packages/validation-harness/package.json
```

**Verify Phase 0 Plan 04 still operational:**

```bash
# Smoke run with --skip-hardware (Pattern 3 — should exit 2)
pnpm --filter @evf/validation-harness exec tsx scripts/run-all.ts --skip-hardware
echo $?  # Expected: 2 (skip-cases per Phase 0 D-08 Pattern 3 uniform)
```
</verification>

<success_criteria>
- 6 packages exist with `@evf/{name}@0.1.0-alpha.0` private, `"type": "module"`
- All packages link via `workspace:*` where applicable (g2-app → shared-protocol + shared-render; bridge → shared-protocol; foundry-module → shared-protocol)
- Every per-package `tsconfig.json` extends `../../tsconfig.base.json` (inherits strict + 5 flags)
- `tests/phase-0/` directory **completely removed**
- All Phase 0 _shared utilities preserved at `packages/validation-harness/src/lib/`
- All Phase 0 hardware scripts preserved at `packages/validation-harness/scripts/` and still executable via tsx
- `upng-js.d.ts` at package top-level (ambient declaration intact)
- Foundry mini-module preserved at `packages/validation-harness/foundry-modules/midiqol-probe-module/`
- Writer path resolution rewritten to repo-root-aware (verified by smoke test)
- All 5 WAVE-1-G* gates GREEN
- Plan 03 unblocked (can consume `@evf/shared-render` workspace package for example snapshot test)
- Phase 0 Plan 04 unblocked (can still run `validate:*` scripts via tsx)
</success_criteria>

## Truths

This plan upholds the following CONTEXT.md decisions:

- **D-1.01** — 5 packages + 1 fold-in materialized: `packages/{g2-app,bridge,foundry-module,shared-protocol,shared-render}` scaffolded in Task 1; `packages/validation-harness/` folded in Task 2; `foundry-mcp` correctly OMITTED (V2 OPZIONALE Phase 11)
- **D-1.02** — `tests/phase-0/*.ts` promoted to `packages/validation-harness/`; `_shared/` becomes `src/lib/`; original `tests/phase-0/` directory **removed at end of Wave 1** (verified WAVE-1-G4) — Tasks 2+3
- **Phase 0 D-15** — Hardware execution still targets `packages/validation-harness/` via `pnpm --filter` + tsx scripts; evidence paths in writer files updated to repo-root-aware resolution (Pitfall 8 mitigation)

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` documenting:
- 6 packages scaffolded with their public surface (package.json `name`, `version`, `private`, `dependencies`)
- Phase 0 fold-in mapping table (was tests/phase-0/X → is packages/validation-harness/Y)
- Path-resolution rewrite snippet in output.ts (Pitfall 8 mitigation)
- WAVE-1-G1..G5 gate results
- Note for Phase 0 Plan 04: how to invoke the harness post-fold-in (`pnpm --filter @evf/validation-harness validate:all`)
- Hand-off to Plan 03 (Wave 2): `@evf/shared-render` exists as workspace package ready for ascii-grid.ts + snapshot.ts + fixture + example test
</output>
