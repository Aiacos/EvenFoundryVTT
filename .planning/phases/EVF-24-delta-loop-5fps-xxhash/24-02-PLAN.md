---
phase: 24-delta-loop-5fps-xxhash
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/g2-app/src/engine/layer-manager.ts
  - packages/g2-app/src/engine/__tests__/layer-manager.test.ts
  - packages/g2-app/src/internal/boot-engine-core.ts
autonomous: true
requirements: [RPROMO-01]

must_haves:
  truths:
    - "LayerManager canvas mode drives the HUD via HudDeltaDriver, not via the Phase 20 naive _startDeltaRecomposite"
    - "The 3 naive-driver symbols (_startDeltaRecomposite, _stopDeltaRecomposite, _deltaRecompositeUnsub) are fully removed — zero references remain (INV-4 zero dead code)"
    - "On canvas-mode page flush, the first frame pushes all 4 tiles and arms the event-driven debounced loop"
    - "Engine teardown stops the driver (cancels pending timer + releases subscriptions) via LayerManager.disposeSubscriptions"
    - "The full canvas suite (Phases 20-23) and the INV-1 raster golden fixture remain green after the swap (no regression, D-24.6)"
  artifacts:
    - path: "packages/g2-app/src/engine/layer-manager.ts"
      provides: "HudDeltaDriver injection + _flushPage uses driver.runFirstFrame()+start(); naive driver symbols removed"
      contains: "HudDeltaDriver"
    - path: "packages/g2-app/src/internal/boot-engine-core.ts"
      provides: "HudDeltaDriver constructed and injected into LayerManager alongside compositor + wsEventBus"
      contains: "HudDeltaDriver"
  key_links:
    - from: "packages/g2-app/src/engine/layer-manager.ts"
      to: "packages/g2-app/src/engine/hud-delta-driver.ts"
      via: "driver.runFirstFrame() + driver.start() in _flushPage canvas branch; driver.stop() in disposeSubscriptions"
      pattern: "driver\\.(runFirstFrame|start|stop)"
    - from: "packages/g2-app/src/internal/boot-engine-core.ts"
      to: "packages/g2-app/src/engine/hud-delta-driver.ts"
      via: "new HudDeltaDriver({compositor, bridge, wsEvents}) injected into LayerManager"
      pattern: "new HudDeltaDriver"
---

<objective>
Integrate `HudDeltaDriver` (from Plan 24-01) into the engine and REMOVE the Phase 20 naive
recomposite driver entirely. In `LayerManager`: delete `_startDeltaRecomposite`,
`_stopDeltaRecomposite`, and the `_deltaRecompositeUnsub` field (INV-4 zero dead code); replace the
`_flushPage` canvas branch's `_compositeAndPush()` + `_startDeltaRecomposite()` pair with
`await driver.runFirstFrame()` + `driver.start()`; replace `disposeSubscriptions()`'s
`_stopDeltaRecomposite()` with `driver.stop()`. In `boot-engine-core.ts`: construct a
`HudDeltaDriver` (compositor + bridge + wsEventBus) and inject it into `LayerManager`. Update the
LayerManager test to assert the driver is used and the old mechanism is gone (DL-07). Run the full
suite to prove no Phase 20-23 / INV-1 regression.

Purpose: Make the new delta loop the live HUD driver in canvas mode while satisfying INV-4 (no dead
leftovers) and D-24.6 (no regressions).
Output: modified LayerManager + boot-engine-core wiring + LayerManager test (DL-07); full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-24-delta-loop-5fps-xxhash/24-CONTEXT.md
@.planning/phases/EVF-24-delta-loop-5fps-xxhash/24-RESEARCH.md
@.planning/phases/EVF-24-delta-loop-5fps-xxhash/24-VALIDATION.md
@packages/g2-app/src/engine/hud-delta-driver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove naive driver + wire HudDeltaDriver into LayerManager</name>
  <files>packages/g2-app/src/engine/layer-manager.ts</files>
  <read_first>
    - packages/g2-app/src/engine/layer-manager.ts (the file being modified — naive symbols at: _deltaRecompositeUnsub field line 136; constructor wsEvents arg line 188-196; _flushPage lines 659-679 calling _compositeAndPush line 673 + _startDeltaRecomposite line 677; _compositeAndPush lines 704-709; disposeSubscriptions lines 721-723 calling _stopDeltaRecomposite; _startDeltaRecomposite lines 737-751; _stopDeltaRecomposite lines 758-763; JSDoc references at lines 86,91,105,113,116,120-130,178-184,392,450,642)
    - packages/g2-app/src/engine/hud-delta-driver.ts (the driver to inject — start/stop/runFirstFrame public API; HudDeltaDriverOpts)
    - packages/g2-app/src/engine/canvas-compositor.ts (CanvasCompositorLike — already a LayerManager dep)
    - packages/g2-app/src/hud/hud-poc-page.ts (pushHudTiles — still used by driver, not LayerManager directly after this change)
  </read_first>
  <action>
    Replace the LayerManager's naive recomposite mechanism with a `HudDeltaDriver` reference.
    Add an optional 5th constructor arg `driver?: HudDeltaDriver` stored as `this._deltaDriver:
    HudDeltaDriver | null` (import the type from `./hud-delta-driver.js`). Keep the existing 4th arg
    `wsEvents` for now ONLY if still referenced elsewhere; if `wsEvents` was used solely by the naive
    driver, remove the `_wsEvents` field and the 4th constructor arg too and update its JSDoc — but
    verify with grep first that no other method reads `this._wsEvents` before removing it (it is read
    only by `_startDeltaRecomposite` per the codebase read; safe to remove). Either way the driver
    must receive the wsEvents bus — done in boot-engine-core (Task 2) by passing it into the driver
    constructor, not LayerManager.

    DELETE entirely (INV-4 zero dead code):
    - the `_deltaRecompositeUnsub` field (line ~136),
    - the `_startDeltaRecomposite()` method (lines ~737-751),
    - the `_stopDeltaRecomposite()` method (lines ~758-763),
    - and (if `_wsEvents` is now unreferenced) the `_wsEvents` field + the `wsEvents` constructor arg
      + their JSDoc paragraphs.

    REWRITE `_flushPage()` canvas branch (currently lines 672-678): replace
    `await this._compositeAndPush(); this._startDeltaRecomposite();` with
    `if (this._deltaDriver !== null) { await this._deltaDriver.runFirstFrame(); await
    this._deltaDriver.start(); } else { await this._compositeAndPush(); }`. Keep `_compositeAndPush`
    as the no-driver fallback (2/3/4-arg construction path used by existing schema-select tests that
    omit a driver) — this preserves all current LayerManager tests AND keeps the first-frame push for
    the driverless path. Update the `_flushPage` JSDoc that mentions `_compositeAndPush()` +
    "Phase 24 replaces this" to describe the driver path.

    REWRITE `disposeSubscriptions()` (lines 721-723): call `this._deltaDriver?.stop()` instead of
    `this._stopDeltaRecomposite()`. Update its JSDoc (remove the `character.delta` listener wording;
    describe stopping the HudDeltaDriver).

    Sweep ALL remaining JSDoc/comment references to `_startDeltaRecomposite`,
    `_stopDeltaRecomposite`, `_deltaRecompositeUnsub`, and the "Phase 20 minimal event-driven driver
    — Phase 24 replaces this" wording; update or delete them so no stale references survive (INV-4).
    Keep `_compositeAndPush` and its JSDoc (still the driverless fallback). No bare `// TODO`. NO
    fenced code blocks in this plan — edit the real file.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && grep -nE "_startDeltaRecomposite|_stopDeltaRecomposite|_deltaRecompositeUnsub" packages/g2-app/src/engine/layer-manager.ts | grep -v '^#' | wc -l && corepack pnpm --filter @evf/g2-app exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "_startDeltaRecomposite|_stopDeltaRecomposite|_deltaRecompositeUnsub" packages/g2-app/src/engine/layer-manager.ts` returns 0 (all three naive symbols fully removed, including comments — INV-4).
    - `grep -n "HudDeltaDriver" packages/g2-app/src/engine/layer-manager.ts` matches (type imported + field typed).
    - `grep -nE "runFirstFrame|_deltaDriver\.start|_deltaDriver\?\.stop" packages/g2-app/src/engine/layer-manager.ts` shows the driver is wired in _flushPage and disposeSubscriptions.
    - `tsc --noEmit` for @evf/g2-app exits 0.
  </acceptance_criteria>
  <done>The 3 naive symbols are gone (grep=0), HudDeltaDriver is injected and drives _flushPage + disposeSubscriptions, _compositeAndPush remains as driverless fallback, typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Construct + inject HudDeltaDriver in boot-engine-core; add DL-07 LayerManager test</name>
  <files>packages/g2-app/src/internal/boot-engine-core.ts, packages/g2-app/src/engine/__tests__/layer-manager.test.ts</files>
  <read_first>
    - packages/g2-app/src/internal/boot-engine-core.ts (compositor created line 607; LayerManager constructed 4-arg line 613 `new LayerManager(bridge, debugMirror, compositor, wsEventBus)`; wsEventBus created step 5a line 543; teardown disposeSubscriptions line 1630)
    - packages/g2-app/src/engine/hud-delta-driver.ts (HudDeltaDriver constructor + HudDeltaDriverOpts: compositor, bridge, wsEvents)
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts (existing LayerManager test patterns — construction args, canvas-mode flush assertions, bridge/compositor/wsEvents mocks)
    - packages/g2-app/src/engine/layer-manager.ts (final constructor signature after Task 1; _flushPage driver path)
  </read_first>
  <action>
    In `boot-engine-core.ts`: after the compositor is created (line ~607) and before the LayerManager
    is constructed (line ~613), construct `const hudDeltaDriver = new HudDeltaDriver({ compositor,
    bridge, wsEvents: wsEventBus });` (import HudDeltaDriver from `../engine/hud-delta-driver.js`).
    Then change `new LayerManager(bridge, debugMirror, compositor, wsEventBus)` to pass the driver as
    the 5th arg: `new LayerManager(bridge, debugMirror, compositor, wsEventBus, hudDeltaDriver)` — OR,
    if Task 1 removed the LayerManager `wsEvents` 4th arg, pass `new LayerManager(bridge, debugMirror,
    compositor, hudDeltaDriver)` (match the final signature from Task 1). Update the step-7 boot
    comment block (lines ~593-613) to describe the HudDeltaDriver injection and that the driver now
    owns the delta loop (replacing the Phase 20 naive trigger). The existing teardown
    `layerManager.disposeSubscriptions()` (line ~1630) already routes to `driver.stop()` after Task 1
    — no extra teardown call needed.

    In `layer-manager.test.ts`: add a DL-07 test proving the canvas path uses the driver. Construct a
    LayerManager with a `HudDeltaDriver` test double (or a real driver with mocked deps) injected;
    drive a canvas-mode bundle/flush; assert `driver.runFirstFrame` and `driver.start` were called and
    that the removed naive mechanism is not invoked. Add a defense assertion that constructing
    WITHOUT a driver still works (driverless fallback uses `_compositeAndPush`) so existing
    schema-select tests remain valid. Name the test(s) `DL-07`.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm --filter @evf/g2-app test -- --run --testNamePattern 'DL-07' 2>&1 | tail -15 && corepack pnpm --filter @evf/g2-app exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "new HudDeltaDriver" packages/g2-app/src/internal/boot-engine-core.ts` matches (driver constructed at boot).
    - The LayerManager construction in boot-engine-core passes the driver (grep shows `hudDeltaDriver` in the `new LayerManager(...)` call).
    - DL-07 test(s) pass: canvas flush calls `driver.runFirstFrame` + `driver.start`; driverless construction still works.
    - `tsc --noEmit` for @evf/g2-app exits 0.
  </acceptance_criteria>
  <done>HudDeltaDriver is constructed and injected at boot; DL-07 proves the canvas path uses the driver and the driverless fallback survives; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression gate (Phases 20-23 canvas + INV-1 raster)</name>
  <files>packages/g2-app/src/engine/layer-manager.ts, packages/g2-app/src/internal/boot-engine-core.ts</files>
  <read_first>
    - packages/g2-app/src/engine/layer-manager.ts (final state after Tasks 1-2)
    - packages/g2-app/src/internal/boot-engine-core.ts (final state after Task 2)
    - .planning/phases/EVF-24-delta-loop-5fps-xxhash/24-VALIDATION.md (sampling rate: per-wave merge runs full `pnpm test`; RINV-01 golden fixture must be unchanged)
  </read_first>
  <action>
    Run the full workspace suite and resolve any regression introduced by the driver swap. If a
    Phase 20-23 canvas test or the INV-1 raster golden fixture (RINV-01) fails, the fix is in
    LayerManager/boot wiring (the driver swap must be behavior-preserving for the first frame: all 4
    tiles still pushed on canvas-mode boot) — NOT in the golden fixture or buildHudTiles (those are
    unchanged this phase). Do NOT edit `hud-raster-frame.ts`, the INV-1 fixture, or any Phase 20-23
    test to make them pass; if they fail it means the swap changed observable first-frame behavior and
    the wiring must be corrected. Confirm Biome + typecheck clean across the changed files.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm test -- --run 2>&1 | tail -25 && corepack pnpm --filter @evf/g2-app exec tsc --noEmit 2>&1 | tail -3 && corepack pnpm lint:ci 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full `pnpm test -- --run` is green (no failures); the count is ≥ the pre-phase baseline plus the new DL tests.
    - The RINV-01 / INV-1 raster golden fixture test passes WITHOUT any fixture edit (`git diff --stat` shows no change to the fixture file or to hud-raster-frame.ts).
    - `tsc --noEmit` exit 0 and `pnpm lint:ci` exit 0.
    - No naive-driver symbol anywhere in g2-app src: `grep -rnE "_startDeltaRecomposite|_stopDeltaRecomposite|_deltaRecompositeUnsub" packages/g2-app/src --include='*.ts'` returns nothing.
  </acceptance_criteria>
  <done>Full suite green, INV-1 fixture untouched, lint+typecheck clean, zero naive-driver symbols remain repo-wide.</done>
</task>

</tasks>

<artifacts_removed>
## Artifacts this phase REMOVES (INV-4 zero dead code)

From `packages/g2-app/src/engine/layer-manager.ts`:
- `_startDeltaRecomposite()` — private method (Phase 20 naive driver; subscribed only to character.delta, no debounce, no hash)
- `_stopDeltaRecomposite()` — private method
- `_deltaRecompositeUnsub` — private field
- (conditionally, if now unreferenced) the `_wsEvents` field + the `wsEvents` 4th constructor arg + their JSDoc

A repo-wide grep for these three symbols MUST return zero after this phase (verified in Task 3 acceptance criteria).
</artifacts_removed>

<verification>
- `corepack pnpm --filter @evf/g2-app test -- --run --testNamePattern 'DL-07'` — DL-07 green
- `corepack pnpm test -- --run` — full workspace green (Phases 20-23 + INV-1 no regression)
- `grep -rnE "_startDeltaRecomposite|_stopDeltaRecomposite|_deltaRecompositeUnsub" packages/g2-app/src --include='*.ts'` — empty (INV-4)
- `git diff --stat` shows no change to the INV-1 raster golden fixture or hud-raster-frame.ts
- `corepack pnpm --filter @evf/g2-app exec tsc --noEmit` + `corepack pnpm lint:ci` — exit 0
</verification>

<success_criteria>
- LayerManager canvas mode is driven by HudDeltaDriver; the 3 naive symbols are fully removed (INV-4).
- First-frame still pushes all 4 tiles; event-driven debounced loop armed at flush; teardown stops the driver.
- Full suite green with no Phase 20-23 / INV-1 regression (criterion #4, D-24.6).
</success_criteria>

<output>
Create `.planning/phases/EVF-24-delta-loop-5fps-xxhash/24-02-SUMMARY.md` when done
</output>
