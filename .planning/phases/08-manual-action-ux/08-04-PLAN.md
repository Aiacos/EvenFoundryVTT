---
phase: 08-manual-action-ux
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - packages/g2-app/src/panels/move-direction-picker.ts
  - packages/g2-app/src/panels/move-direction-picker.test.ts
  - packages/foundry-module/src/write-path/combat-movement-tracker.ts
  - packages/foundry-module/src/write-path/combat-movement-tracker.test.ts
  - packages/shared-protocol/src/payloads/movement.ts
  - packages/shared-protocol/src/payloads/movement.test.ts
  - packages/shared-protocol/src/index.ts
  - packages/foundry-module/src/module.ts
  - packages/foundry-module/src/module.test.ts
  - packages/g2-app/src/status-hud/status-hud-renderer.ts
  - packages/g2-app/src/status-hud/__tests__/status-hud-renderer.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - packages/shared-render/src/fixtures/move-picker.idle.it.txt
  - packages/shared-render/src/fixtures/move-picker.ne-selected.it.txt
  - packages/shared-render/src/fixtures/move-picker.exhausted.it.txt
  - packages/shared-render/src/fixtures/status-hud.move-chip.it.txt
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "MoveDirectionPicker z=2 overlay renders 8-direction (N/NE/E/SE/S/SW/W/NW) compass with ▶ active-direction indicator; scroll cycles direction; tap commits via move-token tool envelope"
    - "Movement budget shown as Mov 25/30 chip in StatusHudRenderer footer when MoveDirectionPicker is mounted; budget = actor.system.attributes.movement.walk - movementUsedThisTurn"
    - "combat-movement-tracker.ts (foundry-module) subscribes to updateToken hook; accumulates per-turn movement delta; resets on combat.turn advance via combat-reader.ts hook subscription pattern"
    - "Exhausted state (remaining ≤ 0) renders 'Movimento esaurito' hint + tap is disabled (no ws.send) — same envelope-suppression pattern as Plan 08-03 requiresTarget gate"
    - "INV-1 layout integrity holds character-perfect for 3 MoveDirectionPicker states (idle / NE-selected / exhausted) + 1 StatusHud move-chip variant"
  artifacts:
    - path: "packages/g2-app/src/panels/move-direction-picker.ts"
      provides: "MoveDirectionPicker OverlayPanel z=2 + 8-direction scroll-cycle + move-token tool.invoke emission"
      exports: ["MoveDirectionPicker", "MoveDirection", "MoveDirectionPickerCloseHandler"]
    - path: "packages/foundry-module/src/write-path/combat-movement-tracker.ts"
      provides: "registerMovementTracker(emit) — Hooks.on('updateToken') subscriber accumulating per-turn movement"
      exports: ["registerMovementTracker", "MovementState"]
    - path: "packages/shared-protocol/src/payloads/movement.ts"
      provides: "MovementBudgetPayloadSchema + R1_MOVEMENT_BUDGET_TYPE for combat-movement-tracker → g2-app StatusHud chip"
      exports: ["MovementBudgetPayloadSchema", "R1_MOVEMENT_BUDGET_TYPE", "MovementBudgetPayload"]
    - path: "packages/g2-app/src/status-hud/status-hud-renderer.ts"
      provides: "Extension — setMovementBudget(budget) public method + _renderMoveChip row in _renderStandard"
      contains: "setMovementBudget"
  key_links:
    - from: "packages/g2-app/src/panels/move-direction-picker.ts"
      to: "ws.send canonical EnvelopeSchema tool.invoke move-token"
      via: "tap → MoveTokenInputSchema args via direction → {x, y} delta"
      pattern: "toolId: 'move-token'"
    - from: "packages/foundry-module/src/write-path/combat-movement-tracker.ts"
      to: "Hooks.on('updateToken')"
      via: "per-token position diff → accumulator"
      pattern: "Hooks\\.on\\('updateToken'"
    - from: "packages/foundry-module/src/module.ts"
      to: "registerMovementTracker"
      via: "Hooks.once('ready') wiring after registerActionResultWatcher"
      pattern: "registerMovementTracker\\("
    - from: "packages/g2-app/src/status-hud/status-hud-renderer.ts"
      to: "renderer.setMovementBudget"
      via: "StatusHudLayer._onDelta dispatch on R1_MOVEMENT_BUDGET_TYPE envelope"
      pattern: "setMovementBudget\\("
---

<objective>
Wave 3 deliverable. Ships the **move-token action surface** (ACT-01 move variant):

  1. `MoveDirectionPicker` — z=2 OverlayPanel. Renders an 8-direction compass per CONTEXT.md §Specifics mockup:
     ```
     ┌──────────────────────────────────────────────┐
     │ MOVIMENTO  (rimangono 25 ft)                 │
     │        N                                     │
     │   NW  ▶NE                                    │
     │ W              E                             │
     │   SW    SE                                   │
     │        S                                     │
     │  [tap] commit  [long] annulla                │
     └──────────────────────────────────────────────┘
     ```
     Constructor: `(bridge, ws, gestureBus, request: MoveRequest, locale, sessionId, onClose)`. MoveRequest carries `{ actorId, tokenId, currentX, currentY, remainingFeet, gridSizeFeet }`. Scroll cycles selectedDirection through 8 compass points. Tap commits via canonical `tool.invoke` envelope with toolId='move-token' + args `{ token_id, x: newX, y: newY }` computed from direction. Long-press cancels (overrides router-level QuickActionMenu — same MVP-accepted pattern as Plan 08-03; the modal stays z=2-top and the long-press dispatcher mounts QuickActionMenu over it, suspending). Double-tap also cancels (consistency with Plan 08-02 TargetPicker).

  2. `combat-movement-tracker.ts` (foundry-module) — `Hooks.on('updateToken', (tokenDoc, change, opts, userId) => ...)` subscriber. When `change.x` or `change.y` is present AND tokenDoc.actorId is the player's actor AND combat is active, accumulate `delta = sqrt(dx² + dy²) / scene.grid.size * scene.grid.distance` (feet). Resets to 0 on `combat.turn` change (separate Hooks.on('combatTurnChange', ...) or detected via cached combatant id). Emits `r1.movement.budget` envelope with `{ actorId, walkSpeed, usedThisTurn, remainingFeet }` after each accumulation OR turn reset.
     - Researcher Q4 resolution: dnd5e 5.3.3's `actor.system.attributes.movement.used` field does NOT exist as a tracked counter (Phase 8 verified via grep on dnd5e 5.3.3 source). MUST hand-roll tracker. Phase 9 COMB-02 may refine with token vision/path-finding.

  3. `MovementBudgetPayloadSchema` — shared-protocol payload with `{ actorId, walkSpeed: number, usedThisTurn: number, remainingFeet: number }` + R1_MOVEMENT_BUDGET_TYPE = 'r1.movement.budget' constant.

  4. StatusHudRenderer extension — new `setMovementBudget(budget: { remaining: number; total: number } | null)` public method (mirrors Phase 4b setMode pattern). When non-null, `_renderStandard` adds a footer row `Mov ${remaining}/${total}` (locale-aware label). When null, footer row omitted (existing standard layout preserved). The MoveDirectionPicker mounting/unmounting drives the setMovementBudget calls via the StatusHudLayer subscriber to R1_MOVEMENT_BUDGET_TYPE.

  5. 4 INV-1 fixtures: `move-picker.idle.it.txt` (idle, N selected by default), `move-picker.ne-selected.it.txt` (NE selected via scroll), `move-picker.exhausted.it.txt` (remainingFeet=0 with hint), `status-hud.move-chip.it.txt` (StatusHud with Mov 25/30 chip visible).

  6. ~8 new i18n keys: `move_picker_title`, `move_picker_remaining_template` (`rimangono {n} ft`), `move_picker_exhausted_hint`, `move_picker_confirm_hint`, `move_picker_cancel_hint`, `status_hud_movement_label` (`Mov`), `direction_<8>` (N/NE/E/SE/S/SW/W/NW — language-neutral letters, NOT i18n? — verify Pitfall 6 compliance; the letters N/NE/etc are universal compass abbreviations, MAY be i18n-NEUTRAL like SEVERITY_PREFIX. Plan 08-04 chooses: language-neutral 2-char letters per Phase 4b Plan 03 Pitfall 6 precedent — they are NOT localised), `hud_r1_move_picker`.

Purpose: Move-token is the LAST untested manual action surface. Phase 7 ships moveTokenHandler (tokenDoc.update); Phase 6 + Phase 5 panels do not route to it. Plan 08-04 adds the picker UI + budget telemetry pipe.

Output:
  - 1 new panel + test
  - 1 new tracker module + test (foundry-module)
  - 1 new shared-protocol payload schema + test
  - 1 i18n-budgets edit (8 keys)
  - 1 status-hud-renderer extension + test
  - 1 module.ts wiring + test
  - 4 INV-1 fixtures
  - 15+ file commits total
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manual-action-ux/08-CONTEXT.md
@.planning/phases/08-manual-action-ux/08-01-PLAN.md
@.planning/phases/08-manual-action-ux/08-02-PLAN.md
@.planning/phases/08-manual-action-ux/08-03-PLAN.md

# Wave 3 — i18n-budgets.ts overlap with 08-03 forces sequential execution.
# files_modified disjoint analysis vs 08-05: 08-04 touches move-direction-picker.ts + combat-movement-tracker.ts + movement.ts schema + status-hud-renderer.ts. 08-05 touches combat-tracker-panel.ts + boot-engine-core.ts + 08-integration-smoke + STATE/ROADMAP/VERIFICATION. ZERO overlap.

# Exemplars to mirror
@packages/g2-app/src/panels/template-placement-panel.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts

# Phase 7 move-token handler (downstream consumer of our tool.invoke envelope)
@packages/foundry-module/src/write-path/handlers/move-token.ts

# Phase 5 status-hud renderer (extend)
@packages/g2-app/src/status-hud/status-hud-renderer.ts
@packages/g2-app/src/status-hud/status-hud-layer.ts

# Phase 7 reaction-watcher (Hooks.on pattern for combat-movement-tracker)
@packages/foundry-module/src/write-path/reaction-watcher.ts

# Phase 7 multi-attack progress payload (shared-protocol structure to mirror)
@packages/shared-protocol/src/payloads/multi-attack.ts

# Phase 7 module.ts wiring slot (after Plan 08-01's registerActionResultWatcher)
@packages/foundry-module/src/module.ts

# Phase 5 combat-reader (combatTurnChange detection pattern, if exists)
@packages/foundry-module/src/readers/combat-reader.ts

<interfaces>
<!-- MoveTokenInputSchema — Plan 08-04 envelope's `args` MUST validate against this (Phase 7 invariant) -->
From packages/shared-protocol/src/tools/move-token.ts:
```typescript
export const MoveTokenInputSchema = z.object({
  token_id: z.string().min(1),
  x: z.number(),
  y: z.number(),
});
```

<!-- FoundryToken update hook signature -->
From Foundry v13 API + dnd5e 5.3.3:
- `Hooks.on('updateToken', (tokenDoc, change, opts, userId) => ...)` — change.x / change.y present on position update.
- `game.combat?.current?.tokenId === tokenDoc.id` indicates active combatant.
- `scene.grid.size` (pixels per square) + `scene.grid.distance` (feet per square) — usually 100px / 5ft = 20px/ft.
- `actor.system.attributes.movement.walk` (number, feet) — base walking speed from dnd5e 5.x.

<!-- Phase 7 Hooks.on pattern — reaction-watcher.ts exemplar -->
From packages/foundry-module/src/write-path/reaction-watcher.ts:
```typescript
export function registerReactionWatcher(emit: (payload: ...) => void): () => void {
  const hookId = Hooks.on('dnd5e.preUseActivity', (...args: unknown[]): void => {
    try { /* ... */ emit(payload); } catch (err) { console.warn(...); }
    // NEVER return false (would cancel the hook chain)
  });
  return (): void => Hooks.off(hookId);
}
```

<!-- StatusHudRenderer extension pattern from Phase 4b Plan 05 (setMode added then) -->
From packages/g2-app/src/status-hud/status-hud-renderer.ts:
- `setMode(mode: StatusHudMode)` — transition-driven; only invokes re-render on state change.
- `_renderStandard(snapshot)` private method — composes the 21-row standard card.
- Public `render(snapshot)` dispatches to _renderStandard or _renderDeathSaves based on mode.
- Plan 08-04 adds: private `_movementBudget: { remaining: number; total: number } | null = null;` + public `setMovementBudget(budget: { remaining: number; total: number } | null): void` — updates field + triggers re-render via existing layer-driven render path.
- `_renderStandard` is amended to insert a `Mov 25/30` row near the footer when `_movementBudget !== null`. Other layouts untouched.

<!-- StatusHudLayer dispatch pattern for new envelope type -->
From packages/g2-app/src/status-hud/status-hud-layer.ts:
- StatusHudLayer subscribes to `wsEvents` (the WS event bus) and on each delta calls `_onDelta(raw)`.
- _onDelta narrows envelope.type and updates renderer state.
- Plan 08-04 extends _onDelta to narrow on R1_MOVEMENT_BUDGET_TYPE → MovementBudgetPayloadSchema.safeParse → renderer.setMovementBudget({remaining, total}).

<!-- Compass direction → {dx, dy} delta -->
Plan 08-04 internal helper `computeDelta(direction, gridSizeFeet): { dx: number; dy: number }`:
- N: (0, -gridSize); NE: (gridSize, -gridSize); E: (gridSize, 0); SE: (gridSize, gridSize);
- S: (0, gridSize); SW: (-gridSize, gridSize); W: (-gridSize, 0); NW: (-gridSize, -gridSize).
- gridSize = pixels per grid square. Default 100px (5ft) — passed from MoveRequest.gridSizeFeet.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MovementBudgetPayloadSchema (shared-protocol) + 8 i18n keys + index export</name>
  <files>
    packages/shared-protocol/src/payloads/movement.ts,
    packages/shared-protocol/src/payloads/movement.test.ts,
    packages/shared-protocol/src/index.ts,
    packages/g2-app/src/status-hud/i18n-budgets.ts
  </files>
  <behavior>
    - MV-01: `R1_MOVEMENT_BUDGET_TYPE === 'r1.movement.budget'` literal const exported.
    - MV-02: `MovementBudgetPayloadSchema` z.strictObject with fields: `actorId: z.string().min(1)`, `walkSpeed: z.number().int().nonnegative()` (feet), `usedThisTurn: z.number().int().nonnegative()` (feet, can be 0), `remainingFeet: z.number().int()` (signed — negative values allowed for over-budget Phase 9 enforcement).
    - MV-03: Strict-object rejection — extra field `{ extra: 'leak' }` rejected.
    - MV-04: `usedThisTurn + remainingFeet === walkSpeed` is a documented invariant but NOT enforced by Zod (sender's responsibility; Phase 9 COMB-02 enforces via action-economy widget).
    - MV-05: Barrel re-export from packages/shared-protocol/src/index.ts compiles + import resolves in g2-app.
    - I18N-01: 8 new keys present:
      - `move_picker_title`: `{ it: 'MOVIMENTO', en: 'MOVEMENT', de: 'BEWEGUNG', max: 12 }`
      - `move_picker_remaining_template`: `{ it: 'rimangono {n} ft', en: '{n} ft remaining', de: '{n} ft übrig', max: 22 }`
      - `move_picker_exhausted_hint`: `{ it: 'Movimento esaurito', en: 'Movement exhausted', de: 'Bewegung erschöpft', max: 28 }`
      - `move_picker_confirm_hint`: `{ it: 'commit', en: 'commit', de: 'bestätigen', max: 12 }`
      - `move_picker_cancel_hint`: `{ it: 'annulla', en: 'cancel', de: 'abbrechen', max: 12 }`
      - `status_hud_movement_label`: `{ it: 'Mov', en: 'Mov', de: 'Bew', max: 4 }`
      - `hud_r1_move_picker`: `{ it: 'commit|direzione|annulla', en: 'commit|direction|cancel', de: 'best.|Richtung|abbr.', max: 30 }` (pipe-separated tap|scroll|long per Phase 6 parseR1HintString)
      - (8th key) `status_hud_movement_chip_template`: `{ it: '{used}/{total}', en: '{used}/{total}', de: '{used}/{total}', max: 7 }` — language-neutral numeric format
  </behavior>
  <action>
    Create `packages/shared-protocol/src/payloads/movement.ts` mirroring `payloads/multi-attack.ts`:

    1. JSDoc header citing Plan 08-04, T-08-01-01 (envelope shape), Phase 8 broad heuristic for movement tracking (Phase 9 COMB-02 enforces).

    2. Exports:
       - `export const R1_MOVEMENT_BUDGET_TYPE = 'r1.movement.budget' as const;`
       - `export const MovementBudgetPayloadSchema = z.strictObject({ actorId: z.string().min(1), walkSpeed: z.number().int().nonnegative(), usedThisTurn: z.number().int().nonnegative(), remainingFeet: z.number().int() });`
       - `export type MovementBudgetPayload = z.infer<typeof MovementBudgetPayloadSchema>;`

    3. Co-located `movement.test.ts` with 4 MV-* tests (MV-01..04).

    4. Edit `packages/shared-protocol/src/index.ts` adding the Phase 8 movement export block immediately after the Plan 08-01 action-result export (executor inserts BOTH blocks; Plan 08-01 lands first):
       ```
       // ─── Phase 8 additions (Plan 08-04) ──────────────────────────────────────────
       // Movement budget payload schema (ACT-01 move variant).
       // combat-movement-tracker.ts in foundry-module emits envelopes of this shape.
       // status-hud-layer.ts in g2-app consumes them via _onDelta narrowing on R1_MOVEMENT_BUDGET_TYPE.
       export { MovementBudgetPayloadSchema, type MovementBudgetPayload, R1_MOVEMENT_BUDGET_TYPE } from './payloads/movement.js';
       ```

    5. Edit `packages/g2-app/src/status-hud/i18n-budgets.ts` adding 8 keys per I18N-01 under "// Phase 8 — move direction picker + status-hud movement chip" block. Verify each value's [...v].length ≤ max.
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test -- --run movement &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    movement.ts + test exist; MV-01..04 pass; 8 i18n keys present + within budget; barrel export resolves; typecheck exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: combat-movement-tracker (foundry-module) + module.ts wiring</name>
  <files>
    packages/foundry-module/src/write-path/combat-movement-tracker.ts,
    packages/foundry-module/src/write-path/combat-movement-tracker.test.ts,
    packages/foundry-module/src/module.ts,
    packages/foundry-module/src/module.test.ts
  </files>
  <behavior>
    - CMT-01: `registerMovementTracker(emit)` calls `Hooks.on('updateToken', handler)` AND `Hooks.on('combatTurnChange', resetHandler)` AND returns an idempotent unsubscribe that calls both `Hooks.off` calls.
    - CMT-02: updateToken handler reads `change.x` + `change.y` defensively. If neither present → silent return (token update unrelated to movement).
    - CMT-03: When change.x/y present AND tokenDoc.actorId === player's actor (game.user?.character?.id) AND game.combat is active: compute delta in feet = `sqrt(dx² + dy²) / scene.grid.size * scene.grid.distance` rounded. Accumulate to a module-scoped `MovementState` map keyed by actorId. After accumulation, call emit(payload) where payload = { actorId, walkSpeed: actor.system.attributes.movement.walk, usedThisTurn: accumulated, remainingFeet: walkSpeed - accumulated }.
    - CMT-04: Outside combat (game.combat is null) → no accumulation, no emit. Phase 8 only tracks combat movement; exploration-mode movement is Phase 9 stretch.
    - CMT-05: combatTurnChange handler resets MovementState[actorId] to 0 + emits a fresh payload with usedThisTurn=0, remainingFeet=walkSpeed.
    - CMT-06: Handler is defensive — try/catch wraps emit; console.warn on throw; NEVER returns false (Hooks contract).
    - CMT-07: When player has no actor (game.user.character is null) → silent return on updateToken (CMT-02 fallback case). No emit. Phase 8 minimal — no orphan-actor handling.
    - CMT-08: registerMovementTracker is exposed as a function with signature `(emit: (payload: MovementBudgetPayload) => void) => () => void` mirroring registerReactionWatcher.
    - CMT-09 (module.ts): registerMovementTracker is called once in Hooks.once('ready') AFTER registerActionResultWatcher (Plan 08-01) and BEFORE registerCanvasExtractor. Insertion order verified in module.test.ts T-08-MOD-03.
    - CMT-10 (module.test.ts T-08-MOD-04): registerComplexHandler count stays exactly 14 (Phase 7 invariant — Plan 08-04 adds NO new socketlib handler).
    - CMT-11: hookId for combatTurnChange may not be a standard Foundry hook; Phase 8 uses a fallback — detect turn change via `combatTurnChange` hook IF it exists (defensive `Hooks.on('combatTurnChange', ...)` is safe even for non-existent hooks per Foundry behavior). Alternative: `Hooks.on('updateCombat', (combat, change, opts, userId) => { if (change.turn !== undefined) resetHandler(); })`. Plan 08-04 uses **`updateCombat`** as the canonical Foundry v13 hook (verified via Phase 7 combat-reader.ts pattern).
  </behavior>
  <action>
    Create `packages/foundry-module/src/write-path/combat-movement-tracker.ts` mirroring `reaction-watcher.ts`:

    1. JSDoc header citing Plan 08-04, Researcher Q4 resolution (no dnd5e 5.3.3 movement.used field — hand-roll tracker), CMT-04 combat-only scope, CMT-06 never-return-false invariant.

    2. Module-scoped state:
       ```ts
       interface MovementState { walkSpeed: number; usedThisTurn: number; }
       const _state = new Map<string, MovementState>();
       ```

    3. Pure helper `_computeDeltaFeet(dx, dy, gridSize, gridDistance)`: `Math.round(Math.sqrt(dx*dx + dy*dy) / gridSize * gridDistance)`.

    4. Pure helper `_getPlayerActorId(): string | null` reads `game.user?.character?.id ?? null`.

    5. Public `registerMovementTracker(emit)`:
       - `const updateTokenHookId = Hooks.on('updateToken', (...args: unknown[]): void => { try { ... } catch (err) { console.warn(...); } });`
       - Inside updateToken handler: read args[0] (tokenDoc), args[1] (change), defensively type-cast as Record<string, unknown>. Extract change.x, change.y as number|undefined. Early return if neither. Check tokenDoc.actorId === _getPlayerActorId(). Check game.combat is non-null. Compute current pos from tokenDoc.x / tokenDoc.y - change.x (since update replaces, we need delta from old to new — researcher hint: change.x is the NEW value, so dx = (change.x ?? tokenDoc.x) - tokenDoc.x; but actually `change` carries the diff, NOT the new value. **Verify via Phase 7 moveTokenHandler pattern**: handler calls `tokenDoc.update({x, y})` where x/y are NEW values. The `change` object in the updateToken hook carries the FIELDS that changed (NEW values). To compute the delta, the tracker needs the OLD position. Foundry's updateToken hook signature gives `(document, changes, options, userId)` where `changes` are the new values. The OLD x/y are accessible via `tokenDoc.x` AT THE TIME the hook fires (post-update). To get the OLD, would need preUpdateToken. **Phase 8 simplification**: track the delta from the LAST emitted position per actorId. The tracker keeps `lastPosition: Map<actorId, {x: number; y: number}>` and computes dx = newX - lastPosition[actor].x; updates lastPosition. First update initializes from the (already current) tokenDoc.x / tokenDoc.y, so first update reports a 0 delta. This is acceptable Phase 8 broad heuristic.
       - After computing delta: accumulate, call emit(payload).
       - `const updateCombatHookId = Hooks.on('updateCombat', (...args: unknown[]): void => { try { ... } catch (err) { ... } });`
       - Inside updateCombat handler: extract args[1] (change), check `change.turn !== undefined` (turn advance). Iterate _state Map, reset usedThisTurn to 0, emit fresh payloads.
       - Return unsubscribe: `() => { Hooks.off(updateTokenHookId); Hooks.off(updateCombatHookId); }`.

    6. Co-located test file with 8 CMT-* tests. Mock Hooks + game globals per reaction-watcher.test.ts pattern.

    7. Edit `packages/foundry-module/src/module.ts`:
       - Add import: `import { registerMovementTracker } from './write-path/combat-movement-tracker.js';` + `import { R1_MOVEMENT_BUDGET_TYPE } from '@evf/shared-protocol';`
       - Insert in Hooks.once('ready'), AFTER `registerActionResultWatcher(...)` (Plan 08-01 wiring) and BEFORE `registerCanvasExtractor({...})`:
         ```
         // Plan 08-04 — register the movement tracker (ACT-01 move variant).
         // Subscribes to updateToken + updateCombat; emits r1.movement.budget on accumulation/reset.
         // NO new socketlib handler — count stays 14 (Phase 7 invariant).
         registerMovementTracker((payload) => bridgeDeltaEmitter(R1_MOVEMENT_BUDGET_TYPE, payload));
         ```

    8. Edit `packages/foundry-module/src/module.test.ts` adding T-08-MOD-03 (registerMovementTracker called once after registerActionResultWatcher) + T-08-MOD-04 (socketlib count stays 14).
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run combat-movement-tracker module</automated>
  </verify>
  <done>
    combat-movement-tracker.ts + test exist; 8 CMT-* tests pass; T-08-MOD-03/04 pass; bridgeDeltaEmitter called with R1_MOVEMENT_BUDGET_TYPE on combat movement; socketlib count stays 14.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MoveDirectionPicker (g2-app) + StatusHud move-chip extension + 4 INV-1 fixtures</name>
  <files>
    packages/g2-app/src/panels/move-direction-picker.ts,
    packages/g2-app/src/panels/move-direction-picker.test.ts,
    packages/g2-app/src/status-hud/status-hud-renderer.ts,
    packages/g2-app/src/status-hud/__tests__/status-hud-renderer.test.ts,
    packages/shared-render/src/fixtures/move-picker.idle.it.txt,
    packages/shared-render/src/fixtures/move-picker.ne-selected.it.txt,
    packages/shared-render/src/fixtures/move-picker.exhausted.it.txt,
    packages/shared-render/src/fixtures/status-hud.move-chip.it.txt
  </files>
  <behavior>
    - MDP-01: Panel ID `'move-direction-picker'`. `static meta` omitted (system overlay).
    - MDP-02: Constructor: `(bridge, ws, gestureBus, request: MoveRequest, locale, sessionId, onClose)` where MoveRequest = `{ actorId: string; tokenId: string; remainingFeet: number; gridSizeFeet: number; }`. Phase 8 minimal — `currentX/currentY` NOT needed since Plan 08-04 emits a DELTA-based move (the bridge resolves absolute position via Phase 7 move-token handler reading tokenDoc.x/y + applying delta? — re-check: MoveTokenInputSchema requires absolute `{x, y}`. So MoveDirectionPicker MUST know currentX/currentY to compute new absolute position. Add to MoveRequest: `currentX: number; currentY: number;`).
    - MDP-03: Internal state `selectedDirection: MoveDirection` (default 'N'). MoveDirection = 'N' | 'NE' | 'E' | 'SE' | 'S' | 'SW' | 'W' | 'NW'. Scroll cycles through fixed order [N, NE, E, SE, S, SW, W, NW] forward (scroll-down) and reverse (scroll-up).
    - MDP-04: onMount subscribes bus + computes initial render. onUnmount unsubscribes.
    - MDP-05: onEvent('scroll') cycles selectedDirection + triggers redraw.
    - MDP-06: onEvent('tap') — IF `request.remainingFeet <= 0` (exhausted) → NO emit, call onClose() (or do not call onClose; let user double-tap to exit; CONTEXT.md §Specifics shows exhausted hint but no explicit close behavior. Plan 08-04 chooses: tap when exhausted = no-op + console.warn telemetry. Use double-tap to close, matching cancel semantics). Verify exhausted via remainingFeet (sender's authoritative value).
    - MDP-07: onEvent('tap') normal path — compute `{dx, dy} = computeDelta(selectedDirection, gridSizeFeet)` in pixels (gridSize === gridSizeFeet * 20 px/ft typically; Phase 8 uses `request.gridSizeFeet * 20` for pixels — OR receive gridSizePixels in MoveRequest. **Revised**: MoveRequest carries `gridSizePixels: number` (canvas units per square, default 100); the move step = gridSizePixels (one tile). dx/dy in canvas units. New position: `newX = request.currentX + dx, newY = request.currentY + dy`. Emit canonical EnvelopeSchema tool.invoke with `args: { token_id: request.tokenId, x: newX, y: newY }`. Then onClose().
    - MDP-08: onEvent('double-tap') → onClose() without emit.
    - MDP-09: onEvent('long-press') → ignored at panel level (router-level dispatcher mounts QuickActionMenu — MVP accepted; same as Plan 08-03 AOM-07).
    - MDP-10: `getContainerCount` returns `{ image: 0, text: 1 }`.
    - MDP-11: `getR1Hints` returns parseR1HintString(getLabel('hud_r1_move_picker', locale)).
    - MDP-12: draw renders the 8-direction compass per CONTEXT mockup with ▶ before selected direction. Layout uses fixed character positions for compass points.
    - MDP-13: Exhausted state: top border, title, blank, centered `Movimento esaurito` hint (locale-aware), blank, bottom border. No compass.
    - MDP-14: W-4 envelope round-trip — emitted JSON parses + EnvelopeSchema + ToolInvocationEnvelopePayloadSchema + MoveTokenInputSchema all pass.
    - MDP-15..17: 3 INV-1 fixture round-trips (idle / NE-selected / exhausted).
    - SHR-MV-01: StatusHudRenderer gains `setMovementBudget(budget: { remaining: number; total: number } | null): void` public method.
    - SHR-MV-02: When `_movementBudget !== null`, `_renderStandard(snapshot)` inserts an extra row at the footer position: `${status_hud_movement_label} ${remaining}/${total}` (locale-aware). When null, row is omitted entirely (existing layout preserved).
    - SHR-MV-03: Transition-driven: setMovementBudget only triggers re-render when the new value differs from the stored value (same SR-DS-6 pattern from Phase 4b Plan 05).
    - SHR-MV-04: INV-1 fixture round-trip — `status-hud.move-chip.it.txt` matches a StatusHud rendering with `_movementBudget = { remaining: 25, total: 30 }` and `mode === 'standard'`.
    - SHR-MV-05: When `_movementBudget` is set AND mode is 'death-saves' → death-saves layout takes priority; move chip NOT rendered (death-saves layout is full-page per Phase 4b Plan 05). Plan 08-04 acceptance: setMovementBudget call during death-saves does NOT throw + does NOT render chip; existing death-saves fixtures stay green.
  </behavior>
  <action>
    Create `packages/g2-app/src/panels/move-direction-picker.ts` mirroring `template-placement-panel.ts`:

    1. JSDoc header citing Plan 08-04, CONTEXT.md §Specifics (mockup), §Decisions Area 1 (move flow), CONTEXT.md §Area 2 (move-token UX bullet), MDP-07 absolute-position envelope construction.

    2. Local types:
       - `export type MoveDirection = 'N' | 'NE' | 'E' | 'SE' | 'S' | 'SW' | 'W' | 'NW';`
       - `export interface MoveRequest { readonly actorId: string; readonly tokenId: string; readonly currentX: number; readonly currentY: number; readonly remainingFeet: number; readonly gridSizePixels: number; }`
       - `export interface MoveDirectionPickerWebSocket { send(data: string): void; }`
       - `export type MoveDirectionPickerCloseHandler = () => void;`

    3. Module-scope const `DIRECTION_ORDER: ReadonlyArray<MoveDirection>` = `['N', 'NE', 'E', 'SE', 'S', 'SW', 'W', 'NW'] as const;`
       and `computeDelta(direction: MoveDirection, gridSizePixels: number): { dx: number; dy: number }` per <interfaces> mapping table.

    4. Class `export class MoveDirectionPicker implements OverlayPanel` — lifecycle + tap/scroll/double-tap dispatch per MDP-* behaviors. Strategy A single 'overlay-block' container. `declare const crypto: { randomUUID(): string };`.

    5. `_buildLines()` renders 14-row compass when remainingFeet > 0, else 14-row exhausted layout. Fixed-width frame matches CONTEXT.md mockup character-perfect.

    6. Co-located test with MDP-01..17 (17 tests).

    7. Edit `packages/g2-app/src/status-hud/status-hud-renderer.ts`:
       - Add private field `private _movementBudget: { remaining: number; total: number } | null = null;`
       - Add public method `setMovementBudget(budget: { remaining: number; total: number } | null): void` with transition guard:
         ```ts
         setMovementBudget(budget): void {
           const same = (budget === null && this._movementBudget === null)
             || (budget !== null && this._movementBudget !== null
                 && budget.remaining === this._movementBudget.remaining
                 && budget.total === this._movementBudget.total);
           if (same) return;
           this._movementBudget = budget;
         }
         ```
       - Amend `_renderStandard(snapshot)` to insert the `Mov 25/30` row when `_movementBudget !== null`. Insert in the footer row position per UI-SPEC §7.10 State 3 example. (Re-check UI-SPEC for the exact row; if absent, insert as a new row JUST above the bottom border, before the existing footer R1 hint line.)
       - Test accessor `_getMovementBudgetForTest()` if needed.

    8. Edit status-hud-renderer.test.ts adding SHR-MV-01..05 tests (5 new tests).

    9. INV-1 fixtures (4 files) authored via vitest --update + verified against CONTEXT.md mockup + UI-SPEC §7.10 State 3. The status-hud.move-chip fixture uses standard-mode rendering with the chip row visible.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run move-direction-picker status-hud-renderer</automated>
  </verify>
  <done>
    move-direction-picker.ts + test exist; 17 MDP-* tests pass; status-hud-renderer.ts ships setMovementBudget; 5 SHR-MV-* tests pass; 4 INV-1 fixtures committed + round-trip green; W-4 envelope round-trip verified (incl. MoveTokenInputSchema inner validation).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry updateToken hook → combat-movement-tracker accumulator | GM-side, in-process (foundry-module only loads on GM client per Phase 2 manifest scope) |
| bridgeDeltaEmitter → bridge → g2-app WS | Same channel as r1.action.result, bearer-authenticated |
| g2-app StatusHudLayer._onDelta → renderer.setMovementBudget | Inner validation via MovementBudgetPayloadSchema.safeParse before invoking renderer |
| MoveDirectionPicker.onEvent('tap') → ws.send tool.invoke | Outgoing canonical EnvelopeSchema envelope; bridge validates ToolInvocationEnvelopePayloadSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering / Spoofing | MoveDirectionPicker outgoing envelope | mitigate | Canonical EnvelopeSchema with `payload` field; W-4 round-trip + MoveTokenInputSchema inner validation (MDP-14). Bridge rejects malformed args downstream. |
| T-08-02-01 | Information disclosure | Movement tracker tracks position across players | accept | Combat-movement-tracker reads ONLY game.user?.character?.id token updates — cross-player movement is NOT tracked (CMT-02 + CMT-07 filter). Phase 13 STRETCH-01 multi-player will need per-user trackers; out of scope. |
| T-08-04-01 | Denial of Service | updateToken hook flood (token dragged continuously) | accept | Foundry's own updateToken hook batches drag operations; one hook per release. Plan 08-04 accumulator's emit fires per hook → bridge → g2-app StatusHud setMovementBudget transition-guarded (SHR-MV-03 only re-renders on state change). No infinite-loop surface. |
| T-08-04-02 | Tampering | Player emits move-token with exhausted budget | mitigate (Phase 8) + defer enforcement (Phase 9) | MDP-06 client-side: tap when remainingFeet <= 0 is a no-op (no ws.send). Server-side enforcement via remainingFeet check is Phase 9 COMB-02 — Phase 7 moveTokenHandler currently accepts any (x, y). Acceptable MVP — client-side guard is the visible UX, server-side enforcement is the action-economy widget gate that Phase 9 owns. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 workspace-wide.
- `pnpm test --filter @evf/shared-protocol movement` + `pnpm test --filter @evf/foundry-module combat-movement-tracker module` + `pnpm test --filter @evf/g2-app move-direction-picker status-hud-renderer` → all green.
- 4 INV-1 fixtures load + round-trip via matchAsciiFixture.
- `pnpm lint:ci` exits 0.
- W-4 grep gate green on move-direction-picker.ts.
- registerComplexHandler count = 14 (T-08-MOD-04).
- `grep -rE 'activity\\.use\\(' packages/g2-app/src/ packages/bridge/src/ | grep -v '^#' | grep -c .` → 0 (CI Gate 8).
- 8 new i18n keys + 4 new fixtures + 4 new test files + 2 modified source files committed.
</verification>

<success_criteria>
- MoveDirectionPicker renders 8-direction compass + exhausted-state hint per CONTEXT.md mockup character-perfect.
- combat-movement-tracker accumulates per-token movement on updateToken + resets on updateCombat turn-change.
- MovementBudgetPayloadSchema validates the strict 4-field shape.
- StatusHudRenderer.setMovementBudget toggles a footer chip without breaking existing layout (Phase 4b death-saves precedent — transition-driven).
- INV-1 fixtures (4) committed; 8 i18n keys committed.
- No changes to boot-engine-core.ts (Plan 08-05 wires).
- registerComplexHandler count stays 14.
- ADR-0011 single-workflow-origin: no `activity.use(` introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-manual-action-ux/08-04-SUMMARY.md` documenting:
- One-liner: MoveDirectionPicker z=2 + combat-movement-tracker hooks + StatusHud move-chip extension + 4 INV-1 fixtures.
- Tasks completed table.
- Test counts (MV-* + CMT-* + MDP-* + SHR-MV-* + module T-08-MOD-03/04).
- INV-1 fixture paths.
- Researcher Q4 resolution: hand-rolled tracker because dnd5e 5.3.3 has no `movement.used` field; Phase 9 COMB-02 may refine.
- Self-Check: PASSED block.
</output>
