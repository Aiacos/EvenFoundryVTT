---
phase: 08-manual-action-ux
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/shared-protocol/src/payloads/action-result.ts
  - packages/shared-protocol/src/payloads/action-result.test.ts
  - packages/shared-protocol/src/index.ts
  - packages/foundry-module/src/write-path/action-result-watcher.ts
  - packages/foundry-module/src/write-path/action-result-watcher.test.ts
  - packages/foundry-module/src/module.ts
  - packages/foundry-module/src/module.test.ts
  - packages/g2-app/src/panels/action-result-dispatcher.ts
  - packages/g2-app/src/panels/action-result-dispatcher.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "ActionResultPayloadSchema validates the 8 canonical fields (idempotencyKey, toolId, d20, outcome, damage, status, errorKind, recipientUserId)"
    - "When the GM's Foundry creates a chat card whose flags.evf.audit.idempotencyKey matches a recent tool dispatch, the action-result-watcher hook fires and emits an r1.action.result envelope via bridgeDeltaEmitter"
    - "The g2-app action-result-dispatcher validates incoming envelopes with double trust boundary (EnvelopeSchema + ActionResultPayloadSchema) and enqueues a typed toast on ToastQueueLayer"
    - "Error toasts render the localized error.action.<kind> string for all 5 typed errors (no-targets / out-of-range / out-of-resource / wrong-turn / gm-rejected) in IT + EN + DE within 28-char budget"
    - "T-08-02 (cross-player leak): dispatcher silently drops envelopes whose recipientUserId does not match the bound bearer user_id"
  artifacts:
    - path: "packages/shared-protocol/src/payloads/action-result.ts"
      provides: "ActionResultPayloadSchema + R1_ACTION_RESULT_TYPE + ActionErrorKind enum + ActionOutcome enum"
      exports: ["ActionResultPayloadSchema", "R1_ACTION_RESULT_TYPE", "ActionResultPayload", "ActionErrorKind", "ActionOutcome"]
    - path: "packages/foundry-module/src/write-path/action-result-watcher.ts"
      provides: "registerActionResultWatcher(emit) — Hooks.on('createChatMessage') filter for flags.evf.audit.idempotencyKey"
      exports: ["registerActionResultWatcher"]
    - path: "packages/g2-app/src/panels/action-result-dispatcher.ts"
      provides: "attachActionResultHandler(ws, toastQueue, locale, currentUserId) double trust boundary + recipient filter"
      exports: ["attachActionResultHandler", "ActionResultDispatcherSocket"]
    - path: "packages/g2-app/src/status-hud/i18n-budgets.ts"
      provides: "5 new keys — error.action.no-targets / out-of-range / out-of-resource / wrong-turn / gm-rejected"
      contains: "error.action.no-targets"
  key_links:
    - from: "packages/foundry-module/src/module.ts"
      to: "registerActionResultWatcher"
      via: "Hooks.once('ready') wiring after registerReactionWatcher"
      pattern: "registerActionResultWatcher\\(\\(payload\\) => bridgeDeltaEmitter\\('r1\\.action\\.result'"
    - from: "packages/foundry-module/src/write-path/action-result-watcher.ts"
      to: "Hooks.on('createChatMessage')"
      via: "chat-card flag introspection"
      pattern: "Hooks\\.on\\('createChatMessage'"
    - from: "packages/g2-app/src/panels/action-result-dispatcher.ts"
      to: "ToastQueueLayer.enqueue"
      via: "Phase 4b toast surface"
      pattern: "toastQueue\\.enqueue\\("
---

<objective>
Atomic Wave 0 foundation for Phase 8 manual action UX (ACT-01). Lands four concerns in one commit:

  1. `ActionResultPayloadSchema` shared-protocol payload schema + R1_ACTION_RESULT_TYPE constant + ActionOutcome/ActionErrorKind enums.
  2. `action-result-watcher` foundry-module hook subscriber that listens on `createChatMessage`, filters for `flags.evf.audit.idempotencyKey` matches, and emits `r1.action.result` envelopes via the existing `bridgeDeltaEmitter` channel — NO new socketlib handler (count stays 14 per ADR-0011).
  3. `action-result-dispatcher` g2-app WS consumer that double-validates envelopes and routes typed toasts into Phase 4b `ToastQueueLayer`. T-08-02 cross-player leak mitigated by filtering on `recipientUserId === currentBearer.user_id`.
  4. 5 typed error i18n keys (`error.action.<kind>`) added to HUD_WIDTH_BUDGETS with IT + EN + DE strings ≤ 28-char toast row budget.

Purpose: Phase 7 already writes audit-log entries with `idempotencyKey` on every `dispatchTool` call. Phase 8 closes the loop by converting GM-side chat-card outputs into player-side typed toasts — without nesting workflows, without adding new socketlib handlers, and without leaking results across players. Plans 02-05 consume this foundation: 02 + 03 + 04 emit `tool.invoke` envelopes that produce chat cards → result toasts; 05 wires the dispatcher into `boot-engine-core.ts`.

Output:
  - 4 new files + 1 schema barrel export + 3 modified files (i18n + module.ts + module.test.ts)
  - 1 atomic commit per Phase 4b Plan 06 precedent (D-1.04 schema fan-out pattern)
  - Zero new socketlib handlers — `registerComplexHandler` count stays at exactly 14 (Phase 7 closure invariant)
  - Zero `activity.use(` in g2-app or bridge — CI Gate 8 stays green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manual-action-ux/08-CONTEXT.md

# Phase 7 closure context (Plan 06 — bearer rotation pattern + ISM-W7 harness)
@.planning/phases/07-foundry-module-write-path/07-06-SUMMARY.md

# Phase 7 hook-pattern exemplar (reaction-watcher.ts uses Hooks.on)
@packages/foundry-module/src/write-path/reaction-watcher.ts

# Phase 7 audit-log pattern (flags.evf.audit shape we must match)
@packages/foundry-module/src/write-path/audit-log.ts

# Phase 7 g2-app dispatcher exemplars (double trust boundary pattern)
@packages/g2-app/src/panels/reaction-toast-dispatcher.ts
@packages/g2-app/src/panels/multi-attack-progress-dispatcher.ts

# Phase 4b/7 wire schemas (envelope shape + tool payload shape)
@packages/shared-protocol/src/envelope.ts
@packages/shared-protocol/src/payloads/tool.ts
@packages/shared-protocol/src/payloads/reaction.ts

# Phase 4b toast surface (consumer)
@packages/g2-app/src/status-hud/toast-types.ts

# Phase 7 module.ts wiring slot (insertion point after scheduleBearerRotation)
@packages/foundry-module/src/module.ts

<interfaces>
<!-- Key types/contracts extracted from the codebase so the executor can ship without exploring -->

From packages/shared-protocol/src/envelope.ts (CANONICAL WS envelope shape — do NOT rename `payload` to `value`, W-4 regression guard):
```typescript
export const EnvelopeSchema = z.object({
  proto: z.literal('evf-v1'),
  seq: z.number().int().nonnegative(),
  ts: z.number().int(),
  type: z.string(),
  session_id: z.string().uuid(),
  payload: z.unknown(),
});
```

From packages/shared-protocol/src/payloads/tool.ts (existing tool-ID enum — `ActionResultPayloadSchema.toolId` reuses this):
```typescript
export const TOOL_ID_SCHEMA = z.enum([
  'cast-spell', 'weapon-attack', 'use-item', 'move-token',
  'drop-concentration', 'place-template', 'confirm-template-placement',
]);
```

From packages/foundry-module/src/write-path/audit-log.ts (the EXACT flag shape action-result-watcher must read):
```typescript
export interface AuditEntry {
  tool: string;                  // e.g. 'cast-spell' — matches TOOL_ID_SCHEMA
  payload: unknown;              // raw args (NOT used by action-result-watcher)
  idempotencyKey: string;        // UUID v4 — the JOIN KEY
  actorId: string | null;
  result: ToolResult;            // { success: true, data: {...} } | { success: false, error: '...' }
  timestamp: number;
  bearer_id: string;             // sha256(bearer).slice(0,8) — present for audit-log only, NOT for action-result
}
```
The chat card created by `writeAuditLog` stores this entry at `msg.flags.evf.audit` (NOT `msg.flags.evf.audit.idempotencyKey` directly — the key is one field within the entry).

From packages/foundry-module/src/write-path/reaction-watcher.ts (hook registration pattern — `Hooks.on` returns `hookId: number`, NEVER return false from the handler):
```typescript
export function registerReactionWatcher(emit: (payload: ReactionAvailablePayload) => void): () => void {
  const hookId = Hooks.on('dnd5e.preUseActivity', (...args: unknown[]): void => { /* ... */ });
  return (): void => { Hooks.off(hookId); };
}
```

From packages/g2-app/src/panels/reaction-toast-dispatcher.ts (the EXACT dispatcher shape to mirror):
```typescript
export interface ReactionDispatcherSocket {
  addEventListener(event: 'message', handler: (ev: MessageEvent) => void): void;
  removeEventListener(event: 'message', handler: (ev: MessageEvent) => void): void;
  send(data: string): void;
}
export function attachReactionToastHandler(
  ws: ReactionDispatcherSocket,
  toastQueue: { enqueue(toast: Toast): void },
  locale: HudLocale,
): () => void { /* JSON.parse → EnvelopeSchema.safeParse → narrow type → inner parse → enqueue */ }
```

From packages/g2-app/src/status-hud/toast-types.ts (ToastSchema — max 38 chars message body; 28-char effective budget for error.action.* per CONTEXT §Area 2):
```typescript
export const ToastSchema = z.strictObject({
  id: z.string().min(1),
  severity: z.enum(['info', 'warn', 'error']),
  message: z.string().min(1).max(38),
  emittedAt: z.number().int().nonnegative(),
});
```

From packages/foundry-module/src/module.ts (insertion point — AFTER scheduleBearerRotation, BEFORE registerCanvasExtractor):
```typescript
Hooks.once('ready', () => {
  registerSocketlibHandlers();
  registerHookSubscribers(bridgeDeltaEmitter);
  setMultiAttackProgressEmitter((payload) => bridgeDeltaEmitter('r1.multiattack.progress', payload));
  registerReactionWatcher((payload) => bridgeDeltaEmitter('r1.reaction.available', payload));
  scheduleBearerRotation({ emit: (payload) => bridgeDeltaEmitter(BEARER_ROTATED_TYPE, payload) });
  // ★ INSERT HERE — Plan 08-01:
  // registerActionResultWatcher((payload) => bridgeDeltaEmitter(R1_ACTION_RESULT_TYPE, payload));
  registerCanvasExtractor({ emit: (payload) => bridgeDeltaEmitter('frame_pixels', payload) });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionResultPayloadSchema + index barrel export + 5 i18n keys (atomic schema fan-out)</name>
  <files>
    packages/shared-protocol/src/payloads/action-result.ts,
    packages/shared-protocol/src/payloads/action-result.test.ts,
    packages/shared-protocol/src/index.ts,
    packages/g2-app/src/status-hud/i18n-budgets.ts
  </files>
  <behavior>
    - ART-01: ActionResultPayloadSchema.safeParse on the 8-field happy-path payload (idempotencyKey UUID v4, toolId 'cast-spell', d20: 18, outcome: 'hit', damage: '1d8+3 = 7 sl', status: 'success', errorKind: undefined, recipientUserId: 'user-abc') → .success === true.
    - ART-02: Missing `recipientUserId` → safeParse .success === false (required field — T-08-02 mitigation; no default to prevent silent cross-player leak).
    - ART-03: `idempotencyKey` must be a valid UUID v4 — non-UUID string rejected.
    - ART-04: `toolId` must be one of TOOL_ID_SCHEMA values — `'unknown-tool'` rejected; `'cast-spell'` accepted.
    - ART-05: `outcome` is one of `'hit' | 'miss' | 'save_success' | 'save_fail' | 'damage_dealt' | 'no_roll'` — `'critical'` rejected.
    - ART-06: `status` is one of `'success' | 'failure' | 'error'` — `'pending'` rejected.
    - ART-07: `errorKind` (optional) is one of `'no-targets' | 'out-of-range' | 'out-of-resource' | 'wrong-turn' | 'gm-rejected'` — `'unknown-error'` rejected; undefined accepted.
    - ART-08: `damage` (optional) is a string — number rejected.
    - ART-09: `d20` is `z.number().int().nullable()` — null accepted (no_roll case); 21 accepted; 0.5 rejected.
    - ART-10: Strict object — extra field `{ extra: 'leak' }` rejected (T-08-01 belt-and-suspenders against field smuggling).
    - ART-11: `R1_ACTION_RESULT_TYPE === 'r1.action.result'` (literal constant exported).
    - ART-12: Barrel re-export from packages/shared-protocol/src/index.ts compiles + `import { ActionResultPayloadSchema, R1_ACTION_RESULT_TYPE } from '@evf/shared-protocol'` resolves in g2-app tests.
    - I18N-01: All 5 error.action.<kind> keys present in HUD_WIDTH_BUDGETS with IT + EN + DE values + `max: 28` (toast row budget per CONTEXT §Area 2).
    - I18N-02: Each value's `[...v].length` ≤ 28 (code-point counting per padRightUnicode convention; assertion via existing budget-table integrity test pattern).
  </behavior>
  <action>
    Create `packages/shared-protocol/src/payloads/action-result.ts` mirroring `payloads/reaction.ts` structure: JSDoc header citing Plan 08-01 + CONTEXT.md §Area 2, exported `R1_ACTION_RESULT_TYPE = 'r1.action.result' as const`, exported `ActionOutcome` enum (z.enum with 6 values per CONTEXT.md schema block), exported `ActionErrorKind` enum (5 values), exported `ActionResultPayloadSchema = z.strictObject({...})` with all 8 fields, and inferred `ActionResultPayload` type. The schema MUST require `recipientUserId: z.string().min(1)` (NOT optional — T-08-02). Strict object rejection prevents future field-rename data loss.

    Add a co-located `action-result.test.ts` covering ART-01..11 (12 assertions). Use `safeParse` (not `parse`) so success/failure tests are symmetric.

    Append to `packages/shared-protocol/src/index.ts` (immediately AFTER the existing Plan 07-05 reaction.js export block) a Phase 8 export block:
    ```
    // ─── Phase 8 additions (Plan 08-01) ──────────────────────────────────────────
    // Action result payload schema (ACT-01).
    // action-result-dispatcher.ts consumes this at the WS-receive trust boundary.
    // action-result-watcher.ts in foundry-module emits envelopes of this shape.
    export { ActionErrorKind, ActionOutcome, ActionResultPayloadSchema, type ActionResultPayload, R1_ACTION_RESULT_TYPE } from './payloads/action-result.js';
    ```

    Edit `packages/g2-app/src/status-hud/i18n-budgets.ts` adding 5 new entries to HUD_WIDTH_BUDGETS per CONTEXT.md Area 2 typed-error table:
    - `'error.action.no-targets'`:      `{ it: 'Nessun bersaglio', en: 'No targets',        de: 'Keine Ziele',         max: 28 }`
    - `'error.action.out-of-range'`:    `{ it: 'Fuori portata',    en: 'Out of range',      de: 'Außer Reichweite',    max: 28 }`
    - `'error.action.out-of-resource'`: `{ it: 'Risorse esaurite', en: 'Out of resources',  de: 'Ressourcen leer',     max: 28 }`
    - `'error.action.wrong-turn'`:      `{ it: 'Non il tuo turno', en: 'Not your turn',     de: 'Nicht dein Zug',      max: 28 }`
    - `'error.action.gm-rejected'`:     `{ it: 'Rifiutato dal GM', en: 'GM rejected',       de: 'Vom DM abgelehnt',    max: 28 }`

    Insert the 5 keys alphabetically within the existing "error.*" section if one exists, otherwise as a new "// Phase 8 — action result error toasts" block. Verify every IT/EN/DE value's code-point length ≤ 28 before committing (the existing budget-table integrity test fails otherwise).

    Do NOT modify any other file in this task. Per RESEARCH §Q3 + reaction.ts precedent, use `z.strictObject` (not `z.object`) on the payload schema for field-smuggling defence.
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test -- --run action-result &amp;&amp; pnpm --filter @evf/g2-app test -- --run i18n-budgets 2>/dev/null; pnpm typecheck</automated>
  </verify>
  <done>
    action-result.ts + action-result.test.ts exist; 12 ART-* tests pass; schema rejects missing recipientUserId + extra fields; 5 error.action.* keys exist in HUD_WIDTH_BUDGETS with IT/EN/DE ≤ 28 cps; barrel export resolves; `pnpm typecheck` exits 0 workspace-wide.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: action-result-watcher (createChatMessage hook) + module.ts wiring</name>
  <files>
    packages/foundry-module/src/write-path/action-result-watcher.ts,
    packages/foundry-module/src/write-path/action-result-watcher.test.ts,
    packages/foundry-module/src/module.ts,
    packages/foundry-module/src/module.test.ts
  </files>
  <behavior>
    - ARW-01: `registerActionResultWatcher(emit)` calls `Hooks.on('createChatMessage', handler)` and returns an unsubscribe closure that calls `Hooks.off(hookId)`. Hook NAME verified against `audit-log.ts` consumer pattern (ChatMessage.create fires the standard Foundry hook). Researcher Q1: hook fires AFTER ChatMessage document is created — chat card has final state including dnd5e roll totals. dnd5e.completeUseActivity is NOT needed because audit-log writes the ChatMessage AFTER the activity completes (Phase 7 dispatchTool awaits handler.handle() before writeAuditLog).
    - ARW-02: When a `createChatMessage` event fires with `msg.flags?.evf?.audit?.idempotencyKey === '<uuid>'`, the handler invokes `emit({ idempotencyKey, toolId: msg.flags.evf.audit.tool, d20: extractD20(msg), outcome: inferOutcome(msg), damage: extractDamage(msg), status: msg.flags.evf.audit.result.success ? 'success' : 'failure', errorKind: undefined, recipientUserId: <resolved> })`.
    - ARW-03: When `msg.flags?.evf?.audit` is undefined (regular chat message — player whisper, GM narration, dnd5e card without audit flag), handler returns silently — emit NEVER called. Verified with a synthetic msg `{ flags: { dnd5e: { ... } } }` (no `evf` key).
    - ARW-04: Handler NEVER returns false (hooks contract — returning false cancels event propagation per reaction-watcher.ts Pitfall 1). Even on emit failure, handler returns void.
    - ARW-05: Handler is defensive — wrapped in try/catch; thrown errors during emit are swallowed with `console.warn('[action-result-watcher] hook handler threw', err)`. The Foundry session must never break because action-result emission failed.
    - ARW-06: `recipientUserId` is resolved from `msg.user` (the user who triggered the action via socketlib — Phase 7 dispatchTool uses `socketlib.executeAsGM(MODULE_ID, handlerId, ..., { bearer })` and the audit-log chat-message creator user is the SAME user). Fallback: `game.users.contents.find(u => u.character?.id === audit.actorId)?.id ?? '<unknown>'`.
    - ARW-07: When `audit.result.success === false`, `errorKind` is mapped from `audit.result.error` string using a deterministic mapper: `'no-targets' ← error includes 'no_targets'`; `'out-of-range' ← error includes 'out_of_range'`; `'out-of-resource' ← error includes 'no_resource' | 'insufficient'`; `'wrong-turn' ← error includes 'wrong_turn'`; `'gm-rejected' ← error includes 'no_gm_connected'`; default `'gm-rejected'` (catch-all).
    - ARW-08: `extractD20` reads `msg.rolls?.[0]?.dice?.[0]?.results?.[0]?.result` defensively — returns `null` (NOT undefined) on any missing path. dnd5e roll total path documented in foundry-globals.d.ts FoundryChatMessage shape; the watcher does NOT need a new global declaration (defensive `(msg as Record<string, unknown>).rolls` cast).
    - ARW-09: `inferOutcome` switch on `audit.tool`: for `'cast-spell'` + spell with attack roll → 'hit'/'miss' from d20 vs AC heuristic (Phase 8 broad heuristic — actual hit/miss is in dnd5e.postRollAttackV2 envelope which is NOT in scope; Phase 8 emits `'damage_dealt'` when `audit.result.success && damage` is non-empty, else `'no_roll'`); for `'weapon-attack'` → same pattern; for `'use-item'` → 'no_roll' (items rarely roll); for `'move-token'` → 'no_roll'; for `'drop-concentration'` → 'no_roll'. ARW-09 acceptance: each tool's outcome is in the canonical set (no `'critical'` slipping through).
    - ARW-10: `extractDamage` reads `msg.flavor` for a `\\d+(d\\d+|sl|hp)` regex match, returning the captured group or `undefined`. Phase 8 acceptance: regex matches "8d6 fire = 28" → returns "8d6 fire = 28" truncated to 24 chars (toast budget).
    - ARW-11: module.test.ts T-08-MOD-01 — `registerActionResultWatcher` is called exactly once in Hooks.once('ready'), AFTER `scheduleBearerRotation`, BEFORE `registerCanvasExtractor`. Verifies insertion ordering per CONTEXT §Area 2 sequence.
    - ARW-12: module.test.ts T-08-MOD-02 — registerComplexHandler count assertion stays exactly 14 (Phase 7 closure invariant — confirms NO new socketlib handler added by Plan 08-01).
  </behavior>
  <action>
    Create `packages/foundry-module/src/write-path/action-result-watcher.ts` following the reaction-watcher.ts structure verbatim:

    1. JSDoc header citing Plan 08-01, RESEARCH-style q1 resolution (createChatMessage hook fires AFTER ChatMessage doc creation; audit-log writes after dispatchTool's handler.handle() resolves, so the chat card has final state), and a CRITICAL warning: NEVER return false from the handler.

    2. Private helpers (all defensive, all wrapped in nullish-coalesce):
       - `extractD20(msg: unknown): number | null` — reads `(msg as any).rolls?.[0]?.dice?.[0]?.results?.[0]?.result`, returns null if any path missing or not number.
       - `extractDamage(msg: unknown): string | undefined` — reads `(msg as any).flavor`, regex `/\d+d\d+[^|]*?(sl|hp|fuoco|fire|dmg)/i`, returns match[0].slice(0, 24) or undefined.
       - `mapErrorToKind(errorStr: string): ActionErrorKind` — switch over substring match: 'no_targets' → 'no-targets'; 'out_of_range' → 'out-of-range'; 'no_resource'|'insufficient' → 'out-of-resource'; 'wrong_turn' → 'wrong-turn'; 'no_gm_connected'|default → 'gm-rejected'.
       - `inferOutcome(toolId: ToolId, status: 'success'|'failure', damage: string | undefined): ActionOutcome` — switch returning the canonical enum value (NEVER `'critical'`).
       - `resolveRecipientUserId(msg: unknown, audit: AuditEntry): string` — reads `(msg as any).user` (string user id) as primary; fallback `game.users.contents.find(u => u.character?.id === audit.actorId)?.id ?? '<unknown>'`. Never throws.

    3. Public `registerActionResultWatcher(emit: (payload: ActionResultPayload) => void): () => void`:
       - `const hookId = Hooks.on('createChatMessage', (...args: unknown[]): void => { ... });` — three args by Foundry contract (msg, opts, userId) but only `args[0]` is consumed.
       - Inside the handler, wrap in `try { ... } catch (err) { console.warn('[action-result-watcher] hook handler threw', err); }`.
       - Read `(msg as any).flags?.evf?.audit` — early return on undefined.
       - Type-narrow audit via duck-typing (`typeof audit.idempotencyKey === 'string'` etc.) — NEVER trust schema on a foreign Foundry doc.
       - Build payload, call `emit(payload)`.
       - NEVER `return false` — TypeScript void return type enforces this.
       - Return idempotent unsubscribe: `() => Hooks.off(hookId)`.

    4. Create co-located `action-result-watcher.test.ts` covering ARW-01..10. Use `vi.stubGlobal('Hooks', { on: vi.fn().mockReturnValue(42), off: vi.fn() })` + `vi.stubGlobal('game', { users: { contents: [...] } })` per reaction-watcher.test.ts pattern. Inject a `vi.fn()` as `emit` and assert call shape.

    5. Edit `packages/foundry-module/src/module.ts`:
       - Add import at the top: `import { registerActionResultWatcher } from './write-path/action-result-watcher.js';` and `import { R1_ACTION_RESULT_TYPE } from '@evf/shared-protocol';`
       - Inside `Hooks.once('ready', () => { ... })`, AFTER the `scheduleBearerRotation({...})` call and BEFORE the `registerCanvasExtractor({...})` call, insert:
         ```
         // Plan 08-01 — register the action-result watcher (ACT-01 telemetry).
         // Listens on createChatMessage; filters for flags.evf.audit.idempotencyKey;
         // emits r1.action.result envelopes via bridgeDeltaEmitter for typed toast rendering.
         // NO new socketlib handler — count stays 14 (Phase 7 invariant).
         registerActionResultWatcher((payload) => bridgeDeltaEmitter(R1_ACTION_RESULT_TYPE, payload));
         ```

    6. Edit `packages/foundry-module/src/module.test.ts` adding:
       - T-08-MOD-01: After triggering the ready hook, assert `registerActionResultWatcher` was called once. Spy via `vi.mock('./write-path/action-result-watcher.js', () => ({ registerActionResultWatcher: vi.fn() }))`.
       - T-08-MOD-02: Assert `registerComplexHandler` (or the equivalent socketlib mock) was called exactly 14 times — same invariant Phase 7 Plan 06 ships.

    Do NOT add a new socketlib handler. Do NOT add the test file under `__tests__/integration/` — keep it co-located with the source per Phase 7 convention.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run action-result-watcher &amp;&amp; pnpm --filter @evf/foundry-module test -- --run module</automated>
  </verify>
  <done>
    action-result-watcher.ts + action-result-watcher.test.ts exist; ARW-01..10 (10 tests) pass; module.test.ts T-08-MOD-01/02 pass; socketlib count stays 14 (assertion verifies); bridgeDeltaEmitter is called with R1_ACTION_RESULT_TYPE on hook fire; handler NEVER returns false; `pnpm typecheck` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: action-result-dispatcher (g2-app — double trust boundary + recipient filter + toast enqueue)</name>
  <files>
    packages/g2-app/src/panels/action-result-dispatcher.ts,
    packages/g2-app/src/panels/action-result-dispatcher.test.ts
  </files>
  <behavior>
    - ARD-01: `attachActionResultHandler(ws, toastQueue, locale, currentUserId)` subscribes a 'message' listener and returns an idempotent unsubscribe closure that calls `ws.removeEventListener`.
    - ARD-02: Valid envelope (EnvelopeSchema OK, type='r1.action.result', ActionResultPayloadSchema OK, recipientUserId===currentUserId) → toastQueue.enqueue called once with `{ id: '<deterministic>', severity, message, emittedAt: Date.now() }`.
    - ARD-03: Outer EnvelopeSchema parse failure (missing session_id) → console.warn + NO enqueue.
    - ARD-04: Envelope type !== 'r1.action.result' (e.g. 'character.delta') → silent return + NO enqueue + NO console.warn (other dispatchers own it — same pattern as reaction-toast-dispatcher.ts).
    - ARD-05: Inner ActionResultPayloadSchema parse failure (missing recipientUserId field) → console.warn + NO enqueue.
    - ARD-06 (T-08-02 mitigation): recipientUserId !== currentUserId → silent drop + NO enqueue + NO console.warn (cross-player leak — drop silently to avoid leaking the fact that another player triggered an action).
    - ARD-07: Severity mapping: status='success' + outcome='hit'/'damage_dealt' → severity='info'; status='success' + outcome='miss'/'save_success' → severity='info'; status='failure' + outcome='miss' → severity='info' (informational miss, NOT warn — UI-SPEC §7.10 State 3 example "i [d20=4] Mancato"); status='error' → severity='error'.
    - ARD-08: Message format (IT locale, hit case): `[d20=18] Colpito! 1d8+3 = 7 sl` for `{ d20: 18, outcome: 'hit', damage: '1d8+3 = 7 sl', status: 'success' }`. Width-budgeted to 38 cps (ToastSchema.message.max). Truncated with `…` if over.
    - ARD-09: Message format (IT, miss): `[d20=4] Mancato` for `{ d20: 4, outcome: 'miss', damage: undefined, status: 'failure' }`.
    - ARD-10: Message format (EN, save-success): `[d20=15 vs DC 13] Save ✓` — DC parsed from damage field if it matches `vs DC \d+` pattern; fallback `[d20=15] Save`.
    - ARD-11: Message format (error): `❌ <localized error.action.<kind>>` for `{ status: 'error', errorKind: 'no-targets', d20: null }` → IT: `❌ Nessun bersaglio`; EN: `❌ No targets`; DE: `❌ Keine Ziele`. Uses `getLabel('error.action.no-targets', locale)`.
    - ARD-12: Toast id is deterministic: `action-result-<idempotencyKey>`. Allows duplicate suppression upstream (ToastQueueLayer uses id as dwell-timer key — same id overwrites timer per toast-types.ts JSDoc).
    - ARD-13: Defensive catch — JSON.parse throw → console.warn('[action-result-dispatcher] handler threw', err) + NO enqueue. Same pattern as conc-conflict-dispatcher.ts outer try/catch.
  </behavior>
  <action>
    Create `packages/g2-app/src/panels/action-result-dispatcher.ts` mirroring `reaction-toast-dispatcher.ts` structure verbatim:

    1. JSDoc header citing Plan 08-01, T-08-01 (envelope forgery) + T-08-02 (cross-player leak) mitigations, double trust boundary description, and the severity/message format spec from CONTEXT.md §Decisions Result toast.

    2. Local interface `ActionResultDispatcherSocket` mirroring `ReactionDispatcherSocket` (add/removeEventListener + send).

    3. Local interface `ActionResultToastQueue { enqueue(toast: Toast): void }` (so tests inject a `vi.fn()` without depending on the full ToastQueueLayer class).

    4. Module-scope `formatActionMessage(payload, locale)` pure function:
       - On `status === 'error'`: return `❌ ${getLabel('error.action.' + payload.errorKind, locale)}` truncated to 38 cps with `…`.
       - On `outcome === 'no_roll'` (status='success'): return `[Uso] <toolId>` or similar minimal text (item/move use case — `damage` may carry an HP update like `HP 24/30 → 36/30` from CONTEXT.md State 3 example; use damage verbatim if present, truncated).
       - On `d20 !== null && outcome === 'hit'`: IT `[d20=${d20}] Colpito!${damage ? ' ' + damage : ''}` / EN `[d20=${d20}] Hit!${damage ? ' ' + damage : ''}` / DE `[d20=${d20}] Treffer!${damage ? ' ' + damage : ''}`.
       - On `outcome === 'miss'`: IT `[d20=${d20}] Mancato` / EN `[d20=${d20}] Miss` / DE `[d20=${d20}] Daneben`.
       - On `outcome === 'save_success'`: parse `damage` for `/vs DC (\d+)/`; output `[d20=${d20} vs DC ${dc}] Save ✓` (locale-neutral ✓ glyph per Pitfall 6 — same as toast severity prefix rationale).
       - On `outcome === 'save_fail'`: `[d20=${d20} vs DC ${dc}] Save ✗`.
       - On `outcome === 'damage_dealt'`: `${damage}` verbatim truncated to 38 cps.
       - Final step: `[...message].slice(0, 38).join('')`. NEVER throw — return a fallback like `'?'` if no match.

    5. Module-scope `formatSeverity(status, outcome)`:
       - `status === 'error'` → 'error'.
       - `outcome === 'miss'`, `outcome === 'save_fail'` → 'info' (per UI-SPEC §7.10 example).
       - All others → 'info'. (Reserved 'warn' for Phase 9 COMB-02 over-budget cases.)

    6. Public `attachActionResultHandler(ws, toastQueue, locale, currentUserId)`:
       - Same outer pattern as `attachReactionToastHandler`: JSON.parse → EnvelopeSchema.safeParse → narrow type to `R1_ACTION_RESULT_TYPE` → ActionResultPayloadSchema.safeParse.
       - ★ T-08-02 step: AFTER inner parse success, if `payloadParse.data.recipientUserId !== currentUserId` → return silently (NO console.warn — silent drop avoids signalling cross-player activity).
       - Build toast: `{ id: 'action-result-' + payload.idempotencyKey, severity: formatSeverity(...), message: formatActionMessage(...), emittedAt: Date.now() }` → `toastQueue.enqueue(toast)`.
       - Wrap in outer try/catch with `console.warn('[action-result-dispatcher] handler threw', err)`.
       - Return idempotent unsubscribe.

    7. Co-located `action-result-dispatcher.test.ts` with 13 ARD-* tests. Use the EventEmitter-backed `MockSocket` pattern from `reaction-toast-dispatcher.test.ts`. Test each ARD-XX behavior explicitly.

    Do NOT wire into boot-engine-core.ts in this plan — Plan 08-05 does the boot integration. Do NOT export `formatActionMessage` from the public surface (keep it module-private, test via the public path).

    No new socketlib handlers. No `activity.use(` reference anywhere — CI Gate 8 stays green.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run action-result-dispatcher</automated>
  </verify>
  <done>
    action-result-dispatcher.ts + action-result-dispatcher.test.ts exist; ARD-01..13 (13 tests) pass; T-08-02 cross-player drop silent (no console.warn); severity mapping correct for hit/miss/error; message format correct for IT/EN/DE across hit/miss/save/error cases; toast id deterministic on idempotencyKey; `pnpm typecheck` + `pnpm lint:ci` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry GM client → bridge (POST /internal/delta) | foundry-module emits r1.action.result envelope server-side; bridge forwards over WS to g2-app |
| Bridge → g2-app WebSocket | Inbound r1.action.result envelope crosses untrusted-input boundary into Even Realities App WebView |
| g2-app dispatcher → ToastQueueLayer | Inner trust boundary — only validated payloads reach the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Spoofing / Tampering | action-result-dispatcher WS receive | mitigate | Double trust boundary: EnvelopeSchema.safeParse (outer canonical wire) + ActionResultPayloadSchema.safeParse with `z.strictObject` (inner — rejects field smuggling, T-07-01 belt-and-suspenders pattern from Plan 07-01). Failure → console.warn + silent drop, NO enqueue. ARD-03/ARD-05 verify. |
| T-08-01-02 | Spoofing | action-result emission origin | mitigate | Watcher runs ONLY on the GM-side Foundry (game.user.isGM is implicit — `Hooks.on('createChatMessage')` only fires for documents the user has permission to read, and `flags.evf.audit` is GM-whisper-only per Phase 7 Plan 01 T-07-04 mitigation). A player client without GM perms NEVER sees the chat-card content + flags → cannot forge an emit. |
| T-08-02-01 | Information disclosure | Cross-player result leakage | mitigate | `recipientUserId` REQUIRED field on ActionResultPayloadSchema (ART-02 verifies). Dispatcher filters `payload.recipientUserId !== currentUserId` and silently drops (ARD-06 verifies). Silent drop chosen over console.warn so attacker can't probe other-player activity by checking warn telemetry. Plan 08-05 boots the dispatcher with `currentUserId` from bearer registry — Plan 02-05 bearer ties user_id to bearer. |
| T-08-02-02 | Information disclosure | Audit log over the wire | mitigate | `r1.action.result` envelope payload contains the d20/outcome/damage but DOES NOT include `bearer_id`, `actor_id`, or raw `audit.payload` (per ActionResultPayloadSchema strict-object 8 fields). Audit log itself (with bearer_id) stays GM-whispered per Phase 7 Plan 01 T-07-04 — never travels over r1.action.result. |
| T-08-01-03 | Tampering / Repudiation | Hook handler swallows errors | accept | `console.warn` on emit failure (ARW-05). Documented as acceptable per Phase 7 reaction-watcher.ts precedent: "audit failure must never crash the Foundry session" (audit-log.ts JSDoc). The audit log itself records every dispatchTool call regardless of toast emission — repudiation defence lives there. |
| T-08-03-01 | Denial of Service | Toast flood from rapid chat-card creation | mitigate | ToastQueueLayer enforces TOAST_BUFFER_SOFT_CAP=100 with FIFO drop-oldest + console.warn telemetry (Phase 4b Plan 03 T-4b-03-02). Action-result toasts get the same protection — no new DoS surface introduced. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 (workspace-wide TypeScript strict).
- `pnpm test` workspace passes: action-result.test.ts (12 ART-*) + action-result-watcher.test.ts (10 ARW-*) + action-result-dispatcher.test.ts (13 ARD-*) + module.test.ts (+ 2 T-08-MOD-*) all green.
- `pnpm lint:ci` exits 0 (Biome clean).
- CI Gate 8: `grep -rE 'activity\\.use\\(' packages/g2-app/src/ packages/bridge/src/ | grep -v '^#' | grep -c .` → 0 (ADR-0011 invariant).
- registerComplexHandler count assertion in module.test.ts T-08-MOD-02 returns exactly 14.
- `grep -c "register[A-Z]" packages/foundry-module/src/socketlib-handlers.ts` UNCHANGED from Phase 7 baseline (no new handler added).
</verification>

<success_criteria>
- 4 new files committed: action-result.ts (+ test), action-result-watcher.ts (+ test), action-result-dispatcher.ts (+ test).
- 3 files modified: shared-protocol/src/index.ts (export block), i18n-budgets.ts (5 keys), foundry-module/src/module.ts (wiring + import) + module.test.ts (2 new tests).
- All must_haves.truths verifiable by run of pnpm test workspace-wide.
- Atomic single commit per Phase 4b Plan 06 precedent (schema fan-out + consumer wiring in one go).
- Phase 7 invariants preserved: socketlib count = 14, ADR-0011 single-workflow-origin (no activity.use in g2-app), CI Gate 8 green.
- Self-check confirms files exist, key links visible via grep, threat dispositions documented.
</success_criteria>

<output>
After completion, create `.planning/phases/08-manual-action-ux/08-01-SUMMARY.md` documenting:
- One-liner: Wave 0 ATOMIC foundation — ActionResultPayloadSchema + createChatMessage watcher + dispatcher + 5 i18n error keys, single commit.
- Tasks completed table with commit hashes.
- Invariants confirmed (socketlib=14, no activity.use, CI Gate 8 green).
- Files created/modified list.
- Threat model dispositions (T-08-01-01..03, T-08-02-01/02, T-08-03-01).
- Self-check PASSED block.
</output>
