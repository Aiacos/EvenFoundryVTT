---
quick_id: 260529-g0j
phase: quick-260529-g0j
plan: 01
type: execute
wave: 1
depends_on: []
branch: develop
files_modified:
  - packages/foundry-module/src/write-path/tool-registry.ts
  - packages/foundry-module/src/write-path/tool-registry.test.ts
  - packages/foundry-module/src/write-path/combat-movement-tracker.ts
  - packages/foundry-module/src/write-path/combat-movement-tracker.test.ts
  - packages/g2-app/src/panels/reaction-prompt-dispatcher.ts
  - packages/g2-app/src/panels/reaction-prompt-dispatcher.test.ts
  - .changeset/quick-260529-g0j.md
autonomous: true
requirements: [WR-01, CMT-05, RPD-TIMEOUT-01]

must_haves:
  truths:
    - "Two concurrent dispatchTool calls with the same (bearer, idempotencyKey) execute handler.handle EXACTLY once and both receive the same ToolResult"
    - "deleteCombat hook clears _state and _lastPosition so a new combat starts from usedThisTurn=0 even before its first turn-advance"
    - "handleClose is a no-op (no second destroy bundle) once mountedPanel is already null"
    - "All pre-existing tests still pass; no behavioral regression"
  artifacts:
    - path: "packages/foundry-module/src/write-path/tool-registry.ts"
      provides: "in-flight dedup registry in dispatchTool preserving cache/audit/never-reject semantics"
      contains: "inFlight"
    - path: "packages/foundry-module/src/write-path/combat-movement-tracker.ts"
      provides: "deleteCombat hook + Hooks.off in unsubscribe closure"
      contains: "deleteCombat"
    - path: "packages/g2-app/src/panels/reaction-prompt-dispatcher.ts"
      provides: "early-return idempotency guard in handleClose"
      contains: "if (mountedPanel === null) return"
  key_links:
    - from: "dispatchTool concurrent caller"
      to: "single handler.handle invocation"
      via: "module-scoped inFlight Map<cacheKey, Promise<ToolResult>>"
      pattern: "inFlight\\.(get|set|delete)"
---

<objective>
Three independent, source-verified latent-bug fixes in the write-path / dispatcher
layer. Each is small, software-testable (NO hardware dependency), and gate-clean.
Verdicts confirmed on source 2026-05-29.

- FIX D (MEDIUM): close the in-flight idempotency race in `dispatchTool` — two
  overlapping calls with the same (bearer, idempotencyKey) currently both pass the
  cache-miss check and double-execute the Foundry write.
- FIX E (LOW/MED): reset combat-movement-tracker state on `deleteCombat` so stale
  `usedThisTurn` cannot leak into a freshly created combat.
- FIX F (LOW/MED, cosmetic): make `handleClose` idempotent so a late gesture after
  the 5s auto-timeout does not issue a redundant second destroy bundle.

Purpose: harden three confirmed concurrency/lifecycle edge cases without altering
any observable happy-path behavior.
Output: 3 source edits + 3 RED→GREEN tests + 1 changeset, gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Extracted from source 2026-05-29 — executor needs no codebase exploration. -->

dispatchTool (packages/foundry-module/src/write-path/tool-registry.ts:272-352):
  export async function dispatchTool(
    toolId: ToolId,
    payload: { args: unknown; idempotencyKey: string; bearer: string },
  ): Promise<ToolResult>
  Pipeline (do NOT change ordering/semantics of any preserved step):
    1. bearerHash = await hashBearer(payload.bearer); cacheKey = buildCacheKey(...)
    2. moduleIdempotencyStore.get(cacheKey) → if hit, return cached.result
    3. handler = TOOL_REGISTRY[toolId]; undefined → { success:false, error:'unknown_tool' }
    4. handler.argsSchema.safeParse → on !success return { success:false, error:msg }
    5. result = await handler.handle(parsed) wrapped in try/catch → normalised ToolResult
    6. if (result.success) moduleIdempotencyStore.set(cacheKey, { result, cachedAt }) — WR-01: failures NOT cached
    7. build AuditEntry (incl. resultAttackId extraction) + try { await writeAuditLog } catch {}
    return result   // always resolves, never rejects
  Module exports already test-visible: moduleIdempotencyStore, TOOL_REGISTRY, dispatchTool, registerToolHandler.

registerMovementTracker (packages/foundry-module/src/write-path/combat-movement-tracker.ts:148-292):
  export function registerMovementTracker(emit: (p: MovementBudgetPayload) => void): () => void
  Module-scoped state: _state: Map<actorId, {walkSpeed, usedThisTurn}>, _lastPosition: Map<actorId, {x,y}>
  Two existing hooks: updateToken (accumulate+emit), updateCombat (turn-advance reset).
  Unsubscribe closure (288-291): Hooks.off(updateTokenHookId); Hooks.off(updateCombatHookId);
  Defensive pattern: every hook body wrapped try/catch + console.warn; NEVER return false (void return).

handleClose (packages/g2-app/src/panels/reaction-prompt-dispatcher.ts:118-125):
  function handleClose(): void {
    if (timeoutId !== null) { clearTimeout(timeoutId); timeoutId = null; }
    mountedPanel = null;
    void deps.layerManager.bundle([{ type: 'destroy', z: ZIndex.Z2_OVERLAY }]);
  }
  Auto-timeout guard (152-158) already gates on `if (mountedPanel !== null)`; handleClose does not.

Test harnesses (reuse, do not rebuild):
  - tool-registry.test.ts: beforeEach clears moduleIdempotencyStore + TOOL_REGISTRY; registers stub handlers via TOOL_REGISTRY[id] = handler.
  - combat-movement-tracker.test.ts: makeHooksMock() captures updateToken/updateCombat handlers by event name; fireUpdateToken(tokenDoc, change); fireUpdateCombat(change); makeTokenDoc/makeGameMock helpers; vi.stubGlobal('Hooks', mock).
  - reaction-prompt-dispatcher.test.ts: vi.useFakeTimers; makeLayerManager() with bundle: vi.fn().mockResolvedValue(undefined); panel onClose captured via the ReactionPromptPanel constructor mock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (FIX D): in-flight idempotency dedup in dispatchTool</name>
  <files>packages/foundry-module/src/write-path/tool-registry.ts, packages/foundry-module/src/write-path/tool-registry.test.ts</files>
  <behavior>
    - RED: two CONCURRENT dispatchTool calls (fire both, do NOT await the first before
      starting the second, then `await Promise.all([p1, p2])`) with the SAME idempotencyKey
      + bearer + tool. The stub handler.handle counts invocations and resolves success after
      a microtask delay (e.g. `await Promise.resolve()` then return). Assert handle was called
      EXACTLY ONCE and r1 === r2 (same ToolResult). RED against current code: handle is called twice.
    - Preserve all existing dispatchTool tests (cache-hit returns cached, unknown_tool,
      validation failure, handler throw, audit isolation, cross-bearer, sequential dedup,
      failure-not-cached). They MUST still pass unchanged.
  </behavior>
  <action>
Target packages/foundry-module/src/write-path/tool-registry.ts dispatchTool (lines 272-352).

Add a module-scoped in-flight registry beside moduleIdempotencyStore:
  const inFlight = new Map&lt;string, Promise&lt;ToolResult&gt;&gt;();   // key = cacheKey

Restructure dispatchTool. KEEP step 1 (hashBearer + buildCacheKey) and step 2 (cache-hit
return) EXACTLY as-is, OUTSIDE the in-flight machinery — a cache hit must short-circuit
before touching inFlight. Then, before handler lookup, check the in-flight map; if absent,
wrap steps 3-7 in an inner async function and register its promise. Shape (pseudocode,
preserve every preserved-step semantic verbatim):

  // ...step 1 + step 2 unchanged (cacheKey computed, cache-hit early return)...

  // FIX D: collapse truly-concurrent duplicates to ONE dispatch
  const existing = inFlight.get(cacheKey);
  if (existing !== undefined) return existing;        // same ToolResult to both callers

  const run = async (): Promise&lt;ToolResult&gt; =&gt; {
    // Step 3 handler lookup (unknown_tool) — unchanged
    // Step 4 argsSchema.safeParse — unchanged
    // Step 5 try { await handler.handle } catch → normalise — unchanged
    // Step 6 if (result.success) moduleIdempotencyStore.set(...) — unchanged (WR-01)
    // Step 7 build AuditEntry + resultAttackId + try { await writeAuditLog } catch {} — unchanged
    return result;
  };

  const p = run();
  inFlight.set(cacheKey, p);
  try {
    return await p;
  } finally {
    inFlight.delete(cacheKey);   // always clear so later (non-overlapping) retries re-run
  }

CRITICAL constraints (verify each before GREEN):
  - Cache hit still returns cached.result and never touches inFlight.
  - Only SUCCESSFUL results are cached (WR-01) — failures stay uncached and, once the
    in-flight promise settles+deletes, a LATER retry re-executes (correct/intended).
  - The in-flight dedup collapses concurrent overlap ONLY: exactly ONE handler.handle(),
    ONE writeAuditLog, ONE moduleIdempotencyStore.set; the second concurrent caller awaits
    the same promise and receives the identical ToolResult.
  - dispatchTool still "always resolves, never rejects": because `run()` itself never throws
    (step 5 catches handler throws, step 7 catches audit throws), `await p` cannot reject.
    Do NOT add error swallowing around `await p` that would change the resolved value.
  - Do NOT cache failures via the in-flight map persisting — the finally{} delete guarantees
    the map only holds OVERLAPPING calls.
INV-4: zero dead code; TSDoc — extend the dispatchTool doc comment with a "Step 2.5: in-flight
dedup (FIX D)" note describing the concurrent-collapse guarantee.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run tool-registry</automated>
  </verify>
  <done>RED test (concurrent dedup) passes; handle called exactly once for overlap; all prior dispatchTool tests green; cache/audit/never-reject semantics unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (FIX E): deleteCombat reset in combat-movement-tracker</name>
  <files>packages/foundry-module/src/write-path/combat-movement-tracker.ts, packages/foundry-module/src/write-path/combat-movement-tracker.test.ts</files>
  <behavior>
    - RED: extend makeHooksMock() to also capture a `deleteCombat` handler (add an
      `if (event === 'deleteCombat')` branch returning a new MOCK_DELETE_COMBAT_HOOK_ID and
      storing the handler; add a `fireDeleteCombat()` helper). Drive movement via
      fireUpdateToken so _state accumulates usedThisTurn > 0 (reuse makeGameMock/makeTokenDoc,
      stub Hooks + game). Fire deleteCombat. Then perform a subsequent movement ("new combat")
      and assert the emitted usedThisTurn restarts from the fresh delta (i.e. state was cleared:
      usedThisTurn equals only the new delta, not new+old). RED against current code: no
      deleteCombat handler is registered → fireDeleteCombat throws / state persists.
    - Preserve CMT-01 (now should also assert deleteCombat registered) and CMT-01b
      (unsubscribe now also Hooks.off the deleteCombat id). Keep all other CMT tests passing.
  </behavior>
  <action>
Target packages/foundry-module/src/write-path/combat-movement-tracker.ts registerMovementTracker
(148-292). After the existing updateCombat hook block (ends ~285) and BEFORE the return closure,
add a third hook mirroring the existing defensive pattern:

  const deleteCombatHookId = Hooks.on('deleteCombat', (..._args: unknown[]): void =&gt; {
    try {
      _state.clear();
      _lastPosition.clear();
    } catch (err) {
      console.warn('[combat-movement-tracker] deleteCombat handler threw', err);
    }
    // NEVER return false — Foundry hook chain must not be interrupted
  });

Then extend the unsubscribe closure (288-291) to also detach it:
  return (): void =&gt; {
    Hooks.off(updateTokenHookId);
    Hooks.off(updateCombatHookId);
    Hooks.off(deleteCombatHookId);   // FIX E
  };

Leave the existing two hooks 100% unchanged. Update the registerMovementTracker TSDoc (140-146)
to document the deleteCombat reset and the third Hooks.off in the unsubscribe closure.
INV-4: no dead code; void return enforces the no-return-false contract.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run combat-movement-tracker</automated>
  </verify>
  <done>deleteCombat clears _state + _lastPosition; new-combat movement before first turn-advance starts at usedThisTurn=0; unsubscribe detaches all three hooks; CMT-01/01b updated and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (FIX F): handleClose idempotency guard in reaction-prompt-dispatcher</name>
  <files>packages/g2-app/src/panels/reaction-prompt-dispatcher.ts, packages/g2-app/src/panels/reaction-prompt-dispatcher.test.ts</files>
  <behavior>
    - RED: with vi.useFakeTimers, mount a panel (deliver a reaction-available envelope so
      mount() runs), advance fake timers 5000ms so the auto-timeout nulls mountedPanel and
      fires exactly one destroy bundle. Capture the panel's onClose (handleClose) via the
      ReactionPromptPanel constructor mock and invoke it. Assert layerManager.bundle was called
      for destroy EXACTLY ONCE total (the timeout's destroy), NOT twice. RED against current
      code: handleClose unconditionally fires a second destroy bundle.
    - Preserve RPD-TIMEOUT-01, RPD-CLEANUP-01 and all other RPD tests.
  </behavior>
  <action>
Target packages/g2-app/src/panels/reaction-prompt-dispatcher.ts handleClose (118-125).
Add an early-return idempotency guard as the FIRST statement of handleClose, mirroring the
auto-timeout's existing `if (mountedPanel !== null)` gate:

  function handleClose(): void {
    if (mountedPanel === null) return;   // FIX F: idempotent — late gesture after timeout is a no-op
    if (timeoutId !== null) { clearTimeout(timeoutId); timeoutId = null; }
    mountedPanel = null;
    void deps.layerManager.bundle([{ type: 'destroy', z: ZIndex.Z2_OVERLAY }]);
  }

Do NOT change any other line. Once the 5s timeout (or a prior close) has nulled mountedPanel,
a late onClose returns immediately — no second destroy bundle, no double-clear. Update the
handleClose doc/inline comment to note the idempotency guard.
INV-4: cosmetic-only fix; no behavioral change to the normal tap→close path.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run reaction-prompt-dispatcher</automated>
  </verify>
  <done>handleClose after timeout is a no-op (destroy bundle count stays 1); normal tap-close path unchanged; RPD-TIMEOUT-01 / RPD-CLEANUP-01 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge/client → foundry-module dispatchTool | retried tool invocations (BLE-lossy) cross here; concurrent duplicates possible |
| Foundry hook chain → combat-movement-tracker | combat lifecycle events drive module-scoped state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-g0j-01 | Tampering | dispatchTool double-write on concurrent retry | mitigate | FIX D in-flight Map collapses overlapping (bearer, idempotencyKey) to one handler.handle + one cache set + one audit write |
| T-g0j-02 | Information disclosure | stale usedThisTurn leaking into new combat | mitigate | FIX E deleteCombat clears _state/_lastPosition |
| T-g0j-03 | Denial of service | redundant z=2 destroy bundle (cosmetic) | accept→mitigate | FIX F early-return guard; impact is cosmetic, no data corruption |
| T-g0j-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies installed; no package-manager changes in this task |
</threat_model>

<verification>
Run from repo root after all three GREEN states (CLAUDE.md Phase 1+ gates):

1. `pnpm typecheck` → exit 0 (tsc --noEmit base + per-package).
2. `pnpm lint:ci` → exit 0 (biome ci, read-only, zero warnings — INV-4).
3. `pnpm test` → exit 0; total test count = 2723 baseline + 3 new RED→GREEN tests (one per fix) = 2726 (the planner allots one new test per fix; if Task 2 also amends CMT-01/01b in place rather than adding tests, the net new count is still ≥ +3 new assertions — record the exact delta in SUMMARY).
4. CI Gate 8 socketlib count = 17 — UNCHANGED (no socketlib registrations touched; verify no new/removed `socketlib.register*` in diff).
5. ADR-0011 single-workflow-origin — CLEAN: no `activity.use(` introduced in g2-app or bridge (all writes still flow through the foundry-module write-path; this task only edits foundry-module + g2-app dispatcher/panel glue, no Foundry activity calls added).
6. Backward compatibility — ALL pre-existing tests in the three touched files still pass:
   tool-registry (cache-hit, unknown_tool, validation, throw, audit isolation, cross-bearer,
   sequential dedup, failure-not-cached), CMT-02..08, RPD-* suite.

No hardware dependency: all three fixes are pure software logic, fully covered by unit tests
with mocked Hooks / fake timers / stub handlers. No Even Hub / G2 / R1 access required.
</verification>

<success_criteria>
- [ ] FIX D: concurrent dispatchTool dedup — handler.handle called exactly once for overlap, both callers receive identical ToolResult; cache/audit/never-reject semantics intact.
- [ ] FIX E: deleteCombat clears _state + _lastPosition; new-combat movement starts at usedThisTurn=0; unsubscribe detaches all three hooks.
- [ ] FIX F: handleClose idempotent — no second destroy bundle after timeout.
- [ ] Three gates green (typecheck, lint:ci, test); test-count delta ≥ +3, recorded in SUMMARY.
- [ ] CI Gate 8 socketlib = 17 unchanged; ADR-0011 clean (no activity.use in g2-app/bridge).
- [ ] One changeset declared (.changeset/quick-260529-g0j.md): patch bump for @evf/foundry-module + @evf/g2-app.
- [ ] Atomic Conventional Commits (one combined `fix(write-path): ...` or one per fix — executor's choice).
</success_criteria>

<output>
Create `.planning/quick/260529-g0j-hardening-d-e-f-in-flight-idempotency-de/260529-g0j-SUMMARY.md` when done.
Add changeset `.changeset/quick-260529-g0j.md` (patch: @evf/foundry-module, @evf/g2-app).
</output>
