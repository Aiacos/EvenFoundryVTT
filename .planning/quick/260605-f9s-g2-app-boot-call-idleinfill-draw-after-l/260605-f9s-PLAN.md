---
phase: quick-260605-f9s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/g2-app/src/internal/boot-engine-core.ts
  - packages/g2-app/src/raster/map-base-layer.ts
  - packages/g2-app/src/raster/map-base-layer.test.ts
  - packages/g2-app/src/__tests__/boot-engine-idle-render.test.ts
autonomous: true
requirements: [F9S-Z05, F9S-MAPCAP]
must_haves:
  truths:
    - "At boot in raster-idle, the z=0.5 strips (z05-combat-log/label/stats) render their spec content (`⚔ —`, `─── z=0.5 idle infill ───…`, stats strip) instead of the SDK default literal `Text`."
    - "At boot in raster-idle with no scene, the map-capture container (id7) is BLANK — it never shows the SDK default literal `Text`; the image tiles sit over a blank capture container."
    - "Glyph mode is NOT regressed: map-capture still receives the ASCII glyph grid when a glyph scene arrives, and is not spuriously blanked when a scene is queued."
    - "Full g2-app vitest suite passes with zero INV-1 fixture drift."
  artifacts:
    - path: "packages/g2-app/src/internal/boot-engine-core.ts"
      provides: "post-bundle idleInfill.draw() call (rejection-guarded, alongside step 13)"
      contains: "idleInfill.draw"
    - path: "packages/g2-app/src/raster/map-base-layer.ts"
      provides: "raster no-frame path that blanks map-capture instead of early-returning"
      contains: "map-capture"
    - path: "packages/g2-app/src/__tests__/boot-engine-idle-render.test.ts"
      provides: "regression test asserting idleInfill.draw() is invoked during boot / z05 written post-bundle"
  key_links:
    - from: "boot-engine-core.ts step 13"
      to: "IdleInfillLayer.draw()"
      via: "explicit post-bundle await with try/catch"
      pattern: "idleInfill\\.draw"
    - from: "MapBaseLayer.draw() raster no-frame path"
      to: "map-capture text container"
      via: "blank textContainerUpgrade"
      pattern: "textContainerUpgrade"
---

<objective>
Fix the two boot-time render defects that leave the EvenHub SDK default literal `Text` visible on the glasses in raster-idle:

1. **z=0.5 strips (ids 8-10) stay `Text`** — after `layerManager.bundle()` the single `rebuildPageContainer` flush resets text containers to the SDK default. Header/footer are repainted at step 12a and status-hud repaints via its WS subscription, but `idleInfill.draw()` is NEVER called post-bundle (despite the step-12a comment claiming idle infill "self-redraws via its own draw()"). FIX: call `await idleInfill.draw()` post-bundle, near step 13.

2. **map-capture (id7) stays `Text` in raster-idle** — in raster mode the visible map area is the image tile grid (`map-tile-0..3`); the `map-capture` TEXT container should be BLANK. But `MapBaseLayer.draw()` early-returns when `currentScene === null`, so map-capture is never written and keeps the SDK default `Text`. FIX: in the raster no-frame path, write an empty string to map-capture so the image tiles (when a real raster frame arrives) sit over a blank capture container.

Purpose: make the live raster-idle render match the canonical INV-1 fixture (`glyph-scene.raster-idle-it.txt`): z05 rows 19-21 show real idle content, the map area (rows 5-18) is the tile grid over a blank capture container, never the literal `Text`.

Output: 2 source edits (boot-engine-core, map-base-layer) + 2 regression tests; full g2-app suite green; zero INV-1 drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# z=0.5 layer — draw() is correct, just never called post-bundle
@packages/g2-app/src/status-hud/idle-infill-layer.ts

# Boot sequence — step 12 bundle (~1176), step 12a chrome (~1200), step 13 mapBase.draw (~1244); idleInfill constructed ~648
@packages/g2-app/src/internal/boot-engine-core.ts

# MapBaseLayer — draw() early-returns on null scene (~198); getCaptureContainer returns 'map-capture' (~124)
@packages/g2-app/src/raster/map-base-layer.ts

# glyph-renderer writes the ASCII grid into map-capture (~199) — glyph mode legitimately fills map-capture
@packages/g2-app/src/raster/glyph-renderer.ts

# Container registry — map-capture id7 (isEventCapture=1), z05-* ids 8-10
@packages/g2-app/src/engine/container-registry.ts

# Canonical INV-1 raster-idle fixture — map area (rows 5-18) is the IMAGE tile grid (NOT map-capture text);
# z05 content rows 19-21; map-capture must be BLANK in raster mode.
@packages/shared-render/src/fixtures/glyph-scene.raster-idle-it.txt

# Glyph-idle fixture — proves glyph mode map area IS text (ASCII grid in map-capture) → DO NOT blank glyph mode.
@packages/shared-render/src/fixtures/glyph-scene.glyph-idle.txt

# Existing test patterns
@packages/g2-app/src/raster/map-base-layer.test.ts
@packages/g2-app/src/status-hud/__tests__/idle-infill-layer.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Blank map-capture in raster no-frame path (FIX 2)</name>
  <files>packages/g2-app/src/raster/map-base-layer.ts, packages/g2-app/src/raster/map-base-layer.test.ts</files>
  <behavior>
    - MBL-IDLE-01 (raster, no scene): `draw()` with `currentScene === null` AND the resolved mode `'raster'` issues exactly one `bridge.textContainerUpgrade` for `map-capture` with empty-string content (so the SDK default `Text` is cleared). It must NOT call `controller.requestFrame` (no pixel data exists).
    - MBL-IDLE-02 (glyph, no scene): `draw()` with `currentScene === null` AND resolved mode `'glyph'` does NOT write map-capture (glyph map-capture is owned by `renderGlyphScene` when a scene arrives; blanking here would be unnecessary and could fight a queued glyph repaint). Assert no `textContainerUpgrade` call in this case (or, if a blank is written, it must equal the canonical glyph-idle empty state — prefer NO write to keep glyph untouched). DECISION: no write in glyph no-frame path.
    - MBL-IDLE-03 (raster, with scene): unchanged — `requestFrame` is called, no extra blank write to map-capture (the image tiles render the map; map-capture stays blank from the earlier no-frame write or is simply not text-written). Assert existing portrait-override / requestFrame behavior is preserved.
    - Regression: the 5 existing MBL-PORT-* tests still pass.
  </behavior>
  <action>
    In `MapBaseLayer.draw()` replace the unconditional early `return` on `currentScene === null` with mode-aware handling: resolve the operative mode via the existing `resolveAutoMode(this.layerManager.getMapMode(), this.controller.getBleVerdict())` BEFORE the null check, then — when there is no scene AND mode is `'raster'` — issue a single `bridge.textContainerUpgrade` for `map-capture` with `content: ''` (use `resolveContainerIdField('map-capture')` + `containerName: 'map-capture'`, mirroring the existing `ImageRawDataUpdate`/`textContainerUpgrade` construction style in this file and in glyph-renderer.ts). When there is no scene AND mode is `'glyph'`, return without touching the bridge (glyph map-capture is the glyph-renderer's job once a scene exists). Keep the existing raster/glyph scene-present paths exactly as they are. Add/extend the `draw()` TSDoc to document the no-frame raster blanking and WHY (raster map area is the image tile grid; map-capture text must be blank, never the SDK `Text` default — ties to INV-1 raster-idle fixture where rows 5-18 are image tiles). Do NOT use the Foundry `eq` Handlebars helper (irrelevant here, but note: this is pure TS). Use `TextContainerUpgrade` import (already used by glyph-renderer; add the import to map-base-layer.ts from `@evenrealities/even_hub_sdk` if not present). INV-4: real assertions, no dead code, TSDoc on the changed behavior.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app exec vitest run src/raster/map-base-layer.test.ts</automated>
  </verify>
  <done>map-base-layer.test.ts passes including MBL-IDLE-01/02/03 and the 5 MBL-PORT-* regressions; raster no-frame draw writes a blank map-capture, glyph no-frame draw writes nothing, scene-present paths unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Call idleInfill.draw() post-bundle at boot (FIX 1) + boot regression test</name>
  <files>packages/g2-app/src/internal/boot-engine-core.ts, packages/g2-app/src/__tests__/boot-engine-idle-render.test.ts</files>
  <behavior>
    - F9S-BOOT-01: during the boot finalize sequence, `idleInfill.draw()` is invoked exactly once AFTER `layerManager.bundle()` (so it is not overwritten by the bundle's `rebuildPageContainer` flush) and alongside/near the step-13 `mapBase.draw()` call. The call is rejection-guarded (try/catch, warn + continue) mirroring the step-12a chrome writers — an idle-infill draw failure MUST NOT abort an already-booted engine.
    - F9S-BOOT-02: the idleInfill.draw() write order is post-bundle (the assertion proves draw is called after bundle, OR that the z05 containers receive non-`Text` content post-bundle). Because the full `bootEngineCore` requires heavy WS/handshake mocking, prefer a focused test: either (a) extract a tiny pure helper `finalizeIdleRender(idleInfill, mapBase)` that awaits idleInfill.draw() then mapBase.draw() with per-call try/catch, and unit-test it with mocked layers (assert both draw()s called, order, and that an idleInfill.draw() rejection does NOT prevent mapBase.draw()); or (b) if extracting a helper is cleaner, export it from boot-engine-core and call it from step 13. Choose the minimal approach that yields a real regression assertion without mocking the whole engine. Document the choice in the SUMMARY.
  </behavior>
  <action>
    In `boot-engine-core.ts`, add an `await idleInfill.draw()` call in the boot finalize region (step 13, near the existing `await mapBase.draw()` at ~line 1244). Wrap it in its own try/catch (warn `[boot-engine-core] idle-infill draw failed:` + continue), matching the step-12a chrome-writer rejection-guard pattern (T-etr-03). It MUST run AFTER the step-12 `layerManager.bundle()` flush and after step 12a chrome, so the bundle's `rebuildPageContainer` cannot reset the z05 containers back to the SDK `Text` default. To make this testable without booting the full engine, extract a small exported pure helper (e.g. `finalizeIdleRender(idleInfill: { draw(): Promise<void> }, mapBase: { draw(): Promise<void> }): Promise<void>`) that awaits `idleInfill.draw()` (try/catch) then `mapBase.draw()` (try/catch), and call it from step 13 in place of the bare `await mapBase.draw()`. Add TSDoc to the helper explaining the post-bundle ordering requirement + the rejection-guard rationale. Remove the now-inaccurate clause in the step-12a comment that claims idle infill self-redraws "via its own draw()" — update it to state idle infill is explicitly drawn in step 13 (keep the comment accurate; INV-4 no misleading dead commentary).
    Create `packages/g2-app/src/__tests__/boot-engine-idle-render.test.ts` importing the exported helper. Tests: F9S-BOOT-01 — both `idleInfill.draw` and `mapBase.draw` are called once (vi.fn mocks); F9S-BOOT-02 ordering — idleInfill.draw resolves before mapBase.draw is invoked; F9S-BOOT-03 resilience — when idleInfill.draw rejects, mapBase.draw is STILL called and the helper resolves (no throw). Follow the vitest mock style in the existing boot-engine tests / map-base-layer.test.ts. INV-4: real assertions, no dead code, TSDoc on the new exported helper, biome clean.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app exec vitest run src/__tests__/boot-engine-idle-render.test.ts</automated>
  </verify>
  <done>boot-engine-idle-render.test.ts passes (F9S-BOOT-01/02/03); step 13 now calls idleInfill.draw() before mapBase.draw() via the rejection-guarded helper; the step-12a comment no longer falsely claims idle-infill self-redraws.</done>
</task>

</tasks>

<verification>
Run after both tasks, all must exit 0:

```bash
corepack pnpm --filter @evf/g2-app exec vitest run
corepack pnpm --filter @evf/g2-app typecheck
corepack pnpm lint:ci
```

INV-1 gate: the full g2-app vitest run includes the fixture/snapshot suite (`glyph-scene.raster-idle-it.txt` et al). Zero fixture drift is required. The changes IMPLEMENT existing fixture content (z05 already specified; map-capture blank in raster-idle) — they must NOT alter any fixture file. If any fixture snapshot changes, STOP: that signals the live render is matching a non-canonical state — re-check against `glyph-scene.raster-idle-it.txt` rather than updating the fixture.

INV-3 gate: this task implements content already present in the canonical fixtures/Specs mockups (z05 strips + blank raster map-capture). No Specs.md / README / showcase change is expected. If a divergence between the implementation and the fixture is discovered, STOP and surface it — do not silently edit the spec.
</verification>

<success_criteria>
- `idleInfill.draw()` is called post-bundle (step 13) under a rejection guard; the misleading step-12a comment is corrected.
- `MapBaseLayer.draw()` blanks map-capture in the raster no-frame path; glyph mode untouched; scene-present paths unchanged.
- New regression tests assert FIX 1 (idleInfill.draw invoked, ordered, resilient) and FIX 2 (raster no-frame blanks map-capture; glyph no-frame does not).
- Full g2-app vitest suite passes; typecheck clean; `lint:ci` clean.
- Zero INV-1 fixture drift; no INV-3 spec change.
</success_criteria>

<output>
Create `.planning/quick/260605-f9s-g2-app-boot-call-idleinfill-draw-after-l/260605-f9s-SUMMARY.md` when done.
</output>
