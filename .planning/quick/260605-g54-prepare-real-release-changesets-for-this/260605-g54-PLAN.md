---
phase: quick-260605-g54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .changeset/g54-bridge-on-connect-character.md
  - .changeset/g54-g2app-render-and-selection.md
  - .changeset/g54-shared-protocol-handshake-actorid.md
  - docs/self-hosting.md
  - README.md
autonomous: true
requirements: [DIST-EHUB-01, RELEASE-G54]
must_haves:
  truths:
    - "Three changesets exist (one per shipped package) and `corepack pnpm changeset:status` reports all three pending with correct minor bumps."
    - "g2-app builds clean (`corepack pnpm --filter @evf/g2-app build` produces dist/index.html) and the pack:ehpk path is documented to match package.json + sync-app-whitelist.mjs exactly."
    - "docs/self-hosting.md walks a NEW user end-to-end (prereqs → .env → compose → build+sync+pack → glasses install → Foundry module + pairing → pick character → HUD), every command verified against actual scripts/compose files."
    - "README has a Self-Hosting pointer that links docs/self-hosting.md and stays coherent with Specs §11.5.3/§3.3/§3.7 (no contradiction)."
  artifacts:
    - path: ".changeset/g54-bridge-on-connect-character.md"
      provides: "@evf/bridge minor changeset (d0v + dog + flv)"
      contains: "@evf/bridge"
    - path: ".changeset/g54-g2app-render-and-selection.md"
      provides: "@evf/g2-app minor changeset (e9t + etr + f9s + flv)"
      contains: "@evf/g2-app"
    - path: ".changeset/g54-shared-protocol-handshake-actorid.md"
      provides: "@evf/shared-protocol minor changeset (flv actorId)"
      contains: "@evf/shared-protocol"
    - path: "docs/self-hosting.md"
      provides: "Consolidated end-to-end self-hosting guide"
      min_lines: 80
  key_links:
    - from: "docs/self-hosting.md"
      to: "docs/release/{bridge,evenhub,foundry-module}.md"
      via: "markdown links (consolidate, do not duplicate)"
      pattern: "docs/release/"
    - from: "README.md"
      to: "docs/self-hosting.md"
      via: "Self-Hosting section link"
      pattern: "docs/self-hosting"
---

<objective>
Prepare the REAL release of this session's character-data/render/selection work and write a consolidated self-hosting guide so OTHER users can deploy their own stack and install the plugin on physical glasses.

Three deliverables in one plan:
1. Three changesets (one per shipped package) in the repo's existing `.changeset` format — NO `changeset version`.
2. Verify + document the working g2-app build + Even Hub `.ehpk` pack path (no private origins committed).
3. New `docs/self-hosting.md` end-to-end guide + a concise README "Self-Hosting" pointer.

Purpose: Turn the now-live (sim-verified) on-connect HUD render + character selection into a versioned release and make the stack reproducible by a stranger.
Output: 3 changeset files, 1 new docs file, README edit, a verified g2-app build (artifact, not committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.changeset/config.json
@.changeset/real-foundry-pairing-push.md
@packages/g2-app/package.json
@packages/g2-app/app.json
@deploy/sync-app-whitelist.mjs
@deploy/.env.example
@deploy/README.md
@docs/release/evenhub.md
@docs/release/foundry-module.md
@README.md
</context>

<key_facts>
Load-bearing facts verified from the repo (do NOT re-derive or invent — use these verbatim):

- **Current versions:** @evf/bridge 0.1.1, @evf/g2-app 0.2.5, @evf/shared-protocol 0.2.0. All this session's bumps are **minor** (additive features, no breaking changes). Do NOT bump @evf/foundry-module (untouched this session; STATE confirms socketlib count stayed 17, no foundry-module edits).
- **Changeset format** (from `.changeset/real-foundry-pairing-push.md`): YAML frontmatter delimited by `---` with one `"@evf/pkg": minor` line per package, then a prose body. `config.json` has `commit:false`, `privatePackages.version:true` (private packages ARE versioned). The project uses a Changesets Release PR via CI — **do NOT run `changeset version`**.
- **pack:ehpk command** (verbatim from packages/g2-app/package.json line 12):
  `pack:ehpk` = `vite build && npx --yes @evenrealities/evenhub-cli pack app.json dist -o evenfoundryvtt.ehpk`.
  Invoked workspace-wide as `corepack pnpm --filter @evf/g2-app pack:ehpk` → emits `packages/g2-app/evenfoundryvtt.ehpk`.
- **build command:** `corepack pnpm --filter @evf/g2-app build` (= `vite build`) → emits `packages/g2-app/dist/index.html` (vite uses `root: 'src'`, `outDir: '../dist'` per docs/release/evenhub.md line 139).
- **app.json whitelist** is currently committed as SAFE placeholders: `https://bridge.REPLACE-WITH-YOUR-ORIGIN.example` + `https://plugin-host.REPLACE-WITH-YOUR-ORIGIN.example`. These are NOT private origins — leave them as-is. `deploy/sync-app-whitelist.mjs` rewrites them from `EVF_BRIDGE_HOST`+`EVF_PLUGIN_HOST` in `deploy/.env`; it **exits 1** if those are missing or still `*.example.com`. Each self-hoster runs sync + pack with THEIR origins (the published-once-for-all model does NOT work — every user has a different bridge origin). The executor must NEVER commit real origins into app.json.
- **HTTPS compose:** `docker compose -f deploy/docker-compose.yml -f deploy/docker-compose.https.yml up -d --build` (bridge + Caddy auto-Let's-Encrypt; from docs/release/evenhub.md line 101). Plain LAN/dev: `docker compose up -d` from `deploy/`.
- **.env keys** (deploy/.env.example): `EVF_INTERNAL_SECRET` (openssl rand -base64 32; bridge refuses to start without it), `EVF_PLUGIN_HOST_URL` (bridge CORS origin), `EVF_BRIDGE_HOST` + `EVF_PLUGIN_HOST` (HTTPS hostnames → whitelist), `EVF_PLUGIN_HOST_URL`.
- **Glasses install reality (INV-2):** Even Hub CLI is `login/init/qr/pack` ONLY — NO non-interactive publish/submit. PERSONAL physical-glasses testing = **dev-mode QR** (`corepack pnpm --filter @evf/g2-app dev` + `dev:qr`) pointing at the LAN dev server (no portal review, no expiry, hot reload). Public distribution = MANUAL portal upload of the `.ehpk`, review-gated. The "trial version expired" trap: uploading an `.ehpk` as a portal trial expires; dev-mode does not.
- **Foundry module install:** manifest URL `https://github.com/Aiacos/EvenFoundryVTT/releases/latest/download/module.json` (auto-installs socketlib, midi-qol; needs dnd5e ≥5.3.3). Then open the EVF Bridge Configuration dialog → set `bridgeUrl` + `bridgeInternalSecret` (= `EVF_INTERNAL_SECRET`) → pair (paste bearer token; NO QR — no camera API). Pick character at wizard step 3 → HUD renders the chosen PC.
- **pnpm convention:** local pnpm is NOT on PATH (see MEMORY: corepack pnpm). `docs/release/debug-harness.md` already uses `corepack pnpm`; `evenhub.md`/`foundry-module.md` use bare `pnpm`. The NEW doc uses `corepack pnpm` throughout (matches local reality + debug-harness). All `<verify>` commands in this plan also use `corepack pnpm`.
- **INV-3 verdict (do NOT change Specs/showcase):** Specs.md already documents the full deployment model — §3.7 (3-hop), §3.3 (no-wildcard whitelist, line 335/472), §11.5.3 (Docker Compose homelab), §495 (dev-mode QR vs manual portal). The new guide is PURELY ADDITIVE operational docs consistent with the spec. No Specs.md or showcase edit needed. README edit is additive (links the new guide), not a cross-cutting fact change.
</key_facts>

<tasks>

<task type="auto">
  <name>Task 1: Write the three release changesets</name>
  <files>.changeset/g54-bridge-on-connect-character.md, .changeset/g54-g2app-render-and-selection.md, .changeset/g54-shared-protocol-handshake-actorid.md</files>
  <action>
Create three changeset files in the EXACT format of `.changeset/real-foundry-pairing-push.md` (YAML frontmatter `---` block with `"@evf/pkg": minor` lines, then prose body). Derive each summary from the STATE.md rows for tasks d0v, dog, e9t, etr, f9s, flv (read them; do not invent). One changeset per package:

1. `g54-bridge-on-connect-character.md` — frontmatter `"@evf/bridge": minor`. Body summarizes the bridge-side work: on-connect initial `character.delta` push (d0v `DeltaEmitter.sendInitialToSession` + `pushInitialCharacterDelta`), the `CharacterSnapshotCache` serving `evf.getCharacterSnapshot` / `GET /v1/character/:actorId` (dog), and per-session `selectedActorId` with `character.delta` targeting that prevents cross-player leakage (flv). Mention `buildServer` stays additive, socketlib count unchanged (17), no new deps.

2. `g54-g2app-render-and-selection.md` — frontmatter `"@evf/g2-app": minor`. Body summarizes: persistent `wsEvents` listener with per-channel last-value replay so the on-connect delta is not dropped (e9t), production-clean HUD header/footer chrome replacing SDK "Text" placeholders (etr), the idle-render / map-capture fix that clears the last placeholders post-bundle (f9s), and `characterId → handshake actorId` selection threading with `?actor=<id>` URL override (flv). Note INV-1 zero fixture drift, no new deps.

3. `g54-shared-protocol-handshake-actorid.md` — frontmatter `"@evf/shared-protocol": minor`. Body: optional `actorId` added to `HandshakeClientSchema` (additive, enables per-session character selection — flv).

Keep bodies concise (3-8 lines each), factual, matching the existing changeset prose register. Do NOT include @evf/foundry-module in any changeset. Do NOT run `changeset version`.
  </action>
  <verify>
    <automated>corepack pnpm changeset:status</automated>
  </verify>
  <done>`corepack pnpm changeset:status` lists exactly the three packages (@evf/bridge, @evf/g2-app, @evf/shared-protocol) each with a `minor` bump pending; no @evf/foundry-module entry; exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the g2-app build + document the .ehpk pack path (no private origins committed)</name>
  <files>packages/g2-app/app.json</files>
  <action>
Confirm the g2-app build compiles (this proves the release artifact is producible). Run `corepack pnpm --filter @evf/g2-app build` and confirm `packages/g2-app/dist/index.html` exists.

Do NOT commit a real `.ehpk` and do NOT write real origins into `packages/g2-app/app.json`. The committed app.json must keep its safe `REPLACE-WITH-YOUR-ORIGIN.example` placeholders (verify they are still present and unmodified at end of task — `git diff packages/g2-app/app.json` must be empty unless you intentionally improved a comment, which is discouraged).

Optionally (pipeline confidence only, NOT committed): you MAY run `corepack pnpm --filter @evf/g2-app pack:ehpk` to confirm the pack tool runs against the placeholder app.json and emits `packages/g2-app/evenfoundryvtt.ehpk`. If you do, DELETE the produced `.ehpk` before finishing (it is build output with placeholder whitelist; never commit it — and `*.ehpk` should already be git-ignored; confirm with `git status --porcelain | grep ehpk` returns nothing tracked). The committed deliverable is the BUILD proof + documented pack path, not an artifact.

Record for the SUMMARY: (a) exact pack command, (b) that whitelist is per-user via `deploy/sync-app-whitelist.mjs` + `pack:ehpk` and NOT committed, (c) that app.json stays placeholder.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app build && test -f packages/g2-app/dist/index.html && grep -q "REPLACE-WITH-YOUR-ORIGIN" packages/g2-app/app.json && echo OK</automated>
  </verify>
  <done>g2-app build succeeds and emits dist/index.html; app.json still contains the placeholder whitelist (no real origins committed); no `.ehpk` is staged/tracked in git.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/self-hosting.md + add README Self-Hosting pointer</name>
  <files>docs/self-hosting.md, README.md</files>
  <action>
Create `docs/self-hosting.md` — a consolidated end-to-end guide for a NEW self-hoster. Use `corepack pnpm` for every pnpm command. LINK to existing component runbooks (`docs/release/bridge.md`, `docs/release/evenhub.md`, `docs/release/foundry-module.md`, `deploy/README.md`, `deploy/.env.example`) for detail instead of duplicating them. Structure (verify every command against the actual scripts/compose files in <key_facts> — invent NO flags/filenames):

1. **Overview** — the 4-component stack: Foundry module → bridge → plugin-host → glasses (reuse the README one-line architecture diagram form). State the model: Docker Compose homelab single-tenant (Specs §11.5.3), per-user no-wildcard whitelist (Specs §3.3), 3-hop deployment (Specs §3.7).
2. **Prerequisites** — Docker + Compose v2, `openssl`, Node 24 + `corepack` (pnpm via corepack), a publicly-resolvable domain + HTTPS (or a Cloudflare Tunnel / Tailscale Funnel for homelab) OR LAN-only dev path; a Foundry VTT world running dnd5e ≥5.3.3.
3. **Step 1 — Clone + configure deploy/.env** — `cp deploy/.env.example deploy/.env`; set `EVF_INTERNAL_SECRET=$(openssl rand -base64 32)`, `EVF_BRIDGE_HOST`, `EVF_PLUGIN_HOST`, `EVF_PLUGIN_HOST_URL`. Link deploy/.env.example.
4. **Step 2 — Boot bridge + plugin-host** — HTTPS: `docker compose -f deploy/docker-compose.yml -f deploy/docker-compose.https.yml up -d --build`; LAN/dev alternative noted. Health check `curl https://<bridge-host>/healthz`. Link deploy/README.md.
5. **Step 3 — Build + pack g2-app for YOUR origins** — `corepack pnpm --filter @evf/g2-app build`, then `node deploy/sync-app-whitelist.mjs` (writes your two HTTPS origins into app.json whitelist), then `corepack pnpm --filter @evf/g2-app pack:ehpk` → `packages/g2-app/evenfoundryvtt.ehpk`. EXPLAIN the per-user whitelist model: the committed app.json ships placeholders; each self-hoster must sync + pack with THEIR origins; no-wildcards (Specs §3.3). Link docs/release/evenhub.md.
6. **Step 4 — Install on physical glasses** — TWO paths, clearly separated: (a) PERSONAL/dev — `corepack pnpm --filter @evf/g2-app dev` + `corepack pnpm --filter @evf/g2-app dev:qr`, scan QR in the Even app (same LAN, hot reload, no expiry, no portal — the realistic path); (b) PUBLIC — manual portal upload of the `.ehpk`, review-gated (NO CLI publish — Even Hub CLI is login/init/qr/pack only, INV-2). Call out the "trial version expired" trap. Link docs/release/evenhub.md.
7. **Step 5 — Install + configure the Foundry module** — manifest URL `https://github.com/Aiacos/EvenFoundryVTT/releases/latest/download/module.json` (auto-installs socketlib + midi-qol); open the EVF Bridge Configuration dialog → set `bridgeUrl` + `bridgeInternalSecret` (= your `EVF_INTERNAL_SECRET`) → pair by pasting the bearer token (NO QR scan — no camera API). Link docs/release/foundry-module.md.
8. **Step 6 — Pick your character + see the HUD** — wizard step 3 selects the PC; the bridge pushes that actor's `character.delta` on connect and the glasses HUD renders the chosen PC (cite this session's behavior).
9. **Troubleshooting / cross-links** — short table pointing to the per-component runbooks for deeper detail.

Then edit `README.md`: add a concise **Self-Hosting** subsection (place it within/after the existing `## Installation` area, near the Bridge/G2 App sections ~lines 56-97, integrating coherently — do NOT contradict the existing per-component install steps). It must: (1) summarize the 4-component stack in one line, (2) link `docs/self-hosting.md` as the end-to-end guide, (3) reaffirm the per-user-whitelist + manual-portal reality in one sentence. Keep it additive — do not rewrite the existing component sections. INV-3: this is additive operational docs consistent with Specs §11.5.3/§3.3/§3.7, so NO Specs.md/showcase edit is required (state this in the SUMMARY).
  </action>
  <verify>
    <automated>test -f docs/self-hosting.md && grep -q "sync-app-whitelist.mjs" docs/self-hosting.md && grep -q "docker-compose.https.yml" docs/self-hosting.md && grep -q "login/init/qr/pack\|login.*init.*qr.*pack\|NO CLI publish\|no.*publish" docs/self-hosting.md && grep -q "docs/self-hosting" README.md && echo OK</automated>
  </verify>
  <done>docs/self-hosting.md exists with all 6 steps, correct commands (build, sync-app-whitelist, pack:ehpk, https compose, manifest URL, bridge config dialog), links to the three release runbooks, and uses `corepack pnpm`; README has a Self-Hosting pointer linking the guide; no broken cross-references.</done>
</task>

</tasks>

<verification>
- `corepack pnpm changeset:status` shows the three minor bumps (bridge, g2-app, shared-protocol), no foundry-module.
- `corepack pnpm --filter @evf/g2-app build` succeeds; dist/index.html present.
- `git diff --name-only` shows ONLY: 3 new `.changeset/*.md`, `docs/self-hosting.md`, `README.md` (app.json unchanged; no `.ehpk` tracked).
- Every command in docs/self-hosting.md matches packages/g2-app/package.json scripts + deploy/sync-app-whitelist.mjs + deploy/docker-compose*.yml exactly (no invented flags/filenames).
- No `changeset version` was run (package.json versions unchanged at bridge 0.1.1 / g2-app 0.2.5 / shared-protocol 0.2.0).
</verification>

<success_criteria>
- DELIVERABLE 1: 3 changesets added in repo format, verified via `corepack pnpm changeset:status`; no manual version bump.
- DELIVERABLE 2: g2-app build verified producible; pack:ehpk path documented; app.json keeps placeholder whitelist (no private origins committed); no `.ehpk` committed.
- DELIVERABLE 3: docs/self-hosting.md — accurate, end-to-end, links existing runbooks, `corepack pnpm` throughout.
- DELIVERABLE 4: README Self-Hosting pointer, coherent with Specs (INV-3 additive-only; no Specs/showcase change).
</success_criteria>

<output>
Create `.planning/quick/260605-g54-prepare-real-release-changesets-for-this/260605-g54-SUMMARY.md` when done. In it, explicitly record: (a) the exact `pack:ehpk` command path, (b) how the app.json whitelist is handled without committing private origins, (c) that no Specs/showcase change was needed (INV-3 additive-only) with the spec sections that already cover it.
</output>
