---
phase: quick-260610-d42
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/foundry-module/src/canvas-extractor.ts
  - packages/foundry-module/src/canvas-extractor.test.ts
  - packages/g2-app/src/hud/map-canvas-layer.ts
  - packages/g2-app/src/hud/__tests__/map-canvas-layer.test.ts
  - packages/g2-app/src/scene-input.ts
  - packages/g2-app/src/__tests__/scene-input.test.ts
  - packages/g2-app/src/internal/boot-engine-core.ts
  - packages/g2-app/src/status-hud/canvas-status-hud-layer.ts
  - packages/g2-app/src/status-hud/__tests__/canvas-status-hud-layer.test.ts
  - packages/g2-app/src/engine/container-registry.ts
  - packages/g2-app/src/engine/__tests__/container-registry.test.ts
  - packages/g2-app/src/panels/root-exit-dispatcher.ts
  - packages/g2-app/src/panels/__tests__/root-exit-dispatcher.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Foundry viewport screenshots stream continuously to the glasses (~1s cadence) even when no hook fires"
    - "In canvas mode, incoming frame_pixels paint a full-screen map at z=0 in the compositor (NOT via the legacy RasterController map-tile path)"
    - "The full-screen opaque chrome no longer hides the z=0 map; status info renders in a native G2 text container instead of over the raster"
    - "A new frame triggers hudDeltaDriver.requestCycle() so the debounced delta loop pushes only changed sub-tiles"
    - "Canvas-mode root double-tap exit fires again (getTopLayer no longer null at root)"
    - "Exactly ONE isEventCapture:1 container remains per page (hud-capture, id 4)"
  artifacts:
    - path: "packages/g2-app/src/hud/map-canvas-layer.ts"
      provides: "z=0 CanvasLayer holding the latest 400x200 frame, paints via putImageData, dirty-on-new-frame, requestCycle trigger"
      min_lines: 60
    - path: "packages/foundry-module/src/canvas-extractor.ts"
      provides: "Continuous periodic capture (intervalMs) + canvasPan hook + interval cleared by unregister"
      contains: "intervalMs"
    - path: "packages/g2-app/src/engine/container-registry.ts"
      provides: "hud-status non-capture text container in the HUD raster page schema"
      contains: "hud-status"
  key_links:
    - from: "packages/g2-app/src/scene-input.ts"
      to: "MapCanvasLayer.setFrame"
      via: "canvas-mode frame_pixels routing"
      pattern: "setFrame"
    - from: "packages/g2-app/src/hud/map-canvas-layer.ts"
      to: "hudDeltaDriver.requestCycle"
      via: "onFrame callback injected at construction"
      pattern: "requestCycle|onFrame"
    - from: "packages/foundry-module/src/canvas-extractor.ts"
      to: "setInterval/clearInterval"
      via: "periodic capture lifecycle"
      pattern: "setInterval"
---

<objective>
Implement the core of roadmap Phase 27 (user-amended design 2026-06-10): Foundry pre-rendered viewport screenshots stream CONSTANTLY to the glasses as a full-screen map occupying all 4 image tiles via the compositor z=0, with status info moved OUT of the raster into a native G2 text container. Closes the documented coexistence gap in `.planning/debug/resolved/map-frame-pipeline-dims.md` where the legacy RasterController scene path (map-tile ids 0-3) clobbered the HudDeltaDriver tiles.

Purpose: The player sees the live Foundry map full-screen on the glasses, refreshed continuously, while glanceable status (PG name, PF/HP, turn/combat) renders as crisp native text instead of dithered raster — the core "never look away from the table" value with a map substrate.

Output: Continuous Foundry capture; a new MapCanvasLayer compositor layer at z=0; scene-input routed to it in canvas mode (legacy RasterController scene path explicitly retired in canvas mode per INV-4); status relocated to a native text container; canvas-mode root-exit double-tap restored.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/resolved/map-frame-pipeline-dims.md
@packages/foundry-module/src/canvas-extractor.ts
@packages/g2-app/src/engine/container-registry.ts
@packages/g2-app/src/engine/hud-delta-driver.ts
@packages/g2-app/src/status-hud/canvas-status-hud-layer.ts
@packages/g2-app/src/scene-input.ts
@packages/g2-app/src/engine/layer-manager.ts
@packages/g2-app/src/engine/canvas-compositor.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/panels/root-exit-dispatcher.ts

# Local toolchain: pnpm is NOT on PATH. Invoke via corepack: `corepack pnpm <cmd>`.
# Commitlint scope-enum: [g2-app, bridge, foundry-module, shared-protocol, shared-render, validation-harness, foundry-mcp, *]; header max 100 chars.
# Known flake: wizard-commands teardown can exit 1 with 0 failures — re-run once.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Continuous periodic capture in the Foundry canvas extractor</name>
  <files>packages/foundry-module/src/canvas-extractor.ts, packages/foundry-module/src/canvas-extractor.test.ts</files>
  <behavior>
    - CE-INT-1: registerCanvasExtractor with no intervalMs defaults to a ~1000ms periodic capture; advancing fake timers by N*intervalMs (no hooks fired) calls emit N times with a valid 400x200 FramePixels payload.
    - CE-INT-2: the returned unregister fn clears the interval — after unregister, advancing timers by 5*intervalMs fires emit zero additional times.
    - CE-INT-3: a canvasPan hook is registered (alongside canvasReady, drawCanvas, refreshToken, updateScene); firing canvasPan schedules a debounced (200ms) extract exactly like the other 4 hooks.
    - CE-INT-4: the existing idempotent singleton (_registered) still holds — a second registerCanvasExtractor returns a no-op unregister and does NOT install a second interval or duplicate hooks.
    - Existing CE-1..CE-7 tests still pass (200ms hook debounce preserved; the 4 original hooks still registered; CE-1 hook-count assertion updated to include canvasPan = 5 hooks).
  </behavior>
  <action>
    Extend CanvasExtractorOpts with `readonly intervalMs?: number` (default DEFAULT_INTERVAL_MS = 1000 — declare alongside DEFAULT_DEBOUNCE_MS). In registerCanvasExtractor, after wiring the 4 existing Hooks.on calls, ALSO register `Hooks.on('canvasPan', onHookFire)` and add `{ event: 'canvasPan', fn: onHookFire }` to the handlers array so the static-grep five-hook contract and the unregister Hooks.off loop both cover it. Add a `setInterval(performExtract, intervalMs)` that fires the SAME `performExtract` closure used by the debounce (continuous capture independent of hooks); store the handle and `clearInterval` it inside the returned unregister fn (next to the existing `clearTimeout(timer)`). The interval respects the idempotent singleton — only installed on the first registration; the no-op unregister branch installs nothing. Keep the 200ms hook debounce semantics unchanged. module.ts keeps passing only `{ emit }` (defaults apply — do NOT touch module.ts). Update the module JSDoc "four Foundry hooks" prose to "five Foundry hooks (canvasReady, drawCanvas, refreshToken, updateScene, canvasPan) + a continuous interval capture". Update the CE-1 test's hook-count expectation to 5 and add CE-INT-1..4 with vitest fake timers.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/foundry-module test -- canvas-extractor</automated>
  </verify>
  <done>canvas-extractor emits continuously at intervalMs (default 1000ms) with no hook fires; canvasPan is the 5th registered hook; unregister clears both the debounce timer and the interval; idempotent singleton unchanged; CE + CE-INT tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MapCanvasLayer at z=0 + scene-input routing + retire legacy RasterController scene path in canvas mode</name>
  <files>packages/g2-app/src/hud/map-canvas-layer.ts, packages/g2-app/src/hud/__tests__/map-canvas-layer.test.ts, packages/g2-app/src/scene-input.ts, packages/g2-app/src/__tests__/scene-input.test.ts, packages/g2-app/src/internal/boot-engine-core.ts</files>
  <behavior>
    - MCL-1: MapCanvasLayer implements CanvasLayer (attachCanvas/paint/isDirty + draw/destroy/getContainerCount). getContainerCount returns {image:0, text:0} (canvas-budget compliant). It does NOT provide a capture container (getCaptureContainer omitted — CanvasStatusHudLayer remains the sole hud-capture provider).
    - MCL-2: setFrame(rgba 400x200x4, w, h) caches the frame, sets _dirty=true, and invokes the injected onFrame callback exactly once per setFrame.
    - MCL-3: paint() blits the cached frame via putImageData onto its layer canvas at (0,0); when no frame received yet, paint() is a no-op (leaves canvas untouched) and still resets _dirty=false as its last line.
    - MCL-4: isDirty() is true at construction and after each setFrame; false after paint().
    - SI-CANVAS-1: attachSceneInputToWs in canvas mode routes a valid frame_pixels envelope to MapCanvasLayer.setFrame (NOT RasterController.requestFrame).
    - SI-CANVAS-2: the padFrameToCanonical normalization to 400x200 is preserved before setFrame.
  </behavior>
  <action>
    Create `packages/g2-app/src/hud/map-canvas-layer.ts` exporting `class MapCanvasLayer implements CanvasLayer` with `id = 'map-canvas'`, ZIndex Z0_MAP intent. Constructor opts: `{ onFrame: () => void }` (the requestCycle trigger). Fields: `_ctx` (set in attachCanvas, null-guarded like CanvasStatusHudLayer), `_frame: ImageData | null`, `_dirty = true`. `attachCanvas(canvas)` async: `getContext('2d')`, degrade gracefully on null (mirror CanvasStatusHudLayer pattern). `setFrame(rgba, w, h)`: store `new ImageData(new Uint8ClampedArray(rgba), w, h)` (or w/h = COMPOSITOR_W/H from canvas-compositor), set `_dirty = true`, call `this._opts.onFrame()`. `paint()`: if ctx null return; if `_frame !== null` `ctx.putImageData(this._frame, 0, 0)`; set `_dirty = false` as LAST line. `isDirty()`, `draw()` (resolved promise), `destroy()` (no subscriptions to release), `getContainerCount()` returns `{image:0, text:0}`. Full TSDoc per INV-4. Use COMPOSITOR_W/COMPOSITOR_H from `../engine/canvas-compositor.js`.

    In `scene-input.ts`: widen `attachSceneInputToWs` to accept a sink that is either the RasterController (glyph map path) OR a MapCanvasLayer. Add a `MapFrameSink` minimal interface (`setFrame(rgba, w, h): void`) and branch: when a MapCanvasLayer sink is provided, call `sink.setFrame(framed, CANONICAL_W, CANONICAL_H)` (synchronous, no Worker) instead of `controller.requestFrame(...)`. Keep the existing RasterController overload for the glyph fallback path. Preserve the full defense-in-depth parse chain and padFrameToCanonical unchanged.

    In `boot-engine-core.ts`: construct `const mapCanvas = new MapCanvasLayer({ onFrame: () => hudDeltaDriver.requestCycle() });` near the CanvasStatusHudLayer construction (~line 822). In canvas mode, change the step-11 wiring (`attachSceneInputToWs(ws, rasterController)` ~line 828 AND the reconnect re-attach ~line 1166) to route to `mapCanvas` instead of `rasterController`. In the step-12 canvas bundle (~line 1476), mount mapCanvas at Z0_MAP in the SAME bundle as the z=1 CanvasStatusHudLayer: `bundle([{mount Z0_MAP mapCanvas}, {mount Z1_STATUS_HUD canvasStatusHud}])`. INV-4 zero dead code: the RasterController scene path is now UNREACHABLE in canvas mode — gate it explicitly. The glyph branch (effectiveVerdict==='glyph') still uses `attachSceneInputToWs(ws, rasterController)` for the legacy map. Add a written rationale comment at the canvas-mode wire site: "canvas mode routes scene frames to the compositor MapCanvasLayer (z=0); the RasterController map-tile path is reserved for the glyph fallback only — see debug map-frame-pipeline-dims, no dual map-tile/HUD contention". Ensure teardown destroys mapCanvas (add to the canvas-mode teardown layer set near line 1730).
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app test -- map-canvas-layer scene-input</automated>
  </verify>
  <done>MapCanvasLayer paints the latest 400x200 frame at z=0 and triggers requestCycle on every setFrame; scene-input routes frame_pixels to it in canvas mode; the legacy RasterController scene path is explicitly canvas-mode-unreachable with a written rationale; glyph fallback unchanged; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Relocate status to a native text container + restore canvas-mode root-exit</name>
  <files>packages/g2-app/src/status-hud/canvas-status-hud-layer.ts, packages/g2-app/src/status-hud/__tests__/canvas-status-hud-layer.test.ts, packages/g2-app/src/engine/container-registry.ts, packages/g2-app/src/engine/__tests__/container-registry.test.ts, packages/g2-app/src/panels/root-exit-dispatcher.ts, packages/g2-app/src/panels/__tests__/root-exit-dispatcher.test.ts</files>
  <behavior>
    - REG-HUDSTATUS-1: buildHudRasterPageSchema() now declares a hud-status non-capture text container (top 576x27 row, isEventCapture:0) in textObject; containerTotalNum becomes 6 (4 image + hud-capture + hud-status).
    - REG-CAPTURE-INV: exactly ONE container in the HUD raster page schema still has isEventCapture=1 (hud-capture). The existing "exactly ONE isEventCapture=1" test passes unchanged (now scanning 2 text containers, still 1 capture).
    - CSHUD-1: CanvasStatusHudLayer no longer paints an opaque full-frame fill — its paint() no longer covers the whole 400x200 canvas (the map at z=0 must show through). Verify _drawChrome no longer issues a full-canvas opaque fillRect (the full-frame `fillRect(0,0,COMPOSITOR_W,COMPOSITOR_H)` is removed/replaced so the compositor blends z=0 below).
    - CSHUD-2: on a valid character.delta, CanvasStatusHudLayer pushes a status line (PG name + PF/HP + turn/combat state) to the native `hud-status` text container via bridge.textContainerUpgrade (id resolved via the registry), NOT only painting into the canvas.
    - ROOT-1: in canvas mode the root double-tap exit fires — with MapCanvasLayer mounted at z=0 (id 'map-canvas'), root-exit recognizes it as the root layer and calls shutDownPageContainer(1). Overlay-open case still suppresses root exit.
  </behavior>
  <action>
    In `container-registry.ts`: add a `hud-status` entry to CONTAINER_REGISTRY in the HUD-raster page-namespace block (kind 'text', x=0, y=0, width=576, height=27, isEventCapture:0). In `buildHudRasterPageSchema()`, append a second `TextContainerProperty` for hud-status to `textObject` (declaration order: image tiles 0-3, hud-capture id 4, hud-status id 5) and bump `HUD_RASTER_CONTAINER_TOTAL` to 6. Keep hud-capture as the SOLE isEventCapture:1. Update the container-registry test: REG-HUDSTATUS-1 (hud-status present, non-capture, 576x27), and confirm "exactly ONE isEventCapture=1" still holds across both text containers.

    In `canvas-status-hud-layer.ts`: (a) STOP the opaque full-frame chrome — remove the `ctx.fillRect(0, 0, COMPOSITOR_W, COMPOSITOR_H)` opaque background in `_drawChrome` (and the prebaked chrome bitmap that bakes it) so the layer no longer hides the z=0 map; the layer may still draw a minimal status strip background in the top 27px row ONLY (not the whole frame). (b) Inject an optional `bridge: Pick<EvenAppBridge,'textContainerUpgrade'>` into CanvasStatusHudLayerOpts; in `_onDelta`, after caching the snapshot, also emit the relocated status line to the native `hud-status` container via `new TextContainerUpgrade({ ...resolveContainerIdField('hud-status'), containerName: 'hud-status', content })` where `content` is a single line: PG name + `PF ${hp}/${maxHp}` + turn/combat indicator (best-effort; await-guarded try/catch — never throw into the WS handler). Subscribe to combat.turn/combat.state too if needed to surface turn state, OR keep it character-only for this slice and include only name+PF/HP (document the choice). Use the existing CharacterSnapshot fields (name if present on the schema; otherwise PF/HP/AC/LV). Pass `bridge` from boot-engine-core into the CanvasStatusHudLayer construction (~line 822).

    In `root-exit-dispatcher.ts`: the canvas-mode root layer is now MapCanvasLayer (id 'map-canvas'), which is NOT an OverlayPanel, so `getTopLayer()` (which returns only OverlayPanels) returns null at root — the double-tap exit never fires. Fix: accept BOTH root layer ids. Add a `ROOT_LAYER_IDS = new Set(['map-base', 'map-canvas'])` and change the guard: fire the exit when there is NO overlay panel on top AND the bottom/root layer is a recognized root. Simplest correct fix per the debug note: when `getTopLayer()` returns null (no overlay open) in canvas mode, that itself means "no overlay to close → root double-tap = exit". Update the dispatcher so `top === null` (no overlay) triggers the graceful exit (Mode 1) instead of returning early — this restores canvas-mode root exit while preserving the overlay-open suppression (when an overlay IS the top layer, its own onEvent consumes double-tap and root-exit returns). Update the dispatcher TSDoc + root-exit-dispatcher.test.ts: ROOT-1 (null top → exit fires in canvas mode), and the existing overlay-open suppression test still passes.
  </action>
  <verify>
    <automated>corepack pnpm --filter @evf/g2-app test -- canvas-status-hud-layer container-registry root-exit-dispatcher</automated>
  </verify>
  <done>HUD raster page schema declares hud-status (non-capture) with exactly one isEventCapture:1 (hud-capture) preserved; CanvasStatusHudLayer no longer paints an opaque full-frame fill and pushes the status line to the native hud-status text container; canvas-mode root double-tap exit fires again; all three test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry canvas → bridge WS | extractor reads PIXI pixels and emits frame_pixels (untrusted size/rate) |
| bridge WS → g2-app scene-input | frame_pixels envelope crosses into the render path |
| WS character.delta → CanvasStatusHudLayer | untrusted snapshot drives native text container content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-d42-01 | Denial of Service | continuous interval capture (~1Hz) flooding BLE | mitigate | 1Hz default interval + 200ms hook debounce share one performExtract; HudDeltaDriver zero-push-on-idle gates BLE — unchanged frames push nothing |
| T-d42-02 | Tampering | frame_pixels payload into scene-input | mitigate | existing EnvelopeSchema + FramePixelsSchema defense-in-depth parse preserved before setFrame; padFrameToCanonical clamps dims |
| T-d42-03 | Tampering | character.delta → native hud-status text | mitigate | CharacterSnapshotSchema.safeParse gate (existing) before textContainerUpgrade; malformed → log + drop, no native write |
| T-d42-04 | Tampering | npm/pip/cargo installs | mitigate | no new dependencies introduced; slopcheck N/A |
</threat_model>

<verification>
- `corepack pnpm typecheck` exits 0.
- `corepack pnpm lint:ci` exits 0 (INV-4: zero dead code, no bare TODO without #issue/ADR).
- `corepack pnpm test` (full vitest workspace) green — re-run once if the known wizard-commands teardown flake exits 1 with 0 failures.
- `corepack pnpm changeset` — add a changeset (patch @evf/g2-app + @evf/foundry-module).
- Live verification recipe (already-running services, OPTIONAL manual): scratch `packages/foundry-module/_scene_e2e.ts` POSTs the PRODUCTION extractor output to bridge :8911 `/internal/delta` (Bearer dev-secret); GET :9898 `/api/screenshot/glasses` should show the full-screen map with a native status line on the top row (NOT opaque chrome over the map).
</verification>

<success_criteria>
- Continuous Foundry capture streams frames at ~1Hz independent of hooks; canvasPan is the 5th hook; unregister clears the interval.
- In canvas mode the full-screen map renders at compositor z=0 via MapCanvasLayer; the legacy RasterController map-tile path is canvas-mode-unreachable with a written rationale (INV-4).
- Each new frame triggers hudDeltaDriver.requestCycle().
- Status info renders in the native hud-status text container; CanvasStatusHudLayer no longer paints opaque full-frame chrome that would hide the map.
- Exactly ONE isEventCapture:1 container per page (hud-capture, id 4) — guard test green.
- Canvas-mode root double-tap exit fires again.
- typecheck + lint:ci + full test + changeset all green.
</success_criteria>

<output>
Create `.planning/quick/260610-d42-full-screen-streamed-map-text-container-/260610-d42-SUMMARY.md` when done.
</output>
