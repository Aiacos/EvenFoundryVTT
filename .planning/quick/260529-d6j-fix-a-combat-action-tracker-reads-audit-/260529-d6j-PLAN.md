---
quick_id: 260529-d6j
type: quick
mode: tdd
branch: develop
autonomous: true
files_modified:
  - packages/foundry-module/src/write-path/combat-action-tracker.test.ts
  - packages/foundry-module/src/write-path/combat-action-tracker.ts
requirements: [INV-4]
must_haves:
  truths:
    - "createChatMessage with a real-shaped audit flag (field `tool: 'cast-spell'`) fires the action-economy emit"
    - "Before the source fix, the adjusted test is RED; after, it is GREEN"
    - "combat-action-tracker docstrings reference flags.evf.audit.tool (matching code)"
  artifacts:
    - path: "packages/foundry-module/src/write-path/combat-action-tracker.ts"
      provides: "createChatMessage hook reading audit.tool"
      contains: "audit.tool"
    - path: "packages/foundry-module/src/write-path/combat-action-tracker.test.ts"
      provides: "test mock writing the real flag property `tool`"
      contains: "tool:"
  key_links:
    - from: "combat-action-tracker.ts createChatMessage hook"
      to: "TOOL_SLOT_MAP"
      via: "audit.tool property read"
      pattern: "audit\\.tool\\b"
---

<objective>
Revive dead action-economy tracking. `combat-action-tracker.ts:200` reads `audit.toolId`,
but `writeAuditLog` (per `AuditEntry` in audit-log.ts:43 and `dispatchTool` in
tool-registry.ts:335) writes the flag as `tool`. The read is therefore always `undefined`
→ early `return` at line 201 → emit never fires. The sibling watcher
`action-result-watcher.ts:275/284` reads `audit.tool` correctly, proving this file is the
anomaly. The test suite never caught it because its mock emitted the wrong property name
(`toolId`), matching the bug instead of production reality.

Purpose: Restore production action-economy emission (CRITICAL bug, removes dead code per INV-4).
Output: One-property source fix, a regression-guard test, and docstring sync — all gates green.
</objective>

<context>
@packages/foundry-module/src/write-path/combat-action-tracker.ts
@packages/foundry-module/src/write-path/combat-action-tracker.test.ts

<interfaces>
<!-- Production truth — what writeAuditLog actually writes to flags.evf.audit -->

From packages/foundry-module/src/write-path/audit-log.ts:43 (AuditEntry):
```typescript
interface AuditEntry {
  tool: string;        // <-- the real field name (NOT toolId)
  // ...actorId, recipientUserId, attackId, etc.
}
```

From packages/foundry-module/src/write-path/tool-registry.ts:335 (dispatchTool):
```typescript
// writes: { tool: toolId, ... }  -> runtime flag is flags.evf.audit.tool
```

Sibling that reads it correctly — packages/foundry-module/src/write-path/action-result-watcher.ts:275/284:
```typescript
audit.tool   // correct read; combat-action-tracker is the outlier
```

Current bug — combat-action-tracker.ts:200:
```typescript
const toolId = audit.toolId as string | undefined;   // ALWAYS undefined
if (typeof toolId !== 'string') return;              // dead-ends here every time
```

Test mock that hid the bug — combat-action-tracker.test.ts:85 (makeChatMsg):
```typescript
audit: { toolId: opts.audit.toolId, ... }   // emits wrong flag property
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): make the test mock write the real flag property `tool`</name>
  <files>packages/foundry-module/src/write-path/combat-action-tracker.test.ts</files>
  <behavior>
    - The shared chat-message builder `makeChatMsg` (line 77) must emit the audit flag
      under property name `tool`, matching what writeAuditLog/AuditEntry actually writes —
      NOT `toolId`. Concretely, line 85 `toolId: opts.audit.toolId,` becomes
      `tool: opts.audit.toolId,`. The `MockAuditFlags` interface field and call-site inputs
      (`audit: { toolId: 'cast-spell', ... }`) may keep the `toolId` name as fixture input
      (planner discretion); only the EMITTED flag-object property must become `tool`.
    - Also fix the two inline literal flag objects that bypass the builder:
      test.ts:439-444 (`flags: { evf: { audit: { toolId: 'cast-spell', ... } } }`) and
      test.ts:467-490 (the weapon-attack inline `audit: { toolId: 'weapon-attack', ... }`
      objects) — every flag object passed into the createChatMessage hook must use `tool:`.
      Grep gate below proves no emitted flag object still uses `toolId:`.
    - Add a dedicated regression-guard test (e.g. CAT-REGRESSION) that builds a
      production-shaped chat message with `tool: 'cast-spell'` through the real
      createChatMessage hook and asserts the economy `emit` fires exactly once with
      `actionsUsed: 1`. This is the assertion that would have caught the original
      field-name bug. Keep it independent of `makeChatMsg` so it documents the real wire shape.
    - EXPECTATION: with these test edits applied but the source still reading `audit.toolId`,
      the suite is RED (emit never fires → CAT-01, CAT-02, weapon-attack, and the new
      regression test all fail; CAT-05 no-emit cases stay green).
  </behavior>
  <action>Edit combat-action-tracker.test.ts so every flag object delivered into the
    createChatMessage hook carries the audit field under property `tool` (not `toolId`):
    the `makeChatMsg` builder at line 85, the inline literal at ~439-444, and the
    weapon-attack inline literals at ~467-490. Add the CAT-REGRESSION test described in
    <behavior>. Do NOT touch the source file in this task — the suite must be observably RED
    first. Do not add or remove any socketlib handler registrations (CI Gate 8 stays 17).</action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && grep -vE '^\s*(//|\*)' packages/foundry-module/src/write-path/combat-action-tracker.test.ts | grep -cE "audit:\s*\{[^}]*toolId\s*:|evf:\s*\{\s*audit:\s*\{[^}]*toolId\s*:" ; echo "expect 0 emitted-flag toolId properties"</automated>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/foundry-module test -- combat-action-tracker 2>&1 | tail -20 ; echo "EXPECT FAIL (RED) — source still reads audit.toolId"</automated>
  </verify>
  <done>Test file emits the audit flag as `tool` in all hook inputs; regression-guard test
    added; running the combat-action-tracker suite FAILS because the source still reads the
    wrong field.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): fix the source read + sync docstrings</name>
  <files>packages/foundry-module/src/write-path/combat-action-tracker.ts</files>
  <action>Single-property source fix at line 200: change `const toolId = audit.toolId as
    string | undefined;` so the PROPERTY ACCESS reads `audit.tool` (local variable name
    `toolId` may stay for downstream readability — only the property read must become
    `audit.tool`). Then sync the two docstring prose references so docs match code (INV-4
    coherence): line ~31 (`filter strictly on flags.evf.audit.toolId in ...`) and line ~165
    (`inspects flags.evf.audit.toolId to determine which economy slot`) → both become
    `flags.evf.audit.tool`. Do NOT change TOOL_SLOT_MAP, the threat-model logic, or any
    socketlib registration (CI Gate 8 stays 17).</action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && grep -vE '^\s*(//|\*)' packages/foundry-module/src/write-path/combat-action-tracker.ts | grep -cE "audit\.toolId" ; echo "expect 0 — no code reads audit.toolId"</automated>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && grep -c "flags.evf.audit.toolId" packages/foundry-module/src/write-path/combat-action-tracker.ts ; echo "expect 0 — docstrings reference audit.tool"</automated>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/foundry-module test -- combat-action-tracker 2>&1 | tail -15 ; echo "EXPECT PASS (GREEN)"</automated>
  </verify>
  <done>`audit.tool` is the property read; no `audit.toolId` code read and no
    `flags.evf.audit.toolId` docstring remain; combat-action-tracker suite passes.</done>
</task>

</tasks>

<verification>
All three repo quality gates MUST exit 0 at task completion:

1. `pnpm typecheck` — strict TS clean
2. `pnpm lint:ci` — Biome read-only clean
3. `pnpm test` — full workspace suite green

Test-count delta: EXPECT +1 (the CAT-REGRESSION guard test). Net test count MUST be
>= the pre-fix count (2712 baseline per task brief); a decrease is a failure.

CI Gate 8 invariant: socketlib handler count MUST remain 17. This fix touches no socketlib
registrations — confirm none were added/removed.
</verification>

<success_criteria>
- combat-action-tracker.ts:200 reads `audit.tool` (was `audit.toolId`).
- Docstrings (~31, ~165) reference `flags.evf.audit.tool`.
- Test mock builds audit flags with property `tool`; regression-guard test present.
- `pnpm typecheck`, `pnpm lint:ci`, `pnpm test` all green; test count +1 (>= baseline).
- socketlib handler count unchanged (17).
- Atomic Conventional Commit: `fix(*): combat-action-tracker reads audit.tool not audit.toolId`.
</success_criteria>

<output>
Create `.planning/quick/260529-d6j-fix-a-combat-action-tracker-reads-audit-/260529-d6j-SUMMARY.md` when done.
</output>
