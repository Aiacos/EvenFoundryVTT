---
phase: quick-260530-x2b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/g2-app/src/raster/map-base-layer.ts
  - packages/g2-app/src/engine/audio-capture.ts
  - packages/bridge/src/voice/audio-stream-route.ts
  - packages/bridge/src/voice/audio-stream-route.test.ts
  - packages/g2-app/src/engine/r1-event-source.ts
  - .changeset/260530-x2b-g2-sdk-conformance.md
autonomous: true
requirements: [G2SDK-CONF-B1, G2SDK-CONF-B2, G2SDK-CONF-B3]

must_haves:
  truths:
    - "Portrait override writes to an IMAGE tile container via the typed SDK ImageRawDataUpdate (no `index` field, no `as unknown as` cast), and the result is checked with ImageRawDataUpdateResult.isSuccess."
    - "Audio-stream WS authenticates in production WKWebView via a `?token=` query param; the bridge accepts it as a fallback when the Authorization header is absent/malformed."
    - "The r1-event-source comment accurately states the wire kinds are the bridge's server-side-normalized strings mapped from OsEventTypeList + EventSourceType.TOUCH_EVENT_FROM_RING."
  artifacts:
    - path: "packages/g2-app/src/raster/map-base-layer.ts"
      provides: "Portrait override using typed ImageRawDataUpdate to map-tile-${slot}, result checked"
      contains: "ImageRawDataUpdate"
    - path: "packages/bridge/src/voice/audio-stream-route.ts"
      provides: "?token= query-param bearer fallback alongside Authorization header"
      contains: "token"
    - path: "packages/bridge/src/voice/audio-stream-route.test.ts"
      provides: "Test asserting query-param (?token=) auth succeeds; header-based test stays green"
      contains: "token"
    - path: ".changeset/260530-x2b-g2-sdk-conformance.md"
      provides: "Patch changeset for @evf/g2-app + @evf/bridge"
      contains: "@evf/g2-app"
  key_links:
    - from: "packages/g2-app/src/raster/map-base-layer.ts"
      to: "@evenrealities/even_hub_sdk"
      via: "value import of ImageRawDataUpdate + ImageRawDataUpdateResult (mirrors raster-controller.ts)"
      pattern: "import \\{ ImageRawDataUpdate.*ImageRawDataUpdateResult.*\\} from '@evenrealities/even_hub_sdk'"
    - from: "packages/g2-app/src/engine/audio-capture.ts"
      to: "packages/bridge/src/voice/audio-stream-route.ts"
      via: "?token= query param on the WS URL, read server-side as header fallback"
      pattern: "token="
---

<objective>
Apply 3 pre-verified G2-SDK-conformance fixes from a completed skill audit. The audit is DONE — do not re-audit. All findings already verified against the installed SDK (`packages/g2-app/node_modules/@evenrealities/even_hub_sdk/dist/index.d.ts`) and the bridge route source.

Purpose: close one CRITICAL real bug (B1 portrait override targets the wrong container with a non-existent SDK field behind a bug-hiding cast), one IMPORTANT production bug (B2 bearer silently dropped in WKWebView because browser WebSocket ignores the `headers` option), and one INV-2 doc drift (B3 incorrect comment about R1 wire-kind provenance).

Output: 3 atomic commits (one per fix), `pnpm typecheck` + `pnpm test` green for `@evf/g2-app` and `@evf/bridge`, plus one multi-package patch changeset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Verified shapes — executor uses these directly; no SDK re-exploration needed. -->

From packages/g2-app/node_modules/@evenrealities/even_hub_sdk/dist/index.d.ts:
```typescript
// line 446 — NOTE: NO `index` field exists. Only the three below.
declare class ImageRawDataUpdate {
    containerID?: number;
    containerName?: string;
    imageData?: number[] | string | Uint8Array | ArrayBuffer;
    constructor(data?: Partial<ImageRawDataUpdate>);
}
// line 589 / 601 — namespace helper
declare namespace ImageRawDataUpdateResult {
    function isSuccess(value: ImageRawDataUpdateResult): boolean;
}
// line 1171 — actual bridge signature (return type is the enum, NOT Promise<boolean>)
updateImageRawData(data: ImageRawDataUpdate): Promise<ImageRawDataUpdateResult>;
// line 707 / 733 — R1 ring input provenance (for B3 comment)
declare enum OsEventTypeList { /* ... */ }
declare enum EventSourceType { TOUCH_EVENT_FROM_RING = 2 /* ... */ }
```

Correct call pattern to MIRROR — packages/g2-app/src/raster/raster-controller.ts:331-341:
```typescript
const payload = new ImageRawDataUpdate({
  containerName: `map-tile-${tile.index}`,
  imageData: tile.pngBytes,
});
const result = await this.bridge.updateImageRawData(payload);
if (!ImageRawDataUpdateResult.isSuccess(result)) {
  this._recordFailure();
}
```

Bridge query-param-WS-auth pattern to MIRROR — packages/bridge/src/debug/debug-routes.ts:308-312:
```typescript
// Secret may arrive as `?secret=` (browser WS cannot set headers) or Authorization.
const url = new URL(req.url ?? '/debug/stream', 'http://localhost');
const querySecret = url.searchParams.get('secret') ?? undefined;
const headerSecret = secretFromAuthHeader(req.headers.authorization);
const provided = querySecret ?? headerSecret;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (FIX B1 — CRITICAL): Portrait override uses typed ImageRawDataUpdate to an image tile, result checked</name>
  <files>packages/g2-app/src/raster/map-base-layer.ts</files>
  <action>
Fix the portrait-override block inside `draw()` (currently ~lines 226-241) in map-base-layer.ts. Three SDK violations exist: (a) the call passes an `index` field that does NOT exist on `ImageRawDataUpdate` (only `containerID?`/`containerName?`/`imageData?` per the verified `<interfaces>` shape); (b) it targets `'map-capture'`, which is the TEXT capture container returned by `getCaptureContainer()` — portraits must go to an IMAGE tile, and per `getContainerCount()` + the class JSDoc the image tiles are `map-tile-0`..`map-tile-3` with slot 3 the portrait slot (D-13-08); (c) it declares the wrong return type (`Promise<boolean>`) and never checks the result.

Required changes:
1. On line 41, keep the existing `import type { EvenAppBridge } from '@evenrealities/even_hub_sdk';` and ADD a value import: `import { ImageRawDataUpdate, ImageRawDataUpdateResult } from '@evenrealities/even_hub_sdk';`. (Two separate import statements from the same module — type-only import stays, value import added — mirrors raster-controller.ts which value-imports both.)
2. Replace the entire `await ( this.bridge as unknown as {...} ).updateImageRawData({ containerName: 'map-capture', index: slot, imageData: bytes });` expression with the typed, mirrored pattern:
   - construct `new ImageRawDataUpdate({ containerName: \`map-tile-${slot}\`, imageData: bytes })`,
   - `const result = await this.bridge.updateImageRawData(payload);`
   - `if (!ImageRawDataUpdateResult.isSuccess(result)) { ... }` and inside the failure branch emit a `console.warn` consistent with raster-controller's `_recordFailure` logging style (e.g. message prefix `[EVF] map-base-layer:` mentioning the portrait-override slot and the failed dispatch). MapBaseLayer has no `_recordFailure` of its own — a `console.warn` is the correct mirror here (do NOT invent a failure-window mechanism; this is a single best-effort overlay write).
3. Remove the `as unknown as {...}` cast entirely — `this.bridge` is already typed `EvenAppBridge`, whose `updateImageRawData(data: ImageRawDataUpdate): Promise<ImageRawDataUpdateResult>` is the correct signature. INV-4 forbids casts that hide bugs.
4. Update the `_portraitOverride` JSDoc (lines ~96-107) and the inline comment at ~223-225 if they still say `map-capture` / `index` — they must reflect the `map-tile-${slot}` image-tile target. Do not change `setPortraitOverride` signature or `getContainerCount` (the 4-image budget is already correct).

INV-4: zero dead/cast-hidden code. Do NOT change glyph path, raster path, or the slot convention (slot 3 = portrait).
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app exec tsc --noEmit && grep -q "map-tile-\${slot}" packages/g2-app/src/raster/map-base-layer.ts && ! grep -q "as unknown as" packages/g2-app/src/raster/map-base-layer.ts && ! grep -nE "index:\s*slot" packages/g2-app/src/raster/map-base-layer.ts && pnpm --filter @evf/g2-app test -- --run map-base-layer</automated>
  </verify>
  <done>map-base-layer.ts portrait override constructs a typed `ImageRawDataUpdate` targeting `map-tile-${slot}`, checks `ImageRawDataUpdateResult.isSuccess`, has no `as unknown as` cast and no `index` field; g2-app typecheck + map-base-layer tests pass. Commit: `fix(g2-app): portrait override targets image tile via typed SDK ImageRawDataUpdate`.</done>
</task>

<task type="auto">
  <name>Task 2 (FIX B2 — IMPORTANT production bug): audio-stream WS bearer via ?token= query param, both sides + test</name>
  <files>packages/g2-app/src/engine/audio-capture.ts, packages/bridge/src/voice/audio-stream-route.ts, packages/bridge/src/voice/audio-stream-route.test.ts</files>
  <action>
The WS to `/v1/audio/stream` is created with `{ headers: { Authorization: \`Bearer ${opts.bearer}\` } }`. Browser/WKWebView WebSocket (the production runtime) IGNORES the `headers` option — only Node's `ws` package honors it — so in production the bearer is silently dropped and the bridge route closes with 1008 'invalid-bearer'. Tests currently pass only because they inject a Node `ws` factory via `_wsFactory`.

Fix BOTH sides. Do NOT log the token anywhere (no `log.info`/`log.debug` containing the bearer; the existing `log.info(\`...opening mic + WS to ${streamUrl}\`)` on line ~141 currently logs `streamUrl` — once the token is in the query string, that log line MUST be changed so it does NOT include the token; log the host/path only, e.g. derive a token-free URL string for logging).

g2-app (`packages/g2-app/src/engine/audio-capture.ts`):
1. Update `buildAudioStreamUrl(bridgeUrl: string)` to accept the bearer and append it as a URL-encoded `?token=` query param. Signature becomes `buildAudioStreamUrl(bridgeUrl: string, bearer: string): string`. Build the base `ws(s)://.../v1/audio/stream` exactly as today, then append `?token=${encodeURIComponent(bearer)}`. Keep the JSDoc `@example` accurate (note the token query param is appended; you may show a redacted/placeholder token in the example — never a real one).
2. At the call site (~line 139) pass `opts.bearer`: `const streamUrl = buildAudioStreamUrl(opts.bridgeUrl, opts.bearer);`.
3. KEEP the `headers: { Authorization: \`Bearer ${opts.bearer}\` }` option on the `wsFactory(streamUrl, {...})` call so the Node-`ws` test path (which honors headers) still works. Both auth channels present = belt-and-suspenders; the query param is what production WKWebView actually uses.
4. Fix the log line at ~141 so it does NOT emit the token: log a token-stripped URL (e.g. the result of `buildAudioStreamUrl` BEFORE appending the token, or the base path string). The "# Auth" doc block (lines ~27-31) should be updated to note the bearer travels as a `?token=` query param in production (WKWebView ignores WS headers) with the header retained for the Node-ws test path.

bridge (`packages/bridge/src/voice/audio-stream-route.ts`):
1. In the WS handler, accept the bearer from the `?token=` query param as a FALLBACK when the Authorization header is absent or malformed. Mirror debug-routes.ts:308-312: parse `const url = new URL(req.url ?? '/v1/audio/stream', 'http://localhost'); const queryToken = url.searchParams.get('token') ?? undefined;`. The handler's `req` type is currently `{ headers: ... }` — widen it to also include `url?: string` (add `url?: string` to the inline req type so `req.url` typechecks; `@fastify/websocket`/Fastify request carries `url`).
2. Resolve the bearer: prefer a well-formed `Authorization: Bearer <t>` header; if absent/malformed, fall back to `queryToken`. Then validate the resolved bearer via the SAME `tokenCache.validate(bearer)` path. If neither source yields a bearer → `socket.close(1008, 'invalid-bearer')` exactly as today. Keep the existing 'invalid-bearer' / 'voice-disabled' close codes and the rest of the flow unchanged.
3. Update the route JSDoc step 2 + "# Security (T-12-07)" block to note the `?token=` fallback (browser WS cannot set headers — same rationale as debug `?secret=`). Do NOT log the token.

bridge test (`packages/bridge/src/voice/audio-stream-route.test.ts`):
1. Add a test asserting a connection authenticated via `?token=<valid>` query param (NO Authorization header) succeeds (reaches the Deepgram-connect / does NOT close 1008). Mirror the existing header-based test's harness.
2. Keep the existing header-based auth test green. Optionally add a negative test (neither header nor query param → 1008) only if it falls out trivially from the existing harness; not required.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app exec tsc --noEmit && pnpm --filter @evf/bridge exec tsc --noEmit && grep -q "searchParams.get('token')" packages/bridge/src/voice/audio-stream-route.ts && grep -q "encodeURIComponent" packages/g2-app/src/engine/audio-capture.ts && pnpm --filter @evf/bridge test -- --run audio-stream-route && pnpm --filter @evf/g2-app test -- --run audio-capture</automated>
  </verify>
  <done>g2-app appends URL-encoded `?token=` to the audio-stream WS URL (and still sets the header for the Node-ws path), never logs the token; bridge reads `?token=` as a header fallback validated through `tokenCache.validate`; a new query-param-auth test passes and the existing header-auth test stays green; both packages typecheck. Commit: `fix(g2-app,bridge): audio-stream WS bearer via ?token= query param for WKWebView`.</done>
</task>

<task type="auto">
  <name>Task 3 (FIX B3 — INV-2 doc): correct R1 wire-kind provenance comment</name>
  <files>packages/g2-app/src/engine/r1-event-source.ts</files>
  <action>
Reword the comment at r1-event-source.ts lines ~23-26 (the "Wire → internal R1Gesture translation" block). It currently states the wire kinds `'scroll-up'`/`'scroll-down'` are "flat string enums from the Even Hub SDK" — this is INV-2 drift and is incorrect. The SDK delivers ring input via `onEvenHubEvent` as `OsEventTypeList` enum values carried with `EventSourceType.TOUCH_EVENT_FROM_RING` (verified in the SDK `index.d.ts` — see `<interfaces>`); the flat-string normalization (`'scroll-up'` / `'scroll-down'` / `'tap'` / etc.) is performed SERVER-SIDE by the bridge, not by the SDK.

Reword to state: the bridge wire kinds are the bridge's normalized strings, mapped server-side from the SDK's `OsEventTypeList` + `EventSourceType.TOUCH_EVENT_FROM_RING` ring-input events. Keep the rest of the block (internal `R1Gesture` discriminated-union shape, "translation lives here", RESEARCH §Q7 reference) intact.

This is COMMENT-ONLY. No logic change, no import change, no signature change. Do not touch any other line.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app exec tsc --noEmit && grep -q "OsEventTypeList" packages/g2-app/src/engine/r1-event-source.ts && grep -q "TOUCH_EVENT_FROM_RING" packages/g2-app/src/engine/r1-event-source.ts && ! grep -q "flat string enums from the Even Hub SDK" packages/g2-app/src/engine/r1-event-source.ts</automated>
  </verify>
  <done>The r1-event-source comment correctly attributes the flat wire kinds to the bridge's server-side normalization of the SDK's `OsEventTypeList` + `EventSourceType.TOUCH_EVENT_FROM_RING`; no `flat string enums from the Even Hub SDK` phrasing remains; no logic changed; g2-app typechecks. Commit: `docs(g2-app): correct R1 wire-kind provenance (INV-2 — bridge-normalized, not raw SDK)`.</done>
</task>

</tasks>

<finalization>
After the 3 fix commits:
1. Add a multi-package patch changeset at `.changeset/260530-x2b-g2-sdk-conformance.md` declaring `"@evf/g2-app": patch` and `"@evf/bridge": patch`, body summarizing the 3 SDK-conformance fixes (B1 portrait image-tile + typed SDK call, B2 ?token= WS auth for WKWebView, B3 R1 provenance comment). Mirror the existing changeset format (`.changeset/260529-eer-forward-advantage-targets.md`).
2. Run the workspace gates green: `pnpm --filter @evf/g2-app exec tsc --noEmit`, `pnpm --filter @evf/bridge exec tsc --noEmit`, `pnpm --filter @evf/g2-app test --run`, `pnpm --filter @evf/bridge test --run`. (Full `pnpm typecheck` + `pnpm test` if cheap.)
</finalization>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| g2-app (WKWebView) → bridge `/v1/audio/stream` WS | Untrusted client; bearer must authenticate the upgrade. B2 fixes a silent auth failure that made this boundary unreachable in production. |
| g2-app → G2 glasses via `updateImageRawData` | SDK call; B1 was targeting the wrong (text) container with a non-existent field — wrong output, not a security hole. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-x2b-01 | Information Disclosure | bearer in `?token=` query param | accept | Bearer in query string mirrors the established debug `?secret=` pattern (debug-routes.ts) forced by WKWebView's inability to set WS headers; transport is WSS in prod (Even Hub HTTPS-only). Mitigation: NEVER log the token (enforced in B2 — log line stripped of token); 24h opaque bearer limits blast radius. |
| T-x2b-02 | Spoofing | audio-stream WS auth | mitigate | Both header AND query-param bearer routed through the same `tokenCache.validate` gate; invalid/missing → close 1008. No new auth surface — same validator as `/ws`. |
| T-x2b-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies installed by this plan. |
</threat_model>

<verification>
- `! grep "as unknown as" map-base-layer.ts` and `! grep "index: slot"` — B1 cast + phantom field gone.
- `grep "map-tile-\${slot}"` present; `grep "ImageRawDataUpdateResult.isSuccess"` present in map-base-layer.ts.
- bridge `grep "searchParams.get('token')"`; g2-app `grep "encodeURIComponent"` — B2 both sides wired.
- No occurrence of the bearer value in any `log.*` call (manual scan of audio-capture.ts log lines).
- `! grep "flat string enums from the Even Hub SDK"` and both `OsEventTypeList` + `TOUCH_EVENT_FROM_RING` present in r1-event-source.ts — B3.
- `pnpm --filter @evf/g2-app test --run` + `pnpm --filter @evf/bridge test --run` green (new query-param-auth test included; existing tests unchanged-green).
- Changeset present with both `@evf/g2-app` and `@evf/bridge` at `patch`.
</verification>

<success_criteria>
- 3 atomic commits (one per fix B1/B2/B3) with Conventional Commit messages.
- `@evf/g2-app` and `@evf/bridge` typecheck + test green.
- New bridge test asserts `?token=` query-param auth succeeds; existing header-auth test still green.
- Multi-package patch changeset added.
- INV-4 honored (no bug-hiding casts, no dead code); INV-2 honored (R1 provenance comment corrected); ADR-0011 honored (no socketlib handlers added — CI Gate 8 count stays 17).
</success_criteria>

<output>
Create `.planning/quick/260530-x2b-fix-3-g2-sdk-conformance-findings-from-s/260530-x2b-SUMMARY.md` when done.
</output>
