---
phase: 25-promozione-raster-a-default-boot-fallback-glyph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/g2-app/src/hud/push-hud-tiles.ts
  - packages/g2-app/src/hud/push-hud-tiles.test.ts
  - packages/g2-app/src/engine/layer-manager.ts
  - packages/g2-app/src/engine/hud-delta-driver.ts
  - packages/g2-app/src/hud/hud-raster-frame.ts
autonomous: true
requirements: [RPROMO-02]

must_haves:
  truths:
    - "pushHudTiles lives in a production module (hud/push-hud-tiles.ts), no longer in the PoC file (D-25.1 — preserve reused helpers)"
    - "layer-manager.ts and hud-delta-driver.ts import pushHudTiles from the new module, not from hud-poc-page.js"
    - "Existing canvas-path tests stay green — pure behavior-preserving move (RPROMO-02)"
  artifacts:
    - path: "packages/g2-app/src/hud/push-hud-tiles.ts"
      provides: "Extracted pushHudTiles serialized tile-push (CM-01 contract)"
      exports: ["pushHudTiles"]
      contains: "export async function pushHudTiles"
    - path: "packages/g2-app/src/hud/push-hud-tiles.test.ts"
      provides: "Isolated tests for pushHudTiles (replaces coverage lost when hud-poc-page.test.ts is deleted in Plan 03)"
  key_links:
    - from: "packages/g2-app/src/engine/layer-manager.ts"
      to: "packages/g2-app/src/hud/push-hud-tiles.ts"
      via: "import { pushHudTiles }"
      pattern: "from '\\.\\./hud/push-hud-tiles\\.js'"
    - from: "packages/g2-app/src/engine/hud-delta-driver.ts"
      to: "packages/g2-app/src/hud/push-hud-tiles.ts"
      via: "import { pushHudTiles }"
      pattern: "from '\\.\\./hud/push-hud-tiles\\.js'"
---

<objective>
Extract the production `pushHudTiles` function out of the PoC file `hud/hud-poc-page.ts` into a new dedicated production module `hud/push-hud-tiles.ts`, and re-point the two production importers (`engine/layer-manager.ts` line 39, `engine/hud-delta-driver.ts` line 34) at the new module. This is a pure, behavior-preserving move — zero functional change — that UNBLOCKS deleting `hud-poc-page.ts` in Plan 03 (RESEARCH Pitfall 1).

Purpose: `pushHudTiles` is the canvas-path serialized tile-push (CM-01 contract: SDK rejects concurrent `updateImageRawData`, so the `for...of` + `await` loop is load-bearing). It currently lives in a PoC-labelled file that D-25.1 mandates deleting. Per D-25.1 "preserve ONLY the helpers genuinely reused by the production canvas path" — so it must be extracted, not removed.

Output: New `hud/push-hud-tiles.ts` module + isolated test file; both production importers re-pointed; `@see` doc tags updated to remove stale `hud-poc-page.ts` references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-CONTEXT.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hud/push-hud-tiles.ts with the extracted pushHudTiles function + isolated tests</name>
  <files>packages/g2-app/src/hud/push-hud-tiles.ts, packages/g2-app/src/hud/push-hud-tiles.test.ts</files>
  <read_first>
    - packages/g2-app/src/hud/hud-poc-page.ts (source of pushHudTiles — lines 207-225 body; lines 33-41 imports for ImageRawDataUpdate, ImageRawDataUpdateResult from @evenrealities/even_hub_sdk, and HudTile from ./hud-raster-frame.js)
    - packages/g2-app/src/hud/hud-raster-frame.ts (HudTile type definition + the @see consumer doc tag near line 93 referencing hud-poc-page.ts)
    - packages/g2-app/src/hud/hud-poc-page.test.ts (existing pushHudTiles test cases — to mirror the assertions in the new isolated test file; do NOT delete this file here, Plan 03 deletes it)
  </read_first>
  <behavior>
    - pushHudTiles(bridge, tiles): for each tile, constructs an ImageRawDataUpdate with containerID, containerName, imageData=tile.bytes, awaits bridge.updateImageRawData, and on non-success result (ImageRawDataUpdateResult.isSuccess === false) emits a console.warn — NEVER throws.
    - Tile pushes are serial (one await per tile, in order) — the CM-01 contract; assert ordering / call count.
    - Test: 2 tiles, both success → updateImageRawData called twice, no warn.
    - Test: 1 tile, non-success result → console.warn called once, function resolves (no throw).
    - Test: empty tiles array → updateImageRawData never called, resolves.
  </behavior>
  <action>Create packages/g2-app/src/hud/push-hud-tiles.ts. Move the EXACT body of pushHudTiles from hud-poc-page.ts (signature `pushHudTiles(bridge: Pick&lt;EvenAppBridge, 'updateImageRawData'&gt;, tiles: ReadonlyArray&lt;HudTile&gt;): Promise&lt;void&gt;`). Import ImageRawDataUpdate + ImageRawDataUpdateResult from '@evenrealities/even_hub_sdk', EvenAppBridge type from the same, and HudTile type from './hud-raster-frame.js'. Change the console.warn prefix from `[EVF] hud-poc:` to `[EVF] push-hud-tiles:` (RESEARCH §Code Examples — PoC label removed for INV-4 cleanliness). Add a module-level TSDoc block documenting the CM-01 serialization contract (cite docs/architecture/0013-hud-raster-rendering.md Amendment 1 + .planning/debug/glasses-render-blank-containerid.md as @see). Then create push-hud-tiles.test.ts mirroring the pushHudTiles cases from hud-poc-page.test.ts (success path, non-success warn path, empty-array path) so coverage is preserved when Plan 03 deletes hud-poc-page.test.ts (RESEARCH Wave 0 Gap 2). Use a mock bridge with a vi.fn() updateImageRawData returning a success-shaped result, mirror existing makeMockBridge patterns from hud-poc-page.test.ts.</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec vitest run src/hud/push-hud-tiles.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/g2-app/src/hud/push-hud-tiles.ts` (file exists)
    - `grep -c 'export async function pushHudTiles' packages/g2-app/src/hud/push-hud-tiles.ts` returns 1
    - `grep -c 'hud-poc' packages/g2-app/src/hud/push-hud-tiles.ts` returns 0 (no PoC label leaked into the new module)
    - `corepack pnpm exec vitest run src/hud/push-hud-tiles.test.ts` exits 0 with ≥3 tests passing
  </acceptance_criteria>
  <done>push-hud-tiles.ts exports pushHudTiles (byte-equivalent logic, warn prefix updated); isolated test file passes; no `hud-poc` string in either new file.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point production importers + remove pushHudTiles from hud-poc-page.ts + fix stale @see tags</name>
  <files>packages/g2-app/src/engine/layer-manager.ts, packages/g2-app/src/engine/hud-delta-driver.ts, packages/g2-app/src/hud/hud-raster-frame.ts</files>
  <read_first>
    - packages/g2-app/src/engine/layer-manager.ts (line 39 import `{ pushHudTiles } from '../hud/hud-poc-page.js'`; _compositeAndPush consumer)
    - packages/g2-app/src/engine/hud-delta-driver.ts (line 34 import + line 27 stale @see `hud-poc-page.ts`)
    - packages/g2-app/src/hud/hud-raster-frame.ts (line ~93 @see tag referencing hud-poc-page.ts as pushHudTiles consumer)
    - packages/g2-app/src/hud/hud-poc-page.ts (still imports/exports pushHudTiles; the PoC's boot-hud-raster-poc.ts:66 ALSO imports pushHudTiles from here — keep hud-poc-page's own export until Plan 03 deletes the whole file, OR re-point boot-hud-raster-poc.ts too; planner decision: re-point boot-hud-raster-poc.ts:66 to the new module as well so hud-poc-page.ts no longer needs to export pushHudTiles)
  </read_first>
  <action>In engine/layer-manager.ts line 39 and engine/hud-delta-driver.ts line 34, change the import source from `'../hud/hud-poc-page.js'` to `'../hud/push-hud-tiles.js'` (keep the named import `pushHudTiles` identical). In hud-delta-driver.ts line 27, update the stale `@see packages/g2-app/src/hud/hud-poc-page.ts (pushHudTiles CM-01 serialization)` tag to point at `packages/g2-app/src/hud/push-hud-tiles.ts` (RESEARCH Pitfall 6). In hud-raster-frame.ts near line 93, update the `@see ... hud-poc-page.ts (consumer — pushHudTiles)` tag to `push-hud-tiles.ts` (RESEARCH Pitfall 5). Also re-point boot-hud-raster-poc.ts:66 — change its `import { createHudPocPage, pushHudTiles } from './hud-poc-page.js'` to import `createHudPocPage` from './hud-poc-page.js' and `pushHudTiles` from './push-hud-tiles.js' (so hud-poc-page.ts can drop its pushHudTiles export). Then REMOVE the `pushHudTiles` function (and its now-unused imports ImageRawDataUpdate/ImageRawDataUpdateResult if no other symbol in hud-poc-page.ts uses them — verify before removing) from hud-poc-page.ts. Do NOT delete hud-poc-page.ts itself (Plan 03 does that). Run typecheck to confirm no dangling `Cannot find module` or unused-import errors.</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec tsc --noEmit &amp;&amp; corepack pnpm exec vitest run src/engine src/hud</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from '../hud/hud-poc-page" packages/g2-app/src/engine/` returns 0 (no engine file imports from the PoC page anymore)
    - `grep -c "from '../hud/push-hud-tiles.js'" packages/g2-app/src/engine/layer-manager.ts` returns 1
    - `grep -c "from '../hud/push-hud-tiles.js'" packages/g2-app/src/engine/hud-delta-driver.ts` returns 1
    - `grep -c "export async function pushHudTiles" packages/g2-app/src/hud/hud-poc-page.ts` returns 0 (removed from PoC file)
    - `grep -rn "hud-poc-page.ts (pushHudTiles\|hud-poc-page.ts (consumer" packages/g2-app/src/engine packages/g2-app/src/hud` returns 0 (stale @see tags fixed; exclude the to-be-deleted PoC files themselves)
    - `corepack pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both production importers + the PoC's own importer point at push-hud-tiles.js; pushHudTiles removed from hud-poc-page.ts; stale @see tags fixed; typecheck + g2-app engine/hud tests green.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @evf/g2-app test` green (existing canvas-path tests unchanged — behavior-preserving move)
- `corepack pnpm exec tsc --noEmit` (from packages/g2-app) exits 0 — no dangling module imports
- No engine file imports `pushHudTiles` from `hud-poc-page.js` (grep returns 0)
</verification>

<success_criteria>
- pushHudTiles is a standalone production module `hud/push-hud-tiles.ts` with isolated tests
- layer-manager.ts + hud-delta-driver.ts (and boot-hud-raster-poc.ts) import it from the new path
- hud-poc-page.ts no longer exports pushHudTiles (now safe for Plan 03 to delete the whole file)
- Zero behavior change: full g2-app suite still green
</success_criteria>

<artifacts_produced>
- `packages/g2-app/src/hud/push-hud-tiles.ts` — extracted production module (pushHudTiles, CM-01 contract)
- `packages/g2-app/src/hud/push-hud-tiles.test.ts` — isolated coverage (replaces hud-poc-page.test.ts coverage of pushHudTiles, deleted in Plan 03)
</artifacts_produced>

<output>
Create `.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-01-SUMMARY.md` when done
</output>
