---
phase: 25-promozione-raster-a-default-boot-fallback-glyph
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - packages/g2-app/src/internal/boot-engine-core.ts
  - packages/g2-app/src/engine/__tests__/layer-manager.test.ts
autonomous: true
requirements: [RPROMO-02]

must_haves:
  truths:
    - "When the effective BLE verdict is 'glyph' (BLE-degraded path or persisted glyph override), boot wires LayerManager.setRenderMode('glyph') so the next bundle emits the 3-container text schema (D-25.3)"
    - "canvas→glyph switch is atomic: setRenderMode('glyph') + bundle([]) produces exactly ONE rebuildPageContainer call with the 3-container glyph schema and ZERO mixed-schema intermediate frame (success criterion #3)"
    - "Glyph fallback schema is byte-identical to pre-v0.10.0 behavior: containerTotalNum=3, header(id4)+footer(id5)+status-hud(id6) (D-25.4)"
  artifacts:
    - path: "packages/g2-app/src/internal/boot-engine-core.ts"
      provides: "effectiveVerdict→setRenderMode glyph fallback wire (the gap from RESEARCH §Glyph Fallback Switch)"
      contains: "setRenderMode"
    - path: "packages/g2-app/src/engine/__tests__/layer-manager.test.ts"
      provides: "canvas→glyph atomic-switch e2e test (Wave 0 gap)"
      contains: "atomic"
  key_links:
    - from: "packages/g2-app/src/internal/boot-engine-core.ts"
      to: "LayerManager.setRenderMode"
      via: "effectiveVerdict === 'glyph' branch"
      pattern: "setRenderMode\\((effectiveVerdict|'glyph')"
---

<objective>
Close the glyph-fallback wiring gap (RESEARCH §Glyph Fallback Switch + Pitfall 3) and add the missing canvas→glyph atomic-switch e2e test (RESEARCH Wave 0 Gap 1 + VALIDATION Wave 0). D-25.3: `RasterController.setBleVerdict('glyph')` / a persisted glyph override must result in `LayerManager.setRenderMode('glyph')` so the next `bundle()` atomically rebuilds to the 3-container text schema — with zero mixed-schema intermediate frame.

Purpose: Today boot-engine-core step 9/9b sets `setBleVerdict('glyph')` + `setMapMode('glyph')` but NEVER `setRenderMode('glyph')` — so even when the system is in glyph mode, `_flushPage()` still emits the canvas `buildHudRasterPageSchema()` (Pitfall 3). The `effectiveVerdict` variable (boot-engine-core line 689) already merges the BLE probe verdict (step 9) and the persisted override (step 9b) into one value — so a single `setRenderMode` call keyed on `effectiveVerdict` covers both paths.

Output: One `setRenderMode` wire in boot-engine-core keyed on `effectiveVerdict`; one new atomic-switch e2e test asserting `rebuildPageContainer` is called exactly once with the 3-container glyph schema after `setRenderMode('glyph')` + `bundle([])`.

Note on depends_on: Plan 02 does not share files with Plan 01, but is sequenced after the extraction (Wave 2) so the boot/engine work lands against a clean post-extraction tree (safe-refactor ordering).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-CONTEXT.md
@.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the canvas→glyph atomic-switch e2e test (Wave 0)</name>
  <files>packages/g2-app/src/engine/__tests__/layer-manager.test.ts</files>
  <read_first>
    - packages/g2-app/src/engine/__tests__/layer-manager.test.ts (existing LMT-RM-* renderMode tests around line 1013; LMT-CF-01 canvas _flushPage test around line 1043 — mirror its makeMockBridge + makeFakeCompositor setup)
    - packages/g2-app/src/engine/layer-manager.ts (_flushPage glyph branch lines 655-665: glyph schema = containerTotalNum BOOT_CONTAINER_TOTAL, textObject buildStatusViewTextContainers(), imageObject []; bundle() at line 289 runs _flushPage)
    - packages/g2-app/src/engine/container-registry.ts (BOOT_CONTAINER_TOTAL value + buildStatusViewTextContainers shape — header id4 + footer id5 + status-hud id6, 3 entries)
  </read_first>
  <behavior>
    - Boot a LayerManager in canvas mode (setRenderMode('canvas'), bundle a capture-providing layer so the canvas invariant is satisfied — mirror LMT-CF-01). Clear the rebuildPageContainer spy.
    - Call setRenderMode('glyph') then bundle([]) (empty ops — no mount/destroy).
    - Assert rebuildPageContainer was called EXACTLY ONCE after the switch (call history cleared first, then length === 1) — proves atomicity, ZERO mixed-schema intermediate frame.
    - Assert the single rebuildPageContainer payload has containerTotalNum === BOOT_CONTAINER_TOTAL (3) and textObject.length === 3 and imageObject.length === 0 — the glyph 3-container schema (D-25.4 byte-identical fallback shape).
    - Assert updateImageRawData was NOT called during the glyph bundle (glyph mode pushes no image tiles).
  </behavior>
  <action>Add a new describe block (suggested id LMT-ATOMIC-01) to layer-manager.test.ts titled for the Phase 25 canvas→glyph atomic switch. Reuse the existing makeMockBridge + makeFakeCompositor + capture-layer pattern from LMT-CF-01. Sequence: construct LayerManager with compositor, setRenderMode('canvas'), bundle a mount of a capture-providing layer (getCaptureContainer returns 'hud-capture', getContainerCount returns {image:0,text:0}), then reset the rebuildPageContainer spy's call history (vi.clearAllMocks or mockClear on the spy), then setRenderMode('glyph') + bundle([]). Extract the schema object passed to the RebuildPageContainer constructor in bridge.rebuildPageContainer and assert containerTotalNum, textObject.length, imageObject.length per the behavior block. Import BOOT_CONTAINER_TOTAL from container-registry to assert against the constant, not a magic 3. This test exercises the LayerManager setRenderMode+bundle glyph mechanism (which Task 2 then wires into boot); it is the regression guard for atomicity. Confirm it passes.</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec vitest run src/engine/__tests__/layer-manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'atomic' packages/g2-app/src/engine/__tests__/layer-manager.test.ts` returns at least 1
    - The new test asserts rebuildPageContainer called exactly once after the canvas→glyph switch (single-call assertion present)
    - The new test asserts containerTotalNum === BOOT_CONTAINER_TOTAL and textObject.length === 3 and imageObject.length === 0
    - `corepack pnpm exec vitest run src/engine/__tests__/layer-manager.test.ts` exits 0
  </acceptance_criteria>
  <done>New atomic-switch e2e test exists and passes: canvas→glyph via setRenderMode('glyph')+bundle([]) yields exactly one rebuildPageContainer with the 3-container glyph schema, zero mixed frame.</done>
</task>

<task type="auto">
  <name>Task 2: Wire setRenderMode to effectiveVerdict in boot-engine-core (glyph fallback)</name>
  <files>packages/g2-app/src/internal/boot-engine-core.ts</files>
  <read_first>
    - packages/g2-app/src/internal/boot-engine-core.ts (line 644 unconditional setRenderMode('canvas') at step 7; step 9 BLE verdict lines 653-657; step 9b persisted override lines 681-684; effectiveVerdict computation line 689 — already merges BLE verdict + persisted override; effectiveVerdict consumers at lines 763 + 1423)
    - packages/g2-app/src/engine/layer-manager.ts (setRenderMode signature line 449 — accepts 'canvas'|'glyph'; setMapMode is a DIFFERENT field for the MAP layer, not the HUD schema)
  </read_first>
  <action>In boot-engine-core.ts, immediately after the effectiveVerdict computation (line 689, the const that resolves to persistedMode when 'raster'/'glyph' else the BLE verdict), add a single setRenderMode wire keyed on effectiveVerdict: call layerManager.setRenderMode(effectiveVerdict === 'glyph' ? 'glyph' : 'canvas'). This SUPERSEDES the unconditional setRenderMode('canvas') at step 7 line 644 for the glyph case — keep the line-644 'canvas' default (correct for the common path and for effectiveVerdict 'auto'/'raster') and let this new call flip to 'glyph' only when degraded. Rationale (RESEARCH Open Q2): placing the wire at effectiveVerdict (boot-time) covers BOTH the step-9 BLE probe verdict and the step-9b persisted glyph override in one site — no duplication across step 9 and 9b (Pitfall 3). Do NOT touch map-mode-toggle.ts (runtime toggle is a separate Phase 20 concern, out of D-25.3 scope per RESEARCH Open Q2). Add an inline comment citing D-25.3 + RPROMO-02 + the Pitfall 3 reference. Confirm the setMapMode calls at step 9/9b are left intact (they govern the MAP layer, not the HUD render substrate).</action>
  <verify>
    <automated>cd packages/g2-app &amp;&amp; corepack pnpm exec tsc --noEmit &amp;&amp; corepack pnpm exec vitest run src/internal src/engine</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setRenderMode(effectiveVerdict === 'glyph'" packages/g2-app/src/internal/boot-engine-core.ts` returns 1
    - `grep -c 'D-25.3' packages/g2-app/src/internal/boot-engine-core.ts` returns at least 1 (decision citation present)
    - `grep -c 'setRenderMode' packages/g2-app/src/engine/map-mode-toggle.ts` returns 0 (runtime toggle untouched — D-25.3 scope)
    - `corepack pnpm exec tsc --noEmit` exits 0
    - `corepack pnpm exec vitest run src/internal src/engine` exits 0 (boot + layer tests green, incl. the new atomic test)
  </acceptance_criteria>
  <done>boot-engine-core sets renderMode to 'glyph' exactly when effectiveVerdict is 'glyph' (covering BLE probe + persisted override); canvas/auto/raster paths still default to 'canvas'; typecheck + boot/engine tests green.</done>
</task>

</tasks>

<verification>
- corepack pnpm --filter @evf/g2-app test green (boot-engine + layer-manager incl. new atomic-switch test)
- corepack pnpm exec tsc --noEmit (from packages/g2-app) exits 0
- Atomic-switch test proves zero mixed-schema intermediate frame on canvas→glyph
</verification>

<success_criteria>
- effectiveVerdict==='glyph' → LayerManager.setRenderMode('glyph') at boot (gap closed, both step-9 + step-9b paths)
- canvas→glyph switch is provably atomic (single rebuildPageContainer, 3-container glyph schema, zero mixed frame) — success criterion #3
- Glyph fallback schema shape unchanged (containerTotalNum=3) — backward-compat with D-25.4
- map-mode-toggle.ts untouched (runtime toggle out of scope)
</success_criteria>

<artifacts_produced>
- glyph-fallback wire in `packages/g2-app/src/internal/boot-engine-core.ts` (effectiveVerdict→setRenderMode)
- canvas→glyph atomic-switch e2e test in `packages/g2-app/src/engine/__tests__/layer-manager.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/EVF-25-promozione-raster-a-default-boot-fallback-glyph/25-02-SUMMARY.md` when done
</output>
