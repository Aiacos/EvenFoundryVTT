---
phase: 23-combat-tracker-su-canvas-combatant-ac
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/foundry-module/src/readers/combat-reader.ts
  - packages/foundry-module/src/readers/readers.test.ts
autonomous: true
requirements: [RDATA-05]
must_haves:
  truths:
    - "getCombatSnapshot populates `ac` from actor.system.attributes.ac.value when numeric"
    - "An unlinked combatant (actor null) yields `ac` undefined (no key emitted)"
    - "A combatant whose ac.value is missing/non-numeric yields `ac` undefined"
    - "AC is read from the edition-stable single field — no flat+bonus+armor derivation (D-23.4)"
  artifacts:
    - path: "packages/foundry-module/src/readers/combat-reader.ts"
      provides: "extractCombatantAc + ac wiring into getCombatSnapshot"
      contains: "extractCombatantAc"
    - path: "packages/foundry-module/src/readers/readers.test.ts"
      provides: "RDATA-05 reader tests (present / null actor / non-number)"
  key_links:
    - from: "packages/foundry-module/src/readers/combat-reader.ts"
      to: "CombatantSchema.ac"
      via: "spread `...(acVal !== undefined ? { ac: acVal } : {})` in the combatant map"
      pattern: "ac: acVal|extractCombatantAc"
---

<objective>
Extend `getCombatSnapshot()` (foundry-module combat reader) to populate the optional
`ac` field on each combatant by reading `combatant.actor?.system.attributes.ac?.value`,
null-safe exactly like the existing `concentration` extraction and the Phase 21
`extractWalkSpeed` pattern. Per D-23.4: read the single edition-stable field only — NO
flat+bonus+armor derivation. Absent/non-numeric/unlinked → `ac` stays undefined and the
key is NOT emitted (so the renderer's `' --'` fallback applies).

Implements RDATA-05 (reader half) per D-23.4 and D-23.6 (second atomic piece — depends on
Plan 01's schema field). `Dnd5eAttributes.ac: { value: number }` is ALREADY declared in
`foundry-globals.d.ts` line 254 (research finding 2) — NO ambient-types change is needed.

Purpose: Foundry-side read path that surfaces the real AC into the WS combat delta.
Output: `extractCombatantAc()` helper + `ac` wired into the combatant map + reader tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-CONTEXT.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-RESEARCH.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-VALIDATION.md
@packages/shared-protocol/src/payloads/combat.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RDATA-05 reader tests for ac to readers.test.ts (RED)</name>
  <files>packages/foundry-module/src/readers/readers.test.ts</files>
  <read_first>
    - packages/foundry-module/src/readers/readers.test.ts (the existing `describe('getCombatSnapshot')` block ~line 1491 — clone its game.combat mock setup, especially how `c.actor.system.attributes` is stubbed)
    - packages/foundry-module/src/readers/combat-reader.ts (current getCombatSnapshot map + concentration null-safety pattern to mirror)
  </read_first>
  <behavior>
    - Test: combatant with `actor.system.attributes.ac.value === 18` → snapshot combatant has `ac === 18`.
    - Test: combatant with `actor === null` (unlinked) → combatant has no `ac` key (`'ac' in combatant === false` / `combatant.ac === undefined`).
    - Test: combatant whose `ac.value` is `undefined`/`'18'` (string)/`NaN` → `ac` undefined.
    - Test: AC value `18.6` rounds to `19`; negative clamps to `0` (matches helper contract; keeps schema `.int().nonnegative()` happy).
  </behavior>
  <action>
    Extend the existing `describe('getCombatSnapshot')` block in `readers.test.ts` with a nested `describe('ac extraction (RDATA-05)')` covering the four cases in <behavior>. Reuse the block's existing `game.combat` mock harness; add `system.attributes.ac` to the mocked actor where needed (the hp mock already stubs `system.attributes.hp` — mirror that shape). Assert on the mapped combatant's `ac`. Tests MUST fail initially (RED) — `getCombatSnapshot` does not yet read `ac`.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm --filter @evf/foundry-module test -- --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ac extraction (RDATA-05)" packages/foundry-module/src/readers/readers.test.ts` returns a match.
    - The `ac === 18` test FAILS before Task 2 (RED).
  </acceptance_criteria>
  <done>readers.test.ts contains the four RDATA-05 reader tests under getCombatSnapshot; the positive case is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractCombatantAc + wire ac into getCombatSnapshot (GREEN)</name>
  <files>packages/foundry-module/src/readers/combat-reader.ts</files>
  <read_first>
    - packages/foundry-module/src/readers/combat-reader.ts (the combatant `.map(...)` callback + the `...(concentration !== undefined ? { concentration } : {})` conditional-spread to mirror for `ac`)
    - packages/foundry-module/src/types/foundry-globals.d.ts lines 247-260 (Dnd5eAttributes.ac already declared — read-only confirmation; do NOT modify this file)
  </read_first>
  <action>
    In `combat-reader.ts`, add a module-level helper `extractCombatantAc(actor: FoundryActor | null): number | undefined` that reads `actor?.system.attributes.ac?.value`, returns `undefined` when `typeof val !== 'number' || !Number.isFinite(val)`, else returns `Math.max(0, Math.round(val))` (null-safe, mirrors extractWalkSpeed; D-23.4 — single field, no derivation). Import the `FoundryActor` ambient type the same way other readers reference it (it is a global ambient interface — reference by name, no import needed if the file already relies on ambient globals; match the existing style in this file). Inside the combatant `.map`, compute `const acVal = extractCombatantAc(c.actor);` and append `...(acVal !== undefined ? { ac: acVal } : {})` to the returned object (after the concentration spread). JSDoc on `extractCombatantAc` per INV-4. Do NOT touch foundry-globals.d.ts (research finding 2 — `ac.value` already declared). Do NOT add flat/bonus/armor logic (D-23.4, deferred idea).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm --filter @evf/foundry-module test -- --run 2>&1 | tail -15 && corepack pnpm --filter @evf/foundry-module exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "extractCombatantAc" packages/foundry-module/src/readers/combat-reader.ts` returns `>= 2` (definition + call site).
    - `git -C /home/aiacos/workspace/EvenFoundryVTT diff --quiet packages/foundry-module/src/types/foundry-globals.d.ts && echo UNCHANGED` prints `UNCHANGED` (no ambient-types edit).
    - All four RDATA-05 reader tests PASS (GREEN).
    - `corepack pnpm --filter @evf/foundry-module exec tsc --noEmit` exits 0.
    - Full foundry-module suite green (no regression; socketlib handler count unaffected — read-path-only change).
  </acceptance_criteria>
  <done>getCombatSnapshot emits real `ac`; null-safe fallbacks proven; foundry-globals.d.ts untouched; typecheck + suite green.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @evf/foundry-module test -- --run` — all green incl. RDATA-05 reader tests.
- `corepack pnpm --filter @evf/foundry-module exec tsc --noEmit` — exit 0.
- foundry-globals.d.ts is byte-unchanged (grep guard above).
</verification>

<success_criteria>
- `extractCombatantAc()` reads `actor.system.attributes.ac.value` null-safely (D-23.4).
- `getCombatSnapshot()` populates `ac` for linked combatants; omits it (undefined) for unlinked/missing.
- No foundry-globals ambient change; no AC derivation arithmetic.
- ROADMAP success criterion #4 (reader half of atomicity) satisfied for this plan.
</success_criteria>

<output>
Create `.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-02-SUMMARY.md` when done.
Record: extractCombatantAc signature, the conditional-spread call site, and confirmation foundry-globals.d.ts was not modified.
</output>
