---
phase: 23-combat-tracker-su-canvas-combatant-ac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-protocol/src/payloads/combat.ts
  - packages/shared-protocol/src/payloads/combat.test.ts
autonomous: true
requirements: [RDATA-05]
must_haves:
  truths:
    - "CombatantSchema accepts an optional integer non-negative `ac` field"
    - "Existing Combatant literals with no `ac` field still parse (no downstream mass-update)"
    - "A Combatant with `ac: 18` parses and round-trips the value"
    - "A Combatant with `ac: -1` or `ac: 17.5` is rejected"
  artifacts:
    - path: "packages/shared-protocol/src/payloads/combat.ts"
      provides: "CombatantSchema.ac optional field"
      contains: "ac: z.number().int().nonnegative().optional()"
    - path: "packages/shared-protocol/src/payloads/combat.test.ts"
      provides: "RDATA-05 schema tests for ac"
  key_links:
    - from: "packages/shared-protocol/src/payloads/combat.ts"
      to: "Combatant type (z.infer)"
      via: "CombatantSchema extension"
      pattern: "ac.*z\\.number\\(\\)\\.int\\(\\)\\.nonnegative\\(\\)\\.optional\\(\\)"
---

<objective>
Extend `CombatantSchema` (shared-protocol) with an OPTIONAL `ac` field so the combat
tracker can carry a real Armor Class value. Per research finding 1 and Pitfall 1/5,
the field MUST be `.optional()`: `CombatantSchema` is a `z.strictObject` and 31+ existing
test literals construct `Combatant` objects without `ac` — a required field would break
them all. The optional field mirrors the `concentration` precedent already in this schema.

Implements RDATA-05 (schema half) per D-23.4 and D-23.6 (atomicity — schema is the first
of the three atomic pieces; reader = Plan 02, canvas panel = Plan 03).

Purpose: Single source of truth for the `ac` field, consumed by foundry-module (producer,
Plan 02) and g2-app (renderer, Plan 03).
Output: `CombatantSchema.ac?: number` + Zod tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-CONTEXT.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-RESEARCH.md
@.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RDATA-05 ac Zod tests to combat.test.ts (RED)</name>
  <files>packages/shared-protocol/src/payloads/combat.test.ts</files>
  <read_first>
    - packages/shared-protocol/src/payloads/combat.test.ts (existing CombatantSchema test patterns + a valid Combatant literal to clone)
    - packages/shared-protocol/src/payloads/combat.ts (current CombatantSchema shape + `concentration` optional precedent)
  </read_first>
  <behavior>
    - Test: a valid Combatant literal WITHOUT `ac` still passes `CombatantSchema.safeParse` (backward-compat — proves no downstream mass-update needed).
    - Test: a Combatant literal WITH `ac: 18` passes and `parsed.data.ac === 18`.
    - Test: `ac: -1` fails `safeParse` (`.nonnegative()` guard).
    - Test: `ac: 17.5` fails `safeParse` (`.int()` guard).
    - Test: `ac` absent → `parsed.data.ac === undefined` (optional, not defaulted).
  </behavior>
  <action>
    Extend `packages/shared-protocol/src/payloads/combat.test.ts` with a `describe('CombatantSchema.ac (RDATA-05)')` block covering the five cases in <behavior>. Clone an existing valid Combatant literal from this file (id/name/actorId/initiative/hp/maxHp/isCurrentTurn) as the base; add/omit `ac` per case. Use `.safeParse` and assert `.success` true/false plus `parsed.data.ac` where applicable. Tests MUST fail initially (RED) because `ac` is not yet in the schema — the `ac: 18` case will fail strictObject (unknown key) until Task 2 lands.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm --filter @evf/shared-protocol test -- --run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CombatantSchema.ac (RDATA-05)" packages/shared-protocol/src/payloads/combat.test.ts` returns a match.
    - The new `ac: 18` test FAILS before Task 2 (RED — strictObject rejects the unknown key).
  </acceptance_criteria>
  <done>combat.test.ts contains the five RDATA-05 ac tests; the positive `ac: 18` case is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add optional ac field to CombatantSchema (GREEN)</name>
  <files>packages/shared-protocol/src/payloads/combat.ts</files>
  <read_first>
    - packages/shared-protocol/src/payloads/combat.ts (CombatantSchema — add the field after `concentration`)
  </read_first>
  <action>
    In `CombatantSchema` (the `z.strictObject`), after the `concentration` field, add `ac: z.number().int().nonnegative().optional()` with a JSDoc comment (Phase 23 addition; OPTIONAL — absence = `' --'` fallback in renderer; mirrors `concentration` optional precedent; per D-23.4/RDATA-05). Do NOT change `Combatant`/`CombatSnapshot` type exports — `z.infer` picks up the new optional field automatically. Do NOT make it required (Pitfall 1: 31+ existing literals would break). JSDoc per INV-4 (public API surface).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/EvenFoundryVTT && corepack pnpm --filter @evf/shared-protocol test -- --run 2>&1 | tail -15 && corepack pnpm --filter @evf/shared-protocol exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ac: z.number().int().nonnegative().optional()" packages/shared-protocol/src/payloads/combat.ts` returns `1`.
    - All five RDATA-05 ac tests now PASS (GREEN).
    - `corepack pnpm --filter @evf/shared-protocol exec tsc --noEmit` exits 0.
    - No other shared-protocol test regresses (full package suite green).
  </acceptance_criteria>
  <done>CombatantSchema carries optional `ac`; all RDATA-05 tests green; typecheck clean; no regressions.</done>
</task>

</tasks>

<verification>
- `corepack pnpm --filter @evf/shared-protocol test -- --run` — all green incl. new RDATA-05 ac tests.
- `corepack pnpm --filter @evf/shared-protocol exec tsc --noEmit` — exit 0.
- Backward-compat: no other package's tests need a literal update (run `corepack pnpm test` at the wave merge to confirm 0 regressions across foundry-module/g2-app/bridge).
</verification>

<success_criteria>
- `CombatantSchema.ac?: number` (int, nonnegative, optional) exists in shared-protocol.
- Existing `Combatant` literals without `ac` still parse (no downstream mass-update).
- ROADMAP success criterion #4 (schema half of atomicity) satisfied for this plan.
</success_criteria>

<output>
Create `.planning/phases/EVF-23-combat-tracker-su-canvas-combatant-ac/23-01-SUMMARY.md` when done.
Record: the exact field line added, test count delta, and confirmation that 0 downstream literals required updates.
</output>

<phase_artifacts>
## Artifacts this phase produces (all 3 plans)

| Artifact | Plan | Kind |
|----------|------|------|
| `CombatantSchema.ac?: number` (int, nonnegative, optional) | 23-01 | schema field (shared-protocol) |
| `extractCombatantAc(actor): number | undefined` | 23-02 | reader helper (foundry-module) |
| `getCombatSnapshot()` emits `ac` per combatant | 23-02 | reader wiring (foundry-module) |
| `CanvasCombatTrackerPanel` (default export) | 23-03 | dual-interface canvas overlay class (g2-app) |
| Panel id `'canvas-combat-tracker'` + `static meta` | 23-03 | panel registry id (g2-app) |
| Real AC in `renderCombatantRow` (`c.ac` else `' --'`) | 23-03 | shared row-renderer change (g2-app) |
| Boot dispatch arm `'combat-tracker'` to `'canvas-combat-tracker'` (canvas mode) | 23-03 | boot-engine onNavigate gate |
| `setPanelInstanceHandler('canvas-combat-tracker')` (wsEventBus + QA handler injection) | 23-03 | boot-engine wiring |
| combat.turn + combat.state self-subscription | 23-03 | panel onMount lifecycle (g2-app) |
</phase_artifacts>

<source_audit>
## Multi-Source Coverage Audit

Every source item is COVERED by a plan. No unplanned items.

| Source | Item | Covered by | Notes |
|--------|------|------------|-------|
| GOAL (ROADMAP) | Combat tracker as raster z=2 overlay (5-window, current-turn highlight, HP/AC, gestures preserved) | 23-03 | SC #1, #2, #3 |
| REQ | RCOMB-01 (combat tracker raster overlay z=2) | 23-03 | panel + render |
| REQ | RDATA-05 (CombatantSchema.ac + read path) | 23-01 (schema), 23-02 (reader), 23-03 (render) | three atomic pieces |
| RESEARCH | CombatantSchema.ac optional (Pitfall 1/5 — avoid 31-literal mass update) | 23-01 | `.optional()` |
| RESEARCH | AC reader null-safe `actor.system.attributes.ac?.value`; no ambient change needed | 23-02 | finding 2 confirmed |
| RESEARCH | Approach A (reuse computeWindow/renderCombatantRow strings to ctx.fillText) | 23-03 | Pattern 6 / A2 |
| RESEARCH | Self-subscribe combat.turn + combat.state via setWsEventBus (Pitfall 4/5) | 23-03 | open Q1 / A4 |
| RESEARCH | Boot dispatch gate one-liner (Pattern 5); distinct panel id (Pitfall 2) | 23-03 | gate + id |
| CONTEXT | D-23.1 mirror CanvasCharacterSheetPanel | 23-03 | structural mirror |
| CONTEXT | D-23.2 per-combatant row content incl. real ac (+ ' --' fallback) | 23-03 | shared renderer |
| CONTEXT | D-23.3 auto-follow + manual scroll | 23-03 | _scrollOffset reset on turn change |
| CONTEXT | D-23.4 ac.value + fallback, no derivation | 23-02 | extractCombatantAc |
| CONTEXT | D-23.5 gesture parity, no PanelGestureBus/panel-router changes | 23-03 | grep guard (Task 3) |
| CONTEXT | D-23.6 atomicity (schema + reader + panel in scope) | 23-01 + 23-02 + 23-03 | one phase |

**Excluded (not gaps):** AC flat+bonus+armor derivation (Deferred), delta-loop xxhash (Phase 24), raster default boot promotion (Phase 25).
</source_audit>
