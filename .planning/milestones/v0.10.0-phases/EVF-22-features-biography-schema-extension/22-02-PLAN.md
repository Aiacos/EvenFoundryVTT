---
phase: 22-features-biography-schema-extension
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - packages/foundry-module/src/readers/character-reader.ts
  - packages/foundry-module/src/types/foundry-globals.d.ts
  - packages/foundry-module/src/readers/readers.test.ts
autonomous: true
requirements: [RDATA-03, RDATA-04]

must_haves:
  truths:
    - "extractFeats() reads actor.items filtered to type==='feat' and maps system.type.value → category (dnd5e 2024 path)"
    - "extractFeats() falls back to category:'general', isOrigin:false when system.type is absent (dnd5e 2014)"
    - "extractFeats() returns [] for an actor with no feat items and never throws on missing structure"
    - "extractBiography() reads system.details.trait as personality (NOT details.personality) and HTML-strips backstory"
    - "getCharacterSnapshot() emits feats + biography on the returned snapshot"
  artifacts:
    - path: "packages/foundry-module/src/readers/character-reader.ts"
      provides: "extractFeats(), extractBiography(), wired into getCharacterSnapshot()"
      contains: "function extractFeats"
    - path: "packages/foundry-module/src/types/foundry-globals.d.ts"
      provides: "FoundryItem.system.type/description + Dnd5eDetails.{trait,ideal,bond,flaw,biography} ambient types"
      contains: "trait?: string"
    - path: "packages/foundry-module/src/readers/readers.test.ts"
      provides: "CR-FT-* and CR-BIO-* reader tests"
      contains: "CR-FT"
  key_links:
    - from: "getCharacterSnapshot()"
      to: "extractFeats() / extractBiography()"
      via: "return object fields feats/biography"
      pattern: "feats: extractFeats\\(actor\\)"
    - from: "extractBiography()"
      to: "actor.system.details.trait"
      via: "personality mapping"
      pattern: "details\\.trait"
---

<objective>
Add the two foundry-module readers `extractFeats()` and `extractBiography()` to `character-reader.ts`, extend the ambient `foundry-globals.d.ts` with the dnd5e field shapes they read, and wire both into `getCharacterSnapshot()` so a live snapshot carries real feats + biography.

Purpose: RDATA-03 / RDATA-04 — the snapshot schema (Plan 22-01) is now extensible; this plan makes the Foundry side actually populate it from `actor.items` (feats) and `actor.system.details.*` (biography). The critical correctness finding (research Pitfall 2): personality lives at `details.trait`, NOT `details.personality`.
Output: two new reader functions + ambient type extensions + getCharacterSnapshot wiring + CR-FT-* / CR-BIO-* tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-VALIDATION.md
@.planning/phases/EVF-22-features-biography-schema-extension/22-01-SUMMARY.md
@packages/foundry-module/src/readers/character-reader.ts
@packages/foundry-module/src/types/foundry-globals.d.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extractFeats() reader + FoundryItem ambient type + CR-FT tests (RED→GREEN)</name>
  <files>packages/foundry-module/src/readers/readers.test.ts, packages/foundry-module/src/readers/character-reader.ts, packages/foundry-module/src/types/foundry-globals.d.ts</files>
  <read_first>
    - packages/foundry-module/src/readers/character-reader.ts (extractClass lines 522-540, extractWalkSpeed lines 558-592 — copy the `ReturnType<typeof game.actors.get>` signature + null-safety style; extractInventory/extractSpellbook for the actor.items.contents iteration pattern)
    - packages/foundry-module/src/types/foundry-globals.d.ts (FoundryItem lines 539-568 — extend system with type/description; mirror existing inline-string-literal style, NO imports per Pitfall 6)
    - packages/foundry-module/src/readers/readers.test.ts (existing reader test conventions; how mock actors/items are constructed)
    - .planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md (Pattern 2 + Pattern 2a + Code Example 3; Pitfall 1; §Suggested Test IDs CR-FT-N; assumptions A1/A2)
  </read_first>
  <behavior>
    - CR-FT-1 (2024 path): mock actor with a feat item `{type:'feat', name:'War Caster', system:{type:{value:'feat', subtype:'origin'}, description:{value:'<p>conc adv</p>'}}}` → extractFeats yields `{category:'feat', name:'War Caster', isOrigin:true, description:'conc adv'}` (HTML stripped)
    - CR-FT-2 (2014 fallback): feat item with NO system.type → `{category:'general', isOrigin:false, ...}`, no throw
    - CR-FT-3: actor with zero type==='feat' items → returns `[]`
    - CR-FT-4: background feature `{type:'feat', system:{type:{value:'background'}}}` → `{category:'background', isOrigin:false}` (background is NOT origin)
    - CR-FT-5: actor === undefined → returns `[]` (mirrors extractClass null-safety)
  </behavior>
  <action>
    Per D-22.3 (RDATA-03) add `function extractFeats(actor: ReturnType<typeof game.actors.get>): FeatEntry[]` to character-reader.ts (import `FeatEntry` type from `@evf/shared-protocol`). Filter `(actor.items?.contents ?? [])` to `item.type === 'feat'`. For each: read `typeObj = item.system?.type ?? {}`; `typeValue = (typeObj.value as string) ?? ''`; `typeSubtype = (typeObj.subtype as string) ?? ''`. Emit `category = typeValue.length > 0 ? typeValue : 'general'`; `name = item.name ?? 'Unknown Feat'`; `isOrigin = typeValue === 'feat' && typeSubtype === 'origin'` (per A2); `description = stripHtml(item.system?.description?.value ?? '')`. Add a small inline `stripHtml(html) => html.replace(/<[^>]*>/g, '')` helper in character-reader.ts (per research Pattern 3 note — foundry-module has no dep on g2-app, so inline rather than import; add JSDoc noting it mirrors the renderer's stripHtml). Mirror extractWalkSpeed null-safety exactly: never throw on missing structure; `actor === undefined` → `return []`. In foundry-globals.d.ts extend `FoundryItem.system` with optional `type?: { value?: string; subtype?: string }` and `description?: { value?: string }` (inline, no imports — Pitfall 6), with JSDoc citing dnd5e release-5.3.3 item-type-field.mjs. Write CR-FT-1..5 first (RED — extractFeats undefined), then implement to green.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run -t "CR-FT"</automated>
    <automated>pnpm --filter @evf/foundry-module exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function extractFeats" packages/foundry-module/src/readers/character-reader.ts` returns a match
    - `grep -n "type?: {" packages/foundry-module/src/types/foundry-globals.d.ts` shows the FoundryItem.system.type extension
    - `grep -v '^\s*\*\|^\s*//' packages/foundry-module/src/types/foundry-globals.d.ts | grep -c "import.*@evf/shared-protocol"` returns 0 (ambient file stays import-free per Pitfall 6)
    - CR-FT-1..5 all pass; package typecheck exits 0
  </acceptance_criteria>
  <done>extractFeats handles 2024 + 2014 + empty + undefined-actor paths; FoundryItem ambient type extended without imports; CR-FT tests green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extractBiography() reader + Dnd5eDetails ambient type + CR-BIO tests, wire both into getCharacterSnapshot (RED→GREEN)</name>
  <files>packages/foundry-module/src/readers/readers.test.ts, packages/foundry-module/src/readers/character-reader.ts, packages/foundry-module/src/types/foundry-globals.d.ts</files>
  <read_first>
    - packages/foundry-module/src/readers/character-reader.ts (getCharacterSnapshot lines 594-654 — the return object to extend; the `actor.system.details` access at line 640)
    - packages/foundry-module/src/types/foundry-globals.d.ts (Dnd5eDetails interface lines 286-288 — currently only `level`; extend with trait/ideal/bond/flaw/biography)
    - .planning/phases/EVF-22-features-biography-schema-extension/22-RESEARCH.md (Pattern 3 + Code Example 3; Pitfall 2 trait-vs-personality; Pitfall 3 HTML strip; assumption A3)
  </read_first>
  <behavior>
    - CR-BIO-1: actor with `system.details:{trait:'brave', ideal:'loyalty', bond:'home', flaw:'pride', biography:{value:'<p>veteran</p>'}}` → `{personality:'brave', ideal:'loyalty', bond:'home', flaw:'pride', backstory:'veteran'}` (note: trait → personality mapping; HTML stripped)
    - CR-BIO-2: HTML-stripping — `biography.value:'<h2>Hi</h2><strong>x</strong>'` → backstory `'Hix'`
    - CR-BIO-3: actor with empty/missing details → all five fields are empty strings (no throw)
    - CR-BIO-4: actor === undefined → all-empty-string BiographySnapshot
    - CR-FT-6 / CR-BIO-5 (integration): getCharacterSnapshot(validActorId) returns an object whose `feats` is FeatEntry[] and whose `biography.personality` reflects details.trait
  </behavior>
  <action>
    Per D-22.4 (RDATA-04) add `function extractBiography(actor: ReturnType<typeof game.actors.get>): BiographySnapshot` (import `BiographySnapshot` from `@evf/shared-protocol`). Read `details = actor.system?.details ?? {}`; map `personality = (details.trait as string) ?? ''` (CRITICAL: `trait`, NOT `personality` — Pitfall 2), `ideal/bond/flaw` similarly from `details.*`, `backstory = stripHtml((details.biography?.value as string) ?? '')` reusing the same inline stripHtml from Task 1. `actor === undefined` → return `{personality:'', ideal:'', bond:'', flaw:'', backstory:''}`. Add JSDoc: "biography.value is HTML; stripped before wire payload (T-05-03-02 mirror)". Extend `Dnd5eDetails` in foundry-globals.d.ts with optional `trait?: string; ideal?: string; bond?: string; flaw?: string; biography?: { value?: string; public?: string }` (JSDoc citing dnd5e 5.3.3 character.mjs / details.mjs; note the trait≠personality naming). Then wire BOTH readers into the `getCharacterSnapshot()` return object (after `speed: extractWalkSpeed(actor)` ~line 651, before `...portraitField`): `feats: extractFeats(actor)` and `biography: extractBiography(actor)`. Note these are populated unconditionally (not via optional spread) — the schema field is optional but the reader always emits a concrete value; that's fine (optional accepts present). Write CR-BIO-1..4 + integration CR-FT-6/CR-BIO-5 first (RED), then implement to green.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run -t "CR-BIO"</automated>
    <automated>pnpm --filter @evf/foundry-module test -- --run -t "CR-FT"</automated>
    <automated>pnpm --filter @evf/foundry-module exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function extractBiography" packages/foundry-module/src/readers/character-reader.ts` returns a match
    - `grep -n "details.trait" packages/foundry-module/src/readers/character-reader.ts` returns a match (proves trait≠personality fix)
    - `grep -n "feats: extractFeats(actor)" packages/foundry-module/src/readers/character-reader.ts` AND `grep -n "biography: extractBiography(actor)" ...` both return matches
    - `grep -n "trait?: string" packages/foundry-module/src/types/foundry-globals.d.ts` returns a match
    - CR-BIO-* + CR-FT-6 all pass; package typecheck exits 0
  </acceptance_criteria>
  <done>extractBiography maps details.trait→personality and strips HTML backstory with full null-safety; Dnd5eDetails ambient type extended; both readers wired into getCharacterSnapshot; CR-BIO + integration tests green; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry actor data → reader output (wire payload) | feat descriptions and biography backstory are author-controlled HTML; they cross into the WS payload here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering | HTML injection via biography.value / feat description | mitigate | `extractBiography()` / `extractFeats()` strip HTML reader-side (`html.replace(/<[^>]*>/g, '')`) before the value enters the wire payload |
| T-22-04 | DoS | missing/malformed dnd5e fields throwing in the reader | mitigate | null-safe reads with `?? ''` / `?? []` mirroring extractWalkSpeed; `actor === undefined` early-returns; never throw |
| T-22-SC | Tampering | npm installs | accept | No new packages this phase |
</threat_model>

<verification>
- `pnpm --filter @evf/foundry-module test -- --run` green (CI Gate 8 socketlib handler count must remain 17 — read-path-only extension, no new handler)
- `pnpm --filter @evf/foundry-module exec tsc --noEmit` exits 0
- `pnpm lint:ci` clean on touched files
</verification>

<success_criteria>
- extractFeats() + extractBiography() exist with the documented null-safety; both wired into getCharacterSnapshot()
- biography personality is sourced from `details.trait` (the Pitfall-2 correctness gate)
- HTML stripped from feat descriptions and backstory reader-side
- foundry-globals.d.ts extended for both shapes without introducing module imports
- All CR-FT-* / CR-BIO-* + integration tests green; package typecheck green; socketlib count unchanged (17)
</success_criteria>

<output>
Create `.planning/phases/EVF-22-features-biography-schema-extension/22-02-SUMMARY.md` when done. Record the exact reader signatures and confirm the getCharacterSnapshot return now carries feats + biography (for Plan 22-03's renderer wiring).
</output>
