---
phase: 21-character-sheet-su-canvas-dati-main-tab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-protocol/src/payloads/character.ts
  - packages/shared-protocol/src/payloads/character.test.ts
  - packages/foundry-module/src/readers/character-reader.ts
  - packages/foundry-module/src/readers/readers.test.ts
  - packages/foundry-module/src/types/foundry-globals.d.ts
autonomous: true
requirements: [RDATA-01, RDATA-02]
must_haves:
  truths:
    - "CharacterSnapshotSchema REQUIRES class (string), initiative (int), speed (non-neg int)"
    - "foundry-module reader emits class name(s) from actor.items, initiative from init.total, walk speed from movement.walk"
    - "getCharacterSnapshot wires the 3 new fields; getCharacterSnapshot output parses against the extended schema"
    - "pnpm test is GREEN workspace-wide after this plan (all ~27 downstream CharacterSnapshot literals carry the 3 new fields)"
  artifacts:
    - path: "packages/shared-protocol/src/payloads/character.ts"
      provides: "class/initiative/speed REQUIRED fields on CharacterSnapshotSchema"
      contains: "initiative: z.number().int()"
    - path: "packages/foundry-module/src/readers/character-reader.ts"
      provides: "extractClass + extractInitiativeModifier + extractWalkSpeed + getCharacterSnapshot wiring"
      contains: "extractInitiativeModifier"
    - path: "packages/foundry-module/src/types/foundry-globals.d.ts"
      provides: "Dnd5eAttributes.init + Dnd5eAttributes.movement"
      contains: "init?:"
  key_links:
    - from: "getCharacterSnapshot"
      to: "CharacterSnapshotSchema"
      via: "return object satisfies extended schema"
      pattern: "class:\\s*extractClass"
---

<objective>
Extend `CharacterSnapshotSchema` with three REQUIRED fields — `class` (display name(s)),
`initiative` (modifier), `speed` (walk feet) — add the corresponding `foundry-module`
readers, wire them into `getCharacterSnapshot`, and update EVERY downstream
`CharacterSnapshot` test literal (~27 files) so the workspace test suite stays GREEN.

This is the data-substrate plan. It must complete atomically: the schema extension makes
all existing literals invalid (TypeScript + Zod), so the literal updates land in the SAME
plan to avoid a red intermediate state (sequencing guidance + Phase 16/17 precedent).

Purpose: RDATA-01 (class reader) + RDATA-02 (initiative + speed readers). These feed the
canvas Main tab (Plan 21-03) and replace the `—` placeholders.
Output: extended schema + 3 readers + d.ts type additions + all downstream literals fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-RESEARCH.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan introduces these new symbols (downstream plans + Phase 22 depend on them):
- `CharacterSnapshotSchema.class` — `z.string()` REQUIRED
- `CharacterSnapshotSchema.initiative` — `z.number().int()` REQUIRED
- `CharacterSnapshotSchema.speed` — `z.number().int().nonnegative()` REQUIRED
- `extractClass(actor) -> string` (foundry-module reader)
- `extractInitiativeModifier(actor) -> number` (foundry-module reader)
- `extractWalkSpeed(actor) -> number` (foundry-module reader)
- `Dnd5eAttributes.init?: { total?: number }` + `Dnd5eAttributes.movement?: { walk?: number; ... }`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend CharacterSnapshotSchema with class/initiative/speed (RDATA-01, RDATA-02)</name>
  <files>packages/shared-protocol/src/payloads/character.ts, packages/shared-protocol/src/payloads/character.test.ts</files>
  <read_first>
    - packages/shared-protocol/src/payloads/character.ts (the `CharacterSnapshotSchema` z.strictObject block, ~line 480 onward; observe how Phase 16 `abilities` + Phase 17 `skills` were appended INSIDE the strictObject after the prior field)
    - packages/shared-protocol/src/payloads/character.test.ts (existing schema validation tests — mirror their assertion style for the 3 new fields)
    - 21-PATTERNS.md §`character.ts (model, CRUD)` (exact TSDoc + field placement)
  </read_first>
  <behavior>
    - Test: parsing a snapshot literal WITHOUT `class` fails (REQUIRED, strictObject)
    - Test: parsing WITHOUT `initiative` fails; WITHOUT `speed` fails
    - Test: `initiative` accepts negative integers; rejects non-integers
    - Test: `speed` rejects negative values (`.nonnegative()`); accepts 0 and 30
    - Test: `class` accepts empty string (classless/fresh actor) and "Fighter / Wizard"
  </behavior>
  <action>
    Append three REQUIRED fields INSIDE the existing `CharacterSnapshotSchema` `z.strictObject({...})`, positioned after the `skills:` field and before the optional `portrait` field (mirror the Phase 16/17 insertion point exactly). Add `class: z.string()`, `initiative: z.number().int()`, `speed: z.number().int().nonnegative()`. Give each field a full TSDoc block per INV-4: class = display name(s), multiclass joined "Fighter / Wizard", note that `level` carries the numeric level separately; initiative = dnd5e prep-time computed total modifier, may be negative, reader path `actor.system.attributes.init.total`; speed = walking speed in feet, reader path `actor.system.attributes.movement.walk`, other movement modes deferred. Do NOT use `.optional()` — these are REQUIRED per the locked decision (D from CONTEXT.md: class/initiative/speed REQUIRED). Add the matching schema-validation tests in character.test.ts following the existing test naming.
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-protocol test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @evf/shared-protocol test -- --run` passes including the new field tests
    - `CharacterSnapshotSchema` rejects a literal missing any of class/initiative/speed
    - TSDoc present on all 3 fields (INV-4)
  </acceptance_criteria>
  <done>Schema carries 3 REQUIRED fields with tests + TSDoc; shared-protocol suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add foundry-module readers + d.ts types + getCharacterSnapshot wiring (RDATA-01, RDATA-02)</name>
  <files>packages/foundry-module/src/readers/character-reader.ts, packages/foundry-module/src/readers/readers.test.ts, packages/foundry-module/src/types/foundry-globals.d.ts</files>
  <read_first>
    - packages/foundry-module/src/readers/character-reader.ts (`extractAbilities` ~line 348 + `extractSkills` + `getCharacterSnapshot` return object — copy the guard+default reader pattern and the wiring insertion point after `skills:`)
    - packages/foundry-module/src/readers/readers.test.ts (`makeActor` mock factory — extend it with `init`/`movement`/class items, then add CR-CLS/CR-INI/CR-SPD tests mirroring existing reader tests)
    - packages/foundry-module/src/types/foundry-globals.d.ts (`Dnd5eAttributes` interface — add `init`/`movement` optional fields)
    - 21-PATTERNS.md §`character-reader.ts` + §`foundry-globals.d.ts` (exact reader bodies)
    - 21-RESEARCH.md §Pattern 1 (dnd5e paths + INV-2 note)
  </read_first>
  <behavior>
    - Test (CR-CLS): mock actor with two `type:'class'` items "Fighter"+"Wizard" → extractClass returns "Fighter / Wizard"; no class items → ""; undefined actor → ""
    - Test (CR-INI): actor.system.attributes.init.total = 3 → extractInitiativeModifier returns 3; missing → 0; negative -1 → -1; undefined actor → 0
    - Test (CR-SPD): movement.walk = 25 → extractWalkSpeed returns 25; missing → 30 (D&D default); negative → 30; undefined actor → 30
    - Test: getCharacterSnapshot output includes class/initiative/speed and parses against CharacterSnapshotSchema
  </behavior>
  <action>
    Add `init?: { total?: number }` and `movement?: { walk?: number; fly?: number; swim?: number; climb?: number }` to the `Dnd5eAttributes` interface in foundry-globals.d.ts (same optional-field style as existing `hp`/`ac`/`death`), each with a one-line TSDoc tagged Phase 21. In character-reader.ts add three `@internal` reader functions mirroring `extractAbilities`'s guard+nullish-coalesce shape: `extractClass(actor)` filters `actor.items?.contents ?? []` to `item.type === 'class'`, maps `.name`, filters non-empty, joins with " / " (returns "" on undefined actor / no class items); `extractInitiativeModifier(actor)` returns `actor.system?.attributes?.init?.total ?? 0` (defensive `?? 0` per resolved decision — INV-2 safety net since init.total is the standard dnd5e path); `extractWalkSpeed(actor)` returns `movement.walk` when it is a non-negative number else `30`. Wire all three into the `getCharacterSnapshot` return object, inserting `class`/`initiative`/`speed` after `skills:` and before the portrait spread (exact location per PATTERNS analog). Extend `makeActor` in readers.test.ts to carry `init`/`movement`/class-items so the new tests and ALL existing reader tests still pass, then add CR-CLS/CR-INI/CR-SPD test blocks. Full TSDoc on each reader per INV-4.
  </action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run --testNamePattern 'CR-CLS|CR-INI|CR-SPD'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @evf/foundry-module test -- --run` passes (new CR-* tests + all existing reader tests)
    - getCharacterSnapshot output parses against the extended CharacterSnapshotSchema
    - Defensive defaults verified by test (undefined/missing paths → 0/30/"")
  </acceptance_criteria>
  <done>3 readers + d.ts types + wiring landed; foundry-module suite green with CR-* coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Update all downstream CharacterSnapshot literals (~27 files) — keep workspace GREEN</name>
  <files>packages/bridge/src/**/*.test.ts, packages/foundry-mcp/src/**/*.test.ts, packages/g2-app/src/**/*.{ts,test.ts}, packages/shared-protocol/src/**/*.test.ts</files>
  <read_first>
    - 21-RESEARCH.md §Pitfall 6 (the blast-radius playbook: schema first, then `pnpm typecheck` surfaces every literal error, then fix mechanically)
    - One existing literal site for the canonical value convention, e.g. packages/bridge/src/ws/handshake-actor.test.ts (use representative values: `class: 'Fighter'`, `initiative: 2`, `speed: 30` — for the Thorin fixture actor use `class: 'Fighter'`, `initiative: 2`, `speed: 25` to match the glyph fixtures updated in Plan 21-05)
  </read_first>
  <action>
    Run `pnpm typecheck` after Tasks 1+2 land to surface the full list of `CharacterSnapshot` literals missing the 3 new REQUIRED fields. For each failing literal, add `class`, `initiative`, `speed` with representative values consistent with that literal's existing semantics (a Fighter test actor: `class: 'Fighter', initiative: 2, speed: 30`; the Thorin Oakenshield sheet fixture actor: `class: 'Fighter', initiative: 2, speed: 25` so it stays consistent with the row-6 vitals values Plan 21-05 will bake into sheet.main.*.txt). This is mechanical, not creative — do NOT change any other field. If a shared snapshot factory exists (`makeSnapshot`/`VALID_SNAPSHOT`), update the factory FIRST (single edit propagates) before touching per-test literals. Keep `boot-hud-raster-poc.ts` and any non-test `.ts` literal producers updated too. Run `pnpm test -- --run` to confirm GREEN before completing.
  </action>
  <verify>
    <automated>pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0 (no missing-field errors)
    - `pnpm test -- --run` GREEN workspace-wide (3180+ tests)
    - No field other than class/initiative/speed altered in any literal
  </acceptance_criteria>
  <done>Every downstream CharacterSnapshot literal carries the 3 fields; workspace test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry actor → reader | dnd5e actor system data is read defensively (nullish-coalesce) |
| reader → snapshot wire | getCharacterSnapshot output is the wire payload validated by CharacterSnapshotSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-DATA-01 | Tampering | reader reading malformed/missing dnd5e paths | mitigate | defensive `?? 0`/`?? 30`/`""` defaults in every reader (no crash on slightly-wrong path; INV-2 safety net) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | no new packages installed this plan (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `pnpm --filter @evf/shared-protocol test -- --run` green (schema field tests)
- `pnpm --filter @evf/foundry-module test -- --run` green (CR-CLS/CR-INI/CR-SPD)
- `pnpm typecheck` exits 0
- `pnpm test -- --run` GREEN workspace-wide
- `pnpm lint:ci` exits 0
- CI Gate 8 socketlib handler count unchanged (read-path-only; no new handlers)
</verification>

<success_criteria>
CharacterSnapshotSchema carries REQUIRED class/initiative/speed; 3 foundry-module readers
emit them with defensive defaults; getCharacterSnapshot wired; all ~27 downstream literals
updated; workspace test suite + typecheck + lint all green; socketlib count = 17.
</success_criteria>

<output>
Create `.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-01-SUMMARY.md` when done
</output>
