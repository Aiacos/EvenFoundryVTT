---
phase: 21-character-sheet-su-canvas-dati-main-tab
plan: 05
type: execute
wave: 4
depends_on: ["21-01", "21-03", "21-04"]
files_modified:
  - packages/shared-render/src/fixtures/sheet.main.2014.it.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.en.txt
  - packages/shared-render/src/fixtures/sheet.main.2014.de.txt
  - packages/shared-render/src/fixtures/sheet.main.2024.it.txt
  - packages/shared-render/src/fixtures/canvas-sheet-panel.raster-hash.json
  - packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts
autonomous: true
requirements: [RSHEET-01, RDATA-01, RDATA-02]
must_haves:
  truths:
    - "The 4 sheet.main.*.txt glyph fixtures row-6 vitals bar shows real initiative + speed (INI +2 / VEL 25 for the Thorin Fighter fixture) instead of the — placeholders, updated ATOMICALLY with the renderer that produces them, preserving INV-1 column alignment"
    - "A new canvas-sheet-panel.raster-hash.json fixture records SHA-256 of the 4 HUD tile PNGs from a canonical synthetic RGBA (same Phase 20 RINV-01 pipeline), and RCSP-INV1 asserts the hashes match"
    - "inv:all (glyph + raster suites) and the full workspace test suite are GREEN; the row-6 fixture values are consistent with the snapshot literal values from Plan 21-01"
  artifacts:
    - path: "packages/shared-render/src/fixtures/canvas-sheet-panel.raster-hash.json"
      provides: "raster INV-1 hash fixture for the canvas sheet panel"
      contains: "sha256"
    - path: "packages/shared-render/src/fixtures/sheet.main.2014.it.txt"
      provides: "row-6 vitals bar with real INI/VEL"
      contains: "INI"
  key_links:
    - from: "RCSP-INV1 test"
      to: "canvas-sheet-panel.raster-hash.json"
      via: "SHA-256 compare of buildHudTiles output"
      pattern: "raster-hash"
---

<objective>
Close Phase 21's INV-1 contract: update the 4 `sheet.main.*.txt` glyph fixtures' row-6 vitals
bar (INI/VEL go from `—` to real values) ATOMICALLY with the renderer that emits them, create
the new `canvas-sheet-panel.raster-hash.json` raster fixture (SHA-256 of the 4 HUD tile PNGs
from a canonical synthetic RGBA, same Phase 20 RINV-01 pipeline) with the RCSP-INV1 assertion,
and run the full regression (inv:all + workspace) green.

Purpose: RSHEET-01 (raster sheet INV-1 contract) + RDATA-01/RDATA-02 (real class/ini/speed
surfaced in the glyph Main tab fixtures). This is the INV-1 load-bearing closure per CLAUDE.md.
Output: 4 updated glyph fixtures + 1 new raster hash fixture + RCSP-INV1 + green inv:all.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-RESEARCH.md
@.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-PATTERNS.md
@packages/shared-render/src/fixtures/sheet.main.2014.it.txt
@packages/shared-render/src/fixtures/status-hud.raster-hash.json
</context>

<artifacts_this_phase_produces>
This plan introduces / updates these artifacts:
- `packages/shared-render/src/fixtures/canvas-sheet-panel.raster-hash.json` (NEW — raster INV-1 hash fixture)
- 4 updated glyph fixtures: `sheet.main.{2014.it,2014.en,2014.de,2024.it}.txt` (row-6 vitals bar real INI/VEL)
- RCSP-INV1 test in canvas-character-sheet-panel.test.ts
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update glyph Main tab renderer + 4 sheet.main.*.txt fixtures row-6 (atomic, INV-1)</name>
  <files>packages/shared-render/src/fixtures/sheet.main.2014.it.txt, packages/shared-render/src/fixtures/sheet.main.2014.en.txt, packages/shared-render/src/fixtures/sheet.main.2014.de.txt, packages/shared-render/src/fixtures/sheet.main.2024.it.txt</files>
  <read_first>
    - packages/shared-render/src/fixtures/sheet.main.2014.it.txt (current row 6 vitals bar: `⛨ CA 18    ⚡ INI —    ⚔ VEL —    COMP +3` — count the exact column positions; INV-1 alignment is character-perfect)
    - packages/g2-app/src/panels/character-sheet-tab-renderers.ts (the glyph `renderMainTab` row-6 construction — find where `INI —` and `VEL —` are emitted; this is where the `—` placeholder becomes `formatAbilityMod(snapshot.initiative)` / `snapshot.speed`)
    - 21-RESEARCH.md §Pattern 5 (Thorin Fighter fixture values initiative +2, speed 25; row-6-only change; possible row-1 class surfacing)
    - 21-PATTERNS.md §`sheet.main.*.txt` (the exact before/after row-6 strings)
  </read_first>
  <action>
    Update the glyph `renderMainTab` so row 6 emits real `snapshot.initiative` (signed, reusing the existing `formatAbilityMod` helper) and real `snapshot.speed` instead of the hardcoded `—` placeholders — this is additive to / consistent with the Plan 21-03 canvas renderer, NOT a separate code path; the glyph string renderer stays the BLE fallback. Then update the 4 fixtures' row 6 to match the renderer output for the Thorin Oakenshield Fighter Lv 8 fixture snapshot (`initiative: 2` → `INI +2`, `speed: 25` → `VEL 25`), preserving EXACT column alignment (INV-1: frame corners, dividers, columns identical from top to bottom — width-budget the new tokens, do not let alignment slip). If the locked decision to surface class on the Main tab requires a row-1 change in the glyph fixture (class name on the identity line), apply it to all 4 fixtures consistently; otherwise leave row 1 as `Lv 8`. Keep the fixture snapshot values byte-consistent with the Plan 21-01 downstream literal values for the Thorin actor. Run the shared-render INV-1 suite to confirm alignment + content match.
  </action>
  <verify>
    <automated>pnpm --filter @evf/shared-render test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - 4 fixtures row 6 show `INI +2` / `VEL 25` (no `—`); column alignment INV-1-preserved
    - `pnpm --filter @evf/shared-render test -- --run` green (renderer output == fixtures)
    - row-6 values consistent with Plan 21-01 Thorin literal (initiative 2, speed 25)
  </acceptance_criteria>
  <done>Glyph Main tab renderer + 4 fixtures show real INI/VEL atomically; shared-render INV-1 green.</done>
</task>

<task type="auto">
  <name>Task 2: Create canvas-sheet-panel.raster-hash.json + RCSP-INV1 raster contract</name>
  <files>packages/shared-render/src/fixtures/canvas-sheet-panel.raster-hash.json, packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts</files>
  <read_first>
    - packages/shared-render/src/fixtures/status-hud.raster-hash.json (exact JSON shape to mirror: version, description, tiles[] of {index, containerName 'hud-tile-N', sha256})
    - packages/g2-app/src/__tests__/20-raster-inv1.test.ts (the canonical synthetic-RGBA → buildHudTiles → SHA-256 first-run-writes / subsequent-compare pattern + the canonical RGBA generator — REUSE it, do not invent a new non-deterministic source)
    - packages/g2-app/src/hud/hud-raster-frame.js exports (`buildHudTiles`)
    - 21-PATTERNS.md §`canvas-sheet-panel.raster-hash.json` + §`canvas-character-sheet-panel.test.ts` (raster hash fixture pattern) + 21-RESEARCH.md §Anti-Patterns (do NOT hash canvas-rendered text — non-deterministic in happy-dom; use the synthetic RGBA pipeline)
  </read_first>
  <action>
    Add an RCSP-INV1 test to canvas-character-sheet-panel.test.ts mirroring 20-raster-inv1.test.ts: build the 4 HUD tile PNGs from the SAME canonical synthetic RGBA generator (reuse, do not duplicate a new generator), SHA-256 each tile, and compare against `packages/shared-render/src/fixtures/canvas-sheet-panel.raster-hash.json`. Use the first-run-writes-fixture / subsequent-runs-compare branch (existsSync) exactly like Phase 20. Run the test once to GENERATE the fixture, then commit the generated JSON as the baseline (4 tiles, containerName 'hud-tile-0..3'). Do NOT hash canvas-rendered text. Ensure the FALSE-PASS guard pattern from Phase 20 (the test must fail if the fixture is deleted/empty) carries over.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run canvas-character-sheet-panel</automated>
  </verify>
  <acceptance_criteria>
    - canvas-sheet-panel.raster-hash.json exists with 4 tile SHA-256 entries
    - RCSP-INV1 green on a second run (compare branch); fails if fixture removed (FALSE-PASS guard)
    - synthetic RGBA generator reused from Phase 20 (no new non-deterministic source)
  </acceptance_criteria>
  <done>Raster INV-1 hash fixture committed; RCSP-INV1 compares + FALSE-PASS-guards; g2-app green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-phase regression (inv:all + workspace) green</name>
  <files>packages/g2-app/src/panels/__tests__/canvas-character-sheet-panel.test.ts</files>
  <read_first>
    - 21-VALIDATION.md §Sampling Rate (phase gate: full suite + inv:all:skip-inv2 before verify)
    - 21-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map; the full RCSP + CR + INV-1 command set)
  </read_first>
  <action>
    Run the full Phase 21 regression and fix any fallout: `pnpm test -- --run` (workspace), `pnpm --filter @evf/validation-harness inv:all:skip-inv2` (both glyph + raster suites must be green), `pnpm typecheck`, `pnpm lint:ci`. Confirm the CI Gate 8 socketlib handler count is still 17 (no new handlers this phase — read-path + render only). If any RCSP/CR/INV-1 test is red, repair within this plan's files (do NOT alter panel-gesture-bus.ts). Add any missing cross-cutting regression assertion (e.g. that opening the canvas sheet in canvas mode then closing via the router double-tap path leaves slot 3 cleared) if a gap surfaces. Do NOT update Specs.md/README/showcase here — INV-3 doc coherence is the Phase 26 milestone-close artifact per the roadmap.
  </action>
  <verify>
    <automated>pnpm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- --run` GREEN workspace-wide
    - `pnpm --filter @evf/validation-harness inv:all:skip-inv2` green (glyph + raster suites)
    - `pnpm typecheck` + `pnpm lint:ci` exit 0
    - socketlib handler count = 17 unchanged
    - panel-gesture-bus.ts unchanged
  </acceptance_criteria>
  <done>Full Phase 21 regression green; inv:all green on both suites; socketlib=17; gesture-bus untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic RGBA → buildHudTiles | test-only deterministic input; no untrusted crossing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-INV1 | Tampering | a renderer change silently breaks INV-1 alignment | mitigate | shared-render INV-1 suite asserts char-perfect fixtures; RCSP-INV1 SHA-256 + FALSE-PASS guard |
| T-21-SC | Tampering | npm installs | accept | no new packages this plan |
</threat_model>

<verification>
- `pnpm --filter @evf/shared-render test -- --run` green (glyph fixtures)
- `pnpm --filter @evf/g2-app test -- --run` green (RCSP-INV1)
- `pnpm --filter @evf/validation-harness inv:all:skip-inv2` green (glyph + raster)
- `pnpm test -- --run` + `pnpm typecheck` + `pnpm lint:ci` all green
- CI Gate 8 socketlib handler count = 17 unchanged
</verification>

<success_criteria>
4 glyph fixtures row-6 show real INI/VEL atomically with the renderer; new raster hash fixture +
RCSP-INV1 with FALSE-PASS guard; inv:all (glyph + raster) + full workspace suite + typecheck +
lint all green; socketlib=17; panel-gesture-bus unchanged.
</success_criteria>

<output>
Create `.planning/phases/EVF-21-character-sheet-su-canvas-dati-main-tab/21-05-SUMMARY.md` when done
</output>
