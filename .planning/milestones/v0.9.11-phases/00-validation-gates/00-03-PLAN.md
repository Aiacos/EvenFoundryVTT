---
phase: 00-validation-gates
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - tests/phase-0/10-0-1-r1-timing.ts
  - tests/phase-0/10-0-2-image-format.ts
  - tests/phase-0/10-0-3-ble-multi-env.ts
  - tests/phase-0/10-0-7-dle-sustained.ts
  - tests/phase-0/10-0-8-queue-depth.ts
  - tests/phase-0/10-0-9-palette-calibration.ts
autonomous: true
requirements: []
gap_closure: false

must_haves:
  truths:
    - "6 hardware test scripts exist at tests/phase-0/10-0-*.ts"
    - "Each script implementa la struttura: load Hub via _shared/hub.ts → measure → derive verdict via _shared/branch-decision.ts → write evidence via _shared/output.ts"
    - "Each script handles Hub-unavailable case emettendo verdict='skipped' evidence (Pattern 3 capability-negotiation skip)"
    - "10-0-1-r1-timing.ts: n=150 per gesture × 6 gestures (tap/double-tap/scroll-up/scroll-down/long-press-1s/long-press-2s) per session × 5 sessions; computes Hartigan dip test for tap/double-tap distinguishability"
    - "10-0-2-image-format.ts: Genera 3 candidate format payloads (PNG indexed via upng-js / raw 4-bit BE / raw 4-bit LE), invia ognuno al G2, capture researcher visual verdict via CLI prompt"
    - "10-0-3-ble-multi-env.ts: parameterized via RF_ENV env var ∈ {clean, 5ghz-loaded, 2-4ghz-microwave}; 30-min sustained tile pushes; computes p50/p95/p99 + renegotiation events; calls deriveBranch() per environment"
    - "10-0-7-dle-sustained.ts: 30-min sustained DLE throughput with 50-byte heartbeat ping every 2 sec for inferred-MTU history (Pitfall 10 mitigation 2)"
    - "10-0-8-queue-depth.ts: pushes 8 tiles back-to-back, counts coalesced/dropped, derives table {1,2,3,≥4} via deriveQueueDepthTier()"
    - "10-0-9-palette-calibration.ts: renders 16-step uniform ramp on G2; researcher photographs; ≤3 iteration loop deriving perceptual palette via inverse CIE L* mapping; verifies spacing within ±10%"
    - "All scripts reference Specs §10.0.X canonical SoT in header comment + cite RESEARCH.md pitfall in pre-committed THRESHOLDS const"
    - "All scripts type-check under TS strict + noUncheckedIndexedAccess"
    - "upng-js is the ONLY external runtime dep added (for 10-0-2 Format A generation); other tests use pure stdlib + _shared utilities"
  artifacts:
    - path: "tests/phase-0/10-0-1-r1-timing.ts"
      provides: "R1 gesture timing window measurement (Specs §10.0.1)"
      contains: "tap_max_ms"
    - path: "tests/phase-0/10-0-2-image-format.ts"
      provides: "updateImageRawData byte format probe (Specs §10.0.2 + Pitfall 7)"
      contains: "upng-js"
    - path: "tests/phase-0/10-0-3-ble-multi-env.ts"
      provides: "BLE bandwidth multi-env multi-percentile (Specs §10.0.3 + D-09)"
      contains: "RF_ENV"
    - path: "tests/phase-0/10-0-7-dle-sustained.ts"
      provides: "DLE 30-min sustained with renegotiation event capture (Specs §10.0.7 + Pitfall 10)"
      contains: "30 * 60 * 1000"
    - path: "tests/phase-0/10-0-8-queue-depth.ts"
      provides: "Queue depth empirical {1,2,3,≥4} table (Specs §10.0.8 + D-10)"
      contains: "deriveQueueDepthTier"
    - path: "tests/phase-0/10-0-9-palette-calibration.ts"
      provides: "Palette calibration luminance ramp + inverse CIE L* (Pitfall 15)"
      contains: "L*"
  key_links:
    - from: "tests/phase-0/10-0-3-ble-multi-env.ts"
      to: "tests/phase-0/_shared/branch-decision.ts via deriveBranch + DEFAULT_THRESHOLDS"
      via: "import statement"
      pattern: "deriveBranch.*DEFAULT_THRESHOLDS"
    - from: "tests/phase-0/10-0-8-queue-depth.ts"
      to: "tests/phase-0/_shared/branch-decision.ts via deriveQueueDepthTier"
      via: "import statement"
      pattern: "deriveQueueDepthTier"
    - from: "tests/phase-0/10-0-1-r1-timing.ts"
      to: "tests/phase-0/_shared/stats.ts via percentile + hartiganDipTest + ci95"
      via: "import statement"
      pattern: "percentile.*hartiganDipTest"
    - from: "tests/phase-0/10-0-*.ts (all 6)"
      to: "tests/phase-0/_shared/hub.ts via loadHub"
      via: "import statement + Pattern 3 skip if !available"
      pattern: "loadHub"
    - from: "tests/phase-0/10-0-*.ts (all 6)"
      to: "tests/phase-0/_shared/output.ts via writeJsonEvidence + writeCsvEvidence"
      via: "import statement"
      pattern: "writeJsonEvidence"
---

<threat_model>
**ASVS L1. Block on: high.**

| ID | Severity | Threat | Mitigation in this plan |
|----|----------|--------|--------------------------|
| T-00-03 | Low | Test harness scripts inadvertently expose research data — BLE measurements with environment metadata that could fingerprint developer's home network (SSID, BSSID, channel, neighbor APs) | `RF_ENV` env var is constrained to enum `{clean, 5ghz-loaded, 2-4ghz-microwave}` (Zod-validated in `_shared/schemas.ts`) — no SSID/MAC/BSSID propagated to evidence. Scripts capture ONLY application-observable throughput timestamps via SDK callbacks (no link-layer detail per RESEARCH §"Don't Hand-Roll"). DSL: never call `navigator.connection.*`, never read `Wi-Fi info` plugin APIs. T-00-03 mitigation verifiable: grep `SSID\|BSSID\|getNetworkInfo\|navigator.connection` across all 6 test files MUST return zero hits. |
| T-00-01 | Medium | Bearer tokens leaked in test output / committed evidence files | All 6 scripts call `loadHub({ required: false })` from `_shared/hub.ts` (Plan 01) — credential read pattern uses `process.env.EVEN_HUB_TOKEN` only. Evidence payloads conform to `*Result` Zod schemas (Plan 01) which contain NO auth fields. Scripts never log raw `bridge` reference (would dump SDK internal state). |
| T-00-04 | Low | Even Hub bearer (post-grant) committed to git | Inherited Plan 01 mitigation: `tests/phase-0/.gitignore` excludes `.env*`; `_shared/hub.ts` env-only credential. This plan adds NO new credential surfaces. |

No high-severity threats. Block on: high → none flagged.

**Note:** This plan creates **harness scripts**, not measurements. Threat surface for the actual 30-min BLE captures + photographs lands in Plan 04 (execution). Researcher operational protocol there: dim ambient + locked exposure + scrub network metadata before commit (already documented in `docs/perf/phase-0/calibration/methodology.md` from Plan 01).
</threat_model>

<objective>
Implementa i **6 hardware-bound test scripts** (Specs §10.0.1, §10.0.2, §10.0.3, §10.0.7, §10.0.8, §10.0.9) come TypeScript COMPLETI ed eseguibili — type-check verde, importano da `_shared/`, gestiscono il caso Hub-unavailable con verdict="skipped" evidence (Pattern 3), e quando hardware diventa disponibile (post Even Hub grant) sono pronti all'esecuzione senza modifiche di codice.

**Costruzione autonoma; esecuzione gated.** Questa pianificazione produce gli script COMPLETI e type-clean. L'esecuzione effettiva (researcher fisico + hardware paired + 3 RF environment setup + camera capture) avviene in Plan 04 fase di chiusura. La separazione costruzione/esecuzione è la chiave per l'autonomia di questo plan: nessun child Claude executor agent ha bisogno di hardware per completare i tasks qui descritti.

**Purpose:** Implementare il pre-grant scaffold (CONTEXT.md D-01) — quando l'Even Hub developer access arriva, gli script possono essere lanciati immediatamente senza ulteriore implementation work. Ogni script ha THRESHOLDS pre-committed top-level (D-12 strict numeric), Zod schema validation, e capability-negotiation skip pattern (Pattern 3 di RESEARCH.md).

**Output:** 6 file TypeScript completi, type-check verde sotto TS strict + noUncheckedIndexedAccess. Ogni file COMPLETO non stub: implementa la misurazione, deriva il verdict, scrive evidence. La parte "fisica" (gesture R1, photo G2, RF env setup) è capturata via prompt CLI / env var / file path conventions; il codice TS è 100%.
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/00-validation-gates/00-CONTEXT.md
@.planning/phases/00-validation-gates/00-RESEARCH.md
@.planning/phases/00-validation-gates/00-VALIDATION.md
@.planning/phases/00-validation-gates/00-01-PLAN.md
@CLAUDE.md
@tests/phase-0/_shared/schemas.ts
@tests/phase-0/_shared/output.ts
@tests/phase-0/_shared/stats.ts
@tests/phase-0/_shared/branch-decision.ts
@tests/phase-0/_shared/hub.ts

<interfaces>
<!-- Imports from Plan 01 _shared utilities — contracts are stable. -->

From _shared/schemas.ts (Plan 01 fixed):
```typescript
export type BleMultiEnvResult = z.infer<typeof BleMultiEnvResult>;
export type DleSustainedResult = z.infer<typeof DleSustainedResult>;
export type QueueDepthResult = z.infer<typeof QueueDepthResult>;
export type ImageFormatResult = z.infer<typeof ImageFormatResult>;
export type R1TimingResult = z.infer<typeof R1TimingResult>;
export type PaletteCalibrationResult = z.infer<typeof PaletteCalibrationResult>;
```

From _shared/output.ts (Plan 01 fixed):
```typescript
export async function writeJsonEvidence(payload: AnyResult): Promise<string>;
export async function writeCsvEvidence(payload: AnyResult): Promise<string | null>;
```

From _shared/stats.ts (Plan 01 fixed):
```typescript
export function percentile(samples: number[], p: number): number;
export function hartiganDipTest(samples: number[]): { dip: number; pValue: number };
export function ci95(samples: number[]): { mean: number; lower: number; upper: number };
```

From _shared/branch-decision.ts (Plan 01 fixed):
```typescript
export function deriveBranch(envs: EnvResult[], t?: Thresholds): BranchVerdict;
export function deriveQueueDepthTier(measured: number): { tier: "A" | "B" | "C"; rationale: string };
export const DEFAULT_THRESHOLDS: Thresholds;
```

From _shared/hub.ts (Plan 01 fixed):
```typescript
export type HubBridge = {
  createImageContainer(opts: { id: string; x: number; y: number; w: number; h: number }): Promise<void>;
  updateImageRawData(id: string, data: Uint8Array): Promise<void>;
  createTextContainer(opts: { id: string; text: string; x: number; y: number }): Promise<void>;
  onR1Event(handler: (ev: { type: string; t_ms: number; raw: unknown }) => void): () => void;
};
export async function loadHub(opts?: { required?: boolean }): Promise<HubLoadResult>;
```

External lib added in this plan:
- `upng-js@2.1.0` — for 10-0-2-image-format.ts Format A (PNG indexed 4-bit) generation. CLAUDE.md §11.5.7 confirms version pin. Add to `tests/phase-0/package.json` devDependencies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add upng-js + types to package.json + 3 lighter hardware scripts (10-0-7 DLE, 10-0-8 queue depth, 10-0-9 palette calibration)</name>
  <files>
    tests/phase-0/package.json
    tests/phase-0/10-0-7-dle-sustained.ts
    tests/phase-0/10-0-8-queue-depth.ts
    tests/phase-0/10-0-9-palette-calibration.ts
  </files>
  <read_first>
    - .planning/phases/00-validation-gates/00-RESEARCH.md (Code Example 3 pattern for BLE-style measurement skeleton; §"Don't Hand-Roll" — application-observable throughput via SDK callback timestamps; Pitfall 10 mitigation 2 — 50-byte heartbeat every 2 sec; Pitfall 12 — queue depth full table; Pitfall 15 mitigation — sRGB linearize before luma + perceptual palette via CIE L*)
    - .planning/phases/00-validation-gates/00-CONTEXT.md (D-09 BLE thresholds + D-10 queue tier mapping + D-11 fps thresholds + D-12 strict numeric — Pattern 1 pre-committed THRESHOLDS at top of each test)
    - tests/phase-0/_shared/schemas.ts (Plan 01 — exact Zod schemas for DleSustainedResult, QueueDepthResult, PaletteCalibrationResult)
    - tests/phase-0/_shared/branch-decision.ts (Plan 01 — deriveQueueDepthTier signature)
    - tests/phase-0/_shared/hub.ts (Plan 01 — loadHub + HubBridge interface)
    - tests/phase-0/_shared/stats.ts (Plan 01 — percentile signature)
    - CLAUDE.md (§11.5.7 — image-q + upng-js + xxhash-wasm pinned versions; §10.0.7 30-min not 30-sec per CONTEXT.md extension)
    - tests/phase-0/package.json (Plan 01 — current devDeps to know what's already installed)
  </read_first>
  <action>
**Step 1.1 — Update `tests/phase-0/package.json`** to add `upng-js@2.1.0` (Task 2 needs it for 10-0-2; add now so type-check works):

Read current `tests/phase-0/package.json`, then update `devDependencies` to add:

```json
"upng-js": "2.1.0"
```

The full devDependencies block becomes:

```json
"devDependencies": {
  "@types/node": "25.6.2",
  "csv-stringify": "6.5.2",
  "tsx": "4.21.0",
  "typescript": "5.8.5",
  "upng-js": "2.1.0",
  "zod": "4.4.3"
}
```

After updating, run `cd tests/phase-0 && pnpm install` so the new dep lands.

**Note on `upng-js` types:** The package ships its own ambient module declaration in some versions; if TS can't resolve, add a top-level `declare module 'upng-js'` in 10-0-2-image-format.ts as a fallback. CLAUDE.md §11.5.7 confirms upng-js@2.1.0 is the pinned version (no caret/tilde).

**Step 1.2 — Create `tests/phase-0/10-0-7-dle-sustained.ts`** — DLE 30-min sustained throughput with renegotiation event capture (Pitfall 10):

```typescript
// Specs §10.0.7 + Pitfall 10 — DLE detected at connect, can degrade silently mid-session.
// CONTEXT.md extends from 30-sec to 30-min sustained per research adjustment.
//
// Protocol:
//   - Push 4 KB tile every 100 ms for 30 min
//   - Every 2 sec, send a 50-byte heartbeat ping (Pitfall 10 mitigation 2 — inferred-MTU canary)
//   - Capture every observed throughput dip ≥50% relative to first-minute baseline as renegotiation event
//   - Compute p50/p99 sustained; verdict: pass if both meet 5fps floor (~80 KB/s = 640 kbps), else fail
//
// Skip case: Hub unavailable → write skipped evidence + exit 2.

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence, writeCsvEvidence } from "./_shared/output.js";
import { percentile } from "./_shared/stats.js";
import { DleSustainedResult } from "./_shared/schemas.js";

// THRESHOLDS pre-committed (D-12 strict numeric).
const THRESHOLDS = {
  duration_ms: 30 * 60 * 1000,
  tile_size_bytes: 4096,
  tile_interval_ms: 100,
  heartbeat_size_bytes: 50,
  heartbeat_interval_ms: 2000,
  renegotiation_drop_threshold_pct: 50,
  baseline_window_samples: 60, // first 60 samples = first minute baseline
  pass_p99_kbps: 100, // align with D-09 Branch B floor (Pitfall 10 mitigation envelope)
} as const;

function sleep(ms: number): Promise<void> {
  return new Promise((r) => setTimeout(r, ms));
}

async function main(): Promise<void> {
  console.log("Specs §10.0.7 — DLE 30-min sustained throughput");
  console.log("================================================");
  console.log(`Duration: ${THRESHOLDS.duration_ms / 60_000} min`);
  console.log(`Tile: ${THRESHOLDS.tile_size_bytes} bytes every ${THRESHOLDS.tile_interval_ms} ms`);
  console.log(`Heartbeat: ${THRESHOLDS.heartbeat_size_bytes} bytes every ${THRESHOLDS.heartbeat_interval_ms} ms`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = DleSustainedResult.parse({
      schema_version: 1,
      test_id: "10-0-7-dle-sustained",
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      duration_sec: 0,
      initial_mtu_bytes: 0,
      inferred_mtu_history: [],
      renegotiation_events: [],
      sustained_kbps_p50: 0,
      sustained_kbps_p99: 0,
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  await hub.bridge.createImageContainer({ id: "dle-test", x: 0, y: 0, w: 200, h: 100 });
  const tile = new Uint8Array(THRESHOLDS.tile_size_bytes);
  for (let i = 0; i < tile.length; i++) tile[i] = i & 0xff;
  const heartbeat = new Uint8Array(THRESHOLDS.heartbeat_size_bytes);

  const samples: number[] = [];
  const inferredMtuHistory: Array<{ t_sec: number; inferred_mtu_bytes: number }> = [];
  const renegotiationEvents: Array<{ t_sec: number; p_drop_pct: number }> = [];
  let baselineKbps = 0;
  let lastHeartbeatAt = 0;

  const t0 = performance.now();
  while (performance.now() - t0 < THRESHOLDS.duration_ms) {
    const tStart = performance.now();
    await hub.bridge.updateImageRawData("dle-test", tile);
    const tEnd = performance.now();
    const elapsedMs = Math.max(0.001, tEnd - tStart);
    const kbps = (THRESHOLDS.tile_size_bytes * 8) / (elapsedMs / 1000) / 1000;
    samples.push(kbps);

    if (samples.length === THRESHOLDS.baseline_window_samples) {
      baselineKbps = percentile(samples, 50);
      console.log(`Baseline established: p50=${baselineKbps.toFixed(1)} kbps`);
    }
    if (baselineKbps > 0 && kbps < baselineKbps * (1 - THRESHOLDS.renegotiation_drop_threshold_pct / 100)) {
      renegotiationEvents.push({
        t_sec: (tEnd - t0) / 1000,
        p_drop_pct: 100 - (kbps / baselineKbps) * 100,
      });
    }

    // Heartbeat (inferred-MTU canary — Pitfall 10 mitigation 2)
    if (tEnd - lastHeartbeatAt >= THRESHOLDS.heartbeat_interval_ms) {
      const hbStart = performance.now();
      await hub.bridge.updateImageRawData("dle-test", heartbeat);
      const hbMs = Math.max(0.001, performance.now() - hbStart);
      // Inferred-MTU heuristic: if heartbeat round-trip exceeds 4× tile-size-normalized expectation, MTU may have dropped.
      const inferredMtu = hbMs < 50 ? 247 : 23;
      inferredMtuHistory.push({ t_sec: (performance.now() - t0) / 1000, inferred_mtu_bytes: inferredMtu });
      lastHeartbeatAt = tEnd;
    }

    const slack = THRESHOLDS.tile_interval_ms - (performance.now() - tStart);
    if (slack > 0) await sleep(slack);
  }

  const p50 = percentile(samples, 50);
  const p99 = percentile(samples, 99);
  const initialMtu = inferredMtuHistory[0]?.inferred_mtu_bytes ?? 247;

  const verdict = p99 >= THRESHOLDS.pass_p99_kbps ? "pass" : "fail";
  const rationale = verdict === "pass"
    ? `30-min sustained: p50=${p50.toFixed(1)} kbps, p99=${p99.toFixed(1)} kbps; ${renegotiationEvents.length} renegotiation event(s) captured`
    : `p99=${p99.toFixed(1)} kbps below pass threshold ${THRESHOLDS.pass_p99_kbps} kbps; ${renegotiationEvents.length} renegotiation event(s)`;

  const result = DleSustainedResult.parse({
    schema_version: 1,
    test_id: "10-0-7-dle-sustained",
    timestamp: new Date().toISOString(),
    verdict,
    rationale,
    duration_sec: THRESHOLDS.duration_ms / 1000,
    initial_mtu_bytes: initialMtu,
    inferred_mtu_history: inferredMtuHistory,
    renegotiation_events: renegotiationEvents,
    sustained_kbps_p50: p50,
    sustained_kbps_p99: p99,
  });
  const fpath = await writeJsonEvidence(result);
  await writeCsvEvidence({ ...result, samples_kbps: samples } as never);
  console.log();
  console.log(`Verdict: ${verdict.toUpperCase()}`);
  console.log(`Rationale: ${rationale}`);
  console.log(`Evidence: ${fpath}`);
  process.exit(verdict === "pass" ? 0 : 1);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Step 1.3 — Create `tests/phase-0/10-0-8-queue-depth.ts`** — pushes 8 tiles back-to-back, count coalesced/dropped:

```typescript
// Specs §10.0.8 + Pitfall 12 — G2 firmware queue depth assumption (full table {1,2,3,≥4}).
// CONTEXT.md D-10 strict tier mapping → deriveQueueDepthTier helper.
//
// Protocol:
//   - Issue 8 distinct tile IDs back-to-back (no awaiting individual SDK promises sequentially)
//   - Race the bulk dispatch against a settle timeout (5 sec)
//   - Inspect what the firmware acknowledged vs dropped (via callback if SDK exposes; else infer from
//     subsequent re-fetch of last applied tile id)
//   - measured_max_queue = how many tiles were accepted concurrently before dropping started

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence } from "./_shared/output.js";
import { deriveQueueDepthTier } from "./_shared/branch-decision.js";
import { QueueDepthResult } from "./_shared/schemas.js";

const THRESHOLDS = {
  burst_size: 8,
  settle_timeout_ms: 5000,
  tile_size_bytes: 4096,
} as const;

function sleep(ms: number): Promise<void> { return new Promise((r) => setTimeout(r, ms)); }

async function main(): Promise<void> {
  console.log("Specs §10.0.8 — Queue depth empirical probe");
  console.log("============================================");
  console.log(`Burst: ${THRESHOLDS.burst_size} tiles back-to-back`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = QueueDepthResult.parse({
      schema_version: 1,
      test_id: "10-0-8-queue-depth",
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      burst_size: THRESHOLDS.burst_size,
      measured_max_queue: 0,
      dropped_count: 0,
      coalesced_count: 0,
      table: {
        "1": "20 fps cap (linear serialization)",
        "2": "tier A — push 2 tiles, wait for settle, push next 2",
        "3": "tier B — adaptive fps Layer 6 + warning chip",
        ">=4": "tier C — automatic degrade glyph mode",
      },
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  // Pre-create 8 image containers with distinct IDs (so we can detect which ones got rendered).
  for (let i = 0; i < THRESHOLDS.burst_size; i++) {
    await hub.bridge.createImageContainer({ id: `queue-test-${i}`, x: i * 10, y: 0, w: 8, h: 8 });
  }

  const tile = new Uint8Array(THRESHOLDS.tile_size_bytes);
  // Distinct payload per tile so coalesce-detection has signal
  for (let i = 0; i < tile.length; i++) tile[i] = i & 0xff;

  // Burst: fire all updateImageRawData calls in a single microtask batch, capture which resolve.
  const promises = Array.from({ length: THRESHOLDS.burst_size }, (_, i) => {
    const dist = new Uint8Array(tile);
    dist[0] = i; // unique payload prefix
    return hub.bridge
      .updateImageRawData(`queue-test-${i}`, dist)
      .then(() => ({ i, status: "accepted" as const }))
      .catch((err: unknown) => ({ i, status: "rejected" as const, err: String(err) }));
  });

  const settled = await Promise.race([
    Promise.all(promises),
    sleep(THRESHOLDS.settle_timeout_ms).then(() => null),
  ]);

  let measuredMaxQueue = 0;
  let dropped = 0;
  let coalesced = 0;
  if (settled === null) {
    // Timeout — count how many actually resolved by polling promise states
    // Heuristic: if Promise.race timed out, queue couldn't absorb 8; pessimistic = ≥4 (tier C).
    measuredMaxQueue = 4;
    dropped = THRESHOLDS.burst_size - measuredMaxQueue;
  } else {
    const accepted = settled.filter((s) => s.status === "accepted").length;
    measuredMaxQueue = accepted;
    dropped = THRESHOLDS.burst_size - accepted;
    // Coalesce inference: if SDK returned accept but tile not visible, firmware coalesced.
    // Phase 0 cannot empirically verify visibility without G2 photo; document as 0 for first run,
    // researcher updates manually in Plan 04 if visual inspection reveals coalescing.
    coalesced = 0;
  }

  const tier = deriveQueueDepthTier(measuredMaxQueue);
  const verdict = tier.tier === "A" ? "pass" : tier.tier === "B" ? "fail" : "fail";
  // Note: "fail" here doesn't mean test errored — it means the queue depth maps to Branch B/C.
  // ADR-0005 will reconcile across all 7 tests; this script's verdict is a per-test signal.

  const result = QueueDepthResult.parse({
    schema_version: 1,
    test_id: "10-0-8-queue-depth",
    timestamp: new Date().toISOString(),
    verdict,
    rationale: tier.rationale,
    burst_size: THRESHOLDS.burst_size,
    measured_max_queue: measuredMaxQueue,
    dropped_count: dropped,
    coalesced_count: coalesced,
    table: {
      "1": "20 fps cap (linear serialization)",
      "2": "tier A — push 2 tiles, wait for settle, push next 2",
      "3": "tier B — adaptive fps Layer 6 + warning chip",
      ">=4": "tier C — automatic degrade glyph mode",
    },
  });
  const fpath = await writeJsonEvidence(result);
  console.log();
  console.log(`Verdict: ${verdict.toUpperCase()}`);
  console.log(`Tier: ${tier.tier}`);
  console.log(`Rationale: ${tier.rationale}`);
  console.log(`Evidence: ${fpath}`);
  process.exit(verdict === "pass" ? 0 : 1);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Step 1.4 — Create `tests/phase-0/10-0-9-palette-calibration.ts`** — render 16-step ramp on G2, prompt researcher for photo path, derive perceptual palette via inverse CIE L*:

```typescript
// Specs §10.0.9 + Pitfall 15 — sRGB-vs-linear FS dither + perceptual palette derivation via CIE L*.
// Protocol per docs/perf/phase-0/calibration/methodology.md:
//   1. Render uniform 16-step ramp (0/15, 1/15, ..., 15/15) on G2
//   2. Researcher photographs G2 in dim ambient + locked exposure
//   3. Researcher places photo at docs/perf/phase-0/calibration/ramp-uniform-{ts}.png
//      and provides per-step measured G luminance values via JSON file path
//   4. Script derives perceptual palette via inverse CIE L* mapping (linearize sRGB → L* → uniform spacing)
//   5. Script re-renders with derived palette
//   6. Researcher photographs again, provides measured luminance
//   7. Script verifies L* spacing within ±10% — pass if yes, iterate ≤3 times then fail
//
// Skip case: Hub unavailable → write skipped evidence + exit 2.

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence } from "./_shared/output.js";
import { PaletteCalibrationResult } from "./_shared/schemas.js";
import { readFile } from "node:fs/promises";

const THRESHOLDS = {
  palette_steps: 16,
  max_iterations: 3,
  spacing_uniformity_pct_threshold: 10, // ±10% per CONTEXT.md D-13 + methodology.md
  ramp_width: 192, // 12 px per step × 16 steps
  ramp_height: 32,
} as const;

// CIE L* derivation (docs/perf/phase-0/calibration/methodology.md formulas).
function yToLstar(yNormalized: number): number {
  if (yNormalized <= 0) return 0;
  if (yNormalized > 0.008856) {
    return 116 * Math.pow(yNormalized, 1 / 3) - 16;
  }
  return 903.3 * yNormalized;
}

function srgbToLinear(srgb: number): number {
  const v = srgb / 255;
  if (v <= 0.04045) return v / 12.92;
  return Math.pow((v + 0.055) / 1.055, 2.4);
}

// Inverse CIE L* — derive sRGB step values that produce uniform L* spacing.
function deriveUniformLstarPalette(): number[] {
  const palette: number[] = [];
  for (let i = 0; i < THRESHOLDS.palette_steps; i++) {
    const targetLstar = (i / (THRESHOLDS.palette_steps - 1)) * 100;
    // Solve L* = 116·(Y/Yn)^(1/3) - 16 for Y/Yn assuming Yn=1
    const yNorm = targetLstar > 8 ? Math.pow((targetLstar + 16) / 116, 3) : targetLstar / 903.3;
    // Linear → sRGB encode
    const linear = yNorm;
    const srgb = linear <= 0.0031308 ? linear * 12.92 : 1.055 * Math.pow(linear, 1 / 2.4) - 0.055;
    palette.push(Math.round(srgb * 255));
  }
  return palette;
}

function makeRampImage(palette: number[]): Uint8Array {
  // Pack as raw 8-bit greyscale — Plan 04 will adapt to 4-bit format once §10.0.2 verdict is known.
  const buf = new Uint8Array(THRESHOLDS.ramp_width * THRESHOLDS.ramp_height);
  const stepWidth = THRESHOLDS.ramp_width / THRESHOLDS.palette_steps;
  for (let y = 0; y < THRESHOLDS.ramp_height; y++) {
    for (let x = 0; x < THRESHOLDS.ramp_width; x++) {
      const stepIdx = Math.min(THRESHOLDS.palette_steps - 1, Math.floor(x / stepWidth));
      buf[y * THRESHOLDS.ramp_width + x] = palette[stepIdx] ?? 0;
    }
  }
  return buf;
}

async function loadMeasuredLuminance(iteration: number): Promise<number[] | null> {
  const fpath = `docs/perf/phase-0/calibration/measured-iter-${iteration}.json`;
  try {
    const text = await readFile(fpath, "utf8");
    const parsed = JSON.parse(text) as { g_means: number[] };
    if (!Array.isArray(parsed.g_means) || parsed.g_means.length !== THRESHOLDS.palette_steps) {
      console.error(`[ERROR] ${fpath}: g_means must be array of ${THRESHOLDS.palette_steps} numbers`);
      return null;
    }
    return parsed.g_means;
  } catch {
    return null;
  }
}

async function main(): Promise<void> {
  console.log("Specs §10.0.9 — Palette calibration (Pitfall 15 mitigation)");
  console.log("=============================================================");
  console.log(`Steps: ${THRESHOLDS.palette_steps}`);
  console.log(`Max iterations: ${THRESHOLDS.max_iterations}`);
  console.log(`Pass threshold: spacing within ±${THRESHOLDS.spacing_uniformity_pct_threshold}% L* uniform`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = PaletteCalibrationResult.parse({
      schema_version: 1,
      test_id: "10-0-9-palette-calibration",
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      iterations: 0,
      uniform_palette_lstar: [],
      derived_palette_lstar: [],
      spacing_uniformity_pct: 0,
      passes_within_10pct: false,
      camera_settings: { iso: 100, exposure_sec: 1 / 30, white_balance: "daylight" },
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  await hub.bridge.createImageContainer({
    id: "palette-cal",
    x: 0,
    y: 0,
    w: THRESHOLDS.ramp_width,
    h: THRESHOLDS.ramp_height,
  });

  // Iteration 0: uniform sRGB ramp
  let palette = Array.from({ length: THRESHOLDS.palette_steps }, (_, i) =>
    Math.round((i / (THRESHOLDS.palette_steps - 1)) * 255)
  );
  let uniformLstars: number[] = [];
  let derivedLstars: number[] = [];
  let spacingUniformityPct = 0;
  let passes = false;
  let iter = 0;

  for (iter = 1; iter <= THRESHOLDS.max_iterations; iter++) {
    console.log(`Iteration ${iter}: rendering palette ${JSON.stringify(palette)}`);
    await hub.bridge.updateImageRawData("palette-cal", makeRampImage(palette));
    console.log(`Photograph G2 now (camera locked: ISO 100, 1/30s, daylight WB).`);
    console.log(`Save as: docs/perf/phase-0/calibration/ramp-iter-${iter}-{ts}.png`);
    console.log(`Then write per-step G luminance JSON to: docs/perf/phase-0/calibration/measured-iter-${iter}.json`);
    console.log(`  Format: { "g_means": [<step0>, <step1>, ..., <step15>] }`);
    console.log(`Press Ctrl+C and re-run after the JSON file is in place.`);

    const measured = await loadMeasuredLuminance(iter);
    if (!measured) {
      console.log(`[WAIT] Measured luminance file not yet present. Aborting iteration loop — re-run after camera capture.`);
      break;
    }

    const yMax = Math.max(...measured);
    const lstars = measured.map((g) => yToLstar(g / yMax));
    if (iter === 1) uniformLstars = lstars;
    derivedLstars = lstars;

    // Compute spacing uniformity: ratio of max gap to mean gap
    const gaps: number[] = [];
    for (let i = 1; i < lstars.length; i++) {
      const cur = lstars[i];
      const prev = lstars[i - 1];
      if (cur === undefined || prev === undefined) continue;
      gaps.push(cur - prev);
    }
    const meanGap = gaps.reduce((s, g) => s + g, 0) / Math.max(1, gaps.length);
    const maxDeviation = Math.max(...gaps.map((g) => Math.abs(g - meanGap)));
    spacingUniformityPct = (maxDeviation / Math.max(0.01, meanGap)) * 100;
    passes = spacingUniformityPct <= THRESHOLDS.spacing_uniformity_pct_threshold;

    console.log(`Iteration ${iter}: spacing uniformity = ${spacingUniformityPct.toFixed(1)}% (pass if ≤${THRESHOLDS.spacing_uniformity_pct_threshold}%)`);
    if (passes) break;

    // Derive next palette via inverse CIE L*
    palette = deriveUniformLstarPalette();
  }

  const verdict = passes ? "pass" : "fail";
  const rationale = passes
    ? `Iteration ${iter}: spacing within ±${spacingUniformityPct.toFixed(1)}% L* uniform — palette calibrated`
    : `After ${iter} iteration(s): spacing ${spacingUniformityPct.toFixed(1)}% exceeds ±${THRESHOLDS.spacing_uniformity_pct_threshold}% threshold — needs Phase 4a follow-up (CIEDE2000 / photometer)`;

  const result = PaletteCalibrationResult.parse({
    schema_version: 1,
    test_id: "10-0-9-palette-calibration",
    timestamp: new Date().toISOString(),
    verdict,
    rationale,
    iterations: iter,
    uniform_palette_lstar: uniformLstars,
    derived_palette_lstar: derivedLstars,
    spacing_uniformity_pct: spacingUniformityPct,
    passes_within_10pct: passes,
    camera_settings: { iso: 100, exposure_sec: 1 / 30, white_balance: "daylight" },
  });
  const fpath = await writeJsonEvidence(result);
  console.log();
  console.log(`Verdict: ${verdict.toUpperCase()}`);
  console.log(`Rationale: ${rationale}`);
  console.log(`Evidence: ${fpath}`);
  process.exit(verdict === "pass" ? 0 : 1);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Why these 3 in same task (sizing):** Each is ~150 LOC with a similar shape (load Hub → measure → derive verdict → write evidence). Combining into one task = ~450 LOC TS, fits the 15-60 min Claude execution budget.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT/tests/phase-0 && grep -q '"upng-js": "2.1.0"' package.json && pnpm install --silent 2>&1 | tail -2 && test -f 10-0-7-dle-sustained.ts && test -f 10-0-8-queue-depth.ts && test -f 10-0-9-palette-calibration.ts && grep -q "30 \* 60 \* 1000" 10-0-7-dle-sustained.ts && grep -q "renegotiation_events" 10-0-7-dle-sustained.ts && grep -q "DleSustainedResult" 10-0-7-dle-sustained.ts && grep -q "deriveQueueDepthTier" 10-0-8-queue-depth.ts && grep -q "burst_size: 8" 10-0-8-queue-depth.ts && grep -q "QueueDepthResult" 10-0-8-queue-depth.ts && grep -q "PaletteCalibrationResult" 10-0-9-palette-calibration.ts && grep -q "yToLstar" 10-0-9-palette-calibration.ts && grep -q "spacing_uniformity_pct_threshold: 10" 10-0-9-palette-calibration.ts && grep -q "loadHub" 10-0-7-dle-sustained.ts && grep -q "loadHub" 10-0-8-queue-depth.ts && grep -q "loadHub" 10-0-9-palette-calibration.ts && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase-0/package.json` devDependencies contains `"upng-js": "2.1.0"` (verbatim, pinned)
    - Command `cd tests/phase-0 && pnpm install` succeeds (upng-js resolves)
    - File `tests/phase-0/10-0-7-dle-sustained.ts` exists, contains `30 * 60 * 1000` (30-min duration), imports `loadHub`, `writeJsonEvidence`, `percentile`, `DleSustainedResult`
    - File `tests/phase-0/10-0-7-dle-sustained.ts` captures `renegotiation_events` array AND `inferred_mtu_history` (Pitfall 10 mitigation 2)
    - File `tests/phase-0/10-0-7-dle-sustained.ts` exits 0/1/2 (pass/fail/skipped) per Plan 02 convention
    - File `tests/phase-0/10-0-8-queue-depth.ts` exists, contains `burst_size: 8`, imports `deriveQueueDepthTier`, `loadHub`, `QueueDepthResult`
    - File `tests/phase-0/10-0-8-queue-depth.ts` produces table object with keys `"1"`, `"2"`, `"3"`, `">=4"` (D-10 strict tier mapping)
    - File `tests/phase-0/10-0-9-palette-calibration.ts` exists, contains `yToLstar` function (CIE L* derivation), `spacing_uniformity_pct_threshold: 10` (±10% per D-13)
    - File `tests/phase-0/10-0-9-palette-calibration.ts` reads measured luminance from `docs/perf/phase-0/calibration/measured-iter-{N}.json` files
    - File `tests/phase-0/10-0-9-palette-calibration.ts` iterates max 3 times (CONTEXT.md D-13 rationale + methodology.md from Plan 01)
    - All 3 files call `loadHub()` and emit `verdict: "skipped"` evidence on Hub-unavailable case (Pattern 3 capability-negotiation skip from RESEARCH.md)
    - All 3 files reference Specs §10.0.7, §10.0.8, §10.0.9 in header comment
    - Command `cd tests/phase-0 && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    - 4 files modified/created (package.json updated + 3 test scripts)
    - All 3 test scripts type-check green under TS strict + noUncheckedIndexedAccess
    - Pattern 3 capability-negotiation skip implemented uniformly
    - THRESHOLDS const top-level pre-committed per D-12 (no runtime overrides)
    - Each script imports ONLY from `_shared/*.js` + node:* + zod (no cross-test dep coupling)
  </done>
</task>

<task type="auto">
  <name>Task 2: 3 hardware test scripts (10-0-1 R1 timing, 10-0-2 image format, 10-0-3 BLE multi-env)</name>
  <files>
    tests/phase-0/10-0-1-r1-timing.ts
    tests/phase-0/10-0-2-image-format.ts
    tests/phase-0/10-0-3-ble-multi-env.ts
  </files>
  <read_first>
    - .planning/phases/00-validation-gates/00-RESEARCH.md (Code Example 3 BLE multi-env skeleton; Code Example 5 Image Format Probe with upng-js Format A; Pitfall 5 — gesture polysemy + n=150 sample protocol; §"Don't Hand-Roll" — application-observable BLE measurement via SDK callback timestamps)
    - .planning/phases/00-validation-gates/00-CONTEXT.md (D-08 — file naming; D-09 — multi-percentile envelope; D-12 — strict numeric ±5% borderline)
    - tests/phase-0/_shared/schemas.ts (Plan 01 — BleMultiEnvResult, ImageFormatResult, R1TimingResult exact shapes)
    - tests/phase-0/_shared/branch-decision.ts (Plan 01 — deriveBranch + DEFAULT_THRESHOLDS)
    - tests/phase-0/_shared/stats.ts (Plan 01 — percentile + hartiganDipTest + ci95)
    - tests/phase-0/_shared/hub.ts (Plan 01 — loadHub + onR1Event)
    - tests/phase-0/10-0-7-dle-sustained.ts (just-created Task 1 — same shape pattern: load Hub → measure → derive → write evidence; this Task 2 mirrors it)
    - CLAUDE.md (§11.5.7 — upng-js 2.1.0 pinned)
  </read_first>
  <action>
**Step 2.1 — Create `tests/phase-0/10-0-1-r1-timing.ts`** — R1 gesture timing window measurement (n=150 per gesture × 5 sessions; bimodality test for tap vs double-tap):

```typescript
// Specs §10.0.1 + Pitfall 5 — R1 gesture timing windows.
// Protocol per RESEARCH §"Don't Hand-Roll" + Open Question 3:
//   - n=30 samples per gesture per session × 5 sessions = 150 samples per gesture
//   - 6 gestures: tap, double-tap, scroll-up, scroll-down, long-press-1s, long-press-2s
//   - Researcher follows CLI prompts: "perform 30 taps now" → SDK callback captures t_ms timestamps
//   - Computes mean ± SD per gesture + Hartigan dip test on tap_vs_double_tap_isi distribution
//   - Outputs recommended_windows_ms (tap_max, double_tap_max, long_press_min) — INV-5 design constants
//
// Skip case: Hub unavailable → write skipped evidence + exit 2.

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence } from "./_shared/output.js";
import { percentile, hartiganDipTest, ci95 } from "./_shared/stats.js";
import { R1TimingResult } from "./_shared/schemas.js";
import { createInterface } from "node:readline/promises";

const THRESHOLDS = {
  samples_per_gesture_per_session: 30,
  sessions: 5,
  bimodality_p_threshold: 0.05, // tap vs double-tap distinguishable if Hartigan dip p < 0.05
  long_press_min_ms_floor: 500, // Specs §10.0.1 GO/NO-GO bar
} as const;

const GESTURES = ["tap", "double-tap", "scroll-up", "scroll-down", "long-press-1s", "long-press-2s"] as const;
type Gesture = typeof GESTURES[number];

async function captureGestureTimings(
  hub: { onR1Event: (h: (ev: { type: string; t_ms: number; raw: unknown }) => void) => () => void },
  gesture: Gesture,
  n: number,
  rl: ReturnType<typeof createInterface>
): Promise<number[]> {
  await rl.question(`\n>>> Perform ${n} × [${gesture}] now. Press ENTER when ready, then perform.`);
  const samples: number[] = [];
  let lastT = 0;
  return new Promise((resolve) => {
    const off = hub.onR1Event((ev) => {
      // Filter: only count events whose type matches the requested gesture (best-effort firmware match)
      if (!ev.type.includes(gesture.split("-")[0] ?? "")) return;
      if (lastT > 0) {
        const isi = ev.t_ms - lastT;
        if (isi > 0 && isi < 5000) samples.push(isi);
      }
      lastT = ev.t_ms;
      if (samples.length >= n) {
        off();
        resolve(samples);
      }
    });
    // 60-sec timeout per gesture batch
    setTimeout(() => {
      off();
      resolve(samples);
    }, 60_000);
  });
}

async function main(): Promise<void> {
  console.log("Specs §10.0.1 — R1 gesture timing windows");
  console.log("==========================================");
  console.log(`Sessions: ${THRESHOLDS.sessions}`);
  console.log(`Samples per gesture per session: ${THRESHOLDS.samples_per_gesture_per_session}`);
  console.log(`Total per gesture: ${THRESHOLDS.sessions * THRESHOLDS.samples_per_gesture_per_session}`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = R1TimingResult.parse({
      schema_version: 1,
      test_id: "10-0-1-r1-timing",
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      sessions: 0,
      samples_per_gesture: 0,
      gestures: {},
      bimodality: {
        tap_vs_double_tap_dip: 0,
        tap_vs_double_tap_p_value: 1,
        distinguishable: false,
      },
      recommended_windows_ms: { tap_max: 250, double_tap_max: 500, long_press_min: 500 },
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  const rl = createInterface({ input: process.stdin, output: process.stdout });
  const allSamples: Record<Gesture, number[]> = {
    "tap": [],
    "double-tap": [],
    "scroll-up": [],
    "scroll-down": [],
    "long-press-1s": [],
    "long-press-2s": [],
  };

  for (let session = 1; session <= THRESHOLDS.sessions; session++) {
    console.log(`\n=== Session ${session}/${THRESHOLDS.sessions} ===`);
    for (const g of GESTURES) {
      const samples = await captureGestureTimings(hub.bridge, g, THRESHOLDS.samples_per_gesture_per_session, rl);
      allSamples[g].push(...samples);
      console.log(`  ${g}: captured ${samples.length} samples`);
    }
  }
  await rl.close();

  // Compute per-gesture stats
  const gestureStats: Record<string, { mean_ms: number; sd_ms: number; p95_ms: number; n: number }> = {};
  for (const g of GESTURES) {
    const samples = allSamples[g];
    const ci = ci95(samples);
    const variance = samples.length > 1
      ? samples.reduce((s, x) => s + (x - ci.mean) ** 2, 0) / (samples.length - 1)
      : 0;
    gestureStats[g] = {
      mean_ms: ci.mean,
      sd_ms: Math.sqrt(variance),
      p95_ms: percentile(samples, 95),
      n: samples.length,
    };
  }

  // Bimodality check on tap vs double-tap distribution
  const tapAndDouble = [...allSamples["tap"], ...allSamples["double-tap"]];
  const dip = hartiganDipTest(tapAndDouble);
  const distinguishable = dip.pValue < THRESHOLDS.bimodality_p_threshold;

  // Recommended windows: tap_max = midpoint between tap p95 and double-tap mean
  const tapP95 = gestureStats["tap"]?.p95_ms ?? 250;
  const doubleMean = gestureStats["double-tap"]?.mean_ms ?? 400;
  const longPress1Mean = gestureStats["long-press-1s"]?.mean_ms ?? 1000;
  const tapMax = Math.round(Math.max(150, Math.min(tapP95 + 50, (tapP95 + doubleMean) / 2)));
  const doubleTapMax = Math.round(Math.max(tapMax + 100, doubleMean + 100));
  const longPressMin = Math.max(THRESHOLDS.long_press_min_ms_floor, Math.round(longPress1Mean * 0.7));

  const verdict = distinguishable && longPressMin >= THRESHOLDS.long_press_min_ms_floor ? "pass" : "fail";
  const rationale = verdict === "pass"
    ? `Tap vs double-tap bimodal (Hartigan dip p=${dip.pValue.toFixed(3)} < ${THRESHOLDS.bimodality_p_threshold}); long_press_min=${longPressMin}ms ≥ ${THRESHOLDS.long_press_min_ms_floor}ms floor`
    : `Distinguishability failed: dip p=${dip.pValue.toFixed(3)}, long_press_min=${longPressMin}ms — INV-5 design needs explicit visual feedback chip (Pitfall 5)`;

  const result = R1TimingResult.parse({
    schema_version: 1,
    test_id: "10-0-1-r1-timing",
    timestamp: new Date().toISOString(),
    verdict,
    rationale,
    sessions: THRESHOLDS.sessions,
    samples_per_gesture: THRESHOLDS.sessions * THRESHOLDS.samples_per_gesture_per_session,
    gestures: gestureStats,
    bimodality: {
      tap_vs_double_tap_dip: dip.dip,
      tap_vs_double_tap_p_value: dip.pValue,
      distinguishable,
    },
    recommended_windows_ms: { tap_max: tapMax, double_tap_max: doubleTapMax, long_press_min: longPressMin },
  });
  const fpath = await writeJsonEvidence(result);
  console.log();
  console.log(`Verdict: ${verdict.toUpperCase()}`);
  console.log(`Rationale: ${rationale}`);
  console.log(`Recommended windows (Phase 6 INV-5 input): tap_max=${tapMax}ms, double_tap_max=${doubleTapMax}ms, long_press_min=${longPressMin}ms`);
  console.log(`Evidence: ${fpath}`);
  process.exit(verdict === "pass" ? 0 : 1);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Step 2.2 — Create `tests/phase-0/10-0-2-image-format.ts`** — Generates 3 candidate format payloads, sends each to G2, captures researcher visual verdict via CLI prompt (RESEARCH Code Example 5):

```typescript
// Specs §10.0.2 + Pitfall 7 — updateImageRawData byte format probe.
// 3 candidate formats:
//   A: PNG indexed-palette 4-bit greyscale (~3-5 KB) — generated via upng-js
//   B: Raw 4-bit packed big-endian (10000 bytes for 200×100)
//   C: Raw 4-bit packed little-endian (same length, swapped nibble order)
//
// Protocol: harness sends each format sequentially (5 sec hold per format), prompts researcher
// to photograph G2 and enter verdict A/B/C/none via CLI. Promotes the identified format to ADR-0006
// + Phase 4a boot-time format probe (Pitfall 7 mitigation 2).

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence } from "./_shared/output.js";
import { ImageFormatResult } from "./_shared/schemas.js";
import { createInterface } from "node:readline/promises";
import { createHash } from "node:crypto";
import UPNG from "upng-js";

const THRESHOLDS = {
  width: 200,
  height: 100,
  hold_per_format_sec: 5,
} as const;

function makeFormatA(width: number, height: number): Uint8Array {
  // 16-step uniform greyscale palette (calibration §10.0.9 will refine)
  const palette = Array.from({ length: 16 }, (_, i) => {
    const v = Math.round((i / 15) * 255);
    return [v, v, v, 255];
  });
  // Test pattern: top half = vertical 16-step gradient, bottom half = 8x8 checker
  const indexed = new Uint8Array(width * height);
  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const grad = Math.min(15, Math.floor((y / (height / 2)) * 16));
      const checker = ((x >> 3) ^ (y >> 3)) & 1 ? 15 : 0;
      indexed[y * width + x] = y < height / 2 ? grad : checker;
    }
  }
  // Note: upng-js encodeLL signature: encodeLL([buffers], w, h, channels, depth, depth_bits)
  // For 4-bit indexed greyscale: we encode as 8-bit indexed and let downstream hardware re-pack.
  // upng-js depth parameter for indexed mode is the bits-per-sample.
  // If runtime errors, fallback: hand-pack 4-bit (see Format B as template).
  void palette; // palette context used by upng-js indexed-palette mode at runtime
  return new Uint8Array(UPNG.encodeLL([indexed.buffer as ArrayBuffer], width, height, 1, 0, 4));
}

function makeFormatB(width: number, height: number): Uint8Array {
  const bytes = new Uint8Array((width * height) / 2);
  for (let i = 0; i < bytes.length; i++) {
    const x0 = (i * 2) % width;
    const y0 = Math.floor((i * 2) / width);
    const x1 = ((i * 2) + 1) % width;
    const y1 = Math.floor(((i * 2) + 1) / width);
    const v0 = Math.min(15, Math.floor((y0 / height) * 16)) & 0xf;
    const v1 = Math.min(15, Math.floor((y1 / height) * 16)) & 0xf;
    // Big-endian: high nibble = first pixel
    bytes[i] = (v0 << 4) | v1;
  }
  return bytes;
}

function makeFormatC(width: number, height: number): Uint8Array {
  const bytes = makeFormatB(width, height);
  // Swap nibbles (little-endian)
  for (let i = 0; i < bytes.length; i++) {
    const cur = bytes[i] ?? 0;
    bytes[i] = ((cur & 0xf) << 4) | ((cur >> 4) & 0xf);
  }
  return bytes;
}

function sleep(ms: number): Promise<void> { return new Promise((r) => setTimeout(r, ms)); }

async function main(): Promise<void> {
  console.log("Specs §10.0.2 — updateImageRawData byte format probe (Pitfall 7)");
  console.log("==================================================================");
  console.log(`Image dimensions: ${THRESHOLDS.width}×${THRESHOLDS.height}`);
  console.log(`Hold per format: ${THRESHOLDS.hold_per_format_sec} sec`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = ImageFormatResult.parse({
      schema_version: 1,
      test_id: "10-0-2-image-format",
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      formats_tested: [],
      identified_format: "none",
      probe_pattern_hash: "",
      researcher_visual_verdict: "skipped — Hub unavailable",
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  await hub.bridge.createImageContainer({ id: "img-probe", x: 0, y: 0, w: THRESHOLDS.width, h: THRESHOLDS.height });

  const rl = createInterface({ input: process.stdin, output: process.stdout });

  const formats = [
    { id: "A-png-indexed" as const, data: makeFormatA(THRESHOLDS.width, THRESHOLDS.height) },
    { id: "B-raw-4bit-be" as const, data: makeFormatB(THRESHOLDS.width, THRESHOLDS.height) },
    { id: "C-raw-4bit-le" as const, data: makeFormatC(THRESHOLDS.width, THRESHOLDS.height) },
  ];

  // Hash for INV-2 traceability — anchors the canonical probe pattern across re-runs
  const hash = createHash("sha256");
  for (const f of formats) hash.update(f.data);
  const probePatternHash = hash.digest("hex");

  console.log(`Probe pattern hash (sha256): ${probePatternHash}`);
  console.log(`(This hash anchors the canonical probe pattern for INV-2 + Phase 4 boot-time self-test per Pitfall 7 mitigation 2.)`);
  console.log();

  for (const f of formats) {
    console.log(`\n>>> Sending Format ${f.id} (${f.data.length} bytes)...`);
    await hub.bridge.updateImageRawData("img-probe", f.data);
    console.log(`>>> Photograph G2 NOW. Holding for ${THRESHOLDS.hold_per_format_sec} sec.`);
    await sleep(THRESHOLDS.hold_per_format_sec * 1000);
  }

  console.log();
  const verdict = await rl.question(
    `Which format rendered the test pattern (top half = 16-step vertical gradient, bottom half = 8×8 checker)?\n` +
    `  A — PNG indexed (Format A)\n` +
    `  B — raw 4-bit big-endian (Format B)\n` +
    `  C — raw 4-bit little-endian (Format C)\n` +
    `  none — none rendered correctly\n` +
    `> `
  );
  const cleanVerdict = verdict.trim().toUpperCase();
  const identified =
    cleanVerdict === "A" ? "A-png-indexed" :
    cleanVerdict === "B" ? "B-raw-4bit-be" :
    cleanVerdict === "C" ? "C-raw-4bit-le" :
    "none";
  await rl.close();

  const passOrFail = identified === "none" ? "fail" : "pass";

  const result = ImageFormatResult.parse({
    schema_version: 1,
    test_id: "10-0-2-image-format",
    timestamp: new Date().toISOString(),
    verdict: passOrFail,
    rationale: identified === "none"
      ? `No format rendered correctly — Phase 4a raster pipeline blocked, glyph-only mode required (Branch C trigger or SDK API mismatch — re-verify post-grant SDK signature)`
      : `Format ${identified} renders correctly — Phase 4a raster pipeline + boot-time format probe pattern locked (Pitfall 7 mitigation 2)`,
    formats_tested: ["A-png-indexed", "B-raw-4bit-be", "C-raw-4bit-le"],
    identified_format: identified,
    probe_pattern_hash: probePatternHash,
    researcher_visual_verdict: `Researcher entered: "${verdict.trim()}" → identified ${identified}`,
  });
  const fpath = await writeJsonEvidence(result);
  console.log();
  console.log(`Verdict: ${passOrFail.toUpperCase()}`);
  console.log(`Identified format: ${identified}`);
  console.log(`Evidence: ${fpath}`);
  process.exit(passOrFail === "pass" ? 0 : 1);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Step 2.3 — Create `tests/phase-0/10-0-3-ble-multi-env.ts`** — multi-env BLE bandwidth (Code Example 3 from RESEARCH.md, fully implemented):

```typescript
// Specs §10.0.3 + Pitfall 2 + CONTEXT.md D-09 — BLE bandwidth multi-environment multi-percentile.
// Run sequence (researcher operationally controls RF env):
//   RF_ENV=clean              pnpm exec tsx 10-0-3-ble-multi-env.ts
//   RF_ENV=5ghz-loaded        pnpm exec tsx 10-0-3-ble-multi-env.ts
//   RF_ENV=2-4ghz-microwave   pnpm exec tsx 10-0-3-ble-multi-env.ts
//
// Each run is a standalone 30-min sustained measurement in one RF environment.
// ADR-0005 closure (Plan 04) reads ALL 3 evidence JSONs and calls deriveBranch() with all envs
// to produce the final A/B/C verdict.

import { loadHub } from "./_shared/hub.js";
import { writeJsonEvidence, writeCsvEvidence } from "./_shared/output.js";
import { percentile } from "./_shared/stats.js";
import { deriveBranch, DEFAULT_THRESHOLDS } from "./_shared/branch-decision.js";
import { BleMultiEnvResult } from "./_shared/schemas.js";

const THRESHOLDS = {
  duration_ms: 30 * 60 * 1000,
  tile_size_bytes: 4096,
  tile_interval_ms: 100,
  baseline_window_samples: 60,
  renegotiation_drop_threshold_pct: 50,
} as const;

const VALID_ENVS = ["clean", "5ghz-loaded", "2-4ghz-microwave"] as const;
type Env = typeof VALID_ENVS[number];

function sleep(ms: number): Promise<void> { return new Promise((r) => setTimeout(r, ms)); }

function parseEnv(): Env {
  const v = process.env["RF_ENV"];
  if (!v) {
    console.error("ERROR: RF_ENV env var is required. Set to one of: clean, 5ghz-loaded, 2-4ghz-microwave");
    process.exit(3);
  }
  if (!VALID_ENVS.includes(v as Env)) {
    console.error(`ERROR: RF_ENV='${v}' not valid. Must be one of: ${VALID_ENVS.join(", ")}`);
    process.exit(3);
  }
  return v as Env;
}

async function main(): Promise<void> {
  const env = parseEnv();
  console.log(`Specs §10.0.3 — BLE bandwidth multi-env (Pitfall 2 + D-09)`);
  console.log(`============================================================`);
  console.log(`RF environment: ${env}`);
  console.log(`Duration: ${THRESHOLDS.duration_ms / 60_000} min`);
  console.log(`Tile: ${THRESHOLDS.tile_size_bytes} bytes every ${THRESHOLDS.tile_interval_ms} ms`);
  console.log();

  const hub = await loadHub();
  if (!hub.available) {
    const skipped = BleMultiEnvResult.parse({
      schema_version: 1,
      test_id: "10-0-3-ble-multi-env",
      env,
      timestamp: new Date().toISOString(),
      verdict: "skipped",
      rationale: hub.reason,
      duration_sec: 0,
      tile_size_bytes: THRESHOLDS.tile_size_bytes,
      tile_interval_ms: THRESHOLDS.tile_interval_ms,
      samples_kbps: [],
      p50_kbps: 0,
      p95_kbps: 0,
      p99_kbps: 0,
      renegotiation_events: [],
    });
    const fpath = await writeJsonEvidence(skipped);
    console.log(`[SKIP] ${hub.reason}`);
    console.log(`Evidence: ${fpath}`);
    process.exit(2);
  }

  await hub.bridge.createImageContainer({ id: "ble-test", x: 0, y: 0, w: 200, h: 100 });
  const tile = new Uint8Array(THRESHOLDS.tile_size_bytes);
  for (let i = 0; i < tile.length; i++) tile[i] = i & 0xff;

  const samples: number[] = [];
  const renegotiationEvents: Array<{ t_sec: number; p_drop_pct: number }> = [];
  let baselineKbps = 0;

  console.log(`Starting 30-min measurement at ${new Date().toISOString()}...`);
  const t0 = performance.now();
  let lastReport = t0;

  while (performance.now() - t0 < THRESHOLDS.duration_ms) {
    const tStart = performance.now();
    await hub.bridge.updateImageRawData("ble-test", tile);
    const tEnd = performance.now();
    const elapsedMs = Math.max(0.001, tEnd - tStart);
    const kbps = (THRESHOLDS.tile_size_bytes * 8) / (elapsedMs / 1000) / 1000;
    samples.push(kbps);

    if (samples.length === THRESHOLDS.baseline_window_samples) {
      baselineKbps = percentile(samples, 50);
      console.log(`Baseline (first ${THRESHOLDS.baseline_window_samples} samples): p50=${baselineKbps.toFixed(1)} kbps`);
    }
    if (baselineKbps > 0 && kbps < baselineKbps * (1 - THRESHOLDS.renegotiation_drop_threshold_pct / 100)) {
      renegotiationEvents.push({
        t_sec: (tEnd - t0) / 1000,
        p_drop_pct: 100 - (kbps / baselineKbps) * 100,
      });
    }

    // Progress every 5 minutes
    if (tEnd - lastReport > 5 * 60 * 1000) {
      const pct = Math.round(((tEnd - t0) / THRESHOLDS.duration_ms) * 100);
      console.log(`[${pct}%] samples=${samples.length}, baseline=${baselineKbps.toFixed(1)} kbps, renegotiations=${renegotiationEvents.length}`);
      lastReport = tEnd;
    }

    const slack = THRESHOLDS.tile_interval_ms - (performance.now() - tStart);
    if (slack > 0) await sleep(slack);
  }

  const p50 = percentile(samples, 50);
  const p95 = percentile(samples, 95);
  const p99 = percentile(samples, 99);

  // Per-env verdict using deriveBranch as if this were the only env (Plan 04 will combine all 3)
  const branch = deriveBranch([{ env, p50, p95, p99 }], DEFAULT_THRESHOLDS);
  const branchLetter: "A" | "B" | "C" | "borderline-A→B" | "borderline-B→C" =
    branch.branch === "A" || branch.branch === "B" || branch.branch === "C" || branch.branch === "borderline-A→B" || branch.branch === "borderline-B→C"
      ? branch.branch
      : "C";

  const result = BleMultiEnvResult.parse({
    schema_version: 1,
    test_id: "10-0-3-ble-multi-env",
    env,
    timestamp: new Date().toISOString(),
    verdict: branchLetter,
    rationale: branch.rationale,
    duration_sec: THRESHOLDS.duration_ms / 1000,
    tile_size_bytes: THRESHOLDS.tile_size_bytes,
    tile_interval_ms: THRESHOLDS.tile_interval_ms,
    samples_kbps: samples,
    p50_kbps: p50,
    p95_kbps: p95,
    p99_kbps: p99,
    renegotiation_events: renegotiationEvents,
  });
  const fpath = await writeJsonEvidence(result);
  await writeCsvEvidence(result);
  console.log();
  console.log(`Verdict (this env only): ${branchLetter}`);
  console.log(`p50=${p50.toFixed(1)} kbps, p95=${p95.toFixed(1)} kbps, p99=${p99.toFixed(1)} kbps`);
  console.log(`Renegotiation events: ${renegotiationEvents.length}`);
  console.log(`Rationale: ${branch.rationale}`);
  console.log(`Evidence: ${fpath}`);
  console.log();
  console.log(`(Plan 04 closure will combine all 3 RF envs for final ADR-0005 Branch verdict.)`);
  // Exit 0 if A or B (per-env), 1 if C, 2 if skipped (handled earlier)
  process.exit(branchLetter === "C" ? 1 : 0);
}

main().catch((err: unknown) => {
  console.error("Fatal:", err);
  process.exit(1);
});
```

**Why these 3 in same task:** Same shape as Task 1 (load Hub → measure → derive → write evidence) but each touches different SDK surface (R1 events vs image rendering vs sustained tile push). ~600 LOC total — fits the 15-60 min execution budget.

**Anti-pattern guard:** Do NOT add `WiFi.scan()`, `navigator.connection.*`, or any network-introspection API to the BLE script. T-00-03 mitigation: only application-observable throughput timestamps; no SSID/MAC/BSSID propagation.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT/tests/phase-0 && test -f 10-0-1-r1-timing.ts && test -f 10-0-2-image-format.ts && test -f 10-0-3-ble-multi-env.ts && grep -q "samples_per_gesture_per_session: 30" 10-0-1-r1-timing.ts && grep -q "hartiganDipTest" 10-0-1-r1-timing.ts && grep -q "tap_max" 10-0-1-r1-timing.ts && grep -q "long_press_min_ms_floor: 500" 10-0-1-r1-timing.ts && grep -q "import UPNG" 10-0-2-image-format.ts && grep -q "makeFormatA" 10-0-2-image-format.ts && grep -q "makeFormatB" 10-0-2-image-format.ts && grep -q "makeFormatC" 10-0-2-image-format.ts && grep -q "probe_pattern_hash" 10-0-2-image-format.ts && grep -q "RF_ENV" 10-0-3-ble-multi-env.ts && grep -q '"clean"' 10-0-3-ble-multi-env.ts && grep -q '"5ghz-loaded"' 10-0-3-ble-multi-env.ts && grep -q '"2-4ghz-microwave"' 10-0-3-ble-multi-env.ts && grep -q "deriveBranch" 10-0-3-ble-multi-env.ts && grep -q "DEFAULT_THRESHOLDS" 10-0-3-ble-multi-env.ts && grep -q "BleMultiEnvResult" 10-0-3-ble-multi-env.ts && ! grep -q "navigator.connection\|getNetworkInfo\|WiFi.scan" 10-0-1-r1-timing.ts 10-0-2-image-format.ts 10-0-3-ble-multi-env.ts && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/phase-0/10-0-1-r1-timing.ts` exists, captures n=30 per gesture per session × 5 sessions (verifiable: grep `samples_per_gesture_per_session: 30` AND `sessions: 5`)
    - File `tests/phase-0/10-0-1-r1-timing.ts` lists all 6 GESTURES: tap, double-tap, scroll-up, scroll-down, long-press-1s, long-press-2s
    - File `tests/phase-0/10-0-1-r1-timing.ts` calls `hartiganDipTest` for tap-vs-double-tap distinguishability AND `ci95` for per-gesture confidence intervals
    - File `tests/phase-0/10-0-1-r1-timing.ts` outputs `recommended_windows_ms` with `tap_max`, `double_tap_max`, `long_press_min` (Phase 6 INV-5 input)
    - File `tests/phase-0/10-0-1-r1-timing.ts` enforces `long_press_min ≥ 500ms` floor (Specs §10.0.1 GO/NO-GO bar)
    - File `tests/phase-0/10-0-2-image-format.ts` imports `UPNG from "upng-js"` AND defines `makeFormatA`, `makeFormatB`, `makeFormatC` functions
    - File `tests/phase-0/10-0-2-image-format.ts` computes sha256 `probe_pattern_hash` for INV-2 traceability + Pitfall 7 mitigation 2 (Phase 4a boot probe)
    - File `tests/phase-0/10-0-2-image-format.ts` prompts researcher via readline for visual verdict (A/B/C/none)
    - File `tests/phase-0/10-0-3-ble-multi-env.ts` requires `RF_ENV` env var ∈ {clean, 5ghz-loaded, 2-4ghz-microwave} (verifiable: all 3 strings present + exit 3 on missing/invalid)
    - File `tests/phase-0/10-0-3-ble-multi-env.ts` calls `deriveBranch` with `DEFAULT_THRESHOLDS` from `_shared/branch-decision.ts`
    - File `tests/phase-0/10-0-3-ble-multi-env.ts` runs 30-min sustained (verifiable: `30 * 60 * 1000` literal)
    - File `tests/phase-0/10-0-3-ble-multi-env.ts` captures `renegotiation_events` with 50% drop threshold (Pitfall 10 mitigation parallel)
    - All 3 files call `loadHub()` and emit `verdict: "skipped"` evidence on Hub-unavailable case
    - T-00-03 mitigation: NO file contains `navigator.connection`, `getNetworkInfo`, `WiFi.scan`, `SSID`, or `BSSID` (verifiable: grep returns zero hits across all 3 files)
    - Command `cd tests/phase-0 && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    - 3 files created — TS strict + noUncheckedIndexedAccess clean
    - All 6 hardware test scripts (Task 1 + Task 2) ready for execution post Even Hub access grant
    - run-all.ts orchestrator (from Plan 02) automatically picks up these 6 files (no orchestrator change)
    - REQ MIDIQ-01 + Specs §10.0.1/§10.0.2/§10.0.3/§10.0.7/§10.0.8/§10.0.9 all have COMPLETE harness scripts
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (after both tasks):**

```bash
# All 6 hardware test scripts exist
ls tests/phase-0/10-0-{1-r1-timing,2-image-format,3-ble-multi-env,7-dle-sustained,8-queue-depth,9-palette-calibration}.ts

# upng-js installed
grep -q '"upng-js": "2.1.0"' tests/phase-0/package.json

# TS type-clean across all 6 + Plan 01/02 files
cd tests/phase-0 && pnpm exec tsc --noEmit
echo "exit: $?"  # MUST be 0

# T-00-03 mitigation: no network-introspection APIs
! grep -rE 'navigator\.connection|getNetworkInfo|WiFi\.scan|SSID|BSSID' tests/phase-0/10-0-*.ts
echo "T-00-03 check: $?"  # MUST be 0

# All scripts use Pattern 3 capability-negotiation skip
for f in tests/phase-0/10-0-*.ts; do
  grep -q "loadHub" "$f" && grep -q '"skipped"' "$f" || { echo "FAIL: $f"; exit 1; }
done
echo "Pattern 3 skip check: $?"  # MUST be 0

# Each script imports from _shared/ only (no cross-test deps)
for f in tests/phase-0/10-0-*.ts; do
  ! grep -E 'from "\./10-0-' "$f" > /dev/null
done
echo "no cross-test coupling check: $?"  # MUST be 0

# Run smoke (skip-hardware mode) — should pass
cd tests/phase-0 && pnpm exec tsx run-all.ts --skip-hardware
echo "smoke exit: $?"  # 0 (all skip-by-flag) or 2 (probe ran + Foundry off → skipped)
```
</verification>

<success_criteria>
Plan 03 succeeds when:
- 7 files created/modified (package.json updated + 6 hardware test scripts)
- All 6 test scripts type-check green under TS strict + noUncheckedIndexedAccess
- All 6 scripts implement Pattern 3 capability-negotiation skip (Hub-unavailable → verdict="skipped" evidence + exit 2)
- All 6 scripts import THRESHOLDS pre-committed top-level (D-12 strict numeric)
- All 6 scripts cite Specs §10.0.X canonical SoT in header comment
- T-00-03 mitigation: zero `navigator.connection|getNetworkInfo|WiFi.scan|SSID|BSSID` across all scripts
- run-all.ts (from Plan 02) automatically discovers these 6 files via filesystem; no orchestrator change needed
- `cd tests/phase-0 && pnpm exec tsx run-all.ts --skip-hardware` runs to completion (exits 0/2)

**Hand-off to Plan 04:** Plan 04 (phase closure) will:
1. Execute the 6 hardware tests in real hardware (post Even Hub grant) + MidiQOL probe in Foundry test world
2. Commit all `docs/perf/phase-0/*.json` evidence files
3. Populate ADR-0005 verdict + per-test rationale (template stub from Plan 01)
4. Resolve ADR-0006 conditional content per Branch verdict from ADR-0005 (D-14)
5. Run INV-3 doc coherence cross-check
</success_criteria>

<output>
Crea `.planning/phases/00-validation-gates/00-03-SUMMARY.md` riassumendo:
- 7 file modificati/creati (package.json + 6 hardware test scripts)
- `pnpm exec tsc --noEmit` exit code (deve essere 0)
- T-00-03 verification: zero hits per network-introspection APIs
- Pattern 3 skip uniformity verification: tutti i 6 scripts gestiscono Hub-unavailable
- THRESHOLDS pre-committed top-level (D-12 verifiability)
- Hand-off a Plan 04: harness COMPLETO; esecuzione gated su Even Hub access grant + 3 RF env physical setup + camera capture + Foundry test world
- 6/7 test scripts SKIP gracefully fino al grant (verdict="skipped" evidence emesse anche pre-grant per documentare absence)
</output>

## Truths

This plan upholds the following CONTEXT.md decisions (cross-ref for traceability gate):

- **D-08** — 1-file-per-Specs-section naming (Task 1 + Task 2: creates exactly `10-0-1-r1-timing.ts`, `10-0-2-image-format.ts`, `10-0-3-ble-multi-env.ts`, `10-0-7-dle-sustained.ts`, `10-0-8-queue-depth.ts`, `10-0-9-palette-calibration.ts`)
- **D-09** — Multi-percentile envelope BLE thresholds applied at runtime in `10-0-3-ble-multi-env.ts` via `_shared/branch-decision.ts` `DEFAULT_THRESHOLDS` (p50≥200 AND p95≥150 AND p99≥100 sustained)
- **D-10** — Strict tier mapping for queue depth applied at runtime in `10-0-8-queue-depth.ts` via `_shared/branch-decision.ts` `deriveQueueDepthTier()` (≤2=A; 3=B; ≥4=C)
- **D-11** — 5 fps Branch A floor — referenced in `10-0-7-dle-sustained.ts` THRESHOLDS for sustained throughput verdict; 3-5 fps maps to Branch B with low-fps chip; 15 fps stretch separate gate documented in test script header comments
