---
phase: 07-foundry-module-write-path
plan: "06"
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - packages/foundry-module/src/pair/bearer-rotation.ts
  - packages/foundry-module/src/pair/bearer-rotation.test.ts
  - packages/foundry-module/src/pair/bearer-registry.ts
  - packages/foundry-module/src/pair/bearer-registry.test.ts
  - packages/foundry-module/src/module.ts
  - packages/foundry-module/src/module.test.ts
  - packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [FOUN-03, ACT-02, ACT-03, MULTI-01, REACT-01]
must_haves:
  truths:
    - "`scheduleBearerRotation()` in `packages/foundry-module/src/pair/bearer-rotation.ts` reads the active bearer (first non-revoked, non-expired entry) at module boot; schedules `setTimeout(rotate, TTL_24H_MS - elapsed)` where `elapsed = Date.now() - activeEntry.createdAt`; on fire, calls `generateBearer(alias, bridgeUrl, worldId, refresh=true)` from Plan 02 bearer-registry (already shortens old token's expiresAt to `now + GRACE_60S_MS` per RESEARCH §Q6 — no structural validateBearer change needed)."
    - "After rotation: emits `bearer.rotated` envelope via the same `bridgeDeltaEmitter` channel using Plan 07-01 `BearerRotatedPayloadSchema` (`{ rotatedAt, graceUntil }`); chains the next rotation via recursive setTimeout. Writes an audit-log entry via Plan 07-01 `writeAuditLog` with `tool: 'bearer.rotation'` + bearer_id."
    - "`scheduleBearerRotation()` returns a `cancel()` closure for teardown; idempotent if called twice (safe in module reload scenarios)."
    - "module.ts wires `scheduleBearerRotation()` inside `Hooks.once('ready')` AFTER `registerReactionWatcher(...)`. NO new socketlib handler (count stays 14)."
    - "`07-write-path-integration-smoke.test.ts` (NEW Phase 7 integration harness) runs ISM-W7-01..ISM-W7-08 covering the full write-path round-trip across all 5 handlers: cast-spell, weapon-attack (single + multi-attack count=2), use-item, move-token, place-template (request + confirm), drop-concentration, AND reaction-watcher hook fan-out. Uses mocked socketlib + dispatchTool + Foundry globals. Existing 04b-integration-smoke + 05-panel-integration-smoke + 06-cross-overlay-reachability harnesses stay green (regression)."
    - "Phase 7 closure: STATE.md updated with PHASE_7_CLOSED status; ROADMAP.md Phase 7 checkbox flipped from `[ ]` to `[x]` with plans list filled (07-01 through 07-06); hardware-pending SC-07-01..05 carry forward to ADR-0005 Branch A (running total 18 + 5 = 23 hardware-pending SCs)."
    - "checkpoint:human-verify Task 3 — operator confirms via `defer-hardware-tests` resume signal (same pattern as Phase 4a/4b/5/6 closures); software-side fully green per harness; hardware-pending SC carry on `human_needed` per ADR-0005 PROVISIONAL Branch A."
  artifacts:
    - path: "packages/foundry-module/src/pair/bearer-rotation.ts"
      provides: "scheduleBearerRotation() — boot-time setTimeout chain reusing generateBearer(refresh=true) infrastructure"
      contains: "scheduleBearerRotation"
    - path: "packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts"
      provides: "ISM-W7-01..ISM-W7-08 — full write-path round-trip across all 5 handlers + reaction-watcher fan-out + bearer rotation"
      contains: "ISM-W7-01"
    - path: ".planning/STATE.md"
      provides: "PHASE_7_CLOSED status block"
      contains: "PHASE_7_CLOSED"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 checkbox flipped + plans list populated with 07-01..07-06"
      contains: "07-06-PLAN.md"
  key_links:
    - from: "packages/foundry-module/src/pair/bearer-rotation.ts"
      to: "packages/foundry-module/src/pair/bearer-registry.ts"
      via: "Reuses existing generateBearer(refresh=true) — no structural change to validateBearer (per RESEARCH §Q6 — refresh=true already shortens old expiresAt by 60s)"
      pattern: "generateBearer.*true"
    - from: "packages/foundry-module/src/module.ts"
      to: "packages/foundry-module/src/pair/bearer-rotation.ts"
      via: "Hooks.once('ready') calls scheduleBearerRotation() after registerReactionWatcher"
      pattern: "scheduleBearerRotation"
    - from: "packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts"
      to: "packages/foundry-module/src/write-path/handlers/*.ts + reaction-watcher.ts + bearer-rotation.ts"
      via: "End-to-end mocked round-trip exercising every Wave 1/2/3/4 deliverable"
      pattern: "ISM-W7"
---

<objective>
Wave 4: Phase 7 closure. Ship bearer rotation (24h + 60s grace) reusing the existing Plan 02 `generateBearer(refresh=true)` infrastructure (RESEARCH §Q6 — the structural building blocks already exist; Plan 07-06 adds scheduling + envelope emission only). Ship the Phase 7 write-path integration smoke harness (ISM-W7-01..08) proving the full round-trip across every handler shipped in Plans 07-02..07-05. Flip Phase 7 closed in STATE.md + ROADMAP.md; hardware-pending SC-07-01..05 carry to ADR-0005 Branch A.

Purpose: Cross-cutting closure. No new feature surface beyond bearer rotation scheduler + integration smoke + STATE/ROADMAP doc closure. The checkpoint:human-verify task lets the operator review hardware-pending SC list and issue `defer-hardware-tests` resume signal per ADR-0005 Branch A precedent (same as Phase 4a/4b/5/6).
Output: 1 new module file (bearer-rotation.ts) + 1 new integration smoke harness + extended bearer-registry.test.ts + module.ts wiring + STATE.md + ROADMAP.md commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-foundry-module-write-path/07-CONTEXT.md
@.planning/phases/07-foundry-module-write-path/07-RESEARCH.md
@.planning/phases/07-foundry-module-write-path/07-01-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-02-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-03-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-04-PLAN.md
@.planning/phases/07-foundry-module-write-path/07-05-PLAN.md
@.planning/phases/04b-overlay-slot-map-mode-toggle-adversarial-ui/04b-05-SUMMARY.md
@.planning/phases/06-r1-integration-quick-action-inv-5/06-04-SUMMARY.md
@packages/foundry-module/src/module.ts
@packages/foundry-module/src/pair/bearer-registry.ts
@packages/foundry-module/src/write-path/tool-registry.ts
@packages/foundry-module/src/write-path/handlers/index.ts

<interfaces>
<!-- Plan 02 + Plan 07-01 contracts reused. -->

From Plan 02 `packages/foundry-module/src/pair/bearer-registry.ts`:
```ts
const TTL_24H_MS = 24 * 3600 * 1000;  // private — exported as constant in Plan 07-06 for the scheduler
const GRACE_60S_MS = 60 * 1000;        // private — exported in Plan 07-06
export async function generateBearer(alias, bridgeUrl, worldId, refresh = false): Promise<BearerEntry>;
export function listBearers(): BearerEntry[];  // returns non-revoked entries sorted createdAt desc
```
Plan 07-06 ADDS exports for `TTL_24H_MS` + `GRACE_60S_MS` (additive — no breaking change). Plan 07-06 also adds `export function getActiveBearer(): BearerEntry | null` — returns `listBearers()[0]` if defined and unexpired, else null. RESEARCH §Q6 confirmed `generateBearer(refresh=true)` already correctly handles the grace window via shortened `expiresAt`.

From Plan 07-01 `packages/shared-protocol/src/payloads/tool.ts`:
```ts
export const BearerRotatedPayloadSchema = z.object({ rotatedAt: z.number().int(), graceUntil: z.number().int() }).strict();
```
Plus envelope `type: 'bearer.rotated'` (define constant in Plan 07-01 OR Plan 07-06; pick Plan 07-06 since this is where it's emitted).

From `module.ts` (Plans 07-02 + 07-04 + 07-05):
- `Hooks.once('ready')` callback already calls: `registerSocketlibHandlers`, `registerHookSubscribers(bridgeDeltaEmitter)`, `registerCanvasExtractor({ emit: ... })`, `setMultiAttackProgressEmitter(...)`, `registerReactionWatcher(...)`.
- Plan 07-06 APPENDS `scheduleBearerRotation()` call.

From Plan 07-01 audit-log + Plan 04b-05 dispatcher pattern:
- `writeAuditLog({ tool: 'bearer.rotation', payload: { ... }, idempotencyKey: <uuid>, actorId: null, result: { success: true, data: {...} }, timestamp, bearer_id: hash.slice(0,8) })` — bearer rotation events get audited too.

From Plan 04b-05 + Plan 06-04 closure precedent:
- `_human-verify` checkpoint with `defer-hardware-tests` resume signal pattern (operator-issued); not autonomous.
- Hardware-pending SC list updated in SUMMARY.md.

Integration smoke harness pattern from `packages/g2-app/src/__tests__/04b-integration-smoke.test.ts`:
- vi.stubGlobal for Foundry mocks, EventEmitter-based MockSocket, real LayerManager + ToastQueueLayer.
- ISM-NN test names: ISM-W7-01 (cast-spell happy), ISM-W7-02 (weapon-attack count=2 emits 2 progress envelopes), ISM-W7-03 (use-item), ISM-W7-04 (move-token), ISM-W7-05 (place-template request returns placementId + N templates), ISM-W7-06 (confirm-template-placement commits via createEmbeddedDocuments), ISM-W7-07 (dnd5e.preUseActivity hook fires → r1.reaction.available envelope emitted), ISM-W7-08 (drop-concentration via conc-modal tap → tool.invoke envelope → effect.delete called).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bearer rotation scheduler + bearer.rotated envelope + audit + getActiveBearer helper</name>
  <files>packages/foundry-module/src/pair/bearer-rotation.ts, packages/foundry-module/src/pair/bearer-rotation.test.ts, packages/foundry-module/src/pair/bearer-registry.ts, packages/foundry-module/src/pair/bearer-registry.test.ts, packages/foundry-module/src/module.ts, packages/foundry-module/src/module.test.ts</files>
  <behavior>
    - `bearer-registry.ts`: EXPORT existing private constants — `export const TTL_24H_MS = 24 * 3600 * 1000;` `export const GRACE_60S_MS = 60 * 1000;`. ADD `export function getActiveBearer(): BearerEntry | null { const entries = listBearers(); const first = entries[0]; if (!first) return null; if (first.expiresAt &lt;= Date.now()) return null; return first; }`. NO change to generateBearer or validateBearer (RESEARCH §Q6 — already correct).
    - `bearer-registry.test.ts`: ADD tests for `getActiveBearer` — returns first non-revoked non-expired entry; returns null when no entries; returns null when all entries expired; returns null when all entries revoked.
    - `bearer-rotation.ts`: `import { generateBearer, getActiveBearer, GRACE_60S_MS, TTL_24H_MS } from './bearer-registry.js'`. `import { writeAuditLog } from '../write-path/audit-log.js'`. `export const BEARER_ROTATED_TYPE = 'bearer.rotated' as const;` `export interface BearerRotationOptions { emit: (payload: { rotatedAt: number; graceUntil: number }) =&gt; void; clock?: () =&gt; number; /* injectable for tests */ }` `export function scheduleBearerRotation(opts: BearerRotationOptions): () =&gt; void`. Body: `const now = (opts.clock ?? Date.now); const active = getActiveBearer(); if (!active) return () =&gt; {}; // no active bearer — nothing to rotate let timer: ReturnType<typeof setTimeout> | null = null; let cancelled = false; const rotateNow = async () =&gt; { if (cancelled) return; try { const oldEntry = active; const newEntry = await generateBearer(oldEntry.alias, oldEntry.bridgeUrl, oldEntry.worldId, true); const payload = { rotatedAt: now(), graceUntil: now() + GRACE_60S_MS }; opts.emit(payload); await writeAuditLog({ tool: 'bearer.rotation', payload: { alias: oldEntry.alias }, idempotencyKey: crypto.randomUUID(), actorId: null, result: { success: true, data: payload }, timestamp: now(), bearer_id: 'rotated' }); } catch (err) { console.warn('[bearer-rotation] rotate failed', err); } finally { if (!cancelled) schedule(); } }; const schedule = () =&gt; { const a = getActiveBearer(); if (!a) return; const elapsed = now() - a.createdAt; const remaining = Math.max(0, TTL_24H_MS - elapsed); timer = setTimeout(() =&gt; { void rotateNow(); }, remaining); }; schedule(); return () =&gt; { cancelled = true; if (timer) clearTimeout(timer); };`.
    - `bearer-rotation.test.ts`: 10+ tests using `vi.useFakeTimers()` — no active bearer → cancel is no-op; setTimeout scheduled at TTL_24H_MS - elapsed (positive remaining); setTimeout scheduled at 0ms (clamp) when elapsed > TTL; after timer fires, generateBearer called with refresh=true (verify via spy); emit called with payload.rotatedAt + graceUntil; writeAuditLog called with `tool: 'bearer.rotation'`; chain continues — second rotation scheduled after first; cancel() prevents next rotation; idempotent (cancel twice safe); rotate failure logged but not thrown.
    - `module.ts`: ADD `import { scheduleBearerRotation, BEARER_ROTATED_TYPE } from './pair/bearer-rotation.js'`. In `Hooks.once('ready')` AFTER `registerReactionWatcher(...)`, add `scheduleBearerRotation({ emit: (payload) =&gt; bridgeDeltaEmitter(BEARER_ROTATED_TYPE, payload) })`. NO socketlib registration changes — count stays 14.
    - `module.test.ts`: regression — `expect(socketlibMock.registerComplexHandler).toHaveBeenCalledTimes(14)` continues passing; ADD regression test that asserts `setTimeout` (mocked via `vi.useFakeTimers`) was called at least once after `ready` fires with an active bearer in `game.settings`.
  </behavior>
  <action>Edit `packages/foundry-module/src/pair/bearer-registry.ts` exporting TTL_24H_MS + GRACE_60S_MS + adding getActiveBearer helper. Extend tests. Create `packages/foundry-module/src/pair/bearer-rotation.ts` with scheduleBearerRotation, BEARER_ROTATED_TYPE constant, recursive chain via setTimeout, generateBearer(refresh=true), audit-log entry, emit envelope, cancel closure. Test with vi.useFakeTimers covering happy path, chain, cancel, idempotency, failure recovery. Wire into module.ts ready hook AFTER existing reaction-watcher call. Update module.test.ts regression. Verify count stays 14.</action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run pair/bearer-rotation.test.ts pair/bearer-registry.test.ts module.test.ts &amp;&amp; pnpm typecheck &amp;&amp; grep -c 'registerComplexHandler' packages/foundry-module/src/pair/socketlib-handlers.ts | awk '{ if ($1 != 14) exit 1 }' &amp;&amp; ! grep -rE 'activity\.use\(' packages/g2-app packages/bridge --include='*.ts'</automated>
  </verify>
  <done>scheduleBearerRotation reuses generateBearer(refresh=true) (no validateBearer change); module.ts wires it in ready; count stays 14; all tests green; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Phase 7 write-path integration smoke harness (ISM-W7-01..08)</name>
  <files>packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts</files>
  <behavior>
    - New harness at `packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts` modelled on Phase 4b's 04b-integration-smoke + Phase 6's 06-cross-overlay-reachability patterns. Uses `vi.stubGlobal` for Foundry mocks (game, Hooks, socketlib, canvas, ChatMessage.create).
    - Each ISM-W7-NN test exercises ONE end-to-end path through the write-path stack using real `dispatchTool` + real `TOOL_REGISTRY` (after the `handlers/index.ts` side-effect import has populated it) + mocked dnd5e activity.use / tokenDoc.update / effect.delete / fromActivity / createEmbeddedDocuments.
    - **ISM-W7-01 (cast-spell happy):** Build a stub actor + item + activity (mock `use()` returns `{ id: 'cm-1' }`). Call `dispatchTool('cast-spell', { args: { actor_id, item_id, targets: [], slot_level: 3 }, idempotencyKey: uuid, bearer: 'bearer-1' })`. Assert returns `{ success: true, data: { chatCardId: 'cm-1' } }`; assert activity.use called once with `{ configure: false }`; assert audit log called once via ChatMessage.create mock with `whisper: gmIds`.
    - **ISM-W7-02 (weapon-attack multi-attack count=2):** Call `dispatchTool('weapon-attack', { args: { actor_id, item_id, targets: [], advantage: 'normal', count: 2 }, idempotencyKey, bearer })`. Assert activity.use called TWICE — first with `{ configure: false, consume: { action: true } }`, second with `{ configure: false, consume: { action: false } }`. Assert emit progress envelope called TWICE with current=1/total=2 then current=2/total=2 (use a spy on bridgeDeltaEmitter via vi.spyOn).
    - **ISM-W7-03 (use-item):** Call `dispatchTool('use-item', ...)`. Assert returns success + activity.use called once.
    - **ISM-W7-04 (move-token):** Set up `game.scenes.active` with a tokenDoc mock; call `dispatchTool('move-token', { args: { token_id: 't-1', x: 100, y: 200 }, ... })`. Assert tokenDoc.update called with `{ x: 100, y: 200 }`; assert returns success.
    - **ISM-W7-05 (place-template request):** Mock `dnd5e.canvas.AbilityTemplate.fromActivity` returning 3 templates (Magic Missile case). Call `dispatchTool('place-template', { args: { actor_id, item_id }, ... })`. Assert returns `{ success: true, data: { placementId, total: 3, templates: [...] } }` with placementId UUID.
    - **ISM-W7-06 (confirm-template-placement):** First call ISM-W7-05 to mint placementId; then call `dispatchTool('confirm-template-placement', { args: { placementId, templateIndex: 1, x: 500, y: 300 }, ... })`. Assert `canvas.scene.createEmbeddedDocuments('MeasuredTemplate', [...])` called with x=500, y=300 + user=game.user.id; assert returns success.
    - **ISM-W7-07 (reaction-watcher hook fan-out):** Register the reaction-watcher (call `registerReactionWatcher(emitSpy)`). Fire the `dnd5e.preUseActivity` hook with a stub NPC activity targeting the player. Assert emitSpy called once with `{ kind: 'shield' | 'counterspell' | 'opportunity-attack', sourceName: NPC.name, expiresAt }`; assert hook handler return value is `undefined` (NOT false).
    - **ISM-W7-08 (drop-concentration via modal-like flow):** Build stub actor with an active effect having `id: 'effect-1'` and mock `delete()`. Call `dispatchTool('drop-concentration', { args: { actor_id, effect_id: 'effect-1' }, ... })`. Assert effect.delete called once; returns success.
    - Idempotency regression in the harness: ISM-W7-01 followed by an immediate repeat with the SAME idempotencyKey + SAME bearer returns cached result; activity.use called only ONCE total. Different bearer + same idempotencyKey → activity.use called TWICE (T-03-05 closure verified end-to-end).
    - 8 tests + 2 regression tests = 10 total.
  </behavior>
  <action>Create `packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts` modelled on `04b-integration-smoke.test.ts`. Set up the vi.stubGlobal scaffolding (game, Hooks, socketlib, canvas, ChatMessage.create, foundry.applications.api.ApplicationV2, dnd5e namespace with canvas.AbilityTemplate.fromActivity mock). Side-effect import `./write-path/handlers/index.js` populates TOOL_REGISTRY. Implement 8 ISM-W7-NN tests + 2 idempotency regression tests covering every handler shipped in Waves 1-3 + bearer rotation envelope emission shape from Wave 4. Each test calls dispatchTool directly with synthetic args + asserts the expected mock interactions (activity.use, tokenDoc.update, effect.delete, createEmbeddedDocuments, ChatMessage.create) AND the expected return shape AND the audit log entry.</action>
  <verify>
    <automated>pnpm --filter @evf/foundry-module test -- --run __tests__/07-write-path-integration-smoke.test.ts &amp;&amp; pnpm test:coverage 2>&amp;1 | tail -5</automated>
  </verify>
  <done>10 ISM-W7-NN tests green; full Phase 7 write-path round-trip exercised end-to-end with mocked Foundry globals; T-03-05 cross-bearer isolation explicitly tested at integration level; pnpm test:coverage stays above 80% gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 7 closure — operator confirms hardware-pending SC carry + STATE.md/ROADMAP.md flip</name>
  <what-built>
    - bearer-rotation.ts scheduleBearerRotation reusing generateBearer(refresh=true) infra (RESEARCH §Q6 — no validateBearer change).
    - 07-write-path-integration-smoke.test.ts ISM-W7-01..ISM-W7-08 + 2 idempotency regression tests (T-03-05 cross-bearer isolation).
    - Phase 7 hardware-pending SC list:
      * SC-07-01: Real `executeAsGM` round-trip — cast-spell produces real chat card in Foundry test world.
      * SC-07-02: Real Magic Missile (target.count=3) lands 3 MeasuredTemplate documents with correct x/y placement via R1 taps.
      * SC-07-03: Fighter L5+ Extra Attack with MidiQOL autoFastForward produces 2 chat cards + chip ticks `[Atk 1/2]` → `[Atk 2/2]` on G2.
      * SC-07-04: Phase 4b conc-drop modal tap → tool.invoke → effect.delete removes concentration in real Foundry world.
      * SC-07-05: Real NPC attack fires `dnd5e.preUseActivity` → r1.reaction.available envelope → toast visible on G2 ~3s dwell.
    - Software-side ALL green: typecheck, biome, vitest coverage, snapshot drift, Gate 8 (single-workflow-origin), registerComplexHandler count = 14.
  </what-built>
  <how-to-verify>
    Operator reviews:
    1. ADR-0011 documents single-workflow-origin discipline.
    2. CI Gate 8 (.github/workflows/ci.yml) blocks `activity.use(` in g2-app + bridge sources.
    3. socketlib-handlers.ts registerComplexHandler count stays exactly 14 (renames in-place, no additions).
    4. 5 hardware-pending SC entries above are accurate + carry to ADR-0005 PROVISIONAL Branch A.
    5. STATE.md PHASE_7_CLOSED block + hardware-pending carry-forward section updated.
    6. ROADMAP.md Phase 7 checkbox flipped + plans list 07-01..07-06 populated.

    Issue `defer-hardware-tests` resume signal (same pattern as Phase 4a/4b/5/6 closures).
  </how-to-verify>
  <resume-signal>defer-hardware-tests</resume-signal>
</task>

<task type="auto">
  <name>Task 4: STATE.md PHASE_7_CLOSED + ROADMAP.md Phase 7 flip + INV-3 atomic commit</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Update `.planning/STATE.md`:
    - Frontmatter: `status: PHASE_7_CLOSED — Plans 01-06 all committed; Phase 8 (Manual Action UX) is next.`; `stopped_at: Completed 07-06-PLAN.md`; bump `last_updated`; bump `progress.completed_phases`/`completed_plans`/`percent` accordingly (treat all 6 Phase 7 plans as complete after Task 3 checkpoint resolves).
    - Add a new "## /gsd-autonomous 2026-05-16 run — Phase 7 closure" section near the bottom listing the 6 plans + 5 hardware-pending SC + the test-count delta + the registerComplexHandler stays-14 invariant + the renames (skill-check → confirmTemplatePlacement; setTargets → dropConcentration).
    - Append to the existing hardware-pending carry-forward list (under "Hardware-pending carry-forward"): the 5 Phase 7 SC entries (SC-07-01..05) producing a new running total of 23 hardware-pending SCs across Phases 4a/4b/5/6/7.

    Update `.planning/ROADMAP.md`:
    - Phase 7 entry: change `- [ ]` to `- [x]`.
    - Phase 7 details block: change `**Plans**: TBD` to `**Plans:** 6/6 plans complete` + populate the Plans list with 07-01..07-06 entries (mirror Phase 6's format: `- [x] 07-01-PLAN.md — Tool Registry + IdempotencyStore + audit-log + ADR-0011 + CI Gate 8 (Wave 0)` etc.).
    - Progress table at the bottom: `7. Foundry Module Write Path | 6/6 | Complete (hardware tests deferred — ADR-0005 Branch A `human_needed` carry) | 2026-05-16`.

    Commit message: `docs(state): close Phase 7 via defer-hardware-tests — Phase 8 unblocked` (INV-3 atomic — STATE.md + ROADMAP.md in single commit).
  </action>
  <verify>
    <automated>grep -q "PHASE_7_CLOSED" .planning/STATE.md &amp;&amp; grep -q "07-06-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -q "SC-07-05" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md PHASE_7_CLOSED with hardware-pending carry; ROADMAP.md Phase 7 `[x]` + plans list populated + progress table updated; INV-3 atomic commit (STATE + ROADMAP single commit).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| setTimeout scheduler → generateBearer(refresh=true) | Internal to module — runs in Foundry GM browser context |
| bearer.rotated envelope → g2-app | Untrusted to client; Plan 07-01 BearerRotatedPayloadSchema validates on receive (Plan 08 / Phase 8 will add g2-app side dispatcher; out of scope here — Phase 7 ships the emission side) |
| writeAuditLog for rotation events | Same trust as Plan 07-01 audit pipeline — whisper: gmIds enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | DoS | setTimeout fires while no active bearer | mitigate | `scheduleBearerRotation` returns no-op cancel when getActiveBearer returns null at boot |
| T-07-06-02 | DoS | Recursive setTimeout chain leaks timers on module reload | mitigate | cancel() returned by scheduleBearerRotation; module.ts ready hook stores cancel ref via WeakMap? — actually module never reloads mid-session in MVP; cancel is for tests only |
| T-07-06-03 | Tampering | Rotation envelope spoofed by player | mitigate | bearer.rotated envelope wrapped in canonical EnvelopeSchema with session_id; bridge attaches session_id from authenticated bearer (Phase 3 pattern) |
| T-07-06-04 | Repudiation | "I didn't know my bearer rotated" | mitigate | writeAuditLog records every rotation with rotatedAt + graceUntil + alias |
| T-07-06-05 | DoS | setTimeout precision drift under 24h schedule | accept | Browser setTimeout drift is bounded; if elapsed > TTL_24H_MS at boot (e.g., Foundry tab was suspended), remaining clamped to 0 → immediate rotation (correct behavior) |
</threat_model>

<verification>
- `pnpm --filter @evf/foundry-module test` green (including new bearer-rotation + 07-write-path-integration-smoke harness).
- `pnpm --filter @evf/shared-protocol test` green.
- `pnpm --filter @evf/g2-app test` green.
- `pnpm typecheck` green.
- `pnpm biome ci .` green.
- `pnpm test:coverage` ≥ 80% line/branch (INV-4 gate).
- `grep -c "registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` exactly 14.
- `! grep -rE 'activity\.use\(' packages/g2-app packages/bridge --include='*.ts'` (Gate 8 still empty).
- `grep -q "PHASE_7_CLOSED" .planning/STATE.md`.
- `grep -q "- \[x\] \*\*Phase 7" .planning/ROADMAP.md` (Phase 7 checkbox flipped).
- `git log -1 --name-only HEAD` shows BOTH STATE.md + ROADMAP.md in same commit (INV-3 atomic).
- `grep -c "ISM-W7-" packages/foundry-module/src/__tests__/07-write-path-integration-smoke.test.ts` ≥ 8.
- Phase 7 total test count growth ~150-200 tests across all 6 plans (rough estimate from prior phases).
- 5 hardware-pending SC declared (SC-07-01..05) — running total 18 + 5 = 23.
</verification>

<success_criteria>
1. scheduleBearerRotation reuses generateBearer(refresh=true) (RESEARCH §Q6 — no structural validateBearer change).
2. Recursive setTimeout chain rotates every 24h with 60s grace; emits bearer.rotated envelope; audits each rotation.
3. cancel() closure returned; idempotent.
4. module.ts wires scheduleBearerRotation in ready hook AFTER reaction-watcher; count stays 14.
5. 07-write-path-integration-smoke.test.ts ISM-W7-01..08 covers every Wave 1-3 handler + reaction-watcher fan-out + idempotency cross-bearer regression.
6. checkpoint:human-verify resolved via defer-hardware-tests resume signal.
7. STATE.md PHASE_7_CLOSED + hardware-pending carry updated; ROADMAP.md Phase 7 flipped + plans list 07-01..07-06 populated.
8. INV-3 atomic commit (STATE + ROADMAP single commit).
9. ADR-0011 + CI Gate 8 + 14-handler-count invariants all intact (full Phase 7 surface).
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundry-module-write-path/07-06-SUMMARY.md` documenting:
- Bearer rotation scheduler reuses Plan 02 infra (no breaking validateBearer changes per RESEARCH §Q6).
- ISM-W7-01..08 integration smoke proves the full write-path stack end-to-end with mocked Foundry globals.
- 5 hardware-pending SC carried forward (SC-07-01..05) — running total 23.
- ADR-0011 single-workflow-origin discipline intact across all 6 plans.
- registerComplexHandler count stays exactly 14 (Pitfall 7 closure preserved through all 5 stub renames).
- STATE.md + ROADMAP.md atomic INV-3 commit closes Phase 7.
- Phase 8 (Manual Action UX) unblocked.
</output>
