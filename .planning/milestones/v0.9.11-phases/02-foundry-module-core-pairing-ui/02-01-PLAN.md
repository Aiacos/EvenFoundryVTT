---
phase: 02-foundry-module-core-pairing-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/foundry-module/package.json
  - packages/foundry-module/tsconfig.json
  - packages/foundry-module/tsup.config.ts
  - packages/foundry-module/module.json
  - packages/foundry-module/src/module.ts
  - packages/foundry-module/src/settings.ts
  - packages/foundry-module/src/types/foundry-globals.d.ts
  - packages/foundry-module/lang/en.json
  - packages/foundry-module/lang/it.json
  - packages/foundry-module/src/module.test.ts
  - .changeset/02-01-foundry-module-skeleton.md
autonomous: true
requirements: [FOUN-01, I18N-01, I18N-03, CONN-03]
gap_closure: false

must_haves:
  truths:
    - "pnpm --filter @evf/foundry-module build produces dist/module.js (ESM, no errors)"
    - "module.json relationships.requires lists socketlib, midi-qol, and dnd5e with correct compatibility fields"
    - "Foundry settings panel registers the EvenFoundryVTT settings section with a Pair button stub at MODULE_ID scope"
    - "lang/en.json and lang/it.json both contain the 23 UI-A i18n keys from 02-UI-SPEC.md"
  artifacts:
    - path: "packages/foundry-module/module.json"
      provides: "Foundry module manifest"
      contains: "relationships"
    - path: "packages/foundry-module/src/module.ts"
      provides: "Foundry module entry point"
      contains: "Hooks.once"
    - path: "packages/foundry-module/src/settings.ts"
      provides: "Settings panel registration + pair button stub"
      contains: "registerSettings"
    - path: "packages/foundry-module/lang/en.json"
      provides: "English i18n catalog"
      contains: "evf.settings.pair_button"
    - path: "packages/foundry-module/lang/it.json"
      provides: "Italian i18n catalog"
      contains: "evf.settings.pair_button"
  key_links:
    - from: "packages/foundry-module/src/module.ts"
      to: "packages/foundry-module/src/settings.ts"
      via: "import { registerSettings } from './settings'"
      pattern: "registerSettings"
    - from: "packages/foundry-module/module.json"
      to: "packages/foundry-module/dist/module.js"
      via: "esmodules field"
      pattern: "\"esmodules\""
    - from: "packages/foundry-module/src/module.ts"
      to: "packages/foundry-module/lang/{en,it}.json"
      via: "languages array in module.json"
      pattern: "\"languages\""
---

<threat_model>
T-02-01 [HIGH]: Bearer token leak in logs — not yet applicable in Wave 0 (no token generation), but tsup build must NOT embed env secrets. Mitigation: tsup config uses no env injection; pino redact list scaffolded as comment with `// TODO (ADR-0002)` note.
T-02-03 [MEDIUM]: XSS surface — no user-rendered strings in Wave 0 (Pair button is a stub). Foundry ApplicationV2 escapes HTML by default.
</threat_model>

<objective>
Bootstrap the `packages/foundry-module` package from placeholder to a buildable Foundry module with a proper `module.json` manifest, tsup ESM build pipeline, settings panel registration, pair-button stub, and the two locale catalogs (EN + IT) covering all 23 UI-A i18n keys from 02-UI-SPEC.md.

Purpose: Every downstream plan (02-04) imports from or extends this skeleton. Without a valid module.json and build, Foundry will refuse to load the module and the pair modal has no mount point.

Output:
- `packages/foundry-module/dist/module.js` (ESM, produced by tsup)
- `packages/foundry-module/module.json` (production-ready manifest with relationships.requires)
- `packages/foundry-module/lang/en.json` + `lang/it.json` (23 UI-A keys)
- `packages/foundry-module/src/settings.ts` (settings registration + pair button onClick stub)
- A `.changeset/` file declaring `minor` bump on `@evf/foundry-module`
</objective>

<execution_context>
@/home/aiacos/.claude/get-shit-done/workflows/execute-plan.md
@/home/aiacos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-foundry-module-core-pairing-ui/02-CONTEXT.md
@.planning/phases/02-foundry-module-core-pairing-ui/02-UI-SPEC.md
@.planning/REQUIREMENTS.md
@packages/validation-harness/foundry-modules/midiqol-probe-module/module.json

<interfaces>
<!-- Reference shape for production module.json. Mirror structure, substitute real metadata. -->
<!-- From packages/validation-harness/foundry-modules/midiqol-probe-module/module.json -->
```json
{
  "id": "evfoundryvtt-phase-0-probe",
  "compatibility": { "minimum": "13.347", "verified": "14", "maximum": "14" },
  "esmodules": ["scripts/probe.js"],
  "relationships": {
    "requires": [{ "id": "midi-qol", "type": "module" }],
    "systems": [{ "id": "dnd5e", "type": "system", "compatibility": { "minimum": "5.3.3" } }]
  }
}
```

<!-- Phase 1 placeholder that this plan replaces -->
<!-- From packages/foundry-module/src/index.ts (current placeholder): -->
```typescript
export const PACKAGE_NAME = 'foundry-module';
```

<!-- Foundry v13+ ApplicationV2 settings registration pattern (from Foundry API docs) -->
```typescript
// In Hooks.once("init") callback:
game.settings.registerMenu("evenfoundryvtt", "pairDevice", {
  name: "evf.settings.pair_button",
  label: "evf.settings.pair_button",
  icon: "fas fa-qrcode",
  type: PairModal,           // defined in Wave 1
  restricted: true,
});
```
</interfaces>
</context>

## Task Breakdown

### Task 1: module.json + build pipeline

**Files:**
- `packages/foundry-module/package.json` — add `name: "@evf/foundry-module"`, `type: "module"`, `scripts.build: "tsup"`, `devDependencies: { tsup: "8.5.1", typescript: "5.8.3" }`, `peerDependencies: {}` (no runtime npm deps — Foundry globals are ambient)
- `packages/foundry-module/tsconfig.json` — extend `../../tsconfig.base.json`, add `compilerOptions.lib: ["ES2023", "DOM"]` for Foundry browser globals, `paths` empty (no local aliases in this package)
- `packages/foundry-module/tsup.config.ts` — entry `src/module.ts`, format `esm`, outDir `dist`, dts false (Foundry does not consume .d.ts), sourcemap true, external `[]` (all Foundry globals are ambient, not bundled)
- `packages/foundry-module/module.json` — production manifest:
  - `id: "evenfoundryvtt"`, `title: "EvenFoundryVTT"`, `version: "0.1.0"`, `compatibility: { minimum: "13.347", verified: "14" }`
  - `esmodules: ["dist/module.js"]`
  - `languages: [{ lang: "en", name: "English", path: "lang/en.json" }, { lang: "it", name: "Italiano", path: "lang/it.json" }]`
  - `relationships.requires`: socketlib `{ id: "socketlib", type: "module", compatibility: { minimum: "1.0.0" } }`, midi-qol `{ id: "midi-qol", type: "module" }` (optional: true per D-2.13 — Phase 7 gates actual usage), dnd5e system `{ id: "dnd5e", type: "system", compatibility: { minimum: "5.3.3" } }`
  - `authors: [{ name: "EvenFoundryVTT", url: "https://github.com/aiacos/EvenFoundryVTT" }]`
  - `url`, `manifest`, `download` pointing to github.com/aiacos/EvenFoundryVTT
  - `socket: true` (needed for socketlib executeAsGM registration — required by socketlib)

**Action:**
Replace the placeholder `package.json` (currently minimal) with a proper monorepo package. Create `tsup.config.ts` using the same tsup@8.5.1 already in root devDependencies. Write `module.json` mirroring the probe module structure but with production metadata. Critical: `socket: true` must be present — socketlib requires it to register the module's socket namespace. The `midi-qol` relationship has `optional: false` per D-2.13 (MIDIQ-01 evidence locked in Phase 0 — MidiQOL config check is required at boot). Do NOT add `"type": "commonjs"` — ESM output only (Foundry v13+ loads ESM natively).

**Verify:**
```bash
cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/foundry-module build && ls packages/foundry-module/dist/module.js
```

**Done:** `dist/module.js` exists, is valid ESM (starts with `import` or is an IIFE), and `module.json` contains `"socketlib"` and `"midi-qol"` in `relationships.requires`.

---

### Task 2: Module entry point + settings registration

**Files:**
- `packages/foundry-module/src/types/foundry-globals.d.ts` — ambient declarations for Foundry globals used in this wave: `declare const game: { settings: FoundrySettings; i18n: { lang: string; localize: (key: string) => string } }; declare const Hooks: { once: (event: string, fn: (...args: unknown[]) => void) => void; on: (event: string, fn: (...args: unknown[]) => void) => number }` — minimal surface, expanded per phase
- `packages/foundry-module/src/settings.ts` — `export function registerSettings(): void` that calls `game.settings.registerMenu(...)` for the pair button stub. The `type` field points to a placeholder class `PairModalStub extends Application { ... }` (replaced in Plan 02). Include JSDoc on `registerSettings`. Read `game.i18n.lang` at call time and store as module-level const `export let detectedLocale: string` (satisfies I18N-01 detection requirement at module boot).
- `packages/foundry-module/src/module.ts` — replaces current `src/index.ts` placeholder. Registers `Hooks.once("init", () => { registerSettings(); })`. Exports `MODULE_ID = "evenfoundryvtt"` as named const. No other logic in Wave 0. JSDoc on the file-level module comment.
- `packages/foundry-module/src/module.test.ts` — unit test with `happy-dom` env (vitest config already wires it). Mock `game` and `Hooks` globals. Test: (1) `registerSettings()` called exactly once in `init` hook; (2) `detectedLocale` is set to `game.i18n.lang` value; (3) `MODULE_ID === "evenfoundryvtt"`. Coverage gate: 80%+ on `settings.ts` and `module.ts`.

**Action:**
The current `src/index.ts` exports a single constant — delete that and create `src/module.ts` as the new entry. TypeScript strict mode (`noUnusedLocals`, `noUncheckedIndexedAccess` per D-1.04) means every Foundry global reference must be typed via the ambient `.d.ts`. Do NOT use `any` — define minimal ambient shapes. The `PairModalStub` is a placeholder class body with `override get title(): string { return "EVF Pair"; }` to satisfy ApplicationV2 signature — Wave 1 replaces this with the real implementation. Biome `noConsole` rule: any debug log must use `console.warn` or `console.error` with a `// biome-ignore lint/suspicious/noConsole: Foundry runtime log` comment on the line.

**Verify:**
```bash
cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm typecheck && pnpm test --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|foundry-module)"
```

**Done:** TypeScript reports 0 errors; `module.test.ts` passes all 3 assertions; coverage ≥80% on the two source files.

---

### Task 3: Locale catalogs + changeset

**Files:**
- `packages/foundry-module/lang/en.json` — 23 keys from UI-A table in 02-UI-SPEC.md: `evf.pair.modal.title`, `evf.pair.qr.scan_instruction`, ..., `evf.settings.pair_button`. Flat JSON, no nesting (Foundry i18n uses dot-notation keys in a flat object). Add `evf.settings.section_title: "EvenFoundryVTT"` as the 24th key (needed for the settings panel label).
- `packages/foundry-module/lang/it.json` — same 24 keys, Italian values from the same table.
- `.changeset/02-01-foundry-module-skeleton.md` — changeset file declaring `minor` bump on `@evf/foundry-module` and `patch` bump on `@evf/shared-protocol` (Wave 4 will add schemas there; declare now to avoid retroactive changesets). Body: "Phase 2 Wave 0: module skeleton, module.json, settings panel, locale catalogs (EN + IT)."

**Action:**
Write exactly the 24 key-value pairs per language as a flat JSON object. The Foundry i18n system loads these via the `languages` array in `module.json`. Italian copy verbatim from 02-UI-SPEC.md UI-A i18n table — do not translate independently. Validate JSON syntax (no trailing commas). The changeset file format follows the pattern in `.changeset/` from Phase 1 — check existing files for exact frontmatter format (packages listed, bump types).

**Verify:**
```bash
cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && node -e "JSON.parse(require('fs').readFileSync('packages/foundry-module/lang/en.json','utf8')); JSON.parse(require('fs').readFileSync('packages/foundry-module/lang/it.json','utf8')); console.log('JSON valid')" && grep -c '"evf\.' packages/foundry-module/lang/en.json
```

**Done:** Both JSON files parse without error; `en.json` contains ≥23 keys starting with `evf.`; `.changeset/` file exists with `@evf/foundry-module: minor`.

---

<verification>
Overall Wave 0 checks:
1. `pnpm --filter @evf/foundry-module build` exits 0 and produces `dist/module.js`
2. `pnpm typecheck` exits 0 (zero TS errors across workspace)
3. `pnpm test` exits 0 with all foundry-module tests passing
4. `pnpm lint:ci` exits 0 (no Biome warnings or errors)
5. `module.json` contains `"socket": true` and both `socketlib` and `midi-qol` in `relationships.requires`
6. Both lang JSON files parse valid and contain ≥23 `evf.` keys
7. `.changeset/` file present
</verification>

<success_criteria>
- `dist/module.js` produced by tsup, valid ESM
- `module.json` passes Foundry module manifest validation shape (compatibility, esmodules, relationships, socket)
- All 24 i18n keys present in both EN and IT catalogs
- Vitest coverage ≥80% on new source files
- `pnpm lint:ci && pnpm typecheck && pnpm test` all exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundry-module-core-pairing-ui/02-01-SUMMARY.md` with:
- Files created/modified
- Key decisions made (file structure chosen, PairModalStub shape, ambient .d.ts scope)
- Interface exports from `src/module.ts` and `src/settings.ts` that Plans 02 and 04 will import
- Coverage report snippet
- Any deviation from this plan and rationale
</output>
