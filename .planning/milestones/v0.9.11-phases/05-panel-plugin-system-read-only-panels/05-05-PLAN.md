---
phase: 05
plan: 05
type: execute
wave: 3
subsystem: g2-app
tags: [g2-app, panels, combat-tracker, log, comb-01, comb-03, concentration-subline, quick-action-bar, inv-1, fixtures]
depends_on: [05-01, 05-02]
files_modified:
  - packages/g2-app/src/panels/combat-tracker-panel.ts
  - packages/g2-app/src/panels/log-panel.ts
  - packages/g2-app/src/panels/__tests__/combat-tracker-panel.test.ts
  - packages/g2-app/src/panels/__tests__/log-panel.test.ts
  - packages/shared-protocol/src/payloads/log.ts
  - packages/shared-protocol/src/__tests__/payloads/log.test.ts
  - packages/foundry-module/src/readers/log-reader.ts
  - packages/foundry-module/src/readers/__tests__/log-reader.test.ts
  - packages/shared-render/src/fixtures/combat-tracker.full-window.it.txt
  - packages/shared-render/src/fixtures/combat-tracker.partial.it.txt
  - packages/shared-render/src/fixtures/combat-tracker.single.it.txt
  - packages/shared-render/src/fixtures/combat-tracker.no-combat.it.txt
  - packages/shared-render/src/fixtures/log.standard.it.txt
  - packages/shared-render/src/fixtures/log.empty.it.txt
autonomous: true
requirements: [COMB-01, COMB-03]
gap_closure: false

requires:
  - phase: 05-01 (HUD_WIDTH_BUDGETS Phase 5 combat + log keys + CombatantSchema.concentration sub-fields)
  - phase: 05-02 (PanelRouter discovery + Strategy A pattern via CharacterSheetPanel exemplar)
  - phase: 04b (OverlayPanel + PanelGestureBus + CombatSnapshot existing schema + concentration sub-line schema from 05-01)
provides:
  - CombatTrackerPanel class — standalone OverlayPanel rendering UI-SPEC §5.8 (5-row sliding window + concentration sub-line + quick-action bar)
  - LogPanel class — standalone OverlayPanel rendering UI-SPEC §5.9 (event log tail with filter bar + roll results)
  - computeWindow(turns, currentTurnIndex, scrollOffset) — pure helper for 5-row windowing algorithm per RESEARCH §Pattern 4
  - renderCombatantRow(combatant, locale): string[] — produces 1-2 rows (main row + optional concentration sub-line)
  - renderQuickActionBar(activeIdx, locale): string — Phase 5 render-only `[A][S][I][M]` bar (UI-SPEC §5.8 footer)
  - renderLogEvent(event, locale): string[] — produces 1-2 rows per UI-SPEC §5.9 (timestamp + actor + icon + description + result sub-line)
  - 6 INV-1 fixtures — combat tracker (full-window + partial + single + no-combat) + log (standard + empty)
  - LogEventSchema (Zod) — new shared-protocol schema for chat log events
  - log-reader.ts (foundry-module) — produces log event snapshots from `game.messages.contents`
affects:
  - 05-06 integration smoke — composes Combat + Log + CharacterSheet + Inventory + Spellbook via PanelRouter

tech-stack:
  added: []
  patterns:
    - "5-row windowing algorithm — pure function `computeWindow(turns, currentIndex, scrollOffset)` per RESEARCH §Pattern 4 with explicit edge cases (1, 2-4, 5, mid, first, last combatants)"
    - "Concentration sub-line — appended UNDER the affected combatant row when `combatant.concentration` is non-null; column-aligned indent (22 spaces) per UI-SPEC §5.8 verbatim"
    - "Quick-action bar render-only — `[ A ]ttacco [ S ]pell [ I ]tem [ M ]ovi` row at footer; Phase 5 produces the static layout, Phase 6 wires the tap-cycle highlight (CONTEXT.md §Area 4 + COMB-03)"
    - "Log event row format — `T+HH:MM  ACTOR  icon description` main row + optional `→ result-line` sub-row (UI-SPEC §5.9 column layout)"

must_haves:
  truths:
    - "CombatTrackerPanel.meta passes PanelMetaSchema (id 'combat-tracker', navKey 'C', requiredCaps [])"
    - "LogPanel.meta passes PanelMetaSchema (id 'log', navKey 'L', requiredCaps [])"
    - "Both panels return getContainerCount image=0, text=1"
    - "computeWindow handles 5 edge cases: 1 combatant (window=1), 2-4 (window=all), 5 (window=all), current=0 (top-anchored), current=last (bottom-anchored), mid (centered)"
    - "Concentration sub-line appears under exactly the combatants with `concentration` field; format `<22-space-indent>conc:<SpellName-12> <duration-6>`"
    - "Quick-action bar row uses i18n keys `combat.tracker.quick_label` + `combat.tracker.quick_attack/spell/item/move`"
    - "Log panel renders T±HH:MM relative timestamps from the most recent event back; scrollOffset shifts the visible window"
    - "Empty states render fixtures with `combat.empty` / `log.empty` i18n strings"
  artifacts:
    - path: packages/g2-app/src/panels/combat-tracker-panel.ts
      provides: CombatTrackerPanel class + computeWindow + renderCombatantRow + renderQuickActionBar
      exports: ["CombatTrackerPanel (default)", "computeWindow", "renderCombatantRow", "renderQuickActionBar"]
      min_lines: 250
    - path: packages/g2-app/src/panels/log-panel.ts
      provides: LogPanel class + renderLogEvent + renderLogFilterBar
      exports: ["LogPanel (default)", "renderLogEvent", "renderLogFilterBar"]
      min_lines: 200
    - path: packages/shared-protocol/src/payloads/log.ts
      provides: LogEventSchema + LogEventTypeSchema enum
      exports: ["LogEventSchema", "LogEvent"]
      min_lines: 40
    - path: packages/foundry-module/src/readers/log-reader.ts
      provides: getLogEventTail(maxCount: number = 50): LogEvent[]
      exports: ["getLogEventTail"]
      min_lines: 50
    - path: packages/shared-render/src/fixtures/combat-tracker.full-window.it.txt
      provides: full 5-row window with current turn centered + concentration sub-lines
      contains: "COMBAT TRACKER"
    - path: packages/shared-render/src/fixtures/combat-tracker.partial.it.txt
      provides: 3-combatant window (less than full 5)
      contains: "COMBAT TRACKER"
    - path: packages/shared-render/src/fixtures/combat-tracker.single.it.txt
      provides: 1-combatant edge case
      contains: "COMBAT TRACKER"
    - path: packages/shared-render/src/fixtures/combat-tracker.no-combat.it.txt
      provides: empty state with `combat.empty` IT message
      contains: "Nessun combattimento"
    - path: packages/shared-render/src/fixtures/log.standard.it.txt
      provides: log with mixed event types
      contains: "REGISTRO"
    - path: packages/shared-render/src/fixtures/log.empty.it.txt
      provides: empty state with `log.empty` IT message
      contains: "Nessun evento"
  key_links:
    - from: packages/g2-app/src/panels/combat-tracker-panel.ts
      to: CombatSnapshot.combatants[].concentration (extended by 05-01)
      via: renderCombatantRow conditional sub-line
      pattern: "concentration"
    - from: packages/g2-app/src/panels/combat-tracker-panel.ts
      to: computeWindow
      via: 5-row sliding window for initiative list
      pattern: "computeWindow"
    - from: packages/foundry-module/src/readers/log-reader.ts
      to: game.messages.contents
      via: chat-message collection iteration
      pattern: "game\\.messages"

metrics:
  duration: TBD
  completed: TBD
  tasks: 3
  task_commits: TBD
  files_created: 11
  files_modified: 0
  tests_added: TBD
---

<objective>
Ship the CombatTrackerPanel (COMB-01 + COMB-03) and LogPanel — two standalone OverlayPanels auto-discovered by PanelRouter. Both render their respective Foundry-source data with i18n width-budget enforcement + INV-1 fixture coverage. The CombatTracker carries the 5-row windowing algorithm + concentration sub-line + quick-action bar; the Log carries the chat-message tail rendering. This plan runs **in parallel with 05-04** in wave 3 — zero file overlap (different panel files, different fixtures, different schema files).

Purpose: close COMB-01 (combat tracker visible + concentration sub-line) + COMB-03 (quick-action bar render-only — Phase 6 wires gestures) end-to-end with INV-1 fidelity.

Output:
- combat-tracker-panel.ts (panel + windowing algorithm + quick-action bar)
- log-panel.ts (panel + event row renderer)
- log.ts shared-protocol schema + log-reader.ts foundry-module reader (atomic schema + reader fan-out)
- 6 INV-1 fixtures
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-VALIDATION.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-01-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-02-PLAN.md
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/panels/character-sheet-panel.ts
@packages/g2-app/src/engine/overlay-panel.ts
@packages/g2-app/src/engine/layer-types.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/shared-protocol/src/payloads/combat.ts
@packages/foundry-module/src/readers/combat-reader.ts

<interfaces>
From 05-01 (atomic extensions):
- `CombatantSchema.concentration: { spellName: string; duration: string } | undefined`
- `combat.tracker.*` HUD keys: panel_title, effects_section, you_marker, party_label, quick_label, quick_attack, quick_spell, quick_item, quick_move, hp_label, ac_label
- `log.*` HUD keys: panel_title, filter.{all,rolls,damage,status,chat}, result.{hit,miss,pass,fail}, concentrating, round_marker, scroll_hint, empty
- `panel.title.{combat,log}` and `footer.hint.{combat,log}` HUD keys

From `packages/shared-protocol/src/payloads/combat.ts`:
- CombatSnapshot { combatId, round, turn, currentCombatantId, combatants: Combatant[] }
- Combatant { id, name, actorId, initiative, hp, maxHp, isCurrentTurn, concentration? }

From `packages/foundry-module/src/readers/combat-reader.ts`:
- `getCombatSnapshot(): CombatSnapshot | null` (returns null when game.combat is null)

For the Log feature, the existing shared-protocol does NOT yet have a `log` payload. This plan creates `packages/shared-protocol/src/payloads/log.ts` with `LogEventSchema` and the matching reader `packages/foundry-module/src/readers/log-reader.ts` — same atomic pattern as combat + character.

LogEvent shape per UI-SPEC §5.9:
- `id: string` (Foundry message ID)
- `timestamp: number` (epoch ms; renderer computes T±HH:MM relative to "now")
- `actorName: string` (max 10 chars; truncate with …)
- `kind: 'attack' | 'damage' | 'spell' | 'feature' | 'round' | 'concentration' | 'roll' | 'chat'`
- `description: string` (max 40 chars; truncate)
- `result: { kind: 'hit'|'miss'|'pass'|'fail'|'concentrating'; value?: number; damage?: string } | undefined` (sub-line content)

Reader: iterate `game.messages.contents.slice(-N)` (latest N messages); map each ChatMessage to a LogEvent. Distinguishing message kinds requires inspecting `message.flags.dnd5e.*` and `message.rolls` per dnd5e 5.x conventions. Defensive: unknown message kinds default to `kind: 'chat'`. RESEARCH does not pre-research this — flag as Assumption A4 in the SUMMARY; verify dnd5e ChatMessage flag shape at execution time.
</interfaces>

<ui_fixtures>
Each fixture is 19-row × 70-code-point body (rows 3-21).

Per UI-SPEC.md verbatim:
- §5.8 Combat tracker full-window (5 combatants + 2 concentration sub-lines + quick-action bar footer)
- Variant fixtures: partial (3 combatants — less than 5 window; no scroll padding); single (1 combatant edge case); no-combat (empty state with `combat.empty` IT message centered)
- §5.9 Log standard (10 event rows); empty (centered `log.empty` IT message)

Every row exactly 70 code-points wide.
</ui_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CombatTrackerPanel + computeWindow + renderCombatantRow + renderQuickActionBar + 4 fixtures</name>
  <files>packages/g2-app/src/panels/combat-tracker-panel.ts, packages/g2-app/src/panels/__tests__/combat-tracker-panel.test.ts, packages/shared-render/src/fixtures/combat-tracker.full-window.it.txt, packages/shared-render/src/fixtures/combat-tracker.partial.it.txt, packages/shared-render/src/fixtures/combat-tracker.single.it.txt, packages/shared-render/src/fixtures/combat-tracker.no-combat.it.txt</files>
  <action>
Create `combat-tracker-panel.ts` exporting:
- Default `class CombatTrackerPanel implements OverlayPanel` with `static meta: PanelMeta = { id: 'combat-tracker', title: { it: 'Combat', en: 'Combat', de: 'Kampf' }, navKey: 'C', requiredCaps: [] }`. Constructor + lifecycle per ConcentrationDropModalPanel exemplar verbatim.
- `computeWindow(turns: Combatant[], currentTurnIndex: number, scrollOffset: number): Combatant[]` — pure helper per RESEARCH §Pattern 4 verbatim:
  - N=0 → []
  - N≤5 → all turns
  - else: targetCenter = clamp(currentTurnIndex + scrollOffset, 2, N-3); start = max(0, targetCenter-2); end = min(N, start+5); adjustedStart = max(0, end-5); return turns.slice(adjustedStart, end).
- `renderCombatantRow(c: Combatant, locale: HudLocale, isYouCombatant: boolean): string[]` — produces 1-2 rows (main + optional concentration sub-line). Main row column layout per UI-SPEC §5.8 verbatim (66-char inner content):
  - cols 0-3: initiative right-aligned (4 chars)
  - cols 4-5: 2-space separator
  - cols 6-7: current-turn marker `▶ ` or `  `
  - cols 8-25: name + YOU marker (18 chars total — see UI-SPEC §5.8 YOU-marker budget note: name 18 chars without YOU; name 12 + 2-space + `◀ TU`/`◀ YOU`/`◀ DU` (4 chars) with YOU)
  - cols 26-27: HP label (locale-resolved `combat.hp_label`)
  - col 28: space
  - cols 29-36: HP bar (8 chars: `█` + `░`)
  - col 37: space
  - cols 38-42: HP value `N/M` right-justified 5 chars
  - cols 43-44: 2-space gap
  - cols 45-46: AC label (locale-resolved `combat.ac_label`)
  - col 47: space
  - cols 48-50: AC value right-justified 3 chars
  - cols 51-52: 2-space gap
  - cols 53-58: distance + direction (6 chars; e.g., `30m NE`)
  - cols 59-61: 3-space gap
  - col 62: faction glyph `★` party / `✕` enemy
  - cols 63-65: 3-space trailing pad
- Concentration sub-line (when `c.concentration !== undefined`): `<22 spaces>conc:<spellName-trunc12> <duration-trunc6>` (UI-SPEC §5.8 sub-line format verbatim).
- `renderQuickActionBar(locale: HudLocale): string` — 66-char footer row per UI-SPEC §5.8: `  Rapida:  [ A ]ttacco  [ S ]pell  [ I ]tem  [ M ]ovi` (with locale-resolved suffix strings via `getLabel('combat.tracker.quick_*', locale)`).
- `renderCombatTrackerContent(snapshot: CombatSnapshot | null, locale: HudLocale, scrollOffset: number, ownActorId: string): string[]` — 18-row body: 5 combatant blocks (each 1-2 rows) + Active effects section + Quick-action footer. Handle null snapshot → render empty state with `getLabel('combat.empty', locale)` centered.

Lifecycle per exemplar: onMount subscribes to gestureBus + WS combat.turn delta channel (Phase 5 stub — Phase 6 wires R1 source provider; for this plan, panel.onSnapshot is the external entry point); onUnmount unsubscribes; onEvent dispatch — tap: cycle quick-action highlight (Phase 6 wires execution — Phase 5 no-op); scroll: window slide (`scrollOffset += direction === 'down' ? 1 : -1`); double-tap: close stub; long-press: stub.

Reset `scrollOffset = 0` on every combat.delta with a different `currentCombatantId` (RESEARCH Open Question 4 resolution — turn-advance resets window).

Tests (CTP-* discriminator markers — 25+ tests):
- CTP-META-1/2: meta passes schema; navKey 'C'.
- CTP-CTOR-1/2: OverlayPanel + container count.
- CTP-WINDOW-EMPTY: computeWindow([], 0, 0) returns [].
- CTP-WINDOW-1: computeWindow([c1], 0, 0) returns [c1].
- CTP-WINDOW-3: 3 combatants → all 3.
- CTP-WINDOW-5: 5 combatants → all 5.
- CTP-WINDOW-MID: 8 combatants, current=4 → [c2,c3,c4,c5,c6] (centered).
- CTP-WINDOW-TOP-ANCHORED: 8 combatants, current=0 → [c0,c1,c2,c3,c4].
- CTP-WINDOW-BOTTOM-ANCHORED: 8 combatants, current=7 → [c3,c4,c5,c6,c7].
- CTP-WINDOW-SCROLL: 8 combatants, current=4, scroll=2 → window shifted forward.
- CTP-ROW-WIDTH: renderCombatantRow main row is 66 code-points.
- CTP-ROW-YOU-MARKER: combatant with `actorId === ownActorId` renders `◀ TU` (IT) + name capped to 12 chars.
- CTP-ROW-NO-YOU: combatant without match renders name capped to 18 chars, no marker.
- CTP-ROW-FACTION-PARTY: combatant with party tag (or alternative source) renders `★`; enemy renders `✕`. Document the source field at execution.
- CTP-ROW-HP-BAR: HP 5/15 renders 3-█ + 5-░ (or similar fraction).
- CTP-ROW-CONC-SUBLINE-1: combatant with concentration produces a 2-row output; sub-line indent 22 spaces + `conc:<name>` + ` <duration>`.
- CTP-ROW-CONC-SUBLINE-WIDTH: sub-line is exactly 66 code-points.
- CTP-ROW-NO-CONC: combatant without concentration produces 1 row only.
- CTP-QUICK-BAR-IT: renderQuickActionBar('it') contains `Rapida:` + `[ A ]ttacco` + etc.
- CTP-QUICK-BAR-EN: 'en' variant uses `Quick:` + `[ A ]ttack`.
- CTP-QUICK-BAR-WIDTH: bar row is exactly 66 code-points.
- CTP-CONTENT-EMPTY: renderCombatTrackerContent(null, 'it', 0, '') returns 18 rows including `Nessun combattimento attivo` centered.
- CTP-CONTENT-FULL: with 5 combatants returns 18 rows, includes quick-action footer.
- CTP-FIX-FULL: matchAsciiFixture against combat-tracker.full-window.it.txt (5 combatants + 2 conc sub-lines + quick bar).
- CTP-FIX-PARTIAL: matchAsciiFixture against combat-tracker.partial.it.txt (3 combatants).
- CTP-FIX-SINGLE: matchAsciiFixture against combat-tracker.single.it.txt (1 combatant).
- CTP-FIX-NO-COMBAT: matchAsciiFixture against combat-tracker.no-combat.it.txt (empty state).
- CTP-SCROLL-RESET: panel.onSnapshot with a different currentCombatantId resets scrollOffset to 0.

Fixtures: 4 INV-1 files per UI-SPEC §5.8 (verbatim full-window — 5 combatants with the GOBLIN ARCHER current turn + THORIN+LYRA concentrating; partial; single; no-combat empty state).

Commit message: feat(05-05): CombatTrackerPanel + computeWindow + concentration sub-line + quick-action bar + 4 fixtures (COMB-01/03)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test combat-tracker-panel</automated>
  </verify>
  <done>
CombatTrackerPanel ships; 5-row windowing verified across 6 edge cases; concentration sub-line renders correctly; quick-action bar uses i18n keys per locale; 4 INV-1 fixtures pass matchAsciiFixture; all 25+ CTP-* tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: LogPanel + renderLogEvent + LogEventSchema + log-reader.ts + 2 fixtures</name>
  <files>packages/g2-app/src/panels/log-panel.ts, packages/g2-app/src/panels/__tests__/log-panel.test.ts, packages/shared-protocol/src/payloads/log.ts, packages/shared-protocol/src/__tests__/payloads/log.test.ts, packages/foundry-module/src/readers/log-reader.ts, packages/foundry-module/src/readers/__tests__/log-reader.test.ts, packages/shared-render/src/fixtures/log.standard.it.txt, packages/shared-render/src/fixtures/log.empty.it.txt</files>
  <action>
Create the LogPanel + supporting schema + reader (atomic schema-reader-renderer pattern, Phase 4b Plan 06 precedent).

Schema (`packages/shared-protocol/src/payloads/log.ts`):
- `LogEventKindSchema = z.enum(['attack', 'damage', 'spell', 'feature', 'round', 'concentration', 'roll', 'chat'])`
- `LogEventResultSchema = z.object({ kind: z.enum(['hit','miss','pass','fail','concentrating']), value: z.number().int().optional(), damage: z.string().optional() })`
- `LogEventSchema = z.strictObject({ id: z.string().min(1), timestamp: z.number().int().nonnegative(), actorName: z.string().min(1), kind: LogEventKindSchema, description: z.string(), result: LogEventResultSchema.optional() })`
- `type LogEvent = z.infer<typeof LogEventSchema>`
- `LogSnapshotSchema = z.strictObject({ events: z.array(LogEventSchema) })` (envelope-friendly shape)
- `export const LOG_DELTA_TYPE = 'log.delta' as const` (matches existing combat.turn / character.delta convention)

Reader (`packages/foundry-module/src/readers/log-reader.ts`):
- `getLogEventTail(maxCount: number = 50): LogEvent[]` — iterate `game.messages.contents.slice(-maxCount)`. Map each `ChatMessage` to a LogEvent. Distinguish kinds by inspecting:
  - Attack rolls: `message.flags?.dnd5e?.roll?.type === 'attack'` → kind 'attack' + result {kind: hit/miss, value: rollTotal}
  - Damage rolls: `dnd5e.roll.type === 'damage'` → kind 'damage' + result.damage
  - Spell cast: `dnd5e.use.type === 'spell'` → kind 'spell'
  - Feature use: `dnd5e.use.type === 'feat'` → kind 'feature'
  - Saving throw: `dnd5e.roll.type === 'save'` → kind 'roll' + result {kind: pass/fail}
  - Round marker (synthetic — generated by combat-reader at round-advance, NOT a ChatMessage): handled separately if combat-reader pushes synthetic markers
  - Unknown → kind 'chat'
  - Actor name from `message.speaker.alias || message.alias`; defensive empty string if missing.
- RESEARCH does not pre-verify exact flag paths — this is Assumption A4. Document at execution.

Panel (`packages/g2-app/src/panels/log-panel.ts`):
- Default `class LogPanel implements OverlayPanel` with `static meta: PanelMeta = { id: 'log', title: { it: 'Registro', en: 'Log', de: 'Protokoll' }, navKey: 'L', requiredCaps: [] }`. Lifecycle per exemplar.
- `renderLogEvent(event: LogEvent, locale: HudLocale, nowEpoch: number): string[]` — 1-2 rows per UI-SPEC §5.9:
  - Main row: `T±HH:MM  <ACTOR-10>  <icon> <description-40>` (66 chars)
  - Optional result sub-line (when `event.result !== undefined`): `             → <result-line-53>` (66 chars, indent 13)
  - Icon mapping: attack=`⚔` feature=`✦` spell=`✧` other=`—`
  - Timestamp: relative to `nowEpoch`; format `T-MM:SS` for past, `T+MM:SS` for future (rare); use Math.floor((nowEpoch - event.timestamp) / 1000) seconds → format.
  - Actor name truncate to 10 chars with `…`.
  - Description truncate to 40 chars with `…`.
  - Result line: e.g., `→ 23 vs CA 13   COLPITO     12 taglio` per UI-SPEC §5.9; locale-resolved `log.result.hit` etc.
- `renderLogFilterBar(activeFilter: 'all'|'rolls'|'damage'|'status'|'chat', locale): string` — filter bar row per UI-SPEC §5.9 (active filter `[▶ALL]` style).
- `renderLogContent(snapshot: LogSnapshot | null, locale, scrollOffset, nowEpoch): string[]` — 18-row body: filter bar + N log event rows (event rows are 1-2 each; total event rows clamp to fit budget; scrollOffset shifts the window). Empty state: 18 rows including `log.empty` centered.

Lifecycle methods per exemplar; onEvent dispatches: tap: toggle result sub-line visibility (Phase 5 simple — re-render); scroll: cycle log offset; double-tap: close stub; long-press: stub.

Tests (LP-* + LE-* + LR-* discriminator markers — 25+ tests):
- LE-SCHEMA-1: LogEventSchema.parse on minimal valid event succeeds.
- LE-SCHEMA-2: missing required field FAILS.
- LE-SCHEMA-3: result optional — event without result parses.
- LR-EMPTY: getLogEventTail with empty messages returns [].
- LR-MAP-ATTACK: a mock chat message with `flags.dnd5e.roll.type === 'attack'` → kind 'attack' + result.
- LR-MAP-CHAT: unknown flag → kind 'chat'.
- LR-COUNT: maxCount=10 limits the returned array.
- LP-META-1/2: panel meta + navKey 'L'.
- LP-CTOR-1/2: OverlayPanel + container count.
- LP-RENDER-MAIN-ROW: `renderLogEvent(event, 'it', now)` produces a 66-char main row + optional sub-line.
- LP-RENDER-TIMESTAMP-RECENT: timestamp = now-30s → `T-00:30`.
- LP-RENDER-TIMESTAMP-MIN: timestamp = now-90s → `T-01:30`.
- LP-RENDER-ACTOR-TRUNC: actorName 15 chars → truncated to 10 with `…`.
- LP-RENDER-ICON-ATTACK: kind 'attack' → `⚔` glyph.
- LP-RENDER-ICON-SPELL: kind 'spell' → `✧`.
- LP-RENDER-RESULT-HIT: result.kind 'hit' → IT row contains `COLPITO`.
- LP-RENDER-RESULT-MISS: result.kind 'miss' → IT row contains `MANCATO`.
- LP-RENDER-NO-RESULT: event without result → only 1 row returned.
- LP-FILTER-BAR-WIDTH: filter bar exactly 66 code-points.
- LP-FILTER-BAR-ACTIVE: active filter cell has `▶` prefix.
- LP-CONTENT-EMPTY: `renderLogContent(null, 'it', 0, now)` returns 18 rows with `Nessun evento nel registro` centered.
- LP-CONTENT-FULL: 10 events → 18 rows including filter bar + event rows.
- LP-FIX-STANDARD: matchAsciiFixture against log.standard.it.txt.
- LP-FIX-EMPTY: matchAsciiFixture against log.empty.it.txt.

Fixtures: 2 INV-1 files per UI-SPEC §5.9 (verbatim — standard with the THORIN/GOBLIN/LYRA event mix from the mockup; empty state). Use a fixed `nowEpoch = 1715798400000` (2024-05-15T16:00:00Z) so the T±HH:MM offsets are deterministic.

Commit message: feat(05-05): LogPanel + LogEventSchema + log-reader + 2 fixtures (read-only Foundry chat tail)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test log-panel && pnpm --filter @evf/shared-protocol test log && pnpm --filter @evf/foundry-module test log-reader</automated>
  </verify>
  <done>
LogPanel ships; LogEventSchema atomic with reader; chat-message mapping handles 7+ kinds with chat fallback; T±HH:MM timestamp renders correctly; 2 INV-1 fixtures pass matchAsciiFixture; all 25+ tests green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Audit + ratify wave-3 parallelism guarantees (no shared file with 05-04)</name>
  <files></files>
  <action>
This is a manual audit task (no file changes) — verify before commit that this plan's files_modified list has ZERO overlap with 05-04's files_modified list. Run:

```
grep -h "^  - " .planning/phases/05-panel-plugin-system-read-only-panels/05-04-PLAN.md | grep -v "^  - phase:" | sort > /tmp/05-04.files
grep -h "^  - " .planning/phases/05-panel-plugin-system-read-only-panels/05-05-PLAN.md | grep -v "^  - phase:" | sort > /tmp/05-05.files
comm -12 /tmp/05-04.files /tmp/05-05.files
```

The intersection MUST be empty. If any file overlaps (likely candidates: `packages/shared-protocol/src/payloads/character.ts` or `packages/foundry-module/src/readers/character-reader.ts` because 05-04 extends them for inventory + spells), THIS plan documents that 05-05 does NOT modify those files — combat schema and log schema are SEPARATE files (`combat.ts` for combatant.concentration touched by 05-01, NOT this plan; `log.ts` is NEW in this plan).

If the audit fails, surface a `## PLANS BLOCKED` signal to the orchestrator. Otherwise, document the wave-3 parallel readiness in the SUMMARY.

No commit for this task — it is a planning-time audit only.
  </action>
  <verify>
    <automated>true</automated>
  </verify>
  <done>
Audit complete; zero file overlap with 05-04 confirmed; wave-3 parallel run is safe. Document the audit result in 05-05-SUMMARY.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game.messages → LogEvent wire | Foundry-side chat messages crossed to wire; reader filters + Zod gates wire payload |
| Combatant.concentration (extended by 05-01) → renderCombatantRow | Optional sub-object on the wire; renderer treats undefined as "no sub-line" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Malformed LogEvent wire shape | mitigate | LogEventSchema strict object; reader produces only valid events |
| T-05-05-02 | Information disclosure | Chat message containing sensitive metadata | accept | MVP single-tenant; chat is already shown to the player via Foundry; G2 mirrors it |
| T-05-05-03 | DoS | game.messages pathological size (>10k entries) | mitigate | log-reader caps at maxCount=50 by default; scroll windowing within panel |
</threat_model>

<verification>
- pnpm typecheck exits 0
- pnpm lint:ci exits 0
- pnpm test workspace-wide passes; ≥50 new tests added (CTP + LE + LR + LP families)
- All 6 fixture files exist; matchAsciiFixture round-trip green
- 5-row windowing algorithm verified across 7 edge cases (empty, 1, 3, 5, mid, top, bottom, scroll)
- Wave-3 parallelism: ZERO file overlap with 05-04 (audit task 3)
</verification>

<success_criteria>
- 6 panels total now exist for Phase 5: ConcDropModal (Phase 4b) + CharSheet + Inventory + Spellbook + CombatTracker + Log
- Concentration sub-line renders correctly under affected combatants (RESEARCH §Pattern 4 assumption A2/A3 verified at execution)
- Quick-action bar visible at Combat panel footer (COMB-03 render-only — Phase 6 wires)
- LogEventSchema + reader complete the Foundry-side read surface for Phase 5
- Wave-3 parallelism intact (zero file overlap with 05-04)
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-05-SUMMARY.md` documenting: 3 task commits, files created (incl. new log.ts schema + log-reader.ts), deviations (esp. RESEARCH A2/A3/A4 — concentration flag + duration + Foundry ChatMessage flag shapes), Wave-3 parallel completion signal, Self-Check table.
</output>
