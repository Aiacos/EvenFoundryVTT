---
phase: 05
plan: 03
type: execute
wave: 2
subsystem: g2-app
tags: [g2-app, panels, character-sheet, tab-renderers, sheet-02, sheet-03, dual-edition, modern-rules, inv-1, fixtures]
depends_on: [05-01, 05-02]
files_modified:
  - packages/g2-app/src/panels/character-sheet-tab-renderers.ts
  - packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts
  - packages/g2-app/src/panels/character-sheet-panel.ts
  - packages/shared-render/src/fixtures/sheet.main.2014.it.txt
  - packages/shared-render/src/fixtures/sheet.main.2024.it.txt
  - packages/shared-render/src/fixtures/sheet.skills.it.txt
  - packages/shared-render/src/fixtures/sheet.feats.2014.it.txt
  - packages/shared-render/src/fixtures/sheet.feats.2024.it.txt
  - packages/shared-render/src/fixtures/sheet.bio.it.txt
autonomous: true
requirements: [SHEET-02, SHEET-03]
gap_closure: false

requires:
  - phase: 05-01 (HUD_WIDTH_BUDGETS Phase 5 keys + CharacterSnapshotSchema.world.modernRules + widened HudLocale)
  - phase: 05-02 (CharacterSheetPanel skeleton with _renderTabContentStub injection point + TABS / TAB_LABELS / buildTabStrip exported)
provides:
  - character-sheet-tab-renderers.ts module exporting `renderTabContent(tab, snapshot, locale, scrollOffset)` plus per-tab pure renderers (`renderMainTab`, `renderSkillsTab`, `renderFeatsTab`, `renderBioTab` — Inventory and Spells tabs are owned by 05-04)
  - Dual-edition rendering branches per RESEARCH §Pattern 3 — `[M]` weapon-mastery flag on Main attack list (modernRules=true), `[Origin]` feat origin column (Feats tab, modernRules=true)
  - 6 INV-1 fixtures — sheet.main.2014.it.txt + sheet.main.2024.it.txt + sheet.skills.it.txt + sheet.feats.2014.it.txt + sheet.feats.2024.it.txt + sheet.bio.it.txt (full 19-row panel body per UI-SPEC §5.2-§5.7)
affects:
  - 05-04 (Inv + Spellbook panels) — owns the Inventory + Spells sheet-tab renderers as a separate concern (both panels CharacterSheet tab + standalone panels)
  - 05-06 (integration smoke) — composes CharacterSheetPanel + Wave-2 panels and asserts ROM end-to-end

tech-stack:
  added: []
  patterns:
    - "Per-tab pure-function renderers — each tab's render is a free function (snapshot, locale, scrollOffset) → string[]; reusability across CharacterSheetPanel tabs and the standalone InventoryPanel / SpellbookPanel"
    - "Dual-edition conditional inside render method — single function per tab, branches off `snapshot.world.modernRules` (no per-edition class split per CONTEXT.md §Area 3)"
    - "Hot-swap re-render on snapshot delta — panel.onSnapshot(newSnapshot) calls draw() which re-runs the renderer; no LayerManager.bundle invocation = no remount/flicker (RESEARCH §Pattern 3)"
    - "Width-budget truncate-then-pad — renderer truncates strings with `…` via padRightUnicode-style helper before they reach the 66-char inner content row; INV-1 width sacred"

must_haves:
  truths:
    - "renderTabContent(tab, snapshot, locale, scrollOffset) returns 18 rows of exactly 66 code-points each (the panel body content area; the row 3 tab strip is owned by 05-02)"
    - "Main tab fixtures differ between 2014 and 2024 ONLY by the `[M]` weapon-mastery flag on the attack list (UI-SPEC §5.2 + RESEARCH §Pattern 3 modernRules table)"
    - "Feats tab fixtures differ between 2014 and 2024 ONLY by the `[Origin]` origin-feat column annotation (UI-SPEC §5.6)"
    - "Skills tab renderer column-aligns 31-char skill names per UI-SPEC §5.3 with truncation indicator `…` on overflow"
    - "Bio tab renderer word-wraps the actor.system.details.biography.value at 66 chars per row (UI-SPEC §5.7) — overflow tracked via scrollOffset"
    - "CharacterSheetPanel.draw() now calls the new `renderTabContent` import — `_renderTabContentStub` from 05-02 is removed"
  artifacts:
    - path: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      provides: renderTabContent dispatcher + 4 per-tab renderers
      exports: ["renderTabContent", "renderMainTab", "renderSkillsTab", "renderFeatsTab", "renderBioTab"]
      min_lines: 250
    - path: packages/shared-render/src/fixtures/sheet.main.2014.it.txt
      provides: full 19-row Main tab body in 2014 edition (UI-SPEC §5.2 mockup verbatim, 2014 row variant)
      contains: "THORIN"
    - path: packages/shared-render/src/fixtures/sheet.main.2024.it.txt
      provides: full 19-row Main tab body in 2024 edition with [M] mastery on weapons
      contains: "[M]"
    - path: packages/shared-render/src/fixtures/sheet.skills.it.txt
      provides: full 19-row Skills tab body (UI-SPEC §5.3 mockup)
      contains: "competente"
    - path: packages/shared-render/src/fixtures/sheet.feats.2014.it.txt
      provides: full 19-row Feats tab body in 2014 edition
      contains: "TALENTI"
    - path: packages/shared-render/src/fixtures/sheet.feats.2024.it.txt
      provides: full 19-row Feats tab body in 2024 edition with [Origin] annotation
      contains: "Origine"
    - path: packages/shared-render/src/fixtures/sheet.bio.it.txt
      provides: full 19-row Bio tab body (UI-SPEC §5.7 mockup)
      contains: "Tratti"
  key_links:
    - from: packages/g2-app/src/panels/character-sheet-panel.ts
      to: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      via: import renderTabContent + invocation in draw()
      pattern: "renderTabContent"
    - from: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      to: snapshot.world.modernRules
      via: per-tab conditional branches
      pattern: "modernRules"
    - from: packages/g2-app/src/panels/character-sheet-tab-renderers.ts
      to: getLabel (HUD_WIDTH_BUDGETS)
      via: i18n-budgets keys for ability/section/vitals/skill/feat/bio sub-strings
      pattern: "getLabel.*sheet\\."

metrics:
  duration: TBD
  completed: TBD
  tasks: 3
  task_commits: TBD
  files_created: 7
  files_modified: 1
  tests_added: TBD
---

<objective>
Ship the per-tab content renderer for CharacterSheetPanel (SHEET-02 live-binding + SHEET-03 dual-edition). This plan owns `character-sheet-tab-renderers.ts` plus the Main / Skills / Feats / Bio fixtures. Inventory and Spells sheet-tab renderers are owned by Plan 05-04 (the Inv + Spellbook standalone panels share that rendering surface).

Purpose: prove dual-edition rendering (PHB 2014 ↔ 2024) end-to-end via INV-1 fixtures + assert hot-swap behavior (snapshot delta re-renders in place without LayerManager.bundle).

Output:
- character-sheet-tab-renderers.ts (4 per-tab pure renderers + dispatcher)
- 6 INV-1 fixtures (2 Main editions + Skills + 2 Feats editions + Bio)
- character-sheet-panel.ts updated: stub method removed, real renderer imported, draw() rewired
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-panel-plugin-system-read-only-panels/05-CONTEXT.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-RESEARCH.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-UI-SPEC.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-VALIDATION.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-01-PLAN.md
@.planning/phases/05-panel-plugin-system-read-only-panels/05-02-PLAN.md
@packages/g2-app/src/panels/character-sheet-panel.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/status-hud/i18n-budgets.ts
@packages/g2-app/src/status-hud/status-hud-renderer.ts
@packages/shared-protocol/src/payloads/character.ts

<interfaces>
From `packages/g2-app/src/panels/character-sheet-panel.ts` (delivered by 05-02):
- `TABS = ['main', 'skills', 'inventory', 'spells', 'feats', 'bio'] as const`
- `TAB_LABELS = ['MAI', 'SKI', 'INV', 'SPL', 'FEA', 'BIO'] as const`
- `type TabId = (typeof TABS)[number]`
- `buildTabStrip(activeIdx: number): string` (row 3, 70 chars)
- Private `_renderTabContentStub` — this plan removes it

From 05-01:
- `getLabel(field, locale)` with per-key EN fallback for best-effort locales
- HUD_WIDTH_BUDGETS keys: `sheet.ability.*`, `sheet.section.*`, `sheet.vitals.*`, `sheet.skill.*`, `sheet.feat.*`, `sheet.bio.*`
- CharacterSnapshot now requires `world: { modernRules: boolean }`

From `packages/g2-app/src/status-hud/status-hud-renderer.ts`:
- Existing `padRightUnicode(str, width)` and `_truncate(str, width)` helpers (code-point counting)
- Existing `_rowFromInner(content, leftBorder, rightBorder)` pattern
</interfaces>

<ui_fixtures>
The 6 fixtures in this plan are 19-row × 70-code-point bodies (the full panel content area cols 1-70, rows 3-21 inclusive). They embed the tab strip on row 3 (delivered by `buildTabStrip` from 05-02). The 18 content rows (rows 4-21) come from the per-tab renderer.

Per UI-SPEC.md verbatim:
- §5.2 Main mockup — the 19-row body with 2014 weapon names (no `[M]`).
- §5.2 modernRules delta — same body but with `[M]` after weapon names in the MAIN attack summary.
- §5.3 Skills mockup — the full 19-row body.
- §5.6 Feats mockup — 2014 and 2024 variants.
- §5.7 Bio mockup — the full 19-row body.

Every row in every fixture MUST be exactly 70 code-points wide. Use `[...row].length === 70` in tests.
</ui_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: character-sheet-tab-renderers.ts — 4 per-tab pure renderers (Main / Skills / Feats / Bio) + dispatcher</name>
  <files>packages/g2-app/src/panels/character-sheet-tab-renderers.ts, packages/g2-app/src/panels/__tests__/character-sheet-tab-renderers.test.ts</files>
  <action>
Create `character-sheet-tab-renderers.ts` exporting per-tab pure renderers and a dispatcher. The Inventory and Spells sheet-tab renderers are owned by Plan 05-04 — this plan ships only Main / Skills / Feats / Bio. To avoid breaking the dispatcher when 05-04 lands, this plan stubs the inventory + spells branches with a "(rendered by 05-04)" placeholder so the dispatcher's switch is exhaustive. 05-04 will replace those 2 case bodies with the real renderers (file overlap: 05-04 will modify this file; therefore 05-04 must be sequential AFTER 05-03, OR — accepted approach per wave structure — 05-04 and 05-03 are wave-2 PARALLEL but touch DIFFERENT files. The cleaner split: 05-03 ships only its 4 renderers + a dispatcher that calls into per-renderer functions imported from 05-04's file once 05-04 lands. For wave-2 parallelism, the dispatcher imports the inventory and spell renderers from `character-sheet-inv-spl-renderers.ts` (owned by 05-04). If 05-04 has not landed at execution time, the dispatcher imports return stubs. **Final resolution: this plan owns the dispatcher and a fallback inventory/spell stub. 05-04 ALSO modifies this dispatcher to swap in real renderers. The two plans therefore must be SEQUENTIAL — 05-04 depends on 05-03. Update 05-04's depends_on accordingly.**

Exports:
- `renderTabContent(tab: TabId, snapshot: CharacterSnapshot | null, locale: HudLocale, scrollOffset: number): string[]` — dispatcher returning 18 rows of exactly 66 code-points each (excluding row 3 tab strip — that is buildTabStrip's responsibility). Switch on `tab`:
  - 'main' → `renderMainTab(snapshot, locale)`
  - 'skills' → `renderSkillsTab(snapshot, locale, scrollOffset)`
  - 'inventory' → STUB returning 18 rows of `' '.repeat(66)` with one centered placeholder row "(inventory rendered by 05-04)"
  - 'spells' → STUB returning 18 rows of `' '.repeat(66)` with one centered placeholder row "(spells rendered by 05-04)"
  - 'feats' → `renderFeatsTab(snapshot, locale, scrollOffset)`
  - 'bio' → `renderBioTab(snapshot, locale, scrollOffset)`
- `renderMainTab(snapshot, locale): string[]` — 18 rows per UI-SPEC §5.2 mockup verbatim. Conditional `[M]` flag after each weapon name in the MAIN attack summary when `snapshot.world.modernRules === true`. Uses `getLabel('sheet.ability.str', locale)` etc. for all i18n strings. Handles `snapshot === null` defensively (renders all 18 rows as space-padded placeholders).
- `renderSkillsTab(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.3. Column layout: 4-char ability label + 1 char prof glyph (◉/★/○) + 30-char skill name + 4-char modifier (right-aligned) + 1-char rollable indicator (⚀). Width-truncates skill names to 30 chars with `…` overflow indicator (RESEARCH Pattern 6 width budget). scrollOffset shifts which skills are visible in the 18 rows (the skill list is longer than 18).
- `renderFeatsTab(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.6. Section headers (CLASS / RACE / BACKGROUND / FEATS) use `getLabel('sheet.feat.*_section', locale)`. When `modernRules === true`, prefix feat-list rows with `[Origin]` annotation for feats with origin metadata (RESEARCH §Pattern 3 dual-edition table). The 2014 variant omits the prefix and uses the ASI/Talent trade-off notation. Source data from `snapshot.actor.feats` if available (defensive — the schema does not yet enumerate feats; render placeholder rows if not present and document as future extension).
- `renderBioTab(snapshot, locale, scrollOffset): string[]` — 18 rows per UI-SPEC §5.7. Strip HTML from `actor.system.details.biography.value` (defensive — use a simple `replace(/<[^>]*>/g, '')` since the snapshot data is producer-trusted; the Bio biography is plain Foundry user input). Word-wrap at 66 chars per row using a word-boundary regex. Subsection headers from `getLabel('sheet.bio.*', locale)` — personality / ideal / bond / flaw / backstory.

Helpers (private to this module or re-imported from status-hud-renderer.ts):
- Reuse `padRightUnicode(str, width)` and `_truncate(str, width)` from `status-hud-renderer.ts` (RESEARCH §Pattern 6 — Unicode code-point counting). Either re-export them or move them to a shared module. Choose re-export from `status-hud-renderer.ts` for minimal churn.

Tests (CSTR-* discriminator markers — 25+ tests):
- CSTR-DISP-MAIN: `renderTabContent('main', mockSnapshot, 'it', 0)` returns 18 rows, each 66 code-points.
- CSTR-DISP-SKILLS / FEATS / BIO: same shape assertion.
- CSTR-DISP-INV-STUB: `renderTabContent('inventory', ...)` returns 18 rows including the placeholder text — proves the stub branch.
- CSTR-DISP-SPL-STUB: same for 'spells'.
- CSTR-DISP-NULL: `renderTabContent('main', null, 'it', 0)` returns 18 rows (defensive null handling).
- CSTR-MAIN-2014: with `modernRules: false`, the attack summary row does NOT contain `[M]`.
- CSTR-MAIN-2024: with `modernRules: true`, the same weapon row contains `[M]` after the weapon name.
- CSTR-MAIN-WIDTH: every row exactly 66 code-points wide.
- CSTR-MAIN-I18N-IT / EN / DE: ability labels match `getLabel('sheet.ability.str', locale)` for each locale.
- CSTR-MAIN-I18N-ES: best-effort fallback — ES locale returns EN strings (proves 05-01 per-key fallback works downstream).
- CSTR-SKILLS-COLALIGN: assert skill rows have the proficiency glyph at col 8 and the modifier value right-aligned at cols 41-44 (UI-SPEC §5.3 column layout).
- CSTR-SKILLS-TRUNC: a 50-char skill name is truncated with `…` to fit the 30-char skill-name column.
- CSTR-SKILLS-SCROLL: scrollOffset=5 shifts visible skills by 5 entries.
- CSTR-FEATS-2014: feat row with `modernRules=false` does NOT contain `[Origin]` annotation.
- CSTR-FEATS-2024: feat row with `modernRules=true` contains `[Origine]` (IT locale).
- CSTR-FEATS-HEADERS: section header row contains `getLabel('sheet.feat.class_section', 'it')`.
- CSTR-BIO-STRIP-HTML: `<p>Hello <b>world</b></p>` snapshot renders as `Hello world` (HTML stripped).
- CSTR-BIO-WORDWRAP: a 100-char biography wraps at 66 chars on word boundaries.
- CSTR-BIO-SCROLL: scrollOffset shifts the visible window.
- CSTR-HOT-SWAP: change `modernRules` from false to true between two calls; assert the output differs ONLY in `[M]` insertion points.

Constraints: every public renderer is a PURE function (no I/O, no class state); panel side-effects (bridge calls, persistence) stay in `character-sheet-panel.ts`. NO LayerManager mutation. NO React/Vue. Use code-point counting widths.

Commit message: feat(05-03): character-sheet-tab-renderers (Main/Skills/Feats/Bio + dispatcher + dual-edition branches)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test character-sheet-tab-renderers</automated>
  </verify>
  <done>
4 per-tab renderers ship + dispatcher returns the right 18-row × 66-code-point output for each tab; modernRules branches verified on Main + Feats; HTML-strip + word-wrap verified on Bio; all 25+ CSTR-* tests green; typecheck and lint:ci exit 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: 6 INV-1 fixtures — Main 2014 / Main 2024 / Skills / Feats 2014 / Feats 2024 / Bio</name>
  <files>packages/shared-render/src/fixtures/sheet.main.2014.it.txt, packages/shared-render/src/fixtures/sheet.main.2024.it.txt, packages/shared-render/src/fixtures/sheet.skills.it.txt, packages/shared-render/src/fixtures/sheet.feats.2014.it.txt, packages/shared-render/src/fixtures/sheet.feats.2024.it.txt, packages/shared-render/src/fixtures/sheet.bio.it.txt</files>
  <action>
Author 6 INV-1 fixtures verbatim from UI-SPEC.md §5.2 (Main 2014 + Main 2024 delta), §5.3 (Skills), §5.6 (Feats 2014 + Feats 2024 delta), §5.7 (Bio).

Each fixture is a 19-row block — row 3 is the tab strip (use `buildTabStrip` output from 05-02 for the appropriate active tab), rows 4-21 are the per-tab content from the renderer. Every row is exactly 70 code-points wide. Use the IT locale only for these fixtures (canonical per CONTEXT.md §Area 4).

Fixture authoring discipline:
- Copy the mockup row verbatim from UI-SPEC.md §5.2 / §5.3 / §5.6 / §5.7.
- Replace placeholder content with realistic snapshot data (THORIN F3/W5 mountain dwarf level 8 — same character across all 6 fixtures for consistency).
- Use the exact IT strings from the i18n-budgets table (Phase 5 keys delivered by 05-01).
- For Main 2024, insert `[M]` after `Spada lunga` (or the equivalent weapon name) in the attack summary row — this is the ONLY structural difference from Main 2014.
- For Feats 2024, prefix origin-feat rows with `[Origine]` annotation — this is the ONLY structural difference from Feats 2014.
- Every row of every fixture MUST satisfy `[...row].length === 70`.
- Each fixture ends with a single terminal newline (Phase 4b convention).

Wire 6 INV-1 round-trip tests in `character-sheet-tab-renderers.test.ts` (CSTR-FIX-* markers) using `matchAsciiFixture`:
- CSTR-FIX-MAIN-2014: `renderMainTab(snapshot2014, 'it')` (with the tab strip prefixed) round-trips against `sheet.main.2014.it.txt`.
- CSTR-FIX-MAIN-2024: `renderMainTab(snapshot2024, 'it')` round-trips against `sheet.main.2024.it.txt`.
- CSTR-FIX-SKILLS: `renderSkillsTab(snapshot2014, 'it', 0)` round-trips against `sheet.skills.it.txt`.
- CSTR-FIX-FEATS-2014: `renderFeatsTab(snapshot2014, 'it', 0)` round-trips against `sheet.feats.2014.it.txt`.
- CSTR-FIX-FEATS-2024: `renderFeatsTab(snapshot2024, 'it', 0)` round-trips against `sheet.feats.2024.it.txt`.
- CSTR-FIX-BIO: `renderBioTab(snapshotWithBio, 'it', 0)` round-trips against `sheet.bio.it.txt`.

For the test setup, define two shared mock snapshots `snapshot2014` and `snapshot2024` — identical except for `world.modernRules: false` vs `true`. Use a single Thorin Oakenshield character definition (per UI-SPEC §5.2 hero name) to keep the fixtures coherent.

If during execution the planner-cited UI-SPEC line drifts by 1 char (count error in the mockup), the executor MUST follow UI-SPEC as the design contract (Phase 4b Plan 01 Deviation #1 pattern). Document inline in the SUMMARY.

Commit message: feat(05-03): 6 INV-1 sheet body fixtures (Main 2014/2024 + Skills + Feats 2014/2024 + Bio)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test character-sheet-tab-renderers</automated>
  </verify>
  <done>
6 fixtures committed; each row of every fixture is exactly 70 code-points (matchAsciiFixture is the gate); Main 2024 differs from Main 2014 only by `[M]` insertion; Feats 2024 differs from Feats 2014 only by `[Origine]` prefix; INV-1 ck 11+14+15 verified in software.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewire CharacterSheetPanel.draw to call renderTabContent — remove _renderTabContentStub</name>
  <files>packages/g2-app/src/panels/character-sheet-panel.ts</files>
  <action>
Replace the call site of `_renderTabContentStub` inside `CharacterSheetPanel.draw()` with the new import:

```
import { renderTabContent } from './character-sheet-tab-renderers.js';
```

And rewrite `draw()` so the content block is:

```
const tabStrip = buildTabStrip(this.activeTabIndex);
const bodyRows = renderTabContent(TABS[this.activeTabIndex] ?? 'main', this.snapshot, this.locale, this.scrollOffset);
const content = [tabStrip, ...bodyRows].join('\\n');
```

Then `await this.bridge.textContainerUpgrade(new TextContainerUpgrade({ containerName: 'overlay-block', content }))`.

Remove the private `_renderTabContentStub` method entirely. Update the existing CHSP-DRAW-1 test in `character-sheet-panel.test.ts` to verify the draw call still happens exactly once (no regression) — extend the assertion to confirm the content now contains a known sentinel from `renderMainTab` (e.g., the IT string for "CARATTERISTICHE" from `getLabel('sheet.section.abilities', 'it')`) when the active tab is Main and snapshot is populated.

Add a new test CHSP-DRAW-2 — call onSnapshot with a mock snapshot, then call onEvent({kind:'tap'}); assert that the next draw call's content matches the renderSkillsTab output (cross-tab transition through the dispatcher).

Constraints: NO change to TABS / TAB_LABELS / buildTabStrip / PERSIST_KEY exports — those stay as 05-02 declared them. NO change to the lifecycle methods. The only change is `draw()` body + import statement + removal of the stub method.

Commit message: refactor(05-03): wire CharacterSheetPanel.draw to renderTabContent (remove stub)
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test character-sheet-panel character-sheet-tab-renderers</automated>
  </verify>
  <done>
`_renderTabContentStub` removed from `character-sheet-panel.ts`; draw() imports and calls `renderTabContent` from the new module; CHSP-DRAW-1 still passes; new CHSP-DRAW-2 (cross-tab transition) passes; typecheck and lint:ci exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot.world.modernRules → renderer branch | Boolean field controls visible UI delta — must originate from server (validated by CharacterSnapshotSchema) |
| actor.system.details.biography.value → Bio renderer | User-authored HTML; renderer strips tags before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | modernRules boolean spoofed at wire-shape | mitigate | Already gated by CharacterSnapshotSchema (z.boolean()) from 05-01 — rejected before reaching renderer |
| T-05-03-02 | XSS | HTML in biography.value | mitigate | Renderer strips tags with `replace(/<[^>]*>/g, '')`; output goes to bridge.textContainerUpgrade (plain text — no DOM) |
| T-05-03-03 | DoS | pathological biography length (10MB string) | mitigate | scrollOffset windowing only renders 18 rows × 66 chars at a time; word-wrap is O(n) |
</threat_model>

<verification>
- pnpm typecheck exits 0
- pnpm lint:ci exits 0
- pnpm test --filter @evf/g2-app passes; ≥25 CSTR-* tests + 6 CSTR-FIX-* tests + 1 new CHSP-DRAW-2 test added
- All 6 fixture files exist; matchAsciiFixture round-trip tests green
- modernRules branch verified on Main + Feats via separate 2014/2024 fixtures
- `_renderTabContentStub` no longer present in `character-sheet-panel.ts`
</verification>

<success_criteria>
- 4 per-tab renderers produce character-perfect output matching UI-SPEC §5.2 / §5.3 / §5.6 / §5.7
- Dual-edition SHEET-03 delta visible in the 2 Main fixtures + 2 Feats fixtures
- Hot-swap re-render works without LayerManager.bundle (onSnapshot → draw() in place)
- CharacterSheetPanel is fully functional (Main + Skills + Feats + Bio); Inventory + Spells remain stubbed until 05-04 lands
</success_criteria>

<output>
After completion, create `.planning/phases/05-panel-plugin-system-read-only-panels/05-03-SUMMARY.md` documenting: 3 task commits, files created/modified, test counts, deviations (esp. UI-SPEC count corrections + dnd5e schema field shapes), Wave-2 dependency signal for 05-04, Self-Check table.
</output>
