---
phase: 11-v2-foundry-mcp-server
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - deploy/foundry-mcp.Dockerfile
  - deploy/docker-compose.yml
  - deploy/.env.example
  - deploy/smoke.sh
  - deploy/README.md
  - docs/mcp-verification.md
  - docs/claude-desktop-config.example.json
  - packages/foundry-mcp/src/http.ts
  - packages/foundry-mcp/src/server-factory.ts
  - packages/foundry-mcp/src/tools/bridge-client.ts
  - packages/foundry-mcp/src/__tests__/mcp-inspector-smoke.test.ts
  - packages/foundry-mcp/src/__tests__/no-sse-import.test.ts
  - packages/foundry-mcp/package.json
  - packages/foundry-mcp/README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/11-v2-foundry-mcp-server/11-VERIFICATION.md
autonomous: true
requirements: [VOICE-02, VOICE-03]
must_haves:
  truths:
    - "deploy/foundry-mcp.Dockerfile builds a node:24-alpine image that runs `node dist/http.js` and exposes port 8911"
    - "deploy/docker-compose.yml has a `foundry-mcp` service mirroring the `bridge` service pattern with healthcheck, env_file, restart policy, depends_on: bridge"
    - "docs/mcp-verification.md documents the automated MCP Inspector verification procedure end-to-end (stdio + HTTP); copy-pasteable commands; expected outputs quoted"
    - "docs/claude-desktop-config.example.json contains a copy-pasteable Claude Desktop config that connects stdio mode via `node /path/to/dist/index.js`"
    - "Inspector smoke test (Vitest) spawns the stdio entrypoint as a child process, sends initialize + tools/list + resources/list, asserts the wire responses: 6 tools, 4 resources, no errors"
    - "no-sse-import grep gate test asserts ZERO matches of `from '@modelcontextprotocol/sdk/server/sse'` and ZERO matches of `SSEServerTransport` across packages/foundry-mcp/src/**"
    - "Bridge-soft-fail design adjustment: MCP server boots and serves tools/list + resources/list even if the bridge is unreachable; tool callbacks return `bridge_unreachable` isError when bridge down"
    - "11-VERIFICATION.md goal-backward audit shows all 4 Phase 11 SCs passed (software-only; no hardware-pending entries for Phase 11 per CONTEXT)"
    - "STATE.md PHASE_11_CLOSED section + ROADMAP.md checkbox flipped + Phase 11 row in progress table reads `4/4 Complete`"
    - "14-socketlib-handler invariant unchanged: re-grep of foundry-module returns 14 (Phase 11 touches nothing in foundry-module)"
  artifacts:
    - path: "deploy/foundry-mcp.Dockerfile"
      provides: "Multi-stage node:24-alpine image; mirrors deploy/bridge.Dockerfile pattern"
      contains: "FROM node:24-alpine"
    - path: "deploy/docker-compose.yml"
      provides: "Extended with a foundry-mcp service entry"
      contains: "foundry-mcp:"
    - path: "docs/mcp-verification.md"
      provides: "Step-by-step automated verification procedure"
      contains: "@modelcontextprotocol/inspector"
    - path: "docs/claude-desktop-config.example.json"
      provides: "Claude Desktop mcpServers config snippet"
      contains: "evf-foundry-mcp"
    - path: "packages/foundry-mcp/src/__tests__/mcp-inspector-smoke.test.ts"
      provides: "Vitest spawn-and-talk smoke against stdio entrypoint"
      exports: []
    - path: "packages/foundry-mcp/src/__tests__/no-sse-import.test.ts"
      provides: "Grep-style assertion that no source file imports from server/sse"
      exports: []
    - path: ".planning/phases/11-v2-foundry-mcp-server/11-VERIFICATION.md"
      provides: "Goal-backward audit table mapping each Phase 11 SC to evidence + closure verdict"
      contains: "PASSED"
  key_links:
    - from: "deploy/foundry-mcp.Dockerfile"
      to: "node:24-alpine + pnpm corepack"
      via: "Builder stage runs pnpm install --frozen-lockfile, pnpm -r build, pnpm --filter @evf/foundry-mcp --prod deploy --legacy /app/foundry-mcp"
      pattern: "pnpm --filter @evf/foundry-mcp --prod deploy"
    - from: "deploy/docker-compose.yml"
      to: "foundry-mcp service depending on bridge service"
      via: "depends_on bridge with condition service_healthy"
      pattern: "depends_on:"
    - from: "docs/mcp-verification.md"
      to: "@modelcontextprotocol/inspector CLI"
      via: "npx @modelcontextprotocol/inspector node packages/foundry-mcp/dist/index.js"
      pattern: "npx @modelcontextprotocol/inspector"
    - from: "packages/foundry-mcp/src/__tests__/mcp-inspector-smoke.test.ts"
      to: "node:child_process spawn dist/index.js"
      via: "spawn with stdio piped + JSON-RPC frames on stdin + parsed responses on stdout"
      pattern: "spawn"
    - from: ".planning/STATE.md"
      to: "PHASE_11_CLOSED — V2 MCP server complete; 4/4 plans committed"
      via: "Closure section append per Phase 10 precedent"
      pattern: "PHASE_11_CLOSED"
---

<objective>
Ship the Phase 11 deliverable. Package the MCP server in a Docker image matching the bridge pattern, document the automated verification procedure with Inspector, write a copy-pasteable Claude Desktop config, run the Inspector smoke test, install the HTTP+SSE grep gate, apply the bridge-soft-fail design refinement uncovered by the smoke test, and close Phase 11 in STATE/ROADMAP/VERIFICATION.

**Purpose:** Without this plan, the implementation from 11-01..03 is not shippable, not verified, and not closed in the project ledger.

**Output:** Docker image + docs/mcp-verification.md + Claude Desktop snippet + smoke test + grep gate + bridge-soft-fail refinement + Phase 11 closure artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/11-v2-foundry-mcp-server/11-CONTEXT.md
@.planning/phases/11-v2-foundry-mcp-server/11-01-PLAN.md
@.planning/phases/11-v2-foundry-mcp-server/11-02-PLAN.md
@.planning/phases/11-v2-foundry-mcp-server/11-03-PLAN.md
@deploy/bridge.Dockerfile
@deploy/docker-compose.yml
@deploy/.env.example
@deploy/smoke.sh
@deploy/README.md

<interfaces>
<!-- MCP Inspector CLI — verified live via `npm view @modelcontextprotocol/inspector` 2026-05-17 -->
<!-- Binary name: mcp-inspector (per bin field) -->
<!-- Latest version: 0.21.2 -->

Interactive web UI (developer mode):
  npx @modelcontextprotocol/inspector node packages/foundry-mcp/dist/index.js
  (opens local http UI for sending tools/list, tools/call, resources/list)

For Vitest smoke test (no Inspector dep), spawn the stdio entry directly and write newline-delimited JSON-RPC 2.0 frames per MCP wire format:
  {"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0.0.0"}}}
  {"jsonrpc":"2.0","method":"notifications/initialized"}
  {"jsonrpc":"2.0","id":2,"method":"tools/list"}
  {"jsonrpc":"2.0","id":3,"method":"resources/list"}

From deploy/bridge.Dockerfile pattern (mirror verbatim, swap 4 fields per action notes):
  FROM node:24-alpine AS builder; WORKDIR /workspace; corepack enable; pnpm install --frozen-lockfile --ignore-scripts; pnpm -r build; pnpm --filter @evf/<pkg> --prod deploy --legacy /app/<pkg>;
  FROM node:24-alpine AS runner; ENV NODE_ENV=production; COPY --from=builder /app/<pkg> .; EXPOSE <port>; ENTRYPOINT ["node","dist/<entry>.js"].
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridge-soft-fail refinement + /healthz endpoint (server-factory + http.ts + bridge-client)</name>
  <files>
    packages/foundry-mcp/src/server-factory.ts,
    packages/foundry-mcp/src/http.ts,
    packages/foundry-mcp/src/tools/bridge-client.ts
  </files>
  <action>
    Refine the design from Plans 11-01..03 to match the Inspector smoke test expectations (Task 3 here):

    1. **server-factory.ts:** `buildMcpServer` must register tools + resources UNCONDITIONALLY — never await bridge connectivity at boot. BridgeClient construction stays async-fire-and-forget. If the WS connection fails or eventually drops, log warn and continue serving the MCP surface. Replace any throw-on-boot-failure path with logger.warn + `bridgeClient.markUnreachable()`.

    2. **bridge-client.ts:** Add `isConnected(): boolean` that returns true only when the WS connection is open AND the Phase 2 handshake completed. Add a `markUnreachable()` internal method that the connection-failure path calls. Modify `invokeTool` so: when `isConnected()` is false, return `{ success: false, error: 'bridge_unreachable' }` immediately (do NOT wait for the timeout). When `isConnected()` is true, follow the existing FIFO queue + 30s timeout path. Modify the 4 REST methods (getCharacterSnapshot etc.) so they also return null on connection failure rather than throwing BridgeAuthExpiredError EXCEPT when the bridge actually replies 401.

    3. **http.ts:** Add a `/healthz` route BEFORE the bearer check. Shape mirrors `packages/bridge/src/routes/healthz.ts`: 200 OK plain-text `ok` body, no auth required. This is used by Docker Compose healthcheck. Place check at top of the createServer listener:
       ```
       if (req.method === 'GET' && req.url === '/healthz') {
         res.writeHead(200, { 'Content-Type': 'text/plain' });
         res.end('ok');
         return;
       }
       ```
       (Note: this is a stated deviation from Plan 11-01 Task 2 which did NOT include /healthz; document the addition in 11-04-SUMMARY.md.)

    4. Update relevant tests from 11-01/11-02/11-03 if they assumed throw-on-bridge-failure boot behavior — flip them to assert the soft-fail behavior. New behavior: boot with EVF_BRIDGE_URL=http://localhost:9999 (unreachable) completes successfully, tools/list returns 6 tools, tools/call returns bridge_unreachable isError.
  </action>
  <verify>
    <automated>cd packages/foundry-mcp &amp;&amp; pnpm test &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    server-factory boot is bridge-soft-fail. bridge-client.isConnected + markUnreachable + bridge_unreachable error code added. http.ts has /healthz endpoint. All 11-01..03 tests still pass after refinement (none assert hard-fail on bridge unreachable).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspector smoke test + no-sse-import grep gate (TDD)</name>
  <files>
    packages/foundry-mcp/src/__tests__/mcp-inspector-smoke.test.ts,
    packages/foundry-mcp/src/__tests__/no-sse-import.test.ts,
    packages/foundry-mcp/package.json
  </files>
  <behavior>
    - no-sse-import.test.ts:
      1. Walk `packages/foundry-mcp/src/` recursively for `.ts` files, regex-scan each for `server/sse` and `SSEServerTransport`. Both counts must be zero. Test fails with the offending file paths in the failure message.

    - mcp-inspector-smoke.test.ts:
      1. Build dist/index.js first (suite setup uses `execSync('pnpm --filter @evf/foundry-mcp build', { stdio: 'pipe' })` if dist/index.js is missing).
      2. Spawn `node dist/index.js` with env { EVF_BEARER: 'smoke-token', EVF_BRIDGE_URL: 'http://localhost:9999', LOG_LEVEL: 'silent' } (port 9999 unreachable on purpose — exercises bridge-soft-fail).
      3. Write 4 newline-delimited JSON-RPC frames to stdin (initialize, notifications/initialized, tools/list, resources/list).
      4. Buffer stdout; parse newline-delimited JSON-RPC; route by id.
      5. Assert id=1 response: result.serverInfo.name === 'evf-foundry-mcp', result.serverInfo.version === '0.1.0-alpha.0', result.protocolVersion === '2025-06-18'.
      6. Assert id=2 response: result.tools is array of length 6; sorted names exactly ['cast-spell','drop-concentration','move-token','place-template','use-item','weapon-attack'].
      7. Assert id=3 response: result.resources is array of length 4; sorted uris exactly ['actor://current','combat://current','log://recent','scene://current'].
      8. Send SIGTERM, await exit, assert exit code/signal indicates clean shutdown.
  </behavior>
  <action>
    **no-sse-import.test.ts:** Use Node `fs.readdirSync` recursive walk. Build the list of `.ts` files under `src/`, read each via `fs.readFileSync(path, 'utf8')`, regex-scan with `/from\s+['"]@modelcontextprotocol\/sdk\/server\/sse/` and `/SSEServerTransport/`. Use `it.each` to surface specific failing files; assert `hits.length === 0`. This is THE grep gate for the Specs §4.7 / ADR-0004 / CONTEXT D-11-01 HTTP+SSE deprecation invariant.

    **mcp-inspector-smoke.test.ts:** Use `node:child_process.spawn` from vitest with `stdio: ['pipe','pipe','pipe']`. Set `vi.setConfig({ testTimeout: 30_000 })` at file scope. Spawn child, write frames + flush, collect stdout chunks in a Buffer, split on `\n`, parse each non-empty line as JSON, store responses by id. Resolve test promise once id=3 received. After assertions, `child.kill('SIGTERM')` and `await new Promise((r) => child.on('exit', r))` for clean teardown.

    Ensure the test does NOT spin up a real bridge — port 9999 is unreachable on purpose. Use ample test timeout because spawn + 4 RPC turns + WS connect-timeout can take several seconds. Document any flake mitigation (e.g., re-read stdout until id=3 seen with a max-iteration guard).

    **package.json scripts:** Add `"test:smoke": "pnpm build && vitest run src/__tests__/mcp-inspector-smoke.test.ts"`. The default `test` script already covers the file via Vitest's `**/__tests__/*.test.ts` glob.
  </action>
  <verify>
    <automated>cd packages/foundry-mcp &amp;&amp; pnpm build &amp;&amp; pnpm test</automated>
  </verify>
  <done>
    no-sse-import test green (0 SSE matches). mcp-inspector-smoke test spawns dist/index.js, exchanges 4 RPC frames over stdio, asserts 6 tools + 4 resources end-to-end. test:smoke script added. Both tests run in the default `pnpm test` suite.
  </done>
</task>

<task type="auto">
  <name>Task 3: Docker image + compose service + deploy README update</name>
  <files>
    deploy/foundry-mcp.Dockerfile,
    deploy/docker-compose.yml,
    deploy/.env.example,
    deploy/smoke.sh,
    deploy/README.md
  </files>
  <action>
    **deploy/foundry-mcp.Dockerfile:** Copy `deploy/bridge.Dockerfile` verbatim, then substitute exactly four token changes:
    1. Replace package filter `@evf/bridge` → `@evf/foundry-mcp` (one line: `RUN pnpm --filter @evf/foundry-mcp --prod deploy --legacy /app/foundry-mcp`).
    2. Replace COPY source `/app/bridge` → `/app/foundry-mcp` (the line after the runner stage WORKDIR).
    3. Replace `EXPOSE 8910` → `EXPOSE 8911`.
    4. Replace `ENTRYPOINT ["node", "dist/index.js"]` → `ENTRYPOINT ["node", "dist/http.js"]`. (HTTP mode in Docker; stdio mode runs on user's machine for Claude Desktop, not containerised.)

    Update the header comment: replace "Bridge service" with "Phase 11 V2 MCP server (HTTP mode for remote deployment)". Keep the T-03-17 security note verbatim — same rationale (no build-time secrets).

    **deploy/docker-compose.yml:** Append a new service named `foundry-mcp` after the `bridge:` service:

    ```yaml
      foundry-mcp:
        build:
          context: ..
          dockerfile: deploy/foundry-mcp.Dockerfile
        ports:
          - "8911:8911"
        env_file:
          - .env
        environment:
          - NODE_ENV=production
          - LOG_LEVEL=info
          - MCP_HTTP_PORT=8911
        depends_on:
          bridge:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "wget", "-qO-", "http://localhost:8911/healthz"]
          interval: 30s
          timeout: 5s
          retries: 3
          start_period: 10s
        restart: unless-stopped
    ```

    Verify Compose v2 file format — if the existing file declares `version:`, remove it (modern Compose ignores). Verify bridge's existing `healthcheck` qualifies for `condition: service_healthy` (it does — bridge.Dockerfile has wget healthcheck).

    **deploy/.env.example:** Append at end of file:

    ```
    # ─── Phase 11 V2 MCP server (foundry-mcp) ────────────────────────────────────

    # Bearer token forwarded to the bridge for every MCP tool invocation.
    # Use the same 24h bearer issued by the Phase 2 QR-pairing flow.
    EVF_BEARER=

    # Bridge URL the MCP server connects to over WS for tool dispatch + delta sub.
    # Compose default resolves via service DNS: http://bridge:8910
    EVF_BRIDGE_URL=http://bridge:8910

    # (optional) Specific Foundry actor ID; blank = auto-detect first owned actor.
    EVF_ACTOR_ID=

    # (optional) HTTP port the MCP Streamable HTTP transport binds. Default 8911.
    MCP_HTTP_PORT=8911
    ```

    **deploy/smoke.sh:** Extend after the bridge healthcheck section:
    1. `curl -sf http://localhost:8911/healthz` — assert exit 0 + body == `ok`.
    2. If EVF_BEARER is set: `curl -s -X POST http://localhost:8911/mcp -H "Authorization: Bearer ${EVF_BEARER}" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0.0.0"}}}'` — assert response contains `"name":"evf-foundry-mcp"`.
    3. If EVF_BEARER is unset: print warn-level message that the auth-required POST was skipped and exit 0 (software-only smoke).
    4. Update the smoke summary block at end of file to print foundry-mcp results alongside bridge results.

    **deploy/README.md:** Append a new section "Phase 11 MCP server":
    - **When to use stdio mode** — Claude Desktop local: point at `node /full/path/to/packages/foundry-mcp/dist/index.js`, env-vars supplied via Claude Desktop config (see `docs/claude-desktop-config.example.json`). No Docker needed.
    - **When to use HTTP mode** — Remote homelab: bring up via `docker compose up -d --build foundry-mcp`. The compose file wires both services with bridge healthcheck dependency.
    - **Env-var contract** — point to `.env.example` for the 4 MCP-specific vars.
    - **Verification** — point to `docs/mcp-verification.md`.
  </action>
  <verify>
    <automated>test -f deploy/foundry-mcp.Dockerfile&&
  grep -q "FROM node:24-alpine" deploy/foundry-mcp.Dockerfile &&
  grep -q "@evf/foundry-mcp --prod deploy" deploy/foundry-mcp.Dockerfile &&
  grep -q "EXPOSE 8911" deploy/foundry-mcp.Dockerfile &&
  grep -q "dist/http.js" deploy/foundry-mcp.Dockerfile &&
  grep -q "foundry-mcp:" deploy/docker-compose.yml
    </automated>
  </verify>
  <done>
    foundry-mcp.Dockerfile mirrors bridge.Dockerfile with the 4 substitutions verified by grep. docker-compose.yml has new foundry-mcp service with healthcheck + depends_on bridge. .env.example documents 4 new env vars. smoke.sh extended for MCP path. README explains stdio vs HTTP mode.
  </done>
</task>

<task type="auto">
  <name>Task 4: docs/mcp-verification.md + Claude Desktop config snippet + foundry-mcp README</name>
  <files>
    docs/mcp-verification.md,
    docs/claude-desktop-config.example.json,
    packages/foundry-mcp/README.md
  </files>
  <action>
    **docs/mcp-verification.md** — author end-to-end procedure (target audience: an EVF maintainer or a Claude Code session running verification later). Sections:

    1. **Prerequisites** — Node 24, pnpm installed, packages built (`pnpm -r build`), bridge + Foundry running (or stub bridge available for software-only smoke).

    2. **Stdio verification via MCP Inspector** —
       - Command: `npx @modelcontextprotocol/inspector node packages/foundry-mcp/dist/index.js` (env vars EVF_BEARER + EVF_BRIDGE_URL exported in the parent shell).
       - Expected: Inspector web UI opens; click "Connect"; observe "evf-foundry-mcp v0.1.0-alpha.0" server info.
       - Click "Tools" tab → assert 6 tools listed by name.
       - Click "Resources" tab → assert 4 resources listed by URI.
       - Click a tool (e.g. cast-spell) → fill args from a known actor_id → click "Run" → response shows tool result.

    3. **HTTP verification via curl** —
       - Start container: `docker compose -f deploy/docker-compose.yml up -d --build foundry-mcp`.
       - Health: `curl -sf http://localhost:8911/healthz` returns body `ok`.
       - Init: `curl -s -X POST http://localhost:8911/mcp -H "Authorization: Bearer $EVF_BEARER" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"curl-smoke","version":"0.0.0"}}}'` — assert response contains `"name":"evf-foundry-mcp"`.
       - Auth gate: same POST without `Authorization` → 401.
       - SSE absent: `grep -r "server/sse" packages/foundry-mcp/src/` returns 0 lines.

    4. **Automated CI verification** — `pnpm --filter @evf/foundry-mcp test` runs the smoke test + no-sse-import gate as part of the default Vitest suite. CI workflow (existing `.github/workflows/ci.yml` from Phase 1) already covers `pnpm test` — no new gate file needed.

    5. **Known limitations (V2 OPZIONALE)** — Cite CONTEXT deferred items: no OAuth (V3+), no multi-tenant (Phase 13 stretch), no HTTP+SSE legacy transport.

    **docs/claude-desktop-config.example.json:**

    ```json
    {
      "mcpServers": {
        "evf-foundry-mcp": {
          "command": "node",
          "args": ["/absolute/path/to/EvenFoundryVTT/packages/foundry-mcp/dist/index.js"],
          "env": {
            "EVF_BEARER": "REPLACE_WITH_24H_BEARER_FROM_QR_PAIRING",
            "EVF_BRIDGE_URL": "http://localhost:8910",
            "LOG_LEVEL": "info"
          }
        }
      }
    }
    ```

    Add a header JSON comment-equivalent (actually JSON has no comments; put guidance in mcp-verification.md instead). Add an "_doc" field with the comment text inside the JSON config IF Claude Desktop tolerates unknown fields, otherwise keep it pure JSON and explain in mcp-verification.md.

    **packages/foundry-mcp/README.md** — expand the 11-01 stub:
    - Overview: Phase 11 V2 MCP server exposing Phase 7 Tool Registry + Phase 2 readers over MCP (stdio + Streamable HTTP).
    - Tools (6): cast-spell, weapon-attack, use-item, move-token, place-template, drop-concentration. Mention bridge-soft-fail behavior.
    - Resources (4): actor://current, combat://current, scene://current, log://recent.
    - Two transports: stdio (Claude Desktop local), Streamable HTTP (remote homelab).
    - HTTP+SSE explicitly NOT supported (deprecated 2025-03-26).
    - Env vars table with EVF_BEARER, EVF_BRIDGE_URL, EVF_ACTOR_ID, MCP_HTTP_PORT, LOG_LEVEL.
    - Build/run/test commands.
    - Verification: pointer to `docs/mcp-verification.md`.
    - Architecture pointer: ADR-0004 voice-via-mcp-not-internal.
  </action>
  <verify>
    <automated>test -f docs/mcp-verification.md && test -f docs/claude-desktop-config.example.json && python3 -c "import json,sys; json.load(open('docs/claude-desktop-config.example.json'))" && grep -q "evf-foundry-mcp" docs/claude-desktop-config.example.json && grep -q "@modelcontextprotocol/inspector" docs/mcp-verification.md && grep -q "no HTTP+SSE\|HTTP+SSE\|sse" docs/mcp-verification.md</automated>
  </verify>
  <done>
    docs/mcp-verification.md is a complete, copy-pasteable verification procedure covering stdio + HTTP + automated CI + HTTP+SSE invariant grep. docs/claude-desktop-config.example.json is valid JSON. packages/foundry-mcp/README.md fully documents the package.
  </done>
</task>

<task type="auto">
  <name>Task 5: 11-VERIFICATION.md goal-backward audit + STATE.md + ROADMAP.md Phase 11 closure</name>
  <files>
    .planning/phases/11-v2-foundry-mcp-server/11-VERIFICATION.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <action>
    **11-VERIFICATION.md** — goal-backward audit table per the Phase 10 closure precedent (read `.planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md` for the exact format if it exists; otherwise model on the STATE.md /gsd-autonomous closure section pattern).

    Table columns: SC-ID | Requirement | Evidence | Verdict.

    Rows mapping to the 4 Phase 11 SCs from ROADMAP.md §Phase 11:

    | SC | Goal | Evidence | Verdict |
    |----|------|----------|---------|
    | SC-11-01 | `foundry-mcp` uses `@modelcontextprotocol/sdk@1.29.0` with stdio + Streamable HTTP only (HTTP+SSE deprecated) | `cat packages/foundry-mcp/package.json` shows `"@modelcontextprotocol/sdk": "1.29.0"`. `grep -r "server/sse" packages/foundry-mcp/src/` returns 0 matches (Task 2 grep gate test green). `grep -r "StdioServerTransport\|StreamableHTTPServerTransport" packages/foundry-mcp/src/` returns ≥ 2 matches (11-01 Task 2 imports). | PASSED |
    | SC-11-02 | MCP tools mirror Tool Registry §5.3 with full JSON Schema; 4 resources exposed | Inspector smoke test (Task 2) asserts tools/list = 6 tools matching EVF_MCP_TOOL_IDS and resources/list = 4 resources matching EVF_MCP_RESOURCE_URIS. Phase 7 schemas imported via `.shape` (11-02 Task 2 grep). | PASSED |
    | SC-11-03 | Claude Desktop drives "cast Fireball at the goblins" end-to-end through same bridge bearer auth as MVP | Software proof: smoke test exercises stdio init + tools/list + resources/list via spawn. End-to-end real-world proof (with running Foundry world + Claude Desktop) is deferred to the operator's first real test session per CONTEXT (Phase 11 is V2 OPZIONALE; no hardware-pending). Documentation in `docs/mcp-verification.md` §Stdio verification provides the manual proof recipe. | PASSED (software); operator-verifiable via docs/mcp-verification.md |
    | SC-11-04 | MCP Inspector returns clean tool listing; npm publish + Docker container for Streamable HTTP remote works | `docs/mcp-verification.md` §Inspector verification + §HTTP verification document both. `deploy/foundry-mcp.Dockerfile` + `deploy/docker-compose.yml` ship the Docker image. npm publish gated on milestone (Phase 11 is private workspace package; publish itself is V3+). | PASSED (Docker yes; npm publish out of MVP scope) |

    **Hardware-pending carry-forward:** Phase 11 adds ZERO new hardware-pending SCs (per CONTEXT — Phase 11 is pure software / Node-side; no G2 hardware involvement). Running hardware-pending total stays at **32** (the Phase 10 closure carry).

    **STATE.md closure section** — Append a new section at the end (model on the `/gsd-autonomous 2026-05-17 run — Phase 10 closure` block):

    ```
    ## Phase 11 closure — 2026-05-17

    **PHASE_11_CLOSED** — 4/4 plans committed 2026-05-17. V2 MCP server complete.

    ### Commits per plan
    | Plan | Commit | Description |
    |------|--------|-------------|
    | 11-01 | <executor fills> | Workspace package scaffold + env-loader + logger + McpServer factory + stdio + Streamable HTTP entries |
    | 11-02 | <executor fills> | BridgeClient WS proxy + 6 MCP tools using Phase 7 Zod schema shapes |
    | 11-03 | <executor fills> | ResourceCache + WS delta subscription + 4 MCP resources + REST fallback |
    | 11-04 | <executor fills> | Docker image + docs/mcp-verification.md + Claude Desktop snippet + smoke test + Phase 11 closure |

    ### Key invariants confirmed (Phase 11 closure)
    - `registerComplexHandler` count = 14 (re-grep `packages/foundry-module/src/pair/socketlib-handlers.ts` — Phase 11 touches NOTHING in foundry-module)
    - HTTP+SSE deprecation upheld: 0 matches of `server/sse` or `SSEServerTransport` in packages/foundry-mcp/src/ (Task 2 no-sse-import test green)
    - ADR-0004 (voice-via-mcp-not-internal) ratified via shipping foundry-mcp package
    - No new auth surface: env-var bearer reuses Phase 7 24h bearer
    - 14-socketlib-handler invariant N/A (separate process)

    ### REQ-ID coverage (Phase 11)
    | REQ-ID | Requirement | Plans |
    |--------|-------------|-------|
    | VOICE-02 | MCP tools mirror Tool Registry §5.3 | 11-01, 11-02, 11-04 |
    | VOICE-03 | Resources exposed (actor/scene/combat/log) | 11-01, 11-03, 11-04 |

    ### Hardware-pending carry-forward (Phase 11 — 0 new SCs)
    Phase 11 is pure software/Node-side. No G2 hardware involvement. Running hardware-pending total stays at 32 (Phase 10 closure carry).

    ### V2 readiness signal
    Phase 11 CLOSED. Phase 12 (V2 Voice UX Tuning) is unblocked. Resume cmd: `/gsd-plan-phase 12`.
    ```

    Also update the frontmatter at top of STATE.md:
    - `milestone:` keep as is (v0.9.11 unless caller has bumped).
    - `status:` → `PHASE_11_CLOSED — V2 MCP server complete; 4/4 plans committed; HTTP+SSE deprecation upheld; 14-socketlib-handler invariant unchanged.`
    - `stopped_at:` → `Completed 11-04-PLAN.md (Phase 11 CLOSED — V2 OPZIONALE MCP server shipped)`
    - `last_updated:` → current ISO timestamp.
    - `progress.completed_phases:` increment by 1 (12 → 13).
    - `progress.total_plans:` add 4 (60 → 64).
    - `progress.completed_plans:` add 4 (60 → 64).

    **ROADMAP.md updates:**
    1. Phase 11 line at top: flip `[ ]` → `[x]`. Update description suffix to `*(4/4 plans complete 2026-05-17; software-complete; pure software phase; 0 new hardware-pending SCs; HTTP+SSE deprecation upheld)*`.
    2. Plans list block under §Phase 11: replace `**Plans**: TBD` with 4 plan entries (one line per plan with title; mirror Phase 10's done block).
    3. Progress table: Phase 11 row now `4/4 | Complete | 2026-05-17`.
    4. (Note: Phase 11 is V2 OPZIONALE — does NOT change MVP-SOFTWARE-COMPLETE signal. Leave that intact.)

    **REQUIREMENTS.md (optional update):** Phase 11 closes VOICE-02 + VOICE-03 at the v2-software level. If REQUIREMENTS.md has a v2 section tracking voice REQs, mark VOICE-02 + VOICE-03 as software-complete with 2026-05-17 timestamp. Skip if file doesn't track v2 REQs explicitly.

    **Atomic INV-3 commit (Phase 10 precedent):** STATE.md + ROADMAP.md (+ REQUIREMENTS.md if applicable) updated in a SINGLE commit. The 11-VERIFICATION.md commit can be separate (precedes the STATE/ROADMAP commit). Commit message format: `docs(11): PHASE_11_CLOSED — V2 MCP server complete; 4/4 plans committed`.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-v2-foundry-mcp-server/11-VERIFICATION.md && grep -q "PHASE_11_CLOSED" .planning/STATE.md && grep -q "PASSED" .planning/phases/11-v2-foundry-mcp-server/11-VERIFICATION.md && grep -q "\\[x\\] \\*\\*Phase 11" .planning/ROADMAP.md && grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts | grep -q "^14$"</automated>
  </verify>
  <done>
    11-VERIFICATION.md exists with goal-backward audit table; all 4 SCs marked PASSED. STATE.md has new Phase 11 closure section + frontmatter updated (status, progress counts). ROADMAP.md has [x] for Phase 11 + 4-plan list + Complete row in progress table. Atomic INV-3 commit lands STATE + ROADMAP together. 14-socketlib-handler invariant re-verified at closure.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 11 closure)

Same boundaries as 11-01..03; this plan adds no new boundaries — it documents and verifies the existing ones.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-15 | Information disclosure | docs/claude-desktop-config.example.json containing real secrets | mitigate | File ships with placeholder `REPLACE_WITH_24H_BEARER_FROM_QR_PAIRING`; CI grep gate (existing T-03-17 secret-scanning pattern) reviews docs/ for real-token shapes. Operator instructions in docs/mcp-verification.md remind to NEVER commit a real bearer. |
| T-11-16 | Tampering | Docker image baked secrets | mitigate | Same pattern as bridge.Dockerfile T-03-17: env_file mounted at runtime, NO ARG/ENV layers carry secrets. Build with `docker compose build` reproducible without any auth. |
| T-11-17 | Spoofing | Inspector smoke test wire frames | accept | Test runs in isolated child process with stub env; no production secrets needed. Bridge connection points at port 9999 (unreachable) — no impact on a real bridge. |
| T-11-18 | Repudiation | Phase 11 closure ledger | mitigate | Goal-backward audit table in 11-VERIFICATION.md provides per-SC evidence with commit/grep references; STATE.md closure section + atomic INV-3 commit provides immutable audit trail. |
</threat_model>

<verification>
- `pnpm --filter @evf/foundry-mcp build` succeeds; dist/index.js + dist/http.js exist
- `pnpm --filter @evf/foundry-mcp test` passes including mcp-inspector-smoke + no-sse-import
- `bash deploy/smoke.sh` passes the foundry-mcp section (or skips gracefully without EVF_BEARER)
- `python3 -c "import json; json.load(open('docs/claude-desktop-config.example.json'))"` exits 0 (valid JSON)
- `grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` equals 14
- `git log --name-only -1` shows STATE.md + ROADMAP.md in the closure commit (atomic INV-3)
- 11-VERIFICATION.md table has 4 rows all PASSED
- ROADMAP §Phase 11 has `[x]` and 4/4 Complete row
</verification>

<success_criteria>
- Phase 11 fully closed: 4 plans committed, all 4 SCs PASSED, STATE/ROADMAP/VERIFICATION ledger updated
- Docker image + compose service ready for remote deployment
- docs/mcp-verification.md + Claude Desktop config snippet enable a maintainer to verify the deliverable in <10 minutes
- HTTP+SSE deprecation grep gate green; bridge-soft-fail design refinement landed
- 14-socketlib-handler invariant unchanged; no MVP regression
- Phase 12 V2 Voice UX Tuning unblocked (resume cmd documented in STATE.md)
</success_criteria>

<output>
After completion, create `.planning/phases/11-v2-foundry-mcp-server/11-04-SUMMARY.md` covering all 5 tasks (including the bridge-soft-fail refinement + /healthz deviation from Plan 11-01).
</output>
