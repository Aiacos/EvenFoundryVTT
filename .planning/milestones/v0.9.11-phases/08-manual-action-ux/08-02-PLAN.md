---
phase: 08-manual-action-ux
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - packages/g2-app/src/panels/target-resolver.ts
  - packages/g2-app/src/panels/target-resolver.test.ts
  - packages/g2-app/src/panels/target-picker-panel.ts
  - packages/g2-app/src/panels/target-picker-panel.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - packages/shared-render/src/fixtures/target-picker.full-list.it.txt
  - packages/shared-render/src/fixtures/target-picker.single-target.it.txt
  - packages/shared-render/src/fixtures/target-picker.empty.it.txt
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "TargetPickerPanel implements OverlayPanel at z=2: onMount subscribes to PanelGestureBus; scroll cycles ▶ indicator through valid-target list; tap emits tool.invoke envelope with target_token_id selected; double-tap cancels via onClose"
    - "target-resolver.ts exports resolveValidTargets(combatSnapshot, sceneSnapshot, callerActorId, range): TargetCandidate[] — shared helper consumed by both TargetPickerPanel and Plan 08-03 ActionOptionsModal"
    - "Empty-target case (no enemies in range) renders the localized 'Nessun bersaglio' hint and auto-closes 2s after mount via setTimeout"
    - "INV-1 layout integrity holds character-perfect for 3 panel states (full list / single target / empty) across IT locale fixtures"
    - "T-08-01-01 (envelope forgery): outgoing tool.invoke envelope uses canonical EnvelopeSchema shape verbatim — proto/seq/ts/type/session_id/payload (NOT value); session_id threaded from constructor; payload validated by ToolInvocationEnvelopePayloadSchema upstream"
  artifacts:
    - path: "packages/g2-app/src/panels/target-resolver.ts"
      provides: "resolveValidTargets pure helper + TargetCandidate type + range-heuristic utilities"
      exports: ["resolveValidTargets", "TargetCandidate", "describeTargetRow"]
    - path: "packages/g2-app/src/panels/target-picker-panel.ts"
      provides: "TargetPickerPanel OverlayPanel class (z=2) with constructor-threaded sessionId + empty-list auto-close"
      exports: ["TargetPickerPanel", "TargetPickerCloseHandler", "TargetPickerWebSocket"]
    - path: "packages/shared-render/src/fixtures/target-picker.full-list.it.txt"
      provides: "INV-1 full 3-target list rendering"
      contains: "BERSAGLIO"
    - path: "packages/shared-render/src/fixtures/target-picker.single-target.it.txt"
      provides: "INV-1 single-target rendering"
      contains: "BERSAGLIO"
    - path: "packages/shared-render/src/fixtures/target-picker.empty.it.txt"
      provides: "INV-1 empty-list 'Nessun bersaglio' hint rendering"
      contains: "Nessun bersaglio"
  key_links:
    - from: "packages/g2-app/src/panels/target-picker-panel.ts"
      to: "target-resolver.ts resolveValidTargets"
      via: "filtering combat.turns + scene tokens to TargetCandidate[]"
      pattern: "resolveValidTargets\\("
    - from: "packages/g2-app/src/panels/target-picker-panel.ts"
      to: "ws.send canonical EnvelopeSchema tool.invoke"
      via: "tap → JSON.stringify envelope with payload field"
      pattern: "type: 'tool\\.invoke'"
---

<objective>
Wave 1 deliverable (parallel with Plan 08-03 — disjoint files). Ships the target-selection surface:

  1. `target-resolver.ts` — pure-function helper module exposing `resolveValidTargets(combatSnapshot, sceneSnapshot, callerActorId, rangeHint?)`. Filters combat.turns + scene tokens by friend/foe classification (caller's own actor excluded) and range heuristic (`actor.system.attributes.distance` from RESEARCH §Q3 — Phase 8 uses heuristic; precise grid distance is Phase 9). Returns `TargetCandidate[]` (deterministic ordering: combat-active-turn first, then initiative order, then scene-only tokens). Reuses Phase 5 CombatTrackerPanel's `combat.turns` shape so Phase 5 helpers compose cleanly.

  2. `TargetPickerPanel` — z=2 OverlayPanel with constructor signature `(bridge, ws, gestureBus, candidates, locale, sessionId, toolInvocation, onClose)`. Where `toolInvocation` is `{ toolId, callerArgs }` — the panel attaches the selected `target_token_id` to `callerArgs` and emits `tool.invoke` on tap. Scrolling cycles `▶` indicator; tap emits + closes; double-tap closes without emit (cancel); empty `candidates` array renders 'Nessun bersaglio' + auto-pops after 2s.

  3. 3 INV-1 fixtures in `packages/shared-render/src/fixtures/`: `target-picker.full-list.it.txt` (3 targets, mid-row selected), `target-picker.single-target.it.txt` (1 target, only-row selected), `target-picker.empty.it.txt` (no targets, 'Nessun bersaglio' hint visible).

  4. ~6 new i18n keys in HUD_WIDTH_BUDGETS: `target_picker_title`, `target_picker_empty_hint`, `target_picker_confirm_hint`, `target_picker_cancel_hint`, `hud_r1_target_picker` (composite tap/scroll/long hint string for status-HUD context chip per Phase 6 Plan 03 pattern).

Purpose: TargetPickerPanel is the FIRST scrolling pick list the player sees during an action flow. Phase 5 SpellbookPanel + InventoryPanel can scroll through spells/items, but the target step has been a stub. ACT-01 demands target selection via R1 scroll + tap. Plan 08-03 + 08-05 both consume `TargetPickerPanel` via a `pushOverlay(picker)` call after selecting a spell/item/weapon.

Output:
  - 2 new modules (resolver + panel) + 1 test file per module
  - 3 new INV-1 fixtures (3 panel states)
  - 6 i18n keys (single i18n-budgets.ts edit)
  - 7-9 file commits total in this plan
  - NO boot-engine-core.ts changes (Plan 08-05 owns wiring)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manual-action-ux/08-CONTEXT.md
@.planning/phases/08-manual-action-ux/08-01-PLAN.md

# Plan 08-01 must land first (provides ActionResultPayloadSchema for completeness — TargetPicker emits tool.invoke but the result toast comes via Plan 08-01 watcher → dispatcher).
# Plan 08-02 and Plan 08-03 are PARALLEL — zero files_modified overlap. Verified:
#   08-02 files: target-resolver.ts, target-picker-panel.ts, i18n-budgets.ts, 3 fixtures.
#   08-03 files: action-options-modal.ts, spellbook-panel.ts, inventory-panel.ts, i18n-budgets.ts, 2 fixtures.
# Wait: i18n-budgets.ts overlap. Resolution → Plan 08-02 runs FIRST in Wave 1 (Plan 08-03 depends on 08-02 not for code but for i18n-budgets.ts edit ordering). Sequential Wave 1.
# Actually re-checked: i18n-budgets.ts overlap forces sequential ordering — same pattern as Phase 4a Wave 2 03/04/06. See PHASE 5 Plan 04+05 precedent for sequential foreground on overlapping i18n-budgets.ts edits.

# Exemplars to mirror verbatim:
@packages/g2-app/src/panels/template-placement-panel.ts
@packages/g2-app/src/panels/template-placement-dispatcher.ts
@packages/g2-app/src/panels/concentration-drop-modal.ts

# Phase 6 panel-router push/pop overlay (caller mounts TargetPickerPanel via router.pushOverlay)
@packages/g2-app/src/engine/panel-router.ts

# Phase 5 combat tracker — combatants schema + windowing helper to reuse
@packages/g2-app/src/panels/combat-tracker-panel.ts

# Phase 5 character-sheet-tab-renderers — padRightUnicode + truncateUnicode helpers (already exported)
@packages/g2-app/src/panels/character-sheet-tab-renderers.ts

# Phase 5 INV-1 fixture writing pattern
@packages/g2-app/src/panels/__tests__/concentration-drop-modal.test.ts

<interfaces>
<!-- Combat snapshot shape — RESEARCH §Q3 confirms TargetPicker reuses combat.turns -->

From packages/shared-protocol/src/payloads/combat.ts:
```typescript
export const CombatantSchema = z.strictObject({
  id: z.string().min(1),
  actorId: z.string().nullable(),
  name: z.string().min(1),
  initiative: z.number().nullable(),
  hp: z.number().nullable(),
  maxHp: z.number().nullable(),
  ac: z.number().nullable(),
  conditions: z.array(z.string()),
  concentration: z.object({ spellName: z.string(), duration: z.string() }).optional(),
});

export const CombatSnapshotSchema = z.strictObject({
  combatId: z.string().nullable(),
  round: z.number().int().nullable(),
  turn: z.number().int().nullable(),
  currentCombatantId: z.string().nullable(),
  combatants: z.array(CombatantSchema),
});
```

From packages/shared-protocol/src/payloads/scene.ts (Phase 4a — scene viewport reader):
```typescript
export const SceneViewportSchema = z.object({
  width: z.number().int(),
  height: z.number().int(),
  // Phase 5 reader includes `tokens` array for visible scene tokens
  tokens: z.array(z.object({ id: z.string(), name: z.string(), x: z.number(), y: z.number(), actorId: z.string().nullable() })).optional(),
});
```

From packages/g2-app/src/engine/panel-router.ts (z=2 single-active invariant + pushOverlay suspension stack):
```typescript
// pushOverlay(panel, layerManager) suspends the current z=2 panel onto overlayStack and atomically mounts the new one.
// popOverlay(layerManager) restores the suspended panel.
// TargetPickerPanel is mounted via pushOverlay from Plan 08-03/08-05 caller — Plan 08-02 does NOT wire boot.
```

From packages/g2-app/src/engine/layer-types.ts (OverlayPanel contract — TargetPickerPanel implements verbatim):
```typescript
export interface OverlayPanel extends Layer {
  onMount(): Promise<void>;        // subscribe gesture bus
  onUnmount(): Promise<void>;      // unsubscribe + clear timers
  onEvent(gesture: R1Gesture): void; // tap/scroll/double-tap/long-press
}
```

From packages/g2-app/src/panels/template-placement-panel.ts (THE exemplar — TargetPickerPanel mirrors lifecycle, constructor signature, container strategy, ws.send envelope shape):
```typescript
// Constructor: (bridge, ws, gestureBus, payload, locale, sessionId, onClose)
// onEvent('tap')  → emit tool.invoke envelope + onClose
// onEvent('double-tap') → ignored (panel stays mounted) ← TargetPicker DIFFERS: double-tap = cancel
// Container: single 'overlay-block' text container (Strategy A)
// getContainerCount() → { image: 0, text: 1 }
// declare const crypto: { randomUUID(): string };  ← idempotencyKey UUID source
```

From packages/g2-app/src/panels/character-sheet-tab-renderers.ts (text utilities):
```typescript
export function padRightUnicode(value: string, width: number): string;
export function truncateUnicode(value: string, max: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: target-resolver.ts — pure-function helper + 5 new i18n keys</name>
  <files>
    packages/g2-app/src/panels/target-resolver.ts,
    packages/g2-app/src/panels/target-resolver.test.ts,
    packages/g2-app/src/status-hud/i18n-budgets.ts
  </files>
  <behavior>
    - TR-01: `resolveValidTargets(combatSnapshot, sceneTokens, callerActorId, rangeHint?)` returns an empty array when combatSnapshot is null AND sceneTokens is empty/undefined.
    - TR-02: When combatSnapshot is non-null: returns `combat.combatants.filter(c => c.actorId !== callerActorId && c.actorId !== null && (c.hp ?? 0) > 0)` mapped to TargetCandidate. Excludes self, excludes already-defeated combatants (HP ≤ 0). Order: combatant whose id === combat.currentCombatantId FIRST (the "active target" hint), then remaining combatants in initiative order (descending).
    - TR-03: When combatSnapshot is null but sceneTokens is non-empty: returns sceneTokens filtered by `t.actorId !== callerActorId && t.actorId !== null`, mapped to TargetCandidate. Order: scene order.
    - TR-04: When BOTH provided: combat candidates FIRST (deduped by actorId), then scene tokens not already in combat.
    - TR-05: `rangeHint` is a number (feet) — when provided, candidates with no inferable position default to PASS (included; Phase 8 broad heuristic per CONTEXT). Phase 9 COMB-02 refines with precise grid distance. Acceptance: passing rangeHint=30 with combatants having no x/y returns all combatants (no filtering).
    - TR-06: TargetCandidate shape: `{ tokenId: string, actorId: string, name: string, hp: number | null, maxHp: number | null, ac: number | null, isActiveTurn: boolean, sourceIdx: number }`. Phase 8 minimal — Phase 9 may add range/conditions.
    - TR-07: `describeTargetRow(candidate, locale, isSelected, width)` returns a string fitted to width code-points. Format mirrors CONTEXT.md TargetPickerPanel mockup: `  ${idx}. ${name}  PF ${hp}/${maxHp}  CA ${ac}` (IT) / `  ${idx}. ${name}  HP ${hp}/${maxHp}  AC ${ac}` (EN). If isSelected: prefix replaced with `▶ ${idx}.`. Truncation applied via truncateUnicode if line > width.
    - TR-08: TargetCandidate is `Readonly` (interface uses `readonly` modifier — no mutation).
    - I18N-01: 5 new keys land in HUD_WIDTH_BUDGETS:
      - `target_picker_title`: `{ it: 'BERSAGLIO', en: 'TARGET', de: 'ZIEL', max: 12 }`
      - `target_picker_empty_hint`: `{ it: 'Nessun bersaglio', en: 'No targets', de: 'Keine Ziele', max: 28 }`
      - `target_picker_hp_label`: `{ it: 'PF', en: 'HP', de: 'TP', max: 3 }`
      - `target_picker_ac_label`: `{ it: 'CA', en: 'AC', de: 'RK', max: 3 }`
      - `hud_r1_target_picker`: `{ it: 'conferma|scroll|annulla', en: 'confirm|scroll|cancel', de: 'best.|scroll|abbr.', max: 30 }` (composite hint per Phase 6 parseR1HintString convention — pipe-separated tap|scroll|long)
  </behavior>
  <action>
    Create `packages/g2-app/src/panels/target-resolver.ts` as a pure-function module (NO bridge, NO ws, NO side effects). JSDoc header citing Plan 08-02 + RESEARCH §Q3 (Phase 8 broad range heuristic; Phase 9 COMB-02 refines).

    Exports:
    - `interface TargetCandidate { readonly tokenId: string; readonly actorId: string; readonly name: string; readonly hp: number | null; readonly maxHp: number | null; readonly ac: number | null; readonly isActiveTurn: boolean; readonly sourceIdx: number; }`
    - `function resolveValidTargets(combatSnapshot: CombatSnapshot | null, sceneTokens: ReadonlyArray<{ id: string; name: string; actorId: string | null }> | undefined, callerActorId: string, rangeHint?: number): TargetCandidate[]` — implementation per TR-01..05 behaviors.
    - `function describeTargetRow(candidate: TargetCandidate, locale: HudLocale, idx: number, isSelected: boolean, width: number): string` — implementation per TR-07.

    Co-located `target-resolver.test.ts` covering TR-01..07. Use minimal CombatSnapshot fixtures (BASE_COMBAT_SNAPSHOT from 05-panel-integration-smoke.test.ts as starting point — drop concentration field, vary combatants).

    Edit `packages/g2-app/src/status-hud/i18n-budgets.ts` adding the 5 new keys per I18N-01. Place them alphabetically within a "// Phase 8 — target picker labels + hints" section. Verify each value's `[...v].length ≤ max`.

    No new external dependencies. Pure TS module — no Vitest plugin needs, no Vite config change.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run target-resolver &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    target-resolver.ts + target-resolver.test.ts exist; TR-01..07 (7 tests) pass; 5 i18n keys present with IT/EN/DE values within budget; describeTargetRow output matches expected format for IT and EN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TargetPickerPanel — z=2 OverlayPanel + 3 INV-1 fixtures</name>
  <files>
    packages/g2-app/src/panels/target-picker-panel.ts,
    packages/g2-app/src/panels/target-picker-panel.test.ts,
    packages/shared-render/src/fixtures/target-picker.full-list.it.txt,
    packages/shared-render/src/fixtures/target-picker.single-target.it.txt,
    packages/shared-render/src/fixtures/target-picker.empty.it.txt
  </files>
  <behavior>
    - TPP-01: Panel ID is `'target-picker'`. `static meta` is OMITTED (TargetPickerPanel is a system overlay opened directly via pushOverlay, NOT via Quick Action menu — same as ConcentrationDropModalPanel pattern; PanelRouter filters empty navKey from registry per Phase 6 Plan 02 decision).
    - TPP-02: Constructor: `(bridge, ws, gestureBus, candidates: ReadonlyArray<TargetCandidate>, locale, sessionId: string, toolInvocation: { toolId: ToolId; callerArgs: Record<string, unknown> }, onClose: () => void)`. All fields private + readonly.
    - TPP-03: onMount subscribes to PanelGestureBus AND, if `candidates.length === 0`, schedules `setTimeout(() => onClose(), 2000)`. The setTimeout handle is saved and cleared in onUnmount (T-08-emptylist-timer-leak mitigation).
    - TPP-04: onUnmount unsubscribes from bus + clearTimeout(autoCloseTimer). Idempotent via null guards.
    - TPP-05: Internal state: `selectedIdx: number` (default 0 — selects active-turn target which is candidates[0] per TR-02 ordering).
    - TPP-06: onEvent('scroll', direction='down') → `selectedIdx = (selectedIdx + 1) % candidates.length`; re-draw. Same for 'up' → `selectedIdx = (selectedIdx - 1 + candidates.length) % candidates.length`. No-op when candidates.length === 0.
    - TPP-07: onEvent('tap') → constructs canonical EnvelopeSchema tool.invoke envelope and ws.send:
      ```
      {
        proto: 'evf-v1',
        seq: 0,
        ts: Date.now(),
        type: 'tool.invoke',
        session_id: this.sessionId,
        payload: {
          toolId: this.toolInvocation.toolId,
          idempotencyKey: crypto.randomUUID(),
          args: { ...this.toolInvocation.callerArgs, targets: [candidates[selectedIdx].tokenId] }
        }
      }
      ```
      Then `onClose()`. No-op when candidates.length === 0 (still inside the auto-close window).
    - TPP-08: onEvent('double-tap') → onClose() WITHOUT emitting (cancel — CONTEXT.md §Decisions: "Double-tap = cancel").
    - TPP-09: onEvent('long-press') → ignored (long-press is owned by quick-action-long-press-dispatcher router-level subscriber per Phase 6 — TargetPickerPanel does NOT short-circuit; the dispatcher will mount QuickActionMenu over the picker, suspending it onto overlayStack per Phase 6 Plan 02 semantics).
    - TPP-10: `getContainerCount()` returns `{ image: 0, text: 1 }` (Strategy A — single 'overlay-block' text container per ADR-0009 Amd 1).
    - TPP-11: `getR1Hints()` returns parseR1HintString(getLabel('hud_r1_target_picker', locale)) — Phase 6 INV-5 chip integration.
    - TPP-12: draw() emits ONE bridge.textContainerUpgrade call with 18-row newline-joined content. Frame: top border `┌─[ BERSAGLIO ]──────...────┐`, blank row, up-to-5 target rows (windowed around selectedIdx), blank rows, hint row (`[tap] conferma  [long] annulla`), bottom border.
    - TPP-13: Empty state: top border, blank rows, centered `Nessun bersaglio` (locale-aware), blank rows, no hint row (auto-close runs the timer), bottom border. Matches `target-picker.empty.it.txt` fixture.
    - TPP-14: W-4 envelope round-trip: emitted envelope JSON parses + EnvelopeSchema.safeParse.success === true + ToolInvocationEnvelopePayloadSchema.safeParse(env.payload).success === true. Test extracts `ws.send.mock.calls[0][0]`, JSON.parse, runs both schemas.
    - TPP-15: INV-1 fixture round-trip for full-list (3 targets, idx=1 selected) — matches `target-picker.full-list.it.txt` character-perfect via matchAsciiFixture.
    - TPP-16: INV-1 fixture round-trip for single-target (1 target, idx=0 selected) — matches `target-picker.single-target.it.txt`.
    - TPP-17: INV-1 fixture round-trip for empty (0 targets) — matches `target-picker.empty.it.txt`.
  </behavior>
  <action>
    Create `packages/g2-app/src/panels/target-picker-panel.ts` mirroring `template-placement-panel.ts` structure verbatim:

    1. JSDoc header citing Plan 08-02, CONTEXT.md §Decisions Area 1 (target picker), §Specifics (mockup), W-4 round-trip regression guard, T-08-01-01 envelope shape mitigation.

    2. Imports:
       - `import { type EvenAppBridge, TextContainerUpgrade } from '@evenrealities/even_hub_sdk';`
       - `import type { ToolName } from '@evf/shared-protocol';`  (use `TOOL_INPUT_SCHEMAS` keys; OR import `ToolId` type from `packages/foundry-module/src/write-path/tool-registry.ts` — but cross-package shape. Use the union type from shared-protocol per index.ts re-exports.)
       - `import type { OverlayPanel, R1Gesture } from '../engine/layer-types.js';`
       - `import { ZIndex } from '../engine/layer-types.js';`
       - `import type { PanelGestureBus } from '../engine/panel-gesture-bus.js';`
       - `import { getLabel, type HudLocale } from '../status-hud/i18n-budgets.js';`
       - `import { parseR1HintString } from '../status-hud/r1-hint-parser.js';`
       - `import { describeTargetRow, type TargetCandidate } from './target-resolver.js';`
       - `declare const crypto: { randomUUID(): string };`  (same pattern as template-placement-panel.ts)

    3. Local types:
       - `export interface TargetPickerWebSocket { send(data: string): void; }`
       - `export type TargetPickerCloseHandler = () => void;`
       - `export interface TargetPickerToolInvocation { readonly toolId: 'cast-spell' | 'weapon-attack' | 'use-item'; readonly callerArgs: Record<string, unknown>; }` (Phase 8 narrows to 3 tool IDs that take a `targets` array; move-token + drop-concentration + template ops don't use TargetPicker).

    4. Constants:
       - `const TARGET_PICKER_CONTAINER_NAME = 'overlay-block' as const;`
       - `const PANEL_WIDTH = 66;`  // matches Phase 5 panel inner width
       - `const VISIBLE_ROWS = 5;`  // 3-5 target rows visible at once per CONTEXT mockup
       - `const AUTO_CLOSE_MS = 2000;`  // empty-state auto-close per CONTEXT.md §Decisions

    5. Class `export class TargetPickerPanel implements OverlayPanel`:
       - `public readonly id = 'target-picker'`
       - `public readonly z = ZIndex.Z2_OVERLAY`
       - All constructor args saved as private readonly.
       - `private selectedIdx = 0;`
       - `private unsubscribe: (() => void) | null = null;`
       - `private autoCloseTimer: ReturnType<typeof setTimeout> | null = null;`
       - `onMount` async — subscribes bus + schedules auto-close timer if `candidates.length === 0`.
       - `onUnmount` async — unsubscribes + clearTimeout.
       - `onEvent` per TPP-06..09.
       - `draw` async — single bridge.textContainerUpgrade. Calls `_buildLines()` private method.
       - `destroy` — no-op (lifecycle in onUnmount).
       - `getContainerCount` returns `{ image: 0, text: 1 }`.
       - `getR1Hints` returns parseR1HintString(getLabel('hud_r1_target_picker', this.locale)).
       - `_buildLines()` private — returns 18-row string array. Branches on candidates.length === 0 (TPP-13 empty rendering).
       - Test accessor `_getSelectedIdxForTest()` for unit tests.

    6. Co-located `target-picker-panel.test.ts` with TPP-01..17 (17 tests). Use `vi.fn()` for bridge.textContainerUpgrade (returns Promise.resolve()) and ws.send. Use real PanelGestureBus instance. Pattern matches `template-placement-panel.test.ts` + `concentration-drop-modal.test.ts`.

    7. INV-1 fixtures in `packages/shared-render/src/fixtures/` — author by running real renderer via vitest --update (same pattern as Phase 5 Plan 06 fixture seeding). Run once to generate, then verify content matches CONTEXT.md mockup character-perfect. The 3 fixture files:
       - `target-picker.full-list.it.txt`: 18 rows, 3 targets at lines 4-6 (GOBLIN ARCHER, GOBLIN BRUTO selected with ▶, CANE OMBRA).
       - `target-picker.single-target.it.txt`: 18 rows, 1 target at line 4 selected with ▶.
       - `target-picker.empty.it.txt`: 18 rows, centered "Nessun bersaglio" hint at line 5.

    Use `AsciiGrid.fromString(content)` + `matchAsciiFixture` for fixture round-trip tests (TPP-15/16/17).

    DO NOT modify any other panel or boot-engine-core.ts. Plan 08-05 owns boot wiring.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run target-picker-panel</automated>
  </verify>
  <done>
    target-picker-panel.ts + target-picker-panel.test.ts exist; 17 TPP-* tests pass; 3 INV-1 fixtures committed in shared-render; W-4 envelope round-trip via EnvelopeSchema + ToolInvocationEnvelopePayloadSchema; empty-state auto-close timer fires + clears on unmount; double-tap cancel never emits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PanelGestureBus → TargetPickerPanel.onEvent | In-process gesture pub-sub; trust boundary owned by r1-event-source.ts (Phase 6) — already validated upstream |
| TargetPickerPanel.onEvent('tap') → ws.send | Outgoing envelope crosses g2-app → bridge boundary; bridge validates via ToolInvocationEnvelopePayloadSchema (Phase 7 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering / Spoofing | TargetPickerPanel outgoing envelope | mitigate | Envelope construction uses canonical EnvelopeSchema shape verbatim (`payload` field NOT `value`, W-4 regression guard from Phase 4b Plan 05). TPP-14 verifies EnvelopeSchema.safeParse + ToolInvocationEnvelopePayloadSchema.safeParse round-trip. `idempotencyKey` is `crypto.randomUUID()` per emission — Phase 7 IdempotencyStore dedupes server-side. |
| T-08-02-01 | Information disclosure | Target list reveals other-player tokens | accept | Phase 8 broad heuristic: `resolveValidTargets` filters by `actorId !== callerActorId && hp > 0`. This INCLUDES tokens owned by other players (visible in scene). MVP scope: single-player play per ROADMAP — STRETCH-01 multi-player sync is Phase 13. Phase 9 COMB-02 may add explicit owner-filter when multiplayer arrives. |
| T-08-03-01 | Denial of Service | Empty-state auto-close timer leak | mitigate | autoCloseTimer saved + cleared in onUnmount (TPP-03 + TPP-04). Idempotent null guard. Same pattern as ConcDropModal unsubscribe-on-unmount (T-4b-01-03). |
| T-08-03-02 | Race condition | onClose called twice on rapid tap+auto-close | mitigate | autoCloseTimer cleared on first tap (onUnmount → clearTimeout) before onClose fires; even if timer races, the dispatcher's `onClose` callback issues a single LayerManager.bundle([destroy z=2]) which is idempotent (Phase 4b LayerManager bundle behavior). |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 workspace-wide.
- `pnpm test --filter @evf/g2-app target-resolver target-picker-panel` → 24 tests pass (7 TR-* + 17 TPP-*).
- INV-1 fixtures load + round-trip via matchAsciiFixture — character-perfect against CONTEXT.md mockup.
- `pnpm lint:ci` exits 0 (Biome).
- W-4 grep gate: `grep -E 'WireEnvelopeSchema|envelope\\.value' packages/g2-app/src/panels/target-picker-panel.ts packages/g2-app/src/panels/target-picker-panel.test.ts` → 0 matches (canonical `payload` field only).
- 5 new i18n keys present in HUD_WIDTH_BUDGETS — verified via grep `grep -c "target_picker_" packages/g2-app/src/status-hud/i18n-budgets.ts | grep -v '^#'` returns ≥ 4 (plus hud_r1_target_picker = 5).
</verification>

<success_criteria>
- target-resolver.ts deterministically produces TargetCandidate[] from CombatSnapshot + scene tokens.
- TargetPickerPanel renders 3 distinct states (full/single/empty) per CONTEXT.md mockup character-perfect.
- INV-1 fixtures committed + round-trip via matchAsciiFixture.
- Empty-state auto-close fires after 2s + properly cleans up timer on unmount.
- W-4 envelope round-trip verified (EnvelopeSchema + ToolInvocationEnvelopePayloadSchema both pass).
- 5-6 i18n keys land in HUD_WIDTH_BUDGETS with IT/EN/DE within budget.
- No changes to boot-engine-core.ts (Plan 08-05 wires).
- registerComplexHandler count remains 14 (no new socketlib handler).
- CI Gate 8 green (no `activity.use(` in target-picker-panel.ts or target-resolver.ts).
</success_criteria>

<output>
After completion, create `.planning/phases/08-manual-action-ux/08-02-SUMMARY.md` documenting:
- One-liner: TargetPickerPanel z=2 OverlayPanel + target-resolver helper + 3 INV-1 fixtures + 5 i18n keys; W-4 envelope round-trip verified.
- Tasks completed table.
- TPP-* + TR-* test count.
- INV-1 fixture paths.
- Self-Check: PASSED block.
</output>
