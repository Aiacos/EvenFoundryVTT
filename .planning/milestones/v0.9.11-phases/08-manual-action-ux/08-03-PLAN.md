---
phase: 08-manual-action-ux
plan: 03
type: execute
wave: 2
depends_on: ["08-02"]
files_modified:
  - packages/g2-app/src/panels/action-options-modal.ts
  - packages/g2-app/src/panels/action-options-modal.test.ts
  - packages/g2-app/src/panels/spellbook-panel.ts
  - packages/g2-app/src/panels/spellbook-panel.test.ts
  - packages/g2-app/src/panels/inventory-panel.ts
  - packages/g2-app/src/panels/inventory-panel.test.ts
  - packages/g2-app/src/status-hud/i18n-budgets.ts
  - packages/shared-render/src/fixtures/action-options-modal.spell.it.txt
  - packages/shared-render/src/fixtures/action-options-modal.item.it.txt
autonomous: true
requirements: [ACT-01]
must_haves:
  truths:
    - "ActionOptionsModal z=2 overlay opens when player long-presses a row in SpellbookPanel or InventoryPanel — modal shows [tap=cast/use] [long=details] [×2=cancel] per CONTEXT.md mockup"
    - "Spellbook long-press now constructs an ActionOptionsModal via pushOverlay with the highlighted spell's payload (NOT a Quick Action menu — overrides Phase 6 long-press dispatcher's behaviour ONLY when a spell row is highlighted)"
    - "Inventory long-press now constructs an ActionOptionsModal with the highlighted item's payload, same pattern as Spellbook"
    - "On modal tap: panel emits canonical EnvelopeSchema tool.invoke envelope (toolId='cast-spell' or 'use-item') + closes via onClose"
    - "INV-1 layout integrity holds character-perfect for 2 ActionOptionsModal states (spell variant / item variant) across IT locale fixtures"
  artifacts:
    - path: "packages/g2-app/src/panels/action-options-modal.ts"
      provides: "ActionOptionsModal OverlayPanel z=2 + canonical tool.invoke envelope on tap"
      exports: ["ActionOptionsModal", "ActionOptionsCloseHandler", "ActionOptionsWebSocket", "ActionOptionsRequest"]
    - path: "packages/g2-app/src/panels/spellbook-panel.ts"
      provides: "onEvent('long-press') wires to ActionOptionsModal via injected openActionOptions handler"
      contains: "openActionOptions"
    - path: "packages/g2-app/src/panels/inventory-panel.ts"
      provides: "onEvent('long-press') wires to ActionOptionsModal via injected openActionOptions handler"
      contains: "openActionOptions"
    - path: "packages/shared-render/src/fixtures/action-options-modal.spell.it.txt"
      provides: "INV-1 spell variant — Palla di Fuoco example"
      contains: "Palla di Fuoco"
    - path: "packages/shared-render/src/fixtures/action-options-modal.item.it.txt"
      provides: "INV-1 item variant — Pozione di Guarigione example"
      contains: "Pozione"
  key_links:
    - from: "packages/g2-app/src/panels/spellbook-panel.ts"
      to: "openActionOptions (injected handler)"
      via: "long-press onEvent dispatch"
      pattern: "openActionOptions\\("
    - from: "packages/g2-app/src/panels/action-options-modal.ts"
      to: "ws.send tool.invoke"
      via: "tap → canonical EnvelopeSchema with toolId='cast-spell' | 'use-item'"
      pattern: "type: 'tool\\.invoke'"
---

<objective>
Wave 2 deliverable. Wires the **launch surface** for Phase 8 action flows (CONTEXT.md §Area 1):

  1. `ActionOptionsModal` — z=2 OverlayPanel with constructor `(bridge, ws, gestureBus, request: ActionOptionsRequest, locale, sessionId, onClose)`. The `request` payload carries `{ kind: 'spell' | 'item', name: string, actorId: string, itemId: string, requiresTarget: boolean }`. Mockup per CONTEXT.md §Specifics:
     ```
     ┌────────────────────────────────────┐
     │ AZIONE: Palla di Fuoco             │
     │  [tap]   Lancia incantesimo        │
     │  [long]  Mostra dettagli           │
     │  [×2]    Annulla                   │
     └────────────────────────────────────┘
     ```
     - Tap → emit `tool.invoke` (cast-spell or use-item) + onClose. If `requiresTarget === true` AND no target hardcoded → the modal does NOT emit; instead it calls `onClose()` AND immediately the caller (Plan 08-05 wires this) opens a TargetPickerPanel via pushOverlay. Phase 8 minimal: Plan 08-03 modal emits the envelope WITHOUT targets array (`targets: []`); Plan 08-05 detects `requiresTarget` and substitutes a TargetPicker pushOverlay before the modal's emission.
     - Long-press → no-op (Phase 9 spell detail viewer). Telemetry: console.warn('[action-options-modal] long-press details viewer not yet implemented (Phase 9)').
     - Double-tap → onClose() without emit (cancel).

  2. **Spellbook long-press wiring** — `SpellbookPanel.onEvent('long-press')` currently returns silently (Phase 6 NAV-02 routes long-press to Quick Action menu via router-level dispatcher). Plan 08-03 INVERTS this priority for spell rows specifically by intercepting long-press at the panel level via a new `openActionOptions` callback injected at construction.
     - SpellbookPanel constructor gains a new optional 8th parameter: `openActionOptions?: (req: ActionOptionsRequest) => void`. When provided, onEvent('long-press') calls `openActionOptions(currentSpellRequest)` BEFORE returning. When omitted (legacy / Phase 5 boot path), long-press stays a no-op stub — Phase 6 router-level dispatcher then mounts Quick Action menu.
     - INV-5 NOTE: This is the FIRST panel-level long-press handler. The router-level dispatcher (quick-action-long-press-dispatcher.ts) ALSO subscribes to the gesture bus and ALSO sees long-press. Both fire. To prevent both effects: the dispatcher MUST short-circuit when `top.id === 'action-options-modal'` (similar to its existing `top.id === 'quick-action-menu'` short-circuit). Plan 08-03 EXTENDS quick-action-long-press-dispatcher.ts with this short-circuit — but Plan 08-05 (which already touches boot-engine-core.ts) is the more natural place; Plan 08-03 just documents the requirement in the modal's JSDoc + adds an integration test to be implemented in 08-05.
     - ALTERNATIVE chosen (simpler, no router patch): SpellbookPanel's onEvent('long-press') calls `openActionOptions(req); event.preventDefault?.()` — but R1Gesture has no preventDefault. The router-level dispatcher will mount Quick Action menu on TOP of ActionOptionsModal via pushOverlay (suspending it). When the user closes the menu via [X], popOverlay restores the modal. This is acceptable MVP behaviour per CONTEXT.md §Specifics — the user can dismiss the menu and the modal is still there. INV-5 chip will name the long-press target as "quick" so the user knows long-press from the modal goes to menu.

  3. **Inventory long-press wiring** — same shape as Spellbook. InventoryPanel constructor gains optional `openActionOptions` callback; long-press dispatches when provided.

  4. 2 INV-1 fixtures + ~4 new i18n keys (`action_options_title_template`, `action_options_tap_label`, `action_options_long_label`, `action_options_cancel_label`, `hud_r1_action_options`).

Purpose: This is the BRIDGE between Phase 5's read-only panels and Phase 7's write path. Without ActionOptionsModal, scrolling through spells/items has no commit surface — long-press currently just opens the Quick Action menu. Plan 08-03 makes long-press from Spellbook/Inventory the action-fire trigger.

Output:
  - 1 new modal class + test
  - 2 modified panels (constructor signature extension + long-press dispatch)
  - 2 INV-1 fixtures (spell + item variants)
  - 4-5 new i18n keys
  - 9 file commits total
  - Plan 08-05 wires the `openActionOptions` callback into boot-engine-core.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-manual-action-ux/08-CONTEXT.md
@.planning/phases/08-manual-action-ux/08-01-PLAN.md
@.planning/phases/08-manual-action-ux/08-02-PLAN.md

# Plan 08-03 runs AFTER Plan 08-02 in Wave 2 — i18n-budgets.ts overlap with 08-02 forces sequential
# foreground execution (Phase 4a/4b precedent: zero same-wave files_modified overlap).
# 08-04 and 08-05 are Wave 3 — DISJOINT files from 08-03 verified.

# Exemplars to mirror — modal shape + long-press dispatch
@packages/g2-app/src/panels/concentration-drop-modal.ts
@packages/g2-app/src/panels/template-placement-panel.ts

# Phase 5 panel long-press stubs to extend
@packages/g2-app/src/panels/spellbook-panel.ts
@packages/g2-app/src/panels/inventory-panel.ts

# Phase 5 panel test patterns
@packages/g2-app/src/panels/__tests__/concentration-drop-modal.test.ts

# Phase 6 long-press dispatcher (the OTHER subscriber to long-press from gesture bus — INV-5 awareness)
@packages/g2-app/src/panels/quick-action-long-press-dispatcher.ts

# Phase 5 spell/item snapshot shapes
@packages/shared-protocol/src/payloads/character.ts

<interfaces>
<!-- Phase 6 INV-5 architecture — the router-level long-press dispatcher subscribes PERSISTENTLY to the bus, and the panel-level handler also subscribes via onMount. BOTH fire on long-press by design. See INVARIANTS.md §5: "every R1 gesture maps to exactly one PANEL handler call". The dispatcher is router-level (not a panel) so this is permitted. -->

<!-- Phase 5 spell/item schema (the panel reads these via onSnapshot — Plan 08-03 reuses) -->
From packages/shared-protocol/src/payloads/character.ts (Plan 5-04):
```typescript
export const SpellEntrySchema = z.strictObject({
  id: z.string().min(1),
  name: z.string().min(1),
  level: z.number().int().min(0).max(9),
  prepared: z.boolean(),
  concentration: z.boolean(),
  activation: SPELL_ACTIVATION_TYPES,
  range: z.string(),
  effectSummary: z.string(),
  requiresTarget: z.boolean().optional(),  // ★ Phase 8 addition? Check character.ts — if absent, add to Plan 08-03 scope.
});

export const InventoryItemSchema = z.strictObject({
  id: z.string().min(1),
  name: z.string().min(1),
  type: InventoryItemTypes,
  damage: z.string().nullable().optional(),
  tags: z.array(z.string()),
  weight: z.number().nonnegative(),
  quantity: z.number().int().nonnegative(),
});
```

<!-- IMPORTANT FOR EXECUTOR: Verify SpellEntrySchema.requiresTarget field is present in current character.ts. If absent, this Plan does NOT add it — Plan 08-03 derives requiresTarget heuristically inside SpellbookPanel: `requiresTarget = entry.range !== 'self' && entry.range !== 'pp' && entry.activation !== 'reaction'` (Phase 8 broad heuristic — Phase 9 refines). Inventory items default `requiresTarget = item.type === 'consumable' ? false : true` — potions used on self by default. -->

<!-- Modal exemplar — exact constructor + lifecycle to mirror -->
From packages/g2-app/src/panels/concentration-drop-modal.ts:
```typescript
// Constructor: (bridge, ws, gestureBus, conflict, locale, sessionId, onClose)
// onEvent('tap') → ws.send canonical envelope + onClose
// onEvent('double-tap') → onClose without emission
// Container: single 'overlay-block' text container
```

<!-- Phase 5 spellbook scrollOffset state — Plan 08-03 reads `currentSpell()` (computed from scrollOffset + filtered spells list) -->
From packages/g2-app/src/panels/spellbook-panel.ts (Phase 5 Plan 04):
- scrollOffset: number
- snapshot: CharacterSnapshot | null
- Internal computation: spells = snapshot.spells.entries; highlightedIdx = scrollOffset; current = spells[highlightedIdx]
- Spell rendering: renderSpellbookStandaloneContent(snapshot, locale, scrollOffset)
- Plan 08-03 must add a private method `_currentSpellRequest(): ActionOptionsRequest | null` that returns null when snapshot is null or spells is empty, else the request payload built from spells[highlightedIdx].

<!-- Phase 5 inventory pattern — same shape -->
From packages/g2-app/src/panels/inventory-panel.ts (Phase 5 Plan 04):
- scrollOffset: number, snapshot: CharacterSnapshot | null
- Internal computation: items = snapshot.inventory.items; highlightedIdx = scrollOffset; current = items[highlightedIdx]
- _currentItemRequest(): ActionOptionsRequest | null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionOptionsModal (z=2 OverlayPanel) + 2 INV-1 fixtures + 4 i18n keys</name>
  <files>
    packages/g2-app/src/panels/action-options-modal.ts,
    packages/g2-app/src/panels/action-options-modal.test.ts,
    packages/g2-app/src/status-hud/i18n-budgets.ts,
    packages/shared-render/src/fixtures/action-options-modal.spell.it.txt,
    packages/shared-render/src/fixtures/action-options-modal.item.it.txt
  </files>
  <behavior>
    - AOM-01: Panel ID is `'action-options-modal'`. `static meta` OMITTED (system overlay — same pattern as ConcentrationDropModalPanel).
    - AOM-02: Constructor: `(bridge, ws, gestureBus, request: ActionOptionsRequest, locale, sessionId: string, onClose: () => void)`.
    - AOM-03: ActionOptionsRequest interface: `{ readonly kind: 'spell' | 'item'; readonly name: string; readonly actorId: string; readonly itemId: string; readonly requiresTarget: boolean; }`.
    - AOM-04: onMount subscribes to bus; onUnmount unsubscribes (T-4b-01-03 mitigation per concentration-drop-modal.ts).
    - AOM-05: onEvent('tap') → IF `request.requiresTarget === true`: call `onClose()` WITHOUT emitting (the boot caller from Plan 08-05 detects this and immediately opens TargetPickerPanel with the request's args). IF `requiresTarget === false`: emit canonical EnvelopeSchema tool.invoke envelope + `onClose()`. The envelope has `payload: { toolId: <'cast-spell' | 'use-item'>, idempotencyKey: crypto.randomUUID(), args: { actor_id: request.actorId, <spell_id | item_id>: request.itemId, targets: [] } }`. The shared-protocol CastSpellInputSchema accepts empty targets array — verified at Wave 1.
    - AOM-06: onEvent('double-tap') → onClose() without emit (cancel).
    - AOM-07: onEvent('long-press') → console.warn('[action-options-modal] details viewer not yet implemented (Phase 9)'). Does NOT call onClose. The router-level long-press dispatcher will ALSO fire and mount QuickActionMenu on top — accepted MVP behaviour (CONTEXT §Specifics).
    - AOM-08: onEvent('scroll') → ignored.
    - AOM-09: `getContainerCount()` returns `{ image: 0, text: 1 }`.
    - AOM-10: `getR1Hints()` returns parseR1HintString(getLabel('hud_r1_action_options', locale)).
    - AOM-11: draw emits ONE bridge.textContainerUpgrade with newline-joined 12-row modal content. Frame: top border `┌─[ AZIONE ]──...──┐`, blank, AZIONE: <name> (truncated to 30 cps), blank, `[tap]   <action_label>` line, `[long]  <details_label>` line, `[×2]    <cancel_label>` line, blank rows, bottom border.
    - AOM-12: `<action_label>` is `getLabel('action_options_tap_label_spell', locale)` for spell ('Lancia incantesimo' IT / 'Cast spell' EN) OR `_item` for item ('Usa oggetto' IT / 'Use item' EN). 2 separate i18n keys for clarity.
    - AOM-13: W-4 envelope round-trip: emitted envelope JSON parses + EnvelopeSchema.safeParse + ToolInvocationEnvelopePayloadSchema.safeParse both pass.
    - AOM-14: INV-1 fixture round-trip — `action-options-modal.spell.it.txt` matches matchAsciiFixture for `{kind:'spell', name:'Palla di Fuoco'}`.
    - AOM-15: INV-1 fixture round-trip — `action-options-modal.item.it.txt` matches for `{kind:'item', name:'Pozione di Guarigione'}`.
    - AOM-16: When `requiresTarget === true`, tap does NOT call ws.send — verified via `expect(ws.send).not.toHaveBeenCalled()`.
    - I18N-01: 5 new keys present:
      - `action_options_title`: `{ it: 'AZIONE', en: 'ACTION', de: 'AKTION', max: 10 }`
      - `action_options_tap_label_spell`: `{ it: 'Lancia incantesimo', en: 'Cast spell', de: 'Zauber wirken', max: 24 }`
      - `action_options_tap_label_item`: `{ it: 'Usa oggetto', en: 'Use item', de: 'Gegenstand verw.', max: 24 }`
      - `action_options_long_label`: `{ it: 'Mostra dettagli', en: 'Show details', de: 'Details zeigen', max: 24 }`
      - `action_options_cancel_label`: `{ it: 'Annulla', en: 'Cancel', de: 'Abbrechen', max: 12 }`
      - `hud_r1_action_options`: `{ it: 'conferma|—|annulla', en: 'confirm|—|cancel', de: 'best.|—|abbr.', max: 30 }` (pipe-separated; scroll segment is `—` because modal ignores scroll per AOM-08)
  </behavior>
  <action>
    Create `packages/g2-app/src/panels/action-options-modal.ts` mirroring `concentration-drop-modal.ts` verbatim:

    1. JSDoc header citing Plan 08-03, CONTEXT.md §Specifics (mockup), CONTEXT.md §Decisions Area 1 (Launch surface), AOM-05 requiresTarget=true branch (no emit; Plan 08-05 caller orchestrates TargetPicker handoff), AOM-07 router-level long-press still fires (MVP accepted behavior).

    2. Local types:
       - `export interface ActionOptionsRequest { readonly kind: 'spell' | 'item'; readonly name: string; readonly actorId: string; readonly itemId: string; readonly requiresTarget: boolean; }`
       - `export interface ActionOptionsWebSocket { send(data: string): void; }`
       - `export type ActionOptionsCloseHandler = () => void;`

    3. Class `export class ActionOptionsModal implements OverlayPanel` — constructor + onMount/onUnmount/onEvent/draw/destroy/getContainerCount/getR1Hints per AOM-* behaviors.

    4. `declare const crypto: { randomUUID(): string };` per concentration-drop-modal.ts pattern.

    5. `_buildLines()` private — returns 12-row newline-joined modal content using `_innerRow(text)` helper (60-char outer width, 56-char inner). Frame uses `┌─[ AZIONE ]─...─┐` top border, blank rows, hard-wrapped instruction lines, bottom border. AOM-12 branches `[tap]` line label on request.kind.

    6. Co-located test file with 16 AOM-* tests. Use vi.fn() for bridge + ws; real PanelGestureBus.

    7. Append 5-6 i18n keys to HUD_WIDTH_BUDGETS in `packages/g2-app/src/status-hud/i18n-budgets.ts` under a "// Phase 8 — action options modal labels" block. Verify code-point length ≤ max for each value.

    8. INV-1 fixtures — seed via `vitest --update` (Phase 5 Plan 06 pattern) then verify against CONTEXT.md mockup.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run action-options-modal &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    action-options-modal.ts + test exist; 16 AOM-* tests pass; W-4 round-trip verified for non-target case; INV-1 fixtures (spell + item) match character-perfect; 6 i18n keys present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spellbook + Inventory long-press wiring — openActionOptions injected callback</name>
  <files>
    packages/g2-app/src/panels/spellbook-panel.ts,
    packages/g2-app/src/panels/spellbook-panel.test.ts,
    packages/g2-app/src/panels/inventory-panel.ts,
    packages/g2-app/src/panels/inventory-panel.test.ts
  </files>
  <behavior>
    - SBP-LP-01: SpellbookPanel constructor signature gains optional 8th parameter `openActionOptions?: (req: ActionOptionsRequest) => void`. Backward-compatible (Phase 5 boot path can omit; tests can omit).
    - SBP-LP-02: When `openActionOptions` is provided AND `snapshot?.spells?.entries[scrollOffset]` exists, onEvent('long-press') calls `openActionOptions(req)` where `req = { kind: 'spell', name: spell.name, actorId: snapshot.id, itemId: spell.id, requiresTarget: <heuristic> }`. The heuristic: `spell.range !== 'self' && spell.range !== '' && spell.activation !== 'reaction'`.
    - SBP-LP-03: When `openActionOptions` is omitted, onEvent('long-press') is a no-op (preserves Phase 5 behavior — router-level dispatcher mounts Quick Action menu).
    - SBP-LP-04: When `openActionOptions` is provided BUT snapshot is null OR scrollOffset out of bounds → onEvent('long-press') is a no-op (defensive — empty spellbook should NOT open ActionOptionsModal). console.warn('[spellbook-panel] long-press with no spell at scrollOffset — no-op').
    - SBP-LP-05: SpellbookPanel.test.ts adds SBP-LP-01..04 tests. Existing Spellbook tests stay green (no regression).
    - INV-LP-01: InventoryPanel constructor signature gains optional 8th parameter `openActionOptions?: (req: ActionOptionsRequest) => void`.
    - INV-LP-02: When provided AND `snapshot?.inventory?.items[scrollOffset]` exists, onEvent('long-press') calls `openActionOptions({ kind: 'item', name: item.name, actorId: snapshot.id, itemId: item.id, requiresTarget: <heuristic> })`. Heuristic: `item.type === 'consumable' ? false : true` (potions used on self).
    - INV-LP-03: Backward-compat — omitted callback → no-op long-press.
    - INV-LP-04: Defensive empty-inventory case — no-op + console.warn.
    - INV-LP-05: InventoryPanel.test.ts adds INV-LP-01..04 tests; existing Inventory tests stay green.
  </behavior>
  <action>
    Edit `packages/g2-app/src/panels/spellbook-panel.ts`:

    1. Add import at top: `import type { ActionOptionsRequest } from './action-options-modal.js';`

    2. Add private readonly field at constructor — extend constructor signature:
       ```ts
       constructor(
         bridge: EvenAppBridge,
         gestureBus: PanelGestureBus,
         locale: HudLocale,
         // ... existing 4-7 args (verify count via Read on Phase 5 source)
         private readonly openActionOptions?: (req: ActionOptionsRequest) => void,
       )
       ```
       Verify the exact existing arity by reading spellbook-panel.ts first. If Phase 5 used a 3-arg constructor (bridge, gestureBus, locale per PanelConstructor type), add `openActionOptions?` as the 4th arg. The PanelConstructor type signature might need a Phase 8 widening — defer to executor's reading of the file.

    3. In onEvent('long-press') case, replace the stub `// Phase 6 Quick Action stub` with:
       ```ts
       case 'long-press': {
         if (this.openActionOptions === undefined) {
           // Phase 5 backward-compat: omitted callback → no-op (router-level dispatcher handles)
           break;
         }
         const spells = this.snapshot?.spells?.entries ?? [];
         const idx = Math.max(0, Math.min(this.scrollOffset, spells.length - 1));
         const spell = spells[idx];
         if (this.snapshot === null || spell === undefined) {
           console.warn('[spellbook-panel] long-press with no spell at scrollOffset — no-op');
           break;
         }
         const requiresTarget = spell.range !== 'self' && spell.range !== '' && spell.activation !== 'reaction';
         this.openActionOptions({
           kind: 'spell',
           name: spell.name,
           actorId: this.snapshot.id,
           itemId: spell.id,
           requiresTarget,
         });
         break;
       }
       ```

    4. Add 4 SBP-LP-* tests to spellbook-panel.test.ts. Use vi.fn() as `openActionOptions` spy; verify call args.

    Edit `packages/g2-app/src/panels/inventory-panel.ts` mirror-image:

    1. Same import at top.
    2. Same constructor extension (verify existing arity first).
    3. onEvent('long-press') replacement:
       ```ts
       case 'long-press': {
         if (this.openActionOptions === undefined) break;
         const items = this.snapshot?.inventory?.items ?? [];
         const idx = Math.max(0, Math.min(this.scrollOffset, items.length - 1));
         const item = items[idx];
         if (this.snapshot === null || item === undefined) {
           console.warn('[inventory-panel] long-press with no item at scrollOffset — no-op');
           break;
         }
         const requiresTarget = item.type !== 'consumable';
         this.openActionOptions({
           kind: 'item',
           name: item.name,
           actorId: this.snapshot.id,
           itemId: item.id,
           requiresTarget,
         });
         break;
       }
       ```
    4. Add 4 INV-LP-* tests.

    CRITICAL: The PanelConstructor type in panel-router.ts uses a 3-arg constructor shape (`new(bridge, gestureBus, locale)`). Adding an optional 4th+ arg is backward-compatible — Vite's `import.meta.glob` discovery + PanelRouter.openPanel construct panels with 3 args. The `openActionOptions` callback is injected at MOUNT time (Plan 08-05 boot wiring) via either:
      (a) A factory pattern similar to `makeMenu` in boot-engine-core.ts (Plan 08-05 owns this), OR
      (b) A setter method `setActionOptionsHandler(handler)` on the panel — Plan 08-03 ships option (b) since it doesn't touch boot-engine-core.ts.

    REVISED Plan 08-03 design: Add `public setActionOptionsHandler(handler: ((req: ActionOptionsRequest) => void) | null): void` to both panels INSTEAD of a constructor arg. Plan 08-05 calls `panel.setActionOptionsHandler(...)` after panel.openPanel resolves. This avoids breaking the PanelConstructor signature and matches Phase 5 dependency-injection-after-mount conventions.

    Update behaviors SBP-LP-01..04 + INV-LP-01..04 accordingly: tests call `panel.setActionOptionsHandler(spy)` before firing long-press gesture; "omitted" case tests skip the setter call.
  </action>
  <verify>
    <automated>pnpm --filter @evf/g2-app test -- --run spellbook-panel inventory-panel</automated>
  </verify>
  <done>
    spellbook-panel.ts + inventory-panel.ts both expose `setActionOptionsHandler(handler)`; long-press dispatches via the handler when set; backward-compat preserved when handler unset; 8 new tests (4 SBP-LP + 4 INV-LP) pass; existing Phase 5 panel tests remain green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PanelGestureBus → SpellbookPanel/InventoryPanel.onEvent | In-process — trust owned by r1-event-source.ts upstream |
| Spellbook/Inventory.onEvent('long-press') → openActionOptions callback | In-process callback — Plan 08-05 owns the boot-side handler that calls PanelRouter.pushOverlay |
| ActionOptionsModal.onEvent('tap') → ws.send | Outgoing tool.invoke envelope → bridge; validated server-side via ToolInvocationEnvelopePayloadSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering / Spoofing | ActionOptionsModal outgoing envelope | mitigate | Same as Plan 08-02 TPP-14 — canonical EnvelopeSchema shape with `payload` field; W-4 round-trip verified AOM-13. `idempotencyKey` is fresh crypto.randomUUID(). |
| T-08-01-02 | Tampering | requiresTarget=true bypassing target picker | mitigate | When requiresTarget=true, modal tap returns WITHOUT calling ws.send (AOM-05 + AOM-16 verify). Plan 08-05 caller detects this case and routes through TargetPickerPanel. Server-side fallback: ToolInvocationEnvelopePayloadSchema requires `args` to pass downstream handler.argsSchema — empty `targets: []` is permitted by CastSpellInputSchema (Phase 7 invariant) so non-targeted casts also pass (player chose 'cast on self'). |
| T-08-03-01 | Race condition | Long-press fires BOTH ActionOptionsModal (via panel handler) AND QuickActionMenu (via router-level dispatcher) | accept | MVP accepted behaviour per CONTEXT.md §Specifics: QuickActionMenu mounts on top via pushOverlay, suspending the modal onto overlayStack. User closes menu with [X] → popOverlay restores the modal. Documented in ActionOptionsModal JSDoc. Future enhancement: extend router-level dispatcher to short-circuit when `top.id === 'action-options-modal'`. Plan 08-05 MAY add this short-circuit if integration smoke surfaces a UX issue. |
| T-08-03-02 | Stack overflow | Recursive long-press from ActionOptionsModal → opens nested modal | accept | AOM-07 ignores long-press on the modal itself; router-level dispatcher mounts QuickActionMenu (not ActionOptionsModal) so no recursion. Single suspension depth (Phase 6 invariant: overlayStack max depth is 1 in MVP). |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 workspace-wide.
- `pnpm test --filter @evf/g2-app action-options-modal spellbook-panel inventory-panel` → all green: 16 AOM-* + 4 SBP-LP-* + 4 INV-LP-* + ALL existing Phase 5 panel tests.
- INV-1 fixture round-trip green (matchAsciiFixture spell + item variants).
- W-4 grep gate: `grep -E 'WireEnvelopeSchema|envelope\\.value' packages/g2-app/src/panels/action-options-modal.ts` → 0 matches.
- 6 new i18n keys present + within budget (verify via existing i18n-budgets integrity test).
- `pnpm lint:ci` exits 0.
</verification>

<success_criteria>
- ActionOptionsModal renders 2 variants (spell + item) character-perfect per CONTEXT.md mockup.
- Modal tap path:
  - requiresTarget=false → ws.send canonical tool.invoke + onClose (AOM-05 verified).
  - requiresTarget=true → onClose without emit (Plan 08-05 caller handles target pick).
- SpellbookPanel.setActionOptionsHandler(spy) → long-press triggers spy with correct ActionOptionsRequest payload.
- InventoryPanel.setActionOptionsHandler(spy) → long-press triggers spy.
- INV-1 fixtures (2) committed; 5-6 i18n keys committed.
- No changes to boot-engine-core.ts (Plan 08-05 wires).
- registerComplexHandler count remains 14 (no new socketlib handler).
- CI Gate 8 green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-manual-action-ux/08-03-SUMMARY.md` documenting:
- One-liner: ActionOptionsModal z=2 + Spellbook/Inventory long-press wiring via setActionOptionsHandler injection point.
- Tasks completed table.
- AOM-* + SBP-LP-* + INV-LP-* test count.
- INV-1 fixture paths.
- Documented MVP-accepted behaviour: router-level long-press dispatcher still mounts QuickActionMenu over the modal (CONTEXT §Specifics acceptance).
- Self-Check: PASSED block.
</output>
