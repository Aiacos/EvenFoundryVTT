---
phase: 10-polish-field-test-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - docs/README.md
  - docs/setup-guide.md
  - docs/runbook.md
  - docs/firmware-compatibility.md
  - docs/field-test-template.md
  - Specs.md
  - README.md
  - docs/showcase/index.html
autonomous: false
requirements: []
must_haves:
  truths:
    - "docs/README.md is the canonical docs-folder landing page pointing to Specs.md + setup + showcase"
    - "docs/setup-guide.md is a 5-step quickstart: Foundry module install → bridge → plugin host → Even App config → R1 pair"
    - "docs/runbook.md covers ops: bridge restart, audit log inspection, bearer revoke, common errors"
    - "docs/firmware-compatibility.md states the verified Even Hub SDK version (0.0.10) + forward-compat policy"
    - "docs/field-test-template.md provides the NASA-TLX self-report template for SC-10-01 fill"
    - "Specs.md + README.md + docs/showcase/index.html all reconcile to a single coherent version stamp in ONE atomic commit (INV-3)"
    - "INV-3 boot-splash mockup line in Specs.md is corrected from v0.9.11 to current spec version"
  artifacts:
    - path: docs/README.md
      provides: "Docs folder index pointing to Specs / setup / runbook / firmware-compat / perf / showcase"
      min_lines: 30
    - path: docs/setup-guide.md
      provides: "End-to-end 5-step setup walkthrough + troubleshooting"
      min_lines: 80
    - path: docs/runbook.md
      provides: "Operational procedures: restart / audit / revoke / metrics / common errors"
      min_lines: 60
    - path: docs/firmware-compatibility.md
      provides: "Even Hub SDK version matrix + forward-compat range"
      contains: "0.0.10"
    - path: docs/field-test-template.md
      provides: "NASA-TLX + Borg CR-10 self-report template for SC-10-01"
      contains: "NASA-TLX"
  key_links:
    - from: docs/README.md
      to: docs/setup-guide.md
      via: "Quickstart link from docs index"
      pattern: "setup-guide.md"
    - from: docs/runbook.md
      to: packages/bridge/src/routes/healthz.ts
      via: "/healthz endpoint reference for ops verification"
      pattern: "/healthz"
    - from: docs/firmware-compatibility.md
      to: packages/g2-app/package.json
      via: "Pinned @evenrealities/even_hub_sdk@0.0.10 dependency"
      pattern: "even_hub_sdk"
    - from: docs/field-test-template.md
      to: .planning/phases/10-polish-field-test-mvp/10-CONTEXT.md
      via: "SC-10-01 closure procedure reference"
      pattern: "SC-10-01"
---

<objective>
Land the 5 documentation files for MVP shipping AND reconcile the version stamps across
Specs.md + README.md + docs/showcase/index.html in a SINGLE ATOMIC COMMIT per INV-3.

Purpose: Phase 10 ROADMAP SC-5 explicitly lists "README, setup guide, video demo, runbook,
firmware compatibility matrix all shipped under `docs/`" as a closure gate. Plus: the
INV verification suite (Plan 10-03) surfaces an existing INV-3 drift — Specs.md line ~2606
boot-splash mockup still reads `v0.9.11` while Specs.md header is `v0.9.12`. The atomic
coherence commit fixes the drift in the same PR as the new docs land, exactly per
`CLAUDE.md §Project Invariants > INV-3` and the Phase 1 Plan 03 atomic-commit precedent
(commit 671a22d).

Output: 5 new doc files + INV-3-coherent version stamps everywhere, committed atomically.
Video demo is OUT OF SCOPE for software (hardware-pending — recorded during the field-test
session); the docs structure references it as a placeholder for the field-test operator.

NOTE: This plan is NOT autonomous because the INV-3 atomic commit requires a
human-verify checkpoint to confirm the version-stamp reconciliation is correct before
committing. The plan-checker will verify the atomic-commit pattern (Specs.md + README.md
+ showcase + new docs in ONE commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-polish-field-test-mvp/10-CONTEXT.md
@.planning/phases/10-polish-field-test-mvp/10-03-PLAN.md
@CLAUDE.md
@docs/architecture/INVARIANTS.md
@README.md
@Specs.md
@docs/showcase/index.html
@packages/g2-app/package.json
@packages/bridge/src/routes/healthz.ts
@packages/bridge/src/routes/metrics.ts

<interfaces>
<!-- Even Hub SDK version pin (canonical, per Phase 0/1 research): -->

From `packages/g2-app/package.json:14`:
```json
"@evenrealities/even_hub_sdk": "0.0.10"
```

From STATE.md Quick Tasks 2026-05-14 `oq-inv2-4-hub-polyfill-via-evenrealities-sdk`:
> Discovered `@evenrealities/even_hub_sdk@0.0.10` (MIT, by Whiskee Chen @ Even Realities)
> already installed on dev machine. Read full 1292-line `index.d.ts` — canonical SDK exposes
> `EvenAppBridge` singleton. Hardware limits confirmed verbatim: image width 20-288, height 20-144.
> Container budget: containerTotalNum 1-12, textObject max 8, imageObject max 4.

Forward-compat policy: Even Hub SDK is pre-1.0 (semver-exempt by convention), so any minor
bump 0.0.X → 0.0.Y requires INV-2 re-verification per `CLAUDE.md §Pre-bump checklist` and a
Phase-0-style probe sweep (already documented in STATE.md Quick Tasks 2026-05-14).

<!-- Bridge operational endpoints (from Phase 3): -->
- `GET /healthz` → liveness (200 OK + version JSON)
- `GET /readyz` → readiness (200 OK when bearer registry loaded)
- `GET /metrics` → Prometheus exposition
- `POST /admin/bearer/revoke` → DM-side bearer revoke (Phase 7 bearer rotation)

<!-- Current version stamps (verified 2026-05-17 via grep across repo): -->
- Specs.md line 9: `# EvenFoundryVTT — Project Specification (v0.9.12)`
- Specs.md line ~2606: `║                              EVENFOUNDRYVTT  v0.9.11`   ← INV-3 DRIFT — must reconcile
- README.md line 6: `[![spec: v0.9.12]...`
- docs/showcase/index.html line ~463: `<span class="num">v0.9.12</span>`
- docs/showcase/index.html line ~1036: `design specification v0.9.12`

THE INV-3 ATOMIC COMMIT MUST RECONCILE THE BOOT-SPLASH MOCKUP TO v0.9.12 (or whatever
the current Specs.md header version is at commit time — verify via Plan 10-03 INV suite).

<!-- NASA-TLX format reference (canonical academic source: Hart & Staveland 1988): -->
6 dimensions × 21-point scale (Very Low → Very High):
1. Mental Demand
2. Physical Demand
3. Temporal Demand
4. Performance (reverse-scored: Good → Failure)
5. Effort
6. Frustration
Plus an optional Borg CR-10 (0-10) eye-fatigue scale per CONTEXT.md §Area3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 doc files (README + setup + runbook + firmware-compat)</name>
  <files>docs/README.md, docs/setup-guide.md, docs/runbook.md, docs/firmware-compatibility.md</files>
  <action>
Step A: `docs/README.md` — docs folder index. Sections: (1) `# EvenFoundryVTT — Docs`. (2) `## Where to start` — bullets pointing to: `Specs.md` (canonical SoT), `docs/setup-guide.md` (5-step install), `docs/runbook.md` (ops), `docs/firmware-compatibility.md` (Even Hub SDK matrix), `docs/perf/phase-10-latency.md` (latency profile — hardware-pending), `docs/showcase/index.html` (animated showcase), `docs/architecture/INVARIANTS.md` (6 ratified invariants), `docs/architecture/README.md` (11 ADRs). (3) `## Field-test template` — link to `docs/field-test-template.md` (created in Task 2). (4) `## Project status` — link to README.md root + ROADMAP.

Step B: `docs/setup-guide.md` — 5-step quickstart. Sections: (1) `# Setup Guide — EvenFoundryVTT`. (2) `## Prerequisites` — Foundry VTT v13.347+ verified on v14; dnd5e ≥ 5.3.3; pnpm 10.33.4; Node 24 LTS; Even Realities G2 + R1 ring + Even Realities App (latest). (3) `## Step 1: Install the Foundry module` — manifest URL `https://github.com/Aiacos/EvenFoundryVTT/releases/latest/download/module.json` + `relationships.requires` for socketlib + midi-qol. (4) `## Step 2: Run the Bridge service` — Docker Compose one-liner from `packages/bridge/Dockerfile` + env vars (BRIDGE_PORT, BEARER_REGISTRY_PATH). (5) `## Step 3: Host the plugin (g2-app)` — `pnpm --filter @evf/g2-app build` + serve `dist/` via any static HTTPS host (Caddy / Cloudflare Pages); whitelist the origin in the Even Realities App `app.json` per Even Hub network constraint. (6) `## Step 4: Configure the Even Realities App` — paste plugin URL + bridge URL into the Even App per-plugin settings UI on the phone. (7) `## Step 5: Pair G2 → Foundry` — DM clicks "Pair a G2 device" in Foundry module Settings → scan QR with Even App. (8) `## Troubleshooting` — table of common errors: "Failed to fetch package manifest" (no GitHub Release yet — install in dev mode); "WS handshake fails" (check bridge bearer + CORS whitelist); "⚠ SYNC LOST persists" (verify bridge reachable from Even App phone network — see runbook); "MidiQOL autoFastForward off" (Foundry settings → MidiQOL → Workflow → Auto fast-forward).

Step C: `docs/runbook.md` — operational procedures. Sections: (1) `# Runbook — EvenFoundryVTT Bridge`. (2) `## Restart the bridge` — `docker compose -f deploy/docker-compose.yml restart bridge` + how to verify via `curl http://bridge:8910/healthz`. (3) `## Inspect the audit log` — chat-message audit log location (per Phase 7 Plan 01); how to grep for player + action type. (4) `## Revoke a bearer token` — Foundry module Settings → "Revoke G2 device" OR `curl -X POST -H 'Authorization: Bearer <dm-bearer>' http://bridge:8910/admin/bearer/revoke` with body `{deviceId, reason}` (Phase 2 CONN-05). (5) `## Metrics / logs` — Prometheus `/metrics`; pino structured logs to stdout; shipping to Loki/CloudWatch reference (Phase 13 stretch — local Docker logs for MVP). (6) `## Common errors with recovery` — table: `⚠ SYNC LOST` (reconnect loop; see Plan 10-01); `BOOT_HANDSHAKE_FAIL` (check bridge READY); `TOKEN_EXPIRED` (re-pair via QR); `MIDIQOL_AUTO_FAST_FORWARD_OFF` (re-run Phase 0 MidiQOL probe). (7) `## See also` — link to Plan 10-01 SUMMARY + Plan 10-02 perf-probe doc.

Step D: `docs/firmware-compatibility.md` — Even Hub SDK matrix. Sections: (1) `# Firmware & SDK Compatibility`. (2) `## Verified versions` — table: `Component | Version | Verified date | Source` with rows for `@evenrealities/even_hub_sdk` (0.0.10, 2026-05-14, STATE.md Quick Tasks `oq-inv2-4-hub-polyfill-via-evenrealities-sdk`), G2 firmware (model "g2" per simulator probe `getGlassesInfo`, 2026-05-14, STATE.md Quick Tasks `adr-0005-oq-inv2-1-resolution-via-simulator`), Foundry VTT (v13.347 minimum, verified v14, 2026-05-10, system.json release-5.3.3), dnd5e system (5.3.3, 2026-05-07, github.com/foundryvtt/dnd5e/releases). (3) `## Hardware limits (verbatim per SDK index.d.ts)` — image width 20-288 px, height 20-144 px; containerTotalNum 1-12; textObject max 8; imageObject max 4; capture container max 1 with `isEventCapture: 1` per Specs §3.1. (4) `## Forward-compat policy` — pre-1.0 SDK (semver-exempt convention) → every minor bump (`0.0.10 → 0.0.11`) requires INV-2 re-verification per `CLAUDE.md §Pre-bump checklist` + a Phase-0-style probe sweep. (5) `## See also` — link to Specs.md §3.1 + STATE.md Quick Tasks 2026-05-14.

ALL FOUR FILES: write directly to disk with the Write tool — DO NOT inline code in this action. Each file follows GitHub-flavored Markdown. Reference D-Area2 verbatim where the file matches a D-Area2 deliverable.
  </action>
  <verify>
    <automated>for f in docs/README.md docs/setup-guide.md docs/runbook.md docs/firmware-compatibility.md; do test -f "$f" && wc -l "$f"; done && grep -q "0.0.10" docs/firmware-compatibility.md && grep -q "Step 5" docs/setup-guide.md && grep -q "/healthz" docs/runbook.md</automated>
  </verify>
  <done>4 doc files exist on disk, each ≥30 lines; firmware-compat references SDK 0.0.10; setup-guide has 5 numbered steps + troubleshooting; runbook references /healthz + bearer revoke endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Field-test template (NASA-TLX + Borg CR-10) for SC-10-01 fill</name>
  <files>docs/field-test-template.md</files>
  <action>Create `docs/field-test-template.md` — empty NASA-TLX self-report form the GM/player fills in once a real session lands (SC-10-01 closure). Sections: (1) `# Field-Test Session — Self-Report Template`. (2) `## Session metadata` — empty fields: date, session number (1, 2, …), duration (hours), DM consent (yes/no), G2 firmware version, R1 firmware version, environment (clean / 5GHz-loaded / 2.4GHz+microwave per Specs §10.0.3 envelope). (3) `## NASA-TLX (Hart & Staveland 1988)` — table with 6 dimensions × 21-point scale (Very Low → Very High; Performance is reverse-scored Good → Failure): `Dimension | Score (0-20) | Notes` rows for: Mental Demand, Physical Demand, Temporal Demand, Performance (reverse), Effort, Frustration. (4) `## Borg CR-10 eye-fatigue` — single 0-10 scale row with anchors (0=nothing at all, 10=extremely strong) per CONTEXT §Area3. (5) `## Incidents` — empty list for: SYNC LOST events (count + recovery time), RF degrade events, microwave-test events, R1 disconnects, MidiQOL stalls. (6) `## Latency observations` — reference `docs/perf/phase-10-latency.md` for the perf-probe measurement table; ask the operator to attach raw `r1.perf.sample` envelope dumps. (7) `## SC-10-01 / SC-10-02 / SC-10-03 closure checkboxes` — three boxes: SC-10-01 multi-session field test + NASA-TLX completed, SC-10-02 p50 latency ≤ 400ms measured, SC-10-03 microwave/2.4 GHz worst-case glyph fallback fires cleanly. (8) `## See also` — link to ADR-0005 Branch A `human_needed` gate + 10-CONTEXT §Area3.

Write the file directly via Write tool — DO NOT inline content in this action description. The template is a fill-in-the-blank deliverable; markdown checkboxes + empty table cells + placeholders like `_pending_`.</action>
  <verify>
    <automated>test -f docs/field-test-template.md && grep -q "NASA-TLX" docs/field-test-template.md && grep -q "Borg CR-10" docs/field-test-template.md && grep -c "SC-10-0" docs/field-test-template.md | awk '{exit ($1 < 3)}'</automated>
  </verify>
  <done>Template exists with all 8 sections; NASA-TLX + Borg CR-10 referenced; all 3 SC-10-0X carry-forward closure checkboxes present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: INV-3 atomic coherence commit — Specs.md + README.md + showcase + new docs in ONE commit</name>
  <files>Specs.md, README.md, docs/showcase/index.html, docs/README.md, docs/setup-guide.md, docs/runbook.md, docs/firmware-compatibility.md, docs/field-test-template.md</files>
  <action>Step 1 (automated, before checkpoint): Run `pnpm --filter @evf/validation-harness inv:all:skip-inv2` and capture which INV checks are red. INV-3 MUST report the boot-splash drift Specs.md line ~2606 `v0.9.11` vs Specs header `v0.9.12` (or whatever the current Specs version is at commit time). Step 2 (automated): Edit Specs.md line ~2606 boot-splash mockup `EVENFOUNDRYVTT  v0.9.11` to match the Specs.md header version (currently `v0.9.12`). Use the Edit tool with a unique-string anchor — pin the surrounding ASCII frame characters in the find string to avoid ambiguous matches. Re-run the INV suite to confirm INV-3 flips to green. Step 3 (human-verify gate — see <how-to-verify>): the operator inspects `git status` + `git diff --stat` and confirms the 5 new doc files, the Specs.md boot-splash mockup correction, and the INV suite green status. Step 4 (automated, post-resume-signal): Stage and commit atomically — `git add docs/README.md docs/setup-guide.md docs/runbook.md docs/firmware-compatibility.md docs/field-test-template.md Specs.md` then `git commit -m "docs(phase-10): ship MVP docs + INV-3 atomic boot-splash reconciliation"` with a message body listing the 5 doc files + the boot-splash version correction + Phase 1 Plan 03 atomic-commit precedent reference. NEVER inline implementation code in this action — the Edit tool fixes the boot-splash, the git commands run via Bash tool.</action>
  <what-built>
Tasks 1-2 produced 5 new doc files: docs/README.md, docs/setup-guide.md, docs/runbook.md, docs/firmware-compatibility.md, docs/field-test-template.md.

This task reconciles any drifting version stamps across Specs.md + README.md + docs/showcase/index.html (verified via the Plan 10-03 INV suite) and commits ALL changes atomically per INV-3.
  </what-built>
  <how-to-verify>
1. `pnpm --filter @evf/validation-harness inv:all:skip-inv2` — confirm all 5 INV checks are green (INV-2 skipped is acceptable). If INV-3 is still red, inspect the divergent stamps and re-edit. If INV-1 is red, the new docs broke an existing fixture — fix forward.

2. `git status` + `git diff --stat` BEFORE committing: confirm the staged set includes the 5 new doc files + Specs.md (boot-splash line only) and EXCLUDES any unrelated changes.

3. `grep -n 'EVENFOUNDRYVTT' Specs.md` — confirm boot-splash mockup line shows the current spec version (matching the Specs.md header line 9).

4. `pnpm lint:ci` exits 0 (no markdownlint surprises in the new docs).

5. After commit: `git log -1 --name-only HEAD` shows the 6 files in a SINGLE commit (no separate Specs commit, per Phase 1 Plan 03 precedent 671a22d).
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @evf/validation-harness inv:all:skip-inv2 2>&1 | grep -E "INV-3.*\\b(green|red)\\b" | grep -qv red</automated>
  </verify>
  <done>INV suite reports INV-3 green; atomic commit landed touching Specs.md + the 5 new doc files in a single `git log -1 --name-only HEAD` entry; boot-splash mockup version matches Specs.md header version.</done>
  <resume-signal>Type "approved" if the atomic commit landed and INV suite reports green, or describe the drift if reconciliation is still incomplete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ → end user | Setup guide instructs users to run Docker containers + paste URLs into Even Realities App; incorrect docs could cause misconfiguration (e.g. token leak via wrong CORS origin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Information disclosure | `docs/setup-guide.md` (could instruct an operator to whitelist `*` in CORS, leaking bearer tokens) | mitigate | Setup guide MUST cite "Origin-complete URLs (plugin host + bridge URL only); NEVER wildcards" verbatim from CLAUDE.md §Constraints / app.json whitelist + Specs §3.3. Verified by code review during human-verify checkpoint |
| T-10-06 | Tampering | `docs/runbook.md` (bearer-revoke procedure could be misused to lock out legitimate players) | accept | Revoke endpoint is bearer-authenticated to the DM; runbook only documents the procedure the DM already authorises. Standard single-tenant homelab MVP threat profile |
</threat_model>

<verification>
- 5 new doc files exist on disk and are ≥30 lines each
- Plan 10-03 INV suite reports INV-1..5 all green (or INV-2 skipped) after the atomic commit
- `git log -1 --name-only HEAD` shows Specs.md AND the 5 new docs in the SAME commit (INV-3 atomic discipline per Phase 1 Plan 03 precedent 671a22d)
- `grep -n 'v0\\.9\\.12' Specs.md README.md docs/showcase/index.html` returns identical version stamps across all 5 sites (per CLAUDE.md §Pre-bump checklist)
- `grep -q 'NASA-TLX' docs/field-test-template.md`
- `grep -q '0.0.10' docs/firmware-compatibility.md` (SDK pin documented)
- `grep -q 'Origin-complete\\|whitelist' docs/setup-guide.md` (T-10-05 mitigation present)
</verification>

<success_criteria>
- ROADMAP SC-5 software portion COMPLETE: 5 MVP docs shipped under docs/
- INV-3 atomic coherence: Specs.md + README.md + showcase reconciled in ONE commit (the new docs ride along — same commit)
- Pre-existing boot-splash mockup version drift (v0.9.11 vs v0.9.12) eliminated
- Field-test operator has a structured NASA-TLX template for SC-10-01 closure
- T-10-05 setup-guide CORS-wildcard mitigation in place
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-field-test-mvp/10-04-SUMMARY.md` with:
- 5 doc files shipped + INV-3 atomic-commit hash
- Drift items reconciled (with before/after version stamps)
- INV suite green/red table snapshot pre-commit and post-commit
- Reference to Plan 10-05 for phase closure / MVP closure
</output>
