---
phase: 10-polish-field-test-mvp
plan: "05"
type: execute
wave: 4
depends_on: ["10-04"]
files_modified:
  - .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "10-VERIFICATION.md exists with goal-backward audit across all 5 ROADMAP SCs"
    - "All 5 ROADMAP SCs marked PASS (software) with SC-10-01..03 carried as human_needed per ADR-0005 Branch A"
    - "STATE.md flipped to PHASE_10_CLOSED / MVP_SOFTWARE_COMPLETE with 32 hardware-pending total"
    - "ROADMAP.md Phase 10 checkbox flipped to [x] with completion date + 'MVP software-complete' annotation"
    - "All 11 MVP phase checkboxes (Phase 0..10) verified [x] in ROADMAP.md"
    - "Plan 10-03 INV suite reports green/red table — green ≥ 4 of 5 (INV-2 may be skipped)"
  artifacts:
    - path: .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md
      provides: "Goal-backward audit for all 5 Phase 10 SCs + 3 hardware-pending carry-forwards"
      min_lines: 80
      contains: "MVP software-complete"
    - path: .planning/STATE.md
      provides: "Updated state: PHASE_10_CLOSED + MVP_SOFTWARE_COMPLETE milestone signal"
      contains: "PHASE_10_CLOSED"
    - path: .planning/ROADMAP.md
      provides: "Phase 10 checkbox [x] + all MVP phase checkboxes verified [x]"
      contains: "Phase 10: Polish & Field Test MVP"
  key_links:
    - from: .planning/STATE.md
      to: .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md
      via: "Phase 10 closure references the verification document"
      pattern: "10-VERIFICATION.md"
    - from: .planning/ROADMAP.md
      to: .planning/phases/10-polish-field-test-mvp/
      via: "Phase 10 plan list completion"
      pattern: "10-05-PLAN.md"
---

<objective>
Close Phase 10 AND signal MVP software-complete. Produce 10-VERIFICATION.md (goal-backward
audit) covering all 5 ROADMAP SCs, document the 3 Phase-10 hardware-pending carry-forwards
(SC-10-01..03) bringing the project total to 32, flip STATE.md / ROADMAP.md / REQUIREMENTS.md
to the closure shape, and surface the next-step recommendation to the user (run
`/gsd-cleanup` to archive phase artifacts OR proceed to a milestone audit).

Purpose: This is the FINAL MVP phase. The project moves from "in development" to
"software-complete; hardware-pending field test" — a meaningful milestone signal that
unblocks the v0.9.11 milestone audit and the eventual real-G2-hardware closure pass per
ADR-0005 Branch A `human_needed` gate (close via `pnpm --filter @evf/validation-harness
validate:all` once the user has G2 + R1 + a willing DM in hand).

Output: 10-VERIFICATION.md, STATE.md + ROADMAP.md + REQUIREMENTS.md updates, atomic
closure commit. Plan-checker focus: 14-socketlib-handler invariant preserved, CI Gate 8
still passes, INV-3 atomic discipline upheld from Plan 10-04, hardware-pending SCs
declared (SC-10-01..03), ROADMAP Phase 10 flip + MVP-complete annotation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/templates/verification-report.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-polish-field-test-mvp/10-CONTEXT.md
@.planning/phases/10-polish-field-test-mvp/10-01-PLAN.md
@.planning/phases/10-polish-field-test-mvp/10-02-PLAN.md
@.planning/phases/10-polish-field-test-mvp/10-03-PLAN.md
@.planning/phases/10-polish-field-test-mvp/10-04-PLAN.md
@.planning/phases/09-action-economy-edge-cases/09-VERIFICATION.md
@docs/architecture/0005-phase0-go-no-go.md
@docs/architecture/INVARIANTS.md

<interfaces>
<!-- Pattern reference: 09-VERIFICATION.md frontmatter shape for closure consistency: -->

```yaml
---
phase: 10
slug: polish-field-test-mvp
status: human_needed
verified_at: "2026-05-XX"
must_haves_total: 5
must_haves_passed: 5
must_haves_failed: 0
hardware_pending: 3
test_count: <fill from `pnpm test --run 2>&1 | tail -3`>
test_files: <fill>
test_delta: "+<n> (2036 → <new>)"
commits: <fill>
mvp_signal: "software-complete"
---
```

Running hardware-pending totals (carried forward through phases):
- Phase 4a: 5 → Phase 4b: 10 → Phase 5: 11 (none added) → Phase 6: 11 → Phase 7: 18 → Phase 8: 21 → Phase 9: 26 (RECHECK: per STATE.md the running totals are 4a=5, 4b=10, 7=23, 8=26, 9=29 — phase 5+6 added 0).

Authoritative current total per STATE.md line 280: **29 hardware-pending SCs after Phase 9**.
Phase 10 adds 3 (SC-10-01..03 per CONTEXT.md §Area3) → **new running total: 32 hardware-pending SCs**.

CI Gate 8 (Phase 7 Plan 01): `grep -rn "activity\\.use(" packages/g2-app/src packages/bridge/src` MUST return 0 results. The plan-checker verifies this gate during closure.

14-socketlib-handler invariant (Phase 7 closure + Phase 8 + Phase 9 each re-confirmed at ISM-W*-09/10): `grep -c "socketlib\\.registerComplexHandler" packages/foundry-module/src/socketlib-handlers.ts` MUST return exactly `14`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 10-VERIFICATION.md — goal-backward audit</name>
  <files>.planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md</files>
  <action>Create `10-VERIFICATION.md` using the `09-VERIFICATION.md` format precedent. Frontmatter: `phase: 10`, `slug: polish-field-test-mvp`, `status: human_needed`, `verified_at`, `must_haves_total: 5`, `must_haves_passed: 5`, `must_haves_failed: 0`, `hardware_pending: 3`, `test_count` (fill from `pnpm test --run 2>&1 | tail -3`), `test_files`, `test_delta` (from 2036 → new total), `commits` (count of commits across Plans 10-01..10-04), `mvp_signal: software-complete`.

Body sections: (1) `# Phase 10 Verification — Goal-Backward Audit` quoting the ROADMAP §Phase 10 goal verbatim. (2) `## Verdict` — split into **SOFTWARE-SIDE: CLOSED ✅** (all 5 SCs met in software; INV-1..5 verification suite green per Plan 10-03; INV-3 atomic doc-coherence verified per Plan 10-04) AND **HARDWARE-SIDE: human_needed** (3 SCs SC-10-01..03 carry to ADR-0005 PROVISIONAL Branch A). (3) `## Success Criteria — Goal-Backward Check` — 5-row table copying the SC verbatim from ROADMAP §Phase 10 with columns `# | SC | Software | Hardware | Evidence`:
- SC-1 Bridge disconnect/Foundry restart/30s blip → ⚠ SYNC LOST + replay buffer resume (Specs §11.5.8.1) → ✅ PASS (Plan 10-01 evidence: SeqTracker + WsReconnectController + 2 INV-1 fixtures + WSR-07 T-10-01 mitigation) — no hardware SC (software-verifiable).
- SC-2 Latency profile p50 <400ms in docs/perf/phase-10-latency.md → ✅ PASS software (Plan 10-02 evidence: PerfProbe at 4 stations + r1.perf.sample envelope + template) — hardware: **SC-10-02**.
- SC-3 Multi-session field test + NASA-TLX → ✅ PASS software (Plan 10-04 evidence: docs/field-test-template.md NASA-TLX form ready) — hardware: **SC-10-01**.
- SC-4 Microwave / 2.4 GHz worst-case RF test → ✅ PASS software (Plan 10-01 ⚠ SYNC LOST chip + Plan 10-02 perf probe will record degrade; Specs §11.5.8.2 glyph-fallback path is from Phase 4a) — hardware: **SC-10-03**.
- SC-5 README + setup + runbook + firmware-compat + INV-1..5 verification suite green → ✅ PASS software (Plan 10-03 `inv:all` orchestrator + Plan 10-04 5 docs + INV-3 atomic commit).

(4) `## REQ-ID Coverage Matrix` — note Phase 10 lands NO new v1 REQ-IDs (cross-cutting verification phase per ROADMAP); the table cites the 48/48 v1 coverage closed across Phases 0-9 with REQUIREMENTS.md as the SoT.

(5) `## Hardware-Pending Carry-Forward (Phase 10 — 3 new SCs)` — list SC-10-01..03 with closure procedure pointing to `docs/field-test-template.md` + `docs/perf/phase-10-latency.md` + `pnpm --filter @evf/validation-harness validate:all`. Running total: previous 29 + 3 = **32 hardware-pending SCs**.

(6) `## Key invariants confirmed (Phase 10 closure)` — bullets: `registerComplexHandler` count = 14 (re-grep on closure); `activity.use(` absent from g2-app + bridge (CI Gate 8 green); INV-1..5 verification suite green (Plan 10-03 `inv:all`); INV-3 atomic doc-coherence verified (Plan 10-04 single commit per Phase 1 Plan 03 precedent 671a22d).

(7) `## MVP Software-Complete Signal` — verbatim: "Phase 10 closes the MVP software stream. All 11 MVP phases (0, 1, 2, 3, 4a, 4b, 5, 6, 7, 8, 9, 10) have shipped their software deliverables. 32 hardware-pending SCs carry to ADR-0005 PROVISIONAL Branch A `human_needed` and close via `pnpm --filter @evf/validation-harness validate:all` once the user has G2 + R1 + a consenting DM in hand. V2 (Phases 11-13) remains OPZIONALE and is out of MVP scope."

(8) `## Next steps for the user` — bullets: (a) Run `/gsd-cleanup 10` to archive Phase 10 artifacts; (b) Run a milestone audit (`/gsd milestone-audit v0.9.11` if available) or proceed directly to hardware testing; (c) Schedule the multi-session field test per `docs/field-test-template.md`; (d) V2 unlock: when ready, `/gsd-discuss-phase 11` for the foundry-mcp server.

NEVER inline code in this action — the file IS the implementation.</action>
  <verify>
    <automated>test -f .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md && grep -q "MVP software-complete\\|software-complete" .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md && grep -c "SC-10-0" .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md | awk '{exit ($1 < 3)}' && grep -q "32" .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md</automated>
  </verify>
  <done>Verification doc exists with all 8 body sections; SC-10-01..03 listed; 32 hardware-pending total cited; MVP-complete signal present.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md + ROADMAP.md + REQUIREMENTS.md closure flip</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Step A — STATE.md frontmatter: update `status: PHASE_10_CLOSED — MVP_SOFTWARE_COMPLETE — 5/5 plans committed; WS reconnect resilience + perf probe + INV-1..5 verification suite + 5 MVP docs + INV-3 atomic coherence. <N> tests pass. 14-socketlib-handler invariant upheld; CI Gate 8 green.` Update `stopped_at: Completed 10-05-PLAN.md (Phase 10 CLOSED — MVP software-complete)`. Update `last_updated` + `last_activity` to today with a Phase 10 closure narrative (mirror the Phase 9 closure activity entry style — list each Plan 10-01..10-05 with one-line summaries). Update `progress`: `total_phases: 15`, `completed_phases: 12` (Phase 0 + 1 + 2 + 3 + 4a + 4b + 5 + 6 + 7 + 8 + 9 + 10 = 12), `total_plans: <fill>`, `completed_plans: <fill>`, `percent: <fill>`.

Step B — STATE.md body: add a `## /gsd-autonomous <today> run — Phase 10 closure` section mirroring the existing Phase 9 closure section: commit-per-plan table (10-01..10-05); test totals (2036 → new); key invariants confirmed (14-socketlib-handler; CI Gate 8; INV-1..5 suite green via Plan 10-03); REQ-ID coverage (none — cross-cutting); hardware-pending carry (Phase 10 adds 3; running total **32**); MVP software-complete signal block. Close with: "Phase 10 CLOSED. **MVP software-complete.** v0.9.11 milestone audit unblocked. Recommendation: run `/gsd-cleanup 10` to archive phase artifacts, then `/gsd milestone-audit v0.9.11` (or proceed to hardware field test per `docs/field-test-template.md`)."

Step C — ROADMAP.md: flip `Phase 10: Polish & Field Test MVP` checkbox from `[ ]` to `[x]` with completion date `(software-complete; 32 hardware-pending SCs deferred to ADR-0005 Branch A human_needed; <today>)`. Update the **Phase Details > Phase 10** subsection with the actual `Plans:` list (5 plans 10-01..10-05) and per-plan one-line bullets. Update the **Progress** table at the bottom: Phase 10 row → `5/5 | Complete (software-complete; hardware tests deferred — ADR-0005 Branch A human_needed carry) | <today>`. Verify all 11 MVP phase checkboxes are now `[x]` (Phase 0 + 1 + 2 + 3 + 4a + 4b + 5 + 6 + 7 + 8 + 9 + 10) — if any are still `[ ]` (e.g. Phase 0 plan 04, Phase 1 plan 03, Phase 2/3 some plans per the ROADMAP Progress table showing "In progress"), surface this as a closure blocker via console output (DO NOT silently flip — that would be MVP-status fraud).

Step D — REQUIREMENTS.md: no REQ-ID status flips (Phase 10 has no new REQ-IDs); add a closure note at the bottom: `## MVP closure status (Phase 10 closed <today>)` — bullet `48/48 v1 REQ-IDs software-complete; 32 hardware-pending SCs across Phases 4a/4b/7/8/9/10 carry to ADR-0005 PROVISIONAL Branch A human_needed`.

NEVER inline content in this action — use Edit tool for the 3 files; preserve all unrelated content verbatim.</action>
  <verify>
    <automated>grep -q "PHASE_10_CLOSED\\|MVP_SOFTWARE_COMPLETE\\|MVP software-complete" .planning/STATE.md && grep -q "\\[x\\] \\*\\*Phase 10" .planning/ROADMAP.md && grep -q "32 hardware-pending\\|MVP closure status" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>STATE.md flipped to PHASE_10_CLOSED + MVP signal; ROADMAP.md Phase 10 checkbox [x] with completion date; REQUIREMENTS.md closure note appended; all 11 MVP phase checkboxes verified [x] in ROADMAP.md (or blocker surfaced).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Atomic closure commit + MVP-complete signal</name>
  <files>.planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>Step 1 (automated): Re-run `pnpm --filter @evf/validation-harness inv:all:skip-inv2` — confirm 4 green + 1 skipped (INV-2). If anything is red, fix forward in the SAME commit. Step 2 (automated): Re-grep the two MVP-critical invariants — `grep -c 'socketlib\\.registerComplexHandler' packages/foundry-module/src/socketlib-handlers.ts` MUST return exactly `14`; `grep -rn 'activity\\.use(' packages/g2-app/src packages/bridge/src` MUST return 0 results (CI Gate 8). Step 3 (automated): Run the full workspace test suite — `pnpm test --run 2>&1 | tail -10` — confirm no failures (≥ 2036 + new Plan 10-01/10-02/10-03 additions). Record the new total for STATE.md `progress` field if not already filled. Step 4 (human-verify gate — see <how-to-verify>): operator inspects `git status` + `git diff --stat` and confirms the 4 closure files are staged with no unrelated changes. Step 5 (automated, post-resume-signal): Commit atomically — `git add .planning/phases/10-polish-field-test-mvp/10-VERIFICATION.md .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` then `git commit -m "docs(state): close Phase 10 — MVP software-complete"` with a message body listing the 32 hardware-pending SCs + next-step recommendation. Step 6 (automated): Surface the closure recommendation message to the user (the orchestrator captures stdout): "MVP SOFTWARE-COMPLETE. Phase 10 closed. 32 hardware-pending SCs carried to ADR-0005 Branch A. Run `/gsd-cleanup 10` to archive phase artifacts, then `/gsd milestone-audit v0.9.11` if you have a milestone-audit command, or proceed directly to hardware field test per `docs/field-test-template.md`." NEVER inline implementation code in this action — git commands run via Bash tool.</action>
  <what-built>
Tasks 1+2 produced 10-VERIFICATION.md + STATE.md / ROADMAP.md / REQUIREMENTS.md updates.

This task atomically commits the closure, re-runs the final invariant grep gates, and surfaces the next-step recommendation to the user (cleanup or milestone audit).
  </what-built>
  <how-to-verify>
1. Plan 10-03 INV suite output: 4 green + 1 skipped (INV-2) or all 5 green. NO red rows.

2. `grep -c 'socketlib\\.registerComplexHandler' packages/foundry-module/src/socketlib-handlers.ts` returns exactly `14`.

3. `grep -rn 'activity\\.use(' packages/g2-app/src packages/bridge/src` returns 0 lines (CI Gate 8 green).

4. `pnpm test --run 2>&1 | tail -5` shows 0 failures.

5. `git log -1 --name-only HEAD` shows the 4 closure files in a SINGLE commit.

6. `git log --oneline | head -20` shows the Phase 10 closure commit at HEAD with sibling Plan 10-01..10-04 commits below.

7. Surface to user: "MVP SOFTWARE-COMPLETE. Phase 10 closed. 32 hardware-pending SCs carried to ADR-0005 Branch A. Run `/gsd-cleanup 10` to archive phase artifacts, then `/gsd milestone-audit v0.9.11` if you have a milestone-audit command, or proceed directly to hardware field test per `docs/field-test-template.md`."
  </how-to-verify>
  <verify>
    <automated>git log -1 --name-only HEAD 2>&1 | grep -c -E "(10-VERIFICATION\\.md|STATE\\.md|ROADMAP\\.md|REQUIREMENTS\\.md)" | awk '{exit ($1 < 4)}'</automated>
  </verify>
  <done>Single atomic commit at HEAD touching all 4 closure files; INV suite green (or 4G+1S); 14-socketlib-handler grep returns 14; CI Gate 8 returns 0; full test suite passes; MVP-complete recommendation surfaced.</done>
  <resume-signal>Type "approved" if the closure commit landed cleanly and the MVP-complete signal is correct, "defer-hardware-tests" if you want to mark the 3 hardware SCs as `human_needed` and proceed (consistent with Phase 4a/4b/8/9 closure pattern), or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closure artifacts → project narrative | STATE.md + ROADMAP.md are the single source of project status; a misleading PHASE_10_CLOSED flag would mislead the user about MVP readiness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Repudiation / Tampering | Premature PHASE_10_CLOSED flag (e.g. if Plan 10-04 INV-3 reconciliation never landed) | mitigate | Task 3 re-runs the INV suite + invariant greps BEFORE committing; checkpoint gate requires human approval; closure commit is atomic so a partial state cannot leak |
| T-10-08 | Tampering | Silently flipping ROADMAP MVP-phase checkboxes from `[ ]` to `[x]` without underlying plan closure | mitigate | Task 2 Step C explicitly surfaces incomplete-checkbox blockers via console output rather than silent flip — "MVP-status fraud" prevention |
</threat_model>

<verification>
- 10-VERIFICATION.md exists with all 8 body sections + MVP software-complete signal
- STATE.md status field flipped to PHASE_10_CLOSED + MVP signal; progress counters incremented
- ROADMAP.md Phase 10 checkbox `[x]` with `<today>` completion date; all 11 MVP phase checkboxes `[x]` (or blocker surfaced)
- REQUIREMENTS.md has MVP closure status note (48/48 v1 software-complete; 32 hardware-pending)
- INV suite (Plan 10-03) reports 4 green + 1 skipped or 5 green
- `grep -c 'socketlib.registerComplexHandler' packages/foundry-module/src/socketlib-handlers.ts` returns `14`
- `grep -rn 'activity.use(' packages/g2-app/src packages/bridge/src` returns 0 lines (CI Gate 8 green)
- `pnpm test --run` exits 0 with no regressions
- Atomic closure commit (4 files) in `git log -1 --name-only HEAD`
- User sees recommendation: `/gsd-cleanup 10` + milestone audit + hardware field test pointer
</verification>

<success_criteria>
- Phase 10 CLOSED. MVP SOFTWARE-COMPLETE. v0.9.11 milestone audit unblocked.
- 32 hardware-pending SCs carried to ADR-0005 PROVISIONAL Branch A `human_needed`
- 14-socketlib-handler invariant preserved through MVP
- CI Gate 8 (no `activity.use(` in g2-app + bridge) preserved through MVP
- INV-1..5 verification suite single-command runner shipped (Plan 10-03)
- All 11 MVP phase checkboxes in ROADMAP.md verified `[x]` (or blocker surfaced if any incomplete)
- User has a clear next-step recommendation (cleanup → milestone audit → hardware field test)
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish-field-test-mvp/10-05-SUMMARY.md` with:
- Closure commits + 10-VERIFICATION.md location
- STATE.md / ROADMAP.md / REQUIREMENTS.md diff highlights
- Final invariant grep results (14-socketlib + CI Gate 8 + INV suite)
- Final test totals (2036 → <new>)
- 32 hardware-pending SC list with closure pointer (validation-harness validate:all)
- Recommendation to user (cleanup / milestone audit / hardware field test)
</output>
