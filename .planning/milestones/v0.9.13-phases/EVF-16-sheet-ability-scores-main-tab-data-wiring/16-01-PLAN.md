---
phase: 16-sheet-ability-scores-main-tab-data-wiring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/shared-protocol/src/payloads/character.ts
  - packages/shared-protocol/src/payloads/character.test.ts
autonomous: true
requirements:
  - SHEET-05
tags:
  - schema
  - tdd
  - character-snapshot
  - dnd5e-abilities

must_haves:
  truths:
    - "CharacterSnapshotSchema parses a snapshot whose top-level `abilities` field is a 6-keyed object {str,dex,con,int,wis,cha}"
    - "Each ability sub-object validates {value, mod, save, proficient, dc} per CONTEXT.md §Area 1 (z.object — forward-compat)"
    - "`abilities` is REQUIRED (NOT .optional()) — Pitfall 3 mitigation; atomic schema-extension"
    - "All pre-existing CS-DS-*, CHAR-MR, CHAR-INV, CHAR-SPL, CS-PORT tests stay green when VALID_SNAPSHOT carries the new field"
    - "Negative `mod`/`save` (CHA 8 → −1) parses; out-of-range `value` (> 30) rejected"
    - "`proficient` is strict-boolean (z.boolean); coercion of 0|0.5|1|2 is the READER's job (Plan 16-02), NOT the schema's"
  artifacts:
    - path: "packages/shared-protocol/src/payloads/character.ts"
      provides: "AbilityScoreSchema + AbilitiesSchema + CharacterSnapshot.abilities field"
      contains: "AbilityScoreSchema = z.object"
      contains_2: "abilities: AbilitiesSchema"
      min_lines_added: 60
    - path: "packages/shared-protocol/src/payloads/character.test.ts"
      provides: "CS-AB-1..7 schema-extension test suite + VALID_SNAPSHOT abilities canonical sample"
      contains: "describe('CharacterSnapshotSchema — abilities extension (CS-AB)'"
      contains_2: "abilities: VALID_ABILITIES"
      min_tests_added: 7
  key_links:
    - from: "packages/shared-protocol/src/payloads/character.ts:228"
      to: "AbilitiesSchema sub-object"
      via: "z.strictObject inner field `abilities`"
      pattern: "abilities:\\s*AbilitiesSchema"
    - from: "packages/shared-protocol/src/payloads/character.test.ts:37"
      to: "VALID_SNAPSHOT canonical"
      via: "object spread carries abilities through all CS-* tests"
      pattern: "abilities:\\s*VALID_ABILITIES"
---

<objective>
Atomic, TDD schema extension: `CharacterSnapshotSchema` gains a top-level `abilities` field (6 sub-objects keyed `str/dex/con/int/wis/cha`, each `{value, mod, save, proficient, dc}`) and a canonical `VALID_ABILITIES` test sample is woven into `VALID_SNAPSHOT` so every pre-existing CS-* test continues to pass without modification.

Purpose: Lock the wire contract for SHEET-05 before the reader (Plan 16-02) or renderer (Plan 16-03) consume it. Pitfall 3 mitigation — there is NO `.optional()` window. The field lands REQUIRED end-to-end in the same atomic phase.

Output:
- `AbilityScoreSchema` (per-ability `z.object`, exported)
- `AbilitiesSchema` (6-key `z.strictObject`, exported)
- `CharacterSnapshotSchema.abilities` field (REQUIRED, no `.optional()`)
- `Ability`, `Abilities` TypeScript types (`z.infer`)
- 7 new CS-AB-* tests; updated `VALID_SNAPSHOT` canonical sample
- All 70+ pre-existing schema tests green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-CONTEXT.md
@.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-UI-SPEC.md
@packages/shared-protocol/src/payloads/character.ts
@packages/shared-protocol/src/payloads/character.test.ts
@packages/shared-protocol/src/index.ts

<interfaces>
<!-- Existing schema landmarks. The new `abilities` field slots BELOW `spells` and ABOVE `portrait?` to mirror death/world/inventory/spells precedent. -->

From packages/shared-protocol/src/payloads/character.ts (existing exports relevant to this plan):
```typescript
export const DeathSavesSchema = z.strictObject({ success, failure });
export const WorldStateSchema = z.object({ modernRules: z.boolean() });
export const InventoryItemSchema = z.object({ ... });
export const SpellbookSchema = z.object({ slots, spells });
export const CharacterSnapshotSchema = z.strictObject({
  actorId, name, hp, maxHp, tempHp, ac, level, conditions,
  exhaustion, death, world, inventory, spells,
  portrait?: z.object({ url }).optional(),
});
export type CharacterSnapshot = z.infer<typeof CharacterSnapshotSchema>;
```

NEW schema additions (this plan, dropped between `spells` and `portrait?`):
```typescript
// Per-ability sub-object — z.object (forward-compat: Phase 17+ may add half-prof/expertise)
export const AbilityScoreSchema = z.object({
  value: z.number().int().min(0).max(30),
  mod: z.number().int(),
  save: z.number().int(),
  proficient: z.boolean(),
  dc: z.number().int().min(0),
});
export type AbilityScore = z.infer<typeof AbilityScoreSchema>;

// 6-keyed abilities container — z.strictObject (the 6 keys are frozen)
export const AbilitiesSchema = z.strictObject({
  str: AbilityScoreSchema,
  dex: AbilityScoreSchema,
  con: AbilityScoreSchema,
  int: AbilityScoreSchema,
  wis: AbilityScoreSchema,
  cha: AbilityScoreSchema,
});
export type Abilities = z.infer<typeof AbilitiesSchema>;
```

Then `CharacterSnapshotSchema` adds the REQUIRED line:
```typescript
abilities: AbilitiesSchema,
```

Re-exports in `packages/shared-protocol/src/index.ts` (lines 38-50 area, alongside existing CharacterSnapshot exports) get three new names:
`AbilityScoreSchema`, `AbilitiesSchema`, and the matching `Ability` + `Abilities` types.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write CS-AB-1..7 failing tests (RED) — schema-extension contract before code</name>
  <files>packages/shared-protocol/src/payloads/character.test.ts</files>
  <behavior>
    - CS-AB-1 happy-path: snapshot with all 6 abilities {value, mod, save, proficient, dc} parses successfully
    - CS-AB-2 REQUIRED: snapshot WITHOUT top-level `abilities` field is REJECTED (not .optional() — Pitfall 3 mirror of CS-DS-6)
    - CS-AB-3 negative mod parses: CHA `{value: 8, mod: -1, save: -1, proficient: false, dc: 10}` accepted
    - CS-AB-4 dc range: `dc: -1` REJECTED (min 0); `dc: 23` accepted
    - CS-AB-5 proficient strict-boolean: `proficient: 1` (numeric) REJECTED — schema does NOT coerce; reader's job to coerce dnd5e 0|0.5|1|2 → boolean (per CONTEXT D-Area-2)
    - CS-AB-6 z.object forward-compat: extra sibling field on a per-ability object (e.g. `abilities.str.expertise: true`) is ACCEPTED (AbilityScoreSchema uses z.object, NOT z.strictObject)
    - CS-AB-7 type inference: `const a: Abilities = {...}` compiles AND `AbilitiesSchema.parse(a)` returns the same shape (belt-and-suspenders compile+runtime sanity)
    - Out-of-range `value`: `value: 31` REJECTED (max 30) — fold into CS-AB-4 or add as CS-AB-4b at executor discretion
  </behavior>
  <action>
    Open `packages/shared-protocol/src/payloads/character.test.ts`. Add a new const `VALID_ABILITIES: Abilities` immediately after `VALID_SNAPSHOT` declaration (around line 51), shaped per Thorin spec from 16-CONTEXT §Area 4 (STR 16/+3/+5/prof, DEX 14/+2/+2/¬prof, CON 14/+2/+5/prof, INT 18/+4/+4/¬prof, WIS 12/+1/+1/¬prof, CHA 8/−1/−1/¬prof; dc=10 for non-spellcaster baseline, 8+prof+mod for casters — use a uniform 10 for the canonical sample, plan 16-02 will compute real DCs).

    EXTEND `VALID_SNAPSHOT` with `abilities: VALID_ABILITIES` so all pre-existing CS-DS-*, CHAR-MR, CHAR-INV, CHAR-SPL, CS-PORT tests pick up the new field via spread and stay green.

    Update the import block at the top (line 21-32) to additionally pull `type Abilities`, `AbilityScoreSchema`, `AbilitiesSchema` from `./character.js` (these don't exist yet — that's the RED step).

    Append a new `describe('CharacterSnapshotSchema — abilities extension (CS-AB)', () => { ... })` block AFTER the CS-PORT describe (~line 388) covering the 7 cases above. Use `safeParse` + `expect(result.success).toBe(true|false)` mirroring CS-DS pattern. Per D-Area-2, CS-AB-5 asserts `proficient: 1` numeric is REJECTED (schema is the canonical type contract; coercion is the reader's job).

    Also update the file-header JSDoc block (~line 4-20) to add the new CS-AB-1..7 marker section per the Phase 4b precedent layout. Add a `@see` line pointing to this plan: `.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-01-PLAN.md`.

    Run `pnpm --filter @evf/shared-protocol test --run character` and verify ALL CS-AB-* tests FAIL with import or schema errors (the field + types don't exist yet). This is the RED gate — commit AS-IS before any production code.

    Pitfall avoidance:
    - Do NOT add `.optional()` anywhere (Pitfall 3 — CONTEXT.md D-Area-1 bullet 5).
    - Do NOT add a coercion shim on `proficient` (Pitfall: type-coercion responsibility split between schema and reader; CONTEXT D-Area-2 places coercion in the reader so the SCHEMA stays the canonical wire contract).
    - Do NOT use `z.coerce.boolean()` — strict `z.boolean()` only.
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/shared-protocol test --run character 2>&1 | grep -E "(CS-AB|FAIL|ReferenceError|Cannot find name)" | head -20</automated>
    Expected: 7 failing CS-AB-* tests with import/type errors referencing missing `AbilityScoreSchema`, `AbilitiesSchema`, or `Abilities` type. NO production code changes yet.
  </verify>
  <done>
    - VALID_ABILITIES constant present in test file with Thorin canonical numbers per CONTEXT §Area 4
    - VALID_SNAPSHOT carries `abilities: VALID_ABILITIES`
    - 7 new CS-AB-* tests added in dedicated describe block
    - Tests FAIL because production schema does not yet export AbilityScoreSchema / AbilitiesSchema / Abilities
    - No pre-existing test broken at compile-time (only the new tests fail; CS-DS/CHAR-MR/CHAR-INV/CHAR-SPL/CS-PORT compile and pass)
    - File header JSDoc updated with CS-AB-1..7 marker block + 16-01-PLAN @see reference
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AbilityScoreSchema + AbilitiesSchema + CharacterSnapshot.abilities (GREEN)</name>
  <files>packages/shared-protocol/src/payloads/character.ts, packages/shared-protocol/src/index.ts</files>
  <behavior>
    - All 7 CS-AB-* tests pass
    - All pre-existing schema tests pass (CS-DS-1..8, CHAR-MR-*, CHAR-INV-*, CHAR-SPL-*, CS-PORT-*)
    - `pnpm typecheck` is green across the workspace (no consumer compile errors yet — the field is REQUIRED but tests/mocks downstream don't import CharacterSnapshot directly for this plan)
    - NOTE: foundry-module and g2-app tests using bare `VALID_SNAPSHOT`-style literals WILL break; that is expected — Plans 16-02 + 16-03 close those breaks atomically per Pitfall 3. Document this in SUMMARY.md as Wave-1 → Wave-2 staged atomicity.
  </behavior>
  <action>
    Open `packages/shared-protocol/src/payloads/character.ts`. After the `Spellbook` type export (~line 201) and BEFORE the `CharacterSnapshotSchema` declaration (~line 228), insert:

    1. JSDoc block describing the new sub-objects (mirror the InventoryItemSchema docstring style; cite Specs.md §7.5.2 + dnd5e canonical `actor.system.abilities.<k>.{value, mod, save.value, proficient, dc}` per CONTEXT D-Area-2 INV-2 cross-check; cite REQ SHEET-05).

    2. `AbilityScoreSchema = z.object({...})` with the exact 5 fields per `<interfaces>` block above. Use `z.object` (NOT `z.strictObject`) for forward-compat per CONTEXT D-Area-1 — Phase 17 may add `expertise` / half-prof fields. `value`: `z.number().int().min(0).max(30)`. `mod`/`save`: `z.number().int()` (no min/max — negative allowed). `proficient`: `z.boolean()` (strict, no coercion). `dc`: `z.number().int().min(0)`.

    3. `AbilitiesSchema = z.strictObject({ str, dex, con, int, wis, cha })` — strict because the 6 keys are FROZEN by D&D 5e rules (no other ability codes exist; future additions like "Sanity" are non-canonical).

    4. `export type AbilityScore = z.infer<typeof AbilityScoreSchema>;` and `export type Abilities = z.infer<typeof AbilitiesSchema>;`.

    5. In `CharacterSnapshotSchema` (line 228 area), insert a new line BETWEEN `spells: SpellbookSchema,` (line 256) and `portrait: ...optional()` (line 265):
       ```
       /**
        * Character ability scores (Phase 16 Plan 16-01 atomic extension; REQ SHEET-05).
        * REQUIRED — atomic commit closes the .optional() drift window (Pitfall 3, Phase 4b precedent).
        * Reader (Plan 16-02) emits defensive defaults for fresh actors lacking `system.abilities`.
        * Renderer (Plan 16-03) replaces `dash` placeholders with formatted values per UI-SPEC §3.
        */
       abilities: AbilitiesSchema,
       ```

    Open `packages/shared-protocol/src/index.ts`. In the same export block that re-exports `CharacterSnapshot` + `CharacterSnapshotSchema` (around line 38-50), add:
    - `type AbilityScore,`
    - `AbilityScoreSchema,`
    - `type Abilities,`
    - `AbilitiesSchema,`

    Pitfall avoidance:
    - Do NOT use `.optional()`. The field is REQUIRED end-to-end per CONTEXT D-Area-1 bullet 5.
    - Do NOT widen `AbilitiesSchema` to `z.object` — the 6 ability keys are a closed enumeration.
    - Do NOT add a default value via `.default({...})` — Zod defaults make the field "feel" optional and break Pitfall 3.
    - Do NOT add a `.transform()` step coercing `proficient` from number to boolean — coercion is the READER's job (CONTEXT D-Area-2).
  </action>
  <verify>
    <automated>cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/shared-protocol test --run character 2>&1 | tail -30 && pnpm --filter @evf/shared-protocol typecheck</automated>
    Expected: shared-protocol test suite shows `7 passed | CS-AB-*` and ALL pre-existing CS-DS/CHAR-MR/CHAR-INV/CHAR-SPL/CS-PORT tests pass. Typecheck exits 0 for the shared-protocol package.
  </verify>
  <done>
    - AbilityScoreSchema + AbilitiesSchema + types exported from character.ts
    - CharacterSnapshotSchema.abilities is REQUIRED (no .optional())
    - index.ts re-exports the 4 new names
    - All 7 CS-AB-* tests pass
    - All pre-existing shared-protocol tests pass (no regression)
    - `pnpm --filter @evf/shared-protocol typecheck` exits 0
    - `grep -c "\.optional()" packages/shared-protocol/src/payloads/character.ts` ≤ pre-Plan baseline (Pitfall 3 grep gate; preserves existing `portrait?.optional()` only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Foundry actor.system.abilities → reader → wire payload | dnd5e prep-time computed values; schema gates structural validity before bridge cache write |
| Wire payload → consumer parse (CharacterSnapshotSchema.parse) | Strict structural gate; rejects malformed `proficient` (non-boolean), out-of-range `value` (> 30), negative `dc` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-S | Spoofing | CharacterSnapshotSchema | accept | Inbound wire payloads already pass JWT-bearer auth (Phase 3); schema is a structural validator, not an auth layer |
| T-16-01-T | Tampering | AbilityScoreSchema | mitigate | `z.object` rejects wrong types (string `value`, non-boolean `proficient`); `min(0).max(30)` clamps value; renderer never sees malformed data |
| T-16-01-R | Repudiation | n/a | accept | Read-only schema extension; no audit-relevant action |
| T-16-01-I | Info Disclosure | n/a | accept | Same scope as Phase 5 — abilities are already visible in Foundry sheet to the same authenticated player |
| T-16-01-D | DoS | AbilitiesSchema | mitigate | `z.strictObject` with 6 fixed keys + `z.number().int()` bounded fields → constant-time parse; no recursion/regex |
| T-16-01-E | Elevation | n/a | accept | Read-path only; no write surface introduced |

**Severity:** LOW — schema gate prevents structural drift; no new auth/network/file boundary; no new input from untrusted source beyond what Phase 5 already validates.
</threat_model>

<verification>
## Wave-16-01 Atomic Gates (all must be green at plan close)

- **WAVE-16-01-G1** TDD RED-then-GREEN: Task 1 commits with failing CS-AB-* tests; Task 2 commits with passing CS-AB-* tests. Two distinct commits.
  - `git log --oneline -2 -- packages/shared-protocol/` shows two commits (RED + GREEN).

- **WAVE-16-01-G2** CS-AB-1..7 all pass:
  - `cd /home/aiacos/workspace/FoundryVTT/EvenFoundryVTT && pnpm --filter @evf/shared-protocol test --run character 2>&1 | grep -cE "CS-AB-[1-7] ✓|✓ CS-AB-[1-7]" | grep -q 7`

- **WAVE-16-01-G3** No regression in pre-existing schema tests:
  - `pnpm --filter @evf/shared-protocol test --run` exits 0 AND total test count >= pre-Plan baseline + 7.

- **WAVE-16-01-G4** Pitfall 3 grep gate (no new `.optional()` window):
  - `grep -cE "abilities[^:]*\.optional\(\)" packages/shared-protocol/src/payloads/character.ts | grep -q '^0$'`

- **WAVE-16-01-G5** Index re-exports present:
  - `grep -cE "(AbilityScoreSchema|AbilitiesSchema|type Ability\\b|type Abilities)" packages/shared-protocol/src/index.ts | grep -q '^4$'` (4 names re-exported)

- **WAVE-16-01-G6** Typecheck clean (shared-protocol scope):
  - `pnpm --filter @evf/shared-protocol typecheck` exits 0.

- **WAVE-16-01-G7** Downstream consumer-test break is EXPECTED and DOCUMENTED:
  - foundry-module + g2-app tests that use CharacterSnapshot literals without an `abilities` field WILL fail in Plan 16-02's RED step — that's the atomic-extension by design. SUMMARY.md must call this out as "Wave 1 closes the schema; Wave 2 + Wave 3 atomically close the producer + consumer in the same logical phase".
  - DO NOT run `pnpm test` at the workspace root at the end of Plan 16-01 — defer that gate to Plan 16-03's close.

## Required test commands (run by executor)

```bash
pnpm --filter @evf/shared-protocol test --run         # 7 new + all CS-DS/CHAR-MR/CHAR-INV/CHAR-SPL/CS-PORT pass
pnpm --filter @evf/shared-protocol typecheck          # exits 0
pnpm --filter @evf/shared-protocol lint:ci            # Biome strict — exits 0
```
</verification>

<success_criteria>
- `AbilityScoreSchema`, `AbilitiesSchema`, `Ability`, `Abilities` exported from `@evf/shared-protocol` (4 new exports verified via grep on index.ts)
- `CharacterSnapshotSchema.abilities` is REQUIRED (no `.optional()`) — Pitfall 3 grep gate green
- CS-AB-1..7 all pass; pre-existing 60+ schema tests green (zero regression in shared-protocol scope)
- Two atomic commits in this plan: `test(16-01): RED — CS-AB-1..7 + VALID_ABILITIES` and `feat(16-01): GREEN — AbilityScoreSchema + AbilitiesSchema + abilities field`
- Closes SHEET-05 (schema half; producer half closes in Plan 16-02)
</success_criteria>

<output>
Create `.planning/phases/EVF-16-sheet-ability-scores-main-tab-data-wiring/16-01-SUMMARY.md` when done.

Include in SUMMARY:
- Two-commit log (RED + GREEN) with hashes
- CS-AB-1..7 test names + pass/fail evolution
- New exports added to index.ts
- Explicit DEVIATION NOTE if Thorin canonical numbers required adjustment for AC/level alignment in VALID_SNAPSHOT
- Forward-pointer to Plan 16-02 RED step: foundry-module mocks will need `abilities` injected to compile/parse — the producer extension closes that window
</output>
