---
milestone: v0.9.12
audited: 2026-05-17
status: passed
scores:
  requirements: 9/9
  phases: 2/2
  integration: 12/12
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 14
    items:
      - "UI-SPEC §2 column anchors drift from fixture bytes (says col 71, real col 68) — UI-REVIEW WR-UI-02"
      - "UI-SPEC §10 width-budget table drifts from fixture bytes (label=40 spec vs 52 fixture; stats=60 spec vs 54 fixture) — UI-REVIEW WR-UI-01"
      - "Locale leak in `glyph-scene.glyph-idle-z05.it.txt` row 17 (`Conditions` EN) + row 1 (`ROUND 3 · TURN 2/5` EN) — UI-REVIEW WR-UI-03 (real defect, not asserted by Z05-INV-02b)"
      - "Pre-existing workspace branch coverage 77.84% < 80% global threshold (raster-worker.ts at 0% is largest contributor — not introduced by Phase 14)"
      - "Pre-existing workspace lint warnings (255 sites: noConsole, noNonNullAssertion, useLiteralKeys, noExplicitAny, useTemplate) — not blocking CI but documented in deferred-items.md"
      - "ADR-0001 Amendment 1 + UI-SPEC §12 + STATE.md Quick Tasks row carry `commit hash TBD` prose placeholder (cosmetic; commit IS 3a0c5cf)"
  - phase: 15
    items:
      - "No Phase 15 `deferred-items.md` file shipped (Phase 14 pattern not followed) — 7 WR findings from 15-REVIEW.md tracked only via commit log"
      - "All 7 WR findings from 15-REVIEW.md (WR-01..WR-07) RESOLVED post-close via standalone commits (adeedbd, be525a9, 922e168, 4b9d02f, 97b8de5, 46fe992, 7923183); no outstanding tech debt from review pass"
      - "IN-01..IN-06 from 15-REVIEW.md remain advisory (closure-flag transition coupling, `tryPush` naming, unused `_payload` parameter, JSDoc precision, frozen-snapshot defense, empty-cache warn assertion tightening) — none block contract"
      - "REQUIREMENTS.md inconsistency: lines 20-24 list INFILL-01..05 as `[ ]` unchecked even though Traceability table line 60-64 + Milestone status line 52 declare them Resolved (doc-coherence drift; VOICE-06..09 lines 30-33 correctly `[x]`)"
      - "Code-review artifact `15-REVIEW.md` (commit 0db5a43) landed AFTER 15-VERIFICATION.md was written — 15-VERIFICATION.md does not cite review findings or carry a re-verification stamp"
---

# v0.9.12 Quick Wins — Milestone Integration Audit

## Executive Summary

Milestone v0.9.12 Quick Wins shipped on 2026-05-17 with both phases (14 + 15) software-complete and end-to-end wired. All 9 v1 REQ-IDs (INFILL-01..05 + VOICE-06..09) resolve via concrete production code, locked test fixtures, and a single INV-3 atomic doc-coherence commit each (`3a0c5cf` Phase 14 ratification, `dc161d6` Phase 15 closure). The workspace test suite is **2626 / 2626 green** (audit-time re-run; +2 over the milestone-close baseline of 2624 due to post-close WR-07 hardening + WR-02 retry coverage that landed between `dc161d6` and audit time). CI Gate 8 socketlib invariant holds at exactly **17** handlers; Phase 12 baseline preservation is enforced byte-for-byte by DGKT-04 / DGKT-06; no new hardware-pending SCs were added (35 carry from v0.9.11 under ADR-0005 Branch A unchanged).

Cross-phase wiring is **WIRED** at every traced point: Foundry module `entity-pack-reader.ts` → `/internal/delta` → `handleEntityPackEnvelope` → `EntityPackCache.set()` → `EntityPackCache.onChange` listener → `KeytermRefresher` (debounce 250ms, drain-then-restart mutex) → `DeepgramAdapter.refreshKeyterm()` invalidation signal → next `connect()` re-evaluates `keytermProvider()` → URL contains fresh `keyterm=` params. Phase 14 ↔ Phase 15 are functionally independent (no direct code coupling), but the toast carve-out (z=1.5 in LMT-DD-04 + LMT-DD-07d) preserves the only shared surface — visual notification independence remains intact for any future voice-derived toast. Integration findings are all INFO-level documentation drifts (UI-SPEC widths, REQUIREMENTS.md checkbox sync), not implementation defects.

## Per-Phase Verification Status

| Phase | Plans | Tests Added | REQ-IDs | Verification Status | Atomic Commit | Files in Commit |
|-------|-------|-------------|---------|---------------------|---------------|-----------------|
| 14 — Raster z=0.5 Idle Content Infill | 3 / 3 | +11 (Z05-FX-01..03 + Z05-INV-01a/01b/02/02b/03/04 + LMT-DD-07a/b/c/d) | INFILL-01..05 | passed 5/5 | `3a0c5cf` | 9 (ADR + Specs + README + showcase + STATE + ROADMAP + UI-SPEC + deferred + auto-format) |
| 15 — Deepgram Keyterm Prompting + Entity-Pack | 5 / 5 | +65 (SKT 5 + KM 15 + DGKT 6 + EPC 8 + DGRF 5 + KRF 8 + SAN 6 + DGEC 3 + DGFM 6 + INT 3) | VOICE-06..09 | passed 5/5 + 4/4 REQ | `dc161d6` | 7 (Specs §3.6+§5.2 + README + showcase + STATE + ROADMAP + REQUIREMENTS + 15-VERIFICATION.md) |
| **Total** | **8 / 8** | **+76** (audit-time +2 from WR-02/WR-07 fixes post-close) | **9 / 9** | **2 / 2 phases passed** | 2 atomic commits | 16 files / 16 atomic |

## Integration Findings

| # | Expected Connection | From | To | Status | Evidence | Severity |
|---|---------------------|------|-----|--------|----------|----------|
| 1 | Phase 14 fixtures consumed by tests | `packages/shared-render/src/fixtures/raster-overlay-open.{it,en}.txt` + `glyph-scene.glyph-idle-z05.it.txt` | `packages/g2-app/src/status-hud/__tests__/z05-state-machine-fixtures.test.ts` | WIRED | 11 `Z05-*` test blocks; `loadSceneFixture` reads all 3 fixtures, `matchAsciiFixture` round-trips, Z05-INV-01a..04 cross-state cell-equality loops PASS | OK |
| 2 | `LayerManager.bundle()` + `_suspendedZ05` consumed by race test | `packages/g2-app/src/engine/layer-manager.ts:80,190-272` | `packages/g2-app/src/engine/__tests__/layer-manager.test.ts` LMT-DD-07a/b/c/d | WIRED | 9 `LMT-DD-07` grep hits across 4 sibling `it()` blocks; atomicity + no-transient-state + reference round-trip + toast carve-out all pass | OK |
| 3 | `idle-infill-layer.ts` data-flow contract (Phase 4a carry) | `packages/g2-app/src/status-hud/idle-infill-layer.ts` | Z05-FX-01..03 byte-stability + Z05-INV-02/02b right-HUD invariance | WIRED | Production file exists; idle-infill-layer.test.ts (8 tests) green; visual contract locked via 3 INV-1 fixtures | OK |
| 4 | ADR-0001 Amendment 1 cross-refs | `docs/architecture/0001-layered-ui-model.md:13,111` | `Specs.md` §7.4c (line 1966) + fixtures + Z05-INV + LMT-DD-07 file-path citations | WIRED | All 3 file-path citations in ADR ratification paragraph resolve to existing files; `Specs.md` §7.4c heading verified at line 1966 | OK |
| 5 | `SPELL_KEYTERMS` static vocab → bridge merger | `@evf/shared-protocol :: SPELL_KEYTERMS` (70 frozen IT+EN tuples at spell-keyterms.ts:60) | `packages/bridge/src/server.ts:30,439` imports + passes to `buildKeytermList` | WIRED | 4 grep hits in server.ts (1 import + 3 uses); SKT-02 drift gate enforces 1:1 mapping to foundry-mcp `SPELL_LOOKUP` (post-close hardened by `7923183`) | OK |
| 6 | `EntityPackCache` snapshot → keyterm merger | `packages/bridge/src/cache/entity-pack-cache.ts` (set by `handleEntityPackEnvelope`, populated by `/internal/delta` push) | `server.ts:438-441` keytermProvider closure captures `entityCache.get()` | WIRED | 1 grep hit `entityCache.get()` inside provider closure; KeytermRefresher reads same `entityCache` ref via `cache.onChange` (server.ts:490) | OK |
| 7 | `buildKeytermList` pure merger → Deepgram URL | `packages/bridge/src/voice/keyterm-merger.ts:108 buildKeytermList()` | `server.ts:439 keytermProvider` → `deepgram-stt.ts:346 keytermProvider` → `buildDeepgramUrl` (private helper) → `encodeURIComponent` (3 sites) | WIRED | 8 `keytermProvider` grep hits in deepgram-stt.ts; 3 `encodeURIComponent`; DGKT-01/02/03 verify URL shape end-to-end; DGKT-04/06 byte-for-byte Phase 12 baseline guard | OK |
| 8 | `EntityPackCache.onChange` → `KeytermRefresher` | `entity-pack-cache.ts:136 onChange()` synchronous listener invoke after state update | `keyterm-refresher.ts:111 _listener` bound at constructor; debounced via `_pendingTimer` setTimeout DEBOUNCE_MS=250 | WIRED | EPC-SUB-01..05 verify synchronous + ordered + isolated + reference-detach listener semantics; KRF-01 verifies subscription happens exactly once at construction | OK |
| 9 | `KeytermRefresher._doRefresh()` → `DeepgramAdapter.refreshKeyterm()` | `keyterm-refresher.ts:150 _doRefresh` with try/finally mutex | `deepgram-stt.ts:174 refreshKeyterm()` interface method | WIRED | 4 `refreshKeyterm` grep hits in deepgram-stt.ts; KRF-02..05 verify debounce + coalesce + 2×debounce → 2 fires + drain-then-restart; DGRF-01..05 verify invalidation signal contract (NOT mid-stream WS reconfig); INT-01 end-to-end cache.set → debounce → next-connect URL contains new keyterm | OK |
| 10 | `/internal/delta` multiplex → both spell-pack + entity-pack handlers | `server.ts:308-327` registers `handleSpellPackEnvelope` + `handleEntityPackEnvelope` on same `onDelta` callback | `EntityPackCache` (Phase 15 consumer) + `SpellPackCache` (Phase 12 quick task) | WIRED | Single multiplex point preserves CI Gate 8 (no new socketlib handler); server.ts:316,327 grep confirms both handlers wired | OK |
| 11 | Foundry module → bridge `/internal/delta` (data origin) | `packages/foundry-module/src/readers/entity-pack-reader.ts` `registerEntityPackReader` | `packages/foundry-module/src/module.ts:201` emits via `bridgeDeltaEmitter(type, payload)` | WIRED | module.ts:196 + 201 confirm both spell + entity readers registered with the same emitter (single channel) | OK |
| 12 | CI Gate 8 socketlib handler invariant | `packages/foundry-module/src/pair/socketlib-handlers.ts` | grep `socketlib.registerComplexHandler` count | WIRED | Audit-time `grep -c` returns **17** (unchanged from Phase 13 baseline; preserved end-to-end through Phases 14 + 15) | OK |

**Integration coverage:** 12 / 12 expected connections WIRED. No orphaned exports, no broken paths.

## Requirements Coverage Map

| REQ-ID | Phase | Integration Path | Status | Verifying Tests |
|--------|-------|------------------|--------|-----------------|
| INFILL-01 | 14 | Specs.md §7.4c (line 1966) + ADR-0001 Amendment 1 RATIFIED + changelog entry (atomic `3a0c5cf`) | WIRED — Resolved | Doc-coherence assertion (grep `§7.4c` heading; ADR Amendment 1 paragraph) |
| INFILL-02 | 14 | `idle-infill-layer.ts` (Phase 4a carry) emits 3 strips → 3 fixtures (`raster-overlay-open.{it,en}.txt` rows 18-20) lock visual contract | WIRED — Resolved | Z05-FX-01..03 + visual fixture inspection rows 18-20 |
| INFILL-03 | 14 | `LayerManager.bundle()` differential demolish + `_suspendedZ05` round-trip | WIRED — Resolved | LMT-DD-07a (atomicity 1 flush) + 07b (no-transient) + 07c (reference round-trip 2 flushes) + 07d (toast carve-out) |
| INFILL-04 | 14 | `docs/architecture/0001-layered-ui-model.md` Status line 13 + Amendment 1 § + Phase 14 ratification paragraph (atomic `3a0c5cf`) | WIRED — Resolved | grep `RATIFIED` on ADR line 13; reciprocal cross-ref Specs.md §7.4c ↔ ADR Amendment 1 |
| INFILL-05 | 14 | 3 INV-1 char-precision 96×24 fixtures + 11 Z05 tests in `z05-state-machine-fixtures.test.ts` | WIRED — Resolved | Z05-FX-01..03 + Z05-INV-01a/01b/02/02b/03/04 — all green; awk row-length check confirms 96 cols × 24 rows |
| VOICE-06 | 15 | `keyterm-merger.ts buildKeytermList()` → `server.ts:439 keytermProvider` → `deepgram-stt.ts keytermProvider opt + buildDeepgramUrl + connect()` URL | WIRED — Resolved | DGKT-01..06 (6 tests) + INT-01 end-to-end |
| VOICE-07 | 15 | `SPELL_KEYTERMS` (shared-protocol, 70) ∪ `EntityPackCache` snapshot via `buildKeytermList` union/dedupe/cap | WIRED — Resolved | KM-01..12 + SKT-01..05 + INT-01 + INT-03 (production-cap saturation case) |
| VOICE-08 | 15 | `SPELL_KEYTERMS` emits both `.it` and `.en` fields; merger preserves both; encodeURIComponent handles `palla di fuoco` → `%20`, accents `è` → `%C3%A8` | WIRED — Resolved | KM-02 (locale-aware emission) + DGKT-02 (URL-encode IT spell name) |
| VOICE-09 | 15 | `EntityPackCache.onChange` → `KeytermRefresher` (debounce 250 + mutex) → `DeepgramAdapter.refreshKeyterm()` invalidation → next `connect()` lazy provider re-eval | WIRED — Resolved | EPC-SUB-01..05 + KRF-01..07 + DGRF-01..05 + INT-01 (end-to-end ≤ DEBOUNCE_MS + 1 connect latency ≪ 5 min SLA) |

**All 9 / 9 v1 REQ-IDs WIRED with explicit traceability path and verifying test count ≥ 1 per requirement.**

**Requirements with no cross-phase wiring (self-contained):** All 9 are self-contained within their owning phase by design — INFILL-01..05 live entirely in Phase 14 (raster pipeline / docs), VOICE-06..09 live entirely in Phase 15 (voice path / bridge). No Phase 14 → Phase 15 dependency expected or shipped; the two phases are intentionally orthogonal software-only quick wins. The only shared surface (z=1.5 toast carve-out under LMT-DD-04 + 07d) is verified independently and preserved unchanged for any future voice-triggered toast.

## CI Gates Verified

| Gate | Command | Expected | Observed | Status |
|------|---------|----------|----------|--------|
| Workspace test suite | `pnpm vitest run` | green | **2626 / 2626 pass** (182 test files, 11.71s — audit re-run 2026-05-17) | PASS |
| Workspace typecheck | `pnpm typecheck` | exit 0 | exit 0 | PASS |
| Workspace lint (CI) | `pnpm lint:ci` | exit 0 | exit 0 (286 warn + 41 info, all pre-existing per Phase 14 baseline) | PASS |
| **CI Gate 8 — socketlib handler count** | `grep -c "socketlib.registerComplexHandler" packages/foundry-module/src/pair/socketlib-handlers.ts` | **17** | **17** (Phase 13 baseline preserved end-to-end) | PASS |
| Phase 12 baseline (DGKT-04) | DGKT-04 + DGKT-06 byte-for-byte URL `.toBe(DEEPGRAM_URL)` when keytermProvider absent / empty | byte-for-byte match | PASS | PASS |
| INV-3 atomic doc coherence — Phase 14 | `git show --stat 3a0c5cf` | single atomic commit with ADR + Specs + README + showcase + STATE + ROADMAP | 9 files, +50 / -24 | PASS |
| INV-3 atomic doc coherence — Phase 15 | `git show --stat dc161d6` | single atomic commit with Specs §3.6+§5.2 + README + showcase + STATE + ROADMAP + REQUIREMENTS + 15-VERIFICATION.md | 7 files, +229 / -48 | PASS |
| Bridge production build (soft-fail) | `DEEPGRAM_API_KEY= pnpm --filter @evf/bridge build` | exit 0 (95.36 KB ESM bundle preserved) | PASS | PASS |
| Hardware UAT carry-forward | 35 `human_needed` from v0.9.11; 0 new in Phase 14/15 | 35 unchanged | 35 unchanged under ADR-0005 Branch A | PASS |
| Workspace test progression | 2546 (pre-milestone) → 2624 (milestone close) → 2626 (audit time) | +80 net at audit | +80 net (65 Phase 15 + 11 Phase 14 + 2 post-close WR-02/WR-07 + 2 prior quick-task balance) | PASS |

## Deferred Items by Phase

### Phase 14

From `.planning/phases/EVF-14-raster-z-0-5-idle-content-infill/deferred-items.md`:

1. **UI-REVIEW WR-UI-01** — UI-SPEC §10 width-budget table drifts from fixture bytes (label declared 40 cells, fixture A_en/A_it carries 52; stats declared 60, fixture carries 54). Reconcile §3+§10 against `idle-infill-layer.ts` constants OR re-pad fixtures. Suggested standalone Phase-14.1 quick task. *Non-blocking — spec-prose drift, not implementation defect.*
2. **UI-REVIEW WR-UI-02** — UI-SPEC §2 spacing-token table cites `║` at col 71 / `right-stop col 70` / `content-width 66 cells`; reality is col 68 / col 67 / 64 cells (verified across all 6 fixtures). Patch UI-SPEC §2 row. *Already flagged in test-file header JSDoc; correction never back-propagated to spec.*
3. **UI-REVIEW WR-UI-03 (real defect)** — `glyph-scene.glyph-idle-z05.it.txt:17` reads `Conditions` (EN) instead of `Condizioni` (IT); same fixture row 1 has `ROUND 3 · TURN 2/5` (EN) instead of `TURNO 2/5`. Z05-INV-02b only asserts A_it ↔ B_it byte-identity, not extended to C_it. Recommended: fix locale leak + extend Z05-INV-02b to triplet A_it ↔ B_it ↔ C_it.
4. **Pre-existing branch coverage gap** — 77.84% < 80% global threshold; raster-worker.ts at 0% is largest contributor. Verified pre-existing (stashed Plan 14-03 changes, identical 77.84%). Not a Phase 14 regression. Suggested standalone quick task `chore: backfill branch coverage to 80%`.
5. **Pre-existing workspace lint warnings** — 255 sites (noConsole 142 + noNonNullAssertion 108 + useLiteralKeys 40 + noExplicitAny 2 + useTemplate 1). Warnings don't fail CI but documented as deferred concern.
6. **Cosmetic `commit hash TBD` placeholders** — ADR-0001 Amendment 1 ratification paragraph + UI-SPEC §12 Approval + STATE.md Quick Tasks row. The commit IS `3a0c5cf` (resolves authoritatively via `git log --grep "phase-14"`). Optional cosmetic follow-up.

### Phase 15

Phase 15 ships **no** `deferred-items.md` file (Phase 14 pattern not followed). The 7 WR findings from `15-REVIEW.md` (commit `0db5a43`, code-review artifact landed AFTER milestone close `dc161d6`) were resolved via standalone commits in the following order:

1. **WR-01 — RESOLVED** (`adeedbd`) — Audio frames during retry-window: explicit `liveWs.close()` before reassignment closes the race window.
2. **WR-02 — RESOLVED** (`be525a9`) — `'error'` events without `'close'`: treated as close-equivalent for retry/fallback path; test coverage added (DGFM-07).
3. **WR-03 — RESOLVED** (`922e168`) — `KeytermRefresher` instantiation guarded behind `deepgramStt.isEnabled()` (server.ts:489); no spurious info-level logs when adapter disabled.
4. **WR-04 — RESOLVED** (`4b9d02f`) — Fastify `onClose` hook (server.ts:513) wired to call `_keytermRefresher.dispose()`; deterministic cleanup per `app.close()`.
5. **WR-05 — RESOLVED** (`97b8de5`) — `extractKeytermsFromRaw` helper extracted; deduplicated resolution logic between `resolveKeyterms()` and `refreshKeyterm()`.
6. **WR-06 — RESOLVED** (`46fe992`) — SAN-03 JSDoc aligned with control-char strip reality.
7. **WR-07 — RESOLVED** (`7923183`) — SKT-02 drift gate hardened against foundry-mcp source-layout restructure (path-existence sanity probe).

**Audit-time outstanding tech debt for Phase 15:**

1. **No `deferred-items.md`** — Phase 14 established the pattern; Phase 15 closure (Plan 15-05) did not emit one. The 6 IN-* findings from 15-REVIEW.md (IN-01..IN-06) remain advisory:
   - IN-01: `_emptyCacheWarned` flag transition coupling not reset on bare-array provider
   - IN-02: `tryPush` semantics mix "should continue" vs "was pushed" (rename suggestion)
   - IN-03: `keyterm-refresher.ts:111` `_payload` parameter bound but unused (1-line comment suggestion)
   - IN-04: `keyterm-sanitizer.ts` JSDoc precision on "drop < 2 chars after normalisation"
   - IN-05: Closure capture of mutable `keyterms` array in retry path (frozen-snapshot defense)
   - IN-06: INT-01 doesn't tighten the empty-cache warn count assertion (INT-02 covers it)
2. **REQUIREMENTS.md inconsistency** — Lines 20-24 (INFILL-01..05) still show `[ ]` unchecked even though Traceability table line 60-64 + Milestone status line 52 declare them Resolved; VOICE-06..09 lines 30-33 are correctly `[x]`. Doc-coherence drift; cosmetic / single-edit fix.
3. **15-VERIFICATION.md does not reference 15-REVIEW.md** — Verification report written before review pass landed; no re-verification stamp recording WR-01..07 closures. Consider adding a "Post-close review-pass closures" section.

## Recommendation

**Recommendation: SHIP**

The milestone is end-to-end wired, CI-green, and fully software-validatable under ADR-0005 Branch A. Every cross-phase connection resolves to WIRED with concrete test evidence. The 7 WR findings raised by 15-REVIEW.md were all fixed post-close with proper TDD discipline (each WR-NN commit carries a fix + lock-in test where applicable). The remaining tech debt items are documentation drifts (UI-SPEC width tables, REQUIREMENTS.md checkbox sync, missing Phase 15 deferred-items.md) and advisory IN-* observations — none of them invalidate any of the 9 REQ-IDs or break any of the verified flows.

The audit-time workspace test count (**2626 / 2626**) is **higher** than the milestone-close baseline (2624 / 2624) by exactly +2 tests, reflecting the WR-02 standalone-`error` retry/fallback coverage (DGFM-07) and WR-07 path-existence sanity probe — both legitimate hardening additions that strengthen the contract beyond the milestone-close gate.

**Next-milestone backlog seed:**
- Phase-14.1 quick task: UI-SPEC §2/§3/§10 width-budget reconciliation + State C IT locale-leak fix + Z05-INV-02b triplet extension (~20 min, single INV-3 atomic commit).
- Standalone cosmetic chore: replace `commit hash TBD` placeholders with `3a0c5cf` in ADR-0001 + UI-SPEC §12 + STATE.md.
- Standalone cosmetic chore: REQUIREMENTS.md INFILL-01..05 checkbox `[ ]` → `[x]` sync.
- Standalone quality chore: branch coverage 77.84% → 80% backfill (raster-worker.ts).
- Standalone quality chore: workspace lint warnings sweep (255 sites).
