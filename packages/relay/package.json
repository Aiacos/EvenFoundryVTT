{
  "name": "@evf/relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EVF relay — opaque WebSocket room relay (Cloudflare Worker + one Durable Object per room) between a Foundry projector tab and the G2 app (ADR-0019).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --project relay --root ../.."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260925.1",
    "vitest": "4.1.5",
    "wrangler": "4.140.0"
  }
}
