{
  "name": "@evf/foundry-module",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EVF Foundry VTT module (evenfoundryvtt) — GM-client projector for the direct G2 channel (ADR-0012), pairing window, readers, write path. Ships the g2-app build under g2/.",
  "scripts": {
    "build": "tsup",
    "build:g2": "pnpm --filter @evf/g2-app build",
    "build:all": "pnpm run build:g2 && pnpm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run --project @evf/foundry-module --root ../.."
  },
  "dependencies": {
    "@evf/shared-protocol": "workspace:*",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@types/qrcode": "1.5.5",
    "tsup": "8.5.1",
    "typescript": "5.8.3",
    "vitest": "4.1.5",
    "happy-dom": "20.9.0"
  }
}
