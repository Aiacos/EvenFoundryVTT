---
'@evf/g2-app': minor
'@evf/shared-render': minor
---

Phase 4a: G2 Engine + Raster + Status HUD — layer manager, raster pipeline (image-q + upng-js + xxhash-wasm + OffscreenCanvas Web Worker singleton), Status HUD z=1 with IT/EN/DE width budgets, glyph fallback, 9 INV-1 ASCII fixtures.
