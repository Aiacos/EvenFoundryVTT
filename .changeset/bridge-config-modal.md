---
"@evf/foundry-module": patch
---

Add a dedicated EVF — Bridge Configuration dialog that pre-loads, displays, validates and reliably persists the bridge URL + internal secret; demote the two settings to config:false (managed solely via the dialog).
