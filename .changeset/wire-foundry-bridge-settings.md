---
"@evf/foundry-module": patch
---

Wire DM-editable bridgeUrl + bridgeInternalSecret world settings so real Forge pairing pushes authenticate against the bridge's static EVF_INTERNAL_SECRET (settings preferred, bearer-entry fallback).
