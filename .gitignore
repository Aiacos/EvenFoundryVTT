# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
# g2-app Vite outDir (ADR-0012) — shipped only via the release zip, never committed
packages/foundry-module/g2/
# release-workflow staging (foundry-module-release.yml)
packages/foundry-module/release-tree/
packages/foundry-module/evenfoundryvtt.zip
*.tsbuildinfo

# Caches
.cache/
.vite/
.parcel-cache/

# Test artifacts
coverage/
.nyc_output/
.vitest-cache/

# Changesets working cache (committed: .changeset/*.md, config.json, README.md)
.changeset/__cache__/

# Husky internals (committed: .husky/{commit-msg,pre-commit})
.husky/_/

# OS junk
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*~

# Logs
*.log

# Env files (defense-in-depth — repo should never need these)
.env
.env.local
.env.*.local
*.secret.json
packages/g2-app/.sim-artifacts/
